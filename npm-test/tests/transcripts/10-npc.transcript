title: 10 — NPCs
story: regression
author: Sharpee Regression Suite
description: Verify NPC exists, can be examined, and produces autonomous behavior

---

# Go to the server room where the bot lives
# onPlayerEnters fires: bot swivels its optical sensor
> east
[OK: contains "Server Room"]
[OK: contains "maintenance bot"]

# Examine the bot
> examine bot
[OK: contains "wheeled robot"]
[OK: contains "optical sensor"]

# Wait several turns — bot has 60% speech chance per turn
# We can't assert on random behavior, but verify the action runs without error
> wait
[OK: not contains "don't understand"]

> wait
[OK: not contains "don't understand"]

> wait
[OK: not contains "don't understand"]
