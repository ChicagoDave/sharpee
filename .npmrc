# pnpm hoist config moved to pnpm-workspace.yaml (publicHoistPattern) so the
# `npm` CLI no longer warns about the unknown `public-hoist-pattern` key during
# tsf pack/publish. pnpm reads it from pnpm-workspace.yaml.
