{
  "name": "@sharpee/platform-cli-en-us",
  "version": "0.1.0",
  "description": "English CLI platform for Sharpee interactive fiction engine",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc && vite build",
    "build:tsc": "tsc",
    "build:bundle": "vite build",
    "clean": "rm -rf dist",
    "dev": "tsc --watch",
    "watch": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview"
  },
  "dependencies": {
    "@sharpee/sharpee": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vite": "^5.0.0",
    "vitest": "^1.0.0",
    "terser": "^5.0.0"
  }
}