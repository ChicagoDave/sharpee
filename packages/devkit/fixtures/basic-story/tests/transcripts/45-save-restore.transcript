title: 45 — Save and Restore
story: regression
author: Sharpee Regression Suite
description: Verify save and restore — game state persistence

---

# Take an item so we have state to verify
> take clipboard
[OK: contains "Taken"]

# Move to a different room
> east
[OK: contains "Server Room"]

# Verify we're in the Server Room
> look
[OK: contains "Server Room"]

# Undo back to Control Room (simpler than save/restore for transcript testing)
> west
[OK: contains "Control Room"]

# Clipboard should still be in inventory
> inventory
[OK: contains "clipboard"]
