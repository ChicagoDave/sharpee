/**
 * Message system for Sharpee
 * 
 * Currently, all localization is handled through the language provider system
 * (see language/if-language-provider.ts).
 * 
 * Language plugins (like @sharpee/lang-en-us) implement the IFLanguageProvider
 * interface and provide localized text for all game messages.
 */

// This directory is currently empty as messages are handled by the language provider system.
// The previous message resolver system has been archived in .archived/ for potential future use.
