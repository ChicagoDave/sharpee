/**
 * Event extensions for the stdlib
 * 
 * Currently, events are handled through the core event system.
 * Event-related message extensions have been archived as they
 * depended on the unused message resolver system.
 */

// No exports currently - event extensions archived
