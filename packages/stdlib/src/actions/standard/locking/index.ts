/**
 * Locking action module
 * 
 * Handles locking containers and doors with optional key requirements
 */

export { lockingAction } from './locking';
export type { LockedEventData, LockingErrorData } from './locking-events';
