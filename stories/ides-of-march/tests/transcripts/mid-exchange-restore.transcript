title: Restore mid-exchange (second half)
story: ides-of-march
author: Sharpee Platform
seed: 42
description: Restore the mid-exchange save through the platform engine;
  the hanging question grips the same answer and serves the same line
  as the uninterrupted run — scene state rides the save.
  Run after mid-exchange-save.transcript (it writes the save file).

---

$restore ides-mid-exchange

# The restored exchange grips the answer exactly as the live run did.
> ask shakespeare about a spy
[OK: contains "given in jest"]
