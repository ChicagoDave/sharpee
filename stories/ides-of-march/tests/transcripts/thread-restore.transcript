title: Restore mid-thread (persistence second half)
story: ides-of-march
author: Sharpee Platform
seed: 42
description: Restore the mid-thread save through the platform engine;
  the talk continues from the held place and serves the same line as
  the uninterrupted run, and the park-and-return dance still works on
  the restored state.
  Run after thread-save.transcript (it writes the save file).

---

$restore ides-mid-thread

# The restored talk serves exactly what the live run served.
> ask shakespeare about the book
[OK: contains "the weight of it"]

# The restored thread still parks and resumes.
> ask shakespeare about the weather
[OK: contains "worst on the thatch"]
[OK: contains "pays out line"]

> ask shakespeare about the book
[OK: contains "You always come back to it"]
