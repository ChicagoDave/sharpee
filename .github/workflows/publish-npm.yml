name: Publish to npm

on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Dry run (pack and validate, publish nothing)'
        type: boolean
        default: true

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write        # required for OIDC — do not remove

    steps:
      - uses: actions/checkout@v5

      - uses: pnpm/action-setup@v4
        with:
          version: 10.13.1

      - uses: actions/setup-node@v5
        with:
          node-version: '22'
          cache: 'pnpm'
          registry-url: 'https://registry.npmjs.org'

      # setup-node ships npm 10.x with Node 22; trusted publishing needs >= 11.5.1
      - name: Upgrade npm
        run: npm install -g npm@latest

      - name: Verify npm version
        run: npm --version

      - name: Install dependencies
        run: pnpm install

      # The repokit engine is not committed built; its source imports @sharpee/core
      # and @sharpee/devkit, so the `...` filter suffix builds the dependency chain
      # first on a cold checkout. Without this, ./repokit exits "engine not built".
      - name: Build repokit engine
        run: pnpm --filter '@sharpee/repokit...' run build

      # MUST be the repokit build, not `pnpm run build`:
      # `pnpm run build` is `turbo run build` and does not stamp versions,
      # which is how @sharpee/stdlib@3.6.0 shipped a stale ENGINE_VERSION.
      # --no-genai: the genai API reference lives in the IDE, not the npm publish
      # (npm-ci.md §10.5); regenerating it here would also dirty tracked files and
      # trip the git-diff guard below.
      - name: Build platform packages (stamps versions first)
        run: ./repokit build --no-genai

      - name: Fail if stamping changed a tracked file
        run: git diff --exit-code

      - name: Build npm artifacts
        run: pnpm exec tsf build --npm

      # --publish (tsf >= 1.0.2): gate only publishable packages — workspace
      # members like stories/dungeo build locally but never ship, and wildcard
      # subpath exports (platform-browser's ./styles/*) validate correctly.
      - name: Validate
        run: pnpm exec tsf validate --publish

      - name: Publish (dry run)
        if: inputs.dry_run
        run: pnpm exec tsf publish --changed --dry-run

      - name: Publish
        if: ${{ !inputs.dry_run }}
        run: pnpm exec tsf publish --changed
