# Session Summary: 2026-05-07 - main

## Goals

- Understand the request to "add system.css (Macintosh design) to the menu for platform browser."
- Identify the correct build target (platform-browser vs. zifmia).
- Plan out the integration path for system.css as a fifth theme.
- Surface the architectural question: is system.css a special case, or the catalyst for a better theming model?

**Session started**: 2026-05-06 03:13 (work completed 2026-05-07 after date rollover)

---

## Plan

No pre-existing plan file. The session was discovery-first: clarify scope, find the right codebase target, write a plan. Two plan files were produced.

---

## Status

**COMPLETE** — scope clarified, architecture understood, two plan files written. Awaiting David's answers to seven open questions in the second plan before Phase 0 starts. No code shipped.

---

## Scope Clarification — Finding the Right Target

The opening request was to "add system.css (Macintosh design) to the menu for platform browser." This required working out what "platform browser" means in practice.

The first finding: the four existing themes (DOS Classic, Modern Dark, Retro Terminal, Paper) live in `templates/browser/infocom.css` as `[data-theme]` CSS variable swaps. The menu wiring that exposes them is in `packages/platform-browser/src/managers/MenuManager.ts` and `DialogManager.ts`. The `templates/browser/index.html` Settings → Theme submenu currently lists four entries; adding a fifth means adding a `[data-theme="system6"]` block in `infocom.css` and a new menu item in `MenuManager.ts`.

The second finding: the React-based Zifmia client (`packages/zifmia/`) has its own theme system and is a sibling, not a parent, of `platform-browser`. Neither package imports the other. Zifmia's future was raised: David indicated it may be shelved unless demand materializes. The target for this work was confirmed as the non-React browser client built via `./build.sh -s dungeo -c browser`.

---

## First Plan — system.css Integration (Superseded)

With the target confirmed, I wrote a first plan at the request to "plan out option B" — actual system.css integration rather than a CSS-variable-only approximation.

**File**: `docs/work/system-css-theme/plan-20260507-system-css-integration.md`

The plan was six phases. The central design question it surfaced was the Markup Strategy: system.css relies on specific HTML structure (window chrome, titlebar, menu classes) that the current `index.html` does not provide. Three approaches were defined:

- **(a)** Restructure the DOM to match system.css markup and rewrite all legacy themes to override the system.css baseline.
- **(b)** Write a system.css wrapper that adapts system.css visual output to the current flat DOM.
- **(c)** Dual-DOM: render two HTML trees and activate one per selected theme.

The recommendation in this first plan was (a) — restructure once, rewrite legacy themes to match.

This plan was superseded before implementation began.

---

## Architectural Pivot — Component-Based Theming

David reframed the question: "can we adapt our themes to the same pattern as system.css?" This shifted the work from "add system.css as a special case" to "adopt the structural pattern that makes system.css possible, then port all themes to that pattern."

The diagnosis of the current model: today's four themes are CSS variable swaps over a fixed DOM structure. They share identical markup assumptions; only palette and font change. A structurally distinct kit like system.css cannot be expressed inside this model without either mangling the DOM or writing a wrapper that fights the kit's natural patterns.

The insight that landed: system.css forces the question, but the right answer is a component-based theming contract. Define a vocabulary of ~20 named component classes (`sharpee-window`, `sharpee-menubar`, `sharpee-prose`, etc.). The HTML declares these classes. Each theme is a full CSS file that styles them. The platform ships four ported legacy kits and one new System 6 kit. Future story-specific themes are drop-in CSS files targeting the same vocabulary.

This is consistent with the `feedback_web_client_author_customizable` direction (authors override per-story), the ADR-163 channels-as-universal-UI-surface principle, and the 2026-04-29 blog post on author-enabled client architecture.

---

## Second Plan — Component-Based Theming (Current)

**File**: `docs/work/theme-architecture/plan-20260507-component-themes.md`

### Component Vocabulary

Twenty-one named classes defined:

| Class | Represents |
|---|---|
| `.sharpee-window` | Outermost game container |
| `.sharpee-window-titlebar` | Window title bar (chrome) |
| `.sharpee-menubar` | Top menu bar |
| `.sharpee-menubar-item` | Individual menu header |
| `.sharpee-menu-dropdown` | Open dropdown panel |
| `.sharpee-menu-option` | Individual menu item |
| `.sharpee-status` | Status / score bar |
| `.sharpee-prose` | Main text output area |
| `.sharpee-input-bar` | Input row container |
| `.sharpee-prompt` | Prompt character (`>`) |
| `.sharpee-input` | Text input element |
| `.sharpee-dialog-overlay` | Modal overlay |
| `.sharpee-dialog` | Dialog box |
| `.sharpee-dialog-title` | Dialog title bar |
| `.sharpee-dialog-body` | Dialog content area |
| `.sharpee-dialog-buttons` | Dialog button row |
| `.sharpee-dialog-button` | Individual dialog button |

State modifiers (`.is-open`, `.is-active`, `.is-focused`) and decoration slots (for CRT scanlines, paper texture, etc.) are part of the contract but not fully resolved pending David's answers.

### Five Phases

**Phase 0**: Write ADR-170 (Component-Based Theming). Take baseline screenshots. Run `/adr-review 170`. No code changes until the ADR passes review.

**Phase 1**: Refactor `index.html`, `MenuManager.ts`, and `DialogManager.ts` to the new class contract. Legacy themes will look broken at this checkpoint — the JS wiring and walkthrough chain are the acceptance gate, not visual polish. One commit.

**Phase 2**: Port the four legacy themes to the new contract, one commit per theme. Goal is visual parity with Phase 0 baseline; no redesign. CSS variable swaps are replaced by full component selectors per theme.

**Phase 3**: Add System 6 as the fifth theme kit. Recommendation: write fresh CSS targeting `.sharpee-*` selectors directly rather than vendoring `system.css` verbatim, to avoid coupling Sharpee's DOM structure to an external project's assumptions.

**Phase 4**: Full smoke test and walkthrough regression (`wt-01` + `wt-02` chain). Commit.

**Phase 5 (optional, follow-up)**: Dynamic per-theme CSS loading — each theme becomes a standalone CSS file loaded at selection time rather than a block in a monolithic stylesheet. This is the architectural feature that makes story-shippable author themes viable. Sequencing relative to ADR-169 implementation is an open question.

### Seven Open Questions for David

1. **Component vocabulary completeness**: are there UI regions or surfaces not covered by the 21-class list? (Examples: scrollbars, selection highlight, visited-link coloring in prose.)
2. **Decoration slots**: should scanlines, paper texture, and similar layer-on effects be `.sharpee-window::after` or standalone overlay elements? Affects cross-kit portability.
3. **Semantic HTML**: should Phase 1 adopt `<nav>`, `<main>`, `<aside>` alongside the `.sharpee-*` classes, or keep all divs?
4. **System 6 sourcing strategy**: write fresh CSS (recommended) vs. vendor `system.css` directly vs. consume it from npm.
5. **Phase 5 sequencing**: before or after ADR-169 implementation?
6. **ADR numbering**: ADR-170 for component theming, or amend ADR-163 (channels/UI surface)?
7. **Retro Terminal / DOS Classic visual evolution**: the plan scopes Phase 2 as porting (same look), but should CRT scanlines and ASCII-art borders be added in Phase 2 or deferred?

---

## Key Decisions

### 1. Platform-browser is the target, not Zifmia

Zifmia is a sibling React client that may be shelved. All theme work targets `templates/browser/` + `packages/platform-browser/`.

### 2. Component-based theming over CSS-variable-swap extension

Adding system.css as a `[data-theme]` variable block would require either DOM restructuring for that theme alone (inconsistent model) or a wrapper that fights system.css's natural patterns. The cleaner path is a shared component-class contract that all themes, including the four legacy ones, target.

### 3. Legacy themes are ported, not redesigned

Phase 2 goal is visual parity, not visual evolution. CRT scanlines, ASCII borders, and other enhancements are explicitly out of scope until the structural migration is stable and David decides where they fit.

### 4. System 6 kit will target `.sharpee-*` selectors directly

Rather than vendoring `system.css` and adapting around its assumptions, write fresh CSS that achieves the System 6 aesthetic while targeting the Sharpee vocabulary. This avoids upstream coupling and gives full control over which system.css patterns to adopt.

### 5. The first plan is superseded but preserved

`docs/work/system-css-theme/plan-20260507-system-css-integration.md` is left in place as a reference artifact. The active plan is `docs/work/theme-architecture/plan-20260507-component-themes.md`.

---

## ADRs Produced

None this session. ADR-170 (Component-Based Theming) is planned as the Phase 0 deliverable of the new plan; it has not been written or reviewed yet.

---

## Outstanding Follow-ups

### Blocked on David's Answers (seven open questions in the new plan)

Phase 0 of `plan-20260507-component-themes.md` cannot start until the vocabulary, decoration-slot, semantic-HTML, and sourcing-strategy questions are resolved. Phase 0 starts with writing ADR-170, which requires those answers to be complete.

### Carryovers from Prior Sessions

1. **ADR-169 implementation** (primary carryover from 2026-05-05 session): rewrite `packages/platform-browser/src/audio/AudioManager.ts` to use the Web Audio graph. ADR is READY FOR IMPLEMENTATION. This work item pre-dates the theme session and is not blocked by the theme planning.

2. **`getHelpText` / `getAboutText` cleanup** in `stories/dungeo/src/browser-entry.ts`: these callbacks are passed via `BrowserClientCallbacks` but the type does not declare them and `platform-browser` does not read them — silently inert excess properties. Carryover from channel-io-event-retirement Phase 4.

---

## Files Modified

**New plan files** (2 files):

- `docs/work/system-css-theme/plan-20260507-system-css-integration.md` — superseded first plan; system.css as a sixth option-B integration
- `docs/work/theme-architecture/plan-20260507-component-themes.md` — current active plan; component-based theming, five phases, seven open questions

---

## Notes

**Session duration**: ~1.5 hours (architecture/planning only)

**Approach**: Scope clarification first (find the real build target), then incremental plan writing. The architectural pivot from "add a theme" to "fix the theming model" emerged from a single question David asked rather than from code analysis. The two plan files trace the before/after of that pivot.

**Architectural connection**: The component vocabulary approach is consistent with three prior architectural commitments — ADR-163 (channels as universal UI surface), the `feedback_web_client_author_customizable` memory item (authors override per-story), and the 2026-04-29 blog post. This session did not introduce a new direction; it found that the direction was already chosen and drew out its implication for theming.

---

## Session Metadata

- Branch: `main`
- Commits added this session: **zero** (planning only).
- Plan: two plan files produced; the second supersedes the first.

---

## Session Metadata (structured)

- **Status**: COMPLETE
- **Blocker**: N/A
- **Blocker Category**: N/A
- **Estimated Remaining**: N/A
- **Rollback Safety**: safe to revert (no code changes this session)

## Dependency/Prerequisite Check

- **Prerequisites met**: existing theme system in `templates/browser/infocom.css` and `MenuManager.ts` read and understood; sibling relationship between `platform-browser` and `zifmia` confirmed via package.json deps.
- **Prerequisites discovered**: ADR-170 must be written and pass `/adr-review` before Phase 1 coding begins. Seven vocabulary/strategy questions must be answered by David before ADR-170 can be written.

## Architectural Decisions

- [ADR-170 planned]: Component-Based Theming — define a ~21-class `.sharpee-*` vocabulary in `index.html`; each theme targets those classes rather than overriding `[data-theme]` variable blocks. Makes structurally-distinct kits (System 6, future author themes) first-class. ADR not yet written; Phase 0 of the new plan produces it.
- Pattern applied: author-customizable UI surface per ADR-163 direction and `feedback_web_client_author_customizable`.

## Mutation Audit

- Files with state-changing logic modified: none (no code changes this session).
- Tests verify actual state mutations (not just events): N/A

## Recurrence Check

- Similar to past issue? NO — this is a new planning session for a new subsystem (theming). No prior session addressed browser client theme architecture.

## Test Coverage Delta

- No test changes this session.
- Known untested areas (pre-existing): Web Audio API fades (Node runtime cannot run Web Audio; ADR-169 implementation gate is human smoke-test). Theme rendering (visual regression testing not part of the current CI setup; Phase 0 baseline screenshots are the proposed gate).

---

**Progressive update**: Session completed 2026-05-07 CST
