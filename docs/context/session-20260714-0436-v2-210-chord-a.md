# Session Summary: 2026-07-14 - v2-210-chord-a

**Goal**: Extract and sync the Chord grammar documentation with the current parser implementation ahead of "chord v1 locked" finalization.
**Status**: COMPLETE
**Outcome**: Created a standalone pure-EBNF grammar file and brought `chord-grammar.md` back in sync with `packages/chord/src/parser.ts` (Phase 2/3 zoo-surfaces drift); no code changed.

**Files modified**: docs/reference/chord.ebnf (new), docs/reference/chord-grammar.md

**Notes**: Docs-only session, nothing to build or test. `chord.ebnf` and `chord-grammar.md` are cross-linked with "keep in sync" notes in both directions. `chord-grammar.md` fixes: CP3/Z3b phrase-override production replacing the old create-line per-entity override section (author key is a single WORD; dotted `<id>.detail.2` keys are analyzer-derived, not author-facing), CP1' `define-phrase` trailing "while <condition>" gate, Z6 "remove name [when …]" statement syntax with `remove` added to the D7 when-suffix legality list, and the coverage paragraph updated to name zoo-surfaces and dated 2026-07-14. Context: David declared "chord v1 locked" for this branch — this work finalizes grammar docs before PR → merge to main → npm republish as 3.0.0.
