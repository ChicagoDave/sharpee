# Session Summary: 2026-07-29 - main (UTC)

## Goals
- Convert the low-hanging-fruit GitHub issues into a reviewed proposal and a session-sized implementation plan.
- Implement and real-path verify Phase 1 of the resulting plan (npm `pack()` JSON-shape fix, P-1 / #199).
- File follow-up issues for any defects discovered along the way rather than folding fixes into scope.

## Phase Context
- **Plan**: `docs/work/tracker-low-hanging-fruit/plan.md` — implement P-1, P-2, P-5, P-7, P-8 from `docs/proposals/tracker-low-hanging-fruit.md`; P-3/P-4/P-6 stay PROPOSED, blocked on ADR work, and are out of plan scope.
- **Phase executed**: Phase 1 — "Fix `pack()` for npm 12's keyed-object `npm pack --json` shape (P-1, #199)" (Small)
- **Tool calls used**: 138 / 100 budget (ran over — driven by the proposal survey, proposal-review, plan-review, and issue-filing work, not by the Phase 1 code change itself)
- **Phase outcome**: Ran over budget; partially completed — the code fix and its unit/real-path tests are done, but Phase 1's own exit criterion ("and installs") is unmet, so the plan already records the phase as BLOCKED rather than DONE.

## Completed

### Proposal: `docs/proposals/tracker-low-hanging-fruit.md` (new, first file in a new `docs/proposals/` directory)
- Surveyed all 36 open GitHub issues; read candidate bodies; grounded every claim against real source locations rather than trusting issue text.
- Wrote 8 items (P-1..P-8) with origin material summarized per item and exclusions recorded with reasons.
- `proposal-review` (auto-invoked per ADR-0008) ran against 302 references and returned BLOCKING on three items — see Architectural Decisions below for the findings.
- David accepted P-1, P-2, P-5, P-7, P-8; left P-3, P-4, P-6 PROPOSED pending ADR work. Proposal Status → PARTIALLY PLANNED.

### Plan: `docs/work/tracker-low-hanging-fruit/plan.md` (new), `.current-plan` pointer set
- `session-planner` wrote 5 phases covering the five ACCEPTED items and flipped them ACCEPTED → PLANNED.
- `plan-review` returned one advisory: Phase 4 cites ADR-187 R1 while `browser.ts:42` already requires devkit on the Chord path under an ADR-252 D5 carve-out ADR-187 never recorded.
- Planner corrections to the source issues: Phase 5 (P-8) is 14 files, not the 10 regions #170 claimed; dungeo has no `browser/dungeo.css`, so Phase 4 needs a fixture stylesheet to exercise its fix.

### Phase 1 implementation (P-1 / #199) — David chose "Phase 1 only, then check in"
- `packFilenameFrom(stdout, packageName)` extracted as a pure exported function in `tools/repokit/src/consumer-gen.ts`: accepts npm ≤11's array shape and npm 12's package-keyed object; throws an error naming `npm pack --json` and the package name when neither shape resolves a filename. `pack()` now calls it.
- Behavior Statement produced before tests (rule 12); 4 tests added to `consumer-gen.test.ts` (array shape, keyed-object shape, named-error throw, non-JSON stdout) — `pnpm --filter '@sharpee/repokit' test consumer-gen` → 11/11 pass.
- Real-path verified (rule 13a): `./repokit test:npm stories/dungeo --local` now completes staging and packs the full 19-package closure; the original `Cannot read properties of undefined (reading 'filename')` is gone.
- Blocked immediately after: `npm install` then fails `ETARGET` on `@sharpee/bootstrap@^4.3.0` — see Session Metadata Blocker. Stopped rather than fixed, per CLAUDE.md's no-auto-retry direction and the plan's stop-and-report exit discipline. Filed as #201 instead of folding a fix into Phase 1's scope.

### Issues filed
- **#201**: `consumer-gen.ts` packs the `@sharpee/transcript-tester` dev dependency as a bare `pack(TT)` with no closure expansion, so `@sharpee/bootstrap` (the one TT dep not already in dungeo's runtime closure) never gets vendored into staging; registry has 4.2.0, staging needs 4.3.0. Filed with a fix sketch and two care-points (double-packing risk, dependencies-vs-devDependencies placement).
- **#202**: `./repokit test:npm dungeo --local` (bare story name) fails because `lookupStory` resolves bare names only via the user-level registry `~/.sharpee/devkit`, which doesn't exist in this container; `./repokit test:npm stories/dungeo --local` (explicit path) works. Per ADR-187, repokit is the in-repo tool, so workspace-relative resolution is the fix, not the user registry lookup.

## Key Decisions

### 1. Scope narrowed to 5 of 8 proposal items
`proposal-review`'s BLOCKING findings on P-3, P-4, P-6 (stale/contradicted ADRs — see below) meant those three needed ADR amendments before they could be planned; David accepted the remaining five and left the plan to cover only those, deferring P-3/P-4/P-6 to future ADR work.

### 2. Stopped after Phase 1 rather than continuing to Phase 2
Explicit user instruction ("Phase 1 only, then check in"); honored even though Phases 2-5 are independent and could have proceeded.

### 3. No-auto-retry on the npm install failure
When `npm install` failed ETARGET after the `pack()` fix verified clean, the session stopped and filed #201 rather than attempting a fix — CLAUDE.md's MAJOR DIRECTIONS forbid build-fail-fix-rebuild loops without explicit instruction.

## Next Phase
- **Plan status**: Phase 1 is BLOCKED (already recorded as such in `plan.md` from the implementation work itself — no phase-status edit made by this summary). No phase was marked DONE this session and none was advanced to CURRENT, since Phase 1 did not complete.
- Phases 2-5 are mutually independent per the plan's own phasing note and may be started in any order without waiting on #201.
- **Recommended next step**: either fix #201 to unblock Phase 1's "and installs" exit criterion and #120's re-measurement, or pick up Phase 2 ("Widen `CheckpointData.version` and validate it at load time", P-2 / #142, Small) since it has no dependency on Phase 1.

## Open Items

### Short Term
- #201 (blocks Phase 1 completion and #120's re-measurement)
- #202 (bare story-name resolution in repokit)
- ADR-279 Phase 3 IDE packaging remains CURRENT and unfinished (notarytool SIGBUS; `package.sh` cleanup trap discarded the signed archive) — a separate front, unrelated to this plan

### Long Term
- Three ADR amendments needed to unblock P-3/P-4/P-6: ADR-041 supersession (EnhancedActionContext), ADR-231 D2a amendment (parse-time gating), ADR-178 v2-baseline amendment (helpers dependency)
- The no-parse-time-gating platform correctness finding (see Architectural Decisions) is broader than this plan's scope and may warrant its own investigation

## Files Modified

**Phase 1 fix** (2 files):
- `tools/repokit/src/consumer-gen.ts` - `packFilenameFrom()` extracted, handles both npm pack --json shapes, named error on failure
- `tools/repokit/src/consumer-gen.test.ts` - 4 new tests (array shape, keyed-object shape, named-error throw, non-JSON stdout)

**Process artifacts** (4 new):
- `docs/proposals/tracker-low-hanging-fruit.md` - the 8-item proposal (first file in new `docs/proposals/` directory)
- `docs/work/tracker-low-hanging-fruit/plan.md` - 5-phase implementation plan
- `docs/context/.current-plan` - pointer to the above plan
- `docs/context/.session-state-5c4586.json` - planner-updated session state

- `docs/context/session-20260729-1642-main.md` - this file

**Not committed**: `.claude/settings.local.json` is untracked and deliberately excluded.

**Commit**: `a83b2604` (2026-07-29) carried everything above except this summary's
post-commit amendments (the recurrence-check rewrite and this line), which land at
finalize.

## Notes

**Session duration**: ~4.5 hours (16:42 UTC start)

**Approach**: Issue survey → grounded proposal → adversarial proposal-review → user acceptance → session-planner → plan-review → single-phase implementation with real-path verification → stop-and-report on the first external blocker.

---

## Session Metadata

- **Status**: INCOMPLETE
- **Blocker**: `consumer-gen.ts` packs the `@sharpee/transcript-tester` dev dependency without expanding its own dependency closure, so `@sharpee/bootstrap` (not in dungeo's runtime closure) resolves against the npm registry (4.2.0) instead of staging (4.3.0), causing `npm install` to fail `ETARGET`. Filed as #201.
- **Blocker Category**: Build / Toolchain
- **Estimated Remaining**: ~1-2 hours for #201 + Phase 1 sign-off; ~4 more sessions for Phases 2-5 (three Small, one Medium); plus 3 separate ADR-amendment sessions to unblock P-3/P-4/P-6.
- **Rollback Safety**: safe to revert — Phase 1's changes are additive (new function + tests). Committed as `a83b2604` on `main` (six files: the fix, its tests, the proposal, the plan, `.current-plan`, and this summary), then pushed at finalize. Committing directly to `main` matched this repo's history and the prior session's choice; flagged to David at the time.

## Dependency/Prerequisite Check

- **Prerequisites met**: pre-session-audit reported a clean type check and no stale artifacts; `proposal-review` and `plan-review` both auto-ran per ADR-0008 gates before implementation began.
- **Prerequisites discovered**: mid-session, `proposal-review` surfaced that ADR-231 D2a's premise (`.where()` function constraints as "the one parse-time gating mechanism") does not hold in code — see Architectural Decisions.

## Architectural Decisions

- No new ADRs written this session. Three pre-existing ADRs found stale or contradicted by `proposal-review`, each blocking a proposal item:
  - **ADR-231 D2a** (ACCEPTED) deleted `.hasTrait()` on the premise that `.where()` function constraints remain the one parse-time gating mechanism. That mechanism does not exist in code: `entity-slot-consumer.ts:480` warns "FunctionConstraint in slot constraints not yet supported" and evaluates nothing; `:484` likewise for PropertyConstraint. **Sharpee currently has no working parse-time gating** — this session's most significant discovery, and broader than the P-4 item it blocks.
  - **ADR-041** (ACCEPTED) still defines `EnhancedActionContext`; code marks it `@deprecated` and the ADR was never amended to match — blocks P-3.
  - **ADR-178** Invariant 4 requires baseline bumps to be ADR amendments; P-6's proposed `@sharpee/helpers` addition is exactly that. Also found in passing: `tutorials/familyzoo` already imports `@sharpee/helpers`, an existing Invariant 2 violation independent of P-6.
- Pattern applied: proposal → proposal-review → plan → plan-review workflow per ADR-0008, with writer-separation honored (this summary cites P-n ids but does not touch `docs/proposals/tracker-low-hanging-fruit.md`).

## Mutation Audit

- Files with state-changing logic modified: `tools/repokit/src/consumer-gen.ts` (`packFilenameFrom()`, `pack()`).
- Tests verify actual state mutations (not just events): YES — each test asserts on the actual filename `packFilenameFrom()` returns for a given stdout shape, or on the specific error thrown, not on side effects alone.

## Recurrence Check

`pattern-recurrence-detector` ran after this summary's first draft (rule 19, Status
INCOMPLETE) across 6 summaries. The draft's "NO" was wrong at the class level — the
two specific defects are new, but two of their *classes* recur:

- **Stale build artifacts producing misleading failures — 4 of 6 sessions.**
  Independently flagged by pre-session-audit and corroborated by the summaries:
  02:00 (`devkit/dist`, "stale-dist gotcha"), 05:30 (own Recurrence Check: YES —
  stale `chord/dist` produced 25 false story-loader failures), 09:41 and 10:15
  (both F1, `dist-esm`, same workaround each time). One root cause: the build graph
  asserts artifact **existence**, never **currency** — `vendor-toolchain.sh` checks
  that `devkit/dist/cli.js` exists, not that it is newer than its source. Fix is an
  mtime-or-hash currency check at the three sites that only check existence
  (`vendor-toolchain.sh`, the story-loader/chord test entry points, `package.sh`
  step 2). Medium effort; highest-value item outstanding.
- **Hand-maintained list drifts from the real dependency graph — 2 occurrences**
  (#146 `@sharpee/helpers` absent from `STORY_RUNTIME_BASELINE`; #201 this session).
  Structurally identical: a list that must mirror the actual graph, nothing enforcing
  the mirror, drift surfacing as an install/runtime failure rather than a compile
  error. Below the 3-occurrence threshold but cheap to audit as a pair.
- **ADR-vs-code drift — 1 session, 3 simultaneous instances** (ADR-231 D2a, ADR-041,
  ADR-178, all above). Counts as one occurrence per the dedup rule, but the detector's
  point stands: `proposal-review` only checked the three ADRs the proposal happened to
  cite, and found drift in all three. Nothing has ever scanned the other ~296 ACCEPTED
  ADRs against code. A dedicated audit is Deep effort (4+ hours) and is recommended
  without waiting for a second occurrence.

Not recurring: #202 (1 occurrence), the `notarytool` SIGBUS (1, in the 05:30 session).

## Test Coverage Delta

- Tests added: 4 (`consumer-gen.test.ts`: array shape, keyed-object shape, named-error throw, non-JSON stdout)
- Tests passing before: 7 → after: 11 (`pnpm --filter '@sharpee/repokit' test consumer-gen`)
- Known untested areas: the `npm install` half of Phase 1's exit criterion remains unverified (blocked by #201); Phases 2-5 have no code yet.

---

**Progressive update**: Session completed 2026-07-29 21:18 UTC
