# Session Summary: 2026-09-03 - feat/adr-321-world-index

## Status: In Progress

## Goals
- Work through `docs/work/publish-readiness/plan.md` phase by phase, committing between phases (David, 2026-09-03 04:30 CDT: "you can work through phases and commit in between - I'm going to bed").
- Phase 1: draft the three gating ADR amendments and record the three design rulings.

## Completed
- Session start: recap presented, pre-session-audit relayed verbatim, core concepts read, profile fresh (2026-08-30), gate cleared.
- **Phase 1 DONE (drafted; acceptance is David's)**:
  - ADR-118 Amendment 1 — every binding consulted, own block first then composed traits in composition order; gated-out clauses fall through. Open question: own-block-first vs traits-first.
  - ADR-267 Amendment 1 — `only while <condition>` scoped grammar lines; refusal fall-through rejected. ADR-087 and ADR-231 amended by reference. Open question: spelling.
  - ADR-320 Amendment D2a — entity topics unscoped; validator's quiet resolution widens to the world. No open questions.
  - Rulings for #312 (actor in parse-time bases, grammar-scope-resolver), #313 (`open-inventory` trait adjective), #314 (two tool-less shapes; re-wear is the worn invariant in `moveEntity`, landed under P-10) recorded in the plan's Phase 1 outcome and folded into Phases 2, 5, 7 entry states.

- **Phase 2 DONE (commit `0e5697f4` closed Phase 1)** — all six fixes landed with tests; package suites green (world-model 1504, stdlib 1666, lang-en-us 450, chord 1125 after the EBNF re-pin and the optional-`oneWay` AST, story-loader 1039 after the daemon-roster expectation gained `chord.act-drain`); `./repokit build dungeo` clean; Dungeo chain 952 passing (17 transcripts); Secret Letter 563 cards / 957 assertions with three new pins; Fernhill 36 cards; Ides of March 39 cards; #327, #326, #331, #329, #334, #325 closed; proposal P-1/P-2/P-3/P-9/P-10/P-12 DONE; mutation-verification found nothing notable (all GREEN).
  - P-10 (#334): `WorldModel.moveEntity` clears worn/wornBy when a worn item leaves its location (the ruling-6 invariant).
  - P-12 (#325): `examined_self` gains `{slot:detail}`.
  - P-2 (#326): going's arrival report applies the initial description on the first visit (replace semantics, as the site documents — the proposal's "the standard description follows it" is not the documented behaviour; flagged for David).
  - P-1 (#327): `, one-way` wired end to end (`ExitDecl.oneWay?` → `IRExit.oneWay?` → `connectRooms(..., { oneWay })`); EBNF production + re-pin under 3.5.0; grammar-changes row; sharpee.net exits page corrected (it claimed exits were one-way as written); Secret Letter's `northeast is blocked` workaround retired and the landing's exit written `, one-way`; the `ne` pin now expects "You can't go that way."
  - P-3 (#331): `describeArrival` runs looking as the player after an outermost authorial move; act flush/drain registered for any story with a `move`; ADR-326 D5 addendum.
  - P-9 (#329): does not reproduce at HEAD — fixed incidentally 2026-09-02 by commit 07e1949d's pre-move sourcing (`const at = this.placeOf(...)` captured before the arm runs); pinned by `offstage-phrase-renders.test.ts` (offstage and remove variants).
  - Harness: `tests/helpers/boot-turns.ts` (real engine, per-turn events + `text:output` text; extends parser and language before `setStory`, bootstrap's order).

- **Phase 3 DONE (commit `10eae625` closed Phase 2)** — six analyzer/parser/binding fixes: P-4 `resolvePossessiveAsName` (#336); P-5 `when` as a `with` value stop + trailing `with` after the statement `when` (#335); P-6 the `bare` binder hint (#337); P-13 `Span.file` in inline kill keys (#324); P-14 closed as built (`analysis.phrase-in-phrase`, #286; resolving is GH #303 item 2); P-15 `canonicalDirectionWord` maps the parser's Direction constant back to the declared canonical (#285). Pins: chord `publish-readiness-phase3.test.ts` (7), lang-en-us `bare-hint.test.ts` (2), story-loader `publish-readiness-phase3.test.ts` (2, real path). Suites: chord 1132, story-loader 1042, lang-en-us 452; `./repokit build dungeo` clean; Dungeo chain 952; Secret Letter 563 cards (peering.chord back on compass canonicals; #336/#335 notes refreshed); Fernhill 36; Ides 39. Grammar-changes row. No side-effect functions touched (rule 15 does not fire).

- **Phase 4 DONE (commit `a67e4e83` closed Phase 3)** — *gone* semantics: Chord's `remove` now marks gone (offstage through the move lifecycle + `chord.gone.<ir-id>`), the world-model primitive stays terminal for TypeScript; `LoadError.storyRule` + `command.failed` `storyRule: true` render `core.story_rule_failed` ("One of the story's rules failed here:") instead of the parser's refusal; ADR-325 Z6 amendment drafted (DRAFT). Pins: `story-loader/tests/gone-semantics.test.ts` (5, real path incl. #330's exit and #345's refusal ladder, states as last set, round trip, revive-by-move, the story-rule message), `engine/tests/unit/command-failed-story-rule.test.ts` (2); `zoo-surfaces-phase3` AC-6 and `offstage-phrase-renders` updated to the amended semantics. Suites: story-loader 1047, engine 682, lang-en-us 452; tsc clean; `./repokit build dungeo` clean; Dungeo chain 952; Secret Letter 563; Fernhill 36; Ides 39. #330/#345 closed; P-7/P-8 DONE.

- **Phase 5 DONE (commit `dac5dcf5` closed Phase 4)** — #312: the parse-time `visible`/`touchable` bases include the actor (`grammar-scope-resolver.ts` `withActor`); world enumerations unchanged. #313: `taking` resolves its target at VISIBLE with `preferredScope: REACHABLE` (a default flip — a visible-but-unreachable target now gets the reach refusal or the author's, not "You can't see any such thing"; Dungeo chain checks it), `open-inventory` trait adjective composes `OpenInventoryTrait` (catalog + loader + site's placing page); `mercenaries.chord` regains `the target must be reachable`. Pins: parser-en-us bases (2), stdlib `taking-npc-inventory.test.ts` (3), story-loader `npc-inventory-scope.test.ts` (3, real path); Secret Letter branches for `take necklace` (monkey guards) and `take sword`. Suites: parser-en-us 326, stdlib 1669, chord 1132, story-loader 1050; build clean; Dungeo chain 952; Secret Letter 565 (the necklace pin lives on the `x monkey` card, before the trade; the sword pin asserts `!= player`); Fernhill 36; Ides 39. #312/#313 closed; P-16/P-18 DONE; ADR-273 D2 addendum.

- **Phase 6 DONE (commit `e4267406` closed Phase 5; P-11 carried to a new Phase 6a pending ADR-118 A1)** — P-17: #349 (1) `analysis.dialogue-only` for the partner-only predicates outside a dialogue body (Scope `dialogue` flag on the six dialogue-lowering sites); #349 (2) `leave` on the floor-turn path: `deliverSeizureBody` lifts it into `InitiativeSeizure.leaves`, the character tick phase applies `close-scene` (`exit`) — a compile gate was tried and withdrawn because `then asks` in a beat is legal and `leave` works on the reply path; #351 `matchShapeWords` reads `to|with` alternation. P-11 slice deferred (ADR-118 A1 DRAFT). **Tick-order audit complete**: the corpus (`branch-stories/`, `stories/`) has exactly one every-turn `knows` clause — `ides-of-march.story:208` (`while second-day and Will Kemp knows the-blow-up, once`) — and it is the arrival-reaction case ADR-332 D4a built and `adr-332-arrival-reaction.test.ts` pins (fires on the arrival tick); Secret Letter carries none (its W-10 prototype under `prototypes/` is not corpus). No fix, no issue. Pins: chord `dialogue-only-forms.test.ts` (4), story-loader `npc-talks-to-player.test.ts` (real path) and a GH #349 case in `adr-320-phase8.test.ts` (real seizure: speaks, scene closes on `exit`, next tick clean). Suites: chord 1136, character 599, story-loader 1052; build clean; Dungeo chain 952; Secret Letter 565; Fernhill 36; Ides 39. #349/#351 closed; P-17 DONE.

- **Phase 7 DONE (commit `0b298f01` closed Phase 6)** — #314: `take the item from the container` and `get the item from the container` added to `removing` in `packages/parser-en-us/grammar/standard-en-us.story` (the SOURCE — `./repokit grammar` regenerates `grammar.ts` and the site's grammar blocks; `./repokit manifest` regenerates `stdlib-manifest.ts`; note: `./repokit grammar --help` is not a flag, it runs the generator). Re-wear half already closed by Phase 2's worn invariant. Pins: story-loader `take-from-container.test.ts` (2, real path: both shapes; worn cap in via authorial move, out carried); Secret Letter change-outfit branch 2 (`take cap from satchel`, `i` not worn). Suites: parser-en-us 326, chord 1136; build clean; Dungeo chain 952; Secret Letter 566 (the satchel pin reaches the story's clothing refusal, not a take — the cap is a garment); Fernhill 36; Ides 39. #314 closed; P-19 DONE. Phase 8 blocked on ADR-267 A1; Phase 9 CURRENT.

- **Phase 9 DONE (commit `bd204663` closed Phase 7; Phase 8 blocked on ADR-267 A1)** — #318 the held command: the executor turns a MISSING_OBJECT/MISSING_INDIRECT parse failure into a `client.query` (new `QuerySource.CLARIFICATION`/`QueryType.CLARIFICATION`) carrying the parser's question, rendered by the client-query handler; `GameEngine.spliceHeldCommand` spends the hold on the next input (splices when the input is not a command of its own and the spliced form parses). Two parser fixes surfaced on the way: the grammar engine's token-count pre-check returned before matching the verb (so a bare `drop` classified as INVALID_SYNTAX and printed "I don't understand that." — the question never reached the player), and the failure classifier ranked by progress alone; now verb-matched failures rank first and a stop at a trailing literal after verb+object is MISSING_INDIRECT ("Put it where?"). ADR-225 amendment drafted (DRAFT, one-input expiry). Freeze declared under P-44 with the `only while` carve-out. Pins: story-loader `held-command.test.ts` (3, real path). Suites: engine 682, story-loader 1057, stdlib 1669, parser-en-us 326. Build clean; Dungeo chain 952; Secret Letter 566; Fernhill 36; Ides 39; Dungeo unit transcripts carry 4–5 pre-existing `attack troll`/`kill troll` game.message failures (Phase 10's #206). #318 closed; P-20 DONE. The Secret Letter two-input pin waits on Phase 8.

- **Phase 10 DONE (commit `8c646c36` closed Phase 9)** — #333 the parser's slot seating skips instrument-typed slots (first non-instrument slot is the direct object) and the Chord dispatch action excludes instrument slots from its primary slot and binds the instrument under its own slot name; #323 `nounPhraseFor` carries `properName`, which the Assembler reads to suppress a hinted article ("to Jack", never "the Jack"); #97 the engine registers the first entity a `blocked: true` (or failed-turn) event names as the pronoun referent; #328 the inventory list-line `items` is a Sequence whose first part is the entity's own noun phrase ("boots (worn)"); #108 four live `looking` keys got templates (+ four `override message` aliases in the chord catalog and the loader map), 21 dead `requiredMessages` declarations pruned across ten actions (their golden expectations updated), and `story-loader/tests/required-messages-sync.test.ts` enforces zero orphans from now on. #206 root cause (traced with `--exec --debug --restore wt-12 --seed 42`): on the sixth blow the thief is out of sight, and the validator's word tier resolved "thief" to the stiletto through its alias "thief knife" — the non-combatant branch then narrated the weapon. Fix in `CommandValidator.resolveEntity`: an exact-name match that is out of scope, when the only in-scope competitors are word-tier matches that carry the query head nowhere in their own name, resolves the exact entity (flagged past the post-resolution scope gate) so the ACTION refuses it by scope — "You can't see the seedy-looking thief." — a blocked command that still spends the turn, keeping wt-13's seven-blow count. Two wrong turns on the way, both caught by the chain: refusing at validation does not spend a turn (the thief never recovered, wt-13..17 collapsed — goldens reverted), and applying the rule with no in-scope competitor broke two scope tests. Pins: `stdlib/tests/unit/validation/exact-out-of-scope.test.ts` (4). Pins: story-loader `publish-readiness-phase10.test.ts` (4, real path), the audit test, stdlib `exact-out-of-scope.test.ts` (4). Suites: stdlib 1672, lang-en-us 452, engine 682, parser-en-us 328, chord 1136, story-loader green; build clean; Dungeo chain 952 (wt-13 golden re-blessed on the one line); Secret Letter 566; Fernhill 36; Ides 39. Six issues closed; P-22/P-24/P-25/P-30/P-31/P-32 DONE.
- **Phase 11 DONE (commit `34a05754` closed Phase 10)** — #338 going's arrival lists open containers'/supporters' contents through looking's shared helpers (Dungeo goldens re-blessed: 14 files, +110 "In/On … you see" lines only). #346 `answering` action + grammar (`answer/reply/respond/say the response`), `GameEngine.offerToOpenExchange` for bare answers, ANSWERING verb entry restored, tree lines moved to bare `yes`/`no`/`never mind`. #300 `saying_goodbye` action + grammar; scene closes on `exit` with the player as leaver, thread `on parting` renders; `on leaving` documented as the NPC's departure (greetings guide, stdlib reference). #241 `resolveToolRequirements`: one held declared tool is implied; single declared tool not held → `needs_tool` naming it (new templates + aliases for cutting/digging/opening). #242 (P-29) deferred on ADR-320 D2a. Pins: `story-loader/tests/publish-readiness-phase11.test.ts` (8); stdlib opening-tool/cutting cases, new `digging.test.ts` (12); registry count 40. Story pins: Fernhill crate refusal names the crowbar; Secret Letter card 69 patience line downgraded (bare answers no longer count as asks — port gap filed).
- **Phase 12 DONE (commit `1b22b301` closed Phase 11)** — #245's seven: (1) win double-print does not reproduce; (2) runtime `storyOver` gates every daemon body after an ending/death; (3) vine `phrase detail while` sentences; (4) validator head-word rule for exact-out-of-scope + the engine's scope-error builder qualified (was blank); (5) going consults the door's opening interceptor vetoes (`doorOpeningVeto`); (6) looking's `hidden_at` line; (7) Smoke `proper`, story-level fed-only follow clause. Text table: `plural` shears, reveal line through looking's `container_contents` with noun phrases (Dungeo: 3 golden lines re-blessed). Pins: `story-loader/tests/publish-readiness-phase12.test.ts` (5), stdlib exact-out-of-scope (+1), going-golden (+1), Fernhill tree 86 cards.
- **Phase 13 DONE (commit `c5a4896e` closed Phase 12)** — #196: `buildBrowser({ menu })` + `stripClientMenu`; `sharpee build/publish --no-menu` (default on, consequence stated); IDE `BuildRunner.buildArguments` always `--no-menu`; Publish tab "Include the in-page menu" checkbox → `PublishController.arguments`; ADR-290 D6 note. #195 closed as built (`playSurfaceScript` wipes storage every boot; `testRestartAlsoBootsFresh`). Tests: devkit browser-core (+2), browser-core-build (+1); IDE PublishTabTests (+1), BuildRunnerTests (+1).
- **Phase 14 DONE for P-38 (Phase 13 + 14 share the next commit)** — #320 seed fixture migrated to the ADR-327 player role; #319 `Alex.inventory` pin; `pnpm test:scripts` 11 passing; world-index Fernhill prose-count pins +3 (Phase 12's phrases). P-37 (#224) deferred to David: 163/187 type errors across every chapter checkpoint of both tutorial editions — an edition decision (options recorded in the plan). IDE: xcodebuild compiled and signed the Phase 13 changes but the test runner could not connect on the shielded screen — the two new Swift tests await David's run.
- **Phase 15 partial** — #240 `sharpee play` pipe-safe (async readline iterator; `play.test.ts`). #239 superseded by ADR-307 (tree document has no goldens; no devkit message names `--bless`) — David's ruling. #231 cloak-of-darkness: delete list posted in the plan, waiting for confirmation.
- **Phase 16 DONE (commit `98980681` closed Phases 13–15)** — #246: new site page `chord/guide/tooling/sharpee-test` (the tree document: shape, seed, cards/branches/skip, six claim families incl. the Chord-spelled `states` grammar, `auto-assertion:`, flags, exit codes), verified by a scratch story + document passing 6 cards / 16 assertions; getting-started's transcript/`--chain` section rewritten; Chord Writer page linked. #94: subject map in the plan; new guide `world/sound-music-and-images`; two sidebar entries.

## Key Decisions
- Refusal fall-through rejected for P-21: every `refuse when` is an authored refusal, so fall-through needs a "does not apply" marker, and a marker evaluated before the parse chooses the action is a scoped grammar line.
- Entity topics are subjects, not objects: scope is irrelevant to whether a topic row serves.
- Worn is an invariant of location: an item is worn only while directly in its wearer; enforced at `WorldModel.moveEntity`.
- Gone lives at the Chord level: `remove` = offstage + `chord.gone.<ir-id>`; `WorldModel.removeEntity` stays terminal for TypeScript stories (Dungeo untouched); a later `move` revives.

## Open Items
- David: accept or amend the five DRAFT amendments (ADR-118 A1, ADR-267 A1, ADR-320 D2a, ADR-325 Z6, ADR-225 one-input expiry); ADR-118 and ADR-267 carry one open question each — rule 11a asks whether to start the interview.
- The tick-order audit is CLOSED (Phase 6): one corpus clause, covered by ADR-332 D4a. Do not re-carry it.
- P-11 (#332/#350) waits in plan Phase 6a on ADR-118 A1 acceptance; P-21 (#317, Phase 8) waits on ADR-267 A1.
- #231 cloak-of-darkness: the legacy TypeScript side is a delete list (plan Phase 15 outcome) — post it, wait; #239: close as superseded by ADR-307 or design a tree-level bless.
- P-37 (#224) — David's edition decision (see plan Phase 14 outcome); the scratch type-check recipe: a tsconfig extending the tutorial's with `paths: { "@sharpee/*": ["packages/*/src/index.ts"] }`, `moduleResolution: Bundler`, no `tsf build --npm`.
- IDE tests for Phase 13 (`PublishTabTests`, `BuildRunnerTests`) compiled but did not run (runner hung before connecting — screen shielded); run the pinned `xcodebuild test` invocation at the keyboard.
- Secret Letter port gap (Phase 11): the stallkeepers' patience counter is the greetings' ask count (`asked again`/`asked many times`); Gentry's source counts approaches. With bare answers no longer counting as asks, the third-approach yell needs one more ask — David's call on the mechanic (GH issue filed).

## Files Modified
- Phase 2 platform: `packages/world-model/src/world/{WorldModel,AuthorModel}.ts`, `packages/world-model/src/world/index.ts`, `packages/stdlib/src/actions/standard/going/going.ts`, `packages/story-loader/src/{runtime,loader}.ts`, `packages/chord/src/{parser,ast,ir,analyzer}.ts`, `packages/chord/chord.ebnf`, `packages/lang-en-us/src/actions/examining.ts`
- Phase 2 tests: `packages/world-model/tests/unit/world/{move-entity-worn,connect-rooms-one-way}.test.ts`, `packages/stdlib/tests/unit/actions/going-first-visit-description.test.ts`, `packages/lang-en-us/tests/examined-self-detail.test.ts`, `packages/chord/tests/{doors,language-version}.test.ts`, `packages/story-loader/tests/{one-way-exit,authorial-move-describes,first-time-on-arrival,examine-self-detail,offstage-phrase-renders,scheduler}.test.ts`, `packages/story-loader/tests/helpers/boot-turns.ts`
- Phase 2 docs/content: `docs/architecture/chord-grammar-changes.md` (row), `docs/architecture/adrs/adr-326-adjacent-room-place-expression.md` (D5 addendum), `website/src/app/chord/guide/world/exits-and-blocked-exits/content.mdx`, `branch-stories/secret-letter/{aerial-runway,grubbers-market}.chord`, `branch-stories/secret-letter/secret-letter.tests.json`, `docs/proposals/publish-readiness-defects.md` (six items DONE)
- Phase 3 platform: `packages/chord/src/{analyzer,parser}.ts`, `packages/lang-en-us/src/parser/parse-phrase-template.ts`, `packages/story-loader/src/runtime.ts`
- Phase 3 tests: `packages/chord/tests/publish-readiness-phase3.test.ts`, `packages/lang-en-us/tests/bare-hint.test.ts`, `packages/story-loader/tests/publish-readiness-phase3.test.ts`
- Phase 3 docs/content: `docs/architecture/chord-grammar-changes.md` (row), `branch-stories/secret-letter/{peering,backdrops,wares,market-scenery}.chord`, `docs/proposals/publish-readiness-defects.md` (six items DONE)
- Phase 16: `website/src/app/chord/guide/tooling/sharpee-test/{page.tsx,content.mdx}` (new), `website/src/app/chord/guide/world/sound-music-and-images/{page.tsx,content.mdx}` (new), `website/src/app/chord/getting-started/compose-and-run/content.mdx`, `website/src/app/chord-writer/building-playing-and-testing/content.mdx`, `website/src/lib/nav.ts`
- Phase 15: `packages/devkit/src/commands/play.ts`, test `packages/devkit/src/commands/play.test.ts`
- Phase 14: `scripts/__tests__/cli-chord-seed.test.ts`, `stories/friendly-zoo/tests/transcripts/state-assertions.transcript`, `packages/world-index/tests/{cli,incomplete}.test.ts` (pins)
- Phase 13: `packages/devkit/src/standalone/{browser-core,build-browser,publish}.ts`, `tools/ide/SharpeeIDE/{Build/BuildRunner,Publish/PublishController,Publish/PublishView,AppDelegate}.swift`, `docs/architecture/adrs/adr-290-test-creation-as-an-atomic-mode.md` (D6 note); tests `packages/devkit/src/standalone/{browser-core,browser-core-build}.test.ts`, `tools/ide/SharpeeIDETests/{PublishTabTests,BuildRunnerTests}.swift`
- Phase 12: `packages/story-loader/src/runtime.ts`, `packages/stdlib/src/validation/command-validator.ts`, `packages/stdlib/src/actions/standard/{going/going,looking/looking,looking/looking-messages}.ts`, `packages/stdlib/src/actions/lifecycle/{lifecycle-engine,index}.ts`, `packages/stdlib/src/chains/opened-revealed.ts`, `packages/engine/src/{action-context-factory,prose-pipeline/handlers/revealed}.ts`, `packages/lang-en-us/src/actions/looking.ts`, alias catalogs (`looking-hidden-at`), stdlib reference row, `branch-stories/fernhill/{fernhill.story,fernhill.tests.json}`, `stories/dungeo/walkthroughs/*.golden` (3); tests `story-loader/tests/publish-readiness-phase12.test.ts`, `stdlib/tests/unit/validation/exact-out-of-scope.test.ts`, `stdlib/tests/unit/actions/going-golden.test.ts`
- Phase 11: `packages/stdlib/src/actions/standard/{going/going,looking/looking,looking/looking-data,tool-shared,cutting/cutting,digging/digging,opening/opening}.ts`, new `packages/stdlib/src/actions/standard/{answering,saying-goodbye}/`, `packages/stdlib/src/actions/{constants,lifecycle/registry,standard/index,helpers/dialogue-selector}.ts`, `packages/world-model/src/traits/{cuttable,diggable,openable}/*Behavior.ts`, `packages/engine/src/game-engine.ts`, `packages/parser-en-us/grammar/standard-en-us.story` (+ regenerated `grammar.ts`, site grammar blocks, `stdlib-manifest.ts`), `packages/lang-en-us/src/{data/verbs,actions/answering,actions/saying-goodbye,actions/cutting,actions/digging,actions/opening,actions/index}.ts`, `packages/chord/src/message-alias-catalog.ts`, `packages/story-loader/src/message-alias-map.ts`, `website/src/app/chord/{stdlib/reference,guide/characters-and-conversation/greetings}/content.mdx`, `stories/dungeo/walkthroughs/*.golden` (14), `branch-stories/{ides-of-march,secret-letter,fernhill}/*.tests.json`, `branch-stories/ides-of-march/WALKTHROUGH.txt`; tests `story-loader/tests/publish-readiness-phase11.test.ts`, `stdlib/tests/unit/actions/{opening-tool,cutting,lifecycle-registry}.test.ts`
- Phase 10: `packages/parser-en-us/src/english-parser.ts`, `packages/stdlib/src/{utils/noun-phrase,validation/command-validator}.ts`, `packages/stdlib/src/actions/standard/{inventory/inventory,dropping/dropping,going/going,looking/looking,putting/putting,inserting/inserting,removing/removing,attacking/attacking,quitting/quitting}.ts`, `packages/stdlib/src/actions/capability-dispatch.ts`, `packages/engine/src/game-engine.ts`, `packages/lang-en-us/src/actions/looking.ts`, `packages/chord/src/message-alias-catalog.ts`, `packages/story-loader/src/{runtime,message-alias-map}.ts`, `stories/dungeo/walkthroughs/wt-13-thief-fight.golden`, tests (`story-loader/tests/{publish-readiness-phase10,required-messages-sync}.test.ts`, `stdlib/tests/unit/validation/exact-out-of-scope.test.ts`, five golden expectations)
- Phase 9: `packages/engine/src/{command-executor,game-engine}.ts`, `packages/engine/src/prose-pipeline/handlers/client-query.ts`, `packages/core/src/query/types.ts`, `packages/parser-en-us/src/{english-grammar-engine,parse-failure}.ts`, `docs/architecture/adrs/adr-225-parser-meta-verb-layer.md` (amendment, DRAFT), test `story-loader/tests/held-command.test.ts`; proposal P-44 freeze line
- Phase 7: `packages/parser-en-us/grammar/standard-en-us.story` (+ regenerated `grammar.ts`, site grammar blocks, `stdlib-manifest.ts`), test `story-loader/tests/take-from-container.test.ts`, Secret Letter satchel pin
- Phase 6: `packages/chord/src/analyzer.ts` (dialogue scope + gate, shape alternation), `packages/story-loader/src/runtime.ts` (seizure `leaves`), `packages/world-model/src/capabilities/scene-runtime-binding.ts`, `packages/character/src/tick-phases.ts`, tests `chord/tests/dialogue-only-forms.test.ts`, `story-loader/tests/npc-talks-to-player.test.ts`, `story-loader/tests/adr-320-phase8.test.ts` (new case), `docs/architecture/chord-grammar-changes.md` (row)
- Phase 5: `packages/parser-en-us/src/grammar-scope-resolver.ts`, `packages/stdlib/src/actions/standard/taking/taking.ts`, `packages/chord/src/catalog.ts`, `packages/story-loader/src/loader.ts`, `docs/architecture/adrs/adr-273-grammar-scope-resolver-world-api.md` (D2 addendum), `website/…/placing-and-wearing/content.mdx`, `branch-stories/secret-letter/{mercenaries.chord,secret-letter.tests.json}`, tests `parser-en-us/tests/grammar-scope-resolver-bases.test.ts`, `stdlib/tests/unit/actions/taking-npc-inventory.test.ts`, `story-loader/tests/npc-inventory-scope.test.ts`
- Phase 4: `packages/story-loader/src/{runtime,state-keys,errors}.ts`, `packages/engine/src/command-executor.ts`, `packages/engine/src/prose-pipeline/handlers/command-failed.ts`, `packages/lang-en-us/src/language-provider.ts`, `docs/architecture/adrs/adr-325-chord-presence-and-duration.md` (Z6 amendment, DRAFT), tests `story-loader/tests/gone-semantics.test.ts`, `engine/tests/unit/command-failed-story-rule.test.ts`, `branch-stories/secret-letter/eavesdrop.chord` (note)
- `docs/architecture/adrs/adr-118-stdlib-action-interceptors.md` — Amendment 1 (DRAFT)
- `docs/architecture/adrs/adr-267-chord-grammar-pattern-constructs.md` — Amendment 1 (DRAFT)
- `docs/architecture/adrs/adr-320-conversation-and-complex-dialogue.md` — Amendment D2a (DRAFT)
- `docs/architecture/adrs/adr-087-action-centric-grammar.md` — amendment note by reference
- `docs/architecture/adrs/adr-231-player-surface-contract-rulings.md` — Amendment 2 by reference
- `docs/work/publish-readiness/plan.md` — Phase 1 outcome + DONE; Phase 2 CURRENT; Phases 2/5/7 entry states carry the rulings
- `docs/context/session-20260903-0432-feat-adr-321-world-index.md` — this file

## Notes
- Session started: 2026-09-03 04:32 CDT (session effb6f)
