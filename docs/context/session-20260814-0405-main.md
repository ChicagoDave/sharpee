# Session Summary: 2026-08-14 - main (CDT)

## Goals
- Answer where the docs archive went and audit/repair fallout from the user moving four more legacy trees into quarantine.
- Prepare an IFWiki page rewrite for sharpee.org and draft a new Chord page.
- Investigate and fix two missing website entry pages (`/chord`, `/sharpee`).

## Phase Context
- **Plan**: `docs/work/docs-consolidation/plan.md` — **Plan Status: DONE** (all five phases complete, closed in a prior session).
- **Phase executed**: None — no active plan phase was in scope. This was unplanned follow-up work triggered by the user's own directory moves and a website gap the user spotted.
- **Tool calls used**: 84 (session-state) / no budget set (phase 0).
- **Phase outcome**: N/A — not a plan phase.

## Completed

### 1. Archive disambiguation
Clarified two distinct "archive" concepts on request: `docs/context/archive/` (6 git-tracked files, re-accumulated since the 2026-08-10 corpus consolidation) vs. `/Volumes/Workspace/sharpee-corpus/` (context-history 1,509 entries, work-history 12, retrospective 33 — corpus README claims 1,533 for context-history, a 24-file gap not investigated). User then clarified they meant `docs/work/archive/` (65 entries) and `docs/unofficial/` (quarantine; `archive/` subdir 51 entries) — the docs-consolidation destinations. Confirmed `docs/archive/`, `docs/_archive/`, `docs/_archived/` no longer exist at `docs/` top level.

### 2. Repaired fallout from four tree moves (demos/, dungeon-walk/, spikes/, checkpoints/)
User moved these into `docs/unofficial/archive/`. Verified completeness (28 tracked deletions, source dirs gone, destination counts matched) and that no workspace membership, build script, or code reference touched them.

Two issues surfaced and fixed:
- **`checkpoints/` was gitignored** (`.gitignore:145`, root-anchored deliberately to spare `packages/extensions/testing/src/checkpoints/`, which is source). The move un-ignored `after-leaflet.json` (503 KB generated `$bookmark` snapshot). Flagged to the user; on explicit instruction, deleted with `rm` and the emptied directory with `rmdir`.
- **19 citation lines across 8 files** still pointed at old `spikes/` paths. Repaired via `sed` to `docs/unofficial/archive/spikes/...`, following the Phase 2 consolidation precedent (commit 3bb7af37 repaired citations in the same commit as its move). Files: `docs/architecture/adrs/adr-163-channel-service-platform.md`, `adr-164-stateless-multiuser-server.md`, `adr-165-renderer-architecture.md`, `adr-175-zifmia-multiuser-product.md`, `adr-176-multi-user-component-vocabulary.md`, `docs/work/zifmia/plan-20260511-adr-175.md`, `docs/work/channel-io-unification/triple-review-20260501.md`, `docs/work/devarch/multi-adr-review-skill-proposal.md`. All 5 distinct repaired paths confirmed to resolve on disk. Two deliberate non-edits: a generic (non-citation) `spikes/...` mention in `multi-adr-review-skill-proposal.md:146`, and generated retrospective digests under `docs/work/history-retrospective/output/` that quote past sessions verbatim.

### 3. IFWiki page rewrite prepared (docs/work/ifwiki/, 4 files, not yet published to the wiki)
Live page at ifwiki.org/Sharpee could not be fetched — Cloudflare managed challenge 403s both WebFetch and curl (recorded so a future session doesn't retry). Wrote: `sharpee-page-current.md` (captured wikitext "before" snapshot), `sharpee-page.md` (nine-section update guide with verdict/reason/replacement per section), `sharpee-page-final.md` (assembled paste-ready page), `chord-page.md` (new Chord page draft with verification notes).

Errors found on the live page, each verified against repo source:
- Distribution section describes shipping `.sharpee` bundles (deprecated, CLAUDE.md:144) to the Zifmia runner (retired 2026-08-13) as a Tauri desktop app (dropped, ADR-180).
- Zifmia bullet describes a retired component as current, and as "built on React" when the web UI is deliberately framework-free (ADR-170).
- Action count wrong: 43 → 57 (`packages/stdlib/src/actions/standard/index.ts:161` — 45 world + 11 meta + 1 internal).
- Dungeo room count wrong: 191 → 172 (`stories/dungeo/README.md:9-10` — 172 rooms/15 regions, 616 main + 100 endgame points).
- Infobox `System=` conflates runtime with editor (lists Windows/macOS/Linux next to a Chord Writer download link; Chord Writer is macOS-only per `tools/ide/project.yml`).
- Missing entirely: Chord, Chord Writer, channels, extensions.
- One malformed heading (sentence wrapped in `==` under a single-`=` heading).

Left for the user's decision rather than guessed: infobox `Date`, Chord Writer's macOS minimum (`project.yml` carries both `macOS "26.0"` and `MACOSX_DEPLOYMENT_TARGET "11.0"` — contradictory), and the Forge roadmap entry (unverifiable — `docs/work/forge/` no longer exists).

Self-correction during drafting: the Chord page first listed `Version=5.0.0` in the infobox — that's the compiler package version. The language version is 3.0.0 (`packages/chord/src/version.ts:181`, `CHORD_LANGUAGE_VERSION`; ADR-257 pins the two separately, and `website/src/lib/nav.ts` states they move independently). Corrected and the distinction documented in the file.

### 4. Website: built `/chord` and `/sharpee` entry pages
User asked why the site never had "What is Sharpee?"/"What is Chord?" pages. Investigated a suspected removal first and found none — no `website/` deletions in 7 days, no `website/src/app/sharpee` ever existing, the consolidation touched exactly one website file (`website/public/chord.ebnf`, a staleness fix). The real finding: `sharpee.net/chord` and `sharpee.net/sharpee` both 404'd live (verified with curl) even though every route *under* them existed (154 routes on disk, all but `/` and `/style-guide` reachable from nav).

Built:
- `website/src/app/chord/page.tsx` + `content.mdx` — "What is Chord?" (modeling-language framing, Foyer Bar example from cloak.story, the no-functions/no-arithmetic/no-control-flow constraint and its payoff, where-next table).
- `website/src/app/sharpee/page.tsx` + `content.mdx` — "What is Sharpee?" (one engine/two surfaces, layer table, two unusual properties — engine never emits English, stories emit on channels rather than printing — four-phase actions, MIT/npm/GitHub).
- `website/src/lib/nav.ts` — both wired as the first item of their section's first group (section-level `items` render after groups, which would otherwise put an intro page last).

Verification (re-run by this session at 2026-08-14T09:06 UTC, after the last edit to these files at 08:56:55Z): `cd website && npx tsc --noEmit` → exit 0. `npx next build` → "✓ Compiled successfully in 2.6s", "Generating static pages using 17 workers (159/159)", `/chord` and `/sharpee` both listed `○ (Static)` in the route table. Not deployed — local build only.

Two things deliberately left unchanged at the time: homepage cards still pointed at `/chord/getting-started/install` and `/sharpee/platform` (the Chord-first CTA is an explicit ADR-232 decision, so it was raised rather than changed), and the nav's differing version labels (Chord 3.0.0 / Sharpee 5.0.0) are correct as-is.

### 4a. Homepage cards repointed at the new entry pages (post-commit round)
The user made the call that was flagged above, so `website/src/app/page.tsx` now sends both cards to the intro pages: "Chord — the language" → `/chord` (was `/chord/getting-started/install`), "Sharpee — the platform" → `/sharpee` (was `/sharpee/platform`). The Chord card's closing line was rewritten with it — it read "Install the CLI and scaffold a playable story in one command," which described the old destination, and now reads "Start here for what the language is and how it reads." The Sharpee card's copy already fit its new target unchanged.

The "Get started" CTA button was left on `/chord/getting-started/install`: the instruction named the cards, and that button is the do-something path rather than a read-about-it one. Raised with the user rather than assumed.

`website/AGENTS.md` warns that this Next.js is not the one in training data and requires reading `node_modules/next/dist/docs/` before writing code. Checked before editing: Next 16.2.11, and `href` on `next/link` is unchanged with no relevant deprecation (`01-app/03-api-reference/02-components/link.md`).

Verification: `npx tsc --noEmit` → exit 0; `npx next build` → "✓ Compiled successfully in 2.8s". Still not deployed.

### 5. Flagged, not fixed: `docs/reference/` quarantined but still cited as normative
Surfaced while writing the Chord page. `docs/reference/` no longer exists — Phase 1 of the consolidation (commit 3bb7af37) `git mv`'d its 9 files into `docs/unofficial/reference/`. But 144 citation lines across 60 live files still point at `docs/reference/...`, including `packages/chord/chord.ebnf:7,21,23`, which names `docs/reference/chord-grammar.md` as the normative source for its layout table. CLAUDE.md says `docs/unofficial/` is junk mail that must not be cited or treated as current — the two cannot both hold. That document also describes Chord 1.4.0 against a current language version of 3.0.0. Same class of problem as the spikes repair, roughly 8x the size, and carrying a design question (fix citations vs. un-quarantine the file) rather than a mechanical path swap — flagged in `docs/work/ifwiki/chord-page.md` rather than fixed.

## Key Decisions

### 1. Repair citations to match precedent, don't invent a new pattern
The spikes-path repair followed the exact precedent Phase 2 of docs-consolidation set (citation repair in the same commit as the move) rather than deferring it — keeps the "moved ≠ broken" invariant intact for the 8 files touched.

### 2. Flag `docs/reference/` rather than fix it
144 citations across 60 files is large enough, and the underlying question (repoint vs. un-quarantine `chord-grammar.md`) is a judgment call CLAUDE.md reserves to the user for `docs/unofficial/` — not something to resolve unilaterally mid-session.

## Next Phase
Plan complete — all phases done. No active plan; this was unplanned follow-up. No next phase to record.

## Open Items

### Short Term
- `docs/reference/` conflict: 144 citation lines across 60 files (incl. `packages/chord/chord.ebnf`) point at a directory CLAUDE.md classifies as junk mail; `chord.ebnf` calls one of those files normative. Needs a user decision: repoint citations to `docs/unofficial/reference/`, or promote `chord-grammar.md` back out of quarantine (it also needs a 1.4.0 → 3.0.0 content update either way).
- Three IFWiki decisions awaiting the user: infobox `Date`, Chord Writer's macOS minimum (`project.yml` contradicts itself: `macOS "26.0"` vs. `MACOSX_DEPLOYMENT_TARGET "11.0"`), and the Forge roadmap entry (unverifiable — `docs/work/forge/` no longer exists).
- Corpus README discrepancy: claims 1,533 files for context-history; directory holds 1,509. Not investigated this session.
- None of this session's changes are staged or committed.

### Long Term
- The "proxy signal standing in for the real property" pattern (see Recurrence Check) has 7 known instances now; still no systemic audit scheduled.

## Files Modified

**Docs — citation repairs** (8 files):
- `docs/architecture/adrs/adr-163-channel-service-platform.md` - spikes/ path repair
- `docs/architecture/adrs/adr-164-stateless-multiuser-server.md` - spikes/ path repair
- `docs/architecture/adrs/adr-165-renderer-architecture.md` - spikes/ path repair
- `docs/architecture/adrs/adr-175-zifmia-multiuser-product.md` - spikes/ path repair
- `docs/architecture/adrs/adr-176-multi-user-component-vocabulary.md` - spikes/ path repair
- `docs/work/zifmia/plan-20260511-adr-175.md` - spikes/ path repair
- `docs/work/channel-io-unification/triple-review-20260501.md` - spikes/ path repair
- `docs/work/devarch/multi-adr-review-skill-proposal.md` - spikes/ path repair

**Tree moves into quarantine** (via Bash, not tracked in session-state's Edit/Write list):
- `demos/` → `docs/unofficial/archive/demos/` (4 files)
- `dungeon-walk/` → `docs/unofficial/archive/dungeon-walk/` (19 files, incl. two ~800KB zips and an 8.6MB sqlite db)
- `spikes/` → `docs/unofficial/archive/spikes/` (4 files)
- `checkpoints/after-leaflet.json` (503KB, gitignored, un-ignored by the move) — deleted by `rm` on user instruction; empty dir removed

**IFWiki drafts, new** (4 files):
- `docs/work/ifwiki/sharpee-page-current.md`
- `docs/work/ifwiki/sharpee-page.md`
- `docs/work/ifwiki/sharpee-page-final.md`
- `docs/work/ifwiki/chord-page.md`

**Website — new entry pages** (4 files) + nav (1 file):
- `website/src/app/chord/page.tsx`
- `website/src/app/chord/content.mdx`
- `website/src/app/sharpee/page.tsx`
- `website/src/app/sharpee/content.mdx`
- `website/src/lib/nav.ts` - wired both pages as first item of their section's first nav group

**Website — post-commit round** (1 file):
- `website/src/app/page.tsx` - both homepage cards repointed at `/chord` and `/sharpee`; Chord card's closing line rewritten to match the new destination

## Notes

**Session duration**: ~1 hour (08:00–09:06 UTC).

**Approach**: Reactive/investigative — three of five work items originated from user questions or observations ("where did the archive go", "what happened to these pages") rather than a pre-set plan; each was chased to a verified root cause before any file changed.

---

## Session Metadata

- **Status**: COMPLETE
- **Blocker** (if any): N/A
- **Blocker Category**: N/A
- **Estimated Remaining**: N/A
- **Rollback Safety**: safe to revert — nothing this session is staged or committed; `git status` shows the tree moves as working-tree deletions/untracked additions and the new website/docs files as untracked.

## Dependency/Prerequisite Check

- **Prerequisites met**: docs-consolidation plan's move/citation-repair precedent (Phase 2, commit 3bb7af37) was available to follow for the spikes repair.
- **Prerequisites discovered**: ifwiki.org's Cloudflare managed challenge blocks both WebFetch and curl — no prerequisite existed to work around it this session; recorded so a future session doesn't retry the same fetch.

## Architectural Decisions

- None this session. ADR-232 (Chord-first homepage CTA) and ADR-257 (Chord package vs. language versioning) were read and respected, not authored or amended.

## Mutation Audit

- Files with state-changing logic modified: none. All changes this session are documentation, archival file moves, citation text repairs, and two React Server Components (`website/src/app/chord/page.tsx`, `website/src/app/sharpee/page.tsx`) that render static MDX content with no client state, no mutations, and no side effects.
- Tests verify actual state mutations (not just events): N/A — no side-effect code was written.
- If NO: N/A

## Recurrence Check

- Similar to past issue? YES — the pre-session-audit at session start relayed a recurring "proxy signal standing in for the real property" pattern with 6 prior instances (not yet systemically audited). The `docs/reference/` finding (Item 5: 144 citations across 60 files treating a quarantined directory as normative, `chord.ebnf` calling it out by name) is a 7th instance of the same class — a reference reads as verified/current while pointing at something moved or quarantined.
- If YES: Consider the systemic audit the pre-session-audit already flagged; this session adds one more concrete, sizable instance to the case for it.

## Test Coverage Delta

- Tests added: 0 — no test suite exists for `website/`; no platform test files were touched.
- Tests passing before: N/A → after: N/A. Verification instead: `npx tsc --noEmit` (exit 0) and `npx next build` ("✓ Compiled successfully in 2.6s", 159/159 static pages, `/chord` and `/sharpee` both listed `○ (Static)`) — both re-run by this writer at 2026-08-14T09:06 UTC, after the 08:56:55Z last edit to the affected files.
- Known untested areas: the two new pages have no automated test coverage beyond type-check + build; content accuracy (e.g., the Foyer Bar example matching `cloak.story`) was checked by inspection, not by an assertion.

---

**Progressive update**: Session completed 2026-08-14 09:07 UTC
