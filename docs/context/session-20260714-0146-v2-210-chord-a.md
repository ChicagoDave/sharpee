# Session Summary: 2026-07-14 (01:46) - v2-210-chord-a

## Goals
- Implement `chord-212-213-seams` Phase 1 (ADR-212 engine slot-entry registry) on David's explicit go-ahead. [DONE — all 3 phases, finalized as 1ba048d4]
- Then: `chord-zoo-surfaces` Phase 1 (Z5 adverbs, Z4 here deictic, Z1 first-time descriptions) on David's go-ahead; `.current-plan` repointed to chord-zoo-surfaces.

## chord-zoo-surfaces Phase 1 (post-finalize, this session) — DONE, uncommitted
- **Z5**: STRATEGIES → {randomly, cycling, stopping, sticky, first-time} (parser.ts); retired `ordered`/`once` are AC-13 fix-it load errors (`parse.phrase-strategy-retired`) at BOTH strategy sites — `define phrase` AND `select` (`parse.select-strategy-retired`; the ratchet's "applies everywhere strategies are legal" — a scope addition vs. the plan's deliverable list, which missed the shared set); IRPhrase.strategy union updated; STRATEGY_SELECTOR rewritten to the five-entry Z5 table; decideStrategy rewritten (stopping/first-time renames, new sticky arm: stored=index+1 encoding under the chord occurrence key); D5 `, once` rule modifier untouched + regression-tested; chord-grammar.md updated (STRATEGY EBNF + notes).
- **Z4**: `is-here` predicate through the condition choke point — AST node, parser branch (before the bare-value fallback), analyzer (canonical key, it-visitor, IR resolution with `analysis.here-subject` on literal/symbol/story subjects), IR pred union, evaluator case (playerPresentAt semantics inline: room subject = player inside; no-location = false; honors negation). Fragment-gate context deferred to Phase 2 per plan.
- **Z1**: `first time` create-block branch (prose body; `parse.create-ordinal-time` for other ordinals, `parse.first-time-duplicate`, `parse.first-time-empty`), CreateDecl.initialDescription, `<id>.initial-description` phrase registration, IREntity.initialDescriptionKey, `analysis.first-time-non-room`, loader binds via room builder `.initialDescription()` → RoomTrait.initialDescription. Render split (first/later look) is pre-existing platform behavior pinned by stdlib looking-golden tests.
- **Tests**: chord `tests/zoo-surfaces-phase1.test.ts` (19) + story-loader `tests/zoo-surfaces-phase1.test.ts` (8); `select ordered` test migrated to `select stopping`; golden snapshots regenerated (+91 lines, all additive `initialDescription: null`/`initialDescriptionKey: null`).
- **Evidence**: chord 197/197; story-loader 111/111; workspace tsc clean.
- Grammar gotchas learned: `on` clauses need `end on`; `or` variant separators sit at indent 0 in `define phrase` blocks.

## chord-zoo-surfaces Phase 2 (this session) — DONE, uncommitted
- **CP1' grammar**: trailing `while <condition>` on the `define phrase` header (parser, after the strategy/verbatim branch, reusing `parseCondition`); `DefinePhrase.condition` AST field; `IRPhrase.condition?` (additive, `verbatim?` precedent); resolved in the analyzer's PASS 2 (entities all known — forward references work), patching the pass-1-registered phrase entry.
- **Marker validation (analyzer, new `checkDescriptionMarkers` pass)**: over room `description`/`initial-description` phrase texts, `{key}`→declared-phrase sites get: `analysis.fragment-not-bare` error + "write the fragment bare; the separator is platform-owned" fix-it (AC-3; charset mirrors engine's `/^[\s,.;:?!]/`; deduped per phrase), `analysis.fragment-terminator` warning at CLAUSE sites only (site classifier mirrors the ADR-211 join rule incl. adjacent-marker transparency + paragraph-edge boundary), `analysis.verbatim-marker` error. `nothing` variants exempt. Non-room descriptions not snippet sites.
- **Loader atomic compile (`compileRoomSnippets`, initializeWorld pass 2)**: compute-then-apply per room — `{key}`→`{snippet:key}` rewrite in IdentityTrait.description + RoomTrait.initialDescription (shared single entry per marker, Z1/Q6), `RoomTrait.snippets[key]` population (`nothing`→`''`; single-variant plain phrase → string entry; strategy → Z5 selector via the now-EXPORTED `STRATEGY_SELECTOR`), gate compile: non-negated `is here`/`is in <this room>` on an entity subject → `mentions` (AC-4/AC-11); anything else → stdlib `registerSnippetGate((roomId, marker), thunk over evaluator.evalCondition)` (AC-12). Verbatim at marker → LoadError before any mutation (defensive; analyzer catches first). `Evaluator` promoted to a loader field.
- **Dependency/flag items**: `@sharpee/stdlib` devDep → runtime dep of story-loader (the planned ADR-211 seam wiring); ONE stdlib line outside the touched-package list, flagged to David — `lookupSnippetGate` added to the looking barrel (registry header always declared it public; 211-core exported only register/clear).
- **Tests**: chord `zoo-surfaces-phase2.test.ts` (11 — trailing-while incl. forward ref, AC-3 + once-per-phrase dedup, clause-vs-sentence lint, verbatim error, nothing-exempt, plain-single clean, non-room-no-diagnostics); story-loader `zoo-surfaces-phase2.test.ts` (7 — AC-4 mentions + rewrite + no gate, AC-11 byte-identical here/is-in, AC-12 live seam gate + JSON-serializable entry, AC-8 (chord,key) statement counter identity, nothing/shared-entry, plain string entry, ATOMIC LoadError-leaves-room-untouched via hand-sabotaged IR); end-to-end fixture `tests/fixtures/snippet-gate.{story,transcript}` 11/11 via CLI bundle (mentions + seam gates flip live; clean separators; platform comma joins).
- **Evidence**: chord 208/208 (snapshots +`condition: null` only), story-loader 119/119, stdlib 1333p/27s, workspace tsc clean, fixture 11/11.
- Fixture-authoring gotchas: scenery blocks `push` before on-clauses fire (used `on examining it`); multi-word entity nouns need `aka` for short-noun commands.

## chord-zoo-surfaces Phase 3 (this session) — DONE, uncommitted
- **Reserved channel keys** (`present/entered/exited/disappeared/detail`): bare declarations → `analysis.reserved-name` (in `registerPhrase` — owner-scoped `<id>.<key>` passes through, which IS the authoring surface); `phrase`/`refuse` statement pushes → `analysis.channel-pushed` (channels are platform-pulled).
- **Override grammar (CP3/Z3b)**: `PhraseOverride` reshaped `value` → `{strategy, condition, variants}`; headers take `, <strategy>` (Z5 set + retired fix-its) and `while <condition>`; `or`-variants at header indent; create-block dispatch relaxed to colon-anywhere. Never-guess: `detail` requires `while` (`analysis.detail-unconditional`), is single-text (`analysis.detail-variants`); `while` on lifecycle channels/ordinary overrides → `analysis.override-gate`. Repeating `detail` keys as `<id>.detail`, `<id>.detail.2`, ….
- **Z6 `remove`**: AST/parser/IR/analyzer (entity resolution like `move`, `analysis.remove-player`, D7 when-suffix, D13 sequence membership, phase-order mutated) + runtime case calling `world.removeEntity` (observer does the narration).
- **Z3 delivery architecture**: runtime `pendingChannelEvents` queue — `moveWithLifecycle` (witnessed exited/entered around `moveEntity`; player-moves and same-room moves never narrate) and the loader's ONE ADR-213 observer (witnessed `disappeared`; player-removal skipped; unwitnessed silent) enqueue; every report-collecting `execStatements` pass drains (mutations-pass returns are DISCARDED by interceptor sites — inline emission would be lost); `chord.channel-drain` daemon (registered only when `disappeared` is authored) delivers TS-initiated removals. Channel Choice counters key `(ownerWorldId, channel)` via a phraseEvent counter override.
- **Z3 present → ADR-212**: `registerPresentEntries` in `onEngineReady` (structural `registerSlotEntry`) — one entry per block: `'here'`, owner, declaration order, `counterKey:'present'`, content Literal/Choice keyed (owner,'present'); gated blocks use the predicate seam ANDed with owner presence.
- **Z3b detail**: `while it is on/lit` → `SwitchableTrait.detailWhenOn`/`LightSourceTrait.detailWhenLit`; anything else → `ChordDetailTrait` marker + ONE `registerClauseContributor` provider per load evaluating gates live.
- **Tests**: chord phase3 (18), loader phase3 (12); `override.value.text` assertion migrated; snapshots regenerated (mechanical reshape).
- **Evidence**: chord 226/226, story-loader 131/131, workspace tsc clean; zoo.story wt chain 37/37 and snippet-gate fixture 11/11 on the rebuilt Phase 3 bundle (early insurance — Phase 4's gate, run clean now).

## Phase Context
- **Plan**: `docs/work/chord-212-213-seams/plan.md` — Phase 1 of 3 (CURRENT).
- **`.current-plan`** repointed from `chord-211-core` (PACKAGE COMPLETE) to this plan at session start (pre-session audit flagged the stale pointer; repointing was the plan's own "deliberate" instruction for when implementation starts).

## Completed

### Phase 1 — ADR-212 engine slot-entry registry (DONE)
- `SlotEntry`/`SlotEntryGate` types + `IProsePipeline.registerSlotEntry` in `packages/engine/src/prose-pipeline/types.ts` (ADR-212 §1–2 verbatim; two-member gate union, `WorldModel` predicate arg).
- `ProsePipeline` (`pipeline.ts`): `slotEntries` Map keyed `` `${slotKey}\0${owner}` `` (last-wins, AC-7); `registerSlotEntry` with the dev-time Choice counter-key `console.warn` (never throws/rewrites); private `stageSlotEntries` evaluates gates and contributes BEFORE the story-contributor loop (platform-entries-first contract). Owner-present gate requires the player's room to EXIST and match (guards the `undefined === undefined` trap for roomless player + removed owner). Throwing predicate gate: warned, treated as not-holding (render-graceful) — small addition beyond the plan's letter, matching the ADR-211 gate-seam posture; flagged to David.
- `GameEngine.registerSlotEntry` delegation beside `registerSlotContributor`; `MockProsePipeline` gains the inert method; prose-pipeline barrel exports the two types.
- Tests: `tests/prose-pipeline/slot-entries.test.ts` (14 tests) — AC-2, AC-3, AC-5 (incl. raw-world identity + throwing predicate + nothing-serialized), AC-7, ordering, world-less no-op, both counter-key warn cases, and AC-4 driven through the REAL `EnglishLanguageProvider` (`{slot:here}` template → Assembler `selectChoice`): counter persisted `{npc1:{present:1}}` in the textState capability, then a FRESH pipeline over a JSON-round-tripped snapshot + re-registration continues the cycle (Beta, counter 2) — the plan's process-fresh requirement, not an in-process undo.

### Phase 2 — ADR-213 world-model removal observer seam (DONE)
- `EntityRemovalObserver` type + `IWorldModel.onEntityRemoved` + `WorldModel.removalObservers`/`onEntityRemoved` in `packages/world-model/src/world/WorldModel.ts`; `removeEntity` reordered per the plan: `lastRoomId` captured via `getContainingRoom` BEFORE `spatialIndex.remove`, observers invoked in individual try/catch (`console.error`, non-aborting, AC-5), then the three pre-existing lines unchanged — the dead `getLocation`/`moveEntity(null)` branch left as-is with a dated NOTE comment (flagged, not silently removed).
- Deviation from plan letter: `AuthorModel implements IWorldModel`, so the interface addition forced a one-line `onEntityRemoved` pass-through on AuthorModel (its `removeEntity` pass-through unchanged as planned). Caught by tsc; recorded in plan.md Phase 2 status.
- Barrel: `EntityRemovalObserver` exported via `src/world/index.ts` (root barrel re-exports `./world` wholesale).
- Tests: `tests/unit/world/entity-removal-observer.test.ts` (8 tests) — AC-1 (once, pre-deletion live-entity queryability asserted inside the callback, correct `lastRoomId` incl. `null`, unknown-id fires nothing, registration order, once-per-removal), AC-5 (throwing observer logged, removal completes, second observer still runs), AC-7 (orphaning fires nothing, entity remains), AuthorModel pass-through fires observers.
- Evidence: world-model 1330 pass / 10 skip (was 1322 + 8); workspace-root `pnpm exec tsc --noEmit` clean.

### Phase 3 — friendly-zoo migration + combined regression gate (DONE; PACKAGE COMPLETE)
- `stories/friendly-zoo/src/index.ts`: presence closure + table replaced with four `engine.registerSlotEntry` calls (slotKey `'here'`, owners = characterIds, `Literal` content resolved once at `onEngineReady`, orders 0–3, default gate, no counterKey), guarded by `if (language)`.
- **Gate learning 1**: `./repokit build dungeo` does NOT rebuild friendly-zoo's dist (story loader prefers `dist/index.js`); first chain run tested a stale closure build. Correct invocation: `./repokit build friendly-zoo`.
- **Gate learning 2**: since 2026-07-12 the wt walkthrough chain gate runs the CHORD story (`--story stories/friendly-zoo/zoo.story`), not the TS story — running it against the TS story fails 12/37 by design (different timeline/score: 85 vs 100 max). The plan's "chain exercises the TS occupants" assumption predated that conversion.
- **Evidence**: zoo.story wt chain 37/37 unedited; zoo.story atomic 61/61; TS-story transcripts (room-snippets, entrance-initial-description) 10/10; **AC-1 byte-identity proven by A/B diff** — full 5-chapter TS-story chain output, old closure dist vs migrated dist on the identical platform: byte-identical except one RNG parrot ambient line; dungeo units 1711p/9xf/4s (run 2; run 1 one RNG flake); dungeo wt chain **912/912 on run 3** (one-good-run rule; run 1: blue-sphere timing cascade ×3 fails; run 2: catastrophic combat-RNG cascade ×285 — documented flake family, totals vary with WHILE-loop gates).
- **Flagged, not fixed**: (1) `DestructibleBehavior.transformTo` removal has no transcript coverage (plan's stop-and-discuss item); (2) post-conversion, no gating transcript asserts the TS story's presence lines — the A/B diff stands as the migration evidence.

## chord-zoo-surfaces Phase 4 (this session) — DONE (one deferred item); PACKAGE COMPLETE
- **zoo.story**: `presence`→`present` ×4 (channel live via ADR-212); `{pins}` marker + `define phrase pins, cycling` (2 fragments + `nothing`) in the Gift Shop; flashlight detail-on/lit + radio detail-on blocks (Z3b, the excluded wt-03 texts).
- **TS story**: the four `registerSlotEntry` calls deleted from `index.ts` (zoo.story is the canonical variety surface now); `PresenceMessages` import dropped.
- **CP6**: excluded pair re-authored as chained chapters `wt-06-occupants-and-pins` (0/1/N occupants, keeper absent post-departure, byte-precise pins comma-join + clean `nothing` splice, cycle wrap) and `wt-07-object-detail` (0/1/N examine detail); both `walkthroughs/excluded/` files DELETED (David's recorded decision, plan-sanctioned at this step).
- **Gate (all green)**: zoo chain 56/56 (7 chapters, run 1); zoo atomics 61/61; TS transcripts 10/10; concealment-test 38/38; dungeo units 1703p/9xf/4s; dungeo chain 864/864 (run 1, no flake).
- **RESOLVED by David (2026-07-14)**: TS `zoo-items.ts` pins entry KEPT. Principle: both friendly-zoo stories are maintained tutorial surfaces — TS demonstrates the Sharpee API, zoo.story demonstrates Chord; the TS story is updated as the platform evolves, never frozen. `room-snippets.transcript` stands unedited; the plan's removal deliverable closed as won't-do.
- Timeline.transcript's keeper assertions verified compatible with live presence lines before running (contains pre-departure, not-contains post).

## story-ts proposal (this session)
- proposal-review run (David-invoked): no contradictions, no decisions-in-disguise. Two blocking findings FIXED: (1) ADR-129 status was PROPOSED while implemented and load-bearing — corrected to ACCEPTED with a dated status-correction note (record fix, no decision content changed); (2) P-7's Done-when now requires its ADR to accept/amend/supersede ADR-119 (State Machines, PROPOSED since 2026-01) — one decision record, not two. Advisory tensions noted in-item: P-5 must engage ADR-123's daemon-abstraction rejection rationale; P-4 coordinates with the open transformTo coverage gap. Proposal Status → REVIEWED.
- `docs/work/story-ts/proposal.md` (PROPOSED, awaiting David's triage): seven P-items inventorying the canonical TS zoo's raw-TS drops against zoo.story's declarative twins — P-1 capability-dispatch boilerplate, P-2 name-string targeting/requirement kit, P-3 scoring chains as data, P-4 penny-press transform-on-event, P-5 hand-rolled daemons vs the sequence kit, P-6 stateful behavior-swap daemon vs conditional composition, P-7 stringly world state vs declared state sets. Each item's done-signal: the raw TS disappears from the canonical story (the ADR-212 presence-closure precedent). Already-at-target listed (presence entries, pins map, dynamic text, patrol helper).

## Key Decisions

### David, 2026-07-14 (canonical-surfaces principle, post-Phase-4)
- The TS zoo story is the CANONICAL Sharpee implementation; zoo.story is the Chord one — the same game on both surfaces, both maintained as the platform evolves. Consequence executed: the four `registerSlotEntry` presence entries RESTORED to `stories/friendly-zoo/src/index.ts` (Phase 4 had deleted them per the plan; superseded by this principle) — TS story rebuilt, its transcripts 10/10. The TS pins entry stays (resolved earlier).
- Design rule (David's clarification): dropping to raw imperative TS is a design signal in ANY Sharpee story — TS or Chord, not just Chord-exposed seams. Hand-rolled closures/bespoke wiring where a declarative surface could exist → consider a platform design change (the presence-closure→ADR-212 arc is the precedent). Saved to memory (raise-sharpee-api-gaps extension).

### David, 2026-07-14 (on story-loader's new runtime stdlib dep)
1. **Single-stdlib invariant**: stdlib is only ever ONE instance in a game — dual-instance scenarios are build defects to be fixed, never designed around. The snippet-gate fixture transcript doubles as the canary for this.
2. **Module-global gate registry vs multi-world**: a client/multi-user concern, deferred — not a platform issue to solve now, and no per-world migration of the ADR-211 registry until the multi-user client work actually needs it.
- Predicate-gate throw handling (warn + skip, never abort the turn) added though the plan didn't spell it out — repo-wide render-graceful posture; flagged for David's review.
- `.current-plan` repointed `chord-211-core` → `chord-212-213-seams` (audit finding; the plan's own "deliberate repoint at implementation start" instruction).

## Next Phase
- Package complete — all three phases DONE, acceptance gate green, `chord-zoo-surfaces` Phase 3 unblocked. Work uncommitted; awaiting David's finalize/commit instruction and decisions on the two flagged coverage gaps.

## Open Items
- None blocking. Mutation-verification agent run pending at last update.

## Files Modified
- **Phase 1 (engine)**: `packages/engine/src/prose-pipeline/types.ts`, `pipeline.ts`, `index.ts`; `packages/engine/src/game-engine.ts`; `packages/engine/src/test-helpers/mock-prose-pipeline.ts`; `packages/engine/tests/prose-pipeline/slot-entries.test.ts` (new).
- **Phase 2 (world-model)**: `packages/world-model/src/world/WorldModel.ts`, `AuthorModel.ts`, `index.ts`; `packages/world-model/tests/unit/world/entity-removal-observer.test.ts` (new).
- **Phase 3 (story)**: `stories/friendly-zoo/src/index.ts`.
- **Docs**: `docs/work/chord-212-213-seams/plan.md` (all 3 phases → DONE, PACKAGE COMPLETE note); `docs/context/.current-plan`.
- **Build-regenerated**: `packages/sharpee/docs/genai-api/{engine,index,stdlib,world-model}.md`, `stories/dungeo/src/version.ts` (version stamp).

## Test Evidence
- Phase 1: slot-entries 14/14; engine suite 501 pass / 7 skip; engine tsc clean.
- Phase 2: removal-observer 8/8; world-model suite 1330 pass / 10 skip; workspace-root tsc clean.

---

## Session Metadata
- **Status**: COMPLETE — two full packages this session: `chord-212-213-seams` (all 3 phases, finalized earlier as 1ba048d4) and `chord-zoo-surfaces` (all 4 phases, Z1–Z6 IMPLEMENTED, this finalize), plus the story-ts design-signals proposal (REVIEWED), the ADR-129 status correction, and David's canonical-surfaces/design-signal principles recorded.
- **Blocker**: N/A
- **Rollback Safety**: two commits on `v2-210-chord-a` (1ba048d4 + this finalize); reverting the second reverts the whole zoo-surfaces package + proposal + ADR-129 status note.
