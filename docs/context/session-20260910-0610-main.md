# Session Summary: 2026-09-10 - main

## Status: COMPLETE

## Goals
- Start and complete ADR-344 plan Phase 4 (D5): remove the loader's first-declared-room placement fallback and amend ADR-289 D4 to say so.

## Phase Context
- **Plan**: `docs/work/adr-344-role-holder-seam/plan.md` — Phase 4 "One placement rule (D5, ADR-289 D4 amendment)" (Medium, 250-call budget)
- **Phase outcome**: DONE, on budget. Phase 5 (D7, `playable` reaches the runtime) marked CURRENT.

## Completed

### Re-measurement (AC-4's premise, re-run before touching code)
- `git grep -l "change the player to" -- "*.story"` → **80 sources**, split **61 explicit placement / 19 on the fallback / 0 shipped stories**. The ADR recorded 85/64/19/0; the same command at `87f06800c` (the commit the premise cites) also returns 80, so the 85 was a mis-count then, not a corpus that shrank. Proceeded rather than stopping: the premise AC-4 rests on — no shipped story relies on the fallback — holds exactly.
- Sharper than the original note: **17 of the 19 never reach the loader** (`gates/*`, `malformed/*` — chord parser/analyzer fixtures, several pinned by hard-coded `span.line` assertions that a new line would break). The two that do (`death.story`, `traits-basic.story`) do not depend on the fallback. **No fixture edits were needed**, confirmed by the green tree rather than predicted — so the plan's list-and-wait gate had an empty list.

### The fallback removed
- `packages/story-loader/src/loader.ts` — `finalizeRoleHolder`'s first-declared-room placement block is gone, along with the dead `irId`/`irPlayer` locals it was the only reader of. A comment states the new rule and cites ADR-344 D5 / the ADR-289 D4 amendment.
- `docs/architecture/adrs/adr-289-chord-routing-decided-once.md` D4 — the clause "keeping the first-declared-room fallback only for a player with no placement line at all" struck, with a dated amendment note. D4's actual subject (`in`/`on`/`starts in` unification) untouched; Status stays ACCEPTED.
- `docs/architecture/adrs/adr-344-player-role-holder-at-the-install-seam.md` D5 — re-measurement recorded and the 85/64 totals corrected to 80/61.

### Tests
- `packages/story-loader/tests/adr-289-d4-player.test.ts` — the fallback test converted: a placement-less protagonist is now asserted **unplaced** (`getLocation` undefined), with the file header's fallback sentence rewritten.
- `packages/story-loader/tests/adr-344-d5-install-refuses-unplaced-player.test.ts` — new, 3 tests: a placement-less Chord protagonist fails `installStory` with `reason: 'unplaced'` naming the character; the same story installs into the room the author wrote; the first-declared-room regression pinned directly. Branches on the step's structured `reason`, not on prose.

### Two Phase 1 leftovers fixed in passing
- `packages/story-loader/tests/helpers/boot-engine.ts` — still fabricated a placeholder actor and passed it as `new GameEngine({ player })`. Named in Phase 1's deliverable, never landed, and invisible because vitest type-checks nothing.
- `stories/dungeo/src/browser-entry.ts`, `stories/armoured/src/browser-entry.ts` — same fabrication, and these are shipped browser entries, so each carried a stray `'player'` actor for the whole session. Missed by Phase 1's exit grep (it matched the one-line `player: placeholder` form, not a multi-line object literal) and unseen by `tsc` because each story's `tsconfig.json` excludes `src/browser-entry.ts` (it needs `lib: DOM`).
- Added `stories/{dungeo,armoured}/tsconfig.browser-check.json` so those files are checkable by hand — `npx tsc -p stories/<story>/tsconfig.browser-check.json`, both exit 0. Probed rather than assumed: re-adding `player:` produced `TS2353 … 'player' does not exist in type` at `browser-entry.ts:118`; the fix restored exit 0. No CI wiring, per project convention.

### Exit gates — all run 2026-09-10 06:15–06:22 CDT
- `pnpm --filter '@sharpee/story-loader' test --run` → 122 files, **1107 passed, 0 failed** (1104 → 1107).
- `./repokit build dungeo` → exit 0, bundle 4,433,802 bytes.
- `./sharpee test branch-stories/fernhill` → **86 cards passing, 104 assertions passing**, 222 commands.
- `node dist/cli/sharpee.js --test --chain stories/dungeo/walkthroughs/wt-*.transcript` → **952 tests in 17 transcripts, 952 passed**, all 17 goldens matched (single run, pinned seed).
- `pnpm exec turbo run test:ci` → **67 tasks, 67 successful** — run after the build, per the prior session's lesson that a dependent-package suite proves nothing until the package it depends on is rebuilt.

## Key Decisions
- **Proceeded past AC-4's premise gate on a corrected measurement.** The plan says stop if the split has moved; the totals moved (85→80, 64→61) but the material premise (19 fixtures, 0 shipped stories) held, and the discrepancy reproduces at the premise commit itself, so it was a mis-count rather than a change. Recorded in both the plan and ADR-344 D5.
- **No Chord fixtures were edited.** The plan's 19-file bulk edit turned out to be unnecessary and, for 17 of them, actively harmful (line-pinned diagnostics).

## Open Items

### Short Term
- I-86894c-1: The remaining `new GameEngine({ ..., player })` sites are all test files — 7 in `packages/story-loader/tests/`, 2 in `packages/engine/tests/sound/`, 1 in `packages/platform-browser/tests/capture-parity.test.ts` — plus `packages/story-loader/tests/helpers/boot-turns.ts`. They pass at runtime (an excess property is ignored) and nothing type-checks them. Belongs in Phase 6's sweep.

### Long Term
- N/A

## Files Modified
- `packages/story-loader/src/loader.ts` — fallback and its dead locals removed
- `packages/story-loader/tests/adr-289-d4-player.test.ts` — fallback test converted
- `packages/story-loader/tests/adr-344-d5-install-refuses-unplaced-player.test.ts` — new, 3 tests
- `packages/story-loader/tests/helpers/boot-engine.ts` — placeholder player dropped
- `stories/dungeo/src/browser-entry.ts`, `stories/armoured/src/browser-entry.ts` — fabricated player dropped
- `stories/dungeo/tsconfig.browser-check.json`, `stories/armoured/tsconfig.browser-check.json` — new local type-check configs
- `docs/architecture/adrs/adr-289-chord-routing-decided-once.md` — D4 amendment
- `docs/architecture/adrs/adr-344-player-role-holder-at-the-install-seam.md` — D5 re-measurement
- `docs/work/adr-344-role-holder-seam/plan.md` — Phase 4 DONE, Phase 5 CURRENT
- `stories/dungeo/src/version.ts` — build-generated (`BUILD_DATE` only)

## Notes
- Session started: 2026-09-10 06:10 CDT
- Uncommitted at time of writing; the loader change and the ADR-289 amendment must land in the same commit (ADR-344's amendment-ownership rule).
- Session state's `files` array is empty for this session — every edit went through shell heredocs/python per Auto Mode, not the Edit tool. The Files Modified list above was cross-checked against `git status` instead.
- `docs/context/session-20260910-0354-main.md` shows modified in `git status` but is a pre-existing change carried in from the prior session, not touched this session — omitted from Files Modified above for that reason.

---

## Session Metadata

- **Session**: 86894c
- **Status**: COMPLETE
- **Blocker**: N/A
- **Blocker Category**: N/A
- **Rollback Safety**: safe to revert — the loader change, the two ADR amendments, and all new/converted tests are uncommitted together in the working tree; nothing has merged or shipped, and no orphaned artifacts exist (the two `tsconfig.browser-check.json` files are new, standalone, and unwired into any build or CI step).

## Dependency/Prerequisite Check

- **Prerequisites met**: Phase 3's install-seam guard (`validate-role-holder`, D1/D2/D4 — landed in prior sessions `8ea6c94a7`/`03a3344c2`) supplies the refusal path this phase's new install test exercises; AC-4's premise ("no shipped story depends on the fallback") was re-verified this session before any edit, not assumed.
- **Prerequisites discovered**: none — the re-measurement confirmed the premise held (corrected to 80 sources / 61 explicit / 19 fixture-only / 0 shipped) rather than surfacing a new gap; the 85/64 in the ADR was a mis-count at the time it was written, reproduced by re-running the same `git grep` at the commit the premise cites.

## Architectural Decisions

- ADR-344 D5 (implemented this session): the loader no longer places an unplaced role holder in the first-declared room; an unplaced protagonist is refused downstream by the install seam's `validate-role-holder` step with reason `'unplaced'`.
- ADR-289 D4 amended: the clause preserving the first-declared-room fallback struck, dated; D4's actual subject (`in`/`on`/`starts in` unification) untouched; Status stays ACCEPTED.
- ADR-344 D5 re-measurement recorded directly in the ADR (85/64 → 80/61), rather than left as a session-only note.
- Pattern applied: refuse-at-the-seam over silently-repair, consistent with ADR-344's overall install-time-guard direction.
- Behavior Statement produced for `finalizeRoleHolder` (DOES/WHEN/BECAUSE/REJECTS WHEN — it no longer moves the role holder; an unplaced protagonist is refused downstream, not repaired). Integration Reality Statement also produced for this phase's OWNED dependencies (the loader and the two browser entries) — nothing stubbed, real-path tests named for each.

## Mutation Audit

- Files with state-changing logic modified: `packages/story-loader/src/loader.ts` (`finalizeRoleHolder`).
- Tests verify actual state mutations (not just events): YES (evidence: the converted `adr-289-d4-player.test.ts` asserts `world.getLocation(player.id)` is `undefined` for a placement-less protagonist, not a return value; the new `adr-344-d5-install-refuses-unplaced-player.test.ts` asserts the structured refusal `reason: 'unplaced'` and, on the success path, the actual room the protagonist sits in; both confirmed passing by `pnpm --filter '@sharpee/story-loader' test --run`, run 2026-09-10 06:15–06:22 CDT — "122 files, 1107 passed, 0 failed").

## Recurrence Check

- Similar to past issue? YES. This is the fourth instance this week of "a check structurally incapable of observing what broke": root `tsc --noEmit`'s narrow scope, an AC asserting both ends but not the middle, a dependent-package suite run against a stale engine build, and now two shipped browser entries excluded from their own story's `tsconfig.json` (confirmed by probe: re-adding `player:` produced `TS2353` at `browser-entry.ts:118`, not caught by any existing gate). Relevant to the pre-session audit's recurring-pattern thread 2 (template/excluded-file drift, invisible to `tsc`). Consider a one-time audit of which story/package `tsconfig` exclusions are silently hiding type errors elsewhere in the tree.

## Test Coverage Delta

- Tests added: 3 new (`adr-344-d5-install-refuses-unplaced-player.test.ts`); 1 converted in place (`adr-289-d4-player.test.ts`, same test count, assertion direction reversed).
- Tests passing before: 1104 → after: 1107 (evidence: `pnpm --filter '@sharpee/story-loader' test --run`, run 2026-09-10 06:15–06:22 CDT — "122 files, 1107 passed, 0 failed").
- Known untested areas: the remaining `new GameEngine({ ..., player })` sites are all test files — 7 in `packages/story-loader/tests/`, 2 in `packages/engine/tests/sound/`, 1 in `packages/platform-browser/tests/capture-parity.test.ts`, plus the `boot-turns.ts` helper. They pass at runtime (the excess property is ignored) and nothing type-checks them; flagged for Phase 6's sweep rather than fixed here.

---

**Progressive update**: Session completed 2026-09-10 06:22 CDT
