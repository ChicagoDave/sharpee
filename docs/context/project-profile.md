# Project Profile: Sharpee

**Generated**: 2026-08-09
**Repository**: sharpee (`@sharpee/*` lockstep v4.3.0, published to npm; Chord language version tracked independently at v2.2.0)
**Branch at generation**: `feat/ide-go-live-phases-1-3`

## Domains

- **Domain Modeling** — DDD-style `world-model` package: traits, behaviors, capability dispatch (ADR-090, ADR-207 per-instance registry), `if-domain` contract types; computed exits (ADR-295) added traversal-resolution logic to exit resolution
- **API / Service (Engine)** — `engine` owns turn cycle/scheduler; `stdlib` standard actions (validate/execute/report/blocked, ADR-051); `parser-en-us` grammar — generated from Chord source (ADR-269: `standard-en-us.story`, 410 rules/55 blocks, `repokit grammar` build step)
- **Chord Story Language (Compiler Frontend + Interpreter)** — `packages/chord` (v4.3.0 npm package; language itself versioned independently at 2.2.0 per ADR-257): lexer, parser, semantic analyzer, Story IR, diagnostics for the `.story` author language. `packages/story-loader` is the generic Story IR interpreter (ADR-210); `packages/bootstrap` is the single story-assembly loader shared by transcript-tester/CLI/devkit (ADR-180); `packages/interpreter` is legacy Tauri-runner-only (excluded from workspace)
- **Deterministic Execution / Choice Points** — seed authority + named RNG streams (ADR-291), testability/bounded-outcome-search contract (ADR-292), choice points with per-point RNG streams (ADR-293, `RandomService` replaces `ActionContext.random`, save format 3.0.0), prose-order emission ownership (ADR-296). Stable since the last profile — no changes this cycle
- **Testing Intelligence / Transcript Authoring (new dominant arc)** — ADR-300 through ADR-306: addressable channels + canonical transcript (300), `sharpee-transcript-editor` (301, superseded in part), transcript branches / `continues:` tree (302), convergent paths + unwinnable states (303), testing-workspace layout (304, superseded in part), create-transcript-from-play (305, ADR-180-style shared substrate — kept), and the **testing play-surface revamp** (306, ACCEPTED 2026-08-09) which removes the "golden path" entirely — "it's just a tree of transcripts" — and moves testing into a dedicated page where playing *is* writing the test suite. `packages/branch-tester` (`auto-assertion.ts`, `tree.ts`, `tree-runner.ts`, `from-play.ts`, `search.ts`, `coverage.ts`) is the platform substrate this arc builds on; the `.golden`-fixture/author-world golden tier from ADR-294 is being retired as ADR-306's Phase 6 lands (D1 supersession, not yet flipped)
- **macOS IDE (Swift native + embedded WebView surfaces)** — `tools/ide/SharpeeIDE` (Xcode project, ~95 Swift source files, `SharpeeIDETests` XCTest target) is the primary product surface alongside Chord: native panes for Project, Editor, Compose, Play, Test, Docs, Publish, Settings, Menus, Persistence, Workspace, Build, Launch; `tools/ide/web/{docs-tab,testing-tab}` are HTML/JS/CSS substrates rendered inside native WebViews via scheme handlers (`TestingTabSchemeHandler.swift`, `TestingTabWebRoot.swift`). Current branch (`feat/ide-go-live-phases-1-3`) is the go-live hardening push for this app
- **Natural-Language Text Rendering ("Phrase Algebra")** — `text-blocks` + `lang-en-us`: phrase-model assembler core and atom system (ADR-192–206), turn narrative slots (ADR-296: transactions order sources, slots place phrases)
- **Event Sourcing / Messaging** — `event-processor` dispatches effects/events; `ext-daemon`/`plugin-scheduler`/`plugin-state-machine`/`plugin-npc` for daemons/fuses/NPC turn phases; ADR-208 interceptor registry
- **Frontend UI (web)** — `packages/platform-browser`, `packages/media`, `packages/map-editor` (excluded from workspace via `!packages/map-editor`), `packages/runtime` (headless engine runtime for iframe embedding via postMessage), `tools/zifmia` multi-user server, `website/` — Next.js 16 / React 19, includes an embedded Chord editor (CodeMirror-based); no website changes this cycle (4 commits, none since 2026-08-02 touched `pnpm-workspace.yaml`/CI/root config)
- **CLI / Tooling / Author Platform** — `packages/devkit` (author tool, `./sharpee`), `packages/transcript-tester` (`golden.ts`, `runner.ts`, `aggregate.ts`, `watch.ts` — scope now narrowing as `branch-tester`'s tree model takes over the regression role per ADR-306), `tools/repokit` (in-repo platform build CLI, ADR-187), `tools/ide` (Swift IDE, see above; also carries Swift-based Chord lexer golden tests), `tools/vscode-ext`, `packages/bridge` (Node subprocess bridge, newline-delimited JSON over stdin/stdout), `packages/helpers` (fluent entity builders), `packages/queries` (LINQ-style fluent entity query API)
- **Library / Package** — Publishable `@sharpee/*` npm packages with auto-generated GenAI API docs (`packages/sharpee/docs/genai-api/`)
- **Long-Form Documentation / Publishing** — `docs/book` (two live editions: `v1.5.0/`, `v2.0.0/`), naive-regression QA gate (transcript-driven walkthroughs of every chapter)

## Tech Stack

- **Language**: TypeScript 5.x (ES2022 target, CommonJS modules) for the platform/Chord/IDE-web layer; Swift for `tools/ide/SharpeeIDE` (native macOS app, XCTest)
- **Runtime**: Node.js (platform); native macOS app process (IDE) hosting bundled Node.js toolchain + WKWebView surfaces
- **Framework(s)**: Custom in-memory World Model engine (no external DB); Next.js 16 / React 19 for `website/`; AppKit/SwiftUI (mixed, per Xcode project layout) for `SharpeeIDE`
- **Data layer**: None — in-memory `WorldModel`, entity/trait system persisted via save/load (save format 3.0.0, ADR-293 — versioned reader); IDE persists project/session state via `SharpeeIDE/Persistence`
- **Messaging**: In-process event/effect dispatch (`event-processor`), daemon/fuse scheduling (`plugin-scheduler`) — no external broker; IDE↔web surfaces communicate over WKWebView scheme handlers + NDJSON line buffers (`NDJSONLineBuffer.swift`)
- **Test framework**: Vitest 3.x (unit/integration, per-package `vitest`/`test:ci` scripts), Stryker 9.x (mutation testing, `stryker.config.json`), custom transcript tester (`.transcript` walkthrough/integration format, run via `dist/cli/sharpee.js --test`) plus the newer `branch-tester` tree-runner (`continues:` trees, `sharpee test --tree`, ADR-302); XCTest for the Swift IDE (`SharpeeIDETests`)
- **Test command**: `pnpm exec turbo run test:ci` — runs every workspace package's `vitest run` (each package defines a bare `test` script that is `vitest` in *watch* mode, so `test:ci`, not `test`, is the non-interactive whole-suite entry point; there is no root-level `test`/`test:ci` alias in `package.json`, only the `turbo.json` task). This covers unit/integration only — the transcript regression baseline (`node dist/cli/sharpee.js --test --chain stories/dungeo/walkthroughs/wt-*.transcript`) requires `./repokit build dungeo` first and is a separate, deliberately-manual gate (per CLAUDE.md, walkthrough chain is the regression baseline; unit transcripts have known failures — do not fold them into a single green/red signal)
- **Build tool**: `@davidcornelson/tsf` (ts-forge) for package compilation, Turborepo for task orchestration, esbuild for CLI/browser bundling; two build CLIs per ADR-187 — `./repokit` (in-repo platform build, includes a `grammar` build step per ADR-269) and `./sharpee` (author tool / `@sharpee/devkit`); Xcode/`xcodebuild` for `SharpeeIDE` (`project.yml` via XcodeGen, `package.sh`/`build-docs-tab.sh`/`build-testing-tab.sh`)
- **Package manager**: pnpm 10.13.1 workspace
- **CI/CD**: GitHub Actions — `build-platforms.yml` and `publish-npm.yml` only (unchanged this cycle; no CI/config diffs since 2026-08-02)
- **Monorepo**: Yes (pnpm workspaces + Turborepo). `pnpm-workspace.yaml` (the source of truth, NOT root `package.json`'s stale `workspaces` array) lists `packages/*` plus `packages/extensions/{testing,basic-combat,scoring,hunger}`, excludes `!packages/map-editor` and `!packages/interpreter`, and curates workspace stories to `dungeo`, `channel-service-test`, `family-zoo-tutorial`, `cloak-of-darkness` plus `tools/{shite,zifmia,repokit}`. `stories/` on disk has more entries than are workspace members — several are pure-Chord `.story` projects with no `package.json` (ADR-252 D2)

## Conventions

- **Test location**: Separate `test`/`tests` dirs per package (excluded from `tsc` build via tsconfig `exclude`); story walkthrough/unit transcripts at `stories/{story}/{walkthroughs,tests/transcripts}/*.transcript`; IDE tests at `tools/ide/SharpeeIDETests/*.swift`
- **Test naming**: `*.test.ts` (Vitest unit tests, 703 files — up from 632 at the last profile), `wt-*.transcript` (walkthroughs, run with `--chain`), other `*.transcript` (unit-style integration tests; 165 total — down from 183, reflecting the ADR-302 tree/branch model consolidating what used to be separate loose transcripts), `wt-*.golden` (golden output fixtures, being retired per ADR-306 D1), `*Tests.swift` (XCTest, e.g. `PlayTranscriptCreationTests.swift`)
- **Source structure**: Layer-based by package (traits/behaviors in world-model, actions in stdlib, grammar in parser-en-us generated from `packages/parser-en-us/grammar/standard-en-us.story`, text/messages in lang-en-us); actions follow a 4-file convention: `<name>.ts`, `<name>-data.ts`, `<name>-events.ts`, `<name>-messages.ts`, `<name>-types.ts`; `chord` compiler frontend follows lexer → parser → analyzer → IR staging; `SharpeeIDE` is feature-folder-based (`Play/`, `Test/`, `Editor/`, `Compose/`, `Docs/`, `Publish/`, etc.)
- **TypeScript strict mode**: Yes — `strict`, `noImplicitAny`, `noImplicitReturns`, `noFallthroughCasesInSwitch`, `forceConsistentCasingInFileNames` (via shared `tsconfig.base.json`, composite project references) — unchanged
- **Import style**: CommonJS module resolution (Node), per-package `references` for project-reference builds; no path-alias convention observed at root
- **Language layer separation**: All user-facing text lives in `lang-en-us`; engine/stdlib/world-model emit semantic events with message IDs only (no embedded English)
- **Versioning**: Uniform lockstep versioning across `@sharpee/*` packages (currently 4.3.0). The Chord language carries an independent semantic version (ADR-257) — currently 2.2.0 — decoupled from the platform release train and IR wire-format version
- **RNG/determinism discipline** (ADR-291–293, stable): `ActionContext.random` is gone — all randomness routes through `RandomService` via named `ChoicePoint`s with per-point seed derivation; tests assert byte-identical output at a pinned seed
- **Testing-surface convention shift (new, ADR-300–306)**: the "golden path" concept is being removed from the author-facing model entirely — regression is a *tree* of transcripts (`continues:` branches) rather than a single blessed golden run; a future session finding ADR-304/305/ADR-294-D1 not yet marked SUPERSEDED is looking at correct pre-Phase-6 state, not an omission (the flip happens when the revamp plan's Phase 6 lands)

## Mutation Signatures

### Domain Modeling / Engine (world-model, stdlib, engine)

- **Mutation calls**: `WorldModel` entity/trait mutations via `*Behavior` classes (e.g., `LightSourceBehavior`, `ContainerTrait` mutators, `HealthTrait`/`HealthBehavior`), capability dispatch handlers (`findTraitWithCapability`), scheduler/turn-cycle state transitions, event/effect application in `event-processor`, computed-exit traversal resolution (ADR-295), `RandomService` draws against a named `ChoicePoint` (ADR-293)
- **Reporting without mutation**: An action's `report` phase emitting success text/events without the corresponding `execute` phase having called a behavior mutator or changed entity/trait state — flag as silent no-op; a randomness-consuming action that reads `Math.random()` directly instead of drawing from a named `ChoicePoint`/`RandomService` stream (ADR-291–293 violation)
- **Test assertions — verify**: Post-call inspection of `WorldModel` entity/trait state (e.g., location changed, trait flag flipped, inventory contents updated, health/alive-status changed), emitted semantic events with correct message IDs and payload, or byte-identical transcript output at a pinned seed against a committed fixture
- **Test assertions — insufficient**: Asserting only that an action "didn't throw," asserting only on the return value of `execute()` without checking world state, asserting on event *type* without checking event *payload*, treating "no diagnostics" or a clean exit as sufficient without checking output actually matches

### Chord Story Language (packages/chord, story-loader, bootstrap)

- **Mutation calls**: Lexer producing a token stream from `.story` source; parser building an AST from tokens (error recovery/diagnostics on malformed input); semantic analyzer resolving symbols, validating catalog references, and lowering AST to Story IR; diagnostic collection mutating a diagnostics sink; `story-loader` interpreting Story IR into a runnable Story; counter `raise`/`lower` mutations (ADR-264) clamped silently at bounds
- **Reporting without mutation**: Claiming a `.story` construct "compiles" without an actual IR node being emitted for it; a diagnostic described as "caught" with no corresponding entry pushed to the diagnostics collection; claiming a language version bump without updating `CHORD_LANGUAGE_VERSION` and the `chord.ebnf` surface pin (ADR-257 D5)
- **Test assertions — verify**: Asserting on the shape/contents of the emitted IR (not just that parsing "succeeded"), asserting on specific diagnostic codes/spans for invalid input, round-trip tests comparing Chord source against known-good IR fixtures, counter state inspected post-`raise`/`lower` for clamping behavior
- **Test assertions — insufficient**: Asserting only that `parse()` or `analyze()` returned without throwing; snapshotting IR without asserting on the specific fields under test; treating "no diagnostics" as sufficient without checking the IR is actually correct

### Testing Intelligence / Transcript Branching (branch-tester, transcript-tester, engine RandomService)

- **Mutation calls**: `RandomService` draws scoped to a named `ChoicePoint`/stream; `branch-tester`'s tree mutations (`tree.ts` node insertion/pruning under `continues:`, `tree-runner.ts` replaying a branch), `auto-assertion.ts` synthesizing claims from the 6e policy header field, `from-play.ts` (`createTranscriptFromPlay`) serializing a played session into a transcript — the one code path all IDE/CLI transcript-creation surfaces route through (ADR-305 D5); save-state seed persistence (save format 3.0.0)
- **Reporting without mutation**: Marking a branch/walkthrough "passing" without the tree actually being re-run and diffed; claiming determinism without pinning `--seed`/`point-seed:` in the test invocation; a "Create Transcript" UI action reported as complete without the transcript file actually written via `createTranscriptFromPlay`
- **Test assertions — verify**: Actual replay of a `continues:` branch against pinned-seed output, asserting the resulting turn output/state matches the recorded assertions; RNG stream isolation — a change to one `ChoicePoint` must not perturb another's draws; `createTranscriptFromPlay` output asserted against the serialized transcript file's contents, not just "did not throw"
- **Test assertions — insufficient**: Running a walkthrough once with default (unpinned) randomness and eyeballing the output; asserting a branch "exists" in the tree without asserting it replays correctly; treating a Swift-side UI action as verified because a callback fired, without checking the NDJSON payload or the file it wrote

### Phrase Algebra / Text Rendering (text-blocks, lang-en-us)

- **Mutation calls**: Phrase-model assembler composing atoms (state-derived adjective, contents, slot, pronoun, numeral, verbatim) into a realized string; subject-verb agreement resolution; message-param substitution; turn narrative slot placement — transactions order sources, slots place phrases (ADR-296)
- **Reporting without mutation**: Returning a phrase-model object without invoking the assembler/realizer — i.e., "built the model but never rendered text"
- **Test assertions — verify**: Asserting on the final rendered string output (exact text or normalized form) for a given world state/param set, not just that an atom object was constructed
- **Test assertions — insufficient**: Snapshot-only tests with no explicit assertion of expected wording; asserting the phrase-model shape without ever calling the realizer

### Event Sourcing / Messaging (event-processor, plugin-scheduler, plugin-npc, plugin-state-machine)

- **Mutation calls**: Effect/event dispatch to registered handlers, fuse/daemon scheduling and firing, NPC turn-phase state transitions, interceptor registry mutations (ADR-208)
- **Reporting without mutation**: A daemon/fuse reported as "fired" with no corresponding scheduled-state change or handler invocation
- **Test assertions — verify**: Post-turn inspection of scheduler state (fuse/daemon queue), confirmation a registered handler actually ran (side effect or spy invocation tied to real dispatch), NPC state after a turn
- **Test assertions — insufficient**: Asserting an event was *constructed* without asserting it was *dispatched* and *handled*

### macOS IDE (SharpeeIDE, Swift)

- **Mutation calls**: `TestRunner.swift` invoking the platform test harness and parsing results; `TranscriptDiscovery.swift`/`TranscriptSourceProvider.swift` scanning and registering transcript files; `TestingTabViewController.swift`/`TestController.swift` writing back UI state that drives an actual runner invocation; persistence writes in `SharpeeIDE/Persistence`; scheme-handler responses in `TestingTabSchemeHandler.swift` bridging web-surface actions to native file/process mutations
- **Reporting without mutation**: A test-tab UI element reported "passing"/"updated" without a corresponding `TestRunner` invocation and result parse; a "Create Transcript" or "Save" action reported complete without a file actually written to disk (verify via `PlayTranscriptCreationTests.swift`-style assertions)
- **Test assertions — verify**: XCTest assertions against actual written files/persisted state (not mocked file managers) for real-path integration tests; NDJSON payloads parsed and asserted field-by-field for scheme-handler round-trips
- **Test assertions — insufficient**: Asserting a Swift closure/callback fired without checking its argument payload; UI-state-only assertions that never touch the underlying file or process the UI claims to have driven

### Long-Form Documentation / Publishing (docs/book, site build)

- **Mutation calls**: `scripts/build-book.sh` regenerating built book output; naive-regression runs executing every book-referenced transcript/walkthrough against the built platform
- **Reporting without mutation**: Marking the naive-regression gate "GREEN" without an accompanying clean run log; claiming a chapter fix without re-running its walkthrough transcript
- **Test assertions — verify**: Transcript run exits clean (zero unexpected diffs) against `dist/cli/sharpee.js`, referenced against the specific published npm version tested
- **Test assertions — insufficient**: Manual read-through of a chapter without executing its transcripts; treating RNG-based combat/thief flakes as gate failures — RNG is largely eliminated by ADR-291–293's pinned-seed determinism, so a flake is more likely a real regression

## Notes

- **New dominant arc since the 2026-08-02 profile: ADR-300–306, the testing-intelligence/IDE surface.** This displaces the determinism arc (ADR-291–296, now stable/unchanged) as the most active area. Sequence: ADR-300 (addressable channels + canonical transcript) → ADR-301 (transcript editor, now partially superseded) → ADR-302 (`continues:` branch tree) → ADR-303 (convergent paths/unwinnable states) → ADR-304 (testing workspace layout, partially superseded) → ADR-305 (create-transcript-from-play, platform substrate — kept) → ADR-306 (testing play-surface revamp, ACCEPTED 2026-08-09, session 2b82b5). ADR-306's capstone ruling: **"there is no golden path anymore — it's just a tree of transcripts,"** which removes the author-world golden tier (ADR-294's `.golden` fixtures) entirely once the revamp's Phase 6 lands. Supersession flips (ADR-304, ADR-305 in part, ADR-294 D1 scoping note, ADR-301 pointer) are recorded now but deliberately not yet applied to those files' Status lines — correct as-is until Phase 6.
- **Current branch is `feat/ide-go-live-phases-1-3`**, 132 commits ahead since the last profile date (2026-08-02) — nearly all IDE/platform work: Docs tab, Publish tab (`sharpee publish`, ADR-284), Testing workspace (ADR-304), transcript editor (Phase 5), theme picker, auto-assertion policy (Phase 6e), create-transcript-from-play (Phase 6f, ADR-305), and now the Phase 1–2 start of the ADR-306 revamp.
- **`tools/ide/SharpeeIDE` (Swift, XcodeGen `project.yml`) is now a first-class domain**, not just a build-tooling footnote — ~95 Swift files, a dedicated XCTest target (`SharpeeIDETests`), and two embedded web substrates (`docs-tab`, `testing-tab`) rendered via WKWebView scheme handlers. Per standing project direction, the macOS IDE and Chord language are the active product; `packages/` is mature supporting infrastructure.
- **`packages/branch-tester`** is a package that did not get its own call-out in the last profile (it existed, but the tree/branch model — `tree.ts`, `tree-runner.ts`, `search.ts`, `coverage.ts`, `from-play.ts` — is what ADR-302/305/306 now build the entire testing-intelligence story on top of). It is distinct from `packages/transcript-tester`, whose golden-fixture role is being narrowed by this arc.
- **Codebase scale**: ~418K lines TypeScript across packages/stories/tools (was ~382K), 703 `*.test.ts` files (was 632, +11%), 165 `.transcript` files (was 183 — a *decrease*, consistent with ADR-302 consolidating loose transcripts into branch trees rather than growing the flat-file count)
- **ADR volume**: 315 top-level ADR files (was 305), now running through ADR-306.
- **No changes this cycle** to `pnpm-workspace.yaml`, root `package.json`, `ts-forge.config.json`, or `.github/workflows/` — confirmed via diff against the 2026-08-02 state. The Tech Stack and Conventions sections below are otherwise unchanged from the last profile except where noted (Testing Intelligence domain, IDE domain, test/transcript counts).
- **Test command gap closed this cycle**: the previous profile omitted an explicit "Test command" field. Filled in now — `pnpm exec turbo run test:ci` is the whole-suite non-watch entry point (per-package bare `test` scripts are `vitest` in watch mode and must not be used as a gate). The transcript walkthrough chain remains a separate, deliberately-manual regression gate per CLAUDE.md and is not folded into this single command.
- Several `docs/context/.devarch-events-*.jsonl` / `.devarch-gate-*` files remain typical untracked DevArch housekeeping artifacts in git status — not part of the profile signal set.
