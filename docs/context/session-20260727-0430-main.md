# Session Summary: 2026-07-27 (0430) - main [332f30]

**Goal**: Return to the IDE arc after the Chord changes; establish where it stands and
what must move before implementation.

**Status**: IN PROGRESS — ADR-258 re-assessed (NEEDS AMENDMENT); **ADR-276 ACCEPTED**
(source-authoritative errors / stdlib manifest): interviewed (Q-1 one arc, Q-2
locale-keyed, Q-3 declarative schema table) and adr-review 15/15 same day, review run
before the flip. D5 amendment to ADR-258 still pending.

## Work done

- Fast-forwarded local main to `e6597bdb` (was 2 behind): **Sharpee 4.0.0 released,
  Chord language consolidated to public 2.0.0** (ADR-257 second exception), npm published,
  website deployed.
- IDE status established: ADR-185 realignment complete (June 20); **ADR-258** (Chord
  authoring environment, TS path dropped) ACCEPTED 07-23, unblocked (ADR-259 implemented
  07-24, merged), **not implemented**.
- **ADR-258 staleness re-assessment** (adr-review, verify-against-source): architecture
  intact, D7 strengthened (grammar arc never touched the lexer — 203 lines, only ADR-264
  added tokens). One BLOCKER: ADR-270 landed alteration errors (`extend action` /
  `remove from action`) as load-time `LoadError`s — invisible to D5's `--json`. Three
  SMALL: hatch lint shape, grammar-file kind, freshness edits. Addendum written into
  ADR-258's Status.
- **Step-back survey** (David's ask: "source-based errors for all of Chord v2?"): census
  of all 50 loader `LoadError` sites vs 312 compile codes. 16 author-reachable load-only
  entries, all needing stdlib *data* only; residue = hatch existence/export shapes,
  provider capabilities, evaluator runtime. Precedents located: `catalog.ts` names-only
  rule, `STDLIB_CHAIN_NAMES`, ADR-269 D7 freshness-gated generation.
- **ADR-276 DRAFT**: `docs/architecture/adrs/adr-276-chord-source-authoritative-errors.md`
  — D1 compile authoritative for source-derivable errors (loader sites → backstops),
  D2 generated freshness-gated stdlib manifest in `@sharpee/chord`, D3 no-new-data checks
  move immediately, D4 hatch lint joins the diagnostics stream, D5 residue closed.
  Open: Q-1 migration ordering, Q-2 locale seam, Q-3 setting-schema source of truth.

## Key decisions

- **David (2026-07-27)**: pursue source-based errors for all of Chord v2; record as a new
  ADR referencing ADR-258 → ADR-276 (DRAFT, awaiting interview).

## Open items / next steps

1. ~~ADR-276 interview → adr-review → ACCEPTED~~ DONE (review before flip, David's rule).
2. ~~ADR-258 amendment~~ DONE — Amended markers at D2 (grammar-file kind), D5
   (ADR-276 re-grounding, hatch record type, residue, sequencing), D7 (lexer 203,
   Chord 2.0.0 golden corpus); Parent + Status updated; 2 acceptance items added.
3. Implementation planning, in order: ADR-276 migration arc (census + manifest +
   generator + schema-table refactor), then ADR-258 (platform `--json` + golden lexer
   fixture from this machine; Swift swaps on the Mac).
4. Parked from audit (3rd+ session): delete ruling for `scripts/chord-gap-report.cjs` +
   `scripts/generate-standard-grammar-chord.cjs`; thealderman story-local tsc failure;
   ADR-266 umbrella status closing note.

## Phase 1 implementation (branch adr-276-p1)

- **checkCompositionLegality** post-pass in `packages/chord/src/analyzer.ts` — census
  9 (patrol route), 11 (dark rooms-only), 12 (worn-not-wearable), 13 (gerund
  implementations — 2+ Chord surfaces unconditional; zero-surface only when hatch-free,
  the ADR-090 capability surface being invisible to source), 14 (conditional
  composition), 15 (trait-not-declared), **plus discovered 17 (unknown kind noun) and
  18 (multiple kind nouns)** — the phrasebook fixtures compiled `a thing, portable`
  clean, proving both load-only. All 8 loader sites converted to backstops same commit.
- Census corrections found in source: entries **7 and 8 were already analyzer-gated**
  (`analysis.extension-not-used`, manifest field gates) — loader sites are backstops
  already; record in the ADR census at Phase 8.
- Tests: `packages/chord/tests/composition-legality.test.ts` (17 cases, all GREEN
  grade); `packages/story-loader/tests/adr-276-backstops.test.ts` (6 rogue-IR
  backstops); `cuttable.test.ts` + `loader.test.ts` rejection tests rewritten as
  rogue-IR mutations (they now fail at compile by design); `phrasebooks.test.ts`
  fixtures fixed (`a thing, portable` was load-invalid vocabulary).
- Suites: chord 632/632, story-loader 417/417, root tsc clean.
- Corpus vs stock baseline (A/B on rebuilt stock bundle): dungeo units 1787✓ (9
  expected failures, 4 skipped — convention), wt chain green (4/4 with-changes runs;
  chain is FLAKY on stock too — time-seeded RNG, failure point moves), fernhill
  499✓, nautical 7✓, grammar-alterations 6✓.
- ~~Pre-existing main blocker: CLI hangs on hatched Chord stories~~ **DIAGNOSED AND
  RESOLVED (same session)**. Causal chain: fix `e3ff8fb9` (2026-07-25, ADR-274 —
  `--external:esbuild` on the bundle step, written for THIS EXACT hang) landed in
  repokit's *source*, but the repokit engine runs from `tools/repokit/dist/`, last
  compiled Jul 23 — **the engine was never rebuilt after the fix**, so every
  `./repokit build` since re-inlined esbuild into `dist/cli/sharpee.js`. Inlined,
  esbuild's sync API spawns `new Worker(__filename)` = the whole CLI bundle instead
  of its service, and the main thread waits in `Atomics.wait` forever (the observed
  `futex_wait`, no child). It stayed invisible through Jul 26 because the
  `/tmp/sharpee-hatch` transpile cache was warm (cold path never ran); a tmp cleanup
  made it cold. **Fix applied: `pnpm --filter repokit build`, then `./repokit build
  dungeo`** — bundle shrank ~97KB (esbuild external again), cloak 81/81 and
  friendly-zoo 76/76 pass COLD (cache wiped first). No source change needed.
  **Recurrence risk to rule on**: nothing detects a stale repokit dist vs src — the
  shim (`./repokit`) could compare mtimes and refuse/warn, or the bundle step could
  assert the artifact contains no inlined `esbuild@` marker (the invariant
  e3ff8fb9's comment states, made executable).

## Phase 2 implementation (branch adr-276-p1)

- **`setting-schema.ts`** (story-loader): the Q-3 declarative table — trait adjective →
  setting key → `boolean | number | entity-ref | rooms`, with platform routing where
  the value lands on a trait. Covers combatant/weapon (16 keys), the five NPC
  adjectives (shared NpcTrait settings + routeless factory params), and the keyless
  v1 entity refs (openable.tool, lockable.key, cuttable/diggable.tool — census 6's
  enumeration). Declared `define trait` schemas stay IR-owned, deliberately.
- `COMBAT_FIELD_ROUTES` / `NPC_FIELD_ROUTES` become **derived views** of the schema
  (exports and shapes unchanged — conformance suite passes verbatim). Exported from
  the barrel for the Phase 5 generator.
- New `setting-schema.test.ts`: byte-for-byte pins of both derived views against the
  historical tables, schema ↔ chord-manifest agreement both directions, and the
  entity-ref enumeration pin.
- Deviation from plan text: the two ad-hoc entity-ref resolution sites
  (`entityRefFor`, `traitFieldValues`) keep their mechanics — converting
  `traitFieldValues` to schema lookup would have CHANGED behavior for declared
  traits (story-owned schema, not platform). Behavior preservation won.
- story-loader 422/422, root tsc clean, corpus byte-same vs Phase 1 baseline
  (zoo 76, cloak 81, fernhill, nautical, grammar-alterations, dungeo units green).

## Phase 3 implementation (branch adr-276-p1)

- **`repokit manifest`** (new command, ADR-269 D7 mechanism): generates the
  locale-keyed `packages/chord/src/stdlib-manifest.ts` (70 action ids from stdlib
  constants.ts SOURCE via the shared `readStdlibActionIds`; locale-neutral top
  level, `locales['en-US']` structurally ready for Phases 4/6). `--check` freshness
  gate wired into `repokit verify` AND the platform build (runs before chord
  compiles). Gate demonstrated: clean 0 → deliberate stale edit 1 → restored 0.
- **Census 1–2 compile-side**: `checkAlterationTargets` post-pass —
  `analysis.extend-target` (story-first order preserved exactly, declared in any
  order) and `analysis.removal-target` (standard-only), both with did-you-mean.
  The `analyzer.ts` "chord stays stdlib-ignorant" doctrine comment rewritten to
  cite ADR-276. Loader sites :1123/:1141 → backstops same commit.
- **Browser parity harness**: `devkit/src/standalone/manifest-browser-parity.test.ts`
  — the REAL runInitCommand → runBuildBrowserCommand chain fails loudly on a bad
  removal target naming the compile code; the BUILT chord dist (what game.js
  bundles) reports the same diagnostic; clean story still builds end-to-end.
  Recorded limitation: in-running-browser assertion still needs a headless
  browser (ADR-259 Phase F precedent). NOTE: the plan cited
  "packages/runtime/src/bridge.ts handleStart()" — that path doesn't exist; the
  real boot-compile is the devkit chord-browser-entry template, which is what
  the harness exercises.
- Two more latent-invalid fixtures found and fixed: chord grammar-alterations
  used `extend action writing` (no if.action.writing exists) → `answering`;
  story-loader's two source-driven census-1/2 rejection tests rewritten as
  rogue-IR backstops (they now fail at compile, by design).
- chord 638/638, story-loader 424/424, devkit 86/87 (1 skipped, pre-existing),
  root tsc clean, corpus green (fernhill count varies run-to-run — its
  `one chance in N` randomness; clean on re-run).

## Guards (same branch, commit 19ad572b)

- `./repokit` shim self-heals on stale engine (tsbuildinfo anchor, inline rebuild);
  bundle step asserts no inlined esbuild (ESBUILD_WORKER_THREADS marker) — ADR-274
  D1 made executable. Both validated positive + negative.

## Files modified

- `docs/architecture/adrs/adr-276-chord-source-authoritative-errors.md` (new, DRAFT)
- `docs/architecture/adrs/adr-258-ide-chord-authoring-environment.md` (re-assessment addendum)
- `docs/context/session-20260727-0430-main.md` (this file)
