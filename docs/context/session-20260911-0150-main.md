# Session Summary: 2026-09-11 - main

## Goals
- Wrap up whatever is needed for a Sharpee 5.3.2 publish (David's direction). The turbo `--dry=json` counting blind spot (I-748218-1) is David's own DevArch research track and is explicitly OUT of scope here.

## Phase Context
- **Plan**: No active plan — issue/release-driven work.
- **Phase executed**: N/A — no plan phase.
- **Tool calls used**: 121 (per `.session-state-fc5724.json`) — no budget assigned (no plan).
- **Phase outcome**: N/A.

## What 5.3.2 is — David's framing (2026-09-11)

**What started the effort: the Chord `playable` syntax had never been connected to the platform.**
ADR-327 D9 (2026-08-26) gave Chord `playable` and `change the player to <character>`; the analyzer
refused a non-`playable` target from that day on. The loader step that would write
`ActorTrait.isPlayable` from `irEntity.isPlayable` was never implemented, and the trait's class
default was `true` — so every actor in every story read as playable, and `switchPlayer`'s guard
(`game-engine.ts:793`) could never fire. The language said one thing and the runtime knew nothing
about it: exactly the two-sided alignment failure core-concepts names.

**Which led to several other seams** (David). Pulling on it produced four ADRs in five days:

| ADR | Seam it opened | Landed |
| --- | --- | --- |
| ADR-343 | `StoryEngine` role interface for `onEngineReady` | `eca36867a` |
| ADR-344 | The player role holder belongs at the install seam — D7 connects `playable` to the runtime and flips the default to `false`; D6 removes `EngineOptions.player` (the engine never receives a player at construction); D5 removes the loader's first-declared-room placement fallback | `f63455aaa`…`70096ec9a` |
| ADR-345 | The engine lifecycle phase becomes an explicit concept — found because ADR-344 Phase 2's last failing test was a symptom: D6a had made an `installStory` guard unreachable | `9d63352f9`…`b2981a776` |
| ADR-346 | Traits do not police completeness — `DoorTrait`/`ExitTrait` fields are optional while a story is still composing the passage | `4c1eacebb` |

The gate work that rode along (#400, #401, #404, #402, #410) is the second thread in this release:
`pnpm typecheck` became a real gate, test files entered it, seven packages' suites entered the
`test:ci` sweep, and engine tests stopped running on the wall clock.

**Commit count, corrected**: 21 commits, `ef0bbf49f` (the "PR #397 merged, Sharpee 5.3.1 published" record commit) through `effaff31a` (verified `git log --oneline ef0bbf49f..HEAD`, 2026-09-11) — not 23. `ef0bbf49f` is the true 5.3.1 publish point; `c855c8df8` (the in-PR version-bump commit) predates the merge and double-counts pre-merge history if used as the range start.

## Completed

### Pre-release gate run, at 5.3.1 (all green, pre-bump)
- `./repokit build dungeo` → exit 0
- `pnpm typecheck` → 77 successful, 77 total
- `pnpm exec turbo run test:ci --no-daemon` → 76 successful, 76 total
- walkthrough chain (`--test --chain stories/dungeo/walkthroughs/wt-*.transcript`) → 952 tests in 17 transcripts, 952 passed
- `pnpm test:scripts` → 3 files, 11 tests, all passed (event log `06:52:56Z`, pre-bump; not re-run post-bump — no files in its path changed)

### Chord corpus smoke check
All four Chord stories declare `playable` (fernhill, ides-of-march, secret-letter, thealderman — verified by grep on the `.story` files). `--exec "look"` boots and renders correctly for three:
- fernhill (`branch-stories/fernhill/fernhill.story`) — verified this session, fresh run
- ides-of-march (`branch-stories/ides-of-march/ides-of-march.story`) — verified this session, fresh run
- thealderman (`stories/thealderman/chord/thealderman.story` — not under `branch-stories/`, corrected path) — verified this session, fresh run

secret-letter fails `--exec "look"` with 2 `analysis.import-unresolved` errors (`peering`, `grubbers-market`) plus 9 cascading `unknown-entity`/`unknown-timer`/`missing-phrase`/`trait-not-declared` errors from content that only the unresolved `grubbers-market` import would have declared (verified this session, fresh run, full error list captured). This is **not** a regression from this session — filed as I-fc5724-2 with the corrected mechanism (see Key Decisions §3).

### The version bump
- `pnpm exec tsf version --bump patch` → 35 packages 5.3.1 → 5.3.2, verified by count: 34 `@sharpee/*` package.json diffs + `stories/dungeo/package.json` = 35 (`git status --porcelain`, counted directly)
- `./repokit build dungeo` stamped `packages/stdlib/src/actions/standard/version/engine-version.ts` (`ENGINE_VERSION = '5.3.2'`) and `stories/dungeo/src/version.ts` (`STORY_VERSION`, `ENGINE_VERSION`, `BUILD_DATE = '2026-09-11T06:55:32Z'`), and regenerated `packages/sharpee/docs/genai-api/` — `index.md`'s version line plus `world-model.md`'s ADR-346 `DoorTrait`/`ExitTrait` optional-field doc drift the prior session (748218) left uncommitted (diff verified directly: `from`/`to`/`command` now `?:`, constructor `data?:`, new doc comments citing ADR-346 D7)
- `node website/scripts/sync-versions.mjs` → `website/src/lib/versions.json` sharpee 5.3.2
- **Chord language version deliberately stays 3.6.0**: ADR-257 D2 — no author-visible syntax changed since 5.3.1 (`packages/chord` took only `package.json` + a `tsconfig.test.json` this cycle)
- Three non-private, unpublished packages stay OUT of the release train: `bridge`, `runtime`, `map-editor` — all at `0.9.113`, none bumped, none in `ts-forge.config.json`'s `projects` list (verified directly). GH #409 already carries the bridge/runtime question. (The prompt's fourth name, `extension-conversation`, does not exist anywhere in this repo — dropped, not carried forward.)

### Post-bump re-verification (this agent, fresh, 2026-09-11 ~02:04 CDT, after the version-bump edits)
- `pnpm typecheck` → 77 successful, 77 total (re-run directly by this agent)
- `pnpm exec turbo run test:ci --no-daemon` → 76 successful, 76 total (event log `06:56:46Z`, post-bump)
- walkthrough chain → 952 tests in 17 transcripts, 952 passed (re-run directly by this agent, `02:04:40–02:04:48 CDT`)
- `./repokit build dungeo` → exit 0, evidenced by the stamped `BUILD_DATE` in the diff (`06:55:32Z`, after all package.json bumps)

### GH #411 filed
The Dungeo **unit** transcript suite (`stories/dungeo/tests/transcripts/*.transcript`, 117 files, 1,746 assertions) is non-deterministic: four consecutive runs at the same 5.3.2 build gave 13, 20 and 18 failures (12, 14 at 5.3.1). 103 of the 117 transcripts carry no `seed:` header, so each boots a fresh engine on `config.seed ?? Date.now()` — the trapdoor GH #410 closed for the engine's vitest suites, one layer up. The walkthrough chain stays the baseline (state persists across one seeded session under `--chain`). The 2026-09-09 recorded baseline was 32 failed, so the suite has genuinely improved, but no figure is reproducible. Filed: https://github.com/ChicagoDave/sharpee/issues/411.

## Key Decisions

### 1. Publish is a separate, later step
This session's scope was "wrap up whatever is needed for a 5.3.2 publish" — version bump, generated docs, gate re-verification, corpus smoke check, and filing what the smoke check found. The actual `gh workflow run publish-npm.yml` dispatch is David's call and was not run.

### 2. Corrected the release commit count and the smoke-check causal story rather than carrying the seeded framing verbatim
Both were independently checked against `git log`, GH issue state, and live re-runs during this write-up (ADR-0019): the commit range base was off by including pre-merge PR history, and the secret-letter failure was attributed to a "known, not-a-regression" gap when it is in fact a fix (GH #352) that landed only on an unmerged branch — see Decision 3.

### 3. GH #352's fix lives only on `feat/secret-letter-port`, not main
GH #352 (bundle CLI has no Chord import resolver) was closed 2026-09-07 as fixed — `scripts/bundle-entry.js`'s `loadChordStory` wired to devkit's `makeFsImportResolver`. That fix is commit `d4f626f56`, which `git merge-base --is-ancestor` confirms is **not** an ancestor of `HEAD`; it lives only on `feat/secret-letter-port` (per project memory, on hold indefinitely since 2026-09-09). On main, `scripts/bundle-entry.js:319` still calls `chord.compile(source)` with no `importResolver` option at all. So secret-letter's `--exec` failure is expected given main's actual state, not a regression from this session or a currently-open gap in the sense GH #352 implies — the GH issue's CLOSED state is what's misleading while main lacks the fix. `./sharpee compose` (devkit) resolves imports fine, confirming the gap is bundle-CLI-specific, same as originally reported. Not release-blocking (secret-letter is a branch story, not in the release train); recorded as I-fc5724-2.

## Next Phase
No active plan — N/A.

## Open Items

### Short Term
- I-fc5724-4: Sharpee 5.3.2 is version-bumped and gate-verified locally but NOT published. Next step (David's call): `gh workflow run publish-npm.yml --ref main -f dry_run=true`, then the real dispatch.
- I-fc5724-1: GH #411 — 103 of 117 Dungeo unit transcripts carry no `seed:` header, so the suite's failure count is non-reproducible run to run (13/20/18 failures observed on one unchanged build). Fix shape: default the transcript runner's master seed to a fixed constant when none is declared, then re-bless goldens.
- I-fc5724-2: GH #352's fix (bundle CLI Chord import resolver) lives only on the unmerged, on-hold `feat/secret-letter-port` branch; main's bundle CLI still fails `--exec`/`--play` on any `.story` with an `import`. GH #352's CLOSED state does not reflect main.
- I-fc5724-3: **Filed as GH #412** — `ts-forge.config.json`'s `projects` array lists `packages/plugin-npc/tsconfig.json`, a directory that does not exist. `tsf` skips it silently (5.3.2 bump succeeded). Same stale-fiction family as GH #407's workspaces array. Not release-blocking. The issue also notes the four non-private packages absent from the same list (bridge, runtime, map-editor, extension-conversation, all at 0.9.11x, never published) are out by omission rather than by statement, and defers that half to GH #409.
- I-fc5724-5: CLOSED THIS SESSION — `docs/core-concepts/README.md` taught the removed `GameEngine({ world, player, ... })` option in two places (the package blurb at line 40 and the perception wiring example at line 587) after ADR-344 D6 removed it. Fixed in this session's commit: the example drops `player` and gains a paragraph naming where the role holder actually comes from. Worth noting as a pattern — the doc every session is required to read at start was three weeks stale on the API this release changed.

### Long Term
- N/A

## Files Modified

**Version bump — 35 packages** (34 `@sharpee/*` package.json + `stories/dungeo/package.json`):
- `packages/{bootstrap,branch-tester,channel-service,character,chord,core,devkit,engine,event-processor,extensions/basic-combat,extensions/chapters,extensions/hunger,extensions/scoring,extensions/testing,helpers,ide-protocol,if-domain,if-services,lang-en-us,media,parser-en-us,platform-browser,plugin-scheduler,plugin-state-machine,plugins,queries,sharpee,stdlib,story-loader,story-runtime-baseline,text-blocks,transcript-tester,world-index,world-model}/package.json` - version 5.3.1 → 5.3.2
- `stories/dungeo/package.json` - version 5.3.1 → 5.3.2

**Build-stamped** (2 files):
- `packages/stdlib/src/actions/standard/version/engine-version.ts` - `ENGINE_VERSION` 5.3.1 → 5.3.2
- `stories/dungeo/src/version.ts` - `STORY_VERSION`, `ENGINE_VERSION` → 5.3.2; `BUILD_DATE` → `2026-09-11T06:55:32Z`

**Generated docs regenerated by the build** (2 files):
- `packages/sharpee/docs/genai-api/index.md` - version line
- `packages/sharpee/docs/genai-api/world-model.md` - ADR-346 `DoorTrait`/`ExitTrait` optional-field doc drift from the prior session, now captured

**Website** (1 file):
- `website/src/lib/versions.json` - sharpee 5.3.2

## Notes

**Session duration**: ~01:34–02:10 CDT.

**Approach**: Verify the pre-existing 5.3.1 gate state, bump, re-verify post-bump, smoke-check the Chord corpus (the real proof of ADR-344 D7's `playable` connection), file what the smoke check found, and stop short of the actual publish dispatch.

**GH #410 is fixed by `124928ae2`** and its body says so, but the issue is still OPEN — awaiting David's call to close. Not recorded as a ledger item; it needs no future session's action, only David's.

**Format-check gap**: `docs/context/session-20260911-0058-main.md` (the immediately prior session) shows in `git status` as modified — this is `session-finalize.sh`'s own auto-appended Activity Log block, not a change made by this session. Left untouched.

**Open items ledger**: this session's carry-forwards are recorded via `devarch items` (ids I-fc5724-1 through -4 above); none of the pre-existing open items in the ledger overlapped with this session's findings.

---

## Session Metadata

- **Session**: fc5724
- **Status**: COMPLETE
- **Blocker** (if any): N/A
- **Blocker Category**: N/A
- **Estimated Remaining** (if incomplete): N/A
- **Rollback Safety**: safe to revert (uncommitted working-tree changes at time of writing — version bump and generated docs, not yet committed)

## Dependency/Prerequisite Check

- **Prerequisites met**: 5.3.1's gate state (typecheck, test:ci, walkthrough chain, `pnpm test:scripts`) was green before the bump, so the bump had a clean baseline to move from.
- **Prerequisites discovered**: None.

## Architectural Decisions

- None this session — ADR-343 through -346 were applied in prior sessions; this session only bumped versions and re-verified. No new ADR written.

## Mutation Audit

- Files with state-changing logic modified: none — this is a release-mechanics session (version stamps, generated docs, one GH issue). No production side-effect code was written; no Behavior Statement was owed.
- Tests verify actual state mutations (not just events): N/A.

## Recurrence Check

- Similar to past issue? YES. GH #411 is the same gate-blindness family as GH #400/#402/#408/#410, and as the prior session's (748218) Recurrence Check flagged — a check that reports a number it cannot actually stand behind. GH #410 sealed the trapdoor for engine vitest suites (`setupTestEngine` now defaults `TEST_MASTER_SEED`); GH #411 is the same trapdoor one layer up, in the transcript runner. Fifth occurrence in ~48 hours across four subsystems (typecheck scope, test:ci naming, test:ci counting semantics, transcript seeding). A systemic audit of every clock-seeded test entry point (not just this one) is warranted — David's call on scope and timing.
- I-fc5724-2 (GH #352 fix unmerged) is the same family as I-4a2d5f-1 (ADR-333 absent from main because it lives only on `feat/secret-letter-port`) — a fix or decision that exists only on the on-hold port branch and reads as done-on-main when it is not.

## Test Coverage Delta

- Tests added: 0 (no test files changed this session).
- Tests passing before (5.3.1) → after (5.3.2), both fresh relative to the last edit to their respective files:
  - `pnpm typecheck`: 77/77 → 77/77 (post-bump run performed directly by this agent, 2026-09-11 02:04 CDT)
  - `pnpm exec turbo run test:ci --no-daemon`: 76/76 → 76/76 (post-bump: event log `06:56:46Z`)
  - walkthrough chain: 952/952 → 952/952 (post-bump run performed directly by this agent, 02:04:40–02:04:48 CDT)
  - `pnpm test:scripts`: 3 files / 11 tests, pre-bump only (event log `06:52:56Z`; no files in its path changed by the bump, not re-run)
- Known untested areas: the Dungeo unit transcript suite (103/117 unseeded, GH #411) cannot serve as a coverage signal until it is seeded — its pass/fail count is not reproducible.

---

**Progressive update**: Session completed 2026-09-11 02:10 CDT
