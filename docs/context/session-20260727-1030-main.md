# Session Summary: 2026-07-27 (1030) - main [59006f]

**Goal**: ADR-258 Swift/Mac side — the IDE becomes a Chord authoring environment
(D1–D4 swaps, D6 tree UI, D7 `ChordLexer.swift` port against the committed
golden, D8 state migration, D9 language version). User pre-authorized running
autonomously ("run as much without questions").

**Status**: COMPLETE — ADR-258 fully IMPLEMENTED (Swift arc, 5 phases on branch
`adr-258-swift`, unpushed). Suite 213/213 green; every Acceptance bullet
verified by test or in-phase demonstration.

## Work done

- Pre-session audit: clean (tree clean, no stale artifacts; tsc shows only
  incremental mtime staleness). Two long-parked items flagged as recurring
  (scripts/*.cjs delete ruling; thealderman tsc) — user's ruling stands, not
  carried into this session.
- Verified Mac toolchain available: Xcode 26.4, xcodegen, swift. The
  lexer-golden fixture (`packages/chord/tests/fixtures/lexer-golden/`) and
  ide-protocol wire contract are in place from the platform-side phases.

## Work done (continued) — branch adr-258-swift

- Swift-side plan written (docs/work/adr-258-ide-swift/plan.md, 5 phases along
  the D3 swap table); plan-review's deletion-gate CONTRADICTION resolved on
  record: ADR-named files delete under David's blanket go-ahead, non-ADR-named
  files (PackageDetector, StoryDetector, ProjectManifest, IntrospectionRunner)
  rewrite/dormant only — removal deferred to an end-of-session question.
- **Phase 1 DONE (b7f770f2)**: compose --json Swift decoder (loud schemaVersion
  reject, probe-before-shape), supersedable ComposeRunner, debounced
  ComposeScheduler (hidden-sibling snapshot for unsaved buffers + record-site
  remap), Problems tab with exact-span click-through, editor span underlines.
  TSCDiagnostic + parser tests deleted (swap row b). 20 new tests incl.
  real-path vs node packages/devkit/dist/cli.js.
- **Phase 2 DONE (9ceac2b2)**: IR-sourced tree (swap row c) — ComposeStoryIR
  entities/actions, kind-based grouping (player under NPCs; Actions group =
  grammar-file tree), IRTreeState last-ok retention w/ stale marker, exact-span
  navigation; EntitySourceIndex deleted; introspect-driven tree population
  removed (IntrospectionRunner/ProjectManifest dormant).
- **Crash incident (user-visible)**: repeated "Sharpee quit unexpectedly"
  dialogs during test runs — two ComposeRunner bugs: (1) MainActor-inferred
  group.notify closure dispatched to a global queue → Swift 6 runtime isolation
  SIGTRAP; (2) terminationStatus read on a never-launched Process → ObjC throw.
  Both fixed (notify on .main with identity-guard-first); suite green with zero
  host restarts.
- **Platform bug found + fixed with David's approval (47e325e8)**: devkit
  cli.ts process.exit() truncated piped compose --json payloads at 64KB (node
  async pipe stdout; file redirects flush, so platform tests never saw it).
  Fix: process.exitCode; regression test pins the subprocess pipe path with a
  >64KB story. devkit 102 passed / 1 pre-existing skip.

## Key decisions

- Q1 (recorded in plan): production IDE resolves `sharpee` via login-shell
  PATH; tests drive the in-repo devkit CLI. Q2: New Story scaffold writes
  NO package.json (D2's letter). Q3: debounced ~400ms live compose.
- Unsaved-buffer compose via hidden sibling snapshot in the story's own dir
  (keeps import/hatch resolution correct); records remapped to the real file.

## Phases 3–5 (same session)

- **Phase 3 DONE (2c341089)**: ChordLexer.swift port (UTF-16 code-unit columns
  = JS span parity), golden conformance XCTest against the committed
  lexer-golden.json — deliberate-mismatch demo run and reverted (RED named
  alterations-counters.story:12); Chord-native SyntaxHighlighter (prose-noisy
  words excluded from the keyword set); tree-sitter GONE from project.yml.
- **Phase 4 DONE (904d4231)**: BuildRunner → PATH-resolved `sharpee build
  <file>.story`; WebBundle dist/web/<id>/ from the IR header id; Play pane
  bundle-dir API + grammar-file unplayable state; Build menu gated (no
  grammar, no missing story); Chord StoryScaffold (<id>.story only, NO
  package.json); BrowserEntry + npm/init-browser paths deleted. Real-path
  fernhill-copy build (linked node_modules simulating the ADR-180 U2 global
  install) → dist/web/fernhill/index.html, WebBundle resolves.
- **Phase 5 DONE (3e25a7a7)**: StoryTarget predicate; recents drop TS/vanished
  entries on load (D8); stale-session restore opens empty state with a
  one-line explanation; ChordVersionCheck launch warning (D9, supported
  2.1.0); README rewritten (Phase-0 claim gone). Alteration-error-with-span
  acceptance bullet pinned real-path.
- **ADR-258 flipped IMPLEMENTED** with a full implementation addendum
  (phases → commits, the devkit pipe-fix note, recorded limitations).

## Post-merge (same session)

- Q1 correction (David): the global author CLI IS shipped — @sharpee/devkit
  on npm carries the `sharpee` bin; the dev Mac just lacks the install.
  Resolution reordered workspace-shim-FIRST (in-repo stories track the local
  build — the published 4.1.1 still had the pipe bug), then PATH (d1b3f53b).
  Memory saved: global-sharpee-cli-shipped.
- **Sharpee 4.1.2 released** (9cf2790a): lockstep tsf version (33 packages),
  Chord unchanged 2.1.0; ships the devkit exitCode/pipe-flush fix stamped.
  repokit rebuild + restamps, checkpoint refreshed, playground re-pinned
  v4.1.2 (gitignored deploy artifact), nav 4.1.2. Gates: devkit 102, dungeo
  units baseline, cloak/zoo, IDE 216.
- **PR #186 merged to main** (a265ec93). npm publish + website deploy remain
  David's steps.

## Open items / next steps

1. ~~Branch push/PR/merge~~ DONE — PR #186 merged (a265ec93).
   ~~npm publish + website deploy~~ DONE (David) and VERIFIED: @sharpee/devkit
   and @sharpee/sharpee 4.1.2 on `latest`; the published cli.js carries
   `process.exitCode = code` (zero `process.exit(code)` — the pipe fix is
   live); sharpee.net shows 4.1.2 / 2.1.0; playground current.json → 4.1.2.
   4.1.2 is fully shipped end-to-end.
2. Question for David: removal ruling for the dormant repokit-era units —
   BuildSettings/BuildSettingsStore/BuildSettingsViewController,
   StoryDetector, PackageDetector, IntrospectionRunner, ProjectManifest
   (+ their tests). All compile and pass but have no live consumer.
3. devkit cli.ts exitCode fix (47e325e8) ships with the next release — npm
   publish not run this session.
4. Not done (recorded in ADR consequences, unchanged): macOS CI for the IDE
   suite; status-bar version label still reads "Sharpee 0.1.0" (pre-existing).
5. Manual GUI walk of the worked example (open fernhill, watch tree/Problems/
   Build/Play) — all bullets are test-verified; the visual pass is David's.
