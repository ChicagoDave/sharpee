# Session Summary: 2026-06-20 - main

## Goals
- Complete Phase 2 spine migration by writing the final 6 Family Zoo source chapters in canonical book format.
- Correct documentation drift found against ground-truth `familyzoo/src/v*.ts` source files.
- Fix tone in the preface comparison section (Inform 7 / Sharpee).

## Phase Context
- **Plan**: `docs/work/book/plan-20260620-sharpee-author-book.md` — *The Sharpee Book*
- **Phase executed**: Phase 2 — "Migrate the existing spine (Parts I–VI)", Batch 3
- **Tool calls used**: 62 / unbudgeted
- **Phase outcome**: Completed — all 6 remaining migration chapters written; no "pending migration" stubs remain in `parts/`; book renders clean to HTML + EPUB + PDF

## Completed

### ch12 — Readable Objects & Switchable Devices (`part-3/12-readable-objects-and-switchable-devices.md`)
Migrated from v09+v10. Covers `ReadableTrait`, `SwitchableTrait` as a standalone radio (distinct from openable), and the switchable-vs-openable design contrast.

### ch13 — Event Handlers (`part-4/13-event-handlers.md`)
Migrated from v12. Covers `registerEventHandler` vs `chainEvent`; reaction and transformation patterns. Drift corrected: chain handlers must return a CUSTOM event type (e.g. `zoo.event.goats_react`) with a `text` field — NOT `game.message`, which the event processor consumes as an override of the action's own text. The per-version docs showed the wrong pattern.

### ch14 — Custom Actions (`part-4/14-custom-actions.md`)
Migrated from v13. Covers the four-phase Action, `getCustomActions` / `extendParser` / `extendLanguage`, and `sharedData` passing between phases.

### ch15 — Capability Dispatch (`part-4/15-capability-dispatch.md`)
Migrated from v14. Covers `PettableTrait` with capabilities, unified behavior, and a hand-written dispatch action. Drift corrected: v14 uses ONE unified `pettingBehavior` dispatching on `PettableTrait.animalKind` (the registry allows one behavior per trait+capability) and a hand-written dispatch action — not separate per-animal behaviors with `condition`, and not `createCapabilityDispatchAction()` (noted as the stdlib shortcut, not used in the tutorial version).

### ch20 — Non-Player Characters (`part-6/20-non-player-characters.md`)
Migrated from v11. Covers `NpcTrait`, `NpcBehavior`, `NpcPlugin`, and `createPatrolBehavior`.

### ch22 — Turns, Timed Events & Daemons (`part-6/22-timed-events-and-daemons.md`)
Migrated from v15. Covers `SchedulerPlugin`, daemon and fuse patterns. Noted: scheduler daemon/fuse events DO use `game.message` + `narrate: true` (correct for scheduler output) — chapter explicitly notes the context difference from ch13's chain-handler correction.

### ch23 — Scoring & Endgame (`part-6/23-scoring-and-endgame.md`)
Migrated from v16. Covers `awardScore` idempotent ledger, `setMaxScore`, and the victory daemon pattern.

### Preface tone fix (`frontmatter/00-preface.md`)
Rewrote the Inform 7 / Sharpee comparison twice per user feedback. Final: direct and rational — names Inform 7's deliberate two-layer split respectfully, states Sharpee's real cost (no gentle natural-language on-ramp; code from line one). Removed "fall off a cliff / can't do this" punching-down language. TADS dropped (single-language; prior claim was inaccurate). Same fix applied to the plan's Goal section.

### Part-1 parallel-session chapters also committed
`01-installing-sharpee.md` and `02-your-first-room.md` (modified by a parallel session) were included in the commit per user confirmation.

## Key Decisions

### 1. chain handler event type (drift from per-version docs)
`chainEvent` must return a custom event type with a `text` field. Returning `game.message` is wrong for chain handlers — the processor treats it as an action-text override. Documented prominently in ch13 and cross-referenced in ch22.

### 2. Capability dispatch is always single behavior + hand-written action in v14
The registry enforces one behavior per trait+capability. v14 uses a single `pettingBehavior` with an internal `animalKind` switch, plus a hand-written dispatch action. `createCapabilityDispatchAction()` exists in stdlib but is not used in the tutorial — noted as a shortcut in the chapter.

### 3. Preface comparison principle
Comparisons to other IF systems are acceptable when direct and rational; punching-down language ("can't do this", "fall off a cliff") is not. TADS dropped from comparisons because its single-language model doesn't support the same contrast.

## Next Phase
- **Phase 3**: "Under-the-Hood (programmer) layer" — add API-anchored Under-the-Hood sections through migrated chapters, citing `@sharpee/*` public APIs/types from `packages/sharpee/docs/genai-api/`; decide and implement drift handling (lean: excerpt-and-verify against `.d.ts`).
- **Tier**: Unbudgeted (prose + API annotation work)
- **Entry state**: All Part I–VI migration chapters present; book renders clean; Phase 2 marked DONE in plan.

## Open Items

### Short Term
- David to write real part-divider blurbs (lorem placeholders remain in all 6 part-dividers).
- David to assign ornament images to chapters that still use the generic sword asset.

### Long Term
- Drift handling mechanism for Under-the-Hood API quotes — decide (a) excerpt-and-verify / (b) ref by symbol / (c) manual re-sync in Phase 3.
- Parts VII–VIII coverage: new zoo versions (v18+) vs teach-against-v17 — decide in Phase 5.

## Files Modified

**Book chapters — Part III** (1 file):
- `docs/book/parts/part-3/12-readable-objects-and-switchable-devices.md` — migrated from v09+v10

**Book chapters — Part IV** (3 files):
- `docs/book/parts/part-4/13-event-handlers.md` — migrated from v12; chain-handler drift corrected
- `docs/book/parts/part-4/14-custom-actions.md` — migrated from v13
- `docs/book/parts/part-4/15-capability-dispatch.md` — migrated from v14; unified-behavior drift corrected

**Book chapters — Part VI** (3 files):
- `docs/book/parts/part-6/20-non-player-characters.md` — migrated from v11
- `docs/book/parts/part-6/22-timed-events-and-daemons.md` — migrated from v15
- `docs/book/parts/part-6/23-scoring-and-endgame.md` — migrated from v16

**Front matter** (1 file):
- `docs/book/frontmatter/00-preface.md` — tone fix on Inform 7 comparison; TADS removed

**Plan** (1 file):
- `docs/work/book/plan-20260620-sharpee-author-book.md` — Phase 2 Batch 3 recorded; Phase 2 acceptance marked; Goal section comparison language updated

## Notes

**Session duration**: ~3 hours (62 tool calls)

**Approach**: Reconciled every code block against `familyzoo/src/v*.ts` ground truth; folded in "Common mistake" callouts from `tutorial.md`; followed the ch8 exemplar voice. Four documentation drifts found and corrected in the canonical text (and in plan notes) so the pattern isn't picked up again in Phase 3.

---

## Session Metadata

- **Status**: COMPLETE
- **Blocker**: N/A
- **Blocker Category**: N/A
- **Estimated Remaining**: N/A
- **Rollback Safety**: safe to revert

## Dependency/Prerequisite Check

- **Prerequisites met**: Phase 1 pipeline operational; Part I–VI skeleton and batch 1+2 chapters in place; `familyzoo/src/v09…v16.ts` available as ground truth.
- **Prerequisites discovered**: None.

## Architectural Decisions

- None this session (documentation-only; no platform code changed).

## Mutation Audit

- N/A — documentation-only session; no state-changing code modified.

## Recurrence Check

- NO — documentation drift corrections are content-specific; no recurring code-level pattern.

## Test Coverage Delta

- No test changes this session.

---

**Progressive update**: Session completed 2026-06-20 23:40 CST

---

## Addendum — Follow-on work (2026-06-21)

### Changes made after the Batch 3 commit

#### Preface tone fix (second pass)
The "Two perspectives, one example" comparison and the plan's Goal section were rewritten to be direct and rational without punching down. Named Inform 7's deliberate two-layer split respectfully; stated Sharpee's own cost (no natural-language on-ramp; code from line one). Removed "fall off a cliff / Inform and TADS can't do this." Principle: comparisons are fine when factual and even-handed, never when diminishing.

#### Part → Volume rename
Reader-facing label changed from "Part" to "Volume" across all six divider headings and two prose cross-references in the book. Internal CSS hooks (`.part-page`, `.part`, class attributes) and the `part-N/` folder names were left unchanged — only the displayed label changed.

#### Sword decoration removed from dividers
The sword image was removed from all six volume dividers. Decoration (sword, ornaments, title art) is deferred to an end-of-book holistic pass per David. The sword CSS selector remains in `book.css` / `epub.css` / `print.css` but is now inert.

#### Public-domain poem epigraphs added to all six volume dividers
One poem per volume divider, all firmly public domain:

| Volume | Poet | Poem |
|--------|------|------|
| I | Rudyard Kipling | "If—" |
| II | Emily Dickinson | "I dwell in Possibility –" |
| III | Edwin Markham | "Outwitted" |
| IV | W. E. Henley | "Invictus" |
| V | Lewis Carroll | "Jabberwocky" (first stanza, excerpt — David approved) |
| VI | William Shakespeare | "All the world's a stage" from *As You Like It* (excerpt — David approved) |

A Jane Hirshfield poem considered for one volume was identified as in-copyright and NOT used.

#### CSS: heading sizes, PDF margins, poem layout
- Chapter/section heading sizes reduced: `h1` 1.9 → 1.5rem, `h2` 1.4 → 1.2rem, `h3` 1.15 → 1.05rem.
- PDF page margins tightened to `0.5in 0.6in` (technical reference density).
- `.has-poem` verse-divider layout added: top-aligned so a long poem never clips; sized to fit on one page.
- Fixed a real bug: the `100vh` flex-centered divider silently clipped an overflowing poem's final stanza. Fixed by switching to top-aligned layout.

### Files modified in this addendum

- `docs/book/parts/part-1/01-installing-sharpee.md` — Volume I divider: Part→Volume rename, sword removed, Kipling epigraph added
- `docs/book/parts/part-2/04-rooms-and-navigation.md` — Volume II divider: Part→Volume rename, sword removed, Dickinson epigraph added
- `docs/book/parts/part-3/10-standard-actions-and-the-four-phase-model.md` — Volume III divider: Part→Volume rename, sword removed, Markham epigraph added; cross-ref updated
- `docs/book/parts/part-3/12-readable-objects-and-switchable-devices.md` — minor cross-ref update (Part→Volume)
- `docs/book/parts/part-4/13-event-handlers.md` — Volume IV divider: Part→Volume rename, sword removed, Henley epigraph added; cross-ref updated
- `docs/book/parts/part-4/14-custom-actions.md` — minor cross-ref update (Part→Volume)
- `docs/book/parts/part-5/17-extending-the-grammar.md` — Volume V divider: Part→Volume rename, sword removed, Carroll epigraph added
- `docs/book/parts/part-6/20-non-player-characters.md` — Volume VI divider: Part→Volume rename, sword removed, Shakespeare epigraph added
- `docs/book/styles/book.css` — heading size reductions; `.has-poem` top-aligned layout; sword selector inert
- `docs/book/styles/epub.css` — poem layout additions; sword selector inert
- `docs/book/styles/print.css` — tighter PDF margins; poem layout additions; sword selector inert

### Verification
`scripts/build-book.sh` renders clean to HTML + EPUB + PDF (only pre-existing CSS warnings). All six poem dividers verified on one page each. No lorem placeholders remain in dividers.

**Progressive update**: Addendum completed 2026-06-21 CST
