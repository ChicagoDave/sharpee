# Open-items ledger triage (ADR-0042 Decision 11)

Generated 2026-09-11T16:02:01Z by `devarch doctor --sweep-ledger`.

This file is **evidence, not a plan**. Nothing here has been filed. Tick an item to
mean "this is still real" and open it as an issue yourself; leave it unticked to let
it die with the ledger. When the list is triaged, delete the ledger(s) it read.

Forge resolution: forge `ChicagoDave/sharpee` consulted via `gh` — `open` means the number is open right now; `not open` means closed OR never existed, which this cannot tell apart and you must.

## `docs/context/.open-items.jsonl`

- [ ] `I-c8a56c-1` [Other, session c8a56c] ON HOLD (David, 2026-09-09: the Secret Letter port is on hold indefinitely). David's lines are outstanding for the placeholder beats added in Chapters 6-8: the street beat and house beat (Ch 6), the square's two beats (Ch 7), the street's two beats (Ch 8); plus loose ends — Estelle's name, Shannon's converted-portrait line, jump/hide/sing away from the closet (no source line), how she leaves a mercenary-full square, FI25's weight, the two dead Red Gate rooms, and one other loose thread. Not actionable until the port resumes.
- [ ] `I-3da76c-2` [Test Infrastructure, session 3da76c] ON HOLD (David, 2026-09-09: the Secret Letter port is on hold indefinitely). branch-stories/secret-letter/secret-letter.recipe.json carries `"spine": []` and `"branches": []` (verified 2026-09-09, 189 bytes); scripts/make-story-artifacts.mjs would replace the 1468-card tree with an empty one if run against it. If the generated-tree path is ever wanted, the recipe needs a spine and branches first. Not actionable until the port resumes.
- [ ] `I-7f0471-2` [Architecture, session 7f0471] Package-by-package refactoring survey (David: 'one at a time') is paused, not finished. Remaining queue: devkit, platform-browser, if-domain, core, world-index, event-processor, channel-service, ide-protocol, media, helpers, plugin-state-machine, bridge, bootstrap, plugin-scheduler, queries, runtime, plugins, text-blocks, extensions/testing.
- [ ] `I-4a2d5f-1` [Documentation, session 4a2d5f] ON HOLD (David, 2026-09-09: the Secret Letter port is on hold indefinitely). ADR-333 (prose provenance + play-to-write) is absent from main — main's ADRs run 332 then 334 (verified 2026-09-09). It lives only on origin/feat/secret-letter-port, added by 03d8ce426 alongside three story-loader tests (adr-333-descriptions-id-mode, adr-333-provenance-stamp, adr-333-source-facts) and an archived plan. Landing it on main is a decision for whenever the port resumes, not a gap to close now.
- [ ] `I-71ed1a-3` [Build / Toolchain, session 71ed1a] GH #391: incremental CJS tsc leaves dist/**/*.d.ts stale (fresh mtime, old content), so an ordinary ./repokit build regenerates packages/sharpee/docs/genai-api/ with removed API re-added. Workaround until fixed: tsc --build --force on any touched package (and its dependents) before trusting the generated API reference. — #391=open
- [ ] `I-71ed1a-4` [Test Infrastructure, session 71ed1a] mutation-verification (session 71ed1a, after ADR-337 D1 landed) flagged that stdlib's dialogue-selector-socket.test.ts and adr-231-d4-topic.test.ts still call the asking/telling action phases directly rather than through CommandExecutor.runPhases — harmless today since neither test exercises a hook path, but worth revisiting if asking/telling's runsOwnHooks contract changes.
- [ ] `I-13688e-1` [Build / Toolchain, session 13688e] A package subpath consumed through another package's .d.ts needs typesVersions (not per-consumer tsconfig paths) under moduleResolution: node; discovered when the first Phase 9 gate build failed devkit's tsc with an any-typed import. Also: tsc --noEmit after a package.json-only change can replay stale diagnostics from tsconfig.tsbuildinfo -- verify with a clean tsbuildinfo before trusting a pass.
- [ ] `I-d99d6d-2` [Type System, session d99d6d] CORRECTED 2026-09-09 by direct read: `parser` is no longer optional — turn/context.ts:109 declares `readonly parser: EngineParser`. Only `textService` (:113) and `languageProvider` (:114) remain `T | undefined`, and five guards test them, not eight: channel-packet.ts:51, input-mode.ts:38, meta-render.ts:26, render-prose.ts:30 and :49. (parse.ts and command-history.ts carry none.) Dead for the same reason the start() guards Phase 5(a) removed were; a natural rider on turn-package work.
- [ ] `I-d99d6d-3` [State Management, session d99d6d] CORRECTED 2026-09-09 by direct read: five ending-event constructors overwrite session.endTime with their own Date.now() read, not three — packages/core/src/events/game-events.ts:367, 384, 401, 417, 434. So the engine's single Date.now() read (Phase 5(b)) is the ending event's alone; the shared record still carries startTime, turns, and moves to both. Core is outside ADR-334's scope; noted for a future core-package look.
- [ ] `I-d99d6d-4` [Other, session d99d6d] turn-event-processor.ts:92 and action-context-factory.ts:125 carry the same Math.random().toString(36).substr(2, 9) id idiom that Phase 5(e) removed from game-engine.ts's emitPlatformEvent; out of that file's scope, worth the same counter-based fix later.
- [ ] `I-a87c9a-1` [Build / Toolchain, session a87c9a] GH #396: entropy allowlist drift found while repointing Phase 7's moved engine entries -- two modules unlisted, four stale (none at moved paths), pre-existing. David's call on skipping stories/_archive/ and repointing the character pair. — #396=open
- [ ] `I-a87c9a-2` [Architecture, session a87c9a] Phase 7 findings not acted on, riders for Phase 8: (i) ports/vocabulary-manager.ts header still says 'Extracted from GameEngine as part of Phase 4 remediation' -- the history-comment class Phase 5(g) stripped elsewhere; (ii) install/narrative-settings.ts (the step) and install/narrative/narrative-settings.ts (the type module) now share a basename one directory apart -- may read badly; (iii) new directories carry no barrel of their own -- root index.ts still names every module directly; which directories get an index.ts is Phase 8's decision.
- [ ] `I-31861b-1` [Build / Toolchain, session 31861b] release-all.sh/package.sh should treat a notarytool submit without a printed 'Successfully uploaded file' as failed and retry the upload immediately, instead of polling a dead In-Progress id (recurrence: 5 sessions since 2026-08-10)
- [ ] `I-31861b-2` [Build / Toolchain, session 31861b] GH #358: sharpee.net /playground and /play serve a stale Chord 3.0.0 compiler (repokit build deadlocks in runManifestStep when dist is present but stale, only on plover); fix is one of four proposed options, David's to apply on his Mac — #358=not open
- [ ] `I-31861b-3` [Architecture, session 31861b] Five DRAFT ADR amendments (ADR-118 Amendment 1, ADR-267 Amendment 1, ADR-320 D2a, ADR-325 Z6, ADR-225 one-input expiry) remain unaccepted; publish-readiness plan Phase 6a (P-11) stays PENDING on ADR-118 A1 and Phase 8 stays PENDING on ADR-267 A1
- [ ] `I-31861b-4` [Other, session 31861b] website/public/downloads/ holds 14 loose ChordWriter DMG/zip files plus an untracked chord-writer/ Sparkle appcast+delta tree, all untracked; whether the appcast XML and Sparkle feed should be tracked in git is undecided
- [ ] `I-7776c8-1` [Test Infrastructure, session 7776c8] GH #398 filed. Real-path test gates use artifact PRESENCE as a proxy for FRESHNESS: absent artifact skips (green on a dev machine), stale artifact runs and fails (red on a deploy host). Sites: packages/channel-service/tests/ac-14-browser-real-path.test.ts:32 (dist/web/dungeo/index.html) and ac-13-cli-real-path.test.ts:16 (CLI bundle + walkthrough). Evidence 2026-09-10 on the laptop at main: `test` and `test:ci` both exit 0, 119 passing / 2 skipped -- the 2 are ac-14's, skipped because dist/web/dungeo is not built here. Corrects commit 3e460d3b1's override trailer, which called them '2 pre-existing failures on main': not reproducible on main elsewhere, so host-conditioned. Second plover commit blocked this way (I-31861b-1 is the first); both landed with --no-verify. Proposed fix in the issue: stamp artifact provenance at build time and SKIP a stale artifact with a rebuild reason rather than testing against it. — #398=open
- [ ] `I-cbdf63-1` [Integration Reality, session cbdf63] GH #399: no real-path test exercises the revive seam — branch-tester's tree walker calling game.reviveEngine?.() (tree-walker.ts:360) through bootstrap's reviveEngine() (bootstrap/src/index.ts:377-379) into engine.resume(). branch-tester's stub game (tests/tree-walker.test.ts:83-110) omits reviveEngine entirely, so the call optional-chains to undefined on every test line in that package; the engine's own tests call resume() directly, bypassing the walker. This is the exact caller ADR-345 D8a's playing-tolerance was written for (pinned engine-side by AC-9), never observed through it. Out of scope for ADR-345; branch-tester work. Filed unlabeled — a parking decision (e.g. testing-ux-revamp) is David's call, not filed as a side effect. — #399=open
- [ ] `I-379680-1` [Test Infrastructure, session 379680] GH #401: mechanism built (session 379680) — 31 tsconfig.test.json + turbo typecheck:tests task + ratchet (18 packages with zero test errors now gate on tests too). True error total 1,500 (not 1,574 — ESM/CJS harness noise for import.meta). 589 (39%) are one pattern: ITrait excess-property checks on entity.add({...}) object literals, fixable per trait type with no platform change. 15 real-drift errors fixed across 9 packages. Burn-down continues: the pattern piloted on packages/stdlib/tests/unit/actions/locking-golden.test.ts (40 literals converted to real trait constructors) took stdlib from 911 to 872 test-type errors, and surfaced ADR-346 (DoorTrait/ExitTrait constructors no longer throw on incomplete data — two fixtures had been silently asserting against a door that connected nothing). Remaining as of the pilot: stdlib 872, plus world-model 210, engine 129, story-loader 66, character 47, queries 32, parser-en-us 27, platform-browser 14, media 14, transcript-tester 13, if-domain 10, core 10, ext-hunger 1, ext-scoring 1. Issue left OPEN for the burn-down. — #401=open
- [ ] `I-379680-3` [Build / Toolchain, session 379680] GH #406: stories/armoured (a retired, deliberately unrunnable sample per project note) is not a workspace member, so its tsconfig.browser-check.json guard has no command wired to it; whether to wire it is a scope call, not urgent. — #406=open
- [ ] `I-379680-4` [Build / Toolchain, session 379680] GH #409: packages/bridge and packages/runtime have no consumer anywhere in the repo (not depended on by anything but themselves; not spawned by the macOS IDE — PlayViewController.swift's 'bridge' is WebView JS-bridge terminology; vendor-toolchain.sh vendors devkit and platform-browser only). Still actively edited by sweeps (10 and 12 commits/90d). Filed as a question, not a bug. Deliberately not added to repokit's PLATFORM_PACKAGES while open. — #409=open
- [ ] `I-379680-5` [Test Infrastructure, session 379680] GH #402: packages/extensions/basic-combat has 2 pre-existing failing tests (tests/npc-attack-through-attacking.test.ts:127, 2 failed / 30 passed), confirmed pre-existing by stashing the #401 change and re-running. The package declares 'test' (bare vitest) and no 'test:ci', so turbo run test:ci never reaches it and the red has been invisible. Concrete evidence for #402's invisible-red claim; commented on the issue, left open. — #401=open #402=not open
- [ ] `I-379680-6` [Type System, session 379680] ext-hunger and ext-scoring each carry 1 test-typecheck error under GH #401: both pass createSeededRandom(1) where a RandomService is wanted, and SeededRandom does not implement resolve. Not a typo — a question of which abstraction TurnPluginContext should take. Deliberately left unfixed pending that design call; belongs to the #401 burn-down. — #401=open
- [ ] `I-379680-8` [Test Infrastructure, session 379680] GH #410 left deliberately open: seeding setupTestEngine() by default makes an unseeded draw harmless, not visible -- a test that reaches for randomness without thinking about seeds now silently gets a good one instead of the wall clock. The stronger form (engine throws when an unseeded instance actually draws) would catch that class at the source, at the cost of churn in any non-test caller relying on the config.seed ?? Date.now() fallback. Needs a decision. — #410=open
- [ ] `I-748218-1` [Build / Toolchain, session 748218] turbo run test:ci --dry=json counts a missing test:ci script as a successful <NONEXISTENT> task, so the sweep's task-count cannot distinguish 'passed' from 'had no tests'. GH #402 persisted because of this blind spot (if-domain's 6 files had no runner and the counter never dropped). Ten packages (bridge, if-services, plugin-scheduler, plugin-state-machine, runtime, sharpee, cloak-of-darkness, concealment-test, text-blocks, vscode-ext) are in this state today with zero test files -- verified nothing is currently hidden -- but the gate structurally cannot detect a future regression. Same recurrence pattern as I-52e228-1 (gate reading green while structurally incapable of observing what it was meant to catch). Left for David to rule on: ADR or issue. — #402=not open
- [ ] `I-fc5724-1` [Test Infrastructure, session fc5724] GH #411: 103 of 117 Dungeo unit transcripts (stories/dungeo/tests/transcripts/) carry no seed: header, so each boots a fresh engine on config.seed ?? Date.now() — same trapdoor GH #410 closed for engine vitest. Four consecutive runs at the same 5.3.2 build gave 13/20/18 failures out of 1,746 assertions; the walkthrough chain is unaffected (--chain boots one seeded session). Fix shape: default the transcript runner's master seed to a fixed constant when no seed: is declared, then re-bless goldens. — #410=open #411=open
- [ ] `I-fc5724-2` [Integration Reality, session fc5724] GH #352 was closed 2026-09-07 as fixed (scripts/bundle-entry.js's loadChordStory wired to devkit's makeFsImportResolver), but that fix (commit d4f626f56) landed only on feat/secret-letter-port, which is on hold and never merged to main. On main, scripts/bundle-entry.js:319's loadChordStory still calls chord.compile(source) with no importResolver — verified 2026-09-11: node dist/cli/sharpee.js --exec look --story branch-stories/secret-letter/secret-letter.story still fails with 2 analysis.import-unresolved errors (peering, grubbers-market) plus 9 cascading unknown-entity/trait errors. Not a regression from this session's work; GH #352's CLOSED state is misleading while main lacks the fix. ./sharpee compose (devkit) resolves imports fine. — #352=not open
- [ ] `I-fc5724-3` [Build / Toolchain, session fc5724] ts-forge.config.json's projects array lists packages/plugin-npc/tsconfig.json, but packages/plugin-npc does not exist in the repo (verified 2026-09-11). tsf apparently skips the missing entry silently — 5.3.2 published (locally bumped) without error. Same stale-fiction family as GH #407's workspaces array. Not release-blocking. — #407=open
- [ ] `I-aceb2e-1` [Architecture, session aceb2e] ADR-347 ('The ending is an explicit concept') is ACCEPTED (20/20 READY FOR IMPLEMENTATION) but unimplemented — Phases 2-5 of docs/work/meta-commands-work-after-ending/plan.md are PENDING. Phase 4 must amend ADR-345 (D15's text and D9) in the same commit per ADR-347 D4b, gating AC-8's retirement of Phase 1's stopped-to-playing resume patch.
- [ ] `I-aceb2e-2` [Test Infrastructure, session aceb2e] GH #415 filed: transcript-tester (command-core.ts:525) and branch-tester (tree-walker.ts:332) detect a stopped engine by string-matching 'Error: Engine is not running', a message ADR-345 retired in 5.3.2 — both detection branches are dead since. Ruled out of ADR-347's scope by its D4a; unfixed. — #415=open

