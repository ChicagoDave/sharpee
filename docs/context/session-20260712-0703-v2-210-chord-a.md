# Session 2026-07-12 07:03 — v2-210-chord-a

## Goal

Zoo-surfaces package Phase 1: ratchet-entry drafts honoring David's
recorded owner answers (Z1 approved; Z2 → plain phrases with comma
strategies; Z3 → occupant lifecycle key family entered/present/exited/
disappeared; Z3b + §3.5 approved). David: "proceed with the ratchet
drafts" after re-confirming the answers in conversation.

## Done

- Pre-session audit clean (no blockers; known open items: `cant`
  dispatch-miss message deferred twice, browser `{br}` fidelity check,
  select-in-each decision keys, loader kind set).
- **Verification pass before drafting** (findings recorded in the doc):
  - Z2 needs zero new grammar — `define phrase, <strategy>` + `or`
    variants shipped; descriptions already compile as IR phrases, so
    `{key}` markers in room prose are already checkMarkers-validated.
  - Two Choice keyspaces: statement-emitted phrases key
    `('chord', key)` (runtime.ts:1233); ADR-209 snippets key
    `(roomId, marker)`. Z2 compiles marker sites onto ADR-209.
  - The frozen gate files never observe the `mentions` gate (pins are
    scenery, can't leave the room) → deferral is a legitimate option.
  - Z3 lifecycle needs zero platform touch — occupant relocation is
    loader-internal (`move`, runtime.ts:937); `present` rides shipped
    ADR-195 S1.
  - Spelling collision: zoo.story's dormant blocks say `presence`,
    David's family says `present` → one-word ×4 story migration (CP3).
- **Z2 CLARIFIED by David after first draft**: his concern was the
  joining comma itself — the author writes BARE fragments and the
  evaluator owns the `, ` stitch (ADR-195 punctuation-authority
  precedent). Verified all shipped snippet entries are comma-led → one
  join rule (mid-sentence marker + non-empty fragment → `', '`;
  empty → nothing) reproduces today's output byte-identically. Z2 entry
  rewritten: loader supplies the separator at compile time; a
  punctuation-led fragment at a marker site is a load error.
- **Drafted `docs/work/chord-zoo-surfaces/ratchet-drafts.md`**: four
  PROPOSED entries (Z1 first-time prose; Z2 snippets-as-plain-phrases
  with the compile-to-ADR-209 contract + strategy/selector mapping +
  `nothing` empty entry; Z3 four-key lifecycle family, witnessed-only
  per D11, platform-pulled; Z3b detail-while compiling to the D1 pair)
  + IR wire-type note + seven checkpoints (CP1 mentions home — defer
  recommended; CP2 `nothing` spelling; CP3 key spellings + adverbs
  later; CP4 all-four runtime vs present-only; CP5 Z3b generality —
  D1-pair recommended; CP6 gate mechanics; CP7 sequencing).

## PIVOT: backtrack to ADR-209 (David, same session)

- David: CP1's mentions dilemma → "backtrack to 209 and rethink the
  entire logic" → "we can't answer any of these questions without a
  deep dive and a new ADR." Ratchet drafts marked ON HOLD.
- **Three-agent deep dive** (ADR-209 implementation map + story
  inventory; phrase-algebra joining/selection machinery; Chord
  condition kit). Decisive findings: 209's machinery ALREADY rides the
  phrase system (Sequence + Choice + textState; provisional Seq never
  added); clause-mode slots already comma-join via joinParts (the
  platform's sole comma-inserter); `mentions` gate = the existing
  `is-in` containment check; `while` has 3 grammar hosts; descriptions
  compile to static strings but the live render path exists; NOT all
  shipped fragments are comma-led — dungeo uses sentence fragments
  (leading space), zoo/concealment use clause fragments → the join
  rule must be SITE-determined (mid-sentence → `, `; post-terminator →
  ` `), which reproduces the whole corpus byte-identically.
- **Drafted `docs/architecture/adrs/adr-211-description-variety-
  phrase-system.md`** (DRAFT/PROPOSED): bare fragments, platform-owned
  separators via the site rule, gates ride the condition kit with
  `mentions` demoted to compile target of presence `while`, one
  selector mapping, ADR-209 machinery/keying/storage all stand.
  7 open questions w/ recommendations (TS-scope unification
  recommended; gate seam deferred; `is in <room>` now, `here` deictic
  later). Migration inventory: dungeo 9 rooms/11 entries + 2 handler
  mutations, zoo pins, concealment-test harness — all mechanical,
  byte-identical.

- **adr-review round 1 run + applied on ADR-211** (David-requested):
  initial NEEDS WORK 5/13 — the gaps were all contract-shaped
  (separator layer unassigned → now lang-en-us owns classification +
  insertion via a new if-domain `Spliced` atom; no touched-packages
  list → added, 9 packages; atomic chord compile contract + ordering
  before engine validateRoomSnippets → added; adjacent-marker /
  boundary / messageId edge rules → added; AC-9/AC-10 → added).
  Post-fix 12/13; the remaining item is Q1–Q7 (owner decisions, by
  design).

## Status

- Ratchet drafts PROPOSED, awaiting David's per-entry approval +
  CP1–CP7 answers. No code touched. Nothing committed this session yet.

## Next

- David reviews drafts → approved entries transcribe into
  chord-grammar-changes.md → package plan (chord-zoo-surfaces) →
  implementation phases.
