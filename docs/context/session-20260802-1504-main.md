# Session Summary: 2026-08-02 - main (session 72111f)

## Goals
- Return to ADR-293 Phase C (forcing, point-seed, coverage, trace, search budget, occurrence indexing) — plan first, discuss with David before platform implementation.

## Phase Context
- **Plan**: `docs/work/adr-293-phase-c/plan.md` (being drafted by session-planner)
- **Phase executed**: none yet — planning stage.

## Completed
- Session start: recap presented (ADR-296 complete, merged via PR #212; GH #208 closed; the three non-dungeo transcript failures already filed as GH issues), pre-session audit clean.
- Oriented on ADR-293 state: Phase A merged (PR #205), Phase B landed (1bc77944), ADR-294 tester rebuild shipped on top (118cd95a) and pre-rules Phase C priorities (forcing first, CLI first, `--coverage` over NDJSON wire; `seed:` header field replaces `[SEED:]` body directive).
- Launched session-planner for the Phase C plan (status PENDING-DISCUSSION until David approves).

## Key Decisions
- All five Phase C open decision points ruled by David (2026-08-02), each on the plan's recommendation: (1) `forces:`/`point-seed:` header fields, not body brackets — AC-9/AC-11 wording restated accordingly; (2) forcing covers `chance()` + `resolve()` only — a future forceable `int()`/`pick()` is expressed by rewriting to `resolve()`; (3) coverage-manifest verify gate DEFERRED — plan ends at Phase 5; (4) typed `IRandomTraceData` + `Subsystems.RANDOM` in core; (5) search tool is in-process world-fork, never subprocess-per-try.

## Phase 1 progress (started after David's green light)
- Branch `adr-293-phase-c` cut from main; plan Phase 1 marked CURRENT.
- **core**: `Subsystems.RANDOM`; new `random/force.ts` (`RandomForceSpec`/`RandomForceMode`/`RandomForceStatus`, `forceKey`, typed load errors `DuplicateForceKeyError`/`UnknownForcePointError`/`UndeclaredForceClassError` under `RandomForceLoadError`); new `random/trace.ts` (`IRandomTraceData`, `RandomTraceSink`); barrel exports.
- **engine**: `EngineRandomService` gains force table (`loadForces` with catalog validation, `clearForces`, `getForceReport`), occurrence counters, indexed-beats-unindexed force matching, forced `chance` (yes/no bijection) and forced `resolve` (`materialize` wired for real) both consuming zero draws, point-seed overrides in `streamFor`'s derivation slot (restored-state > override > derivation), trace emission with a draw-counting wrapper (built only when a sink is installed); `GameEngine.setRandomTraceEnabled` routes records onto `systemEventSource` (subsystem `random`, type `draw`, severity `debug`), off by default.
- **Tests**: 20 new tests in `packages/engine/tests/engine-random-service-forcing.test.ts` (load rejections, zero-draw AC-10 assertions, occurrence indexing incl. the AC-9 wound-spiral shape, D11 bystander-untouched, trace shape/provenance/silence, D9 session-state boundaries).
- **Verification**: core 176 green, engine 598 green, `./repokit build dungeo` green, walkthrough chain 952/952 green.

## Phase 2 progress (continue approved)
- Phase 1 committed as 03dbd077.
- **parser**: `forces:` parses `point[#occurrence]=CLASS` into canonical strings + once-mode specs (malformed/duplicate-key/zero-occurrence are parse errors); new `point-seed:` header field (`point=seed` pairs, validated).
- **golden**: optional `point-seeds` provenance key — written only when non-empty, absence parses as none, so all pre-Phase-C recordings stay byte-valid.
- **runner**: `configureRandomInstruments` (reset+load forces, catalog-validate point-seed names, apply overrides, enable trace) before tier dispatch; unfired-`once`-force hard failure in both tiers, blocking bless; `point-seeds` staleness check; typed load errors surfaced as `file:line` error-status results.
- **Fixture**: `stories/dungeo/tests/transcripts/force-forest-ambience.transcript` + golden — occurrence-indexed forces (#1=yes, #2=no, #3 natural) through the real bundle, blessed and replayed green twice at seed 4242.
- **Verification**: tester 179 green, dungeo corpus 1729 green, walkthrough chain 952/952, `./repokit build dungeo` green.

## Phase 3 progress (continue approved)
- Phase 2 committed as 5dc5ecf8.
- **ide-protocol**: `CoveragePoint`/`CoverageRecord` + `isCoverageRecord` guard, additive at schema version 1 (emission opt-in via `--coverage`); both dist and dist-esm rebuilt (dist-esm staleness caught by tests — tsf builds CJS only).
- **transcript-tester**: new `coverage.ts` (`CoverageTracker` — record/collectFrom/buildReport with D2/A1 first-segment prefix filter; summary + breakdown formatters); `coverageRecord` builder in aggregate; runner feeds the tracker from `system.draw` events in both tiers; `RunnerOptions.coverage`.
- **CLIs**: bundle `--coverage` (summary always prints per D15; breakdown on flag); package CLI same + `coverage-<ts>.json` in `--output-dir`; devkit `sharpee test --coverage` (human breakdown, or NDJSON coverage record before run-end in `--json`).
- **Real-path evidence**: wt-* chain 952/952 with a correct 33-point dungeo report — notably `dungeo.melee.blow.villain → KILLED` (player death, the D10 motivating row) is unobserved by the whole walkthrough suite, and grue/deadly-room/vsUnconscious never fire. AC-8 + ADR-294 AC-10 closed.
- **Verification**: tester 195 green (11 new coverage tests over a real EngineRandomService), ide-protocol 24 green, `./repokit build dungeo` green.

## Next Phase
- Finish Phase 3 (mutation-verification, commit); then Phase 4 (search-budget tool).

## Open Items

### Short Term
- [To be filled]

### Long Term
- [To be filled]

## Files Modified
- [To be filled]

## Notes
- [To be filled]

---

## Session Metadata

- **Status**: IN PROGRESS
- **Blocker** (if any): N/A
- **Blocker Category**: N/A
