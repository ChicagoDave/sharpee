# Session 2026-07-12 03:12 — v2-210-chord-a

## Goal

Each package P3 (analyzer + IR), P4 (loader evaluator/runtime + e2e),
then P5 (regression + docs + close) per docs/work/chord-each/plan.md.
David: "Proceed", "Go" (P4), "Finalize" (commit 03f32dd9), "Proceed" (P5).
THEN: "Chained walkthrough testing is required" → the zoo-chain package
(docs/work/zoo-chain/plan.md; David: zoo only, renumber into one chain,
fix platform first).

## "All of it" wave (same session, after f380916c; David: "All of it")

- **Interceptor-wiring audit COMPLETE**: 13 standard actions consult
  interceptors; only two gaps existed — closing lacked postReport
  (report-phase overrides/emits from `on closing it` silently dropped;
  FIXED + test) and taking lacked onBlocked (FIXED + covered by
  contract). All other 11 actions invoke the full five hooks.
- **Small-bore FIXED**: (1) chord parser `parse.trait-field-trailing` —
  uncomma'd `starts` tails now error instead of silently dropping the
  initial (+2 tests in parser-phase-b); (2) authored `with x = y`
  phrase params now flow to rendering (entity values as display names;
  test in action-body). DEFERRED with note: the `cant` dispatch-miss
  renders as parser-confusion text — needs a language-layer message-id
  decision, not a mechanical fix.
- **Two proposals DRAFTED for David's checkpoints** (docs/work/chord/):
  victory-trigger-proposal.md (recommends declarative `win/lose
  <phrase-key> when <condition>`, story-owned + broadcast; 5
  checkpoints incl. the Given-5 score-threshold question) and
  zoo-surfaces-proposal.md (agent-drafted: Z1 `first time` prose in
  create, Z2 `snippet` blocks, Z3 reserved presence/detail channel
  phrases — zoo's dormant `phrase presence:` lines make Z3a nearly
  free; checkpoints incl. the reserved-key governance class).
- Deferred by design: select-in-each per-iteration decision keys, §2.3
  award-once question, loader kind set (dungeo conversion forcing
  function).
- Regression: stdlib 1319+27skip, loader 103, chord 178 (+2), ide 11;
  Cloak 81/81, zoo atomic + chain green through rebuilt bundle; dungeo
  chain re-run green.

## Zoo-chain COMPLETE (same session; F1 platform fixes + F2 chain)

- Investigation: cloak/zoo gates were ALWAYS atomic (no chained
  conversion in history; the plans' `--chain` text was copy-pasta).
  Cloak cannot chain (4 of 8 files end the story). Zoo could — and
  building the full playthrough surfaced three latent defects capping
  the score at 75/85; David chose fix-first.
- **F1 fixes (David-approved platform work)**:
  1. stdlib taking.ts — interceptor postExecute/postReport were NEVER
     invoked (validate-only wiring); now full ADR-118 contract, per-item
     for multi-object. Zoo map's `after taking it / award collected`
     lives. 3 new state-asserting tests in taking-golden.
  2. story-loader buildDispatchAction — def.musts now gate validate,
     def.body executes through mutations/reports phases (own decision
     snapshot; behavior host OPTIONAL with a body). "No otherwise refuse
     ⇒ won't parse" was a MISDIAGNOSIS: verbs always parsed; the miss
     path's `cant` renders as parser-confusion text (noted, unfixed).
     New action-body.test.ts (4 tests incl. each-in-action-body).
  3. phraseEvent binds grammar-slot entity names as params — `{the
     target}` templates render.
- **F2**: zoo walkthroughs/wt-01…05 = ONE chained playthrough (37
  assertions, green), ending at the first-ever **perfect 85/85**.
  Excluded pair → walkthroughs/excluded/ (byte-identical); old
  smoke/optional/choice-cycle absorbed + retired. Gate-audit addendum
  records the new gate: 5 atomic files + `--chain walkthroughs/wt-*`.
- Discovery pinned: `$restore` rewinds world state but NOT the engine
  turn counter — post-restore turn-timed broadcasts land one command
  earlier (asserted in wt-02/wt-03).
- Follow-ups (recorded in zoo-chain plan): audit other stdlib actions
  for validate-only interceptor wiring; `cant` dispatch-miss message;
  authored `with x = y` phrase params still unplumbed (slot params only).
- Regression: stdlib 1317+27skip, loader 103, chord 176, ide 11,
  devkit 26+1; Cloak 81/81; zoo atomic + chain green; Dungeo units
  1691/9xf/4skip + chain 874/874 (RNG-flex counts, one-good-run).

## P5 COMPLETE — EACH PACKAGE CLOSED (same session)

- `./repokit build` clean; **Cloak 81/81 and Zoo 79/79 untouched through
  the rebuilt bundle** — the §5 acceptance proof. GATE-INVOCATION
  GOTCHAS (now recorded in plan.md P5 status): cloak runs WITHOUT
  --chain and WITH `--story stories/cloak-of-darkness/cloak.story`
  (chained alphabetical glob fails — ac6-save-restore ends the story,
  "Engine is not running" for everything after; bare glob also picks up
  bar-darkness, excluded by transcript-lint). Zoo runs its David-approved
  8-file/79-assertion set explicitly (hatch-text, scoring,
  state-assertions, timeline, ac6-trait-state + wt-01/04/05) — a glob
  drags in the 4 Phase-B-excluded phrase-algebra files, whose failures
  are the documented missing host-shell surfaces, not regressions.
- chord-grammar.md re-cut against parser.ts: each-block statement +
  layout row + host/diagnostic notes, any/no quantifier condition
  primary w/ standalone rule, `the match` value-expr + binder notes,
  must-infinitive `be any` + must-be-no error, coverage header,
  analyzer reading 14 (quantifier gates, reserved name, enumeration
  domain).
- chord-grammar-changes.md: each-package as-shipped reconciliation
  entry appended (E1–E3 final state; the four dated P3/P4 decisions;
  evaluator is-a note). Append-only discipline kept.
- Scope fence E4 held (diff grep: no every/cardinality/inline forms).
- Final suites: chord 176/176, story-loader 99/99, ide-protocol 11/11,
  devkit 26+1skip.
- Package closed against all five §6 criteria — full close report in
  plan.md P5 status. Uncommitted: this file, plan.md, proposal §6.5 (in
  03f32dd9 already), chord-grammar.md, chord-grammar-changes.md. Commit
  pending David's word.

## P4 COMPLETE (same session)

- Evaluator: any-of/none-of short-circuit over ir.entities in DECLARATION
  order (Risk 3 resolved — no world-model touch); satisfies membership
  (non-IR subject → false, outside the domain); is-a over IR kind
  compositions (PLATFORM KINDS ONLY — loader instantiates just
  room/container/person/supporter; `treasure`-style kinds future scope);
  `the match` via ctx.match, innermost by context spread; matchesOf
  public for the runtime.
- Runtime: each executes per match in both single-pass and two-phase
  paths; match set SNAPSHOTTED pre-mutation in snapshotDecisions (§5.4:
  iteration is routing — else mutate-everyone/narrate-no-one). Snapshot
  does NOT descend each bodies: nested each + select-on inside bodies
  decide live per pass (per-iteration decision keys = recorded
  follow-up).
- Fixture each-iteration.story + each-runtime.test.ts (18 tests):
  creation-order, empty sets, match/it + nested binding, exact report
  sequence proving the snapshot, satisfies both polarities via must
  ladder, E2 refusal on emptied set, AC-5 per-seed pins (42 miss/miss,
  7 miss/hit).
- Mutation-verification gaps (5) ALL closed with tests: entity + trait
  standard-action interceptor paths w/ each (snapshot proof through
  postReport), sequence-daemon each (live-eval context), set/award
  inside each (per-match field write, score dedup), nested-each live
  re-enumeration pin (inner-note ×1 not ×2), is-a on a non-IR subject.
- Gotcha: trait-field initials are `, starts <v>` — comma REQUIRED;
  parser silently ignores an uncomma'd `starts` tail (future lint?).
- Suites: story-loader 99/99 (+18), chord 176/176, ide-protocol 11/11,
  devkit 26+1skip; tsc clean; SHIPPING STORIES UNTOUCHED (git fence).
- Integration Reality: OWNED = chord compiler, story-loader, WorldModel
  — all exercised on the real path (compile → createStory → real
  WorldModel → capability phases / event-chain handlers). Only stub:
  FakeContext, a structural stand-in for the engine ActionContext
  (dispatch.test.ts precedent); full engine chain = Cloak/Zoo gates at
  P5. EXTERNAL: none.

## Decisions (David, 2026-07-12, this session)

1. **`must be any <name>` PARSES** (P2 flag 2 resolved): membership
   semantics — the subject must satisfy the named open condition.
   `<subject> must be any <open-cond>: <key>` compiles to a new IR
   condition kind `{ kind: 'satisfies', subject, condition }` — a dated
   deviation from proposal §3.5's pinned IR contract (any-of/none-of/
   each/match only), to be reconciled in the P5 ratchet-log pass.
   `must be no <name>` is a negated requirement → error (D6 stance,
   same as `must not`).
2. **`match` is a reserved name** (P2 flag 3 resolved): declaring an
   entity named `match` (or alias), a trait data field `match`, or a
   grammar slot `:match` is a load error (`analysis.reserved-name`) —
   the `br` precedent. Multi-word names containing "match" stay legal.

## Status

- **P3 COMPLETE** (same session). Suites: chord 176/176 (+23 over P2's
  153), ide-protocol 11/11, story-loader 81/81, devkit 26+1skip;
  chord/story-loader/ide-protocol tsc clean. Platform touch exactly the
  forecast: chord + story-loader only. Plan status lines flipped
  (P1/P2/P3 COMPLETE, P3 close report in plan.md). Proposal gained §6.5
  (dated P3 decisions for the P5 ratchet reconciliation).
- Mutation-verification: all gate/IR emission paths covered by
  state-asserting tests; its two flagged branch gaps (phase-order and
  inline-text descent into each bodies) closed with tests same session.
  Loader throw-stubs deliberately untested — P4 replaces them with real
  evaluation and its own tests.
- Pre-session audit was clean (only unrelated untracked doc drafts:
  blog-v2.md, phrase-*-prompt files).
- Uncommitted: all P3 changes + docs. Commit pending David's word.

## P4 carry-flags

- runtime snapshotDecisions doesn't descend into each bodies yet.
- evaluator `is-a` still throws (Phase A note) — quantifier conditions
  like `it is a treasure` need is-a evaluation in P4.
- `satisfies` (must-be-any membership), `match` binding stack, and
  creation-order iteration all land P4; loader stubs throw LoadError
  until then (never a silent no-op).
- Gotcha found: `pnpm --filter X test` in a background shell puts
  vitest into WATCH mode (background task shells present a TTY) — use
  `pnpm --filter X exec vitest run` for background/scripted runs.

## P3 work list

- ir.ts: IRCondition += any-of/none-of/satisfies; IRStatement += each;
  IRValue += match.
- analyzer.ts: quantifier gates (revive `analysis.closed-condition-
  selection` verbatim code; unknown-condition; story-state case), each
  gate + body resolution with inEach scope, `the match` resolution in
  value + NameRef positions (`analysis.match-outside-each`), reserved-
  name gates, collectInlineTexts + checkPhaseOrder descend into each
  bodies (P2 flag 1), open-condition-truth fix-it names live forms,
  is-any predicate → satisfies.
- parser.ts: `be any <name>` (standalone) in parseInfinitivePredicate →
  is-any; `be no <name>` (standalone) → positive-requirement error.
- story-loader: honest throw stubs for new IR kinds (evaluator
  evalCondition/evalValue are exhaustive-return; runtime statement
  switch gets explicit `each` throw — no silent no-op). Runtime
  execution lands P4. P4 note: snapshotDecisions walk doesn't descend
  each bodies yet.
- Tests: analyzer-each-package.test.ts (IR snapshot + exact-code gate
  tests), compile-clean fixture, gate fixtures, must-be-any parse
  coverage added to each-package.story (+ snapshot regen, deliberate).
