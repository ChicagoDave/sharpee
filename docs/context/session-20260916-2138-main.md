# Session Summary: 2026-09-16 - main (2026-09-16 21:38 CDT)

## Goals
- Finish the macOS-authorable half of Phase 3b (toolchain portability) and record its evidence.
- Attempt Phase 5 (macOS shipping integration) packaging tooling.
- Make the Avalonia app an actual openable application (Phase 4 macOS portion, GH #474).

## Phase Context
- **Plan**: `docs/work/chord-writer-avalonia-production/plan.md` — "Turn the Avalonia + Velopack spike into a shipping, cross-platform Chord Writer."
- **Phase executed**: Phase 3b ("Toolchain portability"), Phase 5 ("macOS shipping integration"), and the macOS portion of Phase 4 ("The pane door") — all three touched this session, per the plan's Slice ordering section, which supersedes phase numbering as an order (macOS slice draws on Phases 4, 5, 8, 9, 10).
- **Tool calls used**: 153 (state file records `budget: 4003`, which spans the plan's multi-phase allocation, not a single-phase budget).
- **Phase outcome**: Mixed — Phase 3b's macOS-authorable work completed and the phase went BLOCKED on hardware; Phase 4 (macOS portion) advanced substantially and stays CURRENT; Phase 5 was deliberately STOPPED mid-phase after a David correction, with real, durable tooling banked.

## Completed

### Phase 3b finish — toolchain portability (macOS-authorable half)
- Fixed `tools/ide/PaneHost/Hosting/NativeHostServices.cs` to resolve `bin/sharpee|sharpee.cmd` and `node/bin/node|node.exe` platform-shaped (via `ShimLeaf`/`NodeLeaf`) instead of hard-coding the POSIX shape.
- Found and fixed an adjacent defect: `Process.Start(UseShellExecute=false)` cannot launch a `.cmd` (`CreateProcess` rejects batch files). `RunAsync` now routes a Windows batch shim through `%ComSpec% /d /s /v:off /c` and refuses arguments containing `"` or `%`.
- `IHostServices.cs` docs and `HostCapabilityTests.cs` made platform-shaped to match.
- Darwin regression run executed: `vendor-toolchain.sh darwin/arm64` exit 0, 177M, 2 binaries signed, seal verified. `dotnet test PaneHost.Tests` against that staged toolchain: 24/24, nothing stubbed [reported by session, unverified — no event log exists for session 9dd6ac to corroborate the run].
- Wrote `docs/work/chord-writer-avalonia-production/evidence/phase-3b-toolchain-portability.md`.
- Phase 3b then marked **BLOCKED** (hardware — David out of town, no Windows/Linux machine); its owed real-path tests (Windows/Linux `compose`/`build` from the vendored toolchain) reassigned to their own platform slices per the Slice ordering ruling.

### Phase 5 tooling built, then deliberately stopped
- New `tools/ide/`: `package-avalonia.sh` (publish → vendor toolchain → stage product assets → icns → Info.plist → relocate → presign → `vpk pack --signAppIdentity`), `build-relocated-app.sh`, `patch-apphost.py`, `presign-payload.sh`, `dotnet-payload.entitlements`.
- Verified the AppHost app-path field on the production binary: one occurrence at offset 66088, 13 bytes clear padding (script searches rather than seeks, so this offset is never load-bearing).
- **Key finding**: `vpk pack --signAppIdentity` does not sign the relocated payload — of 20 Mach-O binaries only 4 carried the Developer ID signature; 16 .NET native libraries were ad-hoc (`flags=0x2`), and `libSkiaSharp`/`libHarfBuzzSharp` carried Microsoft's signature with no hardened runtime, which would fail notarization. `presign-payload.sh` closes this gap and is the durable output of the phase.
- `vpk` refuses to emit a version at or below one already in its output directory, so the script now clears `Releases/` per run.
- `tools/ide/.gitignore` gained `release-avalonia/`.
- **Stopped deliberately, nothing notarized** — see Key Decisions below.

### Phase 4 macOS portion — the app opens
- `App.axaml.cs` entry point inverted: the shell is now the default; `--pane-probe`, `--shell-probe`, `--editor` select the harnesses (previously the *default* was the Phase 1 probe driver, which is why an installed app ran four seconds and quit — David found this by double-clicking the installed build).
- `ShellWindow` now holds the window instead of closing itself after its scripted pass; probes are opt-in; a surface that fails to start no longer takes the window down.
- **GH #474 CLOSED** (this session, in the issue store). `tools/ide/PaneHost/Hosting/RepoPaths.cs` is bundle-aware: product assets resolve at `Contents/Resources/<name>` when bundled; the fernhill development story is nullable and checked null whenever bundled — checked BEFORE looking for a checkout — so an `.app` staged inside a working tree still refuses to load it. Every story-dependent surface opens empty rather than throwing.
- `package-avalonia.sh` stages `testing-surface`, `docs-tab`, `editor-bridge/lexer-server.js` into the bundle; fernhill deliberately is not staged.
- **Pane door contract module** (ADR-341 D3): new `tools/ide/PaneHost/Hosting/IPaneDoor.cs` (`Mechanism`, `IsOpen`, `Configure`, `Open`, `PaneUri`, `MessageReceived`, `EvaluateAsync`) plus `LoopbackPaneDoor.cs` (macOS backend). `ShellWindow` depends only on the interface — no `LocalOrigin`, port, token, or scheme-string building leaks through.
- New `tools/ide/PaneHost.Tests/PaneDoorTests.cs`: 4 real-path tests (real `HttpListener`, fernhill's real bundle, real HTTP) asserting panes return 200 with the shim injected, plus both rejections (`PaneUri` before open, opening twice) and that `Dispose` stops the port answering.

## Key Decisions

### 1. David: don't sign before there's an actual app
Phase 5 was building toward re-notarizing a payload that wasn't yet a working application. ADR-351 Q-5 already proved macOS notarization on a real bundle (submission `cbcd0706-f65c-4f13-9460-e9be833044ca`, Accepted, stapled) — re-proving it against a non-product payload buys nothing and would need redoing once the payload is real. Phase 5 was stopped with nothing notarized.

### 2. David: build one platform slice at a time, not one concern across three platforms
The plan gained a "Slice ordering" section (`docs/work/chord-writer-avalonia-production/plan.md:17-53`) that **supersedes the phase numbering** — phase numbers are now a work inventory, not an execution order. Order is macOS, then Linux, then Windows, each slice done only when that platform has an app a person can install, open, and use. Phase 4 splits across slices (contract + one backend per slice); Phase 5/6/7 (installers) come after their slice's app opens, not before.

### 3. Root cause worth carrying forward: Phase 5's exit state was entirely bytes, signatures, and a subprocess
"The app launches and stays open" was not in the original exit-state criteria, so the phase ran a full evening green while the artifact was unopenable — this is GH #435's shape (green without exercising the real thing) applied to the app instead of the toolchain. Phase 5's exit state now carries a launch check ahead of the signing checks (plan.md:279-285). Recurrence Check below confirms this shape has appeared multiple times in this project's history.

## Next Phase
- **Phase 4, continued (macOS portion)**: the exit-state run — real Docs/Play/Testing panes loaded inside a real `ShellWindow`, both message directions (page→host, host→page) proven the way the loopback origin's HTTP behavior is already proven. `Configure` and the messaging pair it wires are currently untested (filed as issue #477 below) because they need a constructed Avalonia view, which a headless `HttpListener` test cannot provide.
- **Tier**: Large (inherited from Phase 4's original budget).
- **Entry state**: Contract module and macOS backend exist and pass 28/28 headless tests; `App.axaml.cs`/`ShellWindow` hold the window; `RepoPaths` is bundle-aware. What's missing is the real-view run itself.
- After that: resume Phase 5 (notarize a real, openable app) — per Key Decision 1, only once Phase 4's macOS portion is done.

## Open Items

### Short Term
- I-477: PaneHost: IPaneDoor.Configure and its messaging pair are untested (need a real Avalonia view)
- I-479: Installed Avalonia app opens empty — decide what it should open by default
- I-480: Chord Writer (Avalonia) bundle identity is provisional — name, bundle id, version need David's ruling
- I-464: Testing surface and play client hard-code a WKWebView-shaped bridge (window.webkit.messageHandlers) — comment added this session: `PaneServer.HostShimScript` already probes both backends, but the panes themselves still call the WebKit name directly; renaming is a cross-app decision (shared assets with the Swift app), David's call.

### Long Term
- I-478: RepoPaths.Node hardcodes the POSIX node leaf while NativeHostServices resolves it per platform — same knowledge in two places, correct for the macOS-only slice today, owed once the Windows slice lands.

## Files Modified

**PaneHost source** (9 files):
- `tools/ide/PaneHost/Hosting/NativeHostServices.cs` - platform-shaped shim/node leaf resolution; batch-shim spawn fix
- `tools/ide/PaneHost/Hosting/IHostServices.cs` - docs updated for platform-shaped resolution
- `tools/ide/PaneHost/Hosting/RepoPaths.cs` - bundle-aware asset/story resolution (GH #474)
- `tools/ide/PaneHost/Hosting/IPaneDoor.cs` (new) - per-platform pane-door contract interface
- `tools/ide/PaneHost/Hosting/LoopbackPaneDoor.cs` (new) - macOS loopback backend implementing IPaneDoor
- `tools/ide/PaneHost/App.axaml.cs` - shell is now the default entry point; probes opt-in via flags
- `tools/ide/PaneHost/MainWindow.axaml.cs` - supporting entry-point change
- `tools/ide/PaneHost/Shell/ShellWindow.axaml.cs` - holds the window instead of self-closing after scripted pass
- `tools/ide/PaneHost/Editor/EditorWindow.axaml.cs` - supporting change

**Tests** (3 files):
- `tools/ide/PaneHost.Tests/HostCapabilityTests.cs` - platform-shaped assertions
- `tools/ide/PaneHost.Tests/PaneServerTests.cs` - supporting updates
- `tools/ide/PaneHost.Tests/PaneDoorTests.cs` (new) - 4 real-path door tests

**Packaging tooling, all new** (5 files):
- `tools/ide/package-avalonia.sh`, `tools/ide/build-relocated-app.sh`, `tools/ide/patch-apphost.py`, `tools/ide/presign-payload.sh`, `tools/ide/dotnet-payload.entitlements`

**Plan and evidence** (3 files):
- `docs/work/chord-writer-avalonia-production/plan.md` - Slice ordering section added; Phase 3b/4/5 status blocks rewritten
- `docs/work/chord-writer-avalonia-production/evidence/phase-3b-toolchain-portability.md` (new)
- `tools/ide/.gitignore` - added `release-avalonia/`

## Notes

**Session duration**: ~4-5 hours (state file `started`: 2026-09-16 21:38 CDT through this write).

**Approach**: Continued the plan's macOS slice bottom-up (toolchain → shell entry point → pane door), stopped mid-packaging on David's correction rather than pushing through to a signed artifact that wasn't yet the real product.

**Evidence gap**: no `docs/context/.devarch-events-9dd6ac.jsonl` exists for this session, so every test/build count above (`24/24`, `28/28`, `dotnet build` clean) is carried as the session's own account, marked `[reported by session, unverified]` rather than independently corroborated. The file-change and phase-status claims are corroborated directly against the session state file and `plan.md`.

---

## Session Metadata

- **Session**: 9dd6ac
- **Status**: INCOMPLETE
- **Blocker**: Phase 3b's Windows/Linux real-path tests need physical machines David does not have with him; Phase 4's exit-state run needs a constructed Avalonia view not yet exercised; Phase 5 was stopped deliberately pending Phase 4's completion.
- **Blocker Category**: Other: Hardware access (Windows/Linux machines unavailable) — Phase 3b specifically; Phase 4/5 are in-progress, not blocked.
- **Estimated Remaining**: ~2-3 sessions for the macOS slice (Phase 4 exit-state run, then Phase 5 resume through notarization); Phase 3b's Windows/Linux legs and Phases 6/7 wait on hardware availability with no session estimate possible until then.
- **Rollback Safety**: safe to revert — all changes are uncommitted local working-tree edits; nothing pushed or merged this session.

## Dependency/Prerequisite Check

- **Prerequisites met**: Phase 2/2a/3a all DONE prior to this session, giving Phase 3b/4/5 a working scaffold, generated protocol types, and a corrected toolchain to build on.
- **Prerequisites discovered but not available**: physical Windows and Linux machines for Phase 3b's real-path tests (David out of town) — this is why Phase 3b went BLOCKED rather than DONE.

## Architectural Decisions

- No ADR written or amended this session.
- Pattern applied: rule 13a Integration Reality Statements were produced in-conversation for the toolchain (Phase 3b: OWNED = vendored Node runtime, launchers, PaneHost's toolchain resolution; REAL-PATH TEST executed on darwin/arm64, Windows/Linux still owed) and for the pane door (Phase 4: OWNED = the three pane-door backends; REAL-PATH TEST executed for the macOS loopback backend via `PaneDoorTests.cs`, Windows/Linux backends deferred to their slices).
- Plan-level pattern: the "Slice ordering" correction and the "app before installer" correction (`plan.md:17-53`, `:376-385`) are both David-directed amendments to the plan's own sequencing, not ADR-level decisions — recorded in the plan, not as new ADRs.

## Mutation Audit

- Files with state-changing logic modified: `NativeHostServices.cs` (subprocess spawn/resolution), `LoopbackPaneDoor.cs`/`IPaneDoor.cs` (pane-door lifecycle: open/configure/dispose), `RepoPaths.cs` (path resolution branching on bundled vs. checkout), `App.axaml.cs`/`ShellWindow.axaml.cs` (window lifecycle).
- Tests verify actual state mutations (not just events): YES [reported by session, unverified] — `PaneDoorTests.cs`'s 4 tests assert on actual HTTP responses (200 with shim injected) from a real `HttpListener`, on `PaneUri` throwing before `Open`/on a second `Open` call, and on the port no longer answering after `Dispose` — these are state assertions, not mock or event-only checks. No event log exists for session 9dd6ac to corroborate the reported 28/28 pass count independently.
- If NO: N/A (graded YES on the test's own design, not corroborated by a fresh run).

## Recurrence Check

- Similar to past issue? **YES** — the GH #435 shape (a suite or phase reporting green without exercising the real production path) recurs across this project's history. Prior sessions naming GH #435 include `docs/context/session-20260916-2006-main.md`, `session-20260916-1728-main.md`, `session-20260916-1623-main.md`, and `session-20260916-0228-main.md`. This session's instance: Phase 5's exit state was entirely bytes/signatures/subprocess, so it ran green for a full evening against an app that opened a probe and quit after four seconds.
- Consider a one-time audit of exit-state criteria across the plan's remaining phases (6, 7, 9) to check none of them share Phase 5's gap (verifying artifacts without verifying the thing the artifact is supposed to do).

## Test Coverage Delta

- Tests added: 4 (`PaneDoorTests.cs`).
- Tests passing before: 24 → after: 28 (reported by session: `dotnet test PaneHost.Tests` — 28 passed, 0 failures [reported by session, unverified — no `docs/context/.devarch-events-9dd6ac.jsonl` exists for this session]).
- Known untested areas: `IPaneDoor.Configure` and its messaging pair (needs a constructed Avalonia view — issue #477); Windows/Linux pane-door backends (not yet built); Windows/Linux toolchain real-path tests (Phase 3b, blocked on hardware).

---

**Progressive update**: Session completed 2026-09-17 03:15 (approx, local write time)
