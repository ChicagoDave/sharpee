# Session Summary: 2026-06-18 (16:50 CST) — Sharpee 1.0.0 release

## Goals
- Verify the platform was ready to (re)publish to npm, then cut the first stable
  **1.0.0** release: version bump → commit → tag → push → publish → smoke-test →
  GitHub Release.

## What was done

### Publish-readiness verification
- Confirmed post-ADR-180 packaging state: `@sharpee/sharpee` is now a pure library
  (bin dropped); `@sharpee/devkit` is the global CLI (`bin: {sharpee}`, `publishConfig:
  {access:public}`, deps on `@sharpee/core` + `@sharpee/transcript-tester` + `fflate`).
- Found `@sharpee/devkit` was **never published** (npm 404) — first-time publish, the
  point of ADR-180 (`npm i -g @sharpee/devkit`).
- **Crossed-wires clarification (important):** `tsf publish` works off `ts-forge.config.json`
  + the staging dir, NOT raw package.json. The publish gate is "source package.json has a
  `publishConfig`" AND "package is in the build context (ts-forge.config projects)" AND "has
  staging output". tsf **hardcodes `npm publish --access public`** (publish.ts:199), so no
  manual `publishConfig.access` edits are needed — David stopped a wrong edit here.
- Two orphans have `publishConfig` but are NOT in the build context → tsf can't publish
  them, and they're version-stale: `@sharpee/core-extensions` (1.0.0-alpha.1) and
  `@sharpee/extension-conversation` (0.9.112). Left as-is (not part of the release).

### Version bump → 1.0.0
- `tsf version 1.0.0` set all 30 tsf-managed packages 0.9.113 → 1.0.0 (stories included).
- Bumped root `package.json` 0.9.111 → 1.0.0 (not tsf-managed).
- Stories/tutorials correctly excluded from publish (no `publishConfig`); `interpreter`
  built-but-not-published (no `publishConfig`).

### pnpm hoist config cleanup (separate commit)
- Moved `public-hoist-pattern` from `.npmrc` → `pnpm-workspace.yaml` (`publicHoistPattern:
  ['@sharpee/*','lz-string']`) so the `npm` CLI stops warning about the unknown key during
  tsf pack/publish. pnpm behavior unchanged (verified `pnpm config get public-hoist-pattern`).
- Committed alone as `chore: move pnpm hoist config` (2aa6c9b7), kept out of the release commit.

### Release: commit / tag / push
- Release commit `f96d588c chore(release): 1.0.0` (the 30 version bumps).
- Annotated tag **v1.0.0** on `f96d588c` (release commit, with the hoist chore on top).
- Pushed `main` (4a7c9460..2aa6c9b7) + tag to origin.

### npm publish
- `tsf build --npm` → staging, then `tsf publish` (David ran it). **All 27 publishable
  `@sharpee/*` packages live at 1.0.0**, incl. first-time `@sharpee/devkit` + `@sharpee/bootstrap`.
- The transient devkit 404 we saw mid-publish was registry/CDN propagation lag (the 404 log
  entries were read-side `npm view` probes from `tsf publish --changed`), not a failed publish.
- Cosmetic: `npm warn Unknown cli config "--git-checks"` — tsf passes pnpm's `--no-git-checks`
  to npm; harmless, lives in the tsf tool, not this repo.

### npm smoke test (registry, real publish)
- `sharpee test:npm tutorials/familyzoo --registry --version 1.0.0`: fresh registry install
  of the published closure compiled (`tsc`) and ran transcripts → **15 passing, 1 failure**.
- The one failure `v16-scoring` ("perfect score" expected; walkthrough scores 70/75 "Master")
  reproduces identically with `--local` tarballs → pre-existing **story** bug, not a publish
  issue. David: it's a timing-puzzle issue; **John has a fix pending merge** — leave it.

### GitHub Release
- Created **Sharpee 1.0.0** (marked Latest, `--verify-tag`) from v1.0.0:
  https://github.com/ChicagoDave/sharpee/releases/tag/v1.0.0
- Notes cover ADR-180 unify-CLI, the `@sharpee/devkit` global CLI, `@sharpee/sharpee` as a
  pure library, ADR-174 prose pipeline, ADR-179 Zifmia GHCR, install instructions.

## Status
- **Sharpee 1.0.0 SHIPPED.** npm (27 pkgs @ 1.0.0) + git tag + GitHub Release, smoke-verified
  from the live registry. 316 commits since the prior `0.9.107` release tag.

## Open items / follow-ups
- `v16-scoring` familyzoo failure: timing-puzzle story bug; fix pending in John's unmerged branch.
- Version-stale orphans `@sharpee/core-extensions` (1.0.0-alpha.1) + `@sharpee/extension-conversation`
  (0.9.112) — not in build context; align or remove if they're meant to ship.
- Deferred from ADR-180: `sharpee update` self-update; dedicated `bundle:story` verb.
