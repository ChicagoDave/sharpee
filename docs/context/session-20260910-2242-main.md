# Session Summary: 2026-09-10 - main (2026-09-10 22:42 CDT)

## Goals
- Table the secret-letter-port plan correctly per David's correction (tabled, not "still live").
- Close GH #400 (root `pnpm typecheck` is a no-op) by giving the repo a real, per-package typecheck gate.
- Close GH #404 (packages compiled by no routine command) by extending workspace membership to the packages #400's gate could not reach.
- Build the GH #401 mechanism (1,574 test-file type errors uncovered by #400's gate): a test-typecheck leg plus a ratchet, and measure the true error corpus rather than closing the issue outright.

## Phase Context
- **Plan**: No active plan — `.current-plan` is absent/empty; this was issue-driven work, not a plan phase. (The session-state file's `phaseName`, "Decide and provision the spike environment," is stale carryover from an unrelated ADR-341 spike context and does not apply to this session.)
- **Phase executed**: N/A (no plan phase)
- **Tool calls used**: 188 / 100 (Small tier, per `.session-state-379680.json` — well over budget; session continued through #404 and into #401 as a direct continuation of #400's fix-direction work, not new scope)
- **Phase outcome**: N/A

## Completed

### Secret Letter Port plan tabled (docs/work/secret-letter-port/plan.md)
- Phases 4 and 6 flipped `**Status**: CURRENT (since …)` → `**Status**: DEFERRED (2026-09-10)`, each keeping its former CURRENT-since stamp inline (e.g. "Was CURRENT (since 2026-08-21; reframed 2026-08-22, session 50a5a8)") so entry state is recoverable when the port resumes.
- Added a `**Tabled**: 2026-09-10` header line under `**Plan Status**: ACTIVE` recording why: David, 2026-09-09, the port is on hold indefinitely; tabled is a pause, not a closure.
- `**Plan Status**` deliberately stays `ACTIVE` — DevArch's plan-level vocabulary is only ACTIVE/DONE/ABANDONED, and both terminal values archive the plan out of `docs/work/`; tabling a plan with no returning pointer is neither.
- Verified `~/.claude/scripts/plan-staleness-scan.sh` is now silent about this plan (it walks CURRENT phases only; run 2026-09-10, no `secret-letter-port` line in its output).

### GH #400 closed — typecheck gate now real (45 config files)
- Per-package route over `tsc -b`: 42 workspace `package.json` files got `"typecheck": "tsc -p tsconfig.json --noEmit"`.
- Root `package.json`: `"typecheck": "tsc --noEmit"` → `"turbo run typecheck"`.
- `turbo.json`: typecheck task `dependsOn` `["build"]` → `["^build"]` (upstream builds only, not a same-package self-dependency).
- `stories/dungeo/package.json` additionally chains `tsc -p tsconfig.browser-check.json --noEmit` (the #406 guard, previously wired to no command).
- Key enabler: `tsc -p <composite> --noEmit` used to fail TS6304; on the repo's TS 5.9.3 it works, so no tsconfig surgery (`composite: true` already comes from `tsconfig.base.json`).
- Evidence (re-run fresh by this agent, 2026-09-11T04:01:01Z, after all edits): `pnpm typecheck` → 75 tasks (41 typecheck + upstream builds), **75 successful, 75 total**, 19.937s wall (34 cached), zero src errors tree-wide.
- Session's own falsification probes (narrated, not re-run by this agent — one-line `export const probe: number = "…"` injected and removed in each location): `packages/core/src/` → exit 2 at `@sharpee/core:build`; `packages/bridge/src/` → exit 2 at `@sharpee/bridge:typecheck` (bridge was #400's "built by nothing" case); `stories/dungeo/src/browser-entry.ts` → exit 2 TS2322 line 171. Old gate on the same core probe: `npx tsc --noEmit` exit 0 (the no-op the issue was filed against).
- #400 closed with this evidence; #406 commented and left open for `stories/armoured` (not a workspace member — wiring it is a scope call on a retired sample, tracked as I-379680-3 below).

### GH #404 closed — the last three gate-coverage gaps wired or dispositioned
- Of #404's six rows, three were already resolved by #400 as a side effect: workspace membership plus a `typecheck` script is what puts a package under a command, and `packages/bridge`, `packages/runtime`, `packages/world-index` already had both.
- `pnpm-workspace.yaml`: added `stories/concealment-test` and `tools/vscode-ext` as workspace members; added a comment beside the existing retired-package notes recording why `packages/extensions/conversation` is deliberately absent (see Findings below).
- `stories/concealment-test/package.json`: added `"typecheck": "tsc -p tsconfig.json --noEmit"` — it had the script's counterpart intent but sat outside the workspace when #400's sweep ran, so turbo never saw it.
- `tools/vscode-ext/package.json`: its existing `"lint": "tsc --noEmit"` gained the sibling name `"typecheck"` so turbo's `typecheck` task finds it (the underlying command was already correct).
- `tools/vscode-ext/src/build-provider.ts`: fixed two call sites — the only source-code change of the session. `resolveBuildScript()` takes no arguments and returns a configured command string (its own comment: "A command run from the workspace root (devkit; ADR-180), not a path"), but two of its three call sites (`createBuildTask` line 124, `handlePlayInBrowser` line 261) still passed `workspaceFolder.uri.fsPath` from when the function took a path. Harmless at runtime — JS ignores the extra argument — and invisible for as long as nothing compiled the file; adding vscode-ext to the gate surfaced it immediately as `TS2554: Expected 0 arguments, but got 1` at both sites. Both fixed by dropping the argument.
- `pnpm-lock.yaml` changed as a consequence of `pnpm install` linking the two new workspace members.

**Findings:**
- `packages/extensions/conversation` is a stub, not a dormant package: its entire source is `export const version = '0.1.0'`, with no dist, no importer, absent from story-loader's `EXTENSION_REGISTRY`, and never published (`npm view @sharpee/extension-conversation` → 404). Its last two commits were repo-wide mechanical sweeps, not feature work. Recorded as deliberately absent in `pnpm-workspace.yaml`'s comment; deleting the directory was **not** done — that is David's call, not this session's.
- `packages/bridge` and `packages/runtime` have no consumer anywhere in the repo: nothing depends on them but themselves, the macOS IDE does not spawn the subprocess (`PlayViewController.swift`'s "bridge" references are WebView JS-bridge terminology, unrelated; `vendor-toolchain.sh` vendors `devkit` and `platform-browser` only), and the only other live mention is in the retired `tools/_archive/shite`. Yet both are still actively edited by mechanical sweeps (10 and 12 commits in the last 90 days respectively) — evidence pointing two ways at once. Filed as **GH #409** (a question, not a bug) rather than resolved by inference, per the "no story invention / David decides content and direction calls" pattern. Deliberately **not** added to `repokit`'s `PLATFORM_PACKAGES` while #409 is open.
- Evidence, re-run fresh by this agent 2026-09-11T04:08:38Z: `pnpm typecheck` → **77 successful, 77 total**, 34 cached, 21.4s wall (up from #400's 75/75 — the two new members account for the delta). Falsification probe on a newly covered row, applied and reverted: appending `const __probe: number = "not a number";` to `stories/concealment-test/src/index.ts` produced `src/index.ts(220,7): error TS2322`, confirming the new gate row actually catches errors rather than passing vacuously.
- Issues this session: #400 closed, #404 closed, #406 commented and left open (`stories/armoured`), #409 filed (question, open).

### GH #401 — test files enter the typecheck gate (mechanism built, issue left OPEN for the burn-down)
- 31 per-package `tsconfig.test.json` files. Each extends the package's own tsconfig and undoes the two things that hid test files: `rootDir` moves to the package root so `tests/` is allowed beside `src/`, and `exclude` is replaced rather than extended (the parent's exclusion of `*.test.ts` is the thing being undone). `types: ["node", "vitest/globals"]`, `noEmit`, `composite: false`.
- `"typecheck:tests": "tsc -p tsconfig.test.json"` added to all 31, a matching `typecheck:tests` turbo task (`dependsOn: ["^build"]`, cache false), and `pnpm typecheck:tests` at the root — the progress report, red by design.
- The ratchet: a package with zero test errors gets the test leg appended to its gating `typecheck` script (`tsc -p tsconfig.json --noEmit && tsc -p tsconfig.test.json`), the same idiom `stories/dungeo` uses for its browser-check. 17 packages were wired this way; `stories/dungeo` already had its tests covered, making 18.
- `stories/dungeo` needed no new config — its tests live under `src/` and its own `tsconfig.json` already compiles them.
- Harness correction worth recording: `module: commonjs` from `tsconfig.base.json` made `tsc` reject `import.meta` (TS1343) in 10 test files that legitimately use it, since vitest runs them as ESM. The test configs set `module: "esnext"` and leave `moduleResolution` alone so `@sharpee/*` resolves exactly as it does for the src typecheck. That means the true error total is **1,500, not the audit's 1,574** — the difference was harness noise, not real drift.
- **Disclosure — three files were overwritten.** `packages/event-processor/tsconfig.test.json`, `packages/stdlib/tsconfig.test.json`, and `packages/world-model/tsconfig.test.json` already existed, committed 2025-07-28 ("Checking in refactored stdlib"), wired to no command and referenced by nothing. The generated versions replaced them. The only meaningful difference was event-processor's unused `@/*` path alias (no test or source file imports through it) and world-model's `sourceMap` settings, which are inert under `noEmit`. Flagged rather than left silent.
- **Corpus analysis** (the point of measuring before burning down): 589 of the 1,500 errors — 39% — are a single pattern. `ITrait` is `{ type }` and nothing more (`packages/world-model/src/traits/trait.ts:13`), and `add(trait: ITrait)` (`if-entity.ts:96`) puts every object literal under TypeScript's excess-property check, so `entity.add({ type: TraitType.OPENABLE, isOpen: false })` is an error — 589 times over a small field set (`isOpen` ×92, `name` ×82, `isOn` ×56, `worn` ×52, `capacity` ×40, `isLocked` ×38, `isPlayer` ×33), concentrated in stdlib (639 of its 911) and world-model. `docs/core-concepts/README.md` already gives the intended form (`new ContainerTrait({...})`), so the fix is test-side and scriptable per trait type, with no platform change. Widening `add()` was considered and rejected: it would delete the invariant rather than satisfy it.
- 15 errors fixed, all real drift, in nine packages that were 1-4 errors from clean:
  - `bootstrap/src/introspect.test.ts`: room exits keyed `north`/`south` where `DirectionType` values are `'NORTH'`/`'SOUTH'` — that fixture's exits were unreachable by the engine; assertion updated to match.
  - `channel-service` ×3 and `stories/channel-service-test` ×1: `authorChannels` missing from `ClientCapabilities` fixtures; set `false`, which preserves behaviour exactly (absent read as `undefined`, which the gate already treated as `false`).
  - `event-processor` ×3: importing `SemanticEvent`; core exports `ISemanticEvent`.
  - `event-processor/tests/fixtures/mock-world.ts`: `registerCapability` returned `void`; WorldModel's returns `ICapabilityData`.
  - `devkit/src/standalone/chord-build.test.ts`: the same import line twice.
  - `extensions/basic-combat`: `report` is optional on `Action`; the line above already used `blocked!`.
  - `helpers`: `WorldModel` → `Record<string, unknown>` cast without going through `unknown`.
  - `world-index`: `span: opener.placement?.span` where `IRPlacement.span` is required; now guarded like the two checks above it.
  - `tools/repokit`: `afterEach(() => vi.clearAllMocks())` returned `VitestUtils` where `void` was wanted.
- **Deliberately NOT fixed**: `ext-hunger` and `ext-scoring`, one error each. Both pass `createSeededRandom(1)` where a `RandomService` is wanted, and `SeededRandom` does not implement `resolve` — a question about which abstraction `TurnPluginContext` should take, not a typo. It belongs to the burn-down (tracked as I-379680-6 below).
- **Separate finding, reported on GH #402**: `packages/extensions/basic-combat` has 2 failing tests (`tests/npc-attack-through-attacking.test.ts:127`, 2 failed | 30 passed). Confirmed pre-existing by stashing the #401 change and re-running — same result. The package declares `test` (bare vitest) and no `test:ci`, so `turbo run test:ci` never reaches it and the red has been invisible. Concrete evidence for #402's claim.
- Evidence, run 2026-09-10:
  - `pnpm typecheck` → 77 successful / 77 total, exit 0 (src everywhere plus tests in 18 packages).
  - `pnpm exec turbo run typecheck:tests --continue` → 51 of 65, exit 2. Remaining 1,485 errors in 14 packages: stdlib 911, world-model 210, engine 129, story-loader 66, character 47, queries 32, parser-en-us 27, platform-browser 14, media 14, transcript-tester 13, if-domain 10, core 10, ext-hunger 1, ext-scoring 1.
  - Every edited package's suite passes: bootstrap 52, channel-service 119, channel-service-test 8, devkit 177, event-processor 27, helpers 17, world-index 169, repokit 97.
- Issues: #401 commented with the mechanism, cluster analysis and ratchet state, left OPEN for the burn-down. #402 commented with the basic-combat evidence.
- Files: 77 changed — 31 new `tsconfig.test.json` (3 of them overwriting the 2025 files above), 31 `package.json` script additions, root `package.json`, `turbo.json`, and 11 test files.

## Key Decisions

### 1. "Tabled" is a third disposition, distinct from rule 18b's "still live"
Rule 18b's "still live" stamp is for interruptions with a returning pointer (another plan takes `.current-plan` temporarily, this one resumes). The secret-letter-port went on hold indefinitely on 2026-09-09 with no returning pointer — there is nothing to point back to it — so the plan-staleness sweep was re-prompting a disposition David had already made. Tabling records the pause inline (a `**Tabled**` header, phases DEFERRED with their entry state preserved) without pretending the plan is either still actively worked or terminal.

### 2. Per-package `tsc -p <pkg> --noEmit` over `tsc -b` for the typecheck gate
`tsc -b` (project references build mode) was the more "correct" TS idiom but the session found `tsc -p <composite-tsconfig> --noEmit` now works cleanly on TS 5.9.3 (the TS6304 failure that used to force `-b` is gone), so the gate could be wired per-package through turbo's own dependency graph (`dependsOn: ["^build"]`) instead of restructuring 42 tsconfigs for a `-b` graph. Falsification probes in `core`, `bridge`, and `stories/dungeo` confirm each package's own compiler surfaces errors independently.

### 3. Ambiguous evidence (bridge/runtime) is filed as a question, not resolved by inference
`packages/bridge` and `packages/runtime` show two contradictory signals — zero consumers anywhere in the repo, yet ongoing mechanical edit activity. Rather than guess at intent (dormant vestige vs. active prep for an unlanded consumer) and either wire them into `PLATFORM_PACKAGES` or flag them for removal, the session filed GH #409 and left the packages exactly as found. This follows the project's standing pattern that content and direction calls on ambiguous repo state are David's, not inferred.

### 4. GH #401 is a ratchet, not a one-shot fix — 1,500 errors stay OPEN by design
With 1,500 real test-file type errors and 589 of them (39%) traceable to one fixable-but-mechanical pattern (`ITrait` excess-property checks on object-literal `add()` calls), fixing all of it in one session either meant a bulk scripted rewrite across stdlib and world-model without review, or stopping short of the stated goal. Neither was right: the session built the enforcement mechanism (per-package `tsconfig.test.json` + `typecheck:tests` turbo task) and the ratchet (a clean package's gating `typecheck` script grows the test leg, so regressions in already-clean packages are caught immediately), fixed the 15 errors that were real drift rather than the cluster pattern, and left the issue open with the corpus measured and characterized. This mirrors the #400/#404 split: build the gate, then burn down what it finds, rather than conflating gate-building with gate-satisfying.

### 5. Harness noise (`import.meta`/CommonJS) is separated from real error count before reporting
The initial audit's 1,574 included 10 `import.meta` failures (TS1343) caused by the test tsconfigs inheriting `module: commonjs` from `tsconfig.base.json`, not by anything wrong in the test files (vitest runs them as ESM). Setting `module: "esnext"` on the test configs (while leaving `moduleResolution` alone, so package resolution stays identical to the src typecheck) removed this noise, dropping the true count to 1,500. Reporting a corpus size that includes harness artifacts would have overstated the burn-down and misdirected effort toward files that were never actually broken.

## Next Phase
- N/A — no active plan. Next issue-driven work is the #401 burn-down (I-379680-1: the 589-error `ITrait` excess-property cluster is the highest-leverage next cut, plus I-379680-6's RandomService/SeededRandom design call for ext-hunger/ext-scoring), David's fix-direction call on #406 (I-379680-3), and his answer to the #409 bridge/runtime question (I-379680-4).

## Open Items

### Short Term
- I-379680-1: GH #401 — mechanism built this session (31 `tsconfig.test.json` + `typecheck:tests` turbo task + ratchet on 18 clean packages); true error total is 1,500 (1,574 audit figure included 10 harness-noise `import.meta` failures). 1,485 remain after 15 real-drift fixes, 589 of them (39%) one fixable `ITrait` excess-property pattern in stdlib/world-model. Issue left OPEN for the burn-down.
- I-379680-3: GH #406 — `stories/armoured` (a retired, deliberately unrunnable sample) is not a workspace member, so its `tsconfig.browser-check.json` guard has no command wired to it; whether to wire it is a scope call, not urgent.
- I-379680-4: GH #409 — `packages/bridge` and `packages/runtime` have no consumer anywhere in the repo but are still actively edited by mechanical sweeps (10 and 12 commits/90d); filed as a question on intent, not a bug. Deliberately not added to `repokit`'s `PLATFORM_PACKAGES` while open.
- I-379680-5: GH #402 — `packages/extensions/basic-combat` has 2 pre-existing failing tests, invisible because the package declares `test` (bare vitest) and no `test:ci`, so `turbo run test:ci` never reaches it; confirmed pre-existing (reproduces with #401's change stashed). Concrete evidence commented on #402; left open.
- I-379680-6: `ext-hunger`/`ext-scoring` each carry 1 test-typecheck error under #401 — `createSeededRandom(1)` passed where a `RandomService` is wanted, and `SeededRandom` doesn't implement `resolve`. A design question on what `TurnPluginContext` should take, not a typo; blocked on that call. Blocked on: item:I-379680-1.

### Long Term
- (none new this session)

**Resolved this session**: I-379680-2 (GH #404) — `resolve --as done --evidence pnpm-workspace.yaml`.

## Files Modified

**Plan disposition** (1 file):
- `docs/work/secret-letter-port/plan.md` - Phases 4 and 6 DEFERRED, `**Tabled**` header added

**Typecheck gate** (44 files):
- `package.json` (root) - `typecheck` script now delegates to `turbo run typecheck`
- `turbo.json` - typecheck task `dependsOn` changed to `["^build"]`
- 42 workspace `package.json` files - added `"typecheck": "tsc -p tsconfig.json --noEmit"` (`stories/dungeo/package.json` additionally chains the browser-check config)

**Typecheck gate coverage — GH #404** (5 files):
- `pnpm-workspace.yaml` - added `stories/concealment-test` and `tools/vscode-ext` as members; comment recording why `packages/extensions/conversation` is deliberately absent
- `stories/concealment-test/package.json` - added `"typecheck": "tsc -p tsconfig.json --noEmit"`
- `tools/vscode-ext/package.json` - named its existing `tsc --noEmit` lint command `"typecheck"` as well
- `tools/vscode-ext/src/build-provider.ts` - fixed two `resolveBuildScript()` call sites passing a now-removed argument (lines 124, 261)
- `pnpm-lock.yaml` - regenerated by `pnpm install` linking the two new workspace members

**Test typecheck gate — GH #401** (77 files):
- 31 new `tsconfig.test.json` files (one per workspace package with tests, `stories/dungeo` excluded — its own `tsconfig.json` already covers `src/`-resident tests); 3 of these overwrite pre-existing, uncommanded 2025 files in `packages/event-processor`, `packages/stdlib`, and `packages/world-model`
- 31 `package.json` files - added `"typecheck:tests": "tsc -p tsconfig.test.json"`; 17 of these plus `stories/dungeo` (18 total) also chain the test leg onto the gating `"typecheck"` script (ratchet)
- `package.json` (root) - added `"typecheck:tests"` script
- `turbo.json` - added `typecheck:tests` task (`dependsOn: ["^build"]`, cache false)
- 11 test/fixture files fixed for real type drift, across `bootstrap`, `channel-service`, `stories/channel-service-test`, `event-processor` (including `tests/fixtures/mock-world.ts`), `devkit`, `extensions/basic-combat`, `helpers`, `world-index`, and `tools/repokit` — see the GH #401 Completed section above for exact files, lines, and causes

## Notes

**Session duration**: not machine-tracked (no end-of-session timestamp recorded in session state); 188 tool calls logged (well over the 100-call Small-tier budget — the session ran past budget through #404 and into #401 as a direct continuation of #400's evidence trail rather than stopping mid-issue).

**Approach**: Issue-driven fix-up work off the open-items ledger (I-6682ed-1, opened by the prior session) rather than plan-phase execution. #404 followed directly from #400's own findings — three of its six rows were already fixed as a side effect of #400's workspace-membership logic, so closing it was verification plus filling the remaining three gaps, not new investigation. #401 followed the same pattern one level deeper: #400's gate excluded test files by design, so #401 was the next uncovered surface. Rather than either bulk-fixing 1,500+ errors unreviewed or declining to touch the issue, the session split it exactly as #400/#404 were split — build the enforcement mechanism and ratchet first, measure and characterize the corpus, fix the small number of errors that were real drift, and leave the issue open for the (now well-scoped) burn-down. GH #400 and #404 evidence were both corroborated by this agent with a fresh `pnpm typecheck` run rather than trusted from the session's narrative alone, since the event log carried no test/build rows for either; #401's evidence (`pnpm typecheck` 77/77, `turbo run typecheck:tests` 51/65 with the 1,485-error breakdown, and each edited package's own test suite) is reported by the session and carries the `[reported by session, unverified]` marker below since this agent did not re-run it.

**Ledger note**: I-6682ed-1 ("GH #400 decides where the typecheck gate should live…") resolved `--as done --evidence turbo.json`. I-379680-2 (GH #404) resolved `--as done --evidence pnpm-workspace.yaml` within this same session, after being opened by it. I-379680-1 (GH #401) was opened earlier in this session with the raw 1,574-error audit figure and amended to the mechanism-built, 1,500/1,485-error state recorded above. I-379680-5 (GH #402 basic-combat evidence) and I-379680-6 (ext-hunger/ext-scoring RandomService question) were opened new this session.

---

## Session Metadata

- **Session**: 379680
- **Status**: COMPLETE (unverified: GH #401's `pnpm typecheck` 77/77 run, the `turbo run typecheck:tests` 51/65 and 1,485-error breakdown, and the eight edited packages' own test-suite pass counts — reported by the session, not re-run by this agent)
- **Blocker**: N/A
- **Blocker Category**: N/A
- **Estimated Remaining**: N/A
- **Rollback Safety**: safe to revert

## Dependency/Prerequisite Check

- **Prerequisites met**: TS 5.9.3's fixed `tsc -p <composite> --noEmit` behavior (TS6304 no longer fires), which made the per-package route viable without tsconfig surgery.
- **Prerequisites discovered**: None.

## Architectural Decisions

- None this session. No ADR was written or referenced; the typecheck-gate approach is a build-tooling fix within existing per-package `tsconfig.json` structure, not a pattern change requiring a new ADR.

## Mutation Audit

N/A — this session changed build/config files (`package.json`, `turbo.json`, `pnpm-workspace.yaml`, 31 new `tsconfig.test.json`), a plan-status document, one prior source-code call-signature fix (`tools/vscode-ext/src/build-provider.ts`), and eleven test/fixture files fixed under GH #401. None of the eleven fixes changed test *logic* — they are type-correctness fixes (an import name, a return-type cast, a fixture field, a duplicate import line, an optional-chaining guard) that make existing assertions compile without changing what they assert. No new side-effect function was introduced or modified, so no Behavior Statement or mutation test applies.

## Recurrence Check

- Similar to past issue? NO — this is the first session to act on the typecheck-gate direction (both the src gate in #400/#404 and the test-file gate in #401); the prior session (6682ed) only filed the decision as an open item without implementing. The `ITrait` excess-property cluster identified in #401 (589 errors, one pattern) is itself evidence of a recurring authoring habit worth flagging to `pattern-recurrence-detector` if it resurfaces after the burn-down.

## Test Coverage Delta

- Tests added: 0
- Tests passing before: N/A → after: N/A. #400 evidence: `pnpm typecheck` — 75/75 tasks successful, 19.937s, run 2026-09-11T04:01:01Z. #404 evidence: `pnpm typecheck` — **77/77 tasks successful**, 34 cached, 21.391s, run fresh 2026-09-11T04:08:38Z (both by this agent) — this is a typecheck gate, not a test suite; "coverage" here means zero `tsc` errors across 43 packages (41 + the 2 newly wired), confirmed by the run above, not a test count. Falsification probe on `stories/concealment-test/src/index.ts` (added and reverted): `error TS2322` at the newly-covered row, confirming the added gate coverage catches real errors.
- #401 evidence **[reported by session, unverified]**: `pnpm typecheck` (src + the 18-package ratchet) — 77/77 successful; `pnpm exec turbo run typecheck:tests --continue` — 51/65 tasks, exit 2, 1,485 errors across 14 packages (stdlib 911, world-model 210, engine 129, story-loader 66, character 47, queries 32, parser-en-us 27, platform-browser 14, media 14, transcript-tester 13, if-domain 10, core 10, ext-hunger 1, ext-scoring 1); the eight packages touched by the 15 real-drift fixes each report their own test suite passing (bootstrap 52, channel-service 119, channel-service-test 8, devkit 177, event-processor 27, helpers 17, world-index 169, repokit 97). This agent did not re-run any of these commands.
- Known untested areas: the 1,485 remaining test-file type errors (#401, open, 589 of them one scriptable `ITrait` pattern); `stories/armoured` (#406, outside the workspace, open); `packages/bridge`/`packages/runtime` gate coverage is real but their consumer status is unresolved (#409, open); `packages/extensions/basic-combat`'s 2 pre-existing failing tests are invisible to `turbo run test:ci` because the package lacks a `test:ci` script (#402, evidence filed, open).

---

**Progressive update**: Session completed 2026-09-10 23:09 (extended to close GH #404 after the initial #400 close); further extended through GH #401 (test-typecheck gate mechanism, corpus measurement, 15 real-drift fixes) — issue left OPEN.
