# Session Summary: 2026-07-25 (1900) - main [4900be]

**Goal**: Resolve ADR-274's open question, accept the ADR, and implement it (bundled CLI
cold hatch-transpile hang).

**Status**: COMPLETE — ADR-274 ACCEPTED and implemented; all 4 acceptance items verified.

## Work done

- **Interview**: Q-1 ruled route (a) — esbuild marked external in the repokit CLI bundle,
  resolved at runtime from workspace `node_modules`. Routes (b) spawn-binary and (c)
  precompile-at-build recorded as rejected. adr-review 13/13 after the SMALL fix (D2's
  error named: `HatchTranspileError` + exact remedy message). **ACCEPTED.**
- **Phase 1 (repokit)**: `--external:esbuild` added in
  `tools/repokit/src/commands/bundle.ts`; stale "byte-for-byte parity with build.sh"
  header claim corrected (no repo-root build.sh exists anymore). Rebundled: bundle now
  contains a literal `require("esbuild")` and zero `Atomics.wait`.
- **Phase 2 (devkit)**: `hatch-transpile.ts` — lazy require moved AFTER the cache check
  (cold path only pays), wrapped: require-failure or environmental buildSync failure →
  `HatchTranspileError` (exported, remedy message per D2, original error as `cause`);
  author-code BuildFailure (has `.errors` array) passes through unwrapped. New
  `tests/hatch-transpile-error.test.ts` (3 tests: subprocess-real error path via
  `--require` preload, BuildFailure classification + no cache pollution, warm-path
  mtime-unchanged). Devkit suite 83 passed + 1 pre-existing skip.
- **Third touchpoint discovered**: `scripts/bundle-entry.js` re-wrapped hatch errors into
  a generic `Error`, discarding the name — now passes `HatchTranspileError` through.
- **Phase 3 (acceptance)**: cache purged; bundle copied to a bare temp dir (real
  no-node_modules path) → named `HatchTranspileError` with remedy, no hang (acceptance 3).
  Cache-cold friendly-zoo unit transcripts 76/76 through the repo bundle (acceptance 1),
  cache file appeared; walkthrough chain 56/56 warm (acceptance 2/4). friendly-zoo
  cache-cold is the standing regression set for this defect class.

## Key decisions

- ADR-274 Q-1 → route (a), recorded in the ADR (D1 mechanism + U2 dependency cost in
  Consequences). `docs/architecture/adrs/adr-274-bundled-cli-cold-hatch-transpile.md`.

## Files modified

- `docs/architecture/adrs/adr-274-bundled-cli-cold-hatch-transpile.md` (Q-1 folded, ACCEPTED)
- `docs/work/hatch-transpile-bundle/plan.md` (new, 3 phases, all DONE)
- `tools/repokit/src/commands/bundle.ts` (+`--external:esbuild`, header fix)
- `packages/devkit/src/standalone/hatch-transpile.ts` (HatchTranspileError, guarded cold path)
- `packages/devkit/tests/hatch-transpile-error.test.ts` (new)
- `packages/devkit/tests/fixtures/no-esbuild-preload.cjs` (new)
- `scripts/bundle-entry.js` (named-error pass-through)
- `dist/cli/sharpee.js` (rebundled)

## Next steps

1. Commit/push (not yet done).
2. ADR-272 inherits the one-line `reachable` semantics statement for the define-action page.
3. Next ADR-266 children: ADR-267 (constructs), ADR-268 (ordering experiment).
