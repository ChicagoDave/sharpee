# Session Summary: 2026-09-15 - main

## Status: COMPLETE

## Goals
- Avalonia evaluation Phase 8, **deliverable (4)**: the `linux/amd64` record run on the PC — the one item the plan marked OWED to this machine. Deliverables (1) the pane door, (2) `vpk` packaging, (3) the vendored-toolchain question, re-asked on x64 so the Linux column is comparable in kind to Phase 7's Windows column.

## Phase Context
- **Plan**: `docs/work/avalonia-ide-evaluation/plan.md` — Phase 8, reopened 2026-09-15 by session `e3fbf7`. **`.current-plan` was deliberately NOT repointed** (it names `docs/work/adr-349-location-heading/plan.md`, owned by a parallel Mac session mid-implementation); the avalonia plan header states this outright, so no rule-18b disposition was owed and none was taken.
- **Phase executed**: Phase 8, deliverable (4). Deliverables (1)-(3) were met on `linux-arm64` by session `e3fbf7`; this leg re-asked all three on x64.
- **Tier/Budget**: Medium / 200.
- **Phase outcome**: **DONE.** All three questions PASS on x64, every arm64 finding confirmed, plus one neither prior leg established. Phase 8 and the plan as a whole are now complete.

## Completed

### Host provisioning — a clean Ubuntu 24.04 rather than the box's default distro
- **Docker is not installed on this PC.** The plan's entry state assumed "Docker Desktop/WSL2 `linux/amd64`"; only WSL2 is present. The run therefore used WSL2 directly, not a container — recorded as a divergence from the arm64 leg, which ran in Docker under colima.
- The default `Ubuntu` distro (26.04 LTS) had a **pre-existing broken package** — `gnu-coreutils` in state `iHR` (half-installed, reinst-required), stranded mid-way through Ubuntu's rust-coreutils transition — which aborted every `apt` operation. Not caused by this work.
- Rather than repair it, installed a clean `Ubuntu-24.04` (`wsl --install -d Ubuntu-24.04 --no-launch`). Double duty: sidesteps the broken package **and** erases the distro divergence from the arm64 leg, leaving architecture as the only material difference between the two Linux columns.
- Provisioned: .NET SDK **10.0.401** (exact match with Phase 7 and the arm64 leg), `vpk` 1.2.0, `libwebkit2gtk-4.1` **2.52.6** (exact match with arm64), xvfb, zstd 1.5.5, squashfs-tools 4.6.1.

### Question 1 — the door, PASS on x64 at the first attempt
- `AdapterCreated` → `args.TryGetPlatformHandle()` → `GtkX11WebViewAdapter` (descriptor `XID`) → `IGtkWebViewPlatformHandle.WebKitWebView` `0x7ddba00884a0` → `webkit_web_view_get_context` `0x7ddba007fa70` → `webkit_web_context_register_uri_scheme("sharpee-panes")` by direct P/Invoke against `libwebkit2gtk-4.1.so.0`. Served 1 request from memory, **no HttpListener in the process**, `document.title` and marker both `PHASE8-LINUX-SCHEME-OK`, `location.origin` `sharpee-panes://host`.
- **The probe was rebuilt, not copied** — the arm64 spike source lives only on the Mac and outside the repository. Reconstructed from the recorded path plus the committed evidence schema, using the PC's own `win-door/Program.cs` (Phase 7) and `apidump/` as templates.
- Before writing it, the two API members the path depends on were **confirmed by reflection against `Avalonia.Controls.WebView` 12.1.0 on this machine** rather than assumed from the arm64 record: `IGtkWebViewPlatformHandle.WebKitWebView` (IntPtr) and `WebViewAdapterEventArgs.TryGetPlatformHandle()`. That check is the arm64 leg's recorded harness defect (GH #435 pattern) turned into a step.
- Three fields the arm64 leg did not capture: `adapterType=WebKitGtk`, `adapterEngine=WebKit`, `adapterVersion=2.52.6`, read from the live adapter rather than from the container image.

### Question 2 — `vpk` on x64, PASS
- Same PaneHost the arm64 leg packed, copied with `bin/`/`obj/` excluded and nothing else changed; it already carried `VelopackApp.Build().Run()`. Publish: **225 entries** (same count as arm64), 110,160,725 B, 0 warnings / 0 errors.
- AppImage 49,801,720 B (`appimagekit-runtime-x86_64`, ELF x86-64 static-pie, stripped); full nupkg 49,119,915 B; delta ~107–110 KB; pack 2.2 s.
- **The delta is a band, not a figure** — two zstd packs of identical source gave 110,050 and 106,944 B because the full nupkg itself moved between runs (build non-determinism). Recorded so a later session does not diff against one number.
- **The zstd trap reproduces at the same multiple**: absent zstd, `vpk` still succeeds and degrades silently to bsdiff — 24.11 s against 2.83 s, 8.5×, matching arm64's 8.5×. `mksquashfs` was present here, so the arm64 leg's structural finding was carried, not re-derived.
- **A porting cost that did not materialise**: PaneHost's Windows-only `<ApplicationManifest>` is ignored silently on a `linux-x64` publish — no warning, no error, no csproj edit.

### NEW — the packaged artifact was executed
Both prior legs list "the AppImage was not executed" under what they did not establish. It runs, directly off FUSE with no extraction, reaching WebKitGTK 2.52.6 through `GtkX11WebViewAdapter` inside the bundle. Two details kept:
1. Its stage 1 drives Avalonia's **own** `WebResourceRequested` path and reports `navigationSucceeded=True webResourceRequestedFired=2 title= href=about:blank` — Phase 1's "no door via Avalonia's own API", reproduced from inside a shipped artifact. Complementary to this phase's door, which is reached *underneath* Avalonia and serves real content.
2. It fails at stage 2 on `DirectoryNotFoundException: /Users/david/repos/sharpee/...` — `SpikePaths` branches Windows vs non-Windows and hands Linux the macOS path. A spike-path defect, not a platform finding.

### Question 3 — the toolchain, confirmed from this clone
`tools/ide/vendor/node/` carries exactly the two darwin tarballs, a README and SHASUMS — no Linux and no Windows asset. The shim written by `tools/ide/vendor-toolchain.sh` is `#!/bin/sh`. Same answer as arm64 and Phase 7; GH #448 downstream.

## Key Decisions

### 1. The record run used a fresh Ubuntu 24.04 WSL distro rather than the machine's default
The default distro was unusable (broken `gnu-coreutils` blocking apt) and was also the wrong *version* for the comparison — 26.04 against the arm64 leg's 24.04. Installing a clean 24.04 solved both at once and reduced the two Linux columns to a single controlled variable, architecture. Recorded because it changes how the two legs may be compared: they now differ in arch and one distro point release, not in distro, WebKitGTK, SDK, or packer.

### 2. ADR-351's R22 annotation is flagged stale; the score is left as David set it
The Consequences gained a Linux bullet (Phase 8's exit state required one and the arm64 leg had not written it). It records that "only macOS built and run" in the R22 cell is now false — all three platforms have been built and run — but **does not touch the score**, which is David's, set by him on a matrix he weighted himself. Changing a scored cell on his behalf would silently rewrite the ruling D2 rests on.

## Open Items

### Short Term
- **The avalonia plan is now terminal but not archived.** Phases 0–8 all DONE. It carries David's rule-18b **"still live"** stamp from 2026-09-14, whose Phase 7 wording is overtaken by events. Whether a now-complete plan gets archived is his call under rule 18b, not this session's — flagged, not acted on.
- **ADR-351 R22 score** — whether the new Windows + Linux build-and-run evidence moves Avalonia's `3`. David's.

### Long Term
- The default `Ubuntu` (26.04) WSL distro on this PC still has its broken `gnu-coreutils`. Out of scope here; noted so a later session does not rediscover it.
- Unattempted on Linux after both legs: signing, the update round trip, the real `docs-tab`/`testing-surface` panes over the scheme, the felt question, and a stock (non-WSL) kernel.

## Files Modified

**Evaluation records** (3 files):
- `docs/work/avalonia-ide-evaluation/phase-8-linux-check.md` - title, header (both runs, both spike locations, all five evidence files), the arm64-scope note, the "did not establish" x64 and artifact bullets, and a new ~100-line **x64 record run** section with its own Integration Reality Statement.
- `docs/work/avalonia-ide-evaluation/plan.md` - Plan Status → DONE (with the no-archive/no-re-disposition note); Phase 8 Status → all four deliverables met, with the x64 findings inline.
- `docs/architecture/adrs/adr-351-chord-writer-host-shape.md` - new Linux bullet in Consequences; R22 annotation flagged stale, score untouched.

**Evidence** (3 new files):
- `docs/work/avalonia-ide-evaluation/evidence/phase-8-linux-door-x64.json`
- `docs/work/avalonia-ide-evaluation/evidence/phase-8-linux-packaging-x64.txt`
- `docs/work/avalonia-ide-evaluation/evidence/phase-8-linux-appimage-run-x64.txt`

**Session** (1 file):
- `docs/context/session-20260915-1030-main.md` - this file.

**Not in the repository**: the reconstructed spike lives at `/root/spikes/avalonia-ide/{linux-door,linux-pack,apidump}` in the PC's `Ubuntu-24.04` WSL distro, per the plan's evidence discipline.

## Notes

**Session Start**: previous-session recap presented, `pre-session-audit` run and relayed verbatim, `project-profile.md` checked (2026-09-11, 4 days old — fresh), gate cleared, `docs/core-concepts/README.md` read in full. The audit's Recommended focus named ADR-349 Phase 4; that is the Mac's thread and was not taken — David redirected to the IDE work and chose this leg.

**One build failure, reported and held** per the no-auto-retry direction: the reconstructed probe used `AdapterInfo?.Type?.ToString()` where `Type`/`Engine` are non-nullable enums (`CS0023`, two diagnostic-only lines). Reported, waited for David's "go", then fixed. No fix-and-rebuild loop.

**Rule 15 (`mutation-verification`) did not fire**: the only repository edits were `.md`, `.json` and `.txt`, all excluded by the rule; the C# written this session is throwaway spike code outside the repository and none of its function names match the side-effect signal.

**Approach**: provision → verify the API shape by reflection on this machine before writing the probe → run → measure → record. The one methodological rule carried from the arm64 leg was its own: confirm the platform handle route rather than infer it, because the defect it caught produced a confident FAIL about behaviour never exercised.

---

## Session Metadata

- **Session**: a146df
- **Status**: COMPLETE
- **Blocker** (if any): N/A
- **Blocker Category**: N/A
- **Estimated Remaining**: N/A — Phase 8 and the avalonia plan are complete.
- **Rollback Safety**: safe to revert — documentation and evidence only; no repository code changed.

## Dependency/Prerequisite Check

- **Prerequisites met**: a linux/amd64 host (WSL2, after installing a clean Ubuntu 24.04), .NET SDK 10.0.401, vpk 1.2.0, libwebkit2gtk-4.1 2.52.6, Xvfb, zstd, squashfs-tools.
- **Prerequisites discovered**: Docker is absent on this PC (the plan assumed it as one of two options); the default WSL distro's apt is broken; `mksquashfs` and `zstd` are build-host prerequisites for `vpk` on Linux (carried from the arm64 leg, and zstd's silent degradation re-measured here).

## Architectural Decisions

- ADR-351 (Chord Writer host shape) — Consequences gained a Linux bullet recording both legs; no decision changed, no score changed, R22's annotation flagged stale for David.
- ADR-341 D3 (the three-web-pane host contract) — further evidence, not a change: Linux satisfies D3 by **its own mechanism** (a custom URI scheme, what D3 specifies and the shipping Mac app uses), where Windows offers a virtual-host analogue. D3 now answers differently on three platforms, confirmed on four architecture/OS combinations.

## Mutation Audit

- Files with state-changing logic modified: none in the repository. Documentation and evidence only.
- Tests verify actual state mutations (not just events): N/A — no repository code changed.
- If NO: N/A. The real-path evidence for this phase's own work is the Integration Reality Statement in `phase-8-linux-check.md`.

## Recurrence Check

- Similar to past issue? **Partly** — the arm64 leg's harness defect (a probe reporting a verdict about behaviour it never exercised) is the GH #435 category. It did not recur: the API shape was confirmed by reflection on this machine before the probe was written, which is that finding applied rather than repeated.

## Test Coverage Delta

- Tests added: 0 — this is an out-of-repo evaluation spike, not repository code.
- Tests passing before → after: N/A, no repository code changed.
- Known untested areas: unchanged by this session; the Linux items still unattempted are listed under Open Items.

---

**Progressive update**: Session completed 2026-09-15.
