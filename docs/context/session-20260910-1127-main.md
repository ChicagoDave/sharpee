# Session Summary: 2026-09-10 - main (11:27 CDT)

## Goals
- Recover and commit the previous session's uncommitted ADR-344 Phase 6 closure.
- Plan and implement ADR-345 (the engine lifecycle phase becomes an explicit concept).

## Phase Context
- **Plan**: `docs/work/adr-345-engine-lifecycle-phase/plan.md` — collapse `GameEngine`'s five ad-hoc lifecycle proxies into one `EnginePhase` discriminant with one guard per method.
- **Phase executed**: Phase 1 — "The lifecycle phase replaces the five proxies (D1, D2, D3, D4, D8, D8a, D9, D11, D13)" (Large); Phase 2 — "The optionality the phase disproves is gone (D7)" (Medium) also executed, not yet committed at time of writing.
- **Tool calls used**: 194 / 400 (Phase 1 budget; Phase 2 tracked separately).
- **Phase outcome**: Phase 1 completed on budget and committed (`9d63352f9`). Phase 2 completed and evidence-gated but landing in the finalize commit that follows this summary.

## Completed

### ADR-344 Phase 6 recovery and commit
- The previous session (4b4f4d) had left a `commit-local` agent running that reported back mid-session: `pnpm exec turbo run test:ci` was SIGKILLed (exit 137) at a 10-minute Bash timeout, so nothing was staged and the entire Phase 6 closure was uncommitted.
- Confirmed via `git status`, `git log`, `git reflog`, and a fetch against `origin/main` — HEAD was `c8a893d79` (Phase 5) both locally and remotely, no Phase 6 commit anywhere, nothing stashed.
- Committed as `70096ec9a` — `docs,test(adr-344): Phase 6 closure — evidence table, eleven-site test sweep`, 20 files. Re-ran `turbo run test:ci`: 67 of 67 tasks in 1m7s — a cold cache had caused the earlier SIGKILL, not a real failure. Local only, not pushed.

### ADR-345 planning and review
- `session-planner` wrote `docs/work/adr-345-engine-lifecycle-phase/plan.md`, three phases. `.current-plan` repointed (no rule-18b disposition needed — the pointer was already released when the ADR-344 plan archived).
- `/devarch:plan-review` found one CONTRADICTION: Phases 1 and 2 gated only on per-package suites while `project-profile.md:28` names `turbo run test:ci` as the project test command, and eleven packages depend on `@sharpee/engine`. Same shape as open item I-52e228-1. Fixed by adding `turbo run test:ci` to both phases' exit states, reason stated inline. One advisory TENSION (Phase 3's possible `lang-en-us` touch) verified rather than fixed: exactly one `game.` template key exists (`game.started.banner`, `language-provider.ts:108`), no `game.resumed` — stays read-only verification.

### Phase 1 — the lifecycle phase becomes explicit (committed `9d63352f9`, 13 files, 562 insertions)
- Added `type EnginePhase` (`empty`/`ready`/`playing`/`stopped`, D1) to `game-engine.ts`; `installStory`, `start`, `resume`, `executeTurn` each collapsed to one phase guard, refusal naming the phase found (D2).
- **D8a ruled by David** (tolerant Option A) after a full pros/cons: `resume()` now tolerates `playing` as a silent no-op, because `branch-tester/src/tree-walker.ts:360` calls `game.reviveEngine?.()` (→ `resume()`) on every test line regardless of phase, commented "Harmless when the engine is running." Option B (the walker asking the phase instead of guessing) would require phase information to leave `GameEngine`, which ADR-345 D7 already ruled out — recorded as a rejected alternative, not silently dropped. Recorded in the ADR as new **D8a** with new **AC-9**.
- Two ADR inaccuracies found by reading, not assumed: `executeTurn` had two guards (`:709` and `:713`), not the one the ADR's table listed; and `emitGameEvent`'s `this._context?.currentTurn ?? 1` (`:1368`) is a **live** case (install steps emit before context exists), not a dead hedge like the bridges' `?? 0` — kept, given a `contextIfInstalled` accessor, a do-not-clean-up-by-analogy comment, and a test pinning install-time events to turn 1.
- **D13/AC-8 discharged in the same commit**: amended ADR-344 D6a, whose quoted refusal string `Cannot start: no story installed — call installStory() first.` went stale the moment D2 landed. This resolved open item **I-b314e2-2**.
- One line removed beyond the ADR's letter: `refreshStoryInfoCapability`'s dead `if (!this.story) return;` guard (only caller is `start()`, which now only runs in `ready`) — now takes the story as a parameter.
- Evidence (2026-09-10 15:31-15:35 CDT): AC-1 grep 33 hits → 19, none a lifecycle guard; AC-2 four rejection tests, one per method; AC-4 stop tolerance, three phases, each asserting no event emitted; AC-7 closed-state set (no back-edge to `empty`); AC-8 the ADR-344 amendment; AC-9 resume tolerance. `pnpm --filter '@sharpee/engine' test --run`: 785 passing, 0 failures, 7 skipped (baseline 776 + 7; nine added). `turbo run test:ci`: 67 of 67. `mutation-verification` GREEN on all four transitions, specifically confirming every moved `running === false` → `phase.name === 'stopped'` assertion was preceded by a real `start()` (not a mismapped `ready`); one cosmetic gap (the `empty`-phase `stop()` no-op asserted only the throw axis) fixed inline.

### Phase 2 — the optionality the phase disproves is gone (D7) — evidence-gated, not yet committed
- `TurnEngine.story`, `.channelService`, and `ISaveRestoreStateProvider.getStory()` lost `| undefined`; both bridge hosts lost the inert `?? 0` hedge.
- Two ADR inaccuracies found by reading. `ISaveRestoreStateProvider.getStory()` cannot narrow directly — the provider is `GameEngine` itself (`createSaveData(this)`), and its public `getStory()` must stay `Story | undefined` because `story-install-order.test.ts:113` and `validate-role-holder.test.ts:75` both pin `expect(engine.getStory()).toBeUndefined()` after a failed install (an ADR-344 contract about adopting nothing). Resolved with a narrow `saveProvider()` adapter mirroring the existing `turnEngine()` pattern. Second: `runtime/src/bridge.ts:316` carries `context?.currentTurn` with **no** `?? 0`, though D7 describes both bridges as having one.
- Beyond D7's letter: `emitChannelPacket`'s `if (!channelService) return;` silently dropped an entire turn's channel packet — same silent-wrongness shape as the bridges' `?? 0`. Replaced with a `channelServiceForTurn` accessor that throws on a case the phase model rules out.
- Evidence (2026-09-10 17:26-17:34 CDT): per-package `tsc --noEmit` for `engine`, `bridge`, `runtime` all exit 0; `./repokit build dungeo` exit 0, bundle 4,439,557 bytes; engine suite unchanged at 785 passing / 0 failures / 7 skipped; `turbo run test:ci` 67 of 67; walkthrough chain 952 passing across 17 transcripts, all 17 goldens matched (run because the bundle rebuilt and a channel-packet guard was deleted, not required by AC-3, one run per the deterministic-seed rule). Root `npx tsc --noEmit` deliberately not cited as evidence, per AC-3.

## Key Decisions

### 1. D8a — `resume()` tolerates `playing` (Option A, David's ruling)
Deciding evidence was `branch-tester/src/tree-walker.ts:360`'s unconditional `reviveEngine?.()` call on every test line. Option B (the walker asking the phase) was rejected as a challenge to ADR-345 D7's explicit "phase never leaves `GameEngine`" survey, not an implementation detail — recorded as a rejected alternative in the ADR, not silently dropped.

### 2. `ISaveRestoreStateProvider.getStory()` narrows via an adapter, not directly
The ADR's D7 assumed the provider's public `getStory()` could narrow like `TurnEngine`'s fields. It cannot — two existing tests pin `undefined` as the correct value after a failed install. A `saveProvider()` adapter (mirroring `turnEngine()`) lets the interface promise a `Story` while the public accessor stays honest.

## Next Phase
- **Phase 3**: "`resume()` emits `game.resumed`, verified to render nothing (D12, D14)" — the ADR's one behavior change, deliberately isolated per the ADR's own instruction that it "must not be verified as one thing" alongside Phases 1-2's refactor. Gated by AC-5 (event emission, exactly once, past-tense, no `-ing` partner) and AC-6 (prose-pipeline zero-blocks test first, then the walkthrough chain as confirmation).
- **Tier**: Medium (200 budget).
- **Entry state**: Phase 1 DONE; Phase 2's commit landing (the finalize commit following this summary) as its own prerequisite for `resume()`'s guard already being `stopped`-only.

## Open Items

### Short Term
- (none carried — I-b314e2-1 and I-b314e2-2 both resolved this session, see Completed)

### Long Term
- I-52e228-1: the systemic dist-vs-source / gate-freshness audit — still standing, explicitly judged out of scope for the ADR-345 plan (recorded in its Sequencing rationale). This session added a narrow instance of the fix (the `test:ci` cross-package gate on Phases 1 and 2), and that gate came back clean both times — "nothing downstream broke" is itself the finding worth recording, since `game-engine.ts` has ten dependent packages and this work made four of its methods strict.

## Files Modified

**ADR-344 Phase 6 (committed `70096ec9a`, 20 files)**:
- `docs/architecture/adrs/adr-344-player-role-holder-at-the-install-seam.md` - evidence table, nine-AC closure
- `packages/story-loader/tests/*` (7 files) and `helpers/boot-turns.ts` - eleven-site sweep of stale `player` field / setPlayer dance
- `packages/engine/tests/*` (3 files) - same sweep

**ADR-345 Phase 1 (committed `9d63352f9`, 13 files)**:
- `packages/engine/src/game-engine.ts` - `EnginePhase` discriminant, one guard per method
- `docs/architecture/adrs/adr-344-player-role-holder-at-the-install-seam.md` - D6a amendment (D13/AC-8)
- `docs/architecture/adrs/adr-345-the-engine-lifecycle-phase-is-an-explicit-concept.md` - D8a, AC-9 added
- `packages/engine/tests/game-engine.test.ts`, `tests/unit/engine-lifecycle-phase.test.ts`, `tests/unit/engine-resume.test.ts`, `tests/unit/story-install-order.test.ts`, `tests/unit/turn-stages.test.ts` - rejection, tolerance, closed-state tests

**ADR-345 Phase 2 (uncommitted, 11 files)**:
- `packages/engine/src/turn/context.ts`, `session/save-restore-service.ts`, `turn/channel-packet.ts`, `turn/ending.ts`, `game-engine.ts` - narrowed types, `saveProvider()`/`channelServiceForTurn` accessors
- `packages/bridge/src/bridge.ts`, `packages/runtime/src/bridge.ts` - dead `?? 0` hedge removed
- `docs/work/adr-345-engine-lifecycle-phase/plan.md` - Phase 1/2 marked DONE with evidence
- `packages/sharpee/docs/genai-api/engine.md`, `index.md`, `stories/dungeo/src/version.ts` - generated/version drift from the rebuild

## Notes

**Session duration**: ~7 hours (11:27-18:00+ CDT, spanning both ADR-344 recovery and ADR-345 Phases 1-2).

**Approach**: Read-first discipline caught four ADR inaccuracies this session (executeTurn's guard count, the `?? 1` vs `?? 0` distinction, the save-provider's identity, runtime's missing `?? 0`) — each fixed in the ADR alongside the code, not just worked around.

---

## Session Metadata

- **Session**: 772af3
- **Status**: COMPLETE
- **Blocker** (if any): N/A
- **Blocker Category**: N/A
- **Estimated Remaining**: N/A (COMPLETE)
- **Rollback Safety**: safe to revert — Phase 1 is committed locally, not pushed; Phase 2 is uncommitted in the working tree.

## Dependency/Prerequisite Check

- **Prerequisites met**: ADR-344 fully closed (Phase 6 committed) before ADR-345 planning started, per I-b314e2-1's own condition. ADR-345 ACCEPTED with all six original open questions resolved before implementation began.
- **Prerequisites discovered**: D8a (the resume-tolerance question) was not resolved in the ADR and required a mid-session ruling from David before Phase 1 could proceed past the `resume()` guard.

## Architectural Decisions

- ADR-345: D8a added (resume tolerates `playing`, Option A, David's ruling) with new AC-9; both landed in `9d63352f9`.
- ADR-344: D6a amended in `9d63352f9` per D13/AC-8 — the quoted refusal string replaced by the shipped phase-naming refusal; the decision is unchanged, only its realization moved.
- Pattern applied: Making Implicit Concepts Explicit (DDD tactical pattern) — the ADR's own framing, applied to one class's internal state machine, not a bounded-context change.

## Mutation Audit

- Files with state-changing logic modified: `packages/engine/src/game-engine.ts` (lifecycle guards), `packages/engine/src/turn/channel-packet.ts` (`emitChannelPacket` → `channelServiceForTurn`), `packages/engine/src/session/save-restore-service.ts` (`saveProvider()` adapter).
- Tests verify actual state mutations (not just events): YES (evidence: `mutation-verification` agent run 2026-09-10, GREEN on all four Phase 1 lifecycle transitions, confirmed each moved `phase.name === 'stopped'` assertion was preceded by a real `start()` call rather than a mismapped `ready`; one cosmetic gap in the `empty`-phase `stop()` no-op test — silence asserted only on the throw axis — fixed inline before shipping).
- Phase 2's changes (type narrowing, accessor introduction) did not receive a separate `mutation-verification` run — they are boundary-tightening on existing mutation paths already covered by Phase 1's suite and the per-package `tsc`/`test:ci`/walkthrough-chain evidence, not new mutation logic.

## Recurrence Check

- Similar to past issue? YES — the CONTRADICTION plan-review found (per-package gate blind to cross-package blast radius) is the same shape as **I-52e228-1**, previously logged in `docs/context/session-20260910-1108-main.md` and traced across at least four prior instances (root `tsc`'s `files: []`, the helpers real-path test, the eleven-site ADR-344 sweep, and now this plan's own first draft).
- If YES: I-52e228-1 already recommends the systemic audit; this session's contribution is one more narrow instance where the fix (adding `turbo run test:ci` to the exit state) was applied and came back clean, rather than a new occurrence of damage going undetected.

## Test Coverage Delta

- Tests added: 9 (Phase 1: 4 rejection tests, 1 stop-tolerance test with 3 sub-cases, 1 resume-tolerance test with 3 sub-cases, plus the install-time-turn-1 pin). Phase 2 added no new tests (type-narrowing only; existing suite covers the paths).
- Tests passing before: 776 passing / 7 skipped → after: 785 passing / 0 failures / 7 skipped (evidence: `pnpm --filter '@sharpee/engine' test --run`, run 2026-09-10 15:31-15:35 CDT, unchanged through Phase 2's 17:26-17:34 CDT run).
- Known untested areas: Phase 3's behavior change (`resume()` emitting `game.resumed`) is deliberately not yet tested — that is next phase's own AC-5/AC-6, kept isolated from this session's green suite by design.

---

**Progressive update**: Session completed 2026-09-10 18:00 (approx, CDT)

## Activity Log (auto-captured)
```
[16:51:16] GIT: Git operation — echo '=== reflog ==='; git reflog -15; echo; echo '=== branches with recent comm
[16:53:27] EDIT: File written — .commit-files
[16:53:35] EDIT: File written — .commit-msg
[16:53:37] GIT: Git operation — bash $HOME/.claude/scripts/git-commit.sh
[16:56:23] EDIT: File written — docs/work/adr-345-engine-lifecycle-phase/plan.md
[16:57:30] TEST: Tests ran (status unverified) — cd /Users/david/repos/sharpee; echo '=== profile: test/build/gate lines ==='; gr
[18:56:29] BUILD: Build passed — cd /Users/david/repos/sharpee; grep -n "pnpm --filter '@sharpee/engine' test --r
[18:56:39] EDIT: File edited — docs/work/adr-345-engine-lifecycle-phase/plan.md
[18:56:44] EDIT: File edited — docs/work/adr-345-engine-lifecycle-phase/plan.md
[20:16:35] EDIT: File edited — docs/architecture/adrs/adr-345-the-engine-lifecycle-phase-is-an-explicit-concept.md
[20:16:44] EDIT: File edited — docs/architecture/adrs/adr-345-the-engine-lifecycle-phase-is-an-explicit-concept.md
[20:16:50] EDIT: File edited — docs/architecture/adrs/adr-345-the-engine-lifecycle-phase-is-an-explicit-concept.md
[20:16:55] EDIT: File edited — docs/work/adr-345-engine-lifecycle-phase/plan.md
[20:17:06] EDIT: File edited — docs/work/adr-345-engine-lifecycle-phase/plan.md
[20:17:11] EDIT: File edited — docs/work/adr-345-engine-lifecycle-phase/plan.md
[20:17:14] EDIT: File edited — docs/work/adr-345-engine-lifecycle-phase/plan.md
[20:18:01] EDIT: File edited — packages/engine/src/game-engine.ts
[20:18:14] EDIT: File edited — packages/engine/src/game-engine.ts
[20:18:21] EDIT: File edited — packages/engine/src/game-engine.ts
[20:18:42] EDIT: File edited — packages/engine/src/game-engine.ts
[20:18:49] EDIT: File edited — packages/engine/src/game-engine.ts
[20:18:57] EDIT: File edited — packages/engine/src/game-engine.ts
[20:19:03] EDIT: File edited — packages/engine/src/game-engine.ts
[20:19:07] EDIT: File edited — packages/engine/src/game-engine.ts
[20:19:14] EDIT: File edited — packages/engine/src/game-engine.ts
[20:19:19] EDIT: File edited — packages/engine/src/game-engine.ts
[20:19:23] EDIT: File edited — packages/engine/src/game-engine.ts
[20:19:28] EDIT: File edited — packages/engine/src/game-engine.ts
[20:19:35] EDIT: File edited — packages/engine/src/game-engine.ts
[20:19:53] BUILD: Build failed — pnpm --filter '@sharpee/engine' test --run 2>&1 | tail -45
[20:20:10] BUILD: Build passed — pnpm --filter '@sharpee/engine' test --run 2>&1 | grep -E "^ (FAIL|❯)|AssertionE
[20:29:40] EDIT: File edited — packages/engine/tests/game-engine.test.ts
[20:29:50] EDIT: File edited — packages/engine/tests/unit/story-install-order.test.ts
[20:29:53] EDIT: File edited — packages/engine/tests/unit/engine-resume.test.ts
[20:30:08] EDIT: File edited — packages/engine/tests/unit/engine-resume.test.ts
[20:30:26] EDIT: File edited — packages/engine/tests/unit/engine-resume.test.ts
[20:30:55] EDIT: File written — packages/engine/tests/unit/engine-lifecycle-phase.test.ts
[20:31:04] BUILD: Build failed — cd /Users/david/repos/sharpee && pnpm --filter '@sharpee/engine' exec tsc --noEm
[20:31:16] EDIT: File edited — packages/engine/tests/unit/turn-stages.test.ts
[20:31:20] EDIT: File edited — packages/engine/tests/unit/engine-lifecycle-phase.test.ts
[20:31:28] BUILD: Build passed — cd /Users/david/repos/sharpee && pnpm --filter '@sharpee/engine' test --run 2>&1
[20:31:50] EDIT: File edited — packages/engine/src/game-engine.ts
[20:31:54] EDIT: File edited — packages/engine/src/game-engine.ts
[20:32:06] EDIT: File edited — docs/architecture/adrs/adr-344-player-role-holder-at-the-install-seam.md
[20:32:15] BUILD: Build passed — pnpm --filter '@sharpee/engine' exec tsc --noEmit 2>&1 | head -20; echo "TSC don
[20:33:35] BUILD: Build passed — pnpm exec turbo run test:ci 2>&1 | tail -25
[20:35:10] EDIT: File edited — packages/engine/tests/unit/engine-lifecycle-phase.test.ts
[20:35:14] BUILD: Build passed — pnpm --filter '@sharpee/engine' test --run tests/unit/engine-lifecycle-phase.tes
[20:35:32] EDIT: File edited — docs/work/adr-345-engine-lifecycle-phase/plan.md
[21:58:29] GIT: Git operation — cat $HOME/.claude/scripts/git-commit.sh 2>/dev/null | head -80; echo "---"; ls -
[21:58:32] GIT: Git operation — sed -n '80,160p' $HOME/.claude/scripts/git-commit.sh
[21:58:36] EDIT: File written — .commit-files
[21:58:44] EDIT: File written — .commit-msg
[21:58:46] GIT: Git operation — bash $HOME/.claude/scripts/git-commit.sh
[22:25:31] EDIT: File edited — packages/engine/src/game-engine.ts
[22:25:51] EDIT: File edited — packages/engine/src/turn/channel-packet.ts
[22:25:55] EDIT: File edited — packages/engine/src/turn/ending.ts
[22:25:58] EDIT: File edited — packages/engine/src/session/save-restore-service.ts
[22:26:04] EDIT: File edited — packages/bridge/src/bridge.ts
[22:26:09] EDIT: File edited — packages/runtime/src/bridge.ts
[22:26:21] EDIT: File edited — packages/engine/src/game-engine.ts
[22:26:30] BUILD: Build passed — cd /Users/david/repos/sharpee; pnpm --filter '@sharpee/engine' exec tsc --noEmit
[22:28:45] BUILD: Build passed — cd /Users/david/repos/sharpee; pnpm exec turbo run test:ci 2>&1 | tail -6
[22:29:18] EDIT: File edited — docs/work/adr-345-engine-lifecycle-phase/plan.md
[22:35:45] EDIT: File written — docs/context/session-20260910-1127-main.md
[22:37:48] EDIT: File written — .commit-files
[22:37:56] EDIT: File written — .commit-msg
[22:38:00] GIT: Git operation — bash $HOME/.claude/scripts/git-commit.sh --push
```
