# Session Summary: 2026-06-27 - v2_phase34 (CST: 2026-06-27 08:50)

## Goals
- Begin ADR-192 Phase 3b+4 (the fused destructive cutover) on a fresh branch.
- Land W1 (pipeline plumbing) + W2 (RenderContext runtime) — the design-sensitive
  core — with the new phrase path wired and tested but NOT switched, so the build
  stays green and the old formatter path stays active (scope-doc sequencing step 1).

## Phase Context
- **Plan**: `docs/context/plan.md` — ADR-192 phrase-algebra; Phase 3b (FUSED w/ 4).
- **Scope**: `docs/work/dynamic-text/phase-3b-4-scope.md` §2 workstreams.
- **Branch**: `v2_phase34` cut from `main` this session (clean tree, no prior v2 branches).
- **Workstreams completed**: W1, W2.

## Completed

### Branch + environment
- Cut `v2_phase34` from `main`.
- Fresh checkout had no `node_modules`; ran `pnpm install --frozen-lockfile` (565 pkgs).
  The bin warnings (sharpee/repokit/shite/transcript-test) are expected — those
  packages' `dist/` is not built yet.

### W1 — pipeline plumbing
- **if-domain** `language-provider.ts`: extended the `LanguageProvider` interface with
  three optional methods — `getTemplate(messageId)`, `getLocaleSettings()`,
  `renderMessage(messageId, params, ctx): ITextBlock[]`. Additive/optional, so every
  existing implementer keeps compiling.
- **lang-en-us** `language-provider.ts`: `EnglishLanguageProvider` implements all three.
  `renderMessage` = perspective pre-pass (ADR-089, kept as a string step BEFORE parse)
  → `parsePhraseTemplate(resolved, params)` → `EnglishAssembler.realize(tree, ctx)`.
  A shared `private readonly assembler = new EnglishAssembler()` (pure → reusable).
  Unregistered IDs echo as a literal block (getMessage-parity fallback).
- **engine** `prose-pipeline/pipeline.ts`: `ProsePipeline` now takes an optional
  `world` (typed to a minimal structural `WorldModelLike`) and builds a per-turn
  render-context factory into `HandlerContext.makeRenderContext`. `createProsePipeline`
  factory and `game-engine.ts:300` updated to pass `this.world`.

### W2 — RenderContext runtime
- New `engine/prose-pipeline/render-context.ts`:
  - `createRenderWorld(world)` — read-only `RenderWorld` adapter (maps
    `getEntityContents` → model `getContents`); no mutation surface.
  - Inert placeholder seams: `PlaceholderReferenceContext` (lastMentioned→undefined,
    note→no-op, ADR-197), `EmptyTextStateStore` (ADR-196), `contribute` no-op (ADR-195).
  - `createRenderContextFactory(world, settings)` — binds the per-turn invariants once;
    `params` vary per message; seams shared across the turn.
- Exported from the prose-pipeline barrel.

### Tests
- `lang-en-us/tests/render-message.test.ts` (11) — getTemplate, getLocaleSettings,
  renderMessage round-trips (definite/indefinite/perspective/serial-comma), the
  echo-ID fallback, and PhraseParseError on unbound param + legacy `:`-chain.
- `engine/tests/prose-pipeline/render-context.test.ts` (6) — adapter delegation,
  per-turn seam sharing, inert-seam behavior, params-vary-per-message.

## Key Decisions
- **§7.3 (LanguageProvider API) resolved** as `renderMessage(messageId, params, ctx)`
  with the orchestration (perspective→parse→realize) living inside lang-en-us, not the
  engine. The engine only supplies the per-turn `RenderContext`. Matches the scope-doc
  signature; keeps language realization in the language layer.
- **`WorldModelLike` minimal interface** in render-context.ts instead of importing the
  full `WorldModel` — the prose pipeline declares only the three read methods it uses.
- **Did NOT declare `@sharpee/text-blocks` in lang-en-us package.json**: the workspace
  public-hoists `@sharpee/*` (pnpm-workspace `publicHoistPattern`), which is how the
  existing assembler already imports text-blocks undeclared. Declaring it forced a
  lockfile regeneration; reverted to match the existing convention and keep frozen
  install working. (Candidate cleanup later: declare all real `@sharpee/*` deps.)

## Build / Test Status
- `pnpm exec turbo run build --filter='@sharpee/engine...'` → **13/13 packages green**
  (core, text-blocks, if-domain, world-model, stdlib, lang-en-us, parser-en-us, engine, …).
- lang-en-us suite **310 pass**; engine suite **442 pass / 7 skipped**.
- New path is wired + tested but NOT switched — handlers still call `getMessage`.
  Old formatter path remains the live text path. Build is green.

## Next Phase
- **W3** — Verb atom (ADR-199): add `Verb` to the closed union, `{verb:is x}` syntax,
  Assembler Agreement-authority realization (suppletive table from `formatters/verb.ts`).
- **W6** — `{list:items}` → programmatic `PhraseList` binding (looking/going).
- **W4+W5** — the bulk: re-author 418 templates + migrate 236 `entityInfoFrom` sites,
  action-by-action; switch the pipeline to the phrase path; **build goes red on-branch
  here (expected, pre-merge)**.
- **W7–W10** — delete legacy infra/tests, ADR-158 amendment, walkthrough reconciliation,
  full `./repokit build dungeo` + `--chain wt-*`. Merge to `main` only when fully green.

## Open Items
- Full `./repokit build` (platform + stories + bundle) not yet run this session — the
  additive optional-interface changes can't break downstream type-checking, and turbo
  already compiled stdlib (an engine dep), but the dungeo story / bundle / devkit were
  not independently verified. Low risk; verify at next natural checkpoint.
- The Assembler's whitespace authority collapses `\n` (templates like the banner use
  newlines). `createBlocks` lifts newlines to block boundaries; the Assembler does not.
  This is a **W10 reconciliation** concern when the pipeline switches — noted, out of
  scope for W1+W2.
- Pin the `world → RenderContext` threading signature (carry-over): now concretely
  `WorldModelLike` + `createRenderContextFactory`. Resolved in practice.

## Files Modified
**if-domain** (1): `src/language-provider.ts` — interface +getTemplate/+getLocaleSettings/+renderMessage.
**lang-en-us** (1 src + 1 test): `src/language-provider.ts` (impl); `tests/render-message.test.ts` (new).
**engine** (4 src + 1 test): `src/prose-pipeline/render-context.ts` (new),
`prose-pipeline/pipeline.ts`, `prose-pipeline/handlers/types.ts`, `prose-pipeline/index.ts`,
`src/game-engine.ts` (construction site); `tests/prose-pipeline/render-context.test.ts` (new).
**docs** (1): `docs/context/plan.md` — Phase 3b progress note.

## Completed (continued — W3, case-B, Verbatim)

### W3 — Verb atom (ADR-199) — commit `d829d14a`
`Verb { lemma, subjectRef, person? }` added to the closed union + `NounPhrase.person`
agreement surface + `isVerb`. `{verb:lemma subject}` parse rule (unbound subject →
parse error). Assembler Agreement case: irregular table (is/are, was/were, has/have,
does/do, goes/go) lifted from `formatters/verb.ts`, regular `-s` strip, mass→singular,
PhraseList(>1)→plural, person first/second→plural (1st-sg suppletive). +12/+6 tests.

### case-B player-person (ADR-199 §4 B) — commit `6ba3b37d`
RenderContext gains required `narrative: { person, playerId? }`; LanguageProvider gains
`getNarrativePerson?()`. Resolved at realize time (Assembler matches subject
`referableId` vs `narrative.playerId`) rather than stamping in `nounPhraseFor` — actions
don't hold a RenderContext at report time. Engine factory builds narrative from
`world.getPlayer()` + provider person. +1 test.

### Verbatim atom (ADR-200) — commit `bc70dd10`
Implemented the reserved `Verbatim` kind (`text` field + `{verbatim:x}` parser bind +
Assembler whitespace-exempt case) as the home for non-entity scalars. This resolved the
W4 bare-placeholder fork **the Sharpee way** — bare `{x}` stays the entity NounPhrase
default (ADR-192 §5 unchanged); scalars use `{verbatim:x}`. The earlier idea of
redefining the bare-`{x}` parser default was a workaround and was dropped after user
feedback ("think how the Sharpee way would do this"). +3/+3/+1 tests. ADR-200 written.

## Key Decisions (continued)
- **ADR-199 §4 B resolved at realize time**, not in the producer (actions lack a
  RenderContext at report time). RenderContext carries `narrative`.
- **ADR-200 (Verbatim) over a parser-default change** — use the reserved atom the
  algebra already provides; keep bare `{x}` = entity NounPhrase. Documented in
  `w4-bare-placeholder-analysis.md` with the finalized W4 migration rules.

## Status update
- **The full atom set the cutover needs is implemented and green**: NounPhrase, Verb,
  Verbatim, PhraseList, Literal, Sequence, Empty. Render path + RenderContext runtime
  in place. New path WIRED but NOT switched (handlers still call `getMessage`).
- Build: turbo `@sharpee/engine...` 13/13 green. lang-en-us suite **334 pass**;
  engine render-context 6 pass.
- **5 commits** on `v2_phase34`: `44ff484f` (W1+W2), `d829d14a` (W3), `6ba3b37d`
  (case-B), `bc70dd10` (Verbatim), plus the branch cut.
- **NEXT**: the mechanical bulk (W4 templates + W5 producers + W6 list + pipeline
  switch), which turns the on-branch build/tests RED until the cutover completes and
  W10 reconciles the walkthroughs. This is the long grind — a dedicated push (or a
  parallelized re-author) from here.

## Completed (continued — the cutover bulk + activation)

### W4+W5 mechanical bulk (parallel workflow) — commit `3a7cb106`
99 file-agents (0 failures): 133 distinct `:`-chain placeholders → phrase grammar
+ 41 scalars → `{verbatim:x}` (59 lang files); 215 `entityInfoFrom`→`nounPhraseFor`
(40 stdlib files). Fixed one agent over-reach (`${kind}` JS interpolation in
sound-messages.ts). Build green.

### Pipeline switch + W6 + newline + perspective + basic-combat — commit `14739671`
- **Switch**: prose-pipeline handlers (domain-message/generic/game-message/audibility)
  → `renderMessage` via `phrase-render.ts` (`phraseAvailable`/`renderViaPhrase`/
  `flattenBlocks`); legacy `getMessage` kept only for world-less construction.
- **Assembler newline handling**: `realize()` lifts `\n` to block boundaries
  (`\n`→tight, `\n\n`→paragraph), matching `createBlocks`. +4 tests.
- **W6**: looking/going `{list:items}`→`{items}`; producers bind a `PhraseList`.
- **Perspective resolver params-aware** (the Sharpee fix): a bare `{word}` that is
  a bound param is left for the parser; every other bare `{word}` is conjugated as
  a verb — replaces the fragile verb allowlist (fixed `{hit}`→throw→blank).
- **basic-combat**: 3 `entityInfoFrom`→`nounPhraseFor` (extension, missed by the
  platform workflow).

### Cutover health (deterministic dungeo unit transcripts)
**1370/1481 passing (92.5%)**. rug-trapdoor 14/14; basic gameplay (move/take/look/
open/containers) renders correctly through the phrase path. Render-time template
failures now degrade gracefully (warn + inline fallback) instead of crashing a turn.

**Refined diagnosis**: only 2 templates actually THROW, both dungeo STORY messages
that emit the param at the event top level instead of under `params` (old formatter
hid it by leaving `{param}` literal). The remaining ~100 failures are
**predominantly story-side** (dungeo param-shapes, bare placeholders rendering
strings as "a X", GDT) + expected-value diffs + pre-existing transcript-format
errors. The PLATFORM phrase path is solid — the next phase is dungeo story
reconciliation (its own autonomous scope), not platform fixes.

## W10 reconciliation — remaining (next session(s))
The ~108 unit failures categorize as:
1. **Combat blank outputs** (~15) — some combat/npc-counterattack messages still
   render blank; basic-combat `target` now a NounPhrase but other combat messages
   need a look.
2. **Missed-scalar articles** — bare scalars left un-verbatim'd render "an Unknown
   GDT command", "a copyright" (about/version/GDT metadata). Add to verbatim.
3. **Bare-entity double-article** — "the a Volcano Bottom": a literal "the " + bare
   `{place}`→indefinite; proper-noun/place placeholders need `{the place}` or proper.
4. **Dungeo STORY templates** — `stories/dungeo` has its own templates (NOT in the
   platform workflow) with unbound params now throwing ("param 'direction' is not
   bound"). Story migration is its own pass (autonomous per policy).
5. **Expected-value diffs** — transcripts asserting old article/grouping text.
6. **Pre-existing transcript-format errors** (no-assertion/unknown-assertion/no-title)
   — NOT caused by the cutover; independent test-authoring issues.

Plus: full stdlib/lang unit suites (action tests assert old EntityInfo/getMessage
shapes — red, expected) need reconciliation; W7 (delete legacy formatter chain,
EntityInfo, entityInfoFrom) and W9 (ADR-158 amendment) still pending; then the
walkthrough `--chain wt-*` pass and merge to `main` only when fully green.

## Build note
The per-package `tsc` incremental build can leave stale `.tsbuildinfo` after a
manual `dist` delete (tsc skips emit thinking it's up-to-date) — delete
`*.tsbuildinfo` to force a clean emit. turbo's lang-en-us `outputs` key also
doesn't restore `dist` from cache; build it directly when the bundle looks stale.

## Notes
- ADRs 199 (Verb) + 200 (Verbatim) are the two follow-on atoms realized this session.
- 8 commits on `v2_phase34`; the phrase path is LIVE (not a parallel path). The
  branch build compiles; unit transcripts 92%. Merge to `main` gated on full green.

---

## Session Metadata
- **Status**: COMPLETE (W1+W2 slice of Phase 3b+4)
- **Blocker**: N/A
- **Estimated Remaining**: W3, W6, W4+W5 (bulk), W7–W10 — multiple sessions.
- **Rollback Safety**: safe — additive only, old path live, branch unmerged, no commit.

## Mutation Audit
- `renderMessage`, `createRenderContextFactory`, `createRenderWorld` are **pure** —
  they return values and mutate no persistent state (seams are deliberate no-ops).
  Tests assert on the returned blocks / context shape. No state-mutation to verify.

## Test Coverage Delta
- +11 lang-en-us (render-message), +6 engine (render-context). Net +17, all green.
