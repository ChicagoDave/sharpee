# Session Summary: 2026-09-05 - feat/secret-letter-port

## Status: In Progress

## Goals
- The re-plan pass Phase 10's exit state calls for: give P-10's checkable terms 2-5 (browser build, hosting, landing page + IFID, announcement) phases in `docs/work/secret-letter-port/plan.md` (David: "proceed").

## Completed
- Session start: recap presented, `pre-session-audit` relayed verbatim, profile fresh (2026-09-04), core concepts read, gate cleared.
- Research for the re-plan: P-10 in the proposal (terms 1-5, the new-IFID decision, the own-address constraint), Phase 3's consequence note and Phase 10's exit state, Chapter 12 in the change document (COMPLETE for Phase 11's purposes; content is David's). Verified: the IFID is already minted (`secret-letter.config.json`, header line 91); `./repokit build <story> --browser` resolves `branch-stories/` (`tools/repokit/src/repo.ts:175`); `./sharpee publish` zips (ADR-284 D2); sharpee.net hosts Fernhill and Dungeo under `website/public/web/` via `/play` and `website/deploy.sh:187`; story metadata (`story-version: 0.1.0`, the Chapter-1 description, the "Scaffold" README) is stale for release.

- The re-plan pass (session-planner): Phases 12-17 appended to `docs/work/secret-letter-port/plan.md` (lines 213-266) — 12 full-game playtest and polish (Large; the content-complete gate), 13 release-readiness metadata, 14 browser build (term 2), 15 hosting with David's where-it-lives decision gate (term 3), 16 landing page (term 4; the IFID half already closed by construction), 17 announcement checklist (term 5). Three existing lines extended, none rewritten: Phase 10's Status (exit state reached 2026-09-05, stays CURRENT), the P-10 trace row, vision item 5 (obligation discharged). `.current-plan` untouched.

- **Missing-lines web app (David: "Build a web app with a walkthrough of text where I can enter the missing text including variations")**: an Artifact at https://claude.ai/code/artifact/56fb7866-c966-450c-85c0-311ad095469c. A scratch extractor (`scratchpad/extract-stubs.mjs`) pulls every `(TODO during play-testing — …)` body from the story's Chord files with its enclosing construct (phrase / timer state / entity description or clause), the `##` comment block above it (the `## DAVID:` notes and Gentry's kept-aside text), and the file:line — **126 stubs**: 63 single lines and beats, 63 whole conversation trees (the Phase 7/8 rewrites, shown under their own filter). Play order by chapter file. Each stub: the brief, the context, one or more variation boxes (two pre-opened for `randomly`/`cycling` phrases), a status (not written / draft / done), and a note-to-Claude field. Entries save to the artifact's `db` store, collection `lines`, one doc per stub id (`<file>-<line>`), fields `vars[]`, `status`, `note`, `updatedAt`, plus `file`/`line`/`name`/`kind`/`chapter`; localStorage is the fallback. Read back with `read_db` on `lines` to fold David's text into the `.chord` files.

- Three rounds on the app (David, on mobile): a compact header under 820px (filter dropdown, hidden hint/sync/subtitle); **surrounding context** per card (the whole enclosing block, ±24 lines of the file, and every firing site across all files with its owning block, from the extractor's cross-file scan); **associated commands** per card — the real command path from the game start, taken from `./sharpee test branch-stories/secret-letter --json --capture-output` (106 root-to-leaf paths, 1468 command results) by matching each stub's brief against captured output (`scratchpad/match-tree.mjs`; **96 of 126 stubs are printed by the tree**, shortest three paths kept), plus a command derived from the wrapping clause for every firing site (`on the player <verb>` → verb + owner noun, `<dir> is blocked` → the direction in the room, `first time` → walk into the room, topics → `ask X about …`, greetings → `talk to X`, story actions → their first grammar word with the refusal condition). The 30 the tree never prints are mostly refusals and calm-walk Teisha topics.

- David: "Jack is never in the dress until the escape" — Jack's dressed-detail card was first in the walkthrough because it lives in the story file. Fixed the ordering: the runner's stream labels transcripts by branch room · first command (labels collide, e.g. `top-of-the-post · d` ×10) and each branch carries only its own commands with a `parent` id; the full root-to-leaf path is rebuilt from `secret-letter.tests.json`'s nested `branches` (ancestors up to the branch-point card + the branch's own cards), joined to the captured transcript by its own command list and the engine `turn` of its first card (106 transcripts, 5 unmatched keep their own commands only). Cards now sort within their chapter by the engine turn they first print (Jack's detail: turn 89, after `change outfit` at 88); unreached cards follow their nearest reached file-neighbour; each header carries a `turn N` chip.

- David's idea: a web client that plays normally and lets the author plug in text while stepping a full walkthrough. Assessed: everything exists (browser build, in-browser compiler, determinism → replay, the test tree as the walkthrough) except **prose provenance** — verified that no block carries a message id (`ITextBlock` is key/content/tight/className; the loader's only `phraseId` is the scoring/hunger rung lookup; the IDE's SourceMap is for JS stack frames). David: "Proceed", then "I don't need the mobile version (yet)" — so the consumer is Chord Writer's Play panel only. **ADR-333 written, DRAFT**: `docs/architecture/adrs/adr-333-prose-provenance-and-play-to-write.md` — D1 stamp `source.messageId` on every block `renderViaPhrase` returns; D2 the id is the whole stamp, spans resolved by the consumer from `IRPhrase.span`; D3 channels pass it, the browser renderer sets `data-message-id`; D4 click-to-edit in Play with save → reconcile → recompile → restart at the fixed seed → replay; D5 the walkthrough is a tests.json path; D6 stubs are an IDE convenience. Three open questions (descriptions and spliced snippets; platform messages; replay history persistence).

- ADR-333 interview (David: "Yes"): Q-1 **a** (descriptions stamped with the entity's key through ADR-107's id mode — the loader sets `descriptionId`, which `room.ts:101-110` already resolves and the examining handler gains; snippets' keys deferred to a `contributors[]` extension), Q-2 **a** (a platform line opens a new ADR-255 override pre-filled with the pack's text), Q-3 **a** (replay history is session-only; the tree is the durable walkthrough). `adr-review` 19/19 after four folded corrections (ADR-097→ADR-106 citation — the pipeline's code comment is wrong; ADR-255 file named; scope adds story-loader + stdlib; D1a rewritten against the code: the loader never sets `descriptionId`, only the room handler honors it). **ACCEPTED** by David the same day. Not implemented; platform work, discuss-first, needs its own plan.

## Key Decisions
- ADR-333 (ACCEPTED 2026-09-05): prose provenance — every block the phrase path realizes carries `source.messageId`; the consumer resolves spans; Chord Writer's Play panel becomes play-to-write. See `docs/architecture/adrs/adr-333-prose-provenance-and-play-to-write.md`.
- The re-plan amends the port plan in place (new phases after Phase 11) rather than opening a second plan: the plan recorded the obligation on itself, the topic is the same, and repointing would fire rule 18b for nothing.

## Open Items
- I-c8a56c-1 (carried): David's lines for the Chapter 6-9 placeholder beats and the DS38-39 conversion.
- I-c8a56c-2 (carried): GH #356, the stallkeeper patience counter — David's ruling pending.

## Files Modified
- `docs/work/secret-letter-port/plan.md` (Phases 12-17 appended — session-planner; Phase 10 Status, P-10 trace row and vision item 5 extended)
- `docs/architecture/adrs/adr-333-prose-provenance-and-play-to-write.md` (new; ACCEPTED)
- `docs/context/session-20260905-1532-feat-secret-letter-port.md` (this file)
- Not in the repo: the missing-lines Artifact (https://claude.ai/code/artifact/56fb7866-c966-450c-85c0-311ad095469c) and its scratch generators (`extract-stubs.mjs`, `match-tree.mjs`, `tree-run.json`) in the session scratchpad

## Notes
- Session started: 2026-09-05 15:32 CDT (session e0ba74)
