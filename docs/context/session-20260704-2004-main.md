# Session Summary: 2026-07-04 20:04 - main (book v2.0.0 Round C lock-down)

## Goals
- Pull the Docker naive-executor run 5 log (validating Round B) from the
  container before it went away.
- Round C "lock-down" pass, per David: fix run 5's reader-facing issues and
  call out the front-matter conventions in the chapters so a literal reader
  can't get lost.

## Completed

### Run 5 log pulled (David then wiped the container's my-zoo project; the
container itself, `confident_clarke`, stayed up)
- `docs/work/test-book/execution-log-20260704-run5.md` — **all 31 chapters
  PASS, 0 BLOCKED, 163/163 transcript assertions**, browser behavior verified
  with Playwright. Verdict: "the book survives a literal reader remarkably
  well." Seven reader-facing issues listed in its final summary.
- Also pulled the tester's working `my-zoo/src/index.ts` to the session
  scratchpad before deletion; used as ground truth for the Round C code.

### Round C edits (all uncommitted)
- `frontmatter/02-how-to-read.md`: the three load-bearing conventions are now
  **named rules** chapters cite: the import rule, the placement rule, the
  replacement rule.
- ch5, ch6, ch7, ch8 (flashlight + nocturnal exhibit), ch12, ch13: added
  one-line placement-rule/replacement-rule callouts where run 5 said placement
  depended silently on the front-matter convention.
- ch20 §20.4: the roomIds widening + two recording lines are now **printed**
  (was: described only), matching the tester's working code.
- ch23 §23.3: the entityIds widening (zooMap/brochure) + recording lines are
  now **printed** (was: a parenthetical).
- ch29 §29.1: the feed-the-goats transcript now has a file path
  (`tests/transcripts/feed-the-goats.transcript`).
- ch1 §1.8: CLI table gains a `sharpee build --test` row; bare `sharpee` now
  correctly described as printing help (with `sharpee list` for the registry).
- ch2 §2.9: the `npx` spelling switch is explained.
- ch21 §21.2: SceneTrait import no longer instructed for the zoo's scene; it
  is scoped to the illustrative timed example only.
- ch25 §25.5: "marks the spot with a comment" now names the scaffold's
  file-header comment (quote verified against
  `packages/devkit/templates/browser/browser-entry.ts.template`).
- Voice check: em-dash grep clean (single permitted ch19 verbatim quote).
- Snippets re-extracted (156 snippets, 31 chapters); `build-book.sh v2.0.0
  all` clean; site/ republished.

### Not fixed (by design / out of scope)
- ch28 requires GitHub access for its read-along tour (as designed).
- ch29 documents walkthrough chaining / event-state assertions / directives
  without exercising them in the single-file track.
- Observation: no `ch21` scene snapshot exists in
  `tutorials/familyzoo/v2.0.0/src/` (snapshots jump ch20 → ch22); nothing in
  the snapshot tree uses `createScene`/`SceneTrait`. Not flagged by run 5;
  left alone.

## Key Decisions
- Named-rule convention pattern adopted for the front matter so chapters can
  cite placement/import/replacement rules by name.

## Next Steps
- Round C PDF copied into the container
  (`confident_clarke:/home/node/repos/book-test/the-sharpee-book-v2.0.0.pdf`,
  chowned to node); `book-test/` is otherwise empty, so run 6 starts as a
  fresh first-time reader. After run 6: pull its log; if clean, commit Round C
  + the run 5/6 logs as the gate-closing commit.

## Files Modified
- 14 book content files (frontmatter + chapters listed above), regenerated
  `code-snippets/` (renumbering in ch20/ch23), `site/` html/epub/pdf,
  plus new `docs/work/test-book/execution-log-20260704-run5.md`.

## Round D (book-side lock-down toward a zero-issue run; uncommitted)

Run 6 pulled (`execution-log-20260704-run6.md`): all 31 chapters PASS, 0
BLOCKED, 0 deviations, 163/163; 8 editor-level notes. Round C committed as
`e93affa4`. David then asked how to reach a zero-issue run and approved the
book-side work:

- **Illustrative rule added** as the fourth named front-matter convention;
  explicit "nothing to type" markers at every spot runs 5-6 called ambiguous:
  ch9 region-handler sketch, ch14 four-phase skeleton, ch16 `dispenser.add`
  line, ch21 storm scene, ch25 score-star renderer (and the zoo.ambience
  renderer explicitly marked as the one that IS typed in).
- **Ch25/26 theme tension resolved**: BrowserClient listing labeled as the
  zoo snapshot's (illustrative), with a note that `zoo-sunny` doesn't exist
  until Chapter 26; scaffold's `modern-dark` is correct before then.
- **Ch22 scheduler placement pinned**: directly below the NPC registration,
  named as a chapter-says-otherwise exception to the placement rule.
- **Ch29 "Your first walkthrough" section added**: two instructed walkthrough
  files (wt-01-into-the-zoo, wt-02-feeding-time) exercising chaining,
  $save/$restore, [EVENT:], and [STATE:]; verified 6/6 green against the
  PUBLISHED @sharpee/*@2.1.0 packages in a scratchpad project scaffolded with
  the published devkit (tester's run-6 index.ts as the story). Ch29 opener
  fixed for single-file readers; STATE bullet rewritten to use exact created
  names (`yourself`, `bag of animal feed`).
- **Platform gap discovered** (needs David's decision): the book-QA fixes
  P1-P5 commit (8c5ddc30) and devkit P6/P7 (75fc4613) postdate the 2.1.0 npm
  publish (2026-07-03). On published 2.1.0: the `player` reserved word is
  absent and IdentityTrait name/alias resolution in [STATE:] is broken (both
  fixed in 8c5ddc30's runner.ts). Packages carrying unpublished fixes:
  engine (command chaining), stdlib (scenery listing, articles, NPC
  direction), transcript-tester (STATE resolution), devkit (P6/P7 build
  fixes). CORRECTION to an earlier note: [GOAL:] is NOT dropped at parse
  time on 2.1.0 (that probe file lacked the `---` header separator); but in
  ALL versions REQUIRES/ENSURES must sit directly under [GOAL:] and only
  fail the run under --stop-on-failure (runner.ts:278). Book ch29 bullet and
  accepted-limitations.md updated accordingly.
- **Accepted-limitations protocol addendum** written to
  `docs/work/test-book/accepted-limitations.md` for the next container brief.
- Voice checks clean; snippets re-extracted; `build-book.sh v2.0.0 all`
  rebuilt and republished `site/`.

## Run 7 CLEAN + Round E (gate-closing)

- David published @sharpee/*@2.1.1 (lockstep bump committed as 7292c8b6);
  Round D committed as c081609c. Round D PDF handed to the container.
- **Run 7 (`execution-log-20260705-run7.md`): CLEAN — all 31 chapters PASS,
  zero BLOCKED, zero deviations, 169 tests / 17 transcripts all green on
  2.1.1**, including the two instructed ch29 walkthroughs running as a chain.
  All issues pre-accepted per accepted-limitations.md except two editor
  notes, fixed in Round E:
  - ch7: lunchbox demonstration prop now carries the illustrative-rule
    "nothing to type" marker.
  - ch25: the typed zoo.ambience listing now includes its own
    `const renderer = client.getChannelRenderer();` line.
  - (also) ch29's printed wt-02 gained the `---` header separator; exact
    printed form re-verified 6/6 with --stop-on-failure against published
    packages.
- Voice check clean; snippets re-extracted; all three formats rebuilt and
  republished to site/.
- **The naive-regression gate for the v2.0.0 book is green.**

---

## Session Metadata
- **Status**: COMPLETE (gate green; Round E + run-7 log in the gate-closing
  commit)
- **Blocker**: none
- **Rollback Safety**: safe to revert — book content, regenerated snippets,
  site outputs, and doc files only; nothing platform-level touched.
