# Session Summary: 2026-08-01 23:46 - main (CDT)

## Goals
- Investigate GH #207 (Low Room franken-turn), design the fix, implement per ADR-295.

## Completed
- **#207 diagnosed** (repro at seed 42): post-hoc carousel daemon retcons completed movement (two arrivals per turn); Round Room daemon has the identical latent bug. Ordering aggravated by dead `_transactionId` scoping in prose-pipeline sort.
- **GH #208 filed**: sort.ts reads `_transactionId` that no producer ever writes — whole turn compares as one transaction, room descriptions hoist across sources.
- **ADR-295 written and ACCEPTED** (`docs/architecture/adrs/adr-295-computed-exits.md`): computed exits — topology/traversal split, story traits fulfill platform contract (Option A), per-world resolver registry (ADR-207/208 shape), resolve-once-in-execute, D7 interceptor scoping. `adr-review`ed 11/15, 2 BLOCKER + 5 SMALL, all folded, then accepted by David.
- **#207 commented** with root cause + ADR-295 pointer.
- **Implementation discussion held**: placements approved (exit-resolver-binding.ts beside the other registries; declaration contract in traits/room/); phases 1-2 approved as proposed; phase 3 ruling — implement the puzzle for Sharpee, not MDL: NO flag unification, gates stay `RoundRoomTrait.isFixed` / `dungeo.carousel.active`; only puzzle logic must match.

## Key Decisions / Findings
- ADR-295 ACCEPTED (see Status line for review trail).
- Event handlers react to facts; daemons must not retcon movement (ADR-295 D1).
- Phase 3 matches puzzle logic, not MDL internals (David, this session).

## Open Items
- Plan: `docs/work/computed-exits/plan-20260801-adr-295.md` — **Phase 1 COMPLETE** (world-model seam: contract, registry, RoomBehavior.getComputedExitDeclaration/resolveExit, AuthorModel delegations; 15 new tests; full world-model suite 1447 passing, 0 failures; package tsc --noEmit clean, ~23:53). AuthorModel-delegation test gap (mutation-verification finding) closed same session — 16 tests. **Phase 2 COMPLETE** (going: D3 existence in validate, resolve-once in execute + blocked path + Acceptance-7 no-crash guard, blocked/narration in report, D7 destination slot, going-data.ts reverse-scan deleted for sharedData.previousLocation; 9 new tests in going-computed-exits.test.ts). Evidence 2026-08-02 ~00:02: stdlib suite 1585 passed / 27 skipped / 0 failures; `./repokit build dungeo` clean; dungeo unit suite 1725 passed / 10 expected failures / 2 skipped (= baseline); walkthrough chain 952/952. Phase 2 mutation-verification gaps (vehicle × computed-exit; D7 claim untested) closed — 11 tests in going-computed-exits.test.ts. **Phase 3 COMPLETE** (dungeo swap): new `CarouselExitTrait` + shared resolver in `handlers/carousel-exit-resolver.ts` (same point names/streams — identical draw outcomes); `registerCarouselExits` in scheduler-setup; Low Room entry message now an actor_moved chain handler; deleted `carousel-handler.ts` + `round-room-handler.ts` (per accepted ADR-295 Implementation §3) + their stale dist artifacts; message constants moved to the resolver module. `carousel.transcript` tightened (#207 TODO → not-contains "Machine Room"). Goldens re-blessed: wt-07 (phantom Engravings Cave descriptions removed, Grail Room contents line restored), wt-10 (doubled Tea Room desc → single + bearings + correct contents). Evidence 2026-08-02 ~00:20: #207 repro at seed 42 = single arrival ✓; unit suite green at pinned seeds; chain 952/952 twice; `tsf build --npm` clean.
- **Pre-existing flake identified, NOT ADR-295**: clock-seeded unit transcripts (e.g. royal-puzzle-basic, no `seed:` header) intermittently die to thief combat ("stiletto severs your jugular" → engine stops → cascade failures). Reproduced deterministically at seed 1785647834031. Fix = pin seeds in those transcripts (not done tonight).
- **Observed while swapping**: RoundRoomMessages ids (`dungeo.round_room.compass_spinning`/`disoriented`) have NO addMessage registration — they render as nothing, before and after the swap. Preserved as-is (puzzle-logic parity); David may want texts registered.
- Carried: #206, #208 (new), D15 channel scoping / AC-8, other stories' transcript corpora, `IDebugEvent` dead tier, `ring-action.ts:141`, publish dry-run 4.3.0 state.

## Files Modified
- New: `docs/architecture/adrs/adr-295-computed-exits.md`, `docs/work/computed-exits/plan-20260801-adr-295.md`, this file.

## Session Metadata
- **Status**: COMPLETE — all three ADR-295 phases implemented and verified; #207 fixed. Uncommitted (committing is David's call).
- **Blocker**: N/A
