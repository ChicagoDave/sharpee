# Session 2026-07-31 22:00 (main) — Fable 5 fresh-eyes RNG assessment

## Goal
Fresh-eyes sweep of all RNG usage (ADRs 290–292 deliberately ignored), documented in `docs/work/rng/fable-assessment.md`, toward normalized RNG + deterministic testing against Dungeo + author play-testing of every randomization point.

## Status
COMPLETE — assessment written; no code changed.

## Key findings
- Five independent RNG stream owners; interfaces thread `SeededRandom` correctly but there is no seed authority, no `--seed`/injection surface anywhere, and the engine master stream isn't persisted in saves.
- Worst defects: module-level time-seeded RNGs in Dungeo melee + basic-combat (melee-npc-attack *ignores* its injected RNG); raw `Math.random` in round-room/bat/carousel/trivia handlers.
- Combinatorial explosion dissolves via outcome-class coverage + per-point forcing + stream independence (~25 gameplay choice points in Dungeo, ~60–80 outcome classes).
- Proposed 5-phase normalization order + 6 open questions at end of the assessment doc.

## Round 2 (2026-08-01)
Read `fable-followup.md` (round-2 questions); answered in `docs/work/rng/fable-followup-response.md`. Key moves: audio correction accepted; granularity revised per-domain → per-point streams; forcing = class-level, zero draws; registry tension resolved by handle-only draw API (declaration is the capability to draw); Phase 0 reframed as prerequisite folded into Phase 2; acceptance bar split (byte-identical rendered transcript = gate; byte-identical event logs deferred); concrete ChoicePoint/RandomService API worked against round-room, resolveBlow, thief steal.

## Round 3 (2026-08-01)
`fable-followup-2.md` answered in `fable-followup-2-response.md`. F1 confirmed (RandomService interface + ChoicePoint + definePoint + catalog + report types in core; implementation in engine) with full dependency sweep — no drawing package can import engine, all can import core; corrections: event-processor doesn't draw, character/tick-phases.ts:57 `random: unknown` is the disease manifest. Raw-SeededRandom rule: construction only in engine impl/test fixtures/Chord-until-folded, enforced by grep gate in repokit verify. F2: NO metadata — first-firing rule + try-budgeted search tool + force-prefix-then-search-last refinement. Blast radius measured: ~45 call sites + ~30 declarations.

## Next steps
Design accepted in shape; Phase A (seed authority + per-point streams + singleton removal + --seed/[SEED:]) ready to scope. Platform seam: RandomService on the four contexts.
