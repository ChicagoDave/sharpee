# Session 2026-07-11 01:00 — v2-210-chord-a (continuation)

## Goal

Chord Phase B (Zoo-complete, ADR-210): plan approved, Phase 1 checkpoints resolved, begin Phase 2 (frontend grammar). Same conversation as the 2026-07-10 sessions (Phase A gate-complete, formatting grammar, Dungeo sweeps).

## Status

**PHASE B GATE-COMPLETE (2026-07-11)** — all 7 phases done. Phase 7 closed: zoo.story (~770 lines, TS src/ untouched) with 3 `define text` hatches bound to new src/chord-extras.ts; AC-2 gate = 7 files / 61 assertions green via `node dist/cli/sharpee.js --test --story stories/friendly-zoo/zoo.story` (4 files excluded per David — all 9 corpus failures are host-shell phrase-algebra surfaces Chord lacks: initialDescription, ADR-209 snippets, ADR-195 slot contributors → Phase C backlog; documented in zoo-gate-audit.md "Gate scope decision"); AC-4 = tests/zoo-pure-ir.test.ts (pure-IR refuses real zoo.story pre-binding via Proxy tripwire; hatch-stripped variant builds full world, maxScore 100); AC-9 = src/event-contract.ts (type-only stdlib import of ActorMovedEventData — payload rename breaks story-loader build; stdlib added as devDependency) + tests/event-selector.test.ts; runtime.ts imports EVENT_TRIGGERS from the contract. Grammar log: no delta beyond the 6 approved entries. Final regression: chord 103, story-loader 60, cloak 81/81, zoo 61/61, dungeo unit 1697 passed (9 expected failures / 4 skipped), stdlib 1314, repokit builds clean. **Everything since ac69118a is uncommitted** (entire Phase B).

Phase 7 details: examining.ts got full interceptor hooks (David-approved); opening/closing completed partial hook coverage (postExecute — the gate-closed flag fix); phraseEvent now INVOKES hatch producers at staging (params carry atoms, not functions — fixed cloak's silently-broken {garbled} too); conditional blocked exits evaluate live via recomputeDerived (were permanent blocks); entity-field IR→world translation restricted to declared entity-typed fields (kind:'parrot' collision); feeding time = 4-step define sequence (TS fuse parity at turns 11/19); pet-award rule removed (TS parity: never lands); timeline/scoring/hatch-text transcripts authored against TS story then frozen.

Phase 6 delivered: profile option (devkit|pure-ir); pure-IR refusal BEFORE any module access (Proxy tripwire proves no author code reads); hatch binding by kind (text producers / action four-phase objects into getCustomActions / behavior CapabilityBehaviors into boundBehaviors) with per-kind shape-validation LoadErrors; hatch-free profile-equivalence test. loader 55 green, chord 103. Open design note: define-behavior attachment referencing is host-exposed only (boundBehaviors) — language construct deferred unless Phase 7 needs it.

Phase 5 delivered: buildSchedulerDaemons (every/once/sequence/every-turn-trait daemons; ALL state in chord.occurrence.* world state — no getRunnerState; transplant test proves resume); conditional composition legal for declared all-every-turn traits (chatty while not after-hours); can-see/reach = co-location; onEngineReady registers SchedulerPlugin (new plugin-scheduler dep); narrate:true on scheduler events. loader 49 green, chord 103, cloak 81/81. Seed gotcha: 42's int(1,2) stream opens 7 misses; tests use seed 11. David policy recorded this session: story randomness is NEVER seeded/disabled — transcript logic gates only (memory: never-turn-off-randomness; both dungeo assessment docs amended).

Phase 4 delivered: ChordDataTrait dynamic-typed trait instances (fields = own props, world-serialized); §5.4 second half — dispatch trait clauses → CapabilityBehaviors, standard clauses → per-trait-type interceptors; define action → four-phase dispatch actions via getCustomActions + extendParser grammar@150; refusal ladders; slots (primary+actor) in EvalContext; derived-verb when-rules fire in dispatch report (anything/entity/any-condition); award→awardScore ADR-129 dedup + setMaxScore; evaluator field reads/writes. Gotcha found: getBehaviorForCapability reads constructor-STATIC type — dynamic trait types need getBehaviorBinding(instance.type) lookup. Role-bound clauses throw at bind (post-Zoo scope). loader 43 green, chord 103, cloak 81/81.

Phase 3 delivered: full Phase B IR (traits/actions/scores/scheduler arrays, hasHatches, routing on clauses, rule-target union, open flags on conditions) + scope-aware analysis (trait fields, action slots, roles, the-actor, flags, boolean symbols) + §5.4 routing both halves + all new gates (unknown-role/slot/score, duplicate-score, open-condition-truth, closed-condition-selection) + when-verb derivation (petting→pets) + inline-phrase pass-1 registration + template-phrase marker exemption. Mid-phase David decisions: open-condition selections extend define condition (ratchet entry 2026-07-11; `any <name>` refs); condition names KEEP hyphens (key namespace). chord 103 green; loader/ide-protocol green; cloak gate 81/81.

Phase 2 delivered (chord frontend, all parsing): define trait (data one-of/optional/starts, embedded phrases via shared parsePhrasesBlock, it/role/every-turn on-clauses, before/after ordering), define action (patterns + → cardinality, constraints, refuse without/when, otherwise, bodies, dedent-terminated), action/behavior hatches, define score, once/every/define sequence, conditional blocked exits, dotted phrase keys, inline-prose phrase sugar, name-valued config, can see/reach predicates (evaluator throws till phases 4-5). Lexer: lone `"` = prose punct (multi-line dialogue). Fixtures zoo-actions/zoo-timeline/traits-basic + 4 malformed; chord 81 green; cloak gate 81/81 post-rebuild. Deliberate §3.2 deviation recorded in plan: quantifier/comparison conditions deferred (stdlib self-hosting scope, not AC-2).

## Key Observations

- Plan at docs/work/chord-phase-b/plan.md (7 phases; .current-plan repointed); plan-review clean against 9 references.
- David's Phase 1 decisions: stay on v2-210-chord-a; conditional blocked exits pulled into Phase B (grammar-log entry added; event-verb growth → Phase C); Zoo gate = freeze existing 8 transcripts + augmentation.
- Zoo-gate audit (docs/work/chord-phase-b/zoo-gate-audit.md): existing transcripts cover pet/feed/dispatch and partially the parrot; scheduler, scoring, and hatch categories are UNTESTED → augmentation is 3 transcripts (timeline, scoring, hatch-text), not 1. All underlying systems exist in stories/friendly-zoo/src.
- Also this conversation, before Phase B: Dungeo test-health assessment (docs/work/dungeo/assessments/full-sweep-20260710.md — unit green, chain 1-of-3 RNG-flaky) and Dungeo→Chord conversion assessment (docs/work/chord/dungeo-conversion.md — ~45% declarative under A+B, gap list §5 feeds Phase B/C).
- Platform-touch forecast for Phase B: zero packages/ deltas expected; any surprise = checkpoint, not silent scope.

## Decisions

- Phase B plan approved; proceed-to-implementation given by David (2026-07-11, four-answer checkpoint).
- Conditional blocked exits (`<dir> is blocked while <cond>: <phrase>`) approved for Phase B — grammar-log entry recorded.

## Artifacts

- docs/work/chord-phase-b/plan.md (+ Phase 1 resolutions), docs/work/chord-phase-b/zoo-gate-audit.md, chord-grammar-changes.md fifth entry.
- Uncommitted from earlier today: dungeo assessments ×2, dungeo transcript event-asserts, compose.test.ts, docs/work/chord/grammar.md + dungeo-conversion.md, memory split-big-sweeps.

## Next Steps

- Commit Phase B (everything since ac69118a is uncommitted) when David gives the word.
- Phase C candidates (from the gate exclusion + plan deferrals): initialDescription, ADR-209 room snippets, ADR-195 slot contributors; event-verb growth; role-bound trait clauses on standard actions; `each <condition>` iterate form; quantifier/comparison conditions (§3.2 deferral).
- The 4 excluded transcripts stay frozen in-repo and rejoin the gate when the grammar grows each feature.
