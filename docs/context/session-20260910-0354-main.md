# Session Summary: 2026-09-10 - main

## Goals
- Finish ADR-344 plan Phase 2 (D3): clear the 15 remaining engine test failures carried from the prior session.
- Resolve the final failure's underlying cause rather than working around it test-side.
- Write up and get accepted the ADR the guard-order defect raised.

## Phase Context
- **Plan**: docs/work/adr-344-role-holder-seam/plan.md — "Implement ADR-344 D1–D8 — the player role-holder invariant at the install seam"
- **Phase executed**: Phase 2 — "The story-less engine tests (D3)" (Small)
- **Tool calls used**: 137 / 150
- **Phase outcome**: Completed on budget

## Completed

### Engine test suite fully green (D3)
- Five files converted from bespoke or pre-D10 engine setup to the story-installing test helpers. Engine suite went from 15 failed / 749 passed / 7 skipped (carried in from session 7776c8) to **764 passed / 7 skipped / 0 failed** across 80 files.
- `unit/presence-tagging.test.ts` — the plan's flagged "non-mechanical" file: its bespoke two-room world, player, and thief now come from a story's `initializeWorld`, with ids captured for assertions (was constructing entities directly, no `ActorTrait`).
- `unit/engine-failure-reports.test.ts` — two platform-event-id cases switched to `setupTestEngineWithStory()`.
- `unit/engine-resume.test.ts`, `unit/player-death-routing.test.ts` — both had a pre-D10 defect: their local test stories' `initializeWorld` called `world.getPlayer()` before `createPlayer` had run, producing `Cannot read properties of undefined (reading 'id')`. Fixed by building and placing the player (with `ActorTrait`) directly in `initializeWorld`.
- `integration.test.ts` — "should create functional standard engine" switched to `setupTestEngineWithStory()`; its `start()`-with-no-story assertion was redundant with the D6a rejection test already in `game-engine.test.ts:208`.

### `installStory` guard reorder (`packages/engine/src/game-engine.ts`)
- The last failure, `story-install-order.test.ts`'s "an install after start() throws naming the running field," was not fixable test-side: ADR-344 D6a's `start()` now requires an installed story, so a running engine always carries one — the `this.story` check (run first) always answered install-after-start with "already installed," making the `this.running` check dead code.
- David approved reordering the two guards: `running` is now checked before `story`, with the rationale written in a comment above the code (not an ADR-number citation, per project convention).
- Verified the test pins the *order*, not just the message: reverted the reorder, confirmed the test fails with the "already installed" wording instead, restored the fix. `mutation-verification` independently re-verified via a `git stash` round-trip and graded every Behavior Statement line GREEN.

### Phase 2 exit gates — all run and passing
- `pnpm --filter '@sharpee/engine' test --run` → 80 files, 764 passed / 7 skipped, 0 failed.
- story-loader 1104, bootstrap 52, platform-browser 148, devkit 177 passed/1 skipped, helpers 13 — all unchanged from baseline.
- `./repokit build dungeo` → exit 0, bundle `dist/cli/sharpee.js` (4,431,547 bytes).
- `node dist/cli/sharpee.js --exec "look" --story stories/cloak-of-darkness/cloak.story` → exit 0, boots and renders the Foyer (run because a build exit 0 is not a boot — the Phase 1 defect that broke every story install had passed the build gate).
- `node dist/cli/sharpee.js --test --chain stories/dungeo/walkthroughs/wt-*.transcript` → 952 tests in 17 transcripts, 952 passed, all 17 goldens matched. One run, per the pinned-seed determinism rule.

### ADR-345 written and ACCEPTED
- The guard-order question was a symptom: the engine's lifecycle phase is read through five proxy fields (`running`, `story`, `_context`, `channelService`, `commandExecutor`) across six guards, and `resume()` must special-case `channelService` because `running === false` cannot distinguish "installed, never started" from "started, then stopped."
- Wrote `docs/architecture/adrs/adr-345-the-engine-lifecycle-phase-is-an-explicit-concept.md`. Six-question interview, all resolved (Q-1 confirmed the premise — "the explicit engine phases are legit"). `adr-review` ran 10/19 NEEDS WORK → five findings folded → 21/21 READY FOR IMPLEMENTATION. David accepted it ("accept it").
- 14 decisions (D1–D14), 8 acceptance criteria. Scope: `packages/core/src/events/game-events.ts` (D12), `packages/engine/src/game-engine.ts`, `packages/engine/src/turn/context.ts`, `packages/engine/src/session/save-restore-service.ts`, `packages/bridge/src/bridge.ts`, `packages/runtime/src/bridge.ts`.
- Deliberately kept out of the ADR-344 plan (D6): three of that plan's four remaining phases still move engine/install state.

## Key Decisions

### 1. Lifecycle states named as conditions, not outcomes
David's grammar question during the interview settled the four states as `empty → ready → playing ↔ stopped` — conditions, not event-outcome names.

### 2. Stop reason stays an emitted event, not duplicated on the phase
David's reasoning: it already reaches a message list; duplicating it on the phase state would repeat the exact proxy-field-sprawl complaint the ADR itself raises.

### 3. `restart` is engine disposal, not a phase transition
Keeps the phase model to the four states above rather than adding a fifth.

### 4. D12 fixes a live rule-10 violation in `resume()`
`resume()` currently sets `running = true` and emits nothing; on the transcript-tester's RETRY path a session can go dead-to-live with no stream record. D12 adds a `game.resumed` event.

### 5. Two `adr-review` findings landed against the ADR's own draft text
An unverified "all three restarting hosts" claim in D10 (only one host actually has a restart path) and no named amendment owner for ADR-344 D6a — both folded before acceptance.

## Next Phase
- **Phase 3**: "The install seam validates the role holder (D1, D2, D4)" — Medium tier, 250 tool-call budget.
- New install step `validate-role-holder` in `packages/engine/src/install/`, `requires: ['create-player']`, inserted between `createPlayerStep` and `listenerTraitStep`; throws (not a diagnostic) on the first failed condition, checked in order: unplaced, no `ActorTrait`, not `isPlayable`. Plus a doc-comment contract on `Story.createPlayer` and four new tests (three rejection, one positive).
- **Entry state**: Phase 1 landed and green; David has already approved this phase's platform touch (`packages/engine/src/install/`).
- Implements ADR-344 D1, D2, D4.

## Open Items

### Short Term
- I-b314e2-1: ADR-345 D6 — the engine-lifecycle-phase work is deliberately scheduled as its own plan, started only after the ADR-344 role-holder-seam plan closes at Phase 6.
- I-b314e2-2: ADR-345 D13 — the session implementing ADR-345 D2 must amend ADR-344 D6a in the same commit, since D6a quotes the refusal string D2's change will make stale.

### Long Term
- N/A

## Files Modified

**Source** (1 file):
- `packages/engine/src/game-engine.ts` — reordered `installStory()`'s `running`/`story` guards (running checked first), with the load-bearing rationale written in a comment above the code.

**Tests** (6 files):
- `packages/engine/tests/unit/presence-tagging.test.ts` — bespoke world rebuilt inside a story's `initializeWorld`.
- `packages/engine/tests/unit/engine-failure-reports.test.ts` — switched to `setupTestEngineWithStory()`.
- `packages/engine/tests/unit/engine-resume.test.ts` — fixed pre-D10 `initializeWorld` player-lookup ordering.
- `packages/engine/tests/unit/player-death-routing.test.ts` — same pre-D10 fix.
- `packages/engine/tests/unit/story-install-order.test.ts` — updated to pin the new guard order.
- `packages/engine/tests/integration.test.ts` — switched to `setupTestEngineWithStory()`.

**Docs / plan** (2 files):
- `docs/architecture/adrs/adr-345-the-engine-lifecycle-phase-is-an-explicit-concept.md` — new, ACCEPTED this session.
- `docs/work/adr-344-role-holder-seam/plan.md` — Phase 2 marked DONE, Phase 3 marked CURRENT (since 2026-09-10).

**Build-generated, not hand-edited** (2 files):
- `packages/sharpee/docs/genai-api/engine.md`
- `stories/dungeo/src/version.ts` (`BUILD_DATE` only, no version bump)

## Notes

**Session duration**: ~50 minutes.

**Approach**: Per-file, cause-specific fixes rather than a blanket helper swap — five of six remaining failures were pre-D10 story fixtures or missing-prerequisite call sites; the sixth was a real production defect (dead guard code) surfaced by a test, fixed with David's approval, and turned into ADR-345 rather than patched around.

**Ledger note**: session state file's `files` array is missing three test files (`engine-resume.test.ts`, `player-death-routing.test.ts`, `integration.test.ts`) because they were edited via shell heredoc per this session's Auto Mode instructions rather than the Edit tool; the full set above is cross-checked against `git status`. `docs/context/session-20260909-2339-main.md` was already modified before this session started and was not touched this session.

---

## Session Metadata

- **Session**: b314e2
- **Status**: COMPLETE
- **Blocker** (if any): N/A
- **Blocker Category**: N/A
- **Estimated Remaining** (if incomplete): N/A
- **Rollback Safety**: safe to revert — one small source guard-reorder, six test files, an ADR, a plan-status update, and build-generated artifacts; no migrations or destructive operations.

## Dependency/Prerequisite Check

- **Prerequisites met**: Phase 1 (D6, D6a) landed and green at session start, verified by the prior session's exit gates; David's platform-change approval for `packages/engine` already in force for this plan.
- **Prerequisites discovered**: none.

## Architectural Decisions

- **ADR-345**: The engine's lifecycle phase is an explicit concept — written and ACCEPTED this session (14 decisions, 8 acceptance criteria). Rationale: guard behavior was reading a single conceptual phase through five unsynchronized proxy fields, and `resume()` had a live rule-10 violation (a phase change with no emitted event).
- Pattern applied: none new this session. ADR-344 D6a's refusal-string comment is now flagged (I-b314e2-2) for amendment whenever ADR-345 D2 lands.

## Mutation Audit

- Files with state-changing logic modified: `packages/engine/src/game-engine.ts` (`installStory()` guard order).
- Tests verify actual state mutations (not just events): YES (evidence: `pnpm --filter '@sharpee/engine' test --run` → 80 files, 764 passed / 7 skipped, 0 failed, run 2026-09-10 09:11 UTC, after the last edit to `game-engine.ts` at 09:10 UTC and to all six test files; `mutation-verification` independently re-verified the guard-order behavior via a `git stash` round-trip and graded all Behavior Statement lines GREEN, completed 09:11:31 UTC).
- If NO: N/A.

## Recurrence Check

- Similar to past issue? NO — the "single lifecycle concept read through multiple unsynchronized proxy fields" defect class is new to this codebase; ADR-345 is the first ADR naming it, and no prior session summary in `docs/context/` describes the same pattern.

## Test Coverage Delta

- Tests added: 0 net new (six existing tests/fixtures converted or fixed; file count held at 80).
- Tests passing before: 749 passed / 15 failed / 7 skipped → after: 764 passed / 0 failed / 7 skipped (evidence: `pnpm --filter '@sharpee/engine' test --run`, 2026-09-10 09:11 UTC, session b314e2 — also recorded in `docs/work/adr-344-role-holder-seam/plan.md` Phase 2 "Exit state VERIFIED").
- Known untested areas: ADR-345's D2 (explicit phase type) and D12 (`game.resumed` event) are decisions only — no implementation or tests exist yet; scheduled as a future plan per ADR-345 D6 (I-b314e2-1).

---

**Progressive update**: Session completed 2026-09-10 04:44 CDT
