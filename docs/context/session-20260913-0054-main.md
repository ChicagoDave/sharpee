# Session Summary: 2026-09-13 - main

## Goals
- Evaluate whether OpenSilver (with a .NET-native desktop host, not Tauri) can replace ADR-341's WPF ruling for Chord Writer on Windows, and eventually macOS.

## Phase Context
- **Plan**: `docs/work/opensilver-ide-evaluation/plan.md` — "An evaluation, not a build. Determine, on evidence, whether OpenSilver … can replace ADR-341's WPF ruling for Chord Writer."
- **Phases executed**: Phase 0 ("Prerequisites, desktop-host identification, scaffold, and closing #439"), Phase 1 ("Native host capability check — the kill phase", Integration Reality/rule 13a), Phase 2 ("Web-pane hosting"), Phase 3 ("Editor"), Phase 4 ("Custom-drawn surfaces and appearance") — all Small/Medium tier, all DONE this session.
- **Tool calls used**: 422+ against a 100-call Phase-0 budget per `.session-state-30faa2.json` (five phases run in one session; the budget field reflects only the phase captured at session start).
- **Phase outcome**: All five phases completed with PASS verdicts (Phase 3 measured PASS, felt judgment left open by design). Ran well over the nominal per-phase budgets but on schedule for the plan's own five-phase evidence-gathering scope.

## Completed

### Phase 0 — Prerequisites and desktop-host identification
- Scaffolded OpenSilver 3.3.3 at `/Users/david/repos/spikes/opensilver-ide/hello/` via `dotnet new opensilverapp --usePhotino`; both the Photino (native AppKit window + WKWebView, .NET runtime, not WASM) and Browser (WASM) targets build and run on .NET 10.0.300 with no side-by-side SDK needed. Evidence: `evidence/phase-0-photino-hello.png`, `evidence/phase-0-browser-hello.png`.
- GH #439 closed with evidence from `~/repos/SecretLetter2026/claude-rundown.md`: that re-host migrates 2009 Silverlight 3 source, so it demonstrates OpenSilver's designed sweet spot, not greenfield XAML authoring (which the IDE would be).

### Phase 1 — Native host capability check (Integration Reality, rule 13a)
- REAL-PATH tests, no stubs: XAML code-behind spawns the real `sharpee compose --json` with streamed stdout, writes/reads the real `~/Documents/OpenSilver Capability Check/` folder, and runs the vendored Node (v22.23.1) via a bundled toolchain from `tools/ide/vendor-toolchain.sh`.
- `Hello.Host.Tests` (xunit): **Passed! Failed: 0, Passed: 9, Skipped: 0, Total: 9, Duration: 1s** (evidence: `phase-1-host-capability.md:103`, dated 2026-09-13, after the tested code's last edit).
- Filed GH #457: the vendored toolchain's `build` fails for any Chord story because `packages/character/package.json` (and six sibling packages) ship `files: ["dist"]` but `exports.import` points at `./dist-esm/index.js`, so `pnpm deploy` omits it — a platform packaging gap, not an OpenSilver limitation.

### Phase 2 — Web-pane hosting
- Play, Testing, and Docs panes all load real bundles inside the OpenSilver/Photino app over custom-scheme origins; fernhill (David's instruction: "the only story to test with is fernhill") drove a full ADR-307 Testing-pane round trip (31-card tree replayed, one typed turn, 32-card document posted back to `out/`, fernhill itself untouched).
- Finding for D3: panes detect their host via the WebKit-specific `window.webkit.messageHandlers` shape; a non-WebKit host must counterfeit that read-only object wholesale. Owned gap: the Browser/WASM target has no custom-scheme origin.

### Phase 3 — Editor
- CodeMirror 6 plus the real Chord lexer (bundled from `packages/chord/src/lexer.ts`) hosted over a third custom scheme. On a real 1755-line Chord file (fernhill + ides-of-march head, 8,545 tokens): whole-document style pass 0.92 ms/pass (50-pass loop), 200 typed chars at 1.09 ms each, working select/scroll-to-span, undo-restored content hash, live wrap.
- Comparison to the WPF spike's 14.8–15.9 ms figure is explicitly "in kind, not like-for-like" — different highlighters (line lexer vs. tree-sitter+query). No Chord tree-sitter grammar exists to try. Felt judgment ("native or a web page in a box") is David's and remains open — the phase's own exit state does not make that call.

### Phase 4 — Custom-drawn surfaces and appearance
- Verdict: OpenSilver is retained composition (DOM/SVG, no `OnRender`/`DrawingContext`) — WinUI 3's side of `decision.md` caveat 2, with an `HtmlPresenter <canvas>` as an immediate-mode escape hatch WinUI 3 lacks.
- Built against David's prompt ("three horizontal panes, one clearly for debugging"): full `MainWindow.swift`-mirrored shell (chrome band, rail, splitters, right/bottom tab strips, status bar), `ThemeManager.cs` (18 tokens), tab strip, editor tab bar, World map (fernhill, two levels), project pane, `MenuBar.cs` (OS-agnostic, OpenSilver 3.3.3 has no native Menu control), `IndexView.cs`, `WorldView.cs`.
- ADR-297 live theme flip: DOM rail `rgb(220,224,232) → rgb(22,23,29)` after one `Apply()`, 0.9 ms, no rebuild — **PASS**.
- Three hands-on bug passes from David's own runs: iframe drag guard (frozen dividers fixed), no `SynchronizationContext` in the OpenSilver Photino host (post-await UI updates needed explicit dispatch — a general rule for any OpenSilver shell), Test Run wired to `sharpee test --tree --capture-output --capture-world --json`, audio inlined as `data:` URLs (WKWebView needs Range support a Photino scheme delegate can't give).

## Key Decisions

### 1. The desktop host is .NET-native (Photino), not Tauri
David corrected the planner's initial framing mid-session ("why would we pair Tauri with OpenSilver?"). This meant subprocess spawn, filesystem access, and the vendored Node toolchain are plain `System.Diagnostics.Process`/`System.IO` calls — the same primitives WPF would use — collapsing the planned "IPC-bridge build" phase into a smaller capability-check phase. Re-briefed by `session-planner` before Phase 0 started.

### 2. fernhill is the only test story from Phase 2 onward
David's instruction retargeted Phase 1's dedicated `~/Documents/OpenSilver Capability Check/` fixture and all later phases to fernhill; the fixture is now orphaned (tracked as issue #458 below).

## Next Phase
- **Phase 5**: "Parity column, decision record, and (on confirmation) a new ADR draft" — fill an OpenSilver column onto the existing ADR-341-spike parity table, write an evidence-first decision record, disposition GH #438, and (only on David's explicit ADR-worthy confirmation) draft a new ADR that supersedes or confirms ADR-341's D2/shape ruling. ADR-341 itself is not edited.
- **Tier**: Medium (200-call budget).
- **Entry state**: Phases 0–4 each already carry an inline evidence record (pass/fail/partial, none left unresolved) — met. Marked CURRENT (since 2026-09-13) in `plan.md`.
- Phase 6 (Windows re-check, separate machine) remains PENDING, scheduling-dependent, and may run out of order relative to Phase 5.

## Open Items

### Short Term
- I-458: Disposition of orphaned Phase 1 OpenSilver fixture directory (`~/Documents/OpenSilver Capability Check/`, superseded by fernhill in Phase 2; not deleted, awaiting David's decision).

### Long Term
- (Plan-carried, not duplicated into the issue store — see `plan.md` "Open questions carried into this evaluation"): whether OpenSilver's editor needs a felt, hands-on comparison session beyond Phase 3's measured pass; GH #440 (TreeSitter.DotNet maintenance) as shared risk if Chord tree-sitter ever lands.

## Files Modified

**Plan and phase records** (7 files):
- `docs/work/opensilver-ide-evaluation/plan.md` — phases 0-4 marked DONE (by the session as it progressed); Phase 5 advanced to CURRENT (since 2026-09-13) by this summary.
- `docs/work/opensilver-ide-evaluation/phase-0-prerequisites.md` through `phase-4-shell-and-appearance.md` — one record per phase, each with inline command/output evidence.

**Evidence** (18 files): PNGs and logs under `docs/work/opensilver-ide-evaluation/evidence/` — screenshots (Photino/Browser hello, capability check, panes, editor, shell light/dark/index/world/build/test-run/audio) and text logs (capability, pane, editor, theme).

**Session records** (3 files): `docs/context/.current-plan` (new pointer), `docs/context/session-20260913-0104-main.md` (progressive record this summary consolidates — see Notes), `docs/context/session-20260912-2031-main.md` (touched by hooks, not authored this session).

**Spike code — outside this repository** at `/Users/david/repos/spikes/opensilver-ide/`: `hello/` (OpenSilver app: `Hosting/{IHostServices,NativeHostServices,BrowserHostServices,PaneServer,PaneBridge,SpikePaths}.cs`, `Shell/{TabStrip,EditorTabBar,ProjectPane,WorldMapView,WorldView,IndexView,MenuBar}.cs`, `Theme/ThemeManager.cs`, `MainPage.xaml(.cs)`, `Hello.Photino/Program.cs`, `Hello.Host.Tests/{NativeHostServicesTests,PaneServerTests}.cs`), `editor/` (CodeMirror 6 bundle: `build.mjs`, `src/{editor.css,index.html,main.ts}`), `toolchain-staging/` (vendored toolchain), `out/` (compose/test outputs, fernhill untouched).

## Notes

**Session duration**: ~7 hours (00:54–08:39 CDT per event log timestamps).

**Approach**: Evidence-first spike-then-record methodology — every phase's acceptance-gating claim is backed by a command and its output, inline, in a dedicated phase-record file (per this project's "evidence inline, never cited" standard), with screenshots as evidence for anything visual. Spike code lives entirely outside the repository at `/Users/david/repos/spikes/opensilver-ide/`; only the plan, phase records, and evidence live under `docs/work/opensilver-ide-evaluation/`.

**Consolidation note**: `docs/context/session-20260913-0104-main.md` is the progressive record this summary was consolidated from — it remains in place (not deleted) and carries additional blow-by-blow detail (e.g. the four Phase-4 hands-on iteration passes) beyond what this summary compresses.

**Test-count discrepancy found during corroboration**: the progressive record and this session's own narrative describe `Hello.Host.Tests` as reaching "15 passing" by Phase 3 (after `PaneServerTests.cs` was added in Phase 2). The only inline evidence with an explicit count is Phase 1's `Passed! - Failed: 0, Passed: 9, Skipped: 0, Total: 9` (`phase-1-host-capability.md:103`). No phase-2/3/4 record or the session event log (`.devarch-events-30faa2.jsonl`, which logs `dotnet test` runs as "status unverified") carries an explicit post-Phase-1 pass count. The "15 passing" figure is therefore `[reported by session, unverified]`.

**Integration Reality (rule 13a) check**: Phase 1's name and deliverable are subprocess/filesystem integration work. Its Integration Reality bar is met — REAL-PATH tests exist and passed against the real `sharpee` CLI, real Documents folder, and real vendored Node, with dated inline evidence (`phase-1-host-capability.md:103`). No carve-out. Phases 2–4 do not match the rule 13a keyword set.

---

## Session Metadata

- **Session**: 30faa2
- **Status**: COMPLETE (Hello.Host.Tests re-run at finalize, 2026-09-13: `dotnet test Hello.Host.Tests` → Passed! Failed: 0, Passed: 15, Skipped: 0, Total: 15 — the 9 at Phase 1 grew to 15 with Phase 2's six PaneServer tests)
- **Blocker**: N/A
- **Blocker Category**: N/A
- **Estimated Remaining**: N/A (not incomplete; Phase 5-6 are the plan's own next steps, not unfinished session work)
- **Rollback Safety**: safe to revert — all repo changes are documentation/plan/evidence files; the OpenSilver spike itself lives outside the repository entirely.

## Dependency/Prerequisite Check

- **Prerequisites met**: .NET SDK 10.0.300 (sufficient, no 8/9 side-by-side install needed); Node 25.8.1; `wasm-tools` workload installed for the Browser target; `tools/ide/vendor-toolchain.sh` produced a signed, sealed toolchain for Phase 1's capability check.
- **Prerequisites discovered**: none blocking — OpenSilver.Photino needed no prerequisite beyond the templates package (`OpenSilver.Templates@3.3.3`).

## Architectural Decisions

- None this session — ADR-341 is explicitly untouched by this plan (its Scope excludes OpenSilver); any new ADR is deferred to Phase 5, contingent on David's explicit ADR-worthy confirmation (rule 11).
- Pattern applied: rule 13a Integration Reality Statement discipline for Phase 1 (see Notes); "evidence inline, never cited" project convention for every phase record.

## Mutation Audit

- Files with state-changing logic modified: spike-only, outside the repo (`Hello/Hosting/*.cs`, `Hello/Shell/*.cs`) — no `packages/` or `stories/` production code touched.
- Tests verify actual state mutations (not just events): YES (evidence: `phase-1-host-capability.md:103` — 9/9 xunit tests passing, asserting on real file-read-back content and real process exit codes, not mocks).
- N/A for Phases 2-4: no new xunit assertions added there per the phase records; verification was manual (`--auto` runs, screenshots, DOM-value reads) rather than unit-test assertions.

## Recurrence Check

- Similar to past issue? NO — this is a new evaluation track; GH #457 (vendored toolchain omitting `dist-esm`) is a newly discovered platform packaging gap, not a repeat of a previously logged pattern in this project's history that I could find.

## Test Coverage Delta

- Tests added: `Hello.Host.Tests` grew from an unspecified starting point to 9 (Phase 1, corroborated) and reportedly to 15 by Phase 3 (uncorroborated — see Notes).
- Tests passing before: 0 (new spike project) → after: 9 corroborated (evidence: `phase-1-host-capability.md:103`, dated 2026-09-13) / 15 reported but unverified.
- Known untested areas: Phases 2-4's shell, panes, and editor surfaces are verified by manual `--auto`/hands-on runs and screenshots, not by added xunit assertions.

---

**Progressive update**: Session completed 2026-09-13 08:39
