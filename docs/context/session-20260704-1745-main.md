# Session Summary: 2026-07-04 - main (book-v2-edition naive-regression gate)

## Goals
- Close out the Docker naive-executor gate for the v2.0.0 book: pull each
  container run's log, fix the chapters it flags, rebuild/republish, and hand
  the next run a cleaner book — repeating until a run comes back clean.
- Land ADR-209 (room-description snippets) and codify book voice rules.

## Phase Context
- **Plan**: `docs/work/book-v2-edition/plan.md` — all 8 phases DONE/CLOSED as of
  2026-07-03 (Phase 8 closed staleness-only; David took the book to the Docker
  naive-executor gate himself, per that phase's exit note).
- **Phase executed**: none — this session's work is post-plan follow-up driven
  directly by Docker container run logs, not a tracked plan phase.
- **Tool calls used**: 407 (no phase budget; plan has no open phase).
- **Phase outcome**: N/A (no phase in flight).

## Completed

### Committed this session (commits f4219d92..62d5b9c4)
- Book v2.0.0 Phases 1-2 rewrite work, voice-rule sweep (30 em-dashes removed),
  `docs/book/CLAUDE.md` voice/style rules, ADR-209 draft and acceptance.

### Uncommitted — naive-regression fix Round A (after container run 3, log
`docs/work/test-book/execution-log-20260704-run3.md`, 29 PASS / 2 issues / 0
BLOCKED)
- ch28 (`parts/part-8/28-the-multi-file-story.md`): reframed as the book's one
  explicit **read-along chapter** — opening note added, "nothing to type in
  this chapter" stated, takeaway's "Version 17" reference replaced with the
  `ch28-multi-file/` snapshot name.
- `frontmatter/02-how-to-read.md`: carve-out added to the "anything you read
  here, you can run" promise, naming ch28 as the one exception.
- ch23 (`parts/part-6/23-scoring-and-endgame.md`): leaked editorial notes
  removed from the scoring transcript; the victory-timing comment rewritten as
  reader-facing prose; the feeding award printed in full instead of elided.

### Uncommitted — naive-regression fix Round B (after container run 4, log
`docs/work/test-book/execution-log-20260704-run4.md`, 26 PASS / 5 minor issues
/ 0 BLOCKED, includes Playwright browser verification)
- ch14 (`parts/part-4/14-custom-actions.md`): import block marked
  complete-set/add-only-missing.
- ch23: feeding snippet no longer re-declares `const target`; photograph
  snippet corrected to rename `_context` -> `context` (run 4 finding: the
  printed Chapter-14 signature uses `_context`, pasting the fragment as-is
  didn't compile); penny-award insertion point stated; transcript comment
  shortened to fit the 6x9 print measure.
- ch1 (`parts/part-1/01-installing-sharpee.md`): init-browser step fully
  described (4 artifacts + `npm install` note); added a version-proof
  "follow the book over CLI hints" rule.
- ch2 (`parts/part-1/02-your-first-room.md`): trait list corrected to
  construct four traits and import `SceneryTrait` for ch5 (run 4 finding:
  ch2 said "five traits" but `SceneryTrait` was only imported, never
  constructed); sample output marked as trimmed.
- ch5 (`parts/part-2/05-scenery-and-portable-objects.md`): added a note that
  the souvenir penny is not re-shown (run 4 finding: ch5 claimed "the whole
  version is in one listing" but omitted the penny).
- ch25 (`parts/part-7/25-the-web-client.md`): renderer registrations now
  located in `src/browser-entry.ts` (verified against the devkit scaffold
  template), plus a scaffold-particulars-differ note.
- All rounds: voice rules held (0 new em-dashes); snippets re-extracted after
  every edit; html/epub/pdf rebuilt via `build-book.sh v2.0.0 all` and
  republished to `site/`.

### Uncommitted — container run logs pulled out of the Docker environment
- `docs/work/test-book/execution-log-20260704.md` (run 2, post-fix book: 27
  PASS / 3 issues / ch28 BLOCKED — the run that motivated Round A's ch28
  read-along reframing).
- `docs/work/test-book/execution-log-20260704-run3.md` (run 3: 29 PASS / 2
  issues / 0 BLOCKED).
- `docs/work/test-book/execution-log-20260704-run4.md` (run 4: 26 PASS / 5
  minor issues / 0 BLOCKED, first run to include Playwright browser
  verification).
- Run 5 is executing in the container against the round-B PDF; its log has
  not yet been pulled.

## Key Decisions
- **ADR-209 accepted** (`docs/architecture/adrs/adr-209-room-description-snippets.md`,
  commit 62d5b9c4): room-description snippets, all 7 open questions resolved
  by David, adr-review 13/13.
- **Plan Phase 8 closed as staleness-only**: completeness gaps are surfaced by
  the real Docker naive-executor run rather than a repair phase (decision
  recorded in the prior session, exercised by this session's fix rounds).
- **Book voice rules codified** in `docs/book/CLAUDE.md`: no em-dashes in
  prose, complete sentences, no "lost meaning" paragraphs; appendices remain
  exempt.

## Next Phase
- Plan complete — all phases done. Follow-on work (not a plan phase): pull
  run 5's log; if clean, declare the naive-regression gate green and fold
  Round A/B changes plus the three execution logs into the finalize commit.
  If run 5 finds new issues, apply a Round C fix following the same
  fix-rebuild-republish loop.

## Open Items

### Short Term
- Pull and read container run 5's log (in flight against the round-B PDF).
- If run 5 is clean: commit Round A + Round B book changes, the three
  execution logs, and the regenerated `site/` artifacts as the finalize
  commit for the naive-regression gate.
- If run 5 flags new issues: Round C fix-rebuild-republish, then re-run.

### Long Term
- GitHub book links (`tree/main/tutorials/familyzoo/v2.0.0/...`) resolve only
  once `v2_familyzoo_split` merges to `main` (carried forward from Phase 4).

## Files Modified

**Uncommitted book content** (9 files):
- `docs/book/v2.0.0/frontmatter/02-how-to-read.md` - ch28 read-along carve-out
- `docs/book/v2.0.0/parts/part-1/01-installing-sharpee.md` - init-browser detail + version-proof rule
- `docs/book/v2.0.0/parts/part-1/02-your-first-room.md` - trait count/import fix
- `docs/book/v2.0.0/parts/part-2/05-scenery-and-portable-objects.md` - penny not-re-shown note
- `docs/book/v2.0.0/parts/part-4/14-custom-actions.md` - import block complete-set note
- `docs/book/v2.0.0/parts/part-6/23-scoring-and-endgame.md` - transcript/comment cleanup, award printed in full
- `docs/book/v2.0.0/parts/part-7/25-the-web-client.md` - browser-entry.ts location
- `docs/book/v2.0.0/parts/part-8/28-the-multi-file-story.md` - read-along chapter reframe

**Uncommitted snippets/build outputs** (regenerated, not hand-edited):
- `docs/book/v2.0.0/code-snippets/CATALOG.md`, `ch01-installing-sharpee/06-playing-it.sh`, `ch23-scoring-and-endgame/07-awarding-points-as-the-player-plays.ts`
- `site/book-snippets.html`, `site/the-sharpee-book-v2.0.0.{html,epub,pdf}`

**Uncommitted new files** (3, untracked):
- `docs/work/test-book/execution-log-20260704.md` - container run 2 log
- `docs/work/test-book/execution-log-20260704-run3.md` - container run 3 log
- `docs/work/test-book/execution-log-20260704-run4.md` - container run 4 log

**Committed this session** (see commits f4219d92..62d5b9c4): book Phases 1-2
rewrite, voice sweep, `docs/book/CLAUDE.md`, ADR-209.

## Notes

**Session duration**: ~2 days elapsed (started 2026-07-03, continued into
2026-07-04); most calendar time is container run wall-clock, not active
editing.

**Approach**: fix-rebuild-republish loop against Docker naive-executor
container runs — pull the run's log, patch only the chapters it flags, rebuild
all three output formats, republish to `site/`, hand the container the next
build.

**Not touched**: `docs/context/session-20260704-book-code-measure.md` belongs
to a different session and was left alone.

---

## Session Metadata

- **Status**: INCOMPLETE
- **Blocker**: Test Infrastructure — run 5 (validating the Round B fixes) is
  still executing in the Docker container; its log has not been pulled or
  read, so the gate cannot yet be declared green.
- **Blocker Category**: Test Infrastructure
- **Estimated Remaining**: ~1 session — pull run 5's log; either declare the
  gate green and commit, or apply a Round C fix and re-run.
- **Rollback Safety**: safe to revert — all uncommitted changes are book
  content, regenerated snippets/build outputs, and new log files under
  `docs/book/` and `docs/work/test-book/`; nothing platform-level is touched.

## Dependency/Prerequisite Check

- **Prerequisites met**: published `@sharpee/*@2.1.0` on npm (container installs
  from the registry, no overlay/tarball grafting); `build-book.sh v2.0.0 all`
  toolchain; Docker naive-executor harness from Phase 7.
- **Prerequisites discovered**: None.

## Architectural Decisions

- ADR-209: room-description snippets accepted — all 7 open questions resolved
  by David, adr-review 13/13 (`docs/architecture/adrs/adr-209-room-description-snippets.md`).
- Pattern applied: book voice rules (no em-dashes, complete sentences, no
  "lost meaning" paragraphs) codified in `docs/book/CLAUDE.md`; appendices
  exempt.

## Mutation Audit

N/A — this session is documentation content and build-output regeneration; no
state-changing application code was modified.

## Recurrence Check

- Similar to past issue? YES — the fix-rebuild-verify loop against naive
  execution is the same pattern as Phase 7's dry run and the prior book-QA
  acceptance-run cycle (`docs/context/session-20260703-1150-v2_familyzoo_split.md`,
  commit `0e8a0719`'s acceptance-run log). Each cycle finds a shrinking set of
  chapter-level issues (run2: 3 issues + 1 BLOCKED -> run3: 2 issues, 0
  BLOCKED -> run4: 5 minor issues, 0 BLOCKED) rather than a single recurring
  root cause; no systemic audit indicated.

## Test Coverage Delta

No test changes this session — work is documentation content and book-build
verification (container transcript/build runs), not unit or integration test
suite changes. In-container suite count referenced by the user's brief:
159/159 passing.

---

**Progressive update**: Session completed 2026-07-04
