# Session Summary: 2026-07-19 02:00 — chord-foundations (session b381db)

## Goals
- Address the pre-session audit's stale flag: the actor `article: undefined` quirk (carried 3+ sessions since ADR-237 Phase 1).

## Key decisions
- **Quirk diagnosed to a live bug**: every Chord NPC rendered as a common noun — "You can see **a Tobias** here." (verified via the bundle). The loader pin was cosmetically invisible (`undefined` ≈ `'a'` through the phrase chain); the real gap was that no Chord person was ever proper-named.
- **RULED (David): `proper` is an explicit, person-scoped trait adjective** (`a person, proper`) — Inform 7's create-line-article inference reviewed (WI §3.18) and rejected in favor of the explicit spelling; create-line articles stay noise for identity (recorded I7 divergence).
- **RULED (David): pronouns folded into the same ADR** — verified platform gap: ADR-197's pronoun seam (`NounPhrase.pronounSet`, named-set hook) was never fed from entities; every singular NPC rendered "it". One person-identity gate, not a sibling ADR.
- **[ADR-242](../architecture/adrs/adr-242-chord-proper-persons.md) written, interviewed, ACCEPTED** (adr-review 13/13 twice — pre- and post-interview). Interview rulings: **Q-1** `define pronouns <name>` is a block with five named rows (subject/object/possessive/possessive-pronoun/reflexive); **Q-2** NO injected default — a person without a `pronouns` line keeps the by-number fallback ("it"); pronouns are declared, never inferred. Review-pass fixes folded: first kind-scoped TRAIT_ADJECTIVES entry named honestly; `applyTraitAdjectives` no-op case; `extendLanguage` as the registration seam ("second author-extensible table", not first).
- Fernhill pronoun words settled by the story's own prose (Tobias "his/he", Kettle "her") — the plan's stop-and-ask resolved by canon, not assumption.

## Work log — ADR-242 implementation COMPLETE (all 3 phases, plan at docs/work/chord-person-identity/plan.md)
- **Phase 1 (chord compiler)**: `proper` in TRAIT_ADJECTIVES (ratchet H1) with person-only + unconditional analyzer gates; `pronouns <word>` create line (H2: unknown-word nearest-match, duplicate, person-only diagnostics); `define pronouns … end pronouns` block (H3: missing/duplicate row, standard-shadowing, duplicate-set diagnostics); `IREntity.pronouns` (emitted only when declared) + `ir.pronounSets` manifest; ide-protocol re-export; ebnf + grammar-doc "Person identity" section + 3 ratchet rows. chord 386/386 (16 new); goldens additive-only (25 × `pronouns: []`, 4 × `pronounSets: []`).
- **Phase 2 (platform)**: `IdentityTrait.pronounSet` (additive); `nounPhraseFor` copies it; lang-en-us `registerPronounSet` registry (module-level beside the PRONOUNS table, provider method delegating) consulted by `renderPronoun` BEFORE standard rows; loader person branch — `proper` → `properName: true, article: ''`, **the `article: undefined` pin DELETED**, `pronouns` → `pronounSet`, `case 'proper': break`, defined sets through the real `extendLanguage` probe (legible LoadError only when sets are declared). REAL-PATH tests: story-loader 294 (7 new — "You can see Tobias here." rendered through real provider; ze forms via real extendLanguage; D6 no-default asserted), lang-en-us 411 (4 new), world-model 1390, stdlib 1537. No existing test pinned the undefined article.
- **Phase 3 (closure)**: fernhill Tobias/Kettle migrated (`proper` + `pronouns he`/`she`); zero transcript churn (no assertion pinned the old article); AC-8 in one pass — all five suites + `./repokit build` + cloak 81/81 + zoo atomic/chained + fernhill 472 atomics + wt chain 76/76; live "You can see Tobias here."; `.current-plan` restored to chord-go-live.

## Gotchas (for future sessions)
- ~~Transcript gates for chord stories need `--story stories/<s>/<s>.story` — bare `--test <transcripts>` defaults to dungeo (the cloak "80 failures" scare), and passing the story DIR picks a stale compiled dist whose engine ESM import (`dist-esm/types`, extensionless) crashes.~~ **FIXED same session (David's ruling): the dungeo default is REMOVED** (`scripts/bundle-entry.js`). `--test` infers the story from the transcript paths' `stories/<name>/` prefix (mixed prefixes = hard error; a lone `.story` file in the dir is preferred over a stale compiled dist — closing the ESM-crash trap too); `--play`/`--exec` require `--story`. Ruled option: infer-from-path over require-always. CLAUDE.md + CLI help updated; verified: cloak/dungeo/fernhill via inference, no-story and mixed-prefix errors legible, explicit `--story` override intact, fernhill wt chain 76/76 via inference. Bundle rebuilt via `./repokit bundle`.
- ~~Latent, unfixed: `packages/engine/dist-esm/index.js` imports `./types` extensionless — crashes any real-ESM consumer.~~ **FIXED same session (David's ruling: source sweep)**: the problem was repo-wide (2,510 extensionless relative imports across 607 files in ~30 packages — every dist-esm was invalid real-Node ESM; bundlers masked it; lang-en-us was the one package already extensioned). One-shot codemod rewrote all relative specifiers to explicit `.js` / `/index.js` (9 skips verified as comments/doc strings). Verified: `./repokit build` + bundle green; REAL Node ESM `import()` of engine (41 exports) and story-loader (18) dist-esm succeeds; full workspace vitest sweep green; all transcript gates green (cloak 81, zoo atomic+chained, fernhill 472 + wt 76, dungeo wt-01).
- Dungeo unit-transcript failures during regression = the RULED troll-family flake (2026-07-16 ruling, one-good-run, villain_attack → "Engine is not running" cascade — CLOSED, not re-flagged): all 8 affected files produced a green run (most on run 1).
- channel-service AC-13 migrated to the new CLI contract (`--exec` now passes `--story stories/dungeo`); its other failure was self-inflicted test concurrency (two bundle runs sharing dungeo saves), green solo (94 passed).
- Pre-existing bit-rot surfaced, NOT fixed (David to rule): `stories/cloak-of-darkness/tests/cloak-of-darkness.test.ts` (11/12 fail) tests the superseded hand-written module story with months-old expectations — candidate for deletion/rewrite against cloak.story.
- Remaining cosmetic: dist-esm trees lack a `package.json {"type":"module"}` marker — real-ESM consumers get Node's module-type-sniffing warning (works, just slow-path). Needs a small build-step change if ever wanted.

## Status: COMPLETE — ADR-242 SHIPPED (uncommitted). Remaining pre-launch: G4 (chord-go-live Phase 8 — release). Other open items from the audit unaddressed this session: stale `.tmp-g3-proof-*` dirs, `.devarch-events-*.jsonl` gitignore entry, platform findings (`repokit --browser` chord branch — note: last session's `play ambient` finding was CLOSED by ADR-241).
