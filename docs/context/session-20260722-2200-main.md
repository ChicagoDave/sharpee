# Session Summary: 2026-07-22 (2200 CST) - main — Phase 1b (ADR-256)

**Session ID**: 448562

## Goals
- Implement Phase 1b: ADR-256 — story-loader Chord-event-id → platform-event-id translation, finishing ADR-254's dot-ban at the event-type sites.

## Phase Context
- **Plan**: `docs/work/chord-browser-build/plan.md`, Phase 1b (Medium/300).
- **Entry state**: Phase 1 (ADR-254 author-label ban) shipped in commit `6ed17e10`; event-type sites still `allowDotted`. ADR-256 ACCEPTED. David gave explicit go-ahead to touch `packages/story-loader` + `packages/chord`.
- **Outcome**: Phase 1b code complete and green (except 1 parked pre-existing red). Uncommitted.

## Completed

### Emit seam located (ADR-256 marked it TBD)
Sole emit chokepoint is `runtime.ts:1491` — the `emit` case in `execStatements` (which `execMachineBody` and every rule/on-clause/action body route through) calling `rawEvent(stmt.event, …)`. Translation applied to `stmt.event` at the call site, **not** inside `rawEvent` (that also mints the internal `chord.phrase` event, which must not translate).

### ADR-256 implementation
- **New** `packages/story-loader/src/event-id-map.ts`: `translateEventId(id) = CHORD_TO_PLATFORM_EVENT_ID[id] ?? id`, plus the explicit `media-*` → `media.*` map (13 entries). Author events (no entry) pass through verbatim (D3).
- **Three seams translated**: emit (`runtime.ts:1491`), channel `from event` (`loader.ts:741`, translated once at build), machine `when event` (`loader.ts:1563`). All apply the same forward map so emit and subscribe always agree.
- **Conformance test** `story-loader/tests/event-id-map.test.ts` (5 tests) pins the map against live `MEDIA_EVENT_TYPES` — a stdlib media rename fails the build.
- **Parser** (`chord/src/parser.ts`): dropped `allowDotted` from the 3 event-type sites AND removed the option from `readLabelKey` (dead after). +3 event-site rejection tests in `dotted-key-rejection.test.ts`.
- **Analyzer** media sugar (`chord/src/analyzer.ts`): lowers to dotless IR (`media-sound-play`, …).
- **Migration**: `.story` fixtures (emit-payload/gatehouse/compass) + chord IR-asserting tests (emit-payload, media-sugar, dynamic-channels, channel-capability) → dotless. story-loader author-event assertions (emit-payload `chord-debug-whereabouts`) → dotless; media runtime-type assertions stay dotted (post-translation).

### Surfaced pre-existing Phase 1 fallout
`chord/dist` is gitignored → Phase 1's local dist was stale, hiding these; Phase 1's summary never ran the story-loader suite. Rebuilding chord exposed 5 dotted-**phrase-key** failures (unrelated to event ids):
1. `scheduler.test.ts` (4) — Phase 1 migrated `zoo-timeline.story` to dotless but left these assertions dotted. **Fixed** this session (dotted→dotless, per David).
2. `dotted-phrase-keys.test.ts` (1) — tests the ADR-254-superseded dotted override (`if.action.taking.fixed_in_place`); sibling `dotted-keys-all-sites.test.ts` was deleted in Phase 1, this one missed. **PARKED** per David (its ADR-255 kebab replacement is Phase 2). The 1 remaining story-loader red.

## Test State
- chord: **453** passed (31 files).
- story-loader: **314/315** (only parked `dotted-phrase-keys.test.ts` red).
- `mutation-verification` flagged one real gap: the machine `when event` seam's non-identity (media) translation was unexercised (its only compiled fixture used an author event = identity pass-through). Closed with `event-id-map-machine.test.ts` (2 tests) — a compiled machine `when event media-sound-play` transitions on the dotted `media.sound.play` and NOT on the dotless id, proving `loader.ts` translated the trigger eventId.
- fernhill: **500** (18 transcripts). friendly-zoo: **71** (7 transcripts) + **56** walkthroughs (7).
- Platform bundle rebuilt (`dist/cli/sharpee.js`).

## Key Decisions
- Fix `scheduler.test.ts`, park `dotted-phrase-keys.test.ts` (David, this session).

## Status
COMPLETE for Phase 1b scope. Code uncommitted. Next: commit Phase 1b (+ the surfaced Phase 1 fixes); then Phase 2 (ADR-255).
