# Session Summary: 2026-07-22 (2200 CST) - main — Phase 1b + Phase 2 (ADR-256, ADR-255)

**Session ID**: 448562

## Goals
- Phase 1b: ADR-256 — story-loader Chord-event-id → platform-event-id translation (finish ADR-254's dot-ban at the event-type sites).
- Phase 2: ADR-255 — `override message` standard-action message-override ACL.

## Phase 1b — ADR-256 (committed `67975a20`)
- New `story-loader/src/event-id-map.ts` (`translateEventId` + pinned `media.*` map) applied at all three seams: emit (`runtime.ts:1491`, sole `execStatements` chokepoint), channel `from event` (`loader.ts:741`), machine `when event` (`loader.ts:1563`). Conformance test pins the map against live `MEDIA_EVENT_TYPES`.
- `allowDotted` dropped from the 3 parser sites and removed as an option; media sugar → dotless IR; fixtures + IR assertions migrated. +3 event-site rejection tests; +1 machine-seam REAL-PATH test (mutation-verification gap).
- Surfaced + fixed stale `scheduler.test.ts` (Phase 1 fallout, masked by gitignored stale chord/dist). Parked `dotted-phrase-keys.test.ts` (superseded capability) per David.
- Green: chord 453, story-loader 314/315, fernhill 500, friendly-zoo 71+56.

## Phase 2 — ADR-255 (uncommitted this write; see below)
- **Catalog**: generated from LIVE `lang-en-us` → **748 aliases / 56 actions** (authored appendix said 734/54; predated `cutting`+`digging`, ADR-230). David ruled: cover all 748, regenerate appendix, note the correction in ADR-255. Data: `chord/src/message-alias-catalog.ts` (names) + `story-loader/src/message-alias-map.ts` (alias→`if.action.*`); pinned bijective-to-live by `message-alias-map.test.ts` (D5).
- **Grammar** (D1): `override message <alias>` (full `define phrase` body via extracted shared `readPhraseBody`) + `override messages <locale>` (mirrors `parsePhrasesBlock`). New AST `OverrideMessage`/`OverrideMessages`, IR `IRMessageOverrides`; `override` is a new top-level keyword. chord stays platform-free (carries the alias).
- **Rejection** (D4): unknown alias → `analysis.unknown-message-alias`; duplicate → `analysis.duplicate-message-override`; dotted alias → `parse.dotted-key`.
- **Resolution** (D6): overrides register on the ADR-250 **phrasebook resolution seam** (`world.registerEvaluator(phrasebookTemplateKey(resolved-dotted-id))`), reusing an extracted `derivePhraseTemplate` — full flat+cycling parity (`{variants}` Choice atom), wins over platform default, loses to per-entity/on-clause. Alias→id resolved loader-side (Interface Contract 3). No engine change.
- **Discovery**: cycling parity for a stdlib-emitted message id needed the phrasebook evaluator seam that `renderViaPhrase` consults before the platform default — the ADR didn't name the mechanism; implementation reuses it.
- **Doc corrections**: appendix regenerated to 748; ADR-255 count note + a note that the worked example's `wearing-already-worn` should be `wearing-already-wearing` (the live key is `already_wearing`).

### Test State (Phase 2)
- chord: **461** (4 golden-IR snapshots updated for the additive `messageOverrides` field; +8 new tests).
- story-loader: **326/327** (+12 new tests; the one red is the Phase-1b-parked `dotted-phrase-keys.test.ts`).
- fernhill: all pass (nondeterministic count ~498–504); friendly-zoo 71. Bundle rebuilt.

## Key Decisions
- Phase 1b: fix scheduler, park dotted-phrase-keys (David).
- Phase 2: catalog covers all 748 live messages; regenerate appendix; note the 734→748 correction (David).

## Follow-ups
- `dotted-phrase-keys.test.ts` can migrate from the dotted spelling to `override message` now that its replacement exists — raise with David.
- Phases 3–5 (ADR-252 browser build, ADR-253 renderers, doc/website sweep) remain.

## Status
COMPLETE for Phase 1b (committed) and Phase 2 code (uncommitted at this write). Next: commit Phase 2.
