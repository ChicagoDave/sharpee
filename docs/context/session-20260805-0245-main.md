# Session Summary: 2026-08-05 — feat/adr-300-302-channels-branch-tester (CST)

## Goals

- Run the ADR-300/ADR-302 plan (`docs/work/branch-tester/plan-20260805-branch-tester.md`) through on a branch, stopping only for something big.

## Phase Context

- **Plan**: `docs/work/branch-tester/plan-20260805-branch-tester.md` — 11 phases.
- **Branch**: `feat/adr-300-302-channels-branch-tester` (off `main` at 993b4b06).

---

## Phase 1 — Dissolve `main`, add `preferred-layout` (ADR-300 D8/D9) — COMPLETE

### The measurement that changed the phase

The plan's entry state predicted migrating 2921 `contains` assertions and
re-blessing 21 goldens. Measured first instead:

```
grep -rn "CHANNEL: *main" --include='*.transcript' stories/ tutorials/ | wc -l   → 0
grep -rn "^channels:" --include='*.transcript' stories/ tutorials/               → 1 file (channels: main, info)
```

**Zero** assertions name `main` explicitly. All 2921 read it *implicitly*,
through the turn text the harness composed from the `main` payload. So the
migration is not a corpus rewrite at all — it is a requirement that the
composed text stay byte-identical.

### Design

- Seven prose channels replace `mainChannel`: `room-name`, `room-description`,
  `room-contents`, `action-result`, `action-blocked`, `error`, `game-message`
  (append / **sparse** — a turn with no error emits no empty `error` array).
- `preferred-layout` (replace / **always**) carries the turn's reading order as
  one entry per prose entry, naming its source channel. A repeated id means
  that channel produced more than one entry, and each occurrence advances that
  channel's cursor — so interleaving (`['game-message','room-name','game-message']`)
  reconstructs exactly. A fixed render order cannot express this, which is why
  D9 rejects one.
- **`composeProse` is the shared rule**, in `@sharpee/channel-service`. Every
  surface composes identically: bootstrap, the CLI bundle, the browser, the IDE
  recording bridge, zifmia's pane.
- **Channel-id vocabulary moved to `@sharpee/if-domain`** (`PROSE_CHANNEL_IDS`,
  `PREFERRED_LAYOUT_CHANNEL`). It is wire protocol shared by producer and every
  consumer; a duplicated list is protocol drift waiting to happen (DEVARCH rule
  8b). stdlib keeps only the engine-side half — which block key routes where —
  typed on `ProseChannelId` so a route to an unknown id fails to compile.

### The one non-obvious client problem

The ADR-165 dispatcher calls `onValue` **per channel**, so seven independent
browser renderers would append in *manifest* order — a fixed order, exactly
what D9 rejects. Resolved by having the prose renderers **buffer** and the
`preferred-layout` renderer **flush** in composed order. That makes
registration order load-bearing: `preferred-layout` must be registered after
every prose channel. Stated as an invariant in `STANDARD_CHANNELS` and pinned
by a test rather than left as a latent trap.

### Verified 2026-08-05 (all commands re-run for this summary)

- `node dist/cli/sharpee.js --test --chain stories/dungeo/walkthroughs/wt-*.transcript`
  → **`952 passed`**, `✓ All tests passed!` — against the **pre-existing**
  recordings, **no `--bless`**. This is the load-bearing evidence: a bless would
  have overwritten the very thing the exit state asks for.
- `git diff --cached --numstat -- '*.golden'` → 21 files, **1 line changed each**
  (provenance `channels: main` → `channels: (none)`). Recorded turn output
  untouched.
- Package suites: stdlib 1604, engine 609, channel-service 115, bootstrap 39,
  platform-browser 117, transcript-tester 252, world-model 1453, story-loader
  472, chord 719 — all passing.
- `npx tsf build --npm` → clean (memory: npm-build regression during refactors).
- Real-path gates green against freshly built artifacts: channel-service AC-13
  (CLI bundle) and AC-14 (`dist/web/dungeo/`, real DOM).
- **AC-5 pinned both ways** in `packages/platform-browser/tests/channels/prose.test.ts`:
  driving all seven prose renderers with no layout renders nothing; the same
  payload under two different layouts produces two different reading orders.

### Found, not fixed

- [issue #223](https://github.com/ChicagoDave/sharpee/issues/223) —
  `info-channel-baseline.golden` pins a wall-clock `buildDate` inside its
  `◦ info` capture, so it breaks after **any** rebuild. Pre-existing: the
  golden's body is byte-identical to the committed version, and only its
  provenance line changed here. Fixing it means widening ADR-294 D6's
  build-date mask to channel captures, which D6 says requires an amendment —
  David's call, not a passing fix.

### Environment blocker (not code)

`tools/zifmia`'s server suite cannot run: `better-sqlite3@11.10.0` has no
native binding for this Node (v25.8.1, ABI `node-v141`). 17 test files fail at
DB open before any channel code executes. Zifmia's **web** tests — the only
zifmia code this phase touched — pass 22/22.

Also noted: `tools/zifmia`'s `test` script is bare `vitest` (watch mode), so
`pnpm test` there never exits. Used `npx vitest run`.

---

## Files Modified — Phase 1

**Wire vocabulary** — `packages/if-domain/src/channels/{types,index}.ts`:
`MainEntry` → `ProseEntry`, added `PreferredLayout`, `PROSE_CHANNEL_IDS`,
`PREFERRED_LAYOUT_CHANNEL`, `ProseChannelId`.

**Producer** — `packages/stdlib/src/channels/{keys,standard,index}.ts`:
routing table replaces `MAIN_KEYS`; seven prose channels + `preferredLayoutChannel`
replace `mainChannel`.

**Shared rule** — `packages/channel-service/src/utils/join-main-entries.ts` →
`prose.ts` (git mv): `composeProse`, `joinProseEntries`, `packetProseText`.

**Consumers** — `packages/bootstrap/src/index.ts`; `packages/platform-browser`
(`channels/main.ts` → `prose.ts` (git mv), `channels/index.ts`,
`BrowserClient.ts`, `package.json` exports subpath);
`scripts/bundle-entry.js`; `tools/zifmia/web/src/components/channel-renderer-host.ts`;
`tools/shite` (comment/option rename only — not built, ADR-180).

**Harness** — `packages/transcript-tester/src/{parser,runner,cli,golden,types}.ts`:
`channels:` default `['main']` → `[]`; empty list serializes as `(none)`
(matching the existing `forces:` convention); divergence surface label `'main'`
→ `'(prose)'`.

**Corpus** — 21 `.golden` provenance lines; one transcript header
(`channels: main, info` → `channels: info`).

---

## Session Metadata

- **Status**: IN PROGRESS — Phase 1 complete and committed; continuing the plan.
- **Blocker**: N/A
- **Rollback Safety**: safe — all work on a branch; the chain replays green
  against pre-existing recordings, so reverting restores a consistent state.
