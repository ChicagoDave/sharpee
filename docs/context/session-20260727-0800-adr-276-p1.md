# Session Summary: 2026-07-27 (0800) - adr-276-p1 [834109]

**Goal**: ADR-276 Phase 7 — hatch lint as structured diagnostic records (D4);
then Phase 8 (census re-audit) if the session allows.

**Status**: COMPLETE — ADR-276 Phases 7–8 DONE (ADR IMPLEMENTED, arc closed),
then the ADR-258 platform-side plan (docs/work/adr-258-ide-platform/plan.md)
planned AND implemented: Phases 1–3 all DONE. Remaining ADR-258 work is
Swift/Mac-side only.

## Work done

- Pre-session audit: clean (tree clean, tsc clean, no stale artifacts). Two
  items parked a 3rd consecutive session flagged for a user decision: delete
  ruling for `scripts/chord-gap-report.cjs` + `scripts/generate-standard-grammar-chord.cjs`;
  thealderman story-local tsc failure.

## Phase 7 implementation (D4 — hatch lint as structured diagnostic records)

- `packages/devkit/src/commands/compose.ts`: new exported `ComposeDiagnostic`
  record type (`severity, code, message, file, line`, `span` present exactly
  for compile diagnostics — hatch findings carry no end-span, D4) and
  `runComposeGates(file)` returning `ComposeGatesResult` whose `diagnostics`
  array is the ONE collection: compile diagnostics first, then hatch records
  (`hatch.chord-namespace`). The lint contributes to the collection even when
  the compile failed (hatch declarations still lower into the IR), so the
  future `--json` (ADR-258 D5) gets the complete stream in one pass.
- Text output preserved: `runCompose` now prints FROM the records via one
  formatter (`file:line[:column] severity [code] message`) with the historical
  gating unchanged — compile errors still return before any hatch text.
  Byte-identity pinned by a new exact-lines regression test.
- Tests (`hatch-lint.test.ts`, +4): byte-for-byte hatch stderr lines;
  compile-error + hatch-violation story yields both record kinds in one
  collection (shape, ordering, span present/absent) while `--check` text
  still reports only the compile failure; clean story → `ok`, empty stream.
  Fixture story extracted to a shared `lintCaseStory()` helper.
- devkit 92 passed / 1 skipped (pre-existing), root tsc clean. Corpus
  unaffected by construction (compose is the author CLI, outside
  `dist/cli/sharpee.js`); cloak 81/81 + friendly-zoo 76/76 sanity-run green.
- Plan statuses trued up: Phases 1–7 DONE, Phase 8 CURRENT (they had never
  been flipped from PENDING).

## Phase 8 implementation (census re-audit + acceptance verification)

### Census table walk (Acceptance item 5) — 50 sites, NO third category

Current `loader.ts` line numbers (post-annotation). Every site is a backstop
or D5 residue:

**D5 residue (9)** — `:255` IR-format refusal; `:290` pure-IR profile refusal;
`:298` hatch module not provided; `:311` `chord.*` literal at hatch bind
(bind-time half of the devkit lint); `:320`/`:332`/`:344` hatch export shapes
(text/action/chain); `:699`/`:710` language-provider capability
(addMessage/registerPronounSet).

**Census-migrated backstops (18 sites)** — `:229`+`:2091` census 6
(`analysis.setting-names-no-entity`); `:680` census 12 (`worn-not-wearable`);
`:1132` census 1 (`extend-target`); `:1152` census 2 (`removal-target`);
`:1167` census 3 (`unmatched-removal-pattern`); `:1363` census 18
(`multiple-kind-nouns`); `:1473` census 17 (`unknown-kind-noun`);
`:1580`/`:1586` census 13 (`ambiguous-gerund-surface`; zero-surface gate is
hatch-free-only — recorded D5 residue boundary); `:1611` census 14
(`conditional-composition-unsupported`); `:1734` census 10
(`unknown-hiding-position`); `:1774` census 7 (pre-gated
`extension-not-used`); `:1791` census 11 (`dark-rooms-only`); `:1806` census
15 (`trait-not-declared`); `:1845`/`:2048` census 4 (`setting-not-boolean`);
`:1901` census 9 (`patrol-needs-route`); `:1920` census 8 (pre-gated manifest
adjective gates); `:2041` census 5 (pre-gated `extension-config-value`);
`:2418` census 16 (parser-gated DIRECTIONS ⊂ enum, conformance-pinned).

**Pre-existing compiler-gate backstops (12)** — `:373` unknown chain
(`analysis.unknown-chain`); `:386` unknown `use` extension (manifest gate);
`:441` door-plain-mirror; `:462` door pair; `:493` conditional deadly exit
(`analysis.deadly-while-unsupported` — convention comment ADDED this phase);
`:524` door never wired (`door-unconnected`); `:554` scoring `use` gate;
`:763` state-machines `use` gate; `:1343` region cycle; `:1505` starts-state
pairing gate; `:2170` verbatim marker (`analysis.verbatim-marker`); `:2306`
phrase existence.

**Internal-consistency / drift guards (11)** — `:1498` starts-state
trait-field table drift; `:1523` trait-config ref never built; `:1905` patrol
route entry never built; `:1941`/`:1971` machine role/trigger never built;
`:2030` combat field-route table drift (conformance-pinned); `:2118`
requireWorldId ordering invariant; `:2473` exhaustive scope predicate.
(These guard loader-internal invariants behind analyzer name-resolution
gates — backstops in D5's sense.)

### Acceptance runs (all recorded this session)

1. **Composite** (item 1): `chord/tests/adr-276-acceptance.test.ts` +
   `devkit/tests/adr-276-acceptance.test.ts` over
   `devkit/tests/fixtures/adr-276-composite.story` — all four ADR-named
   violations → four collected error diagnostics with spans from ONE
   `compose --check` run, exit 1, no IR on stdout. The unknown direction
   surfaces as `analysis.trait-not-declared` (census 16 parser-gated).
2. **Collected** (item 7): three alteration errors (census 1/2/3) in one
   compile — pinned at chord level.
3. **Browser parity** (item 2): parity harness Phase 8 sweep — built chord
   dist compiles the SAME fixture file to the same four codes; the real
   browser build fails loudly with multiple codes in one run. 8/8.
4. **Import direction** (item 6): chord has no `dependencies` and no
   `@sharpee/*` src imports (unchanged vs pre-arc `7d505ee3`);
   `bundle-entry.js` keeps chord out of bootstrap; story-loader's chord
   import block byte-identical to pre-arc.
5. **Corpus final sweep** (item 8): dungeo units 1783 passed / 9 expected
   failures / 4 skipped (convention); wt chain 907/907 (17 transcripts,
   `--chain --stop-on-failure`); cloak 81/81; friendly-zoo 76/76; fernhill
   500/500; nautical 7/7; grammar-alterations 6/6.
6. Suites: chord 654/654, story-loader 428/428, devkit 95 + 1 pre-existing
   skip, root tsc clean.
7. **ADR-276**: Status → IMPLEMENTED; Implementation addendum appended
   (phases → codes, census corrections incl. the four pre-gated discoveries,
   the 50-site classification, residue restated per D5, recorded runs).
   Freshness-gate demos were recorded in Phases 3/4/5 (item 3); rogue-IR
   backstop tests stand from Phases 1–6 (item 4).

## Key decisions

- Phase 7 scope ruling (in-plan): the unified collection includes hatch
  findings even for a failed compile — text mode keeps historical gating, so
  behavior is unchanged while the D4 stream is complete (matches the plan's
  "both an analyzer error and a hatch violation in one run" test scenario).

## ADR-258 platform side (same session, planned via session-planner + plan-review clean)

- **Phase 1** (d8120e9f): ADR-258 Status amendment — D5 sequencing gate OPEN,
  ADR-276 Acceptance item 9 closed.
- **Phase 2** (a3ae5d24): `compose --json` (D5). Wire contract in
  `@sharpee/ide-protocol/src/compose-diagnostics.ts`: `ComposeJsonPayload` /
  `ComposeDiagnosticRecord` / `COMPOSE_JSON_SCHEMA_VERSION` (=1, distinct from
  ProjectManifest's) + decode guards. devkit `compose --json` serializes the
  `runComposeGates` collection to stdout; `ir` present iff compile ok and no
  `--check` (atomic load); exit = gates.ok; NO load-proof — proven by the
  unresolvable-hatch story (succeeds under `--json`, rejects under default
  mode). `ComposeDiagnostic` now aliases the ide-protocol type (rule 8b);
  the schema constant is lazy-required to keep CLI startup lazy (ide-protocol's
  barrel pulls chord values). D6 (same-run IR) and D9 (`languageVersion` =
  `CHORD_LANGUAGE_VERSION`) pinned by test. devkit gained the ide-protocol
  workspace dep. ide-protocol 16/16, devkit 101+1skip, corpus green (wt chain
  + fernhill re-run green — known time-seeded flakes; bundle unchanged).
- **Phase 3**: D7 golden lexer fixture. Corpus
  `packages/chord/tests/fixtures/lexer-golden/{grammar-surface,
  alterations-counters,story-core}.story` covering the full 2.0.0 amendment
  list; committed `lexer-golden.json` (147KB, full `Line[]` fidelity);
  `lexer-golden.test.ts` conformance pin with `UPDATE_GOLDEN=1` regeneration
  (package script `golden:lexer`) + corpus-coverage drift guard (input regexes
  AND output token-kind coverage). Demonstrated: deliberate compare-token
  lexer edit → RED with the regenerate-and-update-the-Swift-port message →
  reverted byte-clean; regeneration idempotent (identical md5 twice); coverage
  guard fires on a removed corpus file. chord 656/656, tsc clean.

## Release bump (David's ask, same session)

- **Sharpee 4.1.0** — lockstep `tsf version 4.1.0` (33 packages, same scope as
  the 4.0.0 release commit ecdfa038); `./repokit build dungeo` restamped
  `ENGINE_VERSION`/story version.ts and rebuilt platform + bundle (manifest
  freshness gates green in-build).
- **Chord 2.1.0** — `CHORD_LANGUAGE_VERSION` bumped with a recorded history
  entry; EBNF pin re-recorded languageVersion-only (grammar hash UNCHANGED);
  ADR-257 third recorded exception appended (owner ruling: the ADR-276
  diagnostics arc ships as a language minor with no grammar change);
  chord-grammar-changes.md row added; the 4 golden-IR snapshots updated
  (diff verified languageVersion-only).
- Verified: `sharpee --version` → "Sharpee 4.1.0 · Chord 2.1.0"; chord
  656/656, story-loader 428/428, devkit 101+1skip, tsc clean, corpus green
  (wt-chain flake re-run green).

## Open items / next steps

1. Swift/Mac side of ADR-258 (D1–D4, D6 UI, D7 `ChordLexer.swift` port against
   the committed golden, D8) — consumes `ComposeJsonPayload` +
   `COMPOSE_JSON_SCHEMA_VERSION` from ide-protocol and
   `packages/chord/tests/fixtures/lexer-golden/lexer-golden.json`.
2. Merge `adr-276-p1` to main (user's call).
3. Parked (4th session — needs a user decision): delete ruling for the two
   `scripts/*.cjs` generators; thealderman story-local tsc failure; ADR-266
   umbrella closing note.
