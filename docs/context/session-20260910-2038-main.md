# Session Summary: 2026-09-10 - main

## Goals
- Organize the systemic audit behind I-52e228-1 (a check "structurally incapable of observing what broke," six recorded instances since 2026-09-10) into one document.
- File every finding as a GitHub issue rather than leaving it as prose.

## Phase Context
- **Plan**: No active plan (`.current-plan` pointer absent; the session state file's `phase`/`phaseName` fields carry stale values from an unrelated plan and are not this session's work).
- **Phase executed**: N/A — investigative audit, not a plan phase.
- **Tool calls used**: 90 (session state file `docs/context/.session-state-6682ed.json`).
- **Phase outcome**: N/A.

## Completed

### Gate coverage audit
- Wrote `docs/work/typecheck-gate-audit/audit-20260910-gate-coverage.md` (237 lines, findings F1-F9), every claim backed by the command output that produced it, run 2026-09-10.
- Verified `npx tsc --noEmit --listFiles | wc -l` → 0: the root gate compiles zero files because root `tsconfig.json` is `files: []` plus 4 references, and references are only followed by `tsc -b`, which `pnpm typecheck` does not invoke — it exits 0 unconditionally (F1).
- Verified 0 of 46 workspace packages define a `typecheck` script despite `turbo.json` declaring the task; `turbo run typecheck --dry=json` crashes on turbo 2.7.2 (F2).
- Typechecked all 781 test files under `tsconfig.base.json` strictness (supplying `vitest/globals` and the `vitest.shared.ts` source aliases so resolution matches vitest): 1,574 errors across 260 files, zero in `src/`, because every package's tsconfig scopes `include` to `src/**/*` and vitest transpiles via esbuild without typechecking (F3).
- Confirmed two representative errors are real drift, not config noise: `packages/stdlib/tests/channels/standard.test.ts:319` builds `{ type: 'em', content: [...] }` against `IDecoration` (`packages/text-blocks/src/types.ts:54`, shape `{ className, content }`) and passes only because `produce()` never reads the unmatched key; `packages/engine/tests/fixtures/index.ts:6` and `tests/fixtures/mock-parser.ts:10-13` import four symbols stdlib does not export.
- Found 7 packages with test files outside `turbo run test:ci`; `packages/if-domain` has 6 test files, no `test` script, no `test:ci` entry, and no vitest config — no command runs them (F4).
- Found 20 packages whose `test` script is bare `vitest` (watch mode, never exits in CI) (F5).
- Found `packages/bridge`, `packages/runtime`, `packages/extensions/conversation` absent from both `PLATFORM_PACKAGES` (`tools/repokit/src/repo.ts:18-62`) and `test:ci`; `stories/concealment-test` and `tools/vscode-ext` sit outside the pnpm workspace entirely. Nothing compiles any of them — `bridge` and `runtime` were edited two commits ago by ADR-345 Phase 2 and verified clean today (`npx tsc -p packages/bridge/tsconfig.json --noEmit`, `npx tsc -p packages/runtime/tsconfig.json`, both exit 0) only by luck, not by coverage (F6).
- Found `stories/dungeo` is the only story that typechecks and ships its tests: 11 colocated `*.test.ts` compile into `dist/` as 12 `.test.js` artifacts (F7).
- Found `stories/dungeo/tsconfig.json` still excludes the live `src/browser-entry.ts` (6,158 bytes) and a non-existent `src/react-entry.tsx`; the `tsconfig.browser-check.json` guards session 86894c added earlier the same day are wired to no command (F8).
- Found the root `package.json` `workspaces` array is inert (pnpm reads `pnpm-workspace.yaml`) and wrong both ways: 6 listed paths don't exist, 18 real packages are missing from it (F9).
- Conclusion (the point of the audit): the repository's only working typecheck is a side effect of building, and building only ever looks at `src/`. All six recorded instances of I-52e228-1 are things living outside a `src/` directory of a package that's on the build list — which is why adding one more check after each instance hasn't stopped the instances.

### GitHub issues filed
Nine issues filed against ChicagoDave/sharpee, one per finding plus an umbrella:
- #400 — root `typecheck` is a no-op + `turbo.json` task with no implementors (F1/F2)
- #401 — 1,574 type errors in untypechecked test files (F3)
- #402 — 7 test suites outside `test:ci`, `if-domain` unrunnable (F4)
- #403 — 20 watch-mode test scripts (F5) — title needed a follow-up `gh issue edit` after zsh command-substituted backticks in the original `--title` argument
- #404 — bridge/runtime/conversation/concealment-test/vscode-ext compiled by nothing (F6)
- #405 — dungeo ships test files in `dist/` (F7)
- #406 — browser-check configs unwired (F8)
- #407 — stale `workspaces` array (F9)
- #408 — umbrella issue, carrying the gate-coverage table and a suggested fix order

## Key Decisions

### 1. Audit before fix
The session produced only the audit document and nine issues — no code was touched. This was deliberate: the audit's conclusion implies the fix requires a decision (where does the typecheck gate live, and does it run against tests) that is David's call, not a mechanical patch. Filing #400 as the gating issue reflects that #401, #404, and #406 are all downstream of it.

### 2. Real drift confirmed before filing
Rather than filing F3 (1,574 errors) as a raw error count, two representative errors were traced to root cause (a decoration-shape mismatch masked by lenient runtime code, and stdlib export drift) to confirm the errors are genuine and not an artifact of the ad-hoc typecheck harness's config.

## Next Phase
No active plan — this was an investigative audit outside plan-phase work. Next session's likely entry point is David's disposition on GH #400, which #401, #404, and #406 depend on.

## Open Items

### Short Term
- I-6682ed-1: GH #400 (root typecheck is a no-op) decides where the typecheck gate should live; #401, #404, and #406 depend on that answer. All nine issues (#400-#408) are open and unstarted — fix direction per issue is David's call.

### Long Term
None new this session beyond the item above.

## Files Modified

**Documentation** (1 file):
- `docs/work/typecheck-gate-audit/audit-20260910-gate-coverage.md` - new, the F1-F9 gate coverage audit (237 lines)

**Session artifacts** (1 file, out-of-band):
- `docs/context/session-20260910-2218-main.md` - a progressive stub hand-written mid-session under wall-clock naming instead of the state file's `summaryPrefix`; left in place per instruction (David decides whether to delete it), superseded by this file as the session's real summary

## Notes

**Session duration**: ~2.5 hours (20:38-22:22+ CDT, per the stub's own timestamp and the state file's `started`).

**Approach**: Every claim in the audit carries the exact command that produced it, run fresh on 2026-09-10, per the "evidence inline, never cited" standard — this let two of the 1,574 errors be traced to genuine type drift rather than accepted as an aggregate number.

**Resolved this session**: I-52e228-1 (the systemic item this audit was organizing) — closed via `devarch items resolve --as done --evidence docs/work/typecheck-gate-audit/audit-20260910-gate-coverage.md`. The audit answers the item's question (which exclusions hide errors from every gate); the fixes it points to remain open as GH #400-#408.

**Session state file discrepancy**: `docs/context/.session-state-6682ed.json` carries `phase: 0`, `phaseName: "Decide and provision the spike environment"`, `tier: "Small"`, `budget: 100` — these belong to an unrelated plan/spike and do not describe this session's actual work (the gate-coverage audit). Its `files: []` array is also empty despite two files being written this session; both `Write` calls apparently went untracked by the state-file hook. Flagged here rather than silently trusted.

---

## Session Metadata

- **Session**: 6682ed
- **Status**: COMPLETE
- **Blocker**: N/A
- **Blocker Category**: N/A
- **Estimated Remaining**: N/A (session's own scope — audit + issues — is finished; the nine filed issues are separate future work, not this session's remainder)
- **Rollback Safety**: safe to revert (documentation-only; no code changed)

## Dependency/Prerequisite Check

- **Prerequisites met**: `gh` CLI authenticated against ChicagoDave/sharpee (all nine issues filed successfully); repo-root `tsc`/`npx tsc` available for the ad-hoc typecheck runs.
- **Prerequisites discovered**: None.

## Architectural Decisions

- None this session — no ADR written or referenced. The audit documents a gap in tooling coverage, not an architectural decision.

## Mutation Audit

N/A — documentation and investigation only; no side-effect code was written or modified.

## Recurrence Check

- Similar to past issue? YES — this session's entire subject is a recurrence: I-52e228-1 itself records six prior instances of the same failure mode (a gate reading green while the thing it was meant to catch went uncaught), first raised by `pre-session-audit`, and this audit is the "one-time audit of the subsystem" that rule 19 / pattern-recurrence-detector would otherwise recommend. Sessions carrying prior instances: `52e228` (item origin, GH #399), `cbdf63` (sixth instance), plus the two `pre-session-audit` mentions referenced in the item statement.
- Consider: with the audit now filed as nine issues, a future `pattern-recurrence-detector` run against this category should find the systemic fix already in motion rather than flag a new occurrence.

## Test Coverage Delta

- Tests added: 0
- Tests passing before: N/A → after: N/A — no test files were added or modified this session (`docs/context/.session-state-6682ed.json` `files: []` corroborates no source/test edits, though see the Notes caveat on that array's reliability for the two docs actually written)
- Known untested areas: unchanged by this session — the audit's F3/F4 findings describe untested/untypechecked areas that already existed; this session did not add or remove test coverage, only document the gap

---

**Progressive update**: Session completed 2026-09-10 22:22 CDT
