# Session Summary: 2026-09-16 - main

## Goals
- Fold the ADR-351 Phase 8 (Linux) result already committed on `main` and correct one stale annotation it left behind.
- Decide disposition of the `adr-349-location-heading` plan under rule 18b before repointing `.current-plan`.
- Open a session plan answering ADR-351 Q-5 (is the macOS bundle layout fixable by post-processing alone) and adversarially review it.

## Phase Context
- **Plan**: `docs/work/velopack-macos-bundle-layout/plan.md` — "Determine whether a Velopack macOS bundle can be post-processed into a shippable, signable, notarizable `.app`."
- **Phase executed**: N/A this session — the plan was newly written, not advanced. `.current-plan` now points to it; Phase 1 is CURRENT but unstarted.
- **Tool calls used**: 103 / 250 (state file `phase: 2` and `phaseName` refer to the unrelated `adr-353-pin-provenance` plan the session started on before switching to `main`; not this session's work).
- **Phase outcome**: N/A — no phase executed this session; a new plan was written and reviewed.

## Completed

### 1. ADR-351 R22 correction
`docs/architecture/adrs/adr-351-chord-writer-host-shape.md`'s R22 cell for Avalonia+Velopack still read "only macOS built and run" after Phase 8's Linux run landed on `main` (commit `11fa37af0`) — a staleness the Consequences section of that same commit had already flagged. Changed to "built and run on all three (Windows x64, Phase 7; Linux x64 and arm64, Phase 8); no signed installer run on any." David's score of 3 was left untouched — R22 is a gate row, not a scored one, so the 183/196 total is unaffected.

### 2. Rule 18b disposition on `adr-349-location-heading/plan.md`
`.current-plan` was about to move off this plan, which still has Phases 5 and 6 PENDING (Phase 4 is DONE on branch `adr-353-pin-provenance` but reads CURRENT here on `main`, since that branch hasn't merged). Offered the three rule-18b options; David chose **"still live"** — *"I have two 'things' going and we're just going to flip back and forth."* Stamped `**Superseded by**: docs/work/velopack-macos-bundle-layout/plan.md` on it; every phase left untouched; not archived (a "still live" plan never is). `.current-plan` then repointed to the new plan.

### 3. New plan: `docs/work/velopack-macos-bundle-layout/plan.md`
Written via `session-planner` for ADR-351 Q-5, explicitly instructed not to touch `.current-plan` itself (it didn't — the pointer was moved separately, above). Four phases: (1) can the AppHost be pointed at a relocated `Contents/Resources` payload at all; (2) does Velopack's update-apply path survive that relocation; (3) sign and notarize the relocated bundle end to end (needs David at the keyboard); (4) write the Q-5 decision record.

### 4. Adversarial review of the new plan
Ran a verification pass plus `/devarch:plan-review` against 7 reference files. Found and fixed four CONTRADICTIONs and five TENSIONs:
- Phase 2 asserted the Velopack update-apply path runs unsigned; `phase-5-velopack-packaging.md` §7 and `parity-table.md:109` both say it wants a signed app.
- Phase 1 cited `docs/context/project-profile.md` for toolchain facts (`vpk` 1.2.0, .NET SDK 10.0.300, `DOTNET_ROOT`) the profile does not contain — they're in `phase-5-velopack-packaging.md`'s Run header. This is the laundered-citation pattern GH #430 tracks (see Recurrence Check).
- Phase 1 claimed `DOTNET_ROOT` was already set; verified false (`~/.dotnet/tools/vpk --version` fails with a misleading "You must install .NET" error; `dotnet --version` → 10.0.300 at `/opt/homebrew/bin/dotnet`).
- Phase 3 asked David to confirm a signing identity that `tools/ide/package.sh:633-641` already settles automatically (`RSNGKW5LNH`; the alternative `54CCCRZJ3X` is the retired business team).

Additions made to the plan: a `vpk`-version check as Phase 1 step 0 (the §6.1 disjunction — "either Velopack grows a layout option, or post-process" — had been narrowed to only the second half); a baseline run against the unmodified bundle (GH #457 confound); an `assert_sealed_toolchain` check immediately after relocation (235 symlinks; the seal "held by accident of directory depth, not by construction"); Phase 4 split into two verdicts (Q-5 as worded vs. the condition D2 actually rests on); and a §6.3 note that no Developer ID Installer certificate exists on this machine.

## Key Decisions

### 1. R22's score stays David's, only the annotation moves
The gate-row score (3) reflects David's own weighting of the matrix; only the factual parenthetical (what was built and run) was stale. Corrected the fact, left the number alone — see `adr-351-chord-writer-host-shape.md` diff.

### 2. "Still live" over "done but unmarked" for `adr-349-location-heading`
David explicitly rejected forcing closure on a plan with two real PENDING phases just because the pointer needed to move; the stamp records the supersession without freezing the plan's phase statuses.

## Next Phase
- **Phase 1**: "Can the AppHost be pointed at a relocated `Contents/Resources` payload at all" — `docs/work/velopack-macos-bundle-layout/plan.md`.
- **Tier**: Medium (250 tool-call budget).
- **Entry state**: Awaiting David's authorization to start; the Phase 5 spike output this phase reads from still exists at `/Users/david/repos/spikes/avalonia-ide/` (verified this session).

## Open Items

### Short Term
- 462: Avalonia + Velopack spike: four items owed from Phase 0-6 evidence (bundle layout, replay over-run, editor palette, localStorage persistence) — item (1) is this new plan's task; carried, comment added this session naming the plan.
- 457: Vendored toolchain cannot build any Chord story: seven packages promise `exports.import` → `dist-esm` but their files list ships only `dist` — carried as a named confound for the new plan's Phase 1 baseline step; no change this session.

### Long Term
- 430: Recurrence: Documentation — stale descriptions outliving the mechanism they describe — carried; comment added this session recording a third, first-cross-session occurrence (see Recurrence Check). Not re-filed or re-counted here; that's `recurrence-count.sh`'s job at next `/devarch:finalize`.

## Files Modified

**Documentation** (4 files):
- `docs/architecture/adrs/adr-351-chord-writer-host-shape.md` - R22 cell corrected from "only macOS built and run" to name Phases 7/8 and the unsigned-installer caveat; score untouched.
- `docs/context/.current-plan` - repointed from `adr-349-location-heading/plan.md` to `velopack-macos-bundle-layout/plan.md`.
- `docs/work/adr-349-location-heading/plan.md` - added `**Superseded by**` stamp (rule 18b, "still live"); no phase statuses changed.
- `docs/work/velopack-macos-bundle-layout/plan.md` - new plan, 4 phases, written by `session-planner` and corrected by `/devarch:plan-review`.

## Notes

**Session duration**: short, documentary session; no code written, no tests run.

**Approach**: Fold a prior commit's loose end, settle a plan-supersession decision with the user, then plan-and-review the next question before any implementation.

**Session-state defects (do not propagate)**: `docs/context/.session-state-242753.json`'s `branch` still reads `adr-353-pin-provenance` (the session switched to `main` as its first action) and `filesChanged` reads 0 despite 4 changed paths in its own `files` array; its `phase: 2` / `phaseName` refer to the unrelated ADR-353 plan, not this session's work. This summary uses the `files` array (which is correct) and ignores the stale `branch`/`filesChanged`/`phase` fields.

---

## Session Metadata

- **Session**: 242753
- **Status**: COMPLETE
- **Blocker** (if any): N/A
- **Blocker Category**: N/A
- **Estimated Remaining**: N/A
- **Rollback Safety**: safe to revert

## Dependency/Prerequisite Check

- **Prerequisites met**: ADR-351 Phase 8 evidence already committed on `main` (commit `11fa37af0`) before this session began.
- **Prerequisites discovered**: `DOTNET_ROOT` is not set in a fresh shell on this Mac — the new plan's Phase 1 entry state now records this as a prerequisite gap rather than an assumption.

## Architectural Decisions

- ADR-351 (Chord Writer host shape): R22 cell factual annotation corrected; no Consequences or Decision text changed; score left to David.
- Pattern applied: rule 18b plan-supersession disposition ("still live") on `docs/work/adr-349-location-heading/plan.md`.
- No new ADR was written this session; the new plan explicitly defers folding its eventual Q-5 answer back into ADR-351 to a future session, as David's call.

## Mutation Audit

- Files with state-changing logic modified: none — all four changed paths are Markdown (three) and a plain-text pointer file (one). N/A.
- Tests verify actual state mutations: N/A — no code changed.
- If NO: N/A.

## Recurrence Check

- Similar to past issue? YES — GH #430 (Recurrence: Documentation), previously two same-session instances (session `275fbe`, both caught within that one session's own review). This session's `/devarch:plan-review` catch is a **third occurrence and the first across sessions**: a plan drafted in an earlier turn of work cited `project-profile.md` for facts it doesn't contain, and the error survived until this session's independent review caught it — the same "stale/laundered description outlives the thing it misdescribes" shape #430 already tracks, now spanning session boundaries rather than being caught same-session. Recorded as a comment on #430; not re-filed, and recurrence-count arithmetic is left to `/devarch:finalize`.
- If YES: Consider whether `/devarch:plan-review`'s "References consulted" citations need a stronger check — e.g., grep-verifying a cited fact actually appears in the cited file — given this is now crossing sessions rather than being self-corrected within one.

## Test Coverage Delta

- Tests added: 0
- Tests passing before: N/A → after: N/A (no code changed, no suites run this session)
- Known untested areas: N/A this session.

---

**Progressive update**: Session completed 2026-09-16, shortly after 00:47 CDT start (local)

## Activity Log (auto-captured)
```
[05:47:58] BUILD: Build passed — ls tsconfig.json 2>/dev/null; ls packages/*/tsconfig.json 2>/dev/null | head -3;
[05:48:03] BUILD: Build passed — npx tsc --noEmit; echo "EXIT:$?"
[05:50:31] EDIT: File edited — docs/architecture/adrs/adr-351-chord-writer-host-shape.md
[05:53:13] EDIT: File changed via Bash — docs/architecture/adrs/adr-351-chord-writer-host-shape.md
[05:54:54] EDIT: File edited — docs/work/adr-349-location-heading/plan.md
[05:55:03] EDIT: File changed via Bash — docs/work/adr-349-location-heading/plan.md
[05:57:10] EDIT: File written — docs/work/velopack-macos-bundle-layout/plan.md
[05:57:12] EDIT: File changed via Bash — docs/work/velopack-macos-bundle-layout/plan.md
[06:02:13] EDIT: File edited — docs/work/velopack-macos-bundle-layout/plan.md
[06:02:23] EDIT: File edited — docs/work/velopack-macos-bundle-layout/plan.md
[06:02:32] EDIT: File edited — docs/work/velopack-macos-bundle-layout/plan.md
[06:02:41] EDIT: File edited — docs/work/velopack-macos-bundle-layout/plan.md
[06:02:48] EDIT: File edited — docs/work/velopack-macos-bundle-layout/plan.md
[06:02:56] EDIT: File edited — docs/work/velopack-macos-bundle-layout/plan.md
[06:03:00] EDIT: File edited — docs/work/velopack-macos-bundle-layout/plan.md
[06:03:05] EDIT: File edited — docs/work/velopack-macos-bundle-layout/plan.md
[06:03:15] EDIT: File edited — docs/work/velopack-macos-bundle-layout/plan.md
[06:03:22] EDIT: File edited — docs/work/velopack-macos-bundle-layout/plan.md
[06:03:28] EDIT: File changed via Bash — docs/context/.current-plan
[06:13:06] EDIT: File written — docs/context/session-20260916-0047-main.md
[06:13:11] EDIT: File edited — docs/context/session-20260916-0047-main.md
[06:13:20] EDIT: File changed via Bash — docs/context/session-20260916-0047-main.md
[06:23:28] TEST: Tests passed — 11 passed 107 passed
[06:23:40] EDIT: File written — .commit-files
[06:23:49] EDIT: File written — .commit-msg
[06:23:53] GIT: Git operation — bash $HOME/.claude/scripts/git-commit.sh --push
```
