# Session Summary: 2026-06-18 (05:40 CST) — feat/adr-180-phase3d-cutover

## Goals
- ADR-180 Phase 3d: retire build.sh; devkit becomes the sole build front door.

## Context
- 3a/3b/3c (CLI bundle, browser, zifmia) merged, all byte-parity gated.
- Branch: feat/adr-180-phase3d-cutover. Two commits: part-1 (non-destructive) + cutover.

## Completed
### Part 1 (non-destructive, commit e298952c)
- `devkit clean` (per-package clean + top-level dist/), `devkit verify` (`tsf build --npm`
  + publish dry-run). tsfBin resolution moved to repo.ts.
- **Umbrella wiring**: `@sharpee/sharpee` → `@sharpee/devkit` (workspace:*); verified the
  `devkit` bin links under packages/sharpee/node_modules/.bin (ADR Decision 1).
- AC-8 living-docs sweep: CLAUDE.md + 6 guides → devkit (also fixed stale text-service /
  zifmia-runner errors in build-system.md). ADRs/brainstorm left as historical record.

### Cutover (this commit)
- **Final parity gate**: ran all three harnesses (CLI bundle, browser, zifmia) — **all PASS
  byte-for-byte** — immediately before deletion (ADR invariant).
- **Deleted**: `build.sh`, `pack-release.sh` (dead; referenced removed text-service),
  `scripts/test-bundle-template.js` (only the dropped fast-test-bundle used it).
- **GENERATOR flip**: version.ts comment "Auto-generated by build.sh" → "devkit".
- **Standalone proof**: `devkit build dungeo --browser --zifmia` built all three targets
  with build.sh gone — clean.
- **Wider blast-radius sweep** (beyond AC-8's named scope — surfaced during deletion):
  - Active invokers repointed to devkit: `build-macos.sh`, `build-ubuntu.sh` (release
    wrappers), `tools/vscode-ext/src/build-provider.ts` (also fixed `-s`→positional + the
    path.join-vs-command bug), `demos/deploy.sh`.
  - Hint/comment/doc strings → devkit: README.md, CONTRIBUTING.md, scripts/mac-release.sh,
    scripts/generate-genai-api.js, CI workflow comments, vscode-ext new-story-wizard +
    test-runner, channel-service ac-13/ac-14 real-path test skip messages, zifmia
    Dockerfile/README/DEPLOYMENT, zifmia E2E global-setup.
  - **Left as-is**: the Swift IDE (`tools/ide/`, parallel session — will break until that
    session migrates it; flagged), `tools/shite/` (abandoned), website/web-save mirror,
    familyzoo tutorial src comments, auto-generated version.ts/genai-api/authoring.html.

## Decisions (confirmed with David)
- Drop the fast test bundle; living-docs-only sweep; keep deferred-`.sharpee` scripts
  (validate-bundle-baseline.js, generate-meta.js).
- Delete build.sh now; **accept the `.sharpee` gap** — the only `.sharpee` builder went with
  build.sh; zifmia uses its stories/ runtime scanner meanwhile; install-guide/DEPLOYMENT note
  it returns when `devkit bundle:story` lands.
- Finish the cutover; skip the IDE.

## Open / next
- `.sharpee` story-bundle builder → a future `devkit bundle:story` (validate-bundle-baseline
  + generate-meta scripts kept for it).
- `devkit init`/`list`/`test`/`play` + `~/.sharpee/devkit` name registry (ADR AC-3) — still reserved.
- IDE session must migrate its build.sh references to devkit.

## Status
- **Status**: COMPLETE (3d) — build.sh retired; devkit is the sole build front door; final
  3-target parity PASS; 18 tests GREEN; standalone build verified.
- ADR-180 fully delivered (Phases 1–3): bootstrap, test:npm, build/bundle/browser/zifmia, cutover.
