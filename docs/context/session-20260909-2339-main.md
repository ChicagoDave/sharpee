# Session Summary: 2026-09-09 - main

## Goals
- Continue post-merge cleanup from PR #397, verify the open-items ledger, and close remaining engine issues.
- Diagnose GH #278 (the last open engine issue) and reconcile the abandoned plover-fixes branch onto main.

## Phase Context
- **Plan**: No active plan (`.current-plan` empty; refactoring-survey plan archived this session as part of resolving I-7f0471-1).
- **Phase executed**: N/A — no plan phase; this session ran issue triage, an ADR, and one implementation fix outside plan structure.
- **Tool calls used**: 201 / 100 (Small tier; over budget — session ran long on diagnosis and review, not scope creep).
- **Phase outcome**: N/A (no plan phase).

## Completed

### Open-items ledger verification
Corrected a recitation-without-verification error (David pushed back on "17 mostly closed GH issues"). Closed I-c8a56c-2 (GH #356, verified via `gh issue view`), I-3da76c-1 (GH #373/#372/#364 all closed 2026-09-06), and I-3da76c-3 (mis-cited GH #206; real issue was #374, closed with a pin at `packages/stdlib/tests/unit/validation/unplaced-entity-scope.test.ts`). Amended three Secret Letter items to ON HOLD per David's ruling the port is on hold indefinitely. Corrected two drifted items (I-d99d6d-2: `parser` no longer optional, five guards not eight; I-d99d6d-3: five `endTime` overwrite sites not three). Resolved I-7f0471-1: the refactoring-survey plan's amendment claiming phases remained was stale — all phases were DONE and the plan is now archived.

### GitHub issue cleanup
Closed #376 (ADR-334 tracking issue, all 17 survey phases landed via PR #397), #393 (channelRegistry singleton — verified unreachable: no multi-story hosting, IDE processes are separate, vitest isolates per file, `add` is idempotent), #395 (`packages/engine/tests/setup.ts` never loaded — David chose delete; removed, engine suite unchanged at 764 passing).

### GH #278 diagnosis and ADR-344
An initial fixture-level patch was reverted at David's direction ("no hacking — diagnose the problem thoroughly"). Root finding: the player role-holder invariant (placed, ActorTrait, playable) is enforced only in story-loader's `finalizeRoleHolder`; the engine's install seam validates nothing, so hand-written TypeScript stories get no guarantee. Wrote `docs/architecture/adrs/adr-344-player-role-holder-at-the-install-seam.md`, DRAFT, 9 decisions, including three sections recording that each of the four verified failures was caught by a check structurally incapable of observing what broke.

### Open-questions interview (rule 11a) — all four resolved
Q-1 → D5 (drop story-loader's first-room fallback; 85 Chord sources measured, 19 fallback-reliant, all compiler fixtures, 0 shipped stories; amends ADR-289 D4). Q-2 → D6 (player always comes from story source; `EngineOptions.player` removed; `bootstrap/src/index.ts:274`'s phantom placeholder actor is discarded and never cleaned up — verified via `--world-json` on cloak.story). Q-3 → D7 (ADR-327 D9's specified `ActorTrait.isPlayable` write was never implemented across 32 constructions; flip default to `false`, write from IR). Q-4 → D8 (only 3 of 18 install steps validate anything; only `config` is guarded among four story-supplied inputs).

### adr-review, twice
First pass: 13/20 NEEDS WORK, six findings, all fixed. Second pass (David asked to re-review) found three more, two blockers: D6 left `GameContext.player`'s pre-install state unspecified (`game-engine.ts:1286` dereferences it unguarded — fixed to `IFEntity | undefined`); D7's default flip silently breaks `packages/helpers/src/builders/actor.ts:128`, the author-facing builder (fixed by adding `.playable()`). Final: 21/21 READY FOR IMPLEMENTATION. David ruled no save-format versioning needed (no released stories yet), so D6's entity-id shift is a plain hard break.

### D9 implemented
David found on plover that the `setStory` → `installStory` rename never reached three devkit browser templates (`init-browser.ts:156` scaffolds them to authors; `browser-core.ts:919` uses one for the playground build). Fixed all three. The existing type-check guard (`runBuildCommand` runs `npx tsc` at `build.ts:122`) was never exercised by `browser-build.test.ts` because it goes init → init-browser → build-browser and esbuild strips types without checking them; added the missing tsc call to the test path. It immediately caught two more stale-template bugs: `StoryConfig.author` → `authors` (ADR-298) in four templates. Verified in both directions (fails with `setStory` restored: TS2551; green with the fix). devkit suite: 27 files, 177 passing / 1 skipped.

### Plover branch reconciliation
Recreated the legitimate changes from `origin/plover-fixes-20260910` on main rather than merging: `website/package.json` (next/eslint-config-next → ^16.3.4) + lockfile, `website/scripts/fix-node-modules-owner.sh`, two session summaries, descriptor 9.0.0 → 9.0.1, `genai-api/lang.md`. Rejected `genai-api/plugins.md` and `index.md`, which would have re-added docs for the dissolved `@sharpee/plugin-npc` package. Fixed root cause: `scripts/generate-genai-api.js:80` and `scripts/audit-api-surface.js:30` still enumerated the dissolved package; cleaned both plus the `genai-api/README.md` row. Appended the branch's four `I-31861b-*` ledger rows. One incident: a zsh expansion (`$B:website/...`) truncated three website files to zero bytes; caught immediately and restored byte-identical from the branch.

### GH #398 filed
Real-path test gates check artifact presence, not freshness. `ac-14-browser-real-path.test.ts:32` gates on `existsSync(dist/web/dungeo/index.html)`: absent skips green on a dev machine, stale runs red on a deploy host. Verified both `test` and `test:ci` exit 0 on this machine (119 passing / 2 skipped). Corrects commit `3e460d3b1`'s override trailer, which called these "2 pre-existing failures on main" — they are host-conditioned, not branch-conditioned. Second plover commit blocked this way (I-31861b-1 was the first); filed as GH #398 and recorded as I-7776c8-1.

## Key Decisions

### 1. ADR-344 (ACCEPTED) — player role-holder invariant belongs at the install seam
Nine decisions (D5-D8 from the interview, plus the base diagnosis) establishing that the player must be validated as an install-seam concern, not left to story-loader alone. Amends ADR-289 D4 (drops the first-room fallback) and ADR-327 D9 (flips `isPlayable` default to `false`, written from IR) once implemented. Status DRAFT — passed adr-review at 21/21 but awaits David's acceptance; authorizes no implementation beyond D9 (already shipped as the type-check guard fix).

### 2. No save-format versioning until a story ships
David's ruling: since no stories are released, D6's entity-id shift under ADR-344 is a plain hard break, not a versioned migration — consistent with the standing "no backward compatibility" direction.

## Next Phase
- Plan complete — no active plan. ADR-344 was ACCEPTED by David at 2026-09-10 01:35, after the commit; acceptance authorizes no implementation by itself. D1-D8 need their own plan (only D9 shipped this session), and D5 and D7 are the two that change author-visible behavior.

## Open Items

### Short Term
- I-7776c8-1: GH #398 filed — real-path test gates (`ac-13`/`ac-14`) use artifact presence as a freshness proxy; proposed fix is provenance-stamping the build and skipping stale artifacts with a rebuild reason.
- I-a87c9a-1: GH #396 entropy allowlist drift (pre-existing, unrepointed entries).
- I-a87c9a-2: Phase 8 riders — stale header comment, colliding basenames, missing barrels in new install directories.
- I-31861b-2, I-31861b-3, I-31861b-4: carried unchanged (stale playground compiler; five unaccepted DRAFT ADR amendments; untracked Sparkle appcast tree).

### Long Term
- I-d99d6d-2, I-d99d6d-4: turn-package dead-guard and random-id-idiom riders for future turn-package work.
- I-c8a56c-1, I-3da76c-2, I-4a2d5f-1: Secret Letter items, ON HOLD indefinitely per David's ruling.

## Files Modified

**Config/ledger** (2 files):
- `.devarch/descriptor.json` - version bump 9.0.0 → 9.0.1 (from plover reconciliation)
- `docs/context/.open-items.jsonl` - closures, amendments, and new items from ledger verification and this session's findings

**devkit (D9 fix)** (5 files):
- `packages/devkit/src/standalone/browser-build.test.ts` - added the missing `tsc` type-check step to the build test path
- `packages/devkit/templates/browser/browser-entry.ts.template`, `chord-browser-entry.ts.template`, `playground-entry.ts.template` - `setStory` → `installStory`, `author` → `authors`
- `packages/devkit/templates/story/index.ts.template` - `author` → `authors`

**genai-api / scripts** (4 files):
- `packages/sharpee/docs/genai-api/lang.md` - added (legitimate; `cutting.ts` exists)
- `packages/sharpee/docs/genai-api/README.md` - dropped dissolved `@sharpee/plugin-npc` row
- `scripts/generate-genai-api.js`, `scripts/audit-api-surface.js` - stopped enumerating the dissolved plugin-npc package

**website** (3 files):
- `website/package.json`, `website/package-lock.json` - next/eslint-config-next → ^16.3.4
- `website/scripts/fix-node-modules-owner.sh` - added

**Deleted**: `packages/engine/tests/setup.ts` (GH #395, David's choice)

**Added**: `docs/architecture/adrs/adr-344-player-role-holder-at-the-install-seam.md`, `docs/context/session-20260904-2128-main.md`, `docs/context/session-20260910-0458-plover-fixes-20260910.md`

## Notes

**Session duration**: ~1.7 hours (23:39 CDT – ~01:20 CDT).

**Approach**: Verification-first ledger cleanup, followed by a diagnosis-before-fix investigation of GH #278 that escalated into an ADR when the root cause proved architectural, then a manual branch reconciliation to avoid merging in a dissolved-package regression.

---

## Session Metadata

- **Session**: 7776c8
- **Status**: COMPLETE
- **Blocker**: N/A
- **Blocker Category**: N/A
- **Estimated Remaining**: N/A
- **Rollback Safety**: safe to revert

## Dependency/Prerequisite Check

- **Prerequisites met**: PR #397 merged (survey phases complete) enabled closing GH #376; David's on-hold ruling for Secret Letter enabled the three ledger amendments; David's confirmation of the complete-double route (prior session) was already resolved before this session started.
- **Prerequisites discovered**: The type-check guard for devkit browser templates existed (`build.ts:122`) but was never exercised by the test that should have caught the `setStory`/`author` drift — a test-path gap, not a missing guard.

## Architectural Decisions

- ADR-344 (ACCEPTED 2026-09-10, session 7776c8): player role-holder invariant belongs at the install seam; amends ADR-289 D4 (drops story-loader's first-room fallback) and ADR-327 D9 (`isPlayable` default flips to `false`, written from IR) once implemented. Those two amendments are written by the implementing session, in the same commit as the code, gated by AC-4 and AC-5.
- Pattern applied: diagnose-before-fix (rule: no hacking a fixture-level patch when the defect is architectural) — per David's explicit direction this session.
- No save-format versioning until a story ships (David's ruling, consistent with the project's no-backward-compatibility stance).

## Mutation Audit

- Files with state-changing logic modified: `packages/devkit/src/standalone/browser-build.test.ts` (test harness, not production mutation logic); `packages/devkit/templates/**` (author-facing scaffolding, not runtime mutation).
- Tests verify actual state mutations (not just events): N/A — this session's code change (D9) is a build/test-harness fix, not a state-mutating feature; verified via TS2551 failing/passing check described above.
- If NO: N/A.

## Recurrence Check

- Similar to past issue? YES — this session's central finding is that four separate defects (the untested install seam; ADR-327's REAL-PATH AC asserting both pipeline ends but not the middle; a repo-wide type check unable to see `.template` files; a real-path build test that bundles with esbuild and never type-checks) were each verified only by a check structurally incapable of observing what broke. Treat as a systemic pattern — "the check that cannot see the failure" — not four unrelated incidents. See also I-7776c8-1 (GH #398), which is the same pattern applied to artifact freshness vs. presence.

## Test Coverage Delta

- Tests added: 0 new test files; one `tsc` invocation added to an existing test's execution path (`browser-build.test.ts`).
- Tests passing before: engine 764 passing / 7 skipped (unchanged after GH #395 delete) → devkit 177 passing / 1 skipped after D9 fix (reported by session, unverified — no event-log corroboration available for this session's runs).
- Known untested areas: ADR-344 D5-D8 remain unimplemented and untested; GH #398's proposed provenance-stamping fix is unimplemented.

---

**Progressive update**: Session completed 2026-09-10 01:20; ADR-344 accepted by David at 01:35 and its Status flipped (adr-review 21/21, no Open Questions section — ADR-0009 contract satisfied).
