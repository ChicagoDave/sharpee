# Session Summary: 2026-06-22 - main

## Goals
- Implement ADR-188 Phase 1: theme engine + `:root` default token set in `platform-browser` (AC-1, AC-7)
- Implement ADR-188 Phase 2: both browser builds (devkit + repokit) source CSS from `platform-browser` (AC-4)
- Stand up visual Playwright harness for ADR-188 visual ACs

## Phase Context
- **Plan**: `docs/work/themes-as-plugins/plan-20260622-themes-as-plugins.md` — replace five hand-copied themes with a design-token engine; `@sharpee/platform-browser` owns the engine; themes become independent npm packages
- **Phase executed**: Phase 1 — "Theme engine + :root default in platform-browser" + Phase 2 — "Both browser builds source CSS from platform-browser" (both ADR-188 Acceptance Criteria phases)
- **Tool calls used**: 82 / unbudgeted
- **Phase outcome**: Completed; both phases verified by typecheck, unit tests, and real-path Playwright visual harness

## Completed

### Phase 1 — Theme engine in platform-browser (AC-1, AC-7)
- Created `packages/platform-browser/styles/base.css` (verbatim devkit structural CSS, owner header updated) and `styles/engine.css` (`:root` default token set — all 16 tokens, white-on-blue `classic` — plus the de-scoped token-consuming component layer extracted from the `modern-dark` block in `templates/browser/styles.css`).
- Extraction was diff-verified: 285 declaration/selector lines identical to the de-scoped source. The `#333` latent bug (dos-classic hardcode) is avoided by construction; `modern-dark` used `var(--theme-border)` throughout. The three System-6 extended tokens (`--theme-desktop-bg`, `--theme-font-body`, `--theme-font-chrome`) default to base values so the 16-token contract is complete.
- Added `packages/platform-browser/ts-forge.json` `{"assets":["styles/**"]}` and `files:["dist","dist-esm","styles"]` so `tsf build --npm` publishes the CSS to `~/.tsf-publish/sharpee/platform-browser/styles/`. Verified.
- Stood up real-path Playwright visual harness at `packages/platform-browser/tests/visual/` — `playwright.config.ts`, `fixture.ts` (generates test pages from the platform's own `index.html` re-headed to link real engine CSS), `theme-engine.spec.ts`. Added `@playwright/test` devDep + `test:visual` script. 3/3 pass: AC-1 (zero-theme → white-on-blue), AC-7 (unknown theme → `:root` default), AC-7 (unset token → per-token `:root` fallback). Config/fixture made ESM-safe (`package.json` is `type:module`). Added `tests/visual/.gitignore` for generated fixtures/output.

### Phase 2 — Both browser builds source CSS from platform-browser (AC-4)
- Moved `decorations.css` into the engine layer (`packages/platform-browser/styles/decorations.css`); content was identical across devkit + repo-root template dirs — lossless move.
- Added `platform-browser` exports `"./package.json"` and `"./styles/*"` so devkit's build resolves the styles dir from a story's `node_modules`.
- `packages/devkit/src/standalone/build-browser.ts`: copies `base`, `engine`, `decorations` from resolved `@sharpee/platform-browser/styles` via new `resolveEngineStylesDir()` using `require.resolve`; no longer reads from `TEMPLATES_DIR`; ships no theme CSS or fonts; help text + header updated.
- `tools/repokit/src/commands/browser.ts`: copies the three CSS files from `packages/platform-browser/styles`; dropped `infocom.css→styles.css` theme copy + `themes/` fonts; website mirror updated; header note updated.
- Both builds now delete obsolete `styles.css` and `themes/` from output (and repokit's website mirror) so a rebuild over a pre-ADR-188 dist never serves stale theme CSS.
- Both `index.html` templates (devkit + repo-root) relinked `base → engine → decorations`, with a comment marking the Phase 4 theme-link injection slot.
- Updated `devkit/src/standalone/browser-build.test.ts` assertions: engine CSS present, `styles.css` + `themes/` absent.
- Verified: devkit + repokit typecheck clean; `repokit/browser.test.ts` (2) pass; devkit real-path `browser-build.test.ts` passes; real `buildBrowserClient(root,'dungeo')` run produces byte-identical engine CSS and removes stale theme artifacts.

## Key Decisions

### 1. decorations.css is a platform-shipped engine layer
User-confirmed: move `decorations.css` to `platform-browser` rather than keep it in template dirs. It is ADR-174 platform vocabulary, not author-customizable content.

### 2. Dungeo renders engine-default in the interim
Between Phase 2 and Phase 4 (theme-link injection), Dungeo renders white-on-blue `classic` (the `:root` default). Accepted as a known interim state — not a regression.

### 3. Visual harness lives in-repo, not in a parallel session
Planned the harness as a parallel-session dependency. Instead stood it up in-repo during Phase 1 so Phase 1 could be verified immediately. This unblocked Phase 2 without waiting.

## Next Phase
- **Phase 3**: "Theme package contract" — define `@sharpee/theme-<name>` shape (`theme.css` with `[data-theme]` token block + flourishes, `package.json` `sharpee.theme` manifest); migrate the 5 themes (`modern-dark`, `retro-terminal`, `paper`, `system-6`, optionally `dos-classic`). Source = retained `templates/browser/styles.css` + Phase-1 flourish diff. Extend visual harness with computed-style parity specs per migrated theme.
- **Tier**: platform change (requires discussion per CLAUDE.md)
- **Entry state**: `templates/browser/styles.css` (5-theme kit) + `templates/browser/themes/` (System-6 fonts) intentionally retained as Phase 3 migration source; `packages/platform-browser/styles/` owns the engine layer; devkit + repokit both wire from it.

## Open Items

### Short Term
- Template cleanup deferred: `templates/browser/styles.css`, `templates/browser/themes/`, and the redundant `base.css`/`decorations.css` copies in template dirs should be deleted after Phase 3 packages exist. Never delete without user confirmation.
- Phase 6 (full in-browser render of a built story end-to-end) not done; scoped out of this session.

### Long Term
- `pnpm-lock.yaml` updated with `@playwright/test` devDep; verify no unexpected lockfile conflicts if other package authors pull.

## Files Modified

**platform-browser** (7 files):
- `packages/platform-browser/package.json` — added `@playwright/test` devDep, `test:visual` script, `exports` for `./styles/*`
- `packages/platform-browser/ts-forge.json` — new; `{"assets":["styles/**"]}`
- `packages/platform-browser/styles/base.css` — new; structural layout CSS
- `packages/platform-browser/styles/engine.css` — new; `:root` 16-token default + de-scoped component layer
- `packages/platform-browser/styles/decorations.css` — new (moved from template dirs)
- `packages/platform-browser/tests/visual/playwright.config.ts` — new
- `packages/platform-browser/tests/visual/fixture.ts` — new
- `packages/platform-browser/tests/visual/theme-engine.spec.ts` — new; 3 real-path AC specs
- `packages/platform-browser/tests/visual/.gitignore` — new

**devkit** (3 files):
- `packages/devkit/src/standalone/build-browser.ts` — sources CSS from `@sharpee/platform-browser/styles`; adds `resolveEngineStylesDir()`; removes theme copy; deletes stale output
- `packages/devkit/src/standalone/browser-build.test.ts` — updated assertions for new CSS layout
- `packages/devkit/templates/browser/index.html` — relinked `base→engine→decorations`; Phase 4 injection comment

**repokit** (1 file):
- `tools/repokit/src/commands/browser.ts` — sources CSS from `packages/platform-browser/styles`; removes theme copy; website mirror updated

**templates** (1 file):
- `templates/browser/index.html` — relinked `base→engine→decorations`; Phase 4 injection comment

**plan** (1 file):
- `docs/work/themes-as-plugins/plan-20260622-themes-as-plugins.md` — Phase 1 and Phase 2 marked DONE with verification notes

**lockfile** (1 file):
- `pnpm-lock.yaml` — `@playwright/test` devDep added

## Notes

**Session duration**: ~1 session (started 2026-06-23T03:34Z; current date 2026-06-22 CST)

**Approach**: Diff-driven extraction (verified 285 lines identical) before authoring engine CSS; real-path Playwright harness before declaring visual ACs done; pnpm-lock updated but not committed separately.

---

## Session Metadata

- **Status**: COMPLETE
- **Blocker**: N/A
- **Blocker Category**: N/A
- **Estimated Remaining**: N/A
- **Rollback Safety**: safe to revert — no schema or wire changes; CSS and build-script changes only

## Dependency/Prerequisite Check

- **Prerequisites met**: ADR-188 accepted (prior session); `plan-20260622-themes-as-plugins.md` written (prior session); `tsf build --npm` mechanism understood from ADR-187 (`ts-forge.json` assets)
- **Prerequisites discovered**: `@playwright/test` not yet in `platform-browser` devDeps — added this session

## Architectural Decisions

- ADR-188 (ACCEPTED, prior session): themes-as-plugins — `platform-browser` owns the token engine; themes are independent npm packages with `[data-theme]` token blocks and `sharpee.theme` manifests
- Pattern applied: `ts-forge.json` `{"assets":[...]}` for publishing non-dist CSS (per `project_tsf_publish_assets.md` memory entry)
- Decision this session: `decorations.css` classified as engine layer (not author template), moved to `platform-browser` — user-confirmed

## Mutation Audit

- Files with state-changing logic modified: none (CSS authoring + build-script copy logic; no domain state mutations)
- Tests verify actual state mutations (not just events): N/A

## Recurrence Check

- Similar to past issue? NO

## Test Coverage Delta

- Tests added: 3 (Playwright visual specs in `theme-engine.spec.ts`) + 1 updated (`browser-build.test.ts`)
- Tests passing before → after: repokit `browser.test.ts` 2/2 → 2/2; devkit `browser-build.test.ts` 1/1 → 1/1; platform-browser `test:visual` 0/0 → 3/3
- Known untested areas: Phase 3 theme packages (not yet created); Phase 4 theme-link injection into `index.html`; Phase 6 full in-browser render

---

**Progressive update**: Session completed 2026-06-22 23:53 CST
