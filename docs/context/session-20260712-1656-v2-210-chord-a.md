# Session 2026-07-12 16:56 — v2-210-chord-a (id 034f09)

## Goal

Walk David through ADR-211's Q1–Q7 owner decisions and record the
resolutions.

## Done

- Pre-session audit clean (only recurring noise: 3 untracked scratch
  files in docs/work — blog-v2.md, phrase-1-prompt-md,
  phrase-2-prompt.md; David hasn't decided commit/ignore/delete).
- **ADR-211 Q1–Q7 all resolved by David** (one at a time, in
  conversation):
  - Q1 → full unification (TS + Chord share the bare-fragment + join
    rule).
  - Q2 → join rule confirmed as drafted (site-determined mode,
    bare-fragment load/lint error, clause-site-terminator warning).
  - Q3 → BOTH presence spellings now: `is in <room>` + the `here`
    deictic (new ratchet entry), both compiling to `mentions`.
  - Q4 → build the registered-gate seam NOW and wire Chord to it
    immediately: any `while <condition>` on a fragment works
    (presence → data, everything else → seam). No presence-only error.
  - Q5 → adverb vocabulary REWORKED to mirror the machinery 1:1:
    `cycling, stopping, randomly, sticky, first-time`; `ordered`/`once`
    retire (no shipped story uses them as strategies; rule-modifier
    `once` untouched). Verified semantics from
    english-assembler.ts pickChoiceAlternative and the loader table
    (runtime.ts STRATEGY_SELECTOR) before proposing.
  - Q6 → counter keying confirmed: `(roomId, marker)` marker sites,
    `('chord', key)` statements, hazard documentation-only.
  - Q7 → supersession note added to ADR-209's Status block.
- **ADR-211 flipped to ACCEPTED** — resolutions recorded inline
  (§Open questions), folded into Decision 3 (here + seam wiring),
  Decision 4 (new adverb table), the compile contract (step 3: seam
  registration instead of LoadError), touched packages, Consequences,
  and new AC-11 (here ≡ is-in), AC-12 (seam gate + save/restore),
  AC-13 (retired adverbs are a load error naming replacements).
- **ADR-209** Status block: appended "superseded in part by ADR-211
  (authoring surface); machinery decisions remain in force."

- **Ratchet drafts RE-CUT v2** (David: "re-cut") —
  docs/work/chord-zoo-surfaces/ratchet-drafts.md rewritten against
  accepted ADR-211. ON HOLD lifted. Z2 rewritten (bare fragments stored
  bare; platform joins at render via Spliced, not loader-prepended);
  Z1/Z3/Z3b re-confirmed; NEW Z4 (`here` deictic, kit-wide scope
  recommended, fragment gates compile to mentions) and Z5 (adverb
  mirror table, ordered/once retired w/ load error). Checkpoints
  re-cut: CP1' gate-attachment spelling (recommend define-phrase
  trailing `while`); CP2 RESOLVED (`nothing`, per ADR-211); CP3/CP4/CP6
  carried; CP5' notes Q4's full-generality pull toward the condition
  kit; CP7' reshaped — recommend two packages, 211-core platform first,
  zoo-surfaces rides it.

- **All re-cut checkpoints RESOLVED by David** (walked one at a time):
  CP1' → gate = trailing `while` on the define-phrase header (one
  spelling across phrase hosts). CP3 → lifecycle keys confirmed AND
  channel phrases get strategy adverbs NOW (`phrase present,
  cycling:`). CP4 → all four keys' runtime ships. CP5' → Z3b full
  condition kit (on/lit → trait fields, others → registered
  state-clause providers). CP6 → excluded wt-02/wt-03 ABSORB into the
  chain, originals retired/deleted once covered (freeze protects gate
  files only). CP7' → two packages, 211-core platform first,
  zoo-surfaces rides it. Resolutions + affected entry rows recorded in
  ratchet-drafts.md.

- **TRANSCRIBED (David: "transcribe")**: all six entries (Z1, Z2, Z3,
  Z3b, Z4, Z5) appended to chord-grammar-changes.md as dated approved
  rows; ratchet-drafts.md header flipped to APPROVED + TRANSCRIBED
  (log rows normative where they differ).

## Status

ADR-211 ACCEPTED; zoo-surfaces grammar entries APPROVED + TRANSCRIBED
into the ratchet log. No code touched. Uncommitted: two ADR edits,
ratchet-drafts rewrite, ratchet-log append, this summary.

## Next

- Package plans per CP7': (1) 211-core platform package (Spliced +
  join rule + gate seam + TS-entry migrations, byte-identical on the
  existing corpus — AC-1/5/7); (2) chord-zoo-surfaces package (Z1–Z5
  surfaces, zoo.story migration incl. presence→present ×4, excluded
  transcripts absorbed into the chain + originals retired —
  AC-4/6/8/11/12/13 + fixtures). Then implementation phases,
  platform work with David's per-package go-ahead.
