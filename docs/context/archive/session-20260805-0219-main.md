# Session Summary: 2026-08-04 into 2026-08-05 - main (CST)

## Goals
- Resume the ADR-300 transcript-tool plan at Phase 3.
- (Emerged) Correct documentation drift and consolidate the ADR-300/301 pair.
- (Emerged) Design and accept ADR-302 (transcript branches) and re-plan the harness work around it.

## Phase Context
- **Plan**: `docs/work/branch-tester/plan-20260805-branch-tester.md` — "Finish ADR-300's channel model, then build `@sharpee/branch-tester`."
- **Phase executed**: None. The session began resuming `docs/work/transcript-tool/plan.md` Phase 3, but that plan and its source ADR were retired mid-session (see Key Decisions). A new 11-phase plan was written at session end; every phase is `PENDING` and Phase 1 explicitly requires go-ahead before starting.
- **Tool calls used**: 264 (from `.session-state-5113ca.json`) / N/A (no phase budget — planning/documentation session).
- **Phase outcome**: N/A — no phase executed. Plan authored and re-sequenced, not started.

## Completed

### Course correction (early session)
- Claude proposed an HTTP server for a CLI-hosted transcript editor; David challenged it — the word "server" appears nowhere in ADR-300, it was inferred.
- Claude then built a full VS Code webview-host recommendation plus a proposed ADR amendment from a passing remark. David stopped it: VS Code / plain-TypeScript authoring is not a current priority; Chord and the macOS IDE are what matter.
- Root cause traced to `docs/reference/core-concepts.md` not having been read at session start per CLAUDE.md. David revealed the whole `docs/reference/` directory is Claude-authored drift he does not maintain — ground truth is the packages, the languages, and sharpee.net.

### Documentation repair
- Audited `docs/reference/core-concepts.md` against the packages; fixed three verified-wrong items: removed `textService` from the documented `GameEngine` constructor, corrected the action-directory file count (3 → 6), and fixed the "four-phase pattern" section (it documented three, with `blocked` missing from the `Action` interface and "Command Processing Flow" saying "three phases").
- Added a Packages section covering all 32 packages (2+ sentences each, grouped by purpose), sourced from each package's own `package.json` description and root-barrel header; names diffed against disk for an exact match.
- Added a "The Command Lines" section disambiguating `./sharpee` (author tool), `./repokit` (in-repo platform build), and `dist/cli/sharpee.js` (platform bundle, testing).
- Added a "Where the work is" section: platform is secondary to Chord and the IDE, then amended per David's instruction — secondary is not subordinate; Sharpee and Chord must align as elegantly as possible, a two-sided obligation.
- Moved the file to `docs/core-concepts/README.md` (`git mv`, history preserved). Created `docs/reference/README.md` warning the directory is unevenly maintained and must never be mistaken for current repo state. Repointed 7 live references (`CLAUDE.md`, `docs/README.md`, `docs/api/README.md`, `docs/architecture/README.md`, `docs/guides/project-structure.md`, `docs/work/website-content/plan.md`, `packages/world-model/CLAUDE.md`); deliberately left 5 archive/historical files alone.

### ADR-300/301 consolidation
- Deleted (no archive, per David's instruction) the old `adr-300-transcript-editor.md` and `adr-301-the-opening-as-addressable-channels.md`.
- Wrote a new single ADR-300, "Addressable Channels and the Canonical Transcript" (`docs/architecture/adrs/adr-300-addressable-channels-and-canonical-transcript.md`), 16 decisions, with a shipped-vs-remaining status table.
- Cut the standalone-editor program out of ADR-300; its design ideas moved to a new ADR-301 (`docs/architecture/adrs/adr-301-sharpee-transcript-editor.md`), status TBD, holding open the question of what hosts an editor.
- Repointed 5 inbound references (adr-282, adr-290, adr-294, adr-298, adr-299); adr-290 and adr-299 needed rewording, not just relinking, because the superseding mechanism they referenced is now TBD rather than specified.
- Renumbered so 301 is the editor and 302 was left free for the branches ADR, with collision notes left in both files.

### ADR-302 — Transcript Branches (ACCEPTED)
- Measured David's own stated state-of-play and found it optimistic: "testing complex channel emissions" is flat substring matching in the `[CHANNEL:]` tier (no dotted paths), and branch support did not exist at all.
- Designed branches with David: file-level parent pointers via `continues:`, no interior addressing — a branch is two children of one parent.
- Ran `/devarch:adr-interview` — 8 questions resolved one at a time; 2 were raised mid-interview by David's own questions (whether transcript tests are needed at all; whether to build a new harness).
- Ran `/devarch:adr-review` — scored 9/17 NEEDS WORK, caught two internal contradictions where the interview had outrun an earlier answer. Fixed all findings, added 9 acceptance criteria and a worked scenario, then resolved the two review-raised open questions (harness home, harness selection).
- **ACCEPTED** with 16 decisions: file-level parent pointers, no interior addressing; a branch is two children of one parent; whole-header inheritance with child override; `--chain` retired, every path runs; a new harness `@sharpee/branch-tester` as a full copy with no shared code; permanent v1/v2 split by story (Dungeo + Family Zoo tutorial stay on `transcript-tester`); no unit tests in v2, every test a branch; filename as identity with rename as a harness operation; in-repo directory separation.
- Discharged ADR-302's ownership clause against ADR-300 by marking ADR-300 D17 **SUPERSEDED IN PART** — split, not blanket-flipped, since only its "convention, not grammar" half fell.

### Code change — `transcript-tester` test scoping
David objected to `header-folding.test.ts` walking the whole `stories/` tree from inside a package's unit tests, calling it aggressive. Told this contradicted ADR-302 D15's freeze, he said "D15 can bite me" — D15 was amended to cover grammar and runtime semantics rather than test hygiene, and D16 was re-based (its stated justification, the test collision, was being deleted; its real reason is that `transcript-tester` silently accepts and standalone-runs a `continues:` file, reporting a meaningless pass).
- New `packages/transcript-tester/tests/corpus.ts` — corpus is configured, never discovered; a configured-but-missing root throws.
- New `packages/transcript-tester/vitest.config.ts` — single place the corpus path is declared.
- `packages/transcript-tester/tests/header-folding.test.ts` and `serializer-roundtrip.test.ts` — both sweeps became `describe.skipIf(!hasCorpus)`; `expect(files.length).toBeGreaterThan(100)` (an assertion on repo size) removed; two tests that read specific repo files converted to inline fixtures.
- **Verified 2026-08-05 02:19 CST (re-run for this summary)**: `pnpm --filter '@sharpee/transcript-tester' test` → `Test Files 21 passed (21)`, `Tests 252 passed (252)` — unchanged from pre-session baseline.
- **Skip-path verification (session event log, hook-recorded)**: with the corpus config moved aside, `2026-08-05T07:01:04Z` — "2 passed 24 passed"; `2026-08-05T07:01:48Z` — "2 passed 19 passed", confirming `describe.skipIf` actually skips when the corpus is absent. That check also disproved a comment Claude had written claiming `SHARPEE_TRANSCRIPT_CORPUS= vitest run` would suppress the sweep — it still swept, because vitest's `test.env` beats an inherited shell variable. Both the code and the comment were corrected.

### Planning
- Wrote `docs/work/branch-tester/plan-20260805-branch-tester.md` (11 phases) and repointed `.current-plan`.
- `plan-review` found a CONTRADICTION: Phase 1 assumed "six registration points" but `transcript-tester` uses exactly one (`ts-forge.config.json`) and needs repokit awareness in four other files instead; registering into the umbrella would violate ADR-178. Fixed, and corrected the stale `feedback_new_package_config` memory (it named a `build.sh` that no longer exists post-ADR-187).
- David asked whether ADR-300 or ADR-302 goes first. Measurement found a collision: 2921 `main`-reading assertions across stories (dungeo 1966, fernhill 584, friendly-zoo 183, cloak-of-darkness 103, others 85) — dissolving `main` after freezing `transcript-tester` would make ADR-302 D9's "keeps working unchanged" false. Plan re-sequenced to 11 phases with the channel work (ADR-300 D8/D9) first; ADR-300's Remaining table gained a "Lands in" harness column.

## Key Decisions

### 1. ADR-300/301 split, not one ADR
The editor's host is a genuinely unresolved question (VS Code webview vs. IDE-hosted vs. something else); forcing it into ADR-300 alongside the settled channel model would have blocked acceptance of the settled parts on the unsettled one.

### 2. ADR-302 D15 amended mid-session on David's objection
"D15 can bite me" — the freeze was never meant to cover test hygiene inside `transcript-tester`'s own package; only its grammar and runtime semantics are frozen for v1/v2 parity.

### 3. Sequencing: channels before the harness copy (ADR-300 before ADR-302's build phases)
Measured, not assumed: `branch-tester` is a full copy of the parser/runner/assertion tier, so dissolving `main` after the copy means migrating 2921 assertions twice; freezing `transcript-tester` before dissolving `main` would falsify ADR-302 D9 for Dungeo's 1966 assertions. See `docs/architecture/adrs/adr-300-addressable-channels-and-canonical-transcript.md` and `docs/architecture/adrs/adr-302-transcript-branches.md`.

## Next Phase
- **Phase 1**: "Dissolve `main`, add `preferred-layout`" (ADR-300 D8, D9) — Large tier, 400-call budget, **requires explicit go-ahead** (rewrites 2921 assertions and re-blesses 21 goldens across every story).
- **Entry state**: `main` still routes seven block keys through `stdlib/src/channels/standard.ts:132`; nothing in this phase has started.

## Open Items

### Short Term
- Get go-ahead for Phase 1 (channel dissolution) before any code work resumes.
- Side items not yet scheduled: `[EVENTS: N]` removal (issue #222, do before Phase 3) and `.skein` retirement (gated on separate explicit confirmation).

### Long Term
- Phase 5's seed-on-restore question is the plan's one genuine unknown — a negative result there is an ADR-302 amendment, not something to engineer around.
- ADR-301 (the editor) remains TBD, blocked on its own open question of what hosts it.

## Files Modified

**Documentation / reference** (10 files):
- `docs/reference/core-concepts.md` → `docs/core-concepts/README.md` (moved, content corrected and expanded)
- `docs/reference/README.md` — new, deprecation notice for the directory
- `CLAUDE.md`, `docs/README.md`, `docs/api/README.md`, `docs/architecture/README.md`, `docs/guides/project-structure.md`, `docs/work/website-content/plan.md`, `packages/world-model/CLAUDE.md` — repointed to the moved file

**ADRs** (7 files):
- `docs/architecture/adrs/adr-300-transcript-editor.md` — deleted (no archive)
- `docs/architecture/adrs/adr-301-the-opening-as-addressable-channels.md` — deleted (no archive)
- `docs/architecture/adrs/adr-300-addressable-channels-and-canonical-transcript.md` — new, ACCEPTED, 16 decisions
- `docs/architecture/adrs/adr-301-sharpee-transcript-editor.md` — new, TBD
- `docs/architecture/adrs/adr-302-transcript-branches.md` — new, ACCEPTED, 16 decisions, 9 acceptance criteria
- `docs/architecture/adrs/adr-282-play-to-test-tagging.md`, `adr-290-test-creation-as-an-atomic-mode.md`, `adr-294-golden-transcripts-tester-rebuild.md`, `adr-298-story-block-metadata.md`, `adr-299-play-skein-bless.md` — repointed/reworded references

**Code — transcript-tester test scoping** (4 files):
- `packages/transcript-tester/tests/corpus.ts` — new, configured corpus discovery
- `packages/transcript-tester/vitest.config.ts` — new, single corpus-path declaration
- `packages/transcript-tester/tests/header-folding.test.ts` — `skipIf`, inline fixtures, size assertion removed
- `packages/transcript-tester/tests/serializer-roundtrip.test.ts` — `skipIf`, inline fixtures

**Planning** (2 files):
- `docs/work/branch-tester/plan-20260805-branch-tester.md` — new, 11 phases
- `docs/work/transcript-tool/plan.md` — retired in place (Phases 1-2 kept as live history, 3-9 marked retired via banner note)

## Notes

**Session duration**: ~15 hours wall clock (2026-08-04 evening into 2026-08-05 early morning), heavy on discussion/design, light on code.

**Approach**: The only code shipped this session is the transcript-tester test-scoping change; everything else — the two ADRs, the documentation repair, and the 11-phase plan — is design and planning work. No implementation phase from either plan was executed.

**Recurring theme**: two separate instances of building on inference rather than verification (the HTTP server, the VS Code webview host), both caught by David before code was written, plus one self-caught wrong claim in a code comment (the `SHARPEE_TRANSCRIPT_CORPUS=` env-var suppression claim, disproved by the skip-path test run). All three are documentation/design-time inference failures, not implementation bugs — worth naming as a pattern.

**Memory files updated outside the repo** (not tracked here as repo files): `project_platform_secondary_to_chord_and_ide.md`, `project_dungeo_is_an_outlier.md`, `project_sharpee_net_canon_docs.md`, `feedback_new_package_config.md` (corrected — the `build.sh` it named no longer exists post-ADR-187).

**Gap**: no `.session-template.md` exists at `docs/context/` — this summary follows the work-summary-writer's built-in full-form template directly.

---

## Session Metadata

- **Status**: COMPLETE
- **Blocker** (if any): N/A
- **Blocker Category**: N/A
- **Estimated Remaining**: N/A (session complete; remaining work is the 11-phase plan, not a continuation of this session's task)
- **Rollback Safety**: safe to revert — no destructive platform changes landed; the only code change is additive/scoping in a test package.

## Dependency/Prerequisite Check

- **Prerequisites met**: ADR-302 required measuring the actual assertion-count collision (2921 across stories) before sequencing could be decided — done via grep-based measurement, recorded in the plan's References consulted.
- **Prerequisites discovered**: `docs/reference/core-concepts.md` was stale against the packages in three concrete ways (found only because David forced a stop-and-verify); the `feedback_new_package_config` memory named a build script (`build.sh`) that ADR-187 had already replaced.

## Architectural Decisions

- ADR-300 (rewritten, ACCEPTED): "Addressable Channels and the Canonical Transcript" — 16 decisions, D17 now SUPERSEDED IN PART by ADR-302.
- ADR-301 (new, TBD): "Sharpee Transcript Editor" — holds the open question of what hosts an editor; decides nothing yet.
- ADR-302 (new, ACCEPTED): "Transcript Branches — Testing Authored Variation" — 16 decisions, 9 acceptance criteria; `@sharpee/branch-tester` will be a full copy of `transcript-tester` with no shared code, permanently split by story.
- Pattern applied: `/devarch:adr-interview` then `/devarch:adr-review` for ADR-302 (interview surfaced 2 new open questions mid-flight; review caught 2 contradictions the interview left behind).

## Mutation Audit

- Files with state-changing logic modified: `packages/transcript-tester/tests/header-folding.test.ts`, `packages/transcript-tester/tests/serializer-roundtrip.test.ts`, `packages/transcript-tester/tests/corpus.ts` (test-scoping only — no production state mutation).
- Tests verify actual state mutations (not just events): N/A — this session's only code change is test infrastructure (corpus discovery scoping), not a state-mutating feature.
- If NO: N/A.

## Recurrence Check

- Similar to past issue? YES — the inference-over-verification pattern (HTTP server, VS Code host) echoes the general shape flagged in `feedback_pragmatic_interims_vs_design_intent.md` (Claude-proposed shortcuts read as design intent when they are not) and `feedback_no_hallucinated_worldmodel_apis.md` (inventing rather than checking). No prior session file matched by name; this is a cross-session pattern in memory, not a repeated finding from one prior `docs/context/session-*.md`.
- If YES: worth a lightweight standing habit — read `docs/core-concepts/README.md` (moved this session) at the start of any session touching platform architecture, since skipping it was the traced root cause here.

## Test Coverage Delta

- Tests added: 0 (test-scoping change only; no new test cases, two existing sweeps converted from unconditional to `skipIf`).
- Tests passing before: 252 → after: 252 (evidence: fresh run 2026-08-05 02:19 CST, `pnpm --filter '@sharpee/transcript-tester' test` → `Test Files 21 passed (21)`, `Tests 252 passed (252)`).
- Known untested areas: the skip-path itself has no permanent regression test (verified manually this session via config-moved-aside + event-log timestamps `2026-08-05T07:01:04Z`/`07:01:48Z`, not a committed assertion).

---

**Progressive update**: Session completed 2026-08-05 02:19 CST
