# Session Summary: 2026-06-20 - main

**Goal**: Update the sharpee.net website to announce the Sharpee 1.0 stable release.
**Status**: COMPLETE
**Outcome**: Added a sitewide announce bar (injected via components.js to cover all pages), a version pill badge on the homepage hero, a June 2026 news entry, and fixed the footer year from 2025 to 2025–2026. Then corrected all author-facing CLI instructions to the ADR-180 model: authors install the CLI globally (`npm i -g @sharpee/devkit`) and run the bare `sharpee` command — replacing every `npx @sharpee/sharpee` reference across getting-started, hero, news, cloak-of-darkness, zoo-17, and the community npm link. Deployed via GitHub Pages (push to site/** auto-triggers the workflow).

**Files modified**: `site/components.js`, `site/index.html`, `site/news.html`, `site/style.css`, `site/getting-started.html`, `site/cloak-of-darkness.html`, `site/zoo-17.html`, `site/community.html`

**Commits**: `f63c064b` (1.0 announce), `46524213` (sharpee CLI / drop npx) — both pushed to origin/main.

**Notes**:
- Session began with a git reconciliation — local branch was 95 commits behind origin/main with a stale CLAUDE.md edit and a deleted .devarch file, both already superseded upstream. Fast-forwarded cleanly to b9f548ac before starting website work.
- The announce bar approach (injected via components.js prepend) avoids touching all 30+ individual HTML pages.
- The npx→CLI correction came after the first push: ADR-180 Amendment 1 reversed the delivery model from `npx @sharpee/sharpee …` to a globally-installed `sharpee` bin shipped by `@sharpee/devkit`. The first commit's npx copy was already live, so the fix was pushed promptly.
- Fixed the `origin` remote URL casing (`chicagodave` → `ChicagoDave`) to stop relying on GitHub's moved-repo redirect on push.
