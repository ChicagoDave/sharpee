# Session Summary: 2026-07-10 — v2-209-book (book edition work, Phases 1+)

## Goals
- Execute docs/work/v2-209-book/plan.md: teach ADR-209 snippets in the book
  (new ch5 section + cross-refs + companion familyzoo code) and apply the
  2026-07-08 reader-pass slice (npx serve, multi-user removal, CLI/terminal
  repositioning).

## Completed
- Phase 1 DONE: planning docs + ADR-107/209 status amendments committed on
  main (`3b829801`); branch `v2-209-book` cut and checked out.
- Phase 4 DONE (pulled ahead of 2-3; no file overlap, all decisions made):
  npx serve swap (ch1/ch31, port 3000), multi-user removed (preface, ch24
  x3), CLI/terminal recast as testing/accessibility (preface, ch24 incl.
  L79-81 screen-reader strengthening, ch27, ch31, appendix-a), ch19
  (roomId, markerName) keyspace + branching cross-ref, ch29 determinism
  note, ch30 counter persistence + mid-cycle resume, ch31 unused-entry
  lint sentence. code-snippets regenerated via extract-book-snippets.cjs.
  Verification: multi-user/python greps zero across book tree; em-dash
  check clean (only permitted ch19 quoted error); build-book.sh v2.0.0
  html clean; spot-checked rendered HTML. Uncommitted on v2-209-book.

- Phase 2 DONE: David chose (a) (issue #179, answered in-session, closed).
  ch4 "First visits get their own prose" section (Aviary example, listing
  updated); ch5 "Room-description snippets" full section + room-snippets
  transcript block + takeaway; ch2 braces parenthetical; ch13 handler
  mutation aside (illustrative). Snippets regenerated, em-dash clean,
  build html clean, rendered output spot-checked.

## In Progress
- Phase 3 (companion code): REAL-PATH VERIFICATION GREEN via
  stories/friendly-zoo (the workspace testing target): enamel-pins snippet
  example in the gift shop (cycling + empty entry + mentions gate),
  room-snippets.transcript 8/8, full friendly-zoo suite 47/47 across 8
  transcripts. Key discoveries: (1) the bundle cannot load the tutorial
  directly (multi-file index.js re-export unresolvable); tutorial vNN
  transcripts run in the Docker/published-npm harness, which predates
  ADR-209, so tutorial-side runs are deferred to the next platform publish
  / naive-regression round. (2) friendly-zoo's petting zoo already has
  rabbits with ADR-195 presence lines (duplicate-entity trap), so the
  friendly-zoo exercise lives in the gift shop instead. Tutorial
  propagation (16 snapshot files: aviary initialDescription pair, petting
  zoo marker, snippet map, rabbits entity for the two multi-file
  snapshots) delegated to a subagent; tutorial room-snippets.transcript
  written (book-exact).

## Completed (continued)
- Phase 3 DONE: subagent propagated all 16 tutorial snapshot files
  (script, exact-match assertions; per-file prose variants preserved;
  multi-file snapshots gained the missing rabbits entity). Tutorial
  room-snippets.transcript written book-exact (runs at next
  naive-regression, post-publish). friendly-zoo real-path proof: 8/8 new
  + 47/47 full suite.
- Phase 4 DONE (see above, executed early).
- Phase 5 verification largely subsumed: book html built clean after the
  last content edit; rendered sections spot-checked; friendly-zoo suite
  re-run green; build/ artifacts gitignored (never committed).

## Post-merge follow-up (same session)
- Branch committed (`b54200ca`), PR #180 merged (`b5c6a58d`), main synced.
- David published lockstep **2.2.0** to npm (previous 2.1.1 predated ADR-209).
- Standalone verification on published 2.2.0 (`npx sharpee build --test`
  from a scaffolded copy): first run 201/202 — the multi-file snapshots
  already had rabbits in characters.ts (grep gap), the propagated
  duplicate broke `examine rabbits` disambiguation. Fixed: duplicate
  removed from both zoo-items.ts, snippet map moved to both
  characters.ts after the existing rabbits. Re-run: **202/202**. The
  published-package verification loop for the book's ch5 material is
  fully closed.

- Rabbits-dedupe fix committed (`6fe8b7ab`) and pushed. Lockstep 2.2.0
  version bumps (29 package.json files) committed as the release record.

## Book gate round (runs 10-11)
- Title page now reads "Version 2, Revision 2" (00-title-page.md +
  .book-version style in book.css/epub.css; book.yaml subtitle v2.0 ->
  v2.2). Verified in rebuilt html + epub.
- naive-prompt.txt updated: title-page artifact check, accepted-by-design
  item 4 (deliberately varying room text: ch4 initialDescription, ch5
  snippet cycling), "Package versions this run expects" pinning 2.2.0.
- Run 10 (2.2.0 PDF): every chapter PASS/PASS-WITH-ISSUES, zero BLOCKED,
  zero deviations, 174 assertions green. Log at
  docs/work/test-book/execution-log-20260710-run10.md.
- Run-10 fixes applied (7 chapters): ch19 reference-chapter "nothing to
  type" markers; ch26 zoo-sunny token block gains --theme-bg-alt +
  --theme-menu-bg (companion familyzoo.css was already correct) + third
  flourish rule (.sharpee-window-title-bar, verified real at
  engine.css:76) + prose; ch5 "final form is the one the zoo keeps";
  ch15 top-level placement sentence; ch22 factory-function placement;
  ch23 setMaxScore split from the awardScore demonstration shape; ch29
  count-proof "more than a dozen" wording.
- Snippets regenerated (163), em-dash clean, html+pdf rebuilt; run-11
  PDF (1,088,047 bytes) delivered to container confident_clarke twice
  (second copy after David wiped book-test for the run).

## Session Metadata
- **Status**: COMPLETE
- **Blocker**: none (run 11 in David's hands)
