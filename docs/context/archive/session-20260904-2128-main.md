# Session Summary: 2026-09-04 - main

## Goals
- Diagnose the sharpee.net production outage reported on plover (playground/`/play` serving a broken or stale build).
- Catch `main` up with the laptop's post-5.3.0 release work.

## Phase Context
- **Plan**: Close the 44-item publish-readiness punch list (`docs/work/publish-readiness/plan.md`).
- **Phase executed**: None — this session was ad hoc production support, not a plan phase. Phase 6a (P-11) and Phase 8 (P-21) remain PENDING on ADR acceptance, Phase 17/18 remain as the previous session left them.
- **Tool calls used**: 85 (no phase, no budget).
- **Phase outcome**: N/A — no phase entered or advanced.

## Completed

### Session start protocol
Previous-session recap presented, `pre-session-audit` run and relayed verbatim, `docs/core-concepts/README.md` read in full, `project-profile.md` confirmed fresh (2026-09-04), session gate cleared.

### Diagnosed GH #358 — playground serves a Chord 3.0.0 compiler
Root-caused a deadlock in `runManifestStep` (`tools/repokit/src/commands/build.ts:149-150,154`; guard at `tools/repokit/src/commands/manifest.ts:285-292,224`; catch at `tools/repokit/src/cli.ts:81`): the manifest step guards on **existence** of four dist files, not usability; on plover all four exist but are frozen at 2026-08-14, so the step skips its cold-build fallback and `require()`s a stale `packages/world-model/dist/traits/character-model/character-vocabulary.js` whose 14 ADR-318 exports are all `undefined`, and `.length` on the first throws — 3 of ~30 packages in, before `chord` (built 4th) ever compiles. Confirmed the playground/website source is correct for current Chord (`before the game starts`, `playable` per ADR-327 D10, create-block `on…/end on` all current and used by `fernhill.story`/`ides-of-march.story`); the pinned bundle (`website/public/playground/current.json` → 5.0.1) embeds `LANGUAGE_VERSION` 3.0.0 against a current 3.6.0 — no v5.3.0 bundle was ever written. Confirmed `website/deploy.sh`'s three guards (:174-178, :189-194, :196-207) are all warn-only or check the wrong thing, which is why this went unnoticed for three weeks. `website/public/web/fernhill/index.html` (2026-08-15) is served from the same dead build step. Verified the crashed build left the working tree unmutated (`stdlib-manifest.ts` rewritten byte-identically; `character-manifest.ts` untouched — `git diff` empty).

No fix applied — stopped at diagnosis per CLAUDE.md's "never auto-retry failed builds" and platform-change-discussion rules (the fix touches `tools/repokit`, and plover's dist state is a David's-Mac fix). Filed **GitHub issue #358** with four checkboxed proposed fixes (clear stale dist; change the manifest guard from "present" to "usable"; make `deploy.sh` assert the playground version, not the directory; promote the client-build warn to a visible failure): https://github.com/ChicagoDave/sharpee/issues/358

### Caught main up
Fast-forwarded `main` to `914b987c1` (7 commits merged from `feat/adr-321-world-index`: Chord Writer 1.4.0 notarization/release, blog draft, session records).

## Key Decisions

### 1. Stop at diagnosis, file the issue
The repokit deadlock is a `packages/` (build tooling) issue and plover's dist state can only be fixed from David's Mac; per CLAUDE.md, no auto-retry and no platform change without discussion. Diagnosis was carried to full root cause and filed as GH #358 rather than attempting a live fix on the deploy host.

### 2. Leave release-artifact tracking undecided
`website/public/downloads/` (14 loose ChordWriter DMG/zip files) and an untracked `chord-writer/` Sparkle appcast+delta tree were left untracked this session rather than added or ignored — whether the Sparkle feed belongs in git is a call for David, not inferred here.

## Next Phase
No plan phase was entered or advanced this session. `docs/work/publish-readiness/plan.md` is unchanged: Phase 6a (P-11) stays PENDING on ADR-118 Amendment 1, Phase 8 (P-21) stays PENDING on ADR-267 Amendment 1, Phase 17 (P-43) and 18 (P-44) remain as the prior session (15cfa3) left them.

## Open Items

### Short Term
- I-31861b-2: GH #358 — sharpee.net `/playground` and `/play` serve a stale Chord 3.0.0 compiler via a repokit build deadlock on stale-but-present dist; fix is David's, on his Mac.
- I-31861b-1: (carried from session 15cfa3) `release-all.sh`/`package.sh` should treat a notarytool submit without a printed "Successfully uploaded file" as failed and retry immediately instead of polling a dead In-Progress id — recurrence: 5 sessions since 2026-08-10.

### Long Term
- I-31861b-3: Five DRAFT ADR amendments (ADR-118 A1, ADR-267 A1, ADR-320 D2a, ADR-325 Z6, ADR-225 one-input expiry) remain unaccepted; publish-readiness Phase 6a stays PENDING on ADR-118 A1 and Phase 8 stays PENDING on ADR-267 A1.
- I-31861b-4: `website/public/downloads/` untracked DMG/zip files plus the untracked `chord-writer/` Sparkle appcast+delta tree — whether these belong in git is undecided.

## Files Modified

**None** — investigation-only session; no source edits and no commits were made (git pull was a fast-forward, not an authored change). Three untracked website analytics files remain staged for a future commit, not this session's to make:
- `website/src/lib/analytics-event.ts`
- `website/src/lib/analytics-stats.ts`
- `website/src/app/private-stats/page.tsx`

## Notes

**Session duration**: ~25 minutes (2026-09-04 21:28-21:52 UTC).

**Approach**: Read-only forensic diagnosis on the deploy host (plover) cross-referenced against source in this repo, followed by a `git pull` to sync with the laptop's release work. No implementation attempted, per CLAUDE.md's platform-change and no-auto-retry rules.

**Untracked local branch**: `feat/adr-321-world-index` exists locally, tracks `origin/feat/adr-321-world-index`, and holds no commits `main` doesn't already have (its content landed via the `914b987c1` merge). Left in place; harmless.

**Rule 13a scan**: "build" appears in this session's Goals (repokit build deadlock). No REAL-PATH TEST applies — this session made no implementation change to the OWNED build tooling; it diagnosed and filed an issue only. Treated as exempt (investigation-only), consistent with the exemption for documentation/analysis sessions.

**Open items ledger**: this developer's ledger (`docs/context/.open-items.jsonl`) did not exist yet; bootstrapped this session per the one-time carry-forward rule, migrating the one still-open item from session 15cfa3's prose Open Items (the notarytool recurrence fix) and opening three new items for this session's findings.

---

## Session Metadata

- **Session**: 31861b
- **Status**: COMPLETE
- **Blocker**: N/A — this session's own goal (diagnose and document) was completed; GH #358 itself remains open, tracked as I-31861b-2, and is not this session's to fix.
- **Blocker Category**: N/A
- **Estimated Remaining**: N/A
- **Rollback Safety**: safe to revert (no working-tree changes made; fast-forward pull only)

## Dependency/Prerequisite Check

- **Prerequisites met**: SSH/shell access to plover for remote diagnosis; `gh` CLI for filing GH #358; clean fast-forward path from origin/main.
- **Prerequisites discovered**: None blocking.

## Architectural Decisions

- None this session. ADR-327 D10 and ADR-318 were read as evidence during diagnosis, not amended.
- Pattern applied: CLAUDE.md's "never auto-retry failed builds or tests — report and wait" and "platform changes require discussion first."

## Mutation Audit

- Files with state-changing logic modified: none.
- Tests verify actual state mutations: N/A — no code changed this session.

## Recurrence Check

- Similar to past issue? NO — this is the first documented occurrence of a stale-but-present-dist deadlock in `runManifestStep`. Distinct from the unrelated notarytool-upload recurrence already tracked as I-31861b-1 (5 prior sessions, different subsystem).

## Test Coverage Delta

- No test changes this session.

---

**Progressive update**: Session completed 2026-09-04 21:52 UTC

## Activity Log (auto-captured)
```
[21:48:22] GIT: Git operation — git log --oneline -3; echo "=== status ==="; git status --short; echo "=== delet
[21:55:21] EDIT: File written — docs/context/session-20260904-2128-main.md
```
