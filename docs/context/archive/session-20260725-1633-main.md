# Session Summary: 2026-07-25 (1633) - main

**Goal**: Write ADR-271 (Chord grammar compiler pass-through and defects) — first child of the
ADR-266 umbrella, per its sequencing (ships first, independent of direction (iv)).

**Status**: IN PROGRESS — ADR-271 ACCEPTED (Q-10 ruled: narrow the docs; adr-review 14/14 READY).
Implementation plan written (`docs/work/grammar-parity/plan.md`, plan-review clean). Phases 1-2 DONE,
Phase 3 in progress.

**Files modified**:
- `docs/architecture/adrs/adr-271-chord-grammar-compiler-pass-through.md` (new, ACCEPTED)
- `docs/work/grammar-parity/plan.md` (new — 5 phases)
- Phase 1: `packages/chord/src/{catalog,analyzer,ir,parser,index}.ts` + `tests/scope-requirements.test.ts` (chord suite 545/545)
- Phase 2: `packages/if-domain/src/grammar/{grammar-builder,grammar-engine}.ts` + `tests/grammar/grammar-builder.test.ts` (if-domain 95/95)
- Phase 3: `packages/story-loader/src/loader.ts` (extendParser rewrite + applyScopePredicate), `tests/helpers/grammar-harness.ts` (new), `tests/scope-constraint-emission.test.ts` (new), `tests/bare-verb-dispatch.test.ts` (harness swap)

## Work done

- Re-verified every code fact from ADR-266's allocation before writing:
  - Constraint pipeline: `parser.ts:2438-2456` → `analyzer.ts:1242-1250` (slot name only) →
    `ir.ts:461` → no consumer in `loader.ts` extendParser (1102-1137).
  - Loader's narrowed cast declares only define/mapsTo/withPriority (`loader.ts:1103-1107`).
  - `forAction()` is sugar that flattens verbs × templates to `.define()` calls
    (`grammar-engine.ts:344-415`); **patterns without verbs emit nothing** — Chord's complete
    pattern lines don't fit the cross-product, which drove D3's `fullPattern()` decision.
  - Engine scope-gating machinery exists (`grammar-scope-resolver.ts` evaluates
    touchable/carried/visible) but the standard grammar has **0 `.where()` call sites** — Chord
    stories become its first production consumer.
  - Five live `must be reachable` lines in shipped stories (fernhill ×3, friendly-zoo ×2), all
    currently no-ops; `cloak.story:82` is the one working `define verb`.
  - EBNF: requirement is syntactically WORD (`chord.ebnf:400`) — closed-set validation is semantic,
    no ADR-257 bump.

## Key decisions (in the ADR, pending acceptance)

- D1: closed requirement set `{reachable→touchable, visible→visible, held→carried}` owned by chord
  pkg, one exported table, analyzer error `analysis.unknown-requirement`, exhaustive loader map.
- D3: additive `ActionGrammarBuilder.fullPattern()` (if-domain platform change) — rejected
  verb-factoring heuristic and status-quo flat emission.
- D4: Q-10 recommended route written out — narrow define-verb docs; general aliasing deferred to
  ADR-270.
- D5: docs-examples-load test reads the two MDX pages' chord fences at test time.
- D6: the five live constraint lines change behavior by design; fernhill/friendly-zoo transcripts
  are the regression gate, diffs reviewed individually.

## Phase 4 finding — ADR-273 split (owner ruling: option 3)

First end-to-end run (fernhill units, 578/593) exposed a pre-existing platform defect: parse-time
scope gating has NEVER worked. `GrammarScopeResolver` (parser-en-us) calls
`world.getTouchableEntities(actorId, currentLocation)` / `getVisibleEntities` / `getCarriedEntities`
— none exist on WorldModel (real surface: `getVisible(observerId)`, `getInScope`,
`getCarriedAndWorn`, `getAllEntities`). Guards fail closed → zero candidates → every `.where()`-gated
command gets "You can't see any such thing." Verified with probe story + PARSER_DEBUG. Also: no
parse-time reachability computation exists anywhere (world-model's own ScopeEvaluator: "touchable =
visible for now").

→ `docs/architecture/adrs/adr-273-grammar-scope-resolver-world-api.md` (DRAFT): resolver adapts to
the real WorldModel surface (D1), base mapping (D2), no silent fail-closed (D3). Open: Q-1 — what
does `touchable` mean at parse time (visible-for-now vs getInScope vs real reachability); Q-2 — where
the darkness ruling is recorded for authors. ADR-271 acceptance items 1+6 blocked on it (items 2, 3
discharged; 4, 5 = Phase 5, independent).

## ADR-273 interview + acceptance (same session)

Q-1 ruled: **build real reachability** — reachability graduates to world-model as
`ReachabilityBehavior` (sibling of VisibilityBehavior; closes the sibling-op asymmetry: stdlib's
canSee delegates to world-model, canReach never did). WorldModel gains `canReach`/`getReachable`;
semantics ported unchanged from stdlib's `ScopeResolver.canReach` (incl. sight precondition —
darkness refuses); stdlib delegates; parse gate == validate semantics. Q-2 ruled: author-facing
semantics line lands on the define-action page via ADR-272 (inherited obligation). Reviewed 14/14
READY; SMALL finding (one-definition discipline for ScopeEvaluator's touchable=visible stub) folded
into D4. **ACCEPTED.**

## ADR-273 implemented (4 phases, docs/work/scope-reachability/plan.md — all DONE)

1. ReachabilityBehavior (world-model, sibling of VisibilityBehavior; rules ported from stdlib
   canReach incl. sight precondition) + WorldModel.canReach/getReachable (+ AuthorModel delegation);
   ScopeEvaluator.getTouchableEntities delegates (one-definition). 10 tests; suite 1432/1432.
2. stdlib ScopeResolver.canReach → one-line delegation; suite identical pre/post (111 files, 1576).
3. GrammarScopeResolver rewritten to the real surface + D3 warnDegraded; 7 per-base tests vs real
   WorldModel; FIVE fictional mocks migrated (story-grammar.test.ts was a fifth the grep missed).
4. E2E: probe passes both ways; fernhill 18/18 + wt 76/76; friendly-zoo 76/76 + wt 56/56.

**Fourth discovery (owner-approved fix)**: findEntitiesByName had no article handling — `pet the
goat` failed while `pet goat` passed. Fixed: leading the|a|an stripped, original-text-first matching
(entity named "The Grail" wins exact before stripping); known limit: my/that/some determiners
unhandled (fuller fix = determiner-tagged token stripping in the slot consumer).

**Fifth discovery (pre-existing, out of scope, workaround only)**: the bundled CLI cannot
cold-transpile hatched stories — devkit hatch-transpile.ts (ADR-259 D6 amendment 2026-07-23) calls
esbuild.buildSync whose inlined-in-bundle sync worker never answers (Atomics.wait, 0% CPU); masked
until the $TMPDIR/sharpee-hatch cache purged (~3-day macOS cleanup). Warmed via
scratchpad/warm-hatch-cache.js (byte-identical cache key). **ADR-274 candidate**: esbuild external
to the CLI bundle (devkit/repokit owned). Affects every hatched story on any fresh machine.

## ADR-271 completed (grammar-parity plan — all 5 phases DONE)

- Phase 4: refusal-proof transcript committed
  (stories/friendly-zoo/tests/transcripts/scope-constraint-gating.transcript, 5/5). Transcript
  format gotcha recorded: header = bare key:value lines closed by ONE `---` (no leading `---`).
- Phase 5: docs-examples-load test written FIRST (failed on sniff pre-edit; also surfaced that the
  petting example needs `use scoring` — harness provides it as presupposed story config);
  define-verb page narrowed per D4/Q-10; LoadError states the same limit sentence (acceptance 5).
- **All six ADR-271 acceptance items discharged; all six ADR-273 acceptance items discharged.**

## Next steps

1. Owner: is the bundle cold-transpile defect ADR-worthy (ADR-274)? Recorded in both plans + here.
2. ADR-272 inherits the one-line `reachable` semantics statement for the define-action page (D5).
3. Next children per ADR-266 sequencing: ADR-267 (constructs) and ADR-268 (ordering experiment).
4. Uncommitted: all of the above — chord, if-domain, story-loader, world-model, stdlib,
   parser-en-us, website, stories/friendly-zoo, docs.
