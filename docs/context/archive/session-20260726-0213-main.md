# Session Summary: 2026-07-26 - main [f9e069]

**Goal**: Draft ADR-269 — the standard grammar as Chord source (umbrella ADR-266 child;
inherits Q-12, Q-13, Q-16; owes acceptance items 9–14; consumes ADR-268's ordering model
and its 29 file-order-sensitive pairs as migration constraints).

**Status**: COMPLETE — ADR-269 drafted, interviewed (4 rulings), reviewed 16/16,
ACCEPTED, and **IMPLEMENTED same session** (all five phases + D5 deletions). Not yet
committed.

## Work done

- Pre-session audit clean (type check green, no blockers; `checkpoints/after-leaflet.json`
  drift explained as a build/test byproduct — 44 prior commits show the identical shape).
- Grounding reads: ADR-266 (umbrella, full), ADR-267/268 (full), ADR-271/273/274/275;
  grammar-parity Part A2/C/D; pairs.json inspected (65 AGREE / 29 TIE / 0 REVERSE).
- Two code explorations (loader/file-kind; load path/build pipeline). Load-bearing facts:
  - `defineGrammar` runs per `new EnglishParser` (CLI boot, browser handleStart, ADR-248
    restart, zifmia per-invocation); startup cost unmeasured anywhere.
  - ADR-210 direction rule (`bundle-entry.js:102-105`): no platform package may depend on
    chord/story-loader — blocks naive parse-at-boot; repo precedent is data-as-code.
  - Chord is dotless (ADR-254); `chord.action.<name>` hardcoded (`loader.ts:1112`); no
    id-override on IRActionDef → target-naming form needed for `if.action.*`.
  - A define-action-only file compiles and loads TODAY (verified by execution);
    EBNF header optional; `.chord` = import fragments only.
  - ADR-265 apparatus still live (57-file set, generator script, reference-only refusal
    at loader.ts:255-261, pinning test) — deletion decision recorded as D5.
  - Rule ids nondeterministic (grammar-engine.ts:131) → equivalence compares shape.
- **ADR-269 DRAFT written**: D1 migration generator (action-first, order-topological over
  29 TIE pairs + 3 LOAD-BEARING sites, no ordering annotations), D2 counted shape
  equivalence with individually-ruled divergences, D3 standard tier / no story-dispatch
  conveniences, D4 grammar-only (no behavior), D5 ADR-265 apparatus deletion, D6
  measure-before-ruling startup cost. Open: Q-1 load path (rec: build-time generation),
  Q-2 file kind, Q-3 locales, Q-4 target-naming (rec: name derivation scoped to file kind).

## Key decisions

- ADR-269 interview (all four ruled by David, each the presented recommendation):
  - **Q-1 → D7**: build-time generation — repokit step compiles the Chord source into a
    committed, freshness-gated generated module (keeps `defineGrammar` export) replacing
    grammar.ts; zero startup delta; ADR-210 untouched.
  - **Q-2 → D8**: distinct top-level `grammar` header; analyzer grammar-file mode
    (grammar-only gates); EBNF addition + ADR-257 bump + changes row.
  - **Q-3 → D9**: locale-generic mechanism, per-locale source in parser-{locale};
    direction not code (en-US only instance).
  - **Q-4 → D10**: id binding by name derivation in grammar-file mode
    (`define action taking` → `if.action.taking`); build step validates names against
    stdlib's id set (chord stays stdlib-ignorant); story files still mint chord.action.*.
- adr-review: 11/16 → five SMALL findings folded (Modules list, export contract, id-set
  ownership, freshness gate, acceptance 9 rejection-diagnostics tests) → 16/16.

## Implementation progress (begun on David's "begin"; ADR flipped ACCEPTED)

- Plan: `docs/work/standard-grammar-chord-source/plan.md` (.current-plan pointer updated).
- **Phase 0 COMPLETE** — D6 baseline: 422 registrations ≈ 2.0 ms cold / 0.3 ms warm;
  CLI whole-boot 0.08 s (`docs/work/standard-grammar-chord-source/measurements.md`).
- **Phase 1 COMPLETE** — chord grammar-file kind: `grammar "<name>"` header (ast/parser:
  `parse.grammar-name`, `parse.grammar-header-body`, `parse.duplicate-grammar-header`,
  `parse.mixed-headers`); analyzer `checkGrammarFileMode` (`analysis.grammar-file-behavior`
  per category, `analysis.grammar-file-declaration`; imports legal); IR additive
  `grammarFile: { name }`; CHORD_LANGUAGE_VERSION 2.3.0 → **2.4.0**; chord.ebnf
  grammar-file/grammar-header productions; language-version pin re-recorded
  (sha 1aacd8ba…); chord-grammar-changes.md row appended (approval: ADR-269 ACCEPTED
  2026-07-26 f9e069). Tests: new `packages/chord/tests/grammar-file.test.ts` (14),
  full chord suite 595/595 (10 golden AST snapshots re-recorded on David's OK —
  additive `grammarHeader: null` field); root tsc clean.
- **Phase 2/3 facts settled**: chord builds 4th, parser-en-us 13th, stdlib 16th →
  build step uses chord dist but must read stdlib action ids from SOURCE
  (`packages/stdlib/src/actions/constants.ts`, IFActions block, regex-as-data);
  story-loader dist also stale at grammar-step time → emitter owns IR→pattern-string
  conversion (mirrors loader.ts emission semantics, standard-tier, no bare-verb/dispatch
  conveniences). Working order re-sequenced: generator (3a) → emitter (2) → equivalence
  harness (3b) → swap (4).

## Phases 2–4 (COMPLETE, same session)

- **Trace ruling (David)**: 12 platform-side TS exception rules — `?` → help (prior
  ruling) + 11 `trace …` → author.trace (author/debug tooling). New
  `parser-en-us/src/platform-grammar.ts`; english-parser calls
  `definePlatformGrammar` after `defineGrammar`.
- **Generator** (`scripts/generate-standard-grammar-chord.cjs`, one-shot): emitted
  `packages/parser-en-us/grammar/standard-en-us.story` — 483 lines, 55 blocks,
  410 rules; going → one `directions` block (144 rules ⇒ ~20 lines); pushing stays
  literal lines (its 24 direction rules carry NO defaults — divergence avoided);
  hiding → `means position` lines; LOAD-BEARING comments carried (`##`, blank-line
  rule ADR-249); topo order over 29 TIE pairs + wearing/exiting edges.
- **Equivalence** (`docs/work/standard-grammar-chord-source/equivalence-check.cjs` +
  `post-swap-check.cjs` vs frozen `rules-baseline.json`): ZERO divergences, both
  directions; 422 = 410 + 12; 29 pair-order checks green pre- and post-swap.
- **repokit grammar step** (`tools/repokit/src/commands/grammar.ts` + cli + build loop
  before parser-en-us + verify gate): compile → D10 name validation (ids
  regex-extracted from stdlib constants SOURCE — stdlib builds after parser-en-us) →
  emit generated `src/grammar.ts` (flat `.define()` table, keeps `defineGrammar`
  export); `--check` freshness gate; 8 tests in `grammar.test.ts` (incl. acceptance-9
  D10 error + real-repo freshness gate).
- **Swap landed**: grammar.ts = generated 433 lines (was 1035 hand-written).
  parser-en-us 285 green (sync tests incl.), story-loader 399, devkit 83/84,
  repokit 40, chord 595. Corpus: dungeo units 1788 + chain 883 ALL PASS (runs 1-2
  failed 564/924 = thief-RNG steal cascade, known accepted class; run 3 clean =
  one-good-run baseline); fernhill units+wt76, friendly-zoo, cloak-of-darkness,
  nautical 7 (ship directions); go-out-exiting 7/7. Root tsc clean.
- **Measurements post-swap**: identical (2.0 ms cold; CLI 0.07 s) — zero regression,
  acceptance 7 recorded in measurements.md.
- **Docs**: parser-en-us/CLAUDE.md rewritten (grammar.ts GENERATED; edit the .story;
  repokit grammar; platform exceptions; order semantics). ADR-269 Session carries the
  implementation addendum.
- **Pre-existing finding (not mine, surfaced)**: `stories/thealderman` fails tsc on
  `CharacterBuilder.topic` ×6 (character-package API drift; unrelated to grammar).

## Phase 5 (COMPLETE — David confirmed "phase 5")

- Deleted: `docs/reference/stdlib-chord/` (57 files), `scripts/generate-stdlib-chord.js`,
  loader `reference-only` refusal (loader.ts), `stdlib-chord-reference.test.ts` +
  `reference-only.test.ts`, repokit `generateStdlibChord()` + its build call.
- `website/.../stdlib/reference/content.mdx`: content kept (umbrella D9), header
  changed to a frozen-provenance note (ADR-272 rederives from the Chord source).
- Post-deletion: story-loader rebuilt, 392/392 (−7 = the two deleted test files);
  repokit rebuilt, 40 green; root tsc clean; bundle rebuilt; go-out-exiting +
  fernhill sanity green; source-tree reference sweep EMPTY.
- **ADR-269 flipped IMPLEMENTED** (status + implementation addendum in the ADR).

## Open questions parked for David (not blocking)

- `scripts/chord-gap-report.cjs` and `scripts/generate-standard-grammar-chord.cjs`:
  both are migration-lifetime instruments (umbrella D5/D7 say they retire with the
  migration). Left in place — deletion not confirmed; flag for a follow-up ruling.
- `stories/thealderman` pre-existing tsc failure (`CharacterBuilder.topic` ×6) —
  character-package API drift, unrelated to grammar.

## Next steps

1. Commit (on go-ahead — not yet committed).
2. Remaining umbrella children: ADR-270 (author alteration model), ADR-272 (docs
   surfaces — content.mdx grammar blocks + define-action capability page).
