# Session Summary: 2026-06-18 (05:10 CST) — feat/adr-180-phase3a-build-bundle

## Goals
- ADR-180 Phase 3a: `devkit build` + `devkit bundle` (CLI bundle target), byte-for-byte
  parity with build.sh, gated by a reusable parity harness. build.sh stays (coexists until
  the 3d cutover).

## Context
- Phase 2 (`devkit test:npm`) merged to main (PR #121). Phase 3 plan:
  docs/work/sharpee-devkit/plan-20260618-phase3-build-replacement.md (decomposed 3a–3d).
- Branch: feat/adr-180-phase3a-build-bundle.

## Decisions (confirmed with David)
- Start 3a now; ship as its own PR; 3b/3c/3d follow.
- AC-5 parity = **byte-identical with frozen stamps** (not normalized/functional).

## Completed
- **`devkit build [story]`** (`commands/build.ts`): version stamping (`stampVersions`),
  ordered platform build via `pnpm --filter` (`buildPlatform`, with dist/index.js existence
  assertion — precludes the .tsbuildinfo silent-no-op class), genai-api gen, story build
  (`buildStory`, resolve_story_pkg semantics), then the bundle. Flags: `--skip`, `--version`,
  `--build-date`, `--no-version`, `--no-genai`, `--no-bundle`, `--esm`.
- **`devkit bundle`** (`commands/bundle.ts`): runs the verbatim build.sh esbuild command
  (18 aliases, same flag order) → `dist/cli/sharpee.js` + hand-written `sharpee.d.ts`;
  asserts the artifact is non-empty.
- **`repo.ts`**: PLATFORM_PACKAGES (27, verbatim order), BUNDLE_ALIASES (18), BUNDLE_DTS,
  findRepoRoot (walks to pnpm-workspace.yaml), resolveStoryDir, storyVersionFile.
- **build.sh**: BUILD_DATE injectable via `BUILD_DATE_OVERRIDE` (enables deterministic parity).
- **Parity harness** `packages/devkit/scripts/parity-cli-bundle.sh` — the AC-5 gate; freezes
  version+date, runs build.sh then devkit, byte-diffs sharpee.js / sharpee.d.ts /
  dungeo version.ts / sharpee package.json. Run before deleting build.sh (3d).
- Tests: +6 (`build.test.ts`: stampVersions byte-exact format, tutorials-not-stamped quirk,
  --no-version, findRepoRoot, resolveStoryDir). devkit suite: **15 passing, 1 skipped**, GREEN.

## Parity results (byte-identical)
- `devkit bundle` reproduced build.sh's `dist/cli/sharpee.js` — **same sha256**.
- `stampVersions` reproduced build.sh's `stories/dungeo/src/version.ts` — **git diff IDENTICAL**.
- **Full harness (two real full builds): PARITY PASS** on all 4 artifacts.

## Notes
- version.ts comment still reads "Auto-generated by build.sh" (GENERATOR const) — kept verbatim
  for byte-parity; flips to "devkit" at the 3d cutover.
- genai-api regen + version.ts stamping discarded post-build (build side effects, per prior practice).

## Open / next
- 3b — browser target (`--browser` → dist/web/{story}/, AC-9). 3c — zifmia. 3d — cutover
  (delete build.sh + dead scripts; `devkit clean`/`verify`; umbrella wiring; AC-8 doc sweep).

## Status
- **Status**: COMPLETE (3a) — build/bundle implemented, 15 tests GREEN, byte-parity gate PASS.
