# Session Summary: 2026-08-15 - feat/adr-310-318-implementation

## Status: COMPLETE

## Goals
- Execute Phase 7 of the ADR-310/318 plan (David's go-ahead at session start): acceptance closure — ADR-310 AC8 (D12 channel isolation in a built story) and AC9 (Dungeo/Fernhill byte-identical regression), ADR-318 AC8 (cost regression), AC-by-AC coverage audit carried from Phase 6, AC2 frozen-fixture confirmation, IDE author-channel surface polish, `tsf build --npm` regression across touched packages.

## Completed
- **AC-by-AC acceptance audit** (`docs/work/adr-310/acceptance-audit.md`): ADR-310 AC1/4/6/7 + ADR-318 AC5/6/7 discharged with cited evidence; owed items enumerated. Two findings beyond the expected deliverables: (1) 310-AC2/318-AC1 need a purpose-built frozen fixture story that doesn't exist; (2) 310-AC3's D16 clause (conversation suppresses goal pursuit) was an implementation gap, not a test gap.
- **D16 suspension gap CLOSED** (David: "fix the interrupted conversation gap"): trait-riding `activeConversation` marker (D17-serialized), `conversation-marker.ts` helpers, both dialogue surfaces stamp (chord topic arm postReport — row hits/evasions/misses alike; selector socket on handled deliveries), goal sub-step skips step execution while fresh (activation still re-evaluates), freshness = lifecycle neutral decay threshold through the clock seam. `CHARACTER_TURN_KEY` + new `dialogueTurn(world)` relocated to `character-clock.ts` (tick-phases re-exports; story-loader's private helper delegates). 11 new tests, all on trait state; mutation-verification clean; character 405 / world-model 1479 / story-loader 495 passing; per-package tsc clean (2026-08-15). contracts.md §2/§2.1 amended.
- **Gap-closure design write-up** (David: "clean fix, not a hack") at `docs/work/adr-310/gap-closure-design.md`: G1 (done, the template), G2 fixture story with import-shared deletion variants, G3 author-channel isolation — REAL defect found: `character` channel is ungated so every client receives author rows; fix = `authorChannels` capability flag + `gatedBy` (needs David's sign-off), G4 318-AC3 legs + proposed ADR amendment (forcing names the wrong mechanism; deposits are deterministic), G5 three composition tests, G6/G7 measurement procedures.

## Key Decisions
- D16 "in conversation" on the chord path = trait-riding marker stamped at the dialogue surfaces, NOT a wired ConversationLifecycle (module state violates D17) and NOT a goal-manager heuristic (second clock).
- An evasion or row-miss still refreshes the conversation window (the exchange is a conversation in progress even when the default reply stands).

- **`authors:` required + indented-list-only (David's directive)**: parser change (`parse.header-inline-list` on inline form, `parse.story-authors` when absent — one mistake, one diagnostic; `testers:` unchanged); ADR-298 amended (D1-A2). Repo-wide sweep: all .story files (incl. devkit `story.story.template`, IDE frozen fernhill fixture), all test sources (template-literal, escaped-string, array-line styles), ~37 line-number pins bumped, 10 snapshots + lexer golden regenerated (UPDATE_GOLDEN=1). **Second real defect found and fixed**: devkit `story-config` header splicer broke on indented list values — spliced `ifid:` between `authors:` and its names and stopped scanning at the first name line; both scanners now treat deeper-indented non-blank lines as field continuations. Verification 2026-08-15: chord 836, story-loader 495, character 405, devkit 167, bootstrap 42, repokit 80 passing; root tsc clean; turbo test:ci 65/65; bundle rebuilt — thealderman 53 and Dungeo chain 952 passing.

- **G3 / 310-AC8 CLOSED (David's sign-off)**: `authorChannels` capability flag (if-domain) + `gatedBy` on the `character` channel (stdlib); false in all player profiles (bootstrap/engine/platform-browser); chord `CLIENT_CAPABILITY_FLAGS` + `author-channels` (loader drift gate caught the parity); wire-level tests both directions (bootstrap negative on raw `channel:packet`, engine manifest filter, stdlib gate pin). Engine 628, bootstrap 43, turbo 65/65, bundle transcripts green (thealderman 53, Dungeo 952).

- **G2 / 310-AC2 + 318-AC1 CLOSED**: `stories/character-acceptance/` frozen fixture (Witness: confided secret + principle + duty-over-fear + panicked/calm phrasebook pair) + sed-generated deletion variants (`b1-no-principle`, `b1-no-temperament` — literal one-line diffs) + 3 transcripts, 15 steps, all passing via the bundle. Building it exposed and fixed TWO platform defects: (a) D3 `change mood`/`change feeling` compiled but silently dropped — `execCharacterTransition` added (runtime statement switch + `RoutingDecision.transition` ledger field + `moodAxesFor` evaluator accessor; loud LoadError on model-less owner); (b) `DefaultStateTransitions` keyed dead event names — rekeyed to `if.event.attacked/given/taken`, making observation live in play. `character-transitions.test.ts` (2, real attackingAction, trait-state asserts); 4 test files' synthetic events renamed. Mutation-verification: 2 warnings (rekeyed `if.event.attacked`/`if.event.taken` observer rows lacked state-asserting tests) — closed same session with +2 `character-observer.test.ts` cases (36 passing) asserting `threatValue`/mood axes and `getDispositionValue`. Verification: stdlib 1616, story-loader 497, character 405, turbo 65/65, tsc clean, thealderman 53 + Dungeo 952 unchanged.

## Open Items
- Carried: stale plans `adr-280-chord-writer-project-model` + `live-derived-state` undispositioned; 23 stranded event logs; ADR-location split (4 dirs) undecided.
- Noted (not fixed): interceptor effect envelope re-mints events as {type, payload}, dropping `entities.actor` — chord-path author rows carry npcId: undefined in the character channel (selector-path rows attribute fine); the CLI banner prints `description:` despite ADR-298 D3's "never emitted in play"; observeEvent's generic witness rule records raw event types (e.g. `npc.character.mood_changed`) as knowledge topics.
- **For the IDE session**: Swift `StoryHeaderIFID` (story-config's twin) has the same indented-list continuation bug; the lexer golden changed (Swift ChordLexer port must be re-validated per ADR-258 D7); docs-tab HTML pages still show inline `authors:` examples; the frozen fernhill fixture was updated to the new form.
- Left as-is deliberately: docs/book v1.5.0/v2.0.0 snippets (version-pinned book releases document their own language version); docs/unofficial (junk mail); branch-stories/fernhill dist copy (regenerates); genai-api docs (regenerate with the tsf --npm pass).
- Pending David: G3 `authorChannels` capability-flag sign-off; ADR-318 AC3 forcing-clause amendment ruling.

## Files Modified
238 changed + 8 new (the `authors:` sweep dominates the count). By group:

- **Docs** (7): plan.md (Phase 7 CURRENT); NEW `docs/work/adr-310/acceptance-audit.md` + `gap-closure-design.md`; contracts.md (§2 D16 amendment, §2.1 clock-seam relocation); ADR-298 (D1-A2 amendment); ifwiki chord-page; this session file (new).
- **D16 suspension**: world-model `characterModelTrait.ts` (+`ActiveConversation`) + leaf barrel; character `conversation/conversation-marker.ts` (NEW), `character-clock.ts` (turn key + `dialogueTurn`), `tick-phases.ts`, `conversation/selector.ts`, barrels; story-loader `runtime.ts` (marker stamp); +5 test files (11 tests).
- **`authors:` required/indented (David's directive)**: chord `parser.ts`/`ast.ts`; ~200 test/fixture/story files swept (3 encodings), ~37 line pins, 10 snapshots + lexer golden regenerated; devkit `story.story.template`; devkit `story-config.ts` splicer defect FIXED (indented-list continuations); all 9 real stories + IDE frozen fernhill fixture.
- **G3 isolation**: if-domain `types.ts` (`authorChannels`); stdlib `character-author.ts` (`gatedBy`); bootstrap/engine/platform-browser profiles; chord `catalog.ts` (`author-channels`); bootstrap + engine + stdlib channel tests.
- **G2 fixture + 2 defects**: NEW `stories/character-acceptance/` (3 stories, 3 transcripts, 15 steps); story-loader `runtime.ts` (`execCharacterTransition`), `evaluator.ts` (`moodAxesFor`), `decisions.ts` (+`transition`); stdlib `character-observer.ts` (rules rekeyed to real events); NEW `character-transitions.test.ts`; +2 observer tests; 4 test files' synthetic event names updated.

## Notes
- Session started: 2026-08-15 19:51 CDT (session 0f32bb). Finalized same evening.
- Phase 7 progress: AC-by-AC audit written; 310-AC2/AC3/AC8 + 318-AC1 newly DISCHARGED (with 310-AC1/4/6/7 + 318-AC5/6/7 already discharged by construction). **Remaining for Phase 7**: G5 composition tests (318-AC2 ordering flip, 310-AC5 propagation→dialogue, 318-AC4 third-NPC hop), 318-AC3 remaining legs (strained voice; seek-out — now unblocked; David's forcing-amendment ruling pending), G6 cost regression, G7 Dungeo/Fernhill byte-compare vs main, IDE author-channel panel polish, `tsf build --npm` regression.
- Four platform defects found and fixed this session, all surfaced by doc-writing or the fixture story: ungated character channel, dead observer rule keys, unexecuted D3 transitions, story-config splicer breaking on indented lists.
