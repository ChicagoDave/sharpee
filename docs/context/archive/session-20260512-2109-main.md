# Session Summary: 2026-05-12 - main

## Goals
- Implement ADR-177 multi-user server (Zifmia) phases 0 through 7 in a single continuous session.
- Apply 5 advisory fixes to `adr-177-multiuser-corrected.md` identified during pre-implementation review.
- Satisfy CLAUDE.md rule 13a for the engine integration phase with a REAL-PATH test against `dist/stories/dungeo.sharpee`.

## Phase Context
- **Plan**: `docs/work/multiuser/plan-20260512-adr-177.md` (written in Phase 0 of this session)
- **Phase executed**: Phases 0 – 7 — "Identity → Rooms/Governance → WS Layer → Succession → Persistence → Engine Integration → Delete/Mute/DMs → Full Client UI"
- **Tool calls used**: 568 / (no per-phase budget set)
- **Phase outcome**: Phases 0–7 completed; Phase 8 (Playwright E2E) deferred to next session

## Completed

### ADR-177 Advisory Fixes
Five clarifications applied to `adr-177-multiuser-corrected.md`: `participants.room_id` FK target made explicit; `recording_notice` source column name corrected; AC-10 heartbeat interval widened to 400 ms / 200 ms; AC-11 reconnect lookup path made explicit; AC-12 (mute enforcement) and AC-13 (DM tier gate) added as new acceptance criteria.

### Phase 0 — Scaffold + Plan
`tools/zifmia/` directory created and registered in `pnpm-workspace.yaml`; `package.json`, `tsconfig.json`, `vitest.config.ts`, and `src/index.ts` stubs written. `docs/work/multiuser/plan-20260512-adr-177.md` written sequencing all 8 phases with tier and tool-call budget per phase. `-c zifmia` build flag stubbed in `build.sh`.

### Phase 1 — Identity
`db/schema.ts` (`identities` table), `identity/validation.ts` (`validateHandle`), `identity/repository.ts` (`IdentityRepository`), `identity/routes.ts` (`POST /api/identities`, `POST /api/identities/erase`), `client/identity-storage.ts` (localStorage helper). 18 REAL-PATH tests covering AC-1, AC-9, and handle-validation rules.

### Phase 2 — Rooms, Participants, Governance
`rooms` + `participants` tables with FK `ON DELETE CASCADE`; `rooms/join-code.ts` (Crockford-32, collision-retry); `rooms/repository.ts`, `rooms/participants.ts`, `rooms/tier-gate.ts` (`loadCaller`), `rooms/types.ts`; all governance routes (`/join`, `/rename`, `/pin`, `/nominate-successor`, `/promote`, `/demote`); `stories/scanner.ts` (`StoryScanner`) + `stories/routes.ts` (`GET /api/stories`); `rooms/state-routes.ts` (`GET /api/code/:join_code`); `-c zifmia` function + case clause in `build.sh` made functional. 30 REAL-PATH tests (AC-3, AC-4, tier matrix, join-code uniqueness).

### Phase 3 — WebSocket Layer
`ws/types.ts` (frame shapes + close codes 4001–4006); `ws/lock.ts` (`RoomLock`, 400 ms expiry / 200 ms heartbeat per AC-10); `ws/rooms-hub.ts` (`RoomsHub`: broadcast registry, presence tracking, lock ownership); `ws/handler.ts` (hello-first protocol, close codes); `turns/command-router.ts` (`EchoCommandRouter` scaffolding); `turns/routes.ts` (`POST /command`, `POST /force-release`); governance routes updated to broadcast `role_change`. 26 REAL-PATH WS tests (AC-6, AC-10, AC-11).

### Phase 4 — Succession + Recycle
`succession/service.ts` (`SuccessionService`: grace timer, cascading PH succession via transactional tier flip + `primary_host_id` swap); `succession/recycle.ts` (`RecycleSweeper`, 14-day idle sweep); `RoomsHub` gained `onPresence` listener subscription. 7 REAL-PATH tests (AC-5: cascading succession against a real WS connection — grace timer, chain, chain-dies, grace-cancellation).

### Phase 5a — Persistence Layer
`sessions/types.ts` + `sessions/events-repo.ts` (`session_events` table + repo); `saves` table (forward-compat schema); `session_events` appended on chat, command, role_change, lifecycle, join, nominate, promote, demote, succession; `rooms/state-routes.ts` extended with `GET /api/rooms/:id/state` (`RoomStateResponse` per ADR-177 §4); identity-erase wired to `hub.closeForIdentity(4007)` (AC-9 re-verification). Latent SQL ordering bug fixed: secondary sort on `rowid` to preserve insertion order when timestamps tie. 15 REAL-PATH tests.

### Phase 5b — Engine Integration
Workspace deps added: `@sharpee/engine`, `@sharpee/world-model`, `@sharpee/parser-en-us`, `@sharpee/lang-en-us`, `@sharpee/stdlib`, `@sharpee/text-blocks`, `@sharpee/core`, `@sharpee/if-domain`, `@sharpee/media`, `fflate`. Built: `engine/bundle-loader.ts` (extracts `.sharpee` zip, dynamic imports via package-local cache), `engine/turn-executor.ts` (`executeTurnAgainstStory`: fresh engine per turn, restore via hooks, captures channel:packet + ISaveData), `engine/room-state-repo.ts` (per-room gzip+JSON ISaveData blob), `engine/saves-repo.ts` + `engine/saves-routes.ts` (named saves, restore, `room_restored` WS broadcast), `engine/engine-router.ts` (replaces echo router when `useEngineRouter: true`), `engine/manifest-cache.ts` (lazy `CmgtPacket` cache per slug, surfaced at `GET /state.cmgt`), `engine/story-health.ts` + `withHealthFilter`. 50-turn save→divergent→restore→correct-world-state test PASSED against real `dist/stories/dungeo.sharpee`. `@sharpee/media` workspace dep was the bug fix that unlocked ESM resolution under vite-node. 10 REAL-PATH tests (6 engine integration + 4 story health).

### Phase 6 — Delete, Mute, DMs, Recording Notice
`config/repo.ts` (`ConfigRepository`, seeded `recording_notice`); `GET /state.recording_notice` reads from config. `mute_state` + `dm:message` WS frame types added (small ADR §3 extension — ADR markdown not yet updated). `POST /api/rooms/:id/delete` (PH-only, title-confirmation gate, closes sockets with 4004). `POST /api/rooms/:id/mute` (PH/CoHost; enforces "cannot mute PH" and "CoHost cannot mute peer CoHost"; AC-12 enforcement in `ws/handler.ts`). `POST /api/rooms/:id/dm` (PH/CoHost only); `broadcastWhere` hub helper. Latent bug fixed in `tests/ws-helpers.ts`: `recvWhere` and the global message listener both consumed frames, duplicating non-matching frames into the queue — refactored to single global dispatcher with predicate-waiter priority. 18 REAL-PATH tests (AC-8, AC-12, AC-13, delete).

### Phase 7 — Full Client UI
`tools/zifmia/web/` created: Vite + framework-free TypeScript per ADR-170. `web/src/http-client.ts` (typed wrappers for all routes); `web/src/ws-client.ts` (hello + backoff reconnect + terminal-code handling); `web/src/store/room-state.ts` (pure observable state mirror); `web/src/identity-store.ts`; `web/src/routing.ts` (hash router: `#/` + `#/room/:id`); `web/src/main.ts`. Views: `views/identity.ts`, `views/lobby.ts`, `views/room.ts`. Components: `recording-indicator`, `participant-roster` (inline mute/promote/demote/successor actions), `channel-renderer-host` (text-shape default, author-customizable per ADR-163), `chat-panel` (mute-aware), `dm-panel` (PH/CoHost only), `command-input` (lock-on-typing, 200 ms heartbeat), `settings-panel` (rename/pin/force-release/save/restore/delete with title-confirm), `grace-banner` (live countdown), `room-closed-overlay` (close-code → human reason). Fastify static-serve wired at `/`. Build output: ~26 KB JS / ~8 KB gzipped. 22 new tests (12 state-store unit + 10 happy-dom component DOM-mutation). **Phase 7 exit gate (two-browser hand-test) requires user verification — UI was not visually tested in a browser this session.**

## Key Decisions

### 1. Phase 5 Split Into 5a and 5b
User directed splitting persistence (5a) from engine integration (5b) so the engine-named phase could get its own REAL-PATH test cycle under CLAUDE.md rule 13a independently of persistence bookkeeping.

### 2. Echo Router as Explicit Scaffolding
`EchoCommandRouter` (Phase 3) is documented as scaffolding that defaults to off (`useEngineRouter: false`). Production boots with `useEngineRouter: true`. This is the inverse of the prior "pragmatic interims" problem — here the interim was explicitly named and replaced by the REAL-PATH gate in the same session.

### 3. `@sharpee/media` as a Required Direct Dep
`@sharpee/media` is not used directly by Zifmia's code but must appear in `tools/zifmia/package.json` because vite-node's URL resolver cannot reach it transitively at test time. Documented inline.

### 4. `mute_state` + `dm:message` as ADR §3 Extensions
Two new WS frame types were added in Phase 6. The ADR §3 frame list was not updated in this session. The ADR needs a "Server-only frames added in Phase 6" subsection.

### 5. Phase 7 Scope: Full Build Despite Unverifiability
User chose "Full Phase 7 — every component listed" after being warned that browser verification would be out of scope for this session. The Phase 7 exit gate (AC-3 through AC-13 via two-browser hand-test) remains open.

## Next Phase
- **Phase 8**: "Playwright E2E — two-browser hand-test + automated AC-3 through AC-13 coverage"
- **Tier**: Integration (real Fastify server, real WS, real browser automation)
- **Entry state**: `tools/zifmia/` builds cleanly; 146 tests pass; user has verified (or is ready to verify) the Phase 7 UI in a real browser before Playwright is wired

## Open Items

### Short Term
- Phase 7 browser hand-test (open Phase 7 exit gate).
- Append `mute_state` + `dm:message` frame types to ADR-177 §3.
- Add a `main.ts` production boot entry (separate from `server.ts` factory) or a `bin` field in `package.json` so the server can be started without a wrapper.
- Phase 8: Playwright two-user E2E covering AC-3 through AC-13.

### Long Term
- Zifmia notarization/packaging script (`scripts/mac-release.sh` parallel) once the Phase 8 gate is green.
- Forge LLM authoring tool integration point (separate product track).

## Files Modified

**ADR + Plan** (2 files):
- `docs/architecture/adrs/adr-177-multiuser-corrected.md` — 5 advisory clarifications (PH FK, recording_notice, AC-10/11/12/13)
- `docs/work/multiuser/plan-20260512-adr-177.md` — new; 8-phase implementation plan

**Workspace registration** (2 files):
- `pnpm-workspace.yaml` — `tools/zifmia` added
- `build.sh` — `-c zifmia` function + case clause

**Zifmia server source** (37 files):
- `tools/zifmia/src/db/schema.ts`, `connect.ts`
- `tools/zifmia/src/identity/validation.ts`, `repository.ts`, `routes.ts`, `types.ts`
- `tools/zifmia/src/rooms/join-code.ts`, `repository.ts`, `participants.ts`, `routes.ts`, `state-routes.ts`, `tier-gate.ts`, `types.ts`
- `tools/zifmia/src/stories/scanner.ts`, `routes.ts`
- `tools/zifmia/src/ws/types.ts`, `lock.ts`, `rooms-hub.ts`, `handler.ts`
- `tools/zifmia/src/turns/command-router.ts`, `routes.ts`
- `tools/zifmia/src/succession/service.ts`, `recycle.ts`
- `tools/zifmia/src/sessions/events-repo.ts`, `types.ts`
- `tools/zifmia/src/engine/bundle-loader.ts`, `turn-executor.ts`, `room-state-repo.ts`, `saves-repo.ts`, `saves-routes.ts`, `engine-router.ts`, `manifest-cache.ts`, `story-health.ts`
- `tools/zifmia/src/config/repo.ts`
- `tools/zifmia/src/client/identity-storage.ts`
- `tools/zifmia/src/server.ts`, `index.ts`

**Zifmia web client** (18 files):
- `tools/zifmia/web/src/http-client.ts`, `ws-client.ts`, `identity-store.ts`, `routing.ts`, `main.ts`
- `tools/zifmia/web/src/store/room-state.ts`
- `tools/zifmia/web/src/views/identity.ts`, `lobby.ts`, `room.ts`
- `tools/zifmia/web/src/components/recording-indicator.ts`, `participant-roster.ts`, `channel-renderer-host.ts`, `chat-panel.ts`, `dm-panel.ts`, `command-input.ts`, `settings-panel.ts`, `grace-banner.ts`, `room-closed-overlay.ts`
- `tools/zifmia/web/src/styles/main.css`, `web/index.html`, `web/tsconfig.json`, `web/vite.config.ts`

**Zifmia tests** (22 files):
- `tests/handle-validation.test.ts`, `identity-routes.test.ts`, `identity-erase-4007.test.ts`
- `tests/join-code.test.ts`, `rooms-routes.test.ts`
- `tests/ws-hello.test.ts`, `ws-chat-lock.test.ts`, `ws-command.test.ts`, `lock.test.ts`
- `tests/succession.test.ts`, `recycle.test.ts`
- `tests/session-events.test.ts`, `state-route.test.ts`
- `tests/engine-integration.test.ts`, `story-health.test.ts`, `story-scanner.test.ts`
- `tests/config-recording.test.ts`, `delete-route.test.ts`, `dm.test.ts`, `mute.test.ts`
- `tests/ws-helpers.ts` (helper — bug fix)
- `tests/web/store-room-state.test.ts`, `tests/web/components.test.ts`

**Config** (3 files):
- `tools/zifmia/package.json`, `tsconfig.json`, `vitest.config.ts`

## Notes

**Session duration**: ~7 hours (crossed midnight CST; started 2026-05-12 21:09 CST, ended 2026-05-13 ~00:40 CST)

**Approach**: User directed each phase sequentially ("phase 0 go" … "phase 7 go"). Each phase had its own REAL-PATH test run before proceeding. Phase 5b satisfied CLAUDE.md rule 13a independently of Phase 5a.

**Note**: `docs/context/plan.md` exists but is unrelated to this session's work (it predates the multiuser plan). The authoritative plan for this session is `docs/work/multiuser/plan-20260512-adr-177.md`.

---

## Session Metadata

- **Status**: COMPLETE (phases 0–7; Phase 8 deferred by design)
- **Blocker**: N/A
- **Blocker Category**: N/A
- **Estimated Remaining**: N/A
- **Rollback Safety**: safe to revert (greenfield package; no existing platform packages modified)

## Dependency/Prerequisite Check

- **Prerequisites met**: `dist/stories/dungeo.sharpee` bundle present; all `@sharpee/*` workspace packages buildable; pnpm workspace resolution functioning; Vitest + vite-node available.
- **Prerequisites discovered**: `@sharpee/media` must be a direct dep of `tools/zifmia` (not just transitive) for vite-node ESM resolution to work at test time.

## Architectural Decisions

- Pattern applied: "echo router as explicit scaffolding, replaced by REAL-PATH engine router in the same session" — inverse of the `feedback_pragmatic_interims_vs_design_intent.md` anti-pattern.
- Pattern applied: `useEngineRouter: boolean` feature flag on `buildServer(options)` — allows tests using fake story slugs to run against the echo path without reaching the real bundle.
- ADR-177 §3 frame list is now stale: `mute_state` + `dm:message` frames were added in Phase 6. A "Server-only frames added in Phase 6" subsection should be appended before Phase 8.
- Phase 7 follows ADR-170 (framework-free web UI) and ADR-163 (channels as universal UI surface) without deviation.

## Mutation Audit

- Files with state-changing logic modified: `rooms/repository.ts`, `rooms/participants.ts`, `succession/service.ts`, `sessions/events-repo.ts`, `engine/room-state-repo.ts`, `engine/saves-repo.ts`, `ws/rooms-hub.ts`, `config/repo.ts`
- Tests verify actual state mutations (not just events): YES — every repository function is exercised by tests that query the SQLite `:memory:` database after the call and assert on persisted rows or returned state objects. The engine integration test asserts on actual world state (inventory contents) after a restore, not on event payloads.

## Recurrence Check

- Similar to past issue? YES — `session-20260512-2056-main.md` (prior session in this streak) touched the pragmatic-interim concern. This session is the resolution: the echo router was explicitly documented as scaffolding, and the REAL-PATH engine integration gate was completed in the same session rather than deferred. The pattern did not recur as a problem; it was preempted.

## Test Coverage Delta

- Tests added: 146 (entire test suite is new; `tools/zifmia/` was greenfield)
- Tests passing before: 0 (package did not exist) → after: 146 / 0 failing across 22 test files
- REAL-PATH highlight: `tests/engine-integration.test.ts` — 50-turn save→divergent→restore test against `dist/stories/dungeo.sharpee` (production bundle, no stubs, `:memory:` SQLite is the production driver)
- Known untested areas: Phase 7 browser UI (happy-dom component tests cover DOM mutations; no real-browser rendering verified); Phase 8 Playwright multi-user E2E not yet written

---

**Progressive update**: Session completed 2026-05-13 00:42 CST
