# Session Summary: 2026-09-10 - main (2026-09-10 11:08 CDT)

## Goals
- Execute ADR-344 plan Phase 6, the closure phase: tree green end-to-end, D8/AC-7 confirmed, the nine-AC evidence table compiled, and the I-86894c-1 `new GameEngine({ ..., player })` test-site sweep.

## Phase Context
- **Plan**: `docs/work/archive/adr-344-role-holder-seam/plan.md` — "Player role holder at the install seam" (ADR-344), now archived.
- **Phase executed**: Phase 6 — "Tree green end-to-end, D8/AC-7 confirmed, evidence compiled" (Small tier).
- **Tool calls used**: 89 / 100.
- **Phase outcome**: Completed on budget. Last phase of the plan; plan closed DONE.

## Completed

### 1. I-86894c-1 sweep — eleven sites, not the ten the item estimated
- All eleven passed a `player` field the `GameEngine` constructor stopped declaring in Phase 1; runtime ignored the excess property and both gates (vitest, root `tsc`) were blind to it.
- `packages/story-loader/tests/`: `adr-329-act-statement.test.ts`, `adr-329-d10-perform-step.test.ts`, `adr-329-goal-steps.test.ts`, `adr-330-chapters.test.ts` (two boot helpers), `adr-320-d10-interruption.test.ts`, `adr-332-arrival-reaction.test.ts`, `runtime-guards.test.ts`, and shared helper `tests/helpers/boot-turns.ts`.
- `packages/engine/tests/`: `sound/engine-wiring.test.ts`, `sound/integration.test.ts`, `unit/engine-parser.test.ts`.
- `packages/platform-browser/tests/capture-parity.test.ts`.
- Nine sites dropped the same three-line dance (create `placeholder` actor, `setPlayer`, `removeEntity` after `installStory`) to match `tests/helpers/boot-engine.ts`'s Phase 4 player-free shape; six now-unused `EntityType` imports removed alongside.
- `engine-parser.test.ts` destructured a `player` from `setupTestEngine()` that has returned `undefined` since Phase 1 — invisible to any gate. `engine-wiring.test.ts`'s stale comment (describing a "player up front, overwritten by installStory" behavior that no longer exists) replaced with the ADR-344 D6 contract.

### 2. Full verification sweep, run after `./repokit build dungeo` (Phase 5's lesson)
- `./repokit build dungeo` exit 0, bundle 4,434,065 bytes (byte-identical to Phase 5's).
- `npx tsc --noEmit` at repo root exit 0 — see the caveat under Key Decisions; this check is near-vacuous here.
- `pnpm --filter '@sharpee/world-model' test --run`: 86 files, 1516 passing, 0 failures.
- `pnpm --filter '@sharpee/story-loader' test --run`: 123 files, 1111 passing, 0 failures.
- `pnpm --filter '@sharpee/engine' test --run`: 82 files, 776 passing, 0 failures, 7 skipped.
- `pnpm --filter '@sharpee/helpers' test --run`: 5 files, 17 passing, 0 failures.
- `pnpm --filter '@sharpee/devkit' test --run`: 28 files, 177 passing, 1 skipped, 0 failures.
- `pnpm exec turbo run test:ci`: 67 tasks, 67 successful.
- `node dist/cli/sharpee.js --test --chain stories/dungeo/walkthroughs/wt-*.transcript`: 952 tests across 17 transcripts, 952 passing, all 17 goldens matched (single run, pinned seed per project convention).
- `./sharpee test branch-stories/fernhill`: 86 cards passing, 104 assertions passing, 222 commands.
- `node dist/cli/sharpee.js --world-json --story stories/cloak-of-darkness/cloak.story`: one actor, `a01` "Alex" at `r01`, no `a02`, no unaccounted id (AC-3).
- All runs 2026-09-10 11:16–11:19 CDT.

### 3. AC-7/D8 and AC-6 confirmed by direct read
- `packages/engine/src/install/steps.ts:46-66`: 19 steps, exactly one added by this ADR — `validateRoleHolderStep` at position 7, between `createPlayerStep` and `listenerTraitStep`. `initialize-world` and `story-initialize` each delegate to their story hook unguarded, matching D8 exactly.
- `packages/engine/src/install/story.ts:280-302` confirms AC-6.

## Key Decisions

### 1. Root `tsc --noEmit` recorded with its caveat rather than left as an unqualified green line
The plan's Phase 6 bullet asked for a "full run" of `npx tsc --noEmit` at repo root. It exits 0 identically before and after the sweep because `tsconfig.json` carries `files: []` with only four project references (`core`, `stdlib`, `extensions/conversation`, `cloak-of-darkness`) — it type-checks none of `engine`, `story-loader`, `world-model`, `helpers`, `devkit`, `bootstrap`, `bridge`, `runtime`, `platform-browser`, or the eleven swept test files. ADR-344's AC-3/AC-8 already corrected the real check to `./repokit build dungeo`; the plan bullet predates that correction. Recorded with the caveat in both the ADR and the plan instead of reported as an unqualified pass. This is the fifth logged instance in this plan of a check structurally incapable of observing what broke (open item I-52e228-1); deliberately not fixed here — widening the root tsconfig is its own decision.

## Next Phase
Plan complete — all six phases DONE, plan archived. Next work is ADR-345's engine-lifecycle-phase plan, which its own D6 scheduled to start exactly at this plan's close, and which owes ADR-344 D6a an amendment (I-b314e2-2).

## Open Items

### Short Term
- I-b314e2-1: ADR-345 D6 engine-lifecycle-phase plan starts now that this plan has closed at Phase 6.
- I-b314e2-2: ADR-345 D13 — the session implementing ADR-345 D2 must amend ADR-344 D6a in the same commit (D6a quotes a refusal string D2 makes stale).

### Long Term
- I-52e228-1: which story/package tsconfig exclusions silently hide type errors from every existing gate — now a fifth data point (root `tsc` blind to the eleven swept files); wants a systemic audit of which suites read `dist/` vs source and which gates trust artifact presence over freshness.

## Files Modified

**Test sweep** (12 files):
- `packages/story-loader/tests/adr-329-act-statement.test.ts`, `adr-329-d10-perform-step.test.ts`, `adr-329-goal-steps.test.ts`, `adr-330-chapters.test.ts`, `adr-320-d10-interruption.test.ts`, `adr-332-arrival-reaction.test.ts`, `runtime-guards.test.ts`, `tests/helpers/boot-turns.ts` — dropped the obsolete `player` field and placeholder-actor dance.
- `packages/engine/tests/sound/engine-wiring.test.ts`, `sound/integration.test.ts`, `unit/engine-parser.test.ts` — dropped `player`, replaced a stale behavior comment.
- `packages/platform-browser/tests/capture-parity.test.ts` — dropped `player`.

**ADR and plan** (2 files):
- `docs/architecture/adrs/adr-344-player-role-holder-at-the-install-seam.md` — added the nine-AC evidence table section and root-tsc caveat.
- `docs/work/archive/adr-344-role-holder-seam/plan.md` — Phase 6 marked DONE, Plan Status DONE, then archived (was `docs/work/adr-344-role-holder-seam/plan.md`).

**Ledger and pointers**:
- `docs/context/.open-items.jsonl` — I-86894c-1 resolved (evidence: archived plan).
- `docs/context/.current-plan` — released by the archive script.
- ADR-344's plan-path reference and ADR-345's `**Related**` line repointed to `docs/work/archive/adr-344-role-holder-seam/plan.md`.

## Notes

**Session duration**: ~1 hour 15 minutes (11:08–~12:25 CDT).

**Approach**: Sweep the remaining stale test call-sites first (the phase's only code change), then verify the full tree in the order Phase 5 established (build before test), then confirm the two D8/AC-6/AC-7 claims by direct source read rather than inference, then compile the evidence table and close the plan.

---

## Session Metadata

- **Session**: 4b4f4d
- **Status**: COMPLETE
- **Blocker**: N/A
- **Blocker Category**: N/A
- **Estimated Remaining**: N/A — plan closed.
- **Rollback Safety**: safe to revert — all changes are test files, docs, and ledger/pointer updates.

## Dependency/Prerequisite Check

- **Prerequisites met**: Phase 5's build-before-test ordering lesson; ADR-344 D1–D7 already implemented in prior phases; `plan-archive.sh` available for the closure move.
- **Prerequisites discovered**: None.

## Architectural Decisions

- ADR-344: D1–D8 confirmed fully implemented and closed this session (AC-6, AC-7/D8 confirmed by direct source read; the nine-AC evidence table compiled into the ADR itself).
- Pattern applied: verification-after-build ordering established in Phase 5, applied throughout this phase's sweep.

## Mutation Audit

- Files with state-changing logic modified: none — this phase's code change was confined to test files (excluded by rule 15) removing stale `player` fields and a placeholder-actor dance; no source side-effect function was created or changed.
- Tests verify actual state mutations (not just events): N/A — no production behavior changed this session.
- If NO: N/A.

## Recurrence Check

- Similar to past issue? YES — "check structurally incapable of observing what broke," now 5 instances across this plan (pre-session-audit reported it flagged in 4 of the last 5 sessions). Tracked as I-52e228-1.
- Consider one-time audit of: which suites read `dist/` vs source, and which gates trust artifact presence over freshness.

## Test Coverage Delta

- Tests added: 0 (test-site cleanup only, no new tests).
- Tests passing before: not independently re-baselined this session → after: world-model 1516/1516, story-loader 1111/1111, engine 776/776 (7 skipped), helpers 17/17, devkit 177/177 (1 skipped), turbo test:ci 67/67 tasks, walkthrough chain 952/952 (17/17 goldens), fernhill 86/86 cards + 104/104 assertions (evidence: commands and results quoted above, run 2026-09-10 11:16–11:19 CDT, after `./repokit build dungeo`).
- Known untested areas: none newly introduced; root `tsc --noEmit` remains a non-observing gate for these packages (see Key Decisions).

---

**Progressive update**: Session completed 2026-09-10 12:25 (approx.)

## Activity Log (auto-captured)
```
[16:15:27] BUILD: Build passed — cd /Users/david/repos/sharpee
pnpm --filter '@sharpee/story-loader' test --run a
[16:15:31] BUILD: Build passed — cd /Users/david/repos/sharpee
pnpm --filter '@sharpee/engine' test --run tests/s
[16:16:38] BUILD: Build passed — cd /Users/david/repos/sharpee
for p in world-model story-loader engine helpers d
[16:17:57] BUILD: Build passed — cd /Users/david/repos/sharpee
pnpm exec turbo run test:ci 2>/dev/null | tail -8

[16:26:29] EDIT: File written — docs/context/session-20260910-1108-main.md
```
