# Session Summary: 2026-07-23 - main (CST)

## Goals
- Correct ADR-256's status line against verified source.
- Investigate the IDE (`tools/ide/`), dormant since the Chord arc landed, and rule its authoring-language target.
- Write and interview the ADRs the IDE and hatch-build gaps require.

## Phase Context
- **Plan**: `docs/work/chord-go-live/plan.md` (ADR-233 gate) — not touched this session; this was ADR-only work outside any plan phase.
- **Phase executed**: N/A — no plan phase advanced.
- **Tool calls used**: 143 (no budget tracked; phase/tier fields empty in session state).
- **Phase outcome**: N/A.

## Completed

### ADR-256 status correction
- Verified `packages/story-loader/src/event-id-map.ts` exists and is wired into `loader.ts`; the ADR's status line still read "Not implemented."
- Updated status to ACCEPTED + IMPLEMENTED (session note appended, 1-line diff).

### IDE investigation — found broken, not stale
`tools/ide/` last touched 2026-06-27 (`26559677`); the whole Chord arc landed without it. Traced four concrete breaks against a Chord `.story`:
- `packages/devkit/src/commands/introspect.ts:32` hard-gates on `dist/index.js`; a `.story` never emits one.
- `WebBundle.swift:15` resolves `<projectRoot>/dist/web/`; ADR-252 D2 emits `dist/web/<id>/`.
- `BuildRunner.swift:57/67/76` runs `node_modules/.bin/sharpee` / `npm install` / `init-browser`; a `.story` needs none, and `browser-entry.ts` was retired by ADR-252 D4.
- `SyntaxHighlighter.swift` / `EntitySourceIndex.swift:14` bind TreeSitterTypeScript; `TSCDiagnostic.swift` scrapes tsc stderr.

David ruled: Chord-first, TypeScript author path dropped.

### ADR-258 written — "The IDE is a Chord authoring environment (TypeScript author path dropped)"
- D1–D9. Supersedes ADR-182 outright; supersedes ADR-184's mechanism (project tree now IR-sourced) while keeping its deliverable; ADR-185's project model survives minus npm prerequisites.
- `adr-interview` resolved Q-1–Q-4. Q-3 (IR-sourced tree) removed a platform change the draft assumed — `introspect` needs no separate Chord path because `compose` yields diagnostics and IR together — and retired ADR-184's name-matching source index. Q-1 dropped tree-sitter entirely in favor of an in-process Swift lexer port.
- `adr-review` first pass: 9/15 NEEDS WORK, 3 blockers, each found by checking the draft against source: (a) the live tree contradicted ADR-210's atomic-load contract (`chord/src/index.ts:57` — IR meaningful only when `ok`), fixed with last-good-IR retention; (b) "one platform change" undercounted — no `compose` mode existed that yields gates+IR without the load-proof; (c) the conformance-pin justification claimed "fails CI" but no workflow runs xcodebuild/Swift, so David chose a TS-side golden fixture instead, keeping the pin where CI already runs.
- Re-scored 15/15 READY. **Status remains DRAFT — David has not flipped it.**
- Recorded gap: the IDE's 27 test files have never run in CI.

### ADR-259 written — "The Chord browser build supports hatch modules (full compile route)"
- D1–D8. Amends ADR-252's build contract; ADR-258 depends on it.
- Headline finding: hatches are designed, implemented at runtime, and green in tests, but no Sharpee build has ever compiled a hatch module. `browser-core.ts:449` hard-throws on `hasHatches`; friendly-zoo (the only hatched story) compiles its hatch with plain `tsc` from its own `package.json`, outside any `sharpee` command — and its `.story` + `src/index.ts` shape is exactly what ADR-252 D1 outlawed, so `sharpee build` rejects it before hatches are even considered.
- Key architectural finding: `story-loader` is already filesystem-free and takes injected `hatchModules` (`loader.ts:17/145/282`, `createStory(ir, options)` at `loader.ts:167`) — the seam was built and left unused; the remaining work is confined to the build and the generated entry.
- `adr-interview` resolved Q-1–Q-3, each shrinking scope: Q-1 dissolved (the loader already enforces the hatch contract at bind time with better diagnostics than tsc, so the build does a bind check with no tsc/tsconfig/typescript dependency — ADR-252 D2's promise survives); Q-2 half-decided by fact (the exclusion targets `src/index.ts` specifically, so hatch modules never trip it — no amendment needed); Q-3 revealed `src/index.ts` is a live multi-file TypeScript tutorial ("Family Zoo Tutorial v17"), so David chose to split the directory — both stories survive, nothing deleted.
- `adr-review` first pass: 11/14 NEEDS WORK, 3 blockers, again from source-checking: D2's generated import would not resolve (entry is written to `dist/.browser-entry/<storyId>/` per `browser-core.ts:500`, not beside the `.story`, so import specifier and map key must differ); D5's bind check had an unstated step (Node can't require author TS directly — needs a second esbuild pass to CJS); D3 rested on a false premise ("pure bundles are story-agnostic" — `generateEntry` interpolates title/theme/storage-prefix into every per-story entry).
- Re-scored 14/14. **Status: ACCEPTED. Not implemented.**

### TypeScript 7.x — researched and deliberately deferred
- TS 7.0 GA'd 2026-07-08 (Go rewrite, ~10x faster, ships as the ordinary `typescript` package) but without a stable programmatic API; expected in 7.1, ~3-4 months out.
- David ruled TS 7 deferred repo-wide: `@davidcornelson/tsf` (his compiler orchestrator, `~/repos/tsf`) needs 7.1. Repo stays on `typescript ^5.2.2` / `^5.3.3`.
- Recorded in ADR-259 D5 as a toolchain note, plus memory entry `typescript-7-deferred-on-tsf.md`. Forward constraint: 7.1's API is the only honest route to hatch TYPE errors joining Chord's `analysis.*` diagnostics in one structured Problems panel, since tsc emits text and ADR-258 D3/D5 deliberately removed text scraping.

## Key Decisions

### 1. IDE targets Chord only
TypeScript author path dropped (ADR-258 D1) — the whole Chord arc landed without the IDE, and the IDE is now broken against a `.story` in four traced ways, not merely stale.

### 2. IDE project tree sourced from Story IR
Inverts ADR-184's mechanism (ADR-258 D6) — `compose` yields diagnostics and IR together, so no separate introspection path is needed and the atomic-load contract (ADR-210) drove a last-good-IR retention fix.

### 3. Chord highlighting via in-process Swift lexer port
Pinned by a TS-side golden token stream, not tree-sitter (ADR-258 D7) — chosen because no CI workflow runs xcodebuild/Swift, so the conformance pin needed to live where CI already runs.

### 4. Hatched builds use a bind check, not a typecheck
ADR-259 D5 — the loader's existing bind-time contract enforcement is stricter than tsc and avoids adding a tsc/tsconfig/typescript dependency to the build.

### 5. friendly-zoo directory split
ADR-259 D8 — `src/index.ts` is a live "Family Zoo Tutorial v17," not dead scaffolding; David chose to split the directory so both the Chord story and the TS tutorial survive rather than deleting either.

### 6. TypeScript 7.x deferred repo-wide
Blocked on `tsf` needing the 7.1 programmatic API; recorded so the constraint isn't rediscovered.

## Next Phase
No active plan phase was executed this session (chord-go-live plan untouched). Natural next step, not yet planned: run `session-planner` over ADR-259's build work and ADR-258's D3 TS-removal swap table — largely independent efforts, since the hatched-build route doesn't touch the TS-removal swaps.

## Open Items

### Short Term
- ADR-258 is DRAFT awaiting David's ACCEPTED flip.
- ADR-259 is ACCEPTED but unimplemented — no session has yet planned its build/entry work.

### Long Term
- CLI hatch gap: `sharpee test`/`sharpee play` still expect pre-compiled JS, unaddressed by ADR-259's browser-only scope.
- IDE's 27 test files have never run in CI (ADR-258 gap).
- TS 7.1 programmatic API, once available, reopens the structured-diagnostics option ADR-258 D3/D5 deliberately closed off.

## Files Modified

**ADRs** (3 files):
- `docs/architecture/adrs/adr-256-event-type-acl.md` - status line corrected from "Not implemented" to ACCEPTED + IMPLEMENTED, citing `event-id-map.ts`
- `docs/architecture/adrs/adr-258-ide-chord-authoring-environment.md` - new, DRAFT (interview resolved, review 15/15, awaiting David's flip)
- `docs/architecture/adrs/adr-259-chord-browser-build-hatch-modules.md` - new, ACCEPTED, not implemented

**Memory** (2 files, outside repo, not committed):
- `~/.claude/projects/-Users-david-repos-sharpee-v2/memory/typescript-7-deferred-on-tsf.md` - new
- `~/.claude/projects/-Users-david-repos-sharpee-v2/memory/MEMORY.md` - index entry added

## Notes

**Session duration**: ~2 hours (started 2026-07-23T20:46:08Z per session state).

**Approach**: No code was changed. All three deliverables are ADR documents produced through `adr-interview` → `adr-review` → fix → re-review cycles, each blocker traced to a specific source line before being accepted as real. This session's whole value was in verified claims against the actual codebase, not assumed gaps.

---

## Session Metadata

- **Status**: COMPLETE
- **Blocker** (if any): N/A
- **Blocker Category**: N/A
- **Estimated Remaining**: N/A (session scoped to ADR work only; implementation is future, unplanned work)
- **Rollback Safety**: safe to revert

## Dependency/Prerequisite Check

- **Prerequisites met**: ADR-256's story-loader implementation (verified present) enabled the status correction; ADR-252 (`.story` browser build) and ADR-210 (Chord IR, hatch design) were prerequisites read and cited by both new ADRs.
- **Prerequisites discovered**: None — this session surfaced gaps (IDE breakage, hatch-build gap), it did not discover missing prerequisites for the ADR-writing work itself.

## Architectural Decisions

- ADR-256: status corrected to ACCEPTED + IMPLEMENTED — Chord-event-id → platform-event-id translation confirmed live in `packages/story-loader/src/event-id-map.ts`.
- ADR-258 (new, DRAFT): the IDE is a Chord-only authoring environment; supersedes ADR-182, supersedes ADR-184's mechanism, amends ADR-185's project model.
- ADR-259 (new, ACCEPTED): the Chord browser build supports hatch modules via a bind check and story-loader's existing injection seam; amends ADR-252's build contract.
- Pattern applied: `adr-interview` (one question at a time, folded immediately) followed by `adr-review` (adversarial, source-checked) on both new ADRs, per ADR-0009/rule 11a.

## Mutation Audit

- N/A — no state-changing code was written or modified this session; all changes are ADR document text.

## Recurrence Check

- Similar to past issue? YES — the "seam built and left unused" pattern (hatch injection existing in `story-loader` since ADR-210 but never wired into any build) echoes the general shape flagged in memory as [[trust-architecture-seams.md]]: platform capability exists, consuming code hasn't caught up. No prior session filename directly matches; this is a new instance of the same class, not a repeat of one specific prior session.
- If YES: no subsystem-wide audit requested by David this session; noted for awareness only.

## Test Coverage Delta

- No test changes this session.

---

**Progressive update**: Session completed 2026-07-23 21:46
