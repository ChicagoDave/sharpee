# Session Summary: 2026-06-22 02:00 - main

## Goals
- Run a full-book completeness audit and remediate every chapter.
- Build a 1:1 book-companion tutorial set (cumulative, compilable per-chapter snapshots).
- Decouple the familyzoo tutorial from the monorepo into a standalone npm consumer.
- Validate the decoupled tutorial end-to-end from published @sharpee/* packages.
- Fix devkit's `classifyStory` so it routes workspace vs. standalone stories correctly.

## Phase Context
- **Plan**: `docs/work/book/plan-20260620-sharpee-author-book.md`
- **Phase executed**: Book QA + tutorial decoupling — not a single named plan phase; spans book remediation, tutorial restructure, and a devkit toolchain fix.
- **Tool calls used**: (not tracked — continuation session)
- **Phase outcome**: Completed — all goals shipped and pushed.

## Completed

### Full-book completeness audit + remediation (commit fa303dde)
Triggered by a Docker "naive reader" QA pass (`docs/book/testing/`) that found ch4's `examine signs` failing: the chapter taught by excerpt while its walkthrough exercised the full reference version. Ran an 8-agent parallel audit (one per volume) cross-checking every chapter against `tutorials/familyzoo/src/vNN.ts`. Wrote `docs/book/testing/2026-06-22-completeness-audit-full-book.md`. Remediated all 8 volumes: every code-bearing chapter now shows complete runnable code — imports, class/method frames, full entity creation, message-id registrations, constants — with no elisions. Every "Try it" command resolves against shown code. Concrete fixes: ch7 `EntityType.DOOR`; ch25 storyInfo placeholders + `#score-badge` guard; ch27 real `mediaEvent`/`Effect` helpers; ch29 malformed assertion + real transcript paths; ch32 docker flag. Two David decisions captured: `SceneryTrait` taught at ch4 (where it first appears); the parrot is a static pettable actor in ch15 and is promoted to a full NPC in ch20 (resolves chapter-vs-version NPC ordering and ch20's missing parrot simultaneously). Book rebuilds clean (HTML/EPUB/PDF/web).

### Book-aligned tutorial set (commit fa303dde)
Built a 1:1 book companion: cumulative compilable `Story` snapshots per chapter under `tutorials/familyzoo/src/book/` (ch02–ch28 + multi-file dirs + README), with NPC-region reconstruction (ch13–15 NPC-stripped; ch15 keeps the static pettable parrot). The 4 genuinely-new files type-check clean; the rest derive from the v-versions.

### Promote + archive (commit 2d398045)
Made the book-aligned set the canonical tutorial: `git mv tutorials/familyzoo/src/book/* tutorials/familyzoo/src/`; archived the original v01–v18 (not deleted) under `docs/archive/tutorial/`; repointed `index.ts` and `browser-entry.ts` at `ch24-27-presentation`.

### Decouple the tutorial (commit 9076987d)
Cut familyzoo from the monorepo into a standalone npm consumer: removed from `pnpm-workspace.yaml`; `package.json` workspace:* refs → `^1.0.8`; renamed `@sharpee/tutorial-familyzoo` → `familyzoo`; `type: module`; tsconfig → NodeNext ESM; `index.ts`/`browser-entry.ts` use explicit `.js` import specifiers. `pnpm install` ran clean (workspace now 35 projects).

### End-to-end validation from published npm packages
Built familyzoo standalone outside the repo: `npm install @sharpee/*@1.0.8` + `sharpee build` compiled every representative snapshot (ch02/ch15/ch23 single-file, ch28 multi-file, ch24-27 presentation) → `dist/familyzoo.sharpee` + `dist/web/`. Confirmed the devkit #148 fix works from npm (1.0.8 CLI runs, no MODULE_NOT_FOUND). Found and fixed: multi-file snapshots needed `.js` import extensions for the standalone NodeNext ESM scaffold.

### devkit convention fix (commit bb26e29f; republished 1.0.9; bump commit dd657cda)
`classifyStory` in `packages/devkit/src/repo.ts` no longer derives `@sharpee/{story,tutorial}-<name>` from the directory path — it reads the real `package.json` name and detects workspace membership via a `workspace:*` dependency. `ResolvedStory` gains a `workspace` field. `buildStory` only pnpm-filters workspace members; the CLI routes decoupled stories (`inRepo && !workspace`) to a standalone build of their own directory, so `sharpee build familyzoo` works in place. 13/13 devkit tests pass. `@sharpee/devkit@1.0.9` republished.

### Remove familyzoo from npm builds (commit faa0da7a)
Dropped `tutorials/familyzoo` from `ts-forge.config.json` projects and removed its orphaned per-package `ts-forge.json` so `tsf build --npm` no longer stages it.

## Key Decisions

### 1. Book teaches against the decoupled standalone tutorial
The tutorial consumes published `@sharpee/*@^1.0.8` (not in-repo path aliases). The monorepo build is for the platform only. This makes every book code example verifiable by a reader who does exactly what the book says: `npm install`, then run.

### 2. Parrot: static pettable actor (ch15) → full NPC (ch20)
Resolves the chapter-vs-version NPC ordering conflict and fills ch20's missing parrot in a single design move. No invented story content — David confirmed both the ch15 static form and the ch20 promotion.

### 3. SceneryTrait taught at ch4
Introduced where it first appears in the zoo story rather than deferred to a later chapter. Keeps code examples consistent with what readers can actually run.

### 4. classifyStory reads package.json, not path
Path-derived package naming was fragile (any directory rename would silently mis-classify). Reading the real `package.json` name is unambiguous; `workspace:*` detection is the correct signal for "managed by pnpm".

## Next Phase
- **Pending (David's action)**: update the live `/zoo` demo — `demos/deploy.sh` builds it and now needs `npm install` in `tutorials/familyzoo/` first (familyzoo is decoupled). Deferred by David.
- No other open plan phases; book is complete across all 8 volumes.

## Open Items

### Short Term
- Live demo `demos/deploy.sh` needs an `npm install` step in `tutorials/familyzoo/` before the zoo demo build. David said handle later.

### Long Term
- Pre-publish smoke test harness (`npm pack` → install in temp dir → `sharpee --version`) remains unbuilt — noted in the 1933 session; still unbuilt.

## Files Modified

**Book** (many chapter files across all 8 volumes):
- `docs/book/testing/2026-06-22-completeness-audit-full-book.md` — audit report created
- Chapter files ch04–ch32 in `docs/book/chapters/` — completeness remediation (imports, full class frames, no elisions)

**Tutorial** (restructured):
- `tutorials/familyzoo/src/book/ch02.ts` through `ch28/` — new per-chapter snapshots
- `tutorials/familyzoo/src/` (top-level) — promoted from book/ subdir; `index.ts` + `browser-entry.ts` repointed
- `tutorials/familyzoo/package.json` — renamed, workspace:* → ^1.0.8, type:module added
- `tutorials/familyzoo/tsconfig.json` — NodeNext ESM
- `pnpm-workspace.yaml` — familyzoo removed
- `docs/archive/tutorial/` — v01–v18 archived (not deleted)

**devkit** (2 files):
- `packages/devkit/src/repo.ts` — `classifyStory` reads `package.json` name; `ResolvedStory.workspace` field added
- `packages/devkit/package.json` — version bump to 1.0.9

**Build config** (1 file):
- `ts-forge.config.json` — `tutorials/familyzoo` removed from npm build projects; orphaned `ts-forge.json` removed

## Notes

**Session duration**: ~4 hours (continuation after the 1933 session)

**Approach**: Audit-first — ran parallel per-volume agents before writing a single fix, so remediation was targeted rather than speculative. Decoupling was validated end-to-end from the published packages, not just by type-checking in-repo.

**The live demo gap is known and deferred**: `demos/deploy.sh` will need updating when David returns to the zoo demo. No action needed in the platform.

---

## Session Metadata

- **Status**: COMPLETE
- **Blocker**: N/A
- **Blocker Category**: N/A
- **Estimated Remaining**: N/A
- **Rollback Safety**: safe to revert — book changes are documentation; tutorial restructure is documentation + standalone config; devkit 1.0.9 is already published (revert would require a 1.0.10 patch)

## Dependency/Prerequisite Check

- **Prerequisites met**: `@sharpee/*@1.0.8` published on npm (from the 1933 session); `tutorials/familyzoo/` type-checked clean in-repo before decoupling.
- **Prerequisites discovered**: The standalone NodeNext ESM scaffold requires explicit `.js` import extensions on all intra-project imports — not enforced by the in-repo setup, so only surfaced during the standalone build.

## Architectural Decisions

- Pattern applied: standalone tutorial as the book's reference implementation — consumed from npm, not path-aliased from the monorepo. Establishes that every book example is verifiable by a first-time reader following only the book's instructions.
- `classifyStory` reads `package.json` name + `workspace:*` detection: eliminates path-derived package naming fragility (devkit).
- No new ADR written — both are implementation conventions confirmed in conversation with David, not platform-level architecture decisions.

## Mutation Audit

- Files with state-changing logic modified: `packages/devkit/src/repo.ts` (`classifyStory`, `buildStory` routing)
- Tests verify actual state mutations: YES — 13/13 devkit tests pass; `classifyStory` tests assert on the returned `ResolvedStory` shape including the new `workspace` field.

## Recurrence Check

- Similar to past issue? YES — path-derived naming (devkit `classifyStory`) is the same class of fragility as the `ts-forge.config.json` `imports: relative` assumption from the 1933 session: both derived a property from a structural convention rather than reading the authoritative source. Both were fixed by reading the real value. Consider auditing other places where devkit infers identity from path rather than manifest.

## Test Coverage Delta

- Tests added: 0 net new (existing 13 devkit tests updated to cover `workspace` field)
- Tests passing before: 13 → after: 13
- Known untested areas: standalone `npm install` + build path is validated manually, not in an automated harness; pre-publish smoke test remains unbuilt.

---

## Addendum — Chapter 2 ': Story' fix + browser-entry hardening (2026-06-22)

A follow-on QA pass (book-faithful, against the published packages) hit a blocker at
Chapter 2: typing §2.7 exactly and running `sharpee build` gave four TS2339 errors —
the `init-browser`-generated `browser-entry.ts` calls `if (story.extendParser) …`, but
the book exported `const story = new FamilyZooStory()` with no type annotation. Under
`strict`, `story` is typed as the concrete class, which doesn't declare the optional
`extendParser`/`extendLanguage` hooks, so even the guarded reference fails. (devkit's
own init scaffold uses `: Story`; the book had dropped it.)

Two independent fixes, both validated by reproducing the exact failing scenario
(ch02 + `sharpee init-browser` + `sharpee build`) against `@sharpee/*@1.0.8`:

1. **Book + tutorial (commit 86d61696, pushed):** `export const story: Story =
   new FamilyZooStory();` in Chapter 2 (with an explanation of why the annotation
   matters) and in every book-aligned tutorial snapshot (`Story` already imported).
2. **devkit browser-entry template (commit 766d073d, bump 1.0.10):** the generated
   `browser-entry.ts` now views the story through the `Story` interface
   (`const typedStory: Story = story;`) before the optional-hook checks, so it compiles
   whether or not the story file annotates its export. (The reporter's `'extendParser'
   in story` idea alone wouldn't compile — `in`-narrowing yields `unknown`, not callable.)

**Status:** book + tutorial fix pushed; devkit 1.0.10 committed locally — David builds
and republishes. QA pass can resume from Chapter 2's "Try it" transcript.

---

**Progressive update**: Session completed 2026-06-22 ~06:00 CST; addendum 2026-06-22
