# Session 2026-07-11 22:35 — v2-210-chord-a

## Goal

Pre-P3 design pause: David asked whether the TS hatch (`chord-extras.ts`, ADR-210 §5.6) creates an I7/I6-style boundary that will haunt the platform, especially the IDE.

## Status

**Hatch legitimacy rule decided and recorded.** Assessment found the boundary structurally sounder than Inform's (hatches bind to public platform interfaces and are declared in IR, not inline blobs; pure-IR profile is a checkable tier) but with one live leak: `gateStatus` reads `chord.flag.gate-closed` by string — a loader-internal key the Phase C runtime no longer writes (flags removed). Silent rot, no diagnostic; the producer context *type* already hides world access and the hatch cast through it.

## Decisions

- **Hatch legitimacy rule (David, 2026-07-11):** any hatch doing IF work is a Chord gap or a misuse. Legitimate shapes: (1) Sharpee-API hatch — public platform interfaces only; (2) pure-computation hatch — non-IF TypeScript, data crosses only via the implemented interface. `chord.*` state keys are off-limits to hatches. Enforcement by construction (narrow typed hatch context from the loader + `'chord.'` string-literal lint as backstop) — implementation is story-loader follow-up work; until then, review-enforced. Hatch count = language-gap metric; pure-IR profile = scoreboard.
- Recorded as a design decision entry in `docs/work/story-language/design.md` §5.6 (NOT in chord-grammar-changes.md — no surface syntax change, and that log is scoped to grammar).

## Artifacts

- `docs/work/story-language/design.md` §5.6 — legitimacy rule, `chord.*` prohibition, enforcement-by-construction contract, gateStatus cautionary precedent.
- `docs/work/chord-phase-c/plan.md` P5 platform-touch list — gateStatus disposition changed from "repoint at openable state" to "retire into a pure-Chord state-conditional phrase"; checklist line added: no hatch module may reference `chord.*` keys.

## Phase C P3 executed (same session, after the hatch decision; commit pending)

**P3 COMPLETE** — all deliverables in, suites green: chord 144/144 (was 124; +20 Phase C tests), ide-protocol 11/11 (wire-type check: builds against the new IR shape), story-loader 61/61, devkit 22. `zoo-phase-c.story` now COMPILES with zero diagnostics (P3 exit; was parse-only) with a golden IR snapshot.

Delivered in `packages/chord`:
- **Fixture-blocking fixes** (the 7 live errors): STATE_ADJECTIVES catalog (D1: open/closed/locked/unlocked/on/off/worn/lit) wired into `resolveIsObject`; owner-scoped inline phrases (entity-clause `phrase <key>` blocks register as `<entity-id>.<key>` — the phrase-override mechanism `phraseEvent` already resolves at runtime; traits/actions/sequences keep bare keys); cross-trait state resolution (trait scope sees own states ∪ every composer's merged set — restless reads feedable's hungry).
- **New gates** (negative fixture + exact-code/line test each in `tests/fixtures/gates/`, `tests/analyzer-phase-c.test.ts`): `analysis.boolean-state` / `analysis.shadow-state` (fix-it names the trait to compose) / `analysis.negated-state` (D9 three rings, encouraging fix-it as drafted, over story/trait/entity sets); `analysis.negated-requirement` (D6: refuse-when top-level `not`, action refusals + body statements); `analysis.duplicate-clause`; `analysis.state-collision` (D8, rename fix-it); `analysis.irreversible-state` (D4 static half: change-to-initial on non-reversible set).
- **IR additions**: `IROnClause.narration: 'presence'`, `IRSequenceDef.narration: 'broadcast'` (decision 10 semantic tags for the P4 loader); snapshots regenerated (2).

**Deviations RESOLVED (David, 2026-07-11)**: (1) duplicate-clause gate APPROVED as shipped — strict key for interceptor/capability-routed clauses (keyed registration = real masking), span-free condition fingerprint for event-verb clauses (the aviary's three condition-differentiated `after entering it` clauses are legal; identical conditions still error). (2) select-on exhaustiveness stays ARM-VALIDITY ONLY (extended to trait scopes); a missing-arm completeness gate would need its own ratchet entry — not requested. Both recorded in plan.md P3 status.

## Phase C P4 executed (same session; commit pending)

**P4 COMPLETE** — story-loader 73/73 (+12 in tests/ownership-runtime.test.ts), chord 144/144, ide-protocol 11/11, devkit 22. Zero `chord.flag`/`CHORD_FLAG` anywhere in packages/ (P4 exit; repo-wide incl. stories is P5's).

- **Flag/if machinery deleted wholesale**: CHORD_FLAG_PREFIX, evaluator flag condition/value cases, runtime set-flag branch + both `if` handlers, loader flag-init loop, index export — AND the legacy IR surface itself (StoryIR.flags/rules/onceRules/everyRules, IRFlagDef/IRRule/IROnceRule/IREveryRule, `if` statement kind, `flag` value/condition/trait-field-type kinds). `if` removal is compile-time impossible now. ide-protocol re-exports trimmed; obsolete empty-array test assertions flipped to absence assertions; devkit compose status line moved off ir.rules.
- **D1 evaluator leg**: symbolHolds reads open/closed/locked/unlocked (Openable/LockableTrait), on/off (Switchable), worn (Wearable), lit (LightSourceTrait) live — declared states take precedence, pure reads, no stored shadow (test proves no chord.state slot appears).
- **D4 runtime forward-march**: change execution resolves the target's declaring set (trait set via IRTraitDef, own set via new IREntity.statesReversible + merged-minus-trait reconstruction) and throws LoadError on backward moves in non-reversible sets; story set included. Cross-set moves and same-state no-ops pass.
- **Decision-10 presence gate**: entity- and trait-every-turn daemons check playerPresentAt (room owner = player IN it; else shared containing room) BEFORE condition eval — off-stage clauses draw no RNG (chord.rng untouched, asserted) and never consume `, once`. Action/event clauses inherently witnessed. Sequences broadcast unchanged.
- **AC-9 hardening (Gemini)**: enteringDestination() type-guard in event-contract.ts replaces the blind payload cast in fireEventClause; return type chained to stdlib's ActorMovedEventData.
- **Finding 8 tests**: dedicated body-level `must` (dispatch validate → block + state assertions) and statement-`when`-suffix tests; CP6 sequence-scope named-entity change; AC-6 trait-state transplant; D4 forward/backward/reversible.
- **Ripple found & fixed**: P3's owner-scoped inline phrases changed inline-phrase messageIds (keeper-leaves → sam-the-zookeeper.zoo.after-hours.keeper-leaves); scheduler test updated — rendered text unchanged. ESM gotcha: story-loader/devkit resolve chord via dist-esm — both chord tsconfigs must rebuild before cross-package testing.

## Next Steps

- Commit P4 when David gives the word (uncommitted: chord ir/analyzer + test assertion updates, story-loader src+tests, ide-protocol story-ir.ts, devkit compose.ts, plan.md P4 status, this file).
- P5 next: migrate cloak.story + zoo.story + chord-extras.ts (retire gateStatus into pure Chord; no chord.* keys in hatches), gate-delta audit (4 verified deltas incl. unpinned keeper-witness — P4's presence gate is the cause), restore zoo-pure-ir.test.ts, repo-wide chord.flag sweep, AC-6 trait-state transcript in the CLI harness.
- David items still open from P2: refuse-when as body statement (D6 extension nod); `any <condition>` dormant until iterate.
- P4 next (loader/runtime ownership semantics): full D4 forward-march ordering at runtime; narration tags acted on (presence-scoped entity daemons); undo-delta-safe state storage + type guards (Gemini adoptions).
- P5 carries the hatch-rule consequences (retire gateStatus; hatch checklist line) plus restoring `zoo-pure-ir.test.ts` from its TODO(phase-c-p5) stand-in.
- Hatch-context narrowing + lint: story-loader follow-up (platform change — needs its own go-ahead when scheduled).
