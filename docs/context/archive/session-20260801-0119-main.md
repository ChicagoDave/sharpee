# Session Summary: 2026-08-01 (0119) - main (5a55e3)

## Goals
- Run the ADR-293 Phase A platform discussion: verify a six-item pre-implementation
  analysis against the tree, obtain David's rulings on the four decision items, and fold
  everything into the ADR.

## Completed

### 1. Verified all six analysis items against the tree
Every claim re-checked directly (no inherited testimony), 2026-08-01:
- **D6 vs D13 conflict — real.** ~35 non-test `Math.random` sites outside the D6
  allowlist, nearly all the deferred `Date.now()`+`Math.random()` id idiom; engine's own
  two id sites passed because `engine/src` was blanket-allowlisted. Gate was
  simultaneously too strict and too loose.
- **`crypto.randomUUID` blind spot** — `ifid.ts:23` (legitimate), `tick-phases.ts:189`
  (per-event id).
- **Teaching surface** — `context.random.chance(0.5)`/`pick` in book §20 + snippet,
  family-zoo-tutorial ×4, devkit scaffold ×2, plus `troll-behavior.ts:75` (same shape).
- **AC-5 unreachable** — `SAVE_FORMAT_VERSION='2.0.0'` exact-match throw at
  `save-restore-service.ts:264-265`.
- **Multi-draw callers** — `melee.ts:213,232,449,450` confirmed; the cited
  `trollBehavior.ts:45,48` was a miscitation (actual: `troll-behavior.ts:75`, bare
  probability, single draw).
- **Catalog invariant** — ADR-internal consistency point, no tree contradiction.

### 2. Four rulings by David (all on the recommended option)
1. **D6 gate split**: strict path gate for `createSeededRandom(`; checked-in allowlist
   file for `Math.random` + `crypto.randomUUID` (only new entropy fails).
2. **Book edit inside Phase A's definition of done**: teaching surface rewritten to a
   named point rather than weakening D2.
3. **AC-5 via version bump**: `2.0.0` → `3.0.0` + the first real version-reader branch.
4. **`resolve()` ships in Phase A with real class labels** — the A/C boundary moves;
   D10's melee taxonomy lands in A; ~45 call sites change shape once; Phase C becomes
   engine/tooling-side.

### 3. ADR-293 Amendment A1 written and folded
Inline edits marked (A1) at D2 (catalog-global invariant), D6 (split gate + randomUUID),
D7 (3.0.0 reader), Touchpoints (devkit, family-zoo-tutorial, docs/book added), Phases
(A gains resolve()+labels+teaching surface; C re-cut to coverage/forcing/search), AC-6
(reworded). Full record in the ADR's Amendment A1 section, evidence inline and dated.

## Key Decisions
- **ADR-293 Amendment A1** — the four rulings above plus two corrections (randomUUID in
  the gate; catalog process-global invariant). See
  `docs/architecture/adrs/adr-293-choice-points-per-point-streams.md`.

## Next Phase
Phase A implementation can start: the platform discussion the ADR required has now
happened and its rulings are folded. Scope per the amended Phases section — substrate +
resolve() with real labels + save-format 3.0.0 reader + teaching-surface rewrite + split
gate in `./repokit verify`.

## Open Items
- Carried forward from audit: `action-interceptor.ts` hook signatures (`:345,371,393,436,473`)
  need the draw context threaded through in Phase A.
- `.current-plan` still points at the finished `tracker-low-hanging-fruit` plan — retarget
  via session-planner when Phase A implementation is planned.
- `IDebugEvent` dead tier, golden event logs, required `timestamp` smell,
  `ring-action.ts:141` bug — all unchanged from previous session's list.

## Files Modified
- `docs/architecture/adrs/adr-293-choice-points-per-point-streams.md` — Amendment A1
- this file

## Session Metadata
- **Status**: COMPLETE — all six items ruled/folded; documentation only, no code changed.
- **Blocker**: none.
- **Rollback Safety**: safe — documentation only.

## Mutation Audit
- N/A — no source code modified.

## Test Coverage Delta
- No tests run; no code changed.
