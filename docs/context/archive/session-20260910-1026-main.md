# Session Summary: 2026-09-10 - main

## Goals
- Implement ADR-344 plan Phase 5 (D7): make `ActorTrait.isPlayable` opt-in and have the loader write it from Chord's `playable` flag, so the role-holder guard's third condition goes live.

## Phase Context
- **Plan**: ADR-344 D1-D8 — the player role-holder invariant at the install seam (`docs/work/adr-344-role-holder-seam/plan.md`)
- **Phase executed**: Phase 5 — "`playable` reaches the runtime (D7, ADR-327 D9 amendment)" (Large)
- **Tool calls used**: 145 / 400
- **Phase outcome**: Completed on budget

## Completed

### D7 — `isPlayable` flips from default-true to opt-in
- `packages/world-model/src/traits/actor/actorTrait.ts:154` — class default `true` → `false`.
- `packages/story-loader/src/loader.ts:1633` — the `'person'` case now writes `new ActorTrait({ isPlayable: irEntity.isPlayable })`.
- `packages/helpers/src/builders/actor.ts` — new `.playable()` opt-in method; `build()` no longer adds a bare `ActorTrait`.
- All three shipped in one commit, as D7 requires: a flip without the write breaks every Chord protagonist, a write without the flip is a no-op.
- `switchPlayer`'s playability guard and `validate-role-holder`'s third condition are live for the first time (previously non-firing — see Recurrence/closed item below).

### ADR amendments
- `docs/architecture/adrs/adr-327-explicit-references.md` D9 — pointer amendment recording the loader write landed here, written in the same commit as the code.
- `docs/architecture/adrs/adr-344-player-role-holder-at-the-install-seam.md` — AC-5 marked MET with inline evidence; D7 gained a "site-list corrections" subsection.

### Opt-in sweep (18 sites) — two found only by confirming the plan's cited line numbers before editing
- Plan-named: `stories/concealment-test/src/index.ts:48`, `stories/armoured/src/index.ts:109`, `stories/family-zoo-tutorial/src/index.ts:268`, `stories/channel-service-test/src/playable-story.ts` (`ensurePlayerTraits`), `packages/devkit/fixtures/basic-story/src/index.ts:93`, and the six `packages/engine/tests/stories/*.ts` fixture stories.
- Found this session, not in the plan's list:
  - `stories/dungeo/src/index.ts` — hand-written `createPlayer` (`:683`) and `ActorTrait` construction (`:697`/`:740`) passing only `isPlayer: true`. Left alone the flagship story would fail install with `not-playable`.
  - `packages/helpers/tests/fixtures/external-story/story.js` — the REAL-PATH bundle fixture, whose protagonist is built through the documented author API (`actor('yourself').…build()`), exactly the case D7's builder paragraph predicts.
- Sixteen test-side role holders in `engine` (8 files, 23 failures), `bootstrap` (2), and the engine's shared fixtures (`setup-test-engine.ts`, `tests/fixtures/index.ts`) needed the same opt-in — each announcing itself as `RoleHolderValidationError … is not marked playable`, the guard doing its job. NPCs/bystanders in the same files (e.g. `sound/integration.test.ts:191`) were deliberately left non-playable.
- Two pre-existing assertions pinning the old default updated: `world-model/tests/unit/traits/actor.test.ts` (three), `engine/tests/unit/validate-role-holder.test.ts:38` (`'compliant'` shape now explicit both ways).

## Key Decisions

### 1. Confirm-before-edit caught two missing opt-in sites the plan's own measurement missed
Dungeo's player and the helpers REAL-PATH fixture were absent from ADR-344 D7's site list. Reading the cited line numbers before editing (the plan's own instruction) surfaced both before the gate would have; see plan.md Phase 5 progress notes for the full account.

### 2. A vitest pass is not evidence the bundle is correct
`pnpm --filter '@sharpee/helpers' test --run` passed before `./repokit build dungeo` and failed after it — vitest reads source, but that one test drives `dist/cli/sharpee.js`, which could not observe the flipped default until rebuilt. Fourth instance of "a check structurally incapable of observing what broke" in this plan (same class as the Phase 2 production defect this suite also caught). Written into Phase 6 as a lesson: run package suites *after* `./repokit build`, not only before.

### 3. David's ruling on the two unreachable stories (2026-09-10)
`branch-stories/secret-letter` is a shelved port (fails on `analysis.import-unresolved`, GH #352, unrelated to D7); `stories/armoured` is an old sample kept for early testing only, not a workspace member, no build path. Neither's inability to boot is a D7 gap. Both opt-ins landed and were verified by inspection only.

## Next Phase
- **Phase 6**: "Tree green end-to-end, D8/AC-7 confirmed, evidence compiled" — closure phase: full-tree `tsc --noEmit`, the AC-8 suites run together, one chain run, AC-7/D8 confirmed by reading `STORY_INSTALL_STEPS`, the nine-AC evidence table compiled, and a sweep of the remaining `new GameEngine({ ..., player })` test sites (I-86894c-1).
- **Tier**: Small (100 tool-call budget)
- **Entry state**: Phases 1-5 landed and each individually green (now true).

## Open Items

### Short Term
- I-86894c-1: The remaining `new GameEngine({ ..., player })` sites are all test files — 7 in `packages/story-loader/tests/`, 2 in `packages/engine/tests/sound/`, 1 in `packages/platform-browser/tests/capture-parity.test.ts`, plus `packages/story-loader/tests/helpers/boot-turns.ts`. Belongs in Phase 6's sweep.

### Long Term
- I-52e228-1: Which story/package tsconfig exclusions silently hide type errors from every existing gate — raised twice by pre-session-audit before this session; Phase 5's helpers real-path test (vitest green, bundle red) is a fourth data point for the same class of check-structurally-incapable-of-observing-what-broke defect.

## Files Modified

**Platform (D7 core)** (3 files):
- `packages/world-model/src/traits/actor/actorTrait.ts` - `isPlayable` default `true` → `false`
- `packages/story-loader/src/loader.ts` - `'person'` case writes `isPlayable` from `irEntity.isPlayable`
- `packages/helpers/src/builders/actor.ts` - new `.playable()` opt-in

**ADRs / plan** (3 files):
- `docs/architecture/adrs/adr-327-explicit-references.md` - D9 pointer amendment
- `docs/architecture/adrs/adr-344-player-role-holder-at-the-install-seam.md` - AC-5 MET, D7 corrections
- `docs/work/adr-344-role-holder-seam/plan.md` - Phase 5 DONE, Phase 6 CURRENT, inline progress/evidence

**Opt-in sites — stories/fixtures** (7 files):
- `stories/dungeo/src/index.ts`, `stories/armoured/src/index.ts`, `stories/concealment-test/src/index.ts`, `stories/family-zoo-tutorial/src/index.ts`, `stories/channel-service-test/src/playable-story.ts`, `packages/devkit/fixtures/basic-story/src/index.ts`, `packages/helpers/tests/fixtures/external-story/story.js`

**Opt-in sites — engine test stories/fixtures** (8 files):
- `packages/engine/tests/stories/{action-test-story,completion-test-story,complex-world-test-story,event-test-story,game-state-test-story,minimal-test-story}.ts`, `packages/engine/tests/test-helpers/setup-test-engine.ts`, `packages/engine/tests/fixtures/index.ts`

**Test-side role holders — engine, bootstrap** (14 files):
- `packages/engine/tests/{action-rng-determinism,unit/deadly-room,unit/player-death-routing,unit/presence-tagging,unit/validate-role-holder,integration/query-events}.test.ts`, `packages/engine/tests/sound/{engine-wiring,integration,listener-wiring}.test.ts`, `packages/bootstrap/src/{assemble-channels,assemble-restart}.test.ts`, `packages/world-model/tests/unit/traits/actor.test.ts`

**New tests** (3 files):
- `packages/story-loader/tests/adr-344-d7-playable-reaches-runtime.test.ts`
- `packages/engine/tests/unit/adr-344-d7-switch-player-playability.test.ts`
- `packages/helpers/tests/actor-builder-playable.test.ts`

**Build-generated, not hand-edited**: `stories/dungeo/src/version.ts`, `stories/armoured/src/version.ts`, `packages/sharpee/docs/genai-api/{authoring,index}.md` (picked up the new `.playable()` method via `./repokit build`).

**Not touched this session**: `docs/context/session-20260910-0610-main.md` shows modified in git status but is a pre-existing change carried in from the prior session.

## Notes

**Session duration**: ~4 hours (10:26-14:41 CDT approx, per exit evidence timestamps and session start)

**Approach**: Confirm every cited line number against current source before editing (per the plan's own instruction), rather than trusting the plan's site list — this is what caught the two missing opt-ins. Produced Behavior Statements for `StoryLoader.buildEntity` ('person' case) and `ActorBuilder.build()` before writing tests; `mutation-verification` graded all 16 new/updated assertions GREEN, confirmed live by deleting each mutation site and observing the suites catch the regression, and corrected the story-loader test's header comment (with the default now `false`, the load-bearing assertion is the positive case, not the negative one it originally claimed).

---

## Session Metadata

- **Session**: 52e228
- **Status**: COMPLETE
- **Blocker** (if any): N/A
- **Blocker Category**: N/A
- **Estimated Remaining**: N/A (Status is COMPLETE for this phase; Phase 6 remains, Small tier, ~100 tool-call budget, one session)
- **Rollback Safety**: safe to revert (worktree not pushed at session-write time)

## Dependency/Prerequisite Check

- **Prerequisites met**: Phases 1-2 landed (seam guard exists, engine constructs no player itself) so a protagonist the opt-in sweep missed fails loudly at install rather than misbehaving silently at `switchPlayer`. David approved this phase's platform touch (`packages/world-model`, `packages/story-loader`, `packages/helpers`) and the six-plus story/fixture edits, and the same-commit flip+write requirement per the ADR.
- **Prerequisites discovered**: None beyond the two missing opt-in sites, already covered under Key Decisions.

## Architectural Decisions

- ADR-344 D7: implemented — `isPlayable` opt-in, loader writes it from Chord's `playable`, AC-5 marked MET.
- ADR-327 D9: pointer amendment recording D7 completed the loader write scoped there; Status stays ACCEPTED.
- Pattern applied: same-commit flip+write per the ADR's "half-measure to avoid."

## Mutation Audit

- Files with state-changing logic modified: `packages/world-model/src/traits/actor/actorTrait.ts` (default value), `packages/story-loader/src/loader.ts` (trait construction from IR), `packages/helpers/src/builders/actor.ts` (builder opt-in flag)
- Tests verify actual state mutations (not just events): YES (evidence: `pnpm --filter '@sharpee/world-model' test --run` → 86 files, 1516 passed, 0 failed, 2026-09-10 10:33-10:41 CDT; `pnpm --filter '@sharpee/story-loader' test --run` → 123 files, 1111 passed, 0 failed; `pnpm --filter '@sharpee/engine' test --run` → 82 files, 776 passed, 0 failed, 7 skipped; `pnpm --filter '@sharpee/helpers' test:ci` → 5 files, 17 passed, 0 failed — all run same window; `mutation-verification` additionally confirmed by live-deleting each mutation site and observing the suites catch the regression)
- If NO: N/A

## Recurrence Check

- Similar to past issue? YES — same class as `I-71ed1a-3` (GH #391, incremental CJS tsc leaves stale `.d.ts` trusted as fresh) and `I-7776c8-1` (GH #398, real-path test gates use artifact PRESENCE as a proxy for FRESHNESS, host-conditioned false green). This session's finding (`pnpm --filter '@sharpee/helpers' test --run` green before `./repokit build dungeo`, red after) is the fourth instance of "a check structurally incapable of observing what broke" surfaced across this plan and recent sessions. Opened as `I-52e228-1` for the systemic audit; the immediate mitigation (run package suites after the build) is written into Phase 6.
- If YES: Consider one-time audit of which suites read `dist/` vs source, and which gates trust artifact presence over artifact freshness.

## Test Coverage Delta

- Tests added: 12 new (4 story-loader, 4 engine, 4 helpers) + 1 new describe block in world-model's existing `actor.test.ts` (assertion count within that file grew; file itself pre-existing)
- Tests passing before: story-loader 1107, helpers 13 → after: story-loader 1111, helpers 17 (evidence: exit-evidence runs above, 2026-09-10 10:33-10:41 CDT). World-model and engine suite totals (1516 and 776) are absolute counts, not deltas against a recorded pre-session baseline for this phase.
- Known untested areas: `branch-stories/secret-letter` and `stories/armoured` opt-ins verified by inspection only (both are out of the active/buildable tree by intent, per David's 2026-09-10 ruling — not a gap).

---

**Progressive update**: Session completed 2026-09-10 14:41

## Activity Log (auto-captured)
```
[15:28:07] BUILD: Build passed — cd /Users/david/repos/sharpee
echo "=== hand-written protagonist sites ==="
for 
[15:30:54] BUILD: Build passed — cd /Users/david/repos/sharpee
python3 - <<'PY'
import pathlib

# (path, old, new
[15:33:17] BUILD: Build passed — pnpm --filter '@sharpee/world-model' test --run 2>/dev/null | tail -25
[15:33:25] BUILD: Build passed — pnpm --filter '@sharpee/helpers' test --run 2>/dev/null | tail -20
[15:33:38] BUILD: Build failed — pnpm --filter '@sharpee/story-loader' test --run 2>/dev/null | tail -30
[15:34:03] BUILD: Build passed — pnpm --filter '@sharpee/story-loader' test --run 2>/dev/null | tail -8
[15:34:10] BUILD: Build failed — pnpm --filter '@sharpee/engine' test --run 2>/dev/null | grep -E "×|✗|FAIL|Test 
[15:34:20] BUILD: Build failed — cd /Users/david/repos/sharpee
pnpm --filter '@sharpee/engine' test --run tests/u
[15:34:46] BUILD: Build passed — pnpm --filter '@sharpee/engine' test --run 2>/dev/null | grep -E "×|✗|FAIL|Test 
[15:35:31] BUILD: Build passed — cd /Users/david/repos/sharpee
for pkg in bootstrap devkit platform-browser chara
[15:36:39] BUILD: Build passed — pnpm --filter '@sharpee/story-loader' test adr-344-d7-playable-reaches-runtime 2
[15:36:41] BUILD: Build passed — pnpm --filter '@sharpee/helpers' test actor-builder-playable 2>&1 | tail -30
[15:36:45] BUILD: Build passed — pnpm --filter '@sharpee/engine' test adr-344-d7-switch-player-playability 2>&1 |
[15:36:47] BUILD: Build passed — pnpm --filter '@sharpee/world-model' test actor.test 2>&1 | tail -40
[15:37:06] BUILD: Build failed — cd /Users/david/repos/sharpee
pnpm --filter '@sharpee/story-loader' test adr-344
[15:37:20] BUILD: Build passed — cd /Users/david/repos/sharpee
pnpm --filter '@sharpee/story-loader' test adr-344
[15:37:31] BUILD: Build passed — cp /tmp/actor-builder.ts.bak /Users/david/repos/sharpee/packages/helpers/src/bui
[15:38:21] BUILD: Build passed — cd /Users/david/repos/sharpee
pnpm exec turbo run test:ci 2>/dev/null | tail -15
[15:38:58] BUILD: Build failed — cd /Users/david/repos/sharpee
pnpm --filter '@sharpee/helpers' test:ci 2>/dev/nu
[15:41:00] BUILD: Build passed — cd /Users/david/repos/sharpee
pnpm exec turbo run test:ci 2>/dev/null | tail -8
[15:41:21] BUILD: Build passed — cd /Users/david/repos/sharpee
for s in armoured concealment-test family-zoo-tuto
[15:41:30] BUILD: Build passed — cd /Users/david/repos/sharpee
pnpm --filter '@sharpee/story-armoured' build 2>/d
[15:47:31] EDIT: File written — /Users/david/.claude/projects/-Users-david-repos-sharpee/memory/project_armoured_is_a_retired_sample.md
[15:55:16] EDIT: File written — docs/context/session-20260910-1026-main.md
[15:57:36] EDIT: File written — .commit-files
[15:57:44] EDIT: File written — .commit-msg
[15:57:49] GIT: Git operation — bash $HOME/.claude/scripts/git-commit.sh --push
```
