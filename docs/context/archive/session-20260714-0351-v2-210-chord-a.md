# Session Summary: 2026-07-13 (CST) - v2-210-chord-a

## Goals
- Resolve ADR-212 and ADR-213 open questions via `/devarch:adr-interview` and flip both to ACCEPTED.
- Re-cut `chord-zoo-surfaces` Phase 3 against the accepted ADRs.
- Draft and transcribe the Z6 (`remove <entity>`) ratchet entry.
- Plan the `chord-212-213-seams` implementation package.

## Phase Context
- **Plan**: chord-211-core (`.current-plan`, untouched this session — no code, no phase execution).
- **Phase executed**: None. This session did planning/ADR work adjacent to `chord-211-core`, not one of its phases.
- **Tool calls used**: 127 (no tier/budget applies — planning session, not a phase execution).
- **Phase outcome**: N/A — no plan phase was executed or advanced.

## Completed

### ADR-212 (slot contributions as data) — DRAFT → ACCEPTED
David resolved Q1–Q4 in interview: predicate-gate seam ships now as a two-member `SlotEntryGate` union (`owner-present` | `predicate`); the registry lives in the engine prose pipeline, not stdlib (stdlib rejected — dependency inversion); friendly-zoo's hand-rolled slot-contributor closure migrates to `registerSlotEntry` entries, done inside ADR-212's own implementation package; any slot key is accepted, with gate semantics carried on the entry rather than enforced by key. Post-flip `adr-review` scored 15/15 READY FOR IMPLEMENTATION.

### ADR-213 (removed-from-play signal) — DRAFT → ACCEPTED
David resolved Q1–Q4: a pre-deletion observer fires with the still-live entity; the seam is observer-only (no `if.event.removed` event type); the Chord `remove <entity>` statement is minted now (the one deviation from the draft's "defer" recommendation), riding `chord-zoo-surfaces` Phase 3 as its own ratchet entry; orphaning via `moveEntity(id, null)` stays unsignaled, pinned as new AC-7. Flipped to ACCEPTED; `adr-review` scored 15/15 READY. Review surfaced an implementation note about `removeEntity`'s internal null-move that was later found stale (see Key Decisions #3).

### chord-zoo-surfaces Phase 3 re-cut (2026-07-14)
Phase 3 now compiles `phrase present:` to `engine.registerSlotEntry` (ADR-212, no synthesized closures) and rides ADR-213's single loader-registered `onEntityRemoved` observer for the `disappeared` channel; it gains the `remove <entity>` statement as a deliverable. New hard prerequisite recorded: the ADR-212/213 implementation package must land first. The former disappeared-channel stop-and-discuss is closed by decision. `plan-review` reported TENSIONS ONLY (3 advisories: stale ratchet-log mechanism sentence, transcribe-before-phase ordering, superseded platform-touch forecast) — no contradictions.

### Z6 ratchet entry — drafted and TRANSCRIBED
`remove <entity>` statement row drafted in `docs/work/chord-zoo-surfaces/ratchet-drafts.md` (addendum), then transcribed into `docs/architecture/chord-grammar-changes.md` (dated 2026-07-14) on David's explicit "transcribe" instruction. Includes David's approval of two sub-decisions: D7 (when-suffix) and D13 (sequence-kit membership), plus the `analysis.remove-player` load error.

### chord-212-213-seams package planned
New plan at `docs/work/chord-212-213-seams/plan.md` via `session-planner`, 3 phases: P1 engine slot-entry registry (Medium/250, ADR-212 AC-2/3/4/5/7); P2 world-model removal observer seam (Small/100, ADR-213 AC-1/5/7); P3 friendly-zoo closure→4-entries migration + combined byte-identical regression gate (Medium/250, ADR-212 AC-1 + ADR-213 AC-4, zoo + dungeo chains). Confirmed fully independent of `chord-211-core` (no file overlap) but a hard prerequisite for `chord-zoo-surfaces` Phase 3. `.current-plan` deliberately left pointing at `chord-211-core`. Investigation findings baked into the plan: registrations are session-lifetime (in-game RESTORE never re-invokes `onEngineReady`, shaping the AC-4 counter test); counter keying is the caller's responsibility (defensive `console.warn`, not enforcement); zoo's migration content is Literal, so AC-4 Choice coverage needs a separate fixture.

## Key Decisions

### 1. ADR-212 registry ownership — engine, not stdlib
Q2 resolved that the declarative `(slotKey, owner)` entry registry lives in the engine prose pipeline beside the existing `SlotContributor` staging pass; stdlib was explicitly rejected to avoid dependency inversion (see `packages/engine/src/prose-pipeline/`).

### 2. ADR-213 statement timing — mint `remove` now, not later
Q3 broke from the ADR-213 draft's own recommendation to defer the Chord `remove <entity>` statement; David chose to mint it immediately, landing it as a `chord-zoo-surfaces` Phase 3 deliverable and its own Z6 ratchet entry rather than a future ratchet.

### 3. ADR-213 §1 correction — dead-code implementation note fixed
Plan-review on the seams plan found ADR-213's §1 implementation note (added during ADR-213's own adr-review pass) incorrectly described `removeEntity`'s internal `moveEntity(id, null)` as live; it is dead code — `SpatialIndex.remove` clears the parent pointer first (verified at `SpatialIndex.ts:39-44`). Per David's instruction, ADR-213 §1 and its Terminology section were corrected: the seam rule is capture-and-invoke the observer BEFORE `spatialIndex.remove` runs; the dead branch is left in place but flagged. The seams plan is now conflict-free against the corrected ADR.

## Next Phase
- No `chord-211-core` phase was touched or advanced this session; it remains CURRENT per `.current-plan`.
- David has two independent starting points for the next session: `chord-211-core` Phase 1, or `chord-212-213-seams` Phase 1 (engine slot-entry registry, Medium/250) — order does not matter, neither blocks the other.
- `chord-zoo-surfaces` Phase 3 rides both `chord-211-core` (adverbs/gate seam) and `chord-212-213-seams` (registry + observer) as prerequisites; it cannot start until both packages have landed.

## Open Items

### Short Term
- David to pick which package starts next: `chord-211-core` Phase 1 or `chord-212-213-seams` Phase 1.
- `chord-212-213-seams` Phase 1 implementer must read the actual `SlotEntry`/`SlotEntryGate` shapes against ADR-212 §1-2 verbatim before wiring (plan calls this out explicitly).

### Long Term
- `chord-zoo-surfaces` Phase 3 cannot begin until both `chord-211-core` and `chord-212-213-seams` have landed and friendly-zoo's closure has migrated to 4 `registerSlotEntry` entries.
- The two zoo-surfaces plan-review advisories (stale ratchet-log mechanism sentence, transcribe-before-phase ordering) remain open as low-priority tensions, not blockers.

## Files Modified

**ADRs** (2 files):
- `docs/architecture/adrs/adr-212-slot-contributions-as-data.md` - Q1-Q4 resolved, DRAFT → ACCEPTED.
- `docs/architecture/adrs/adr-213-removed-from-play-signal.md` - Q1-Q4 resolved, DRAFT → ACCEPTED, §1/Terminology corrected post-plan-review.

**Ratchet log** (1 file):
- `docs/architecture/chord-grammar-changes.md` - Z6 (`remove <entity>`) entry transcribed, dated 2026-07-14.

**Plans** (3 files):
- `docs/work/chord-zoo-surfaces/plan.md` - Phase 3 re-cut against ADR-212/213, new hard prerequisite recorded.
- `docs/work/chord-zoo-surfaces/ratchet-drafts.md` - Z6 addendum drafted.
- `docs/work/chord-212-213-seams/plan.md` (new) - 3-phase implementation plan for both ADRs.

## Notes

**Session duration**: single sitting, 127 tool calls, docs/planning only — no source files touched.

**Approach**: Two sequential `/devarch:adr-interview` runs (ADR-212 then ADR-213), each followed by an automatic `adr-review` re-score; then a plan re-cut, a ratchet transcription on explicit instruction, a `session-planner` run for the new seams package, and a `plan-review` pass that caught one genuinely stale ADR implementation note (dead-code claim), corrected per David's direction.

---

## Session Metadata

- **Status**: COMPLETE
- **Blocker** (if any): N/A
- **Blocker Category**: N/A
- **Estimated Remaining**: N/A
- **Rollback Safety**: safe to revert (docs-only changes, nothing merged to main)

## Dependency/Prerequisite Check

- **Prerequisites met**: ADR-212/213 open-questions interviews required both ADRs in DRAFT state with non-empty Open Questions sections — both were, from prior sessions.
- **Prerequisites discovered**: `chord-zoo-surfaces` Phase 3 now hard-depends on the new `chord-212-213-seams` package landing first (not previously recorded before this session).

## Architectural Decisions

- ADR-212 (slot contributions as data): ACCEPTED — declarative `(slotKey, owner)` registry in the engine prose pipeline; friendly-zoo's closure migrates to entries.
- ADR-213 (removed-from-play signal): ACCEPTED — pre-deletion observer seam at the `WorldModel.removeEntity` choke point, observer-only, orphaning unsignaled (AC-7).
- Pattern applied: registered-gate-seam and observer-seam patterns both extend existing platform choke points (prose pipeline staging, `removeEntity`) rather than inventing new mechanisms — consistent with CLAUDE.md's "trust the architecture" checklist.

## Mutation Audit

N/A — documentation/ADR/planning session only, no code changed.

## Recurrence Check

- Similar to past issue? NO — new pattern this session (stale ADR implementation note caught by `plan-review`, not a recurrence of a previously logged blocker category).

## Test Coverage Delta

No test changes this session (docs/planning only).

---

**Progressive update**: Session completed 2026-07-14
