# Session Summary: 2026-06-26 - main (session ea7f6c)

## Goals
- Accept ADR-192 (PROPOSED → ACCEPTED) and execute Phases 1–3a of the phrase-algebra rollout.
- Scope the remaining Phase 3b+4 work before committing to it.

## Phase Context
- **Plan**: `docs/context/plan.md` — ADR-192 phrase-algebra implementation
- **Phases executed**: Phase 1 (if-domain contracts), Phase 2 (English Assembler + foundational realization), Phase 3a (parser + nounPhraseFor additive half)
- **Tool calls used**: 272
- **Phase outcome**: Phases 1, 2, 3a completed and merged to main. Phase 3b+4 scoped but NOT started — session paused at a decision gate.

## Completed

### ADR-192 accepted
Promoted `adr-192-phrase-algebra-phrase-model-assembler-core.md` from PROPOSED to ACCEPTED before cutting any phase branch.

### Phase 1 — if-domain Phrase contracts (`bfe796c1`)
12-member `Phrase` union declared in `packages/if-domain/src/phrase.ts`; 74-test contract suite passing.

### Phase 2 — English Assembler + foundational realization (`37756e67`, NUL-byte fix `3ef443ad`)
`english-assembler.ts` wired to the Phrase union; `assembler/errors.ts` and barrel exports added; 299 lang-en-us tests green.

### Phase 3a — additive parser half (`499cdd9f`, `a35aa770`)
`parsePhraseTemplate` + `PhraseParseError` in `packages/lang-en-us/src/parser/`; `nounPhraseFor` utility + `NounPhrase.capitalize` carrier in `packages/stdlib/src/utils/noun-phrase.ts`; stdlib suite 1300 tests green.

### Phase 3b+4 scope document
Full scope written to `docs/work/dynamic-text/phase-3b-4-scope.md`: 418 `:` -chain templates across 46 files, 236 `entityInfoFrom` call sites across 41 files, integration point at engine/prose-pipeline, LanguageProvider as the single runtime formatter consumer, perspective pre-pass design, article-rendering.transcript as regression sentinel.

## Open Decision Gate (WHY WE PAUSED)

**This is the primary reason the session is BLOCKED. Next session must resolve both before branching.**

### Decision 1 — Verb agreement (BLOCKER)
52 templates use `{is:target}`, `{was:}`, `{has:}` constructs. These are not in ADR-192's closed 12-member union, and the `:` inside `{is:target}` causes `parsePhraseTemplate` to **throw** today. Three options:

- **(A, recommended)** A transitional string pre-pass that resolves `{is:x}` → `is`/`are` from `params[x].number` before handing off to `parsePhraseTemplate`. No new ADR needed; scoped to a single helper.
- **(B)** Add a `Verb` kind to the algebra now, requiring a follow-on ADR before Phase 3b can start.
- **(C)** Defer behind a temporary shim/dispatch seam, deferring the decision further.

User wants to choose before implementation begins. **Discussion pending.**

### Decision 2 — Appetite / sequencing
Phase 3b+4 is an atomic cutover by ADR-192 design (dual pipeline forbidden). The full branch (`v2_phase34`) must be fully green — build + all suites + dungeo walkthrough chain — before merging to main. Options: de-risk by doing one vertical-slice action first as a proof-of-concept, vs. committing to the full 650-edit single-branch push.

## Next Phase
- **Phase 3b+4**: Destructive + integration cutover on branch `v2_phase34`.
- **Entry state**: Decision 1 (verb agreement) and Decision 2 (appetite) both resolved by user; `v2_phase34` not yet created.

## Open Items

### Short Term
- Resolve verb-agreement option (A/B/C) with user.
- Decide on vertical-slice vs. full cutover sequencing.
- Branch `v2_phase34` from main and begin cutover only after both decisions.

### Long Term
- D3 design doc (`docs/work/dynamic-text/phrase-algebra-design.md`) amend question — still pending user answer.
- Three 0849-session manual items: 1.1.x republish, bundle rebuild, dungeo SceneryTrait alignment (#170).

## Files Modified

**if-domain** (2 files):
- `packages/if-domain/src/phrase.ts` — 12-member Phrase union
- `packages/if-domain/src/index.ts` — barrel export
- `packages/if-domain/tests/phrase/phrase-contracts.test.ts` — 74-test contract suite

**lang-en-us assembler** (4 files):
- `packages/lang-en-us/src/assembler/english-assembler.ts`
- `packages/lang-en-us/src/assembler/errors.ts`
- `packages/lang-en-us/src/assembler/index.ts`
- `packages/lang-en-us/tests/assembler/english-assembler.test.ts`

**lang-en-us parser** (3 files):
- `packages/lang-en-us/src/parser/parse-phrase-template.ts`
- `packages/lang-en-us/src/parser/index.ts`
- `packages/lang-en-us/tests/parser/parse-phrase-template.test.ts`

**stdlib utils** (3 files):
- `packages/stdlib/src/utils/noun-phrase.ts`
- `packages/stdlib/src/utils/index.ts`
- `packages/stdlib/tests/unit/utils/noun-phrase.test.ts`

**Docs / ADR** (3 files):
- `docs/architecture/adrs/adr-192-phrase-algebra-phrase-model-assembler-core.md` — PROPOSED → ACCEPTED
- `docs/context/plan.md` — phase status updated
- `docs/work/dynamic-text/phase-3b-4-scope.md` — new scope document

## Notes

**Session duration**: ~1.5 hours of active implementation across three merged PRs, plus scoping work.

**Per-phase detail**: See `session-20260626-1705-v2_phase1.md`, `-1716-v2_phase2.md`, `-1746-v2_phase3.md` for implementation specifics.

**Key scoping insight**: `{the-list:}` and `{count:}` have zero real usages — both are safe to exclude from Phase 3b scope. Perspective resolver (`{You}` / `{take}`) is confirmed as a pre-pass layer sitting above `parsePhraseTemplate`, not a Phrase kind.

---

## Session Metadata

- **Status**: BLOCKED
- **Blocker**: Architecture — verb agreement (`{is:target}` etc.) is not representable in the current closed Phrase union; `parsePhraseTemplate` throws on the `:` separator. 52 templates affected. Option selected by user required before Phase 3b can begin.
- **Blocker Category**: Architecture
- **Estimated Remaining**: ~2–4 hours across 1 session (once decision is made; most work is mechanical)
- **Rollback Safety**: safe to revert (all merged commits are additive; Phase 3b not started)

## Dependency/Prerequisite Check

- **Prerequisites met**: ADR-192 accepted; Phases 1, 2, 3a merged and green; scope document written.
- **Prerequisites discovered**: Verb-agreement construct family (`{is:}`, `{was:}`, `{has:}`) requires a design decision not covered by ADR-192 before Phase 3b can proceed.

## Architectural Decisions

- ADR-192: PROPOSED → ACCEPTED this session.
- No new ADRs written. Option B (add Verb kind) would require a follow-on ADR; Options A and C would not.

## Mutation Audit

- Files with state-changing logic modified: `english-assembler.ts`, `parse-phrase-template.ts`, `noun-phrase.ts`
- Tests verify actual state mutations (not just events): YES — assembler tests assert on rendered string output; parser tests assert on returned `Phrase[]`; noun-phrase tests assert on formatted string result.

## Recurrence Check

- Similar to past issue? NO — verb agreement gap is new; prior sessions (0849 summary) did not encounter `{is:}` template throws.

## Test Coverage Delta

- Tests added this session: ~130 (74 phrase-contracts + new assembler + parser + noun-phrase tests)
- Suites passing before: if-domain 0, lang-en-us ~169, stdlib ~1270 (estimated baselines)
- Suites passing after: if-domain 74, lang-en-us 299, stdlib 1300
- Known untested areas: `{is:target}` / verb-agreement path (untestable until Decision 1 is resolved)

---

**Progressive update**: Session ended 2026-06-26 ~18:30 CST — paused at decision gate
