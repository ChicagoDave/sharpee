# Session Summary: 2026-08-03 (night) - main (session a17580)

## Goals
- Continue ADR-299 (play–skein–bless): Phase 5 (Replay Driver — Swift, D6/D5 forced branches, real-path gate) per
  `docs/work/ide-adr299-play-skein-bless/plan-20260803-ide-adr299-play-skein-bless.md`.

## Completed
- **Phase 5 (Replay Driver, D6/D5) — COMPLETE**:
  - `tools/ide/SharpeeIDE/Skein/ReplayDriver.swift` — root→node re-execution at the skein's pinned seed: thread serialized
    as a `[SKIP]` transcript (title/`seed:`/joined `forces:` headers + opening `[SKIP]`'d `look`, emitted through
    `RecordingSession.serialize` — one grammar home), run via `sharpee test <story> <tmp>.transcript --json --capture-output`
    on the existing `TestRunner` spawn/NDJSON machinery; `command-result` records map back to nodes offset by the opening
    turn. Typed refusals: `unknownNode`, `duplicateForcing` (pre-spawn, ADR-293 D9), `replayInFlight`, `cliFailed` (exit≠0
    incl. the unfired-forcing D8/D9 hard failure — outputs withheld), `outputMismatch`, `missingOutput`.
  - `SkeinDocument.forcedSibling(of:forcings:)` (Skein.swift) — D5 model op: first-class sibling branch (same command,
    forcing annotations, empty output until replay); nil + untouched tree for unknown/root/empty-forcings.
  - `RecordingSession.serialize` static gained `headerFields:` (internal) — replay now, Phase 9 exporter next.
  - Tests: `ReplayDriverTests` (12 unit — synthesis/join/mapping/refusals), `ReplayRealPathTests` (3 real-path, rule 13a):
    AC-2 — real WKWebView plays a real fernhill bundle (built in-test by the real devkit CLI), typed turns through the
    client's own input, skein grows on disk, ReplayDriver replays byte-identical incl. an RNG-drawing turn
    (`stdlib.inventory.empty-variant`); AC-4 — forced sibling replays reproduce forced `stdlib.throwing.*` outcomes on a
    dedicated fixture story (never a real story); unfired forcing fails loudly.
  - Gates: **full IDE suite 469 tests, 0 failures** (21:42); devkit 117 passing, 1 skipped.

- **Mutation-verification follow-up (3 gaps closed, each falsified before acceptance)**:
  - `ReplayDriver.finish()`'s temp-dir cleanup had no assertion (and is deliberately `try?`, so a dropped call would be
    silent). Added `testAReplayLeavesNoScratchDirectoryBehindOnEitherPath` — counts `SharpeeIDE-Replay-*` dirs before/after
    a real replay on BOTH the success and failure paths. Falsified: stubbing out the `removeItem` fails both assertions,
    naming the leaked directories.
  - `browser-build.test.ts` (the TS/non-chord path) asserted only that `game.js` existed — the identical top-level-`seed`
    regression could ship there unseen. Added `__SHARPEE_PLAY_SEED__` + `/config:\s*\{\s*seed:/` content assertions.
    Falsified: reverting the template to top-level `seed` fails it.
  - `chord-build.test.ts`'s regex covered only the GameEngine sink; the `createStory` seed (chord evaluator stream) had no
    distinct guard. Added a structural, minifier-name-agnostic `/hatchModules:[^}]*\{\s*seed:/`. Falsified: dropping the
    `createStory` seed fails it.
- **Cancel-test timeout bumped 5s→30s** (`TestRunnerTests.testCancelTerminatesAndKeepsDecodedRecords`), applying David's
  standing ruling on recurrence. It failed in two of three full-suite runs on the HOST this session while passing targeted
  in 0.109s. Mechanism identified: the SIGKILL escalation is a main-runloop `Timer`, and Phase 5 added four
  subprocess-spawning tests, so a busy main actor delays it past the 2s backstop. Evidence recorded inline at the call site.

- **Platform bugs found by AC-2 and fixed (packages/devkit templates — within Phase 2's David-approved seed-injection scope)**:
  1. Both browser entry templates passed `seed` at the TOP level of `GameEngine` options, where it is silently ignored
     (the seed rides `EngineConfig` — `options.config.seed`). Play's engine was clock-seeded despite the injected global;
     Phase 2's gates missed it because the devkit regression asserted only hook PRESENCE and the IDE D5 test asserted only
     the page-visible global. Fixed: `config: { seed: playSeed }`; chord-build regression now pins the compiled
     `config:{seed:` shape.
  2. The chord entry omitted `seed` from `createStory`, leaving chord `randomly`/`one chance in` draws (fernhill's
     night-wind/bell atmospherics) clock-seeded even with the engine pinned — the exact bug author-game.ts fixed headless
     (its comment says so). Fixed via `pinnedPlaySeed()` threaded into `createStory` options.
- Test-host findings encoded in the tests: WKWebView `innerText` is empty for unrendered (windowless) content — probe
  `textContent`; `audioContext.resume()` hangs in the sandboxed test host — remove the ctor so `unlock()` takes the
  client's own production no-AudioContext fallback.

- **Phase 6 (Skein View — D8 tree surface) — COMPLETE except AC-4's live half (BLOCKED)**:
  - `SkeinView` (NSOutlineView, TestPanelView precedent): thread tree with per-row command, blessing scope, forcings, tags,
    lock, annotation, and a ▶ marker for where play sits; the document root is the trunk, not a row. New **Skein** tab in
    `RightPanelViewController`, which also hosts the tag/force sheets and the replay action.
  - Model: `SkeinDocument.updateNode(withId:_:)` (the one per-node mutation door, for Phases 7–8 too);
    `SkeinSession.setTags` / `growForcedSibling` / `moveTo`, each persisting through `SkeinStore`.
  - `PlayViewController.replay(toNodeId:)` — click-to-replay.
  - **Deliberate deviation from the plan's wording**: replay drives the LIVE client, not `ReplayDriver`. D1/D6 need the
    story live at the node and a headless run cannot do that; a fresh boot at the pinned seed plus the thread's commands
    is the only mechanism that can, and Phase 5's AC-2 pins it byte-identical. `ReplayDriver` stays the headless
    instrument Phase 7 needs (reading a thread without disturbing play).
  - **Production bug caught by the real-path test**: `replay()` probed readiness after `reloadFromOrigin()`, but the
    reload is asynchronous and the OUTGOING page satisfies every readiness probe until replaced — so the replay typed
    into the old world (the page showed "You already have the glass bottle"). Fixed with an outgoing-document stamp
    (`__sharpeeStalePage`) whose absence is the only reliable "this is the fresh page" signal; same guard in the test's
    restart helper. Suite time for that file fell 33s → 2s once the 30s timeouts stopped firing.
  - Tests: `SkeinViewTests` (16 unit), `SkeinReplayRealPathTests` (2 real-path passing — replay leaves the story live AND
    the next typed command branches from the replayed node, which is the actual proof of "live"; refused replay leaves
    play alone). Gate: **full IDE suite 488 tests, 1 skipped, 0 failures** (22:57).

- **Live forcing hook (platform, David-approved mid-session) — DONE, Phase 6's blocker cleared**:
  - `packages/devkit` both browser entry templates: `window.__SHARPEE_PLAY_FORCES__` →
    `engine.getRandomService().clearForces()/loadForces(...)`, applied before any turn (including the client's own boot
    `look`). The wire carries **already-structured `RandomForceSpec` objects, never the `point[#occurrence]=CLASS` header
    text** — parsing that grammar belongs to transcript-tester, and a second copy shipped inside every built page is a
    copy that drifts. A load error logs to the page console rather than blanking the boot; a published page never defines
    the global. Forces are session state (ADR-293 D9), so they load per boot and survive restart reboots.
  - `tools/ide/SharpeeIDE/Skein/Forcing.swift` — the one Swift home for the grammar (`parse`, `playSpec`).
    `RightPanelViewController.isWellFormedForcing` now delegates to it, so the Force sheet cannot answer "is this valid?"
    differently from the code that later hands the same annotation to the engine.
  - `PlayViewController.pendingForcings` rides the injected user script, set from the replayed thread and **cleared by
    "new thread from root"** — a leftover force silently bending an ostensibly fresh playthrough is the worst failure
    mode here, so it has its own assertion.
  - Verified by falsification: disabling the template's `applyPinnedPlayForces` call makes the live forced-replay test
    fail. devkit regressions now assert both the global AND the `loadForces(` call in the built bundle, on both templates.
  - Gates: **full IDE suite 490 tests, 0 skipped, 0 failures** (23:10); devkit 117 passing, 1 skipped;
    `npx tsc --noEmit` exit 0.

## Carryover
- Phase 7 (Transcript View + scoped blessing verification, D8/D3/D4) is next — not blocked.
- Prior carryover stands: live IDE check of the ADR-298 fix (David-side); optional StoryHomeTests template-mimic cleanup.
- **For David**: the two seed-plumbing bugs mean Play has NOT actually been running at the skein's pinned seed since
  Phase 2 — worth knowing before trusting any skein grown before this session. Existing `.skein` files
  (`stories/fernhill/play-testing/`) hold outputs captured at clock seeds; their stored prose will not match a replay.

## Key Decisions
- AC-2's capture side is a genuinely live WKWebView session of a real built bundle (not a captureResponses stand-in) —
  which is what caught both seed-plumbing bugs; the byte-identity claim now spans live-browser capture vs headless replay
  with an RNG draw on the thread.
- AC-2 pins the skein seed (12345, pre-written skein) for reproducible failures, per the repo's pinned-seed discipline.

## Session Metadata
- **Status**: IN PROGRESS (Phases 1–5 complete, Phase 6 Skein View next)
- **Blocker**: N/A
- **Rollback Safety**: safe — tools/ide additions; packages/ touch is the two entry templates (seed plumbing fix, inert
  without the IDE's injected global) + one strengthened devkit test assertion
