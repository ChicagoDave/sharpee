# Session 2026-07-10 13:39 — v2-210-chord-a

## Goal

Chord Phase A (ADR-210, ACCEPTED): Cloak-complete core — @sharpee/story-lang parser/IR + story-loader subset. Gates AC-1/3/5/6/10. Branch created from main (a11a38d2). Plan first (session-planner), then implementation on David's confirmation of the plan.

## Status

IN PROGRESS — plan approved (3 tensions resolved, package names decided), **Phases 1 and 2 COMPLETE**. Phase 1: packages/chord + packages/story-loader scaffolded and building (cjs+esm), registered in repokit PLATFORM_PACKAGES (chord before ide-protocol for the Phase 3 IR re-export; story-loader after engine; repokit rebuilt, 25 tests green), chord-grammar-changes.md governance log, golden Cloak transcript suite (6 files, FROZEN — David approved both gate semantics: stumble-on-entry, drop-in-dark-bar allowed) + divergences.md + ac5-fixture-spec.md. Phase 2: lexer/AST/parser (packages/chord/src: span, diagnostics, lexer, ast, parser), 22 tests green (cloak.story §3.1 verbatim zero-diagnostics, ac5-random.story authored, 4 malformed-recovery fixtures), grammar notation doc at docs/reference/chord-grammar.md (living EBNF per David's follow-along request). **Phase 3 COMPLETE**: ir.ts (`story language 1` versioned wire types), catalog.ts (closed vocab: 5 kind nouns, 12 trait adjectives, event verb `enters` — names only), analyzer.ts (two-pass resolver: exact→alias→unique-subset with never-guess ambiguity errors; six AC-3 gates each tested for exact code+line+suggestion; descriptions/overrides → derived phrase keys `<id>.description`). 44 chord tests green (cloak IR snapshot, JSON round-trip, gate fixtures under tests/fixtures/gates/). ide-protocol re-exports IR schema (story-ir.ts, chord workspace dep + tsconfig reference), 11 tests green. Grammar doc gained analyzer readings 8–12. **Phase 4 COMPLETE**: story-loader loader.ts+errors.ts (ChordStory generic Story: two-pass world build via explicit createHelpers, prereqs-§5 kind mapping, bidirectional connectRooms, blocked exits with phrase text, chord.state.* seeding, default player yourself/self/me/myself + worn-item wiring, extendLanguage registration with `{variants}` templates for strategy phrases, hang→PUT_ON vocabulary, hatch binding via host-supplied hatchModules so the loader stays fs-free/browser-safe). if-domain endings.ts = the one approved wire type (StoryEndingEvents story.victory/story.defeat, STORY_ENDING_FLAG 'story.ending', IStoryEndingData). Extra platform touch (packaging only, flagged to David): @sharpee/helpers exports gained a `default` condition (was CJS-require-only, unresolvable from vitest ESM). 17 loader tests green (state-asserting per Behavior Statement); commit 0cf7f0d2 covers Phases 1-3. Deferred to Phase 5: dark-while initial eval + turn-end rule; whether `on reading it` targets need ReadableTrait for the reading action to validate. **Phase 5 COMPLETE** (commit pending): evaluator.ts (closed-selector AST walk; `one chance in n` via SeededRandom with cursor persisted at chord.rng → AC-5/AC-6), runtime.ts (when-rules → keyed chainEvent registrations + fireRules test entry; ordinal counters chord.occurrence.*; dark-while → initial eval in createPlayer + recompute chains on possession/movement triggers; on-clauses → ActionInterceptor via §5.4 partition with pre-mutation decision snapshot for if+select-on — box routing test proves report follows the decided branch; strategy phrases → persistent Choice atoms (platform selectors are seeded); select-strategy via occurrence counters, sticks-on-last verified). **stdlib reading.ts gained interceptor hooks** (David-approved; exact opening.ts pattern; golden 11/11). ReadableTrait auto-added to on-reading targets. story-loader 31/31. Discovery of note: registerEventHandler is one-handler-per-type (map.set) — runtime uses keyed chains exclusively. Phase 6 (CLI integration gate) is CURRENT and opens with the David checkpoint on how .story reaches `node dist/cli/sharpee.js --test`.

## Key Observations

- No .transcript suite existed for cloak — only Vitest unit tests against WorldModel internals; current src/index.ts predates both divergences (permanent lighting, Outside room), so the golden suite was authored from cloak.story (design.md §3.1) + Firth's canonical behavior, never from src/index.ts.
- Stumbling in cloak.story is an ENTRY event (`when the player enters ... while in-darkness` + first/third-time ordinals) — message state advances on dark entries, not on commands in the dark. Transcripts encode that deliberately (documented in divergences.md).
- Standard-action texts asserted from lang-en-us: "Taken.", "Dropped.", "You put on the velvet cloak.", "You put the velvet cloak on the brass hook.", "It's pitch dark, and you can't see a thing.", blocked-exit → story's cant-leave phrase.
- Ending assertions use story.victory/story.defeat events (the one approved if-domain wire type, Phase 4).
- transcript header gotcha honored: no leading `---`; key:value lines first, single closing `---`.

## Decisions

- David resolved all 3 plan-review tensions (recommended options): (1) story-lang owns IR schema, ide-protocol re-exports; (2) Phase A includes ActionInterceptor half of on-block compilation, CapabilityBehavior half stays Phase B; (3) AC-1 golden suite is authored in Phase A, then frozen. ADR-210 (Interface Contract 1, AC-1, Implementation Phasing) and design.md §5.9 amended accordingly; resolutions recorded in plan.md.
- David chose to review the plan before Phase 1 starts — implementation on hold pending his read.
- Package naming (Phase 1 checkpoint) DECIDED: frontend = **`@sharpee/chord`** (owns IR wire types; only package that defines IR — no separate story-ir package, no second frontend anticipated); loader stays **`@sharpee/story-loader`** (language-neutral, consumes IR only). ADR-210 Terminology/Packages table/Interface Contract 1 and plan.md updated.

## Artifacts

- docs/work/chord-phase-a/plan.md — 6-phase Phase A plan (scaffolding+golden transcripts → lexer/parser/AST → resolution/IR → loader world-building → loader behavior/RNG → integration gate `sharpee compose`). `.current-plan` repointed here. plan-review: 3 advisory tensions (IR canonical package, Phase-5 on-block ActionInterceptor slice, AC-1 "existing suite" wording), no blockers.

## Next Steps

- Phase 4 (CURRENT): story-loader core — kinds-catalog → trait mapping (prereqs §5 table exactly), create-block world building via ADR-140 helpers, phrase registration (dual-mode ADR-107, strategies → ADR-192/196 Choice producers), minimal `define text` hatch binding (ADR-196 producer signature), win/lose endings incl. the one approved if-domain wire type, Story lifecycle binding.
- Phase 5: when-rules/ordinals/on-block ActionInterceptor slice/seeded RNG. Phase 6: `sharpee compose` + golden gate (opens with the CLI-integration checkpoint — David must confirm approach before CLI code is touched).
- Golden suite is FROZEN (David approved stumble-on-entry + drop-in-dark-bar semantics 2026-07-10); it must not change to make later phases pass.
