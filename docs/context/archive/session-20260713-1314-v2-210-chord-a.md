# Session 2026-07-13 13:14 — v2-210-chord-a (id 0248bb)

## Goal

Draft the two CP7' package plans: (1) 211-core platform package
(Spliced + join rule + gate seam + TS-entry migrations, byte-identical
on the existing corpus — AC-1/5/7), (2) chord-zoo-surfaces package
(Z1–Z5 surfaces, zoo.story migration, excluded transcripts rejoining —
AC-4/6/8/11/12/13 + fixtures). Present to David for per-package
go-ahead before any implementation.

## Done

- Pre-session audit clean (type check 0 errors, no stale artifacts,
  only the 3 known untracked scratch files in docs/work).
- **211-core plan written** (docs/work/chord-211-core/plan.md,
  .current-plan pointer set; plan-review clean). 4 phases: (1) Spliced
  atom in if-domain, (2) lang-en-us realizeSpliced + AC-1/2/9 fixture
  tests, (3) stdlib gate seam (Q4 scaffold + AC-12 seam unit test),
  (4) FLAG DAY — resolver switch + engine load gate + all TS entry
  migrations + full byte-identical regression in one atomic commit
  (verified: resolver is one global function, so Spliced emission and
  entry migration can't be split). Flags: inventory says 11 entries,
  investigation found 10; concealment-test `dust` entry fits neither
  join-mode category cleanly — both explicit Phase-4 decisions.
- **chord-zoo-surfaces plan written** (docs/work/chord-zoo-surfaces/
  plan.md; plan-review clean; .current-plan untouched — package 2).
  4 phases: (1) Z5 adverbs + Z4 here + Z1 first-time, (2) Z2 markers +
  atomic compile contract + gates, (3) Z3/Z3b entity channels,
  (4) zoo.story migration + excluded-transcript rejoin/retirement.
  Drift found: `present` needs new IR-driven slot-contributor
  synthesis (current consumers hand-write TS contributors);
  `disappeared` has no removal/destroy IR statement to hook —
  possible stop-and-discuss in Phase 3.

- **David's standing instruction recorded** (memory:
  raise-sharpee-api-gaps-for-chord): Chord-surfaced Sharpee API gaps
  are raised to him explicitly — "both sound like ADRs" → drafted.
- **ADR-212 DRAFT** (adr-212-slot-contributions-as-data.md):
  declarative slot-entry registry `(slotKey, owner) → {content, order,
  gate, counterKey}` behind {slot:here}; one platform contributor;
  presence default gate (player's room at staging time) + predicate
  seam; Chord `phrase present:` and friendly-zoo's hand-rolled closure
  both become entries. Grounded in a dedicated investigation (only ONE
  registerSlotContributor consumer exists — zoo's closure;
  {slot:detail} feeds via a separate __slots__ path; ADR-195 assumed
  closures by design). Open Q1–Q4 for David (gate generality, registry
  home, zoo migration timing, slot-key scope).
- **ADR-213 DRAFT** (adr-213-removed-from-play-signal.md): pre-removal
  observer seam at the WorldModel.removeEntity choke point
  (live entity + lastRoomId; in-memory, re-registered at load);
  Chord's `disappeared` channel rides it via the loader's phrase-event
  path, witnessed-only; covers TS-initiated removals for free; Chord
  removal statement deferred (own ratchet entry when needed).
  Grounded in a dedicated investigation (removeEntity is a SILENT
  hard-delete; every shipped disappearance narration is hand-rolled;
  Chord's move has no null form). Open Q1–Q4 for David (payload
  timing, seam-vs-event, statement now/deferred, orphaning
  unsignaled).
- **adr-review round 1 (multi-ADR)** run on the pair: 6 SMALL
  findings (SlotEntryGate shape, slot-vs-channel counter-key drift +
  the mis-cited cross-ref, described-room vs player's-room gate
  ambiguity, missing re-registration AC, unspecified narration
  delivery path) — all fixed in the ADRs, noted in their headers.

## Status

Both CP7' package plans written and reviewed (211-core is CURRENT).
ADR-212/213 drafted from the Phase-3 gap findings, reviewed, fixes
applied. AWAITING David: ADR-212/213 open questions + per-package
go-ahead. No code touched. Note: zoo-surfaces plan Phase 3 needs a
re-cut against 212/213 once they're resolved (it currently plans
loader-synthesized closures + a disappeared stop-and-discuss).

## Next

- David resolves ADR-212 Q1–Q4 and ADR-213 Q1–Q4 (adr-interview
  flow), then flip to ACCEPTED and re-cut zoo-surfaces Phase 3.
- On go-ahead: 211-core Phase 1 (Spliced atom in if-domain).
