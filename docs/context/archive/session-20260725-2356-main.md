# Session Summary: 2026-07-25 (2356) - main [ea9d13]

**Goal**: ADR-268 (Chord grammar rule ordering) — run the specificity experiment (A7) first,
then draft the ADR from its result.

**Status**: COMPLETE (design phase) — A7 experiment run (decisive); **ADR-268 ACCEPTED**
(interviewed + reviewed 14/14 same day). Implementation (D5) not started — needs its own
go-ahead.

## Work done

- Pre-session audit clean (type check green, no stale artifacts; `.devarch/descriptor.json`
  the only uncommitted change).
- **A7 specificity experiment run (both halves), results decisive for design (c):**
  - Static (`docs/work/chord-grammar-ordering/specificity-dump.cjs` + `specificity-pairs.cjs`):
    422 rules dumped; every different-priority pair swept for co-matchability.
    **REVERSE: 0** — specificity never contradicts priority. TIE: 29 (2 confidence-comparable;
    the real one is `go out` exiting@105 vs going@100 — the ONLY duplicate pattern with
    different actions in all 422 rules). AGREE: 65. The 37 abbreviation rules at priority 90
    (`n`, `go n`, `g`) co-match NOTHING — the feared "shorter than what they outrank" problem
    is vacuous.
  - Dynamic: instrumented scratchpad COPY of dist/cli/sharpee.js (behavior unchanged, logs
    when the priority term matters) over the full corpus — dungeo units + wt chain, fernhill,
    friendly-zoo, cloak, nautical, all green. **1,000 records, ZERO DIVERGE.** Visible priority
    work = story tier only (`press yellow`, `break frame`); everything else same-action ties
    (`look`) or specificity-agrees. concealment-test + channel-service-test fail on the STOCK
    bundle too (stale story dists, pre-existing, excluded).
  - Report: `docs/work/chord-grammar-ordering/specificity-experiment.md`.
- **ADR-268 DRAFT written** (`docs/architecture/adrs/adr-268-chord-grammar-rule-ordering.md`):
  no numeric priority; resolution = confidence → tier → literalSpecificity (ADR-231 D2b) →
  definition order; `go out` + the 29 static-tie pairs preserved by file order in ADR-269's
  migration; zero new constructs (no version bump) unless Q-1 rules otherwise. Open questions:
  Q-1 explicit override construct at all?, Q-2 tier-vs-specificity order, Q-3 when the engine
  swap lands. Inherited ADR-266 Q-7 restated per D14.

## Key decisions

- **ADR-268 ACCEPTED (David, via interview 2026-07-26)** — `docs/architecture/adrs/adr-268-chord-grammar-rule-ordering.md`:
  - Q-1: **no override construct** — definition order is the only escape hatch; adding one later is additive.
  - Q-2: **tier-first** — story overrides platform absolutely; loader's 150/140 collapse to one story tier.
  - Q-3: **engine swap lands under this ADR** (ahead of ADR-269), with `.withPriority` REMOVED
    from the builder API and every call site (never accepted-but-ignored).
  - adr-review 14/14 after two SMALL fixes: D5 tier contract (`GrammarRule.tier: 'standard'|'story'`
    replaces `priority`), D5 full module list (if-domain builder + engine `sortRules` removal is
    load-bearing; RichCandidate; family-zoo-tutorial/thealderman/devkit fixture; 7 parser test files).

## Observations surfaced (not changed)

- Optional tokens skipped at end-of-input carry no ×0.9 confidence penalty
  (english-grammar-engine.ts:157-161, :342-345) — why `look [around]`@101 exists; bare `look`
  ties it at conf 1.0. Same-action, invisible.
- RichCandidate carries no rule reference (action+priority+lit used to map back to rules).
- `findMatches` maxMatches:10 early-stop iterates rules priority-first today.

## Files modified

- `docs/architecture/adrs/adr-268-chord-grammar-rule-ordering.md` (new, DRAFT)
- `docs/work/chord-grammar-ordering/` (new): specificity-dump.cjs, specificity-pairs.cjs,
  analyze-ordering.cjs, rules.json, pairs.json, specificity-experiment.md
- `docs/context/session-20260725-2356-main.md` (this file)

## Next steps

1. Implement ADR-268 D5 (on go-ahead): engine swap to confidence → tier → specificity →
   definition order; remove `.withPriority` + all call sites; GrammarRule.tier contract;
   acceptance = corpus green, gap report EMPTY full stop, `go out` → exiting assertion.
2. Then ADR-269 (standard grammar as Chord source) — carries D3's 29-pair file-order
   constraint.
