# Session Summary: 2026-07-27 (1900) - main [7ca178]

**Goal**: Investigate and draft an ADR for a community-requested relations
feature (Inform 7-style declared entity relationships).

**Status**: COMPLETE. This session's goal — an exploratory ADR — is done;
nothing blocked.

## Work done

- Recap of prior session + pre-session audit: clean (type check green, no
  stale artifacts). Noted plan-pointer staleness and a 3-session-old dormant
  IDE-source-unit carried item, both belonging to the parked ADR-277 stream
  (not touched this session).
- David relayed a community feature request from Nathaniel: relations —
  Inform 7-style declared relationships between entities.
- Investigated the existing seams: `WorldModel` already has a dormant
  relationship store (`WorldModel.ts:1154-1206`) — forward-only,
  unconstrained, zero callers, though save/load already works via
  `WorldSerializer`. Chord has no relations surface at all (only `in`/`on`/
  `starts-in` placement and `before`/`after` trait ordering).
- Drafted **ADR-278** (`docs/architecture/adrs/adr-278-relations.md`):
  EXPLORATORY, no commitment, candidate umbrella for a possible Sharpee
  5.0.0 / Chord 3.0.0 anchor feature. Four exploration layers: E1
  world-model registry hardening, E2 Chord syntax, E3 TS parity surface,
  E4 text enumeration. Route-finding, relations-to-values, and parser
  integration are explicitly parked out of scope. Six open questions
  recorded, unresolved.
- David tabled it: declined the open-questions interview for now. ADR stays
  exploratory/DRAFT.

## Key decisions

- ADR-278 written as an exploratory umbrella — decisions deferred to child
  ADRs if the feature is ever pursued. No architecture or code committed to
  this direction yet.

## Open items / next steps

- None scheduled — tabled until David decides on 5.0.0/3.0.0 direction.
- ADR-277 Phase 4 blocker (bare `[OK]` exact-match ruling) remains parked in
  its own stream, untouched this session.
