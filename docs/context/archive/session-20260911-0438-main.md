# Session Summary: 2026-09-11 - main

**Session**: c3f995

## Status: COMPLETE

## Goals
- ADR-347 Phase 2: `IStoryEnding`, the world member, and the declaring verb `endStory`.
- ADR-347 Phase 3: the engine reads the Ending, and the `isComplete()` poll goes.
- ADR-347 Phase 4: the restore seam derives the phase from the Ending; Phase 1's patch retired; ADR-345 amended.
- ADR-347 Phase 5: the state-mode channel sibling and the browser client — GH #414 defects 2 and 3.
- Bump the platform version and stage the release once the plan closes.

## Phase Context
- **Plan**: `docs/work/meta-commands-work-after-ending/plan.md`
- **Phases executed**: Phases 2, 3, 4 and 5 — the whole of ADR-347. (Phase 1 was ADR-345 D15, completed in the prior session, aceb2e.)
- **Phase outcomes**: all four DONE; **Plan Status: DONE** — the plan is closed.
- **Tool calls used this session**: 527 (session state `c3f995`).

## Completed
- `IStoryEnding` added to `packages/if-domain/src/endings.ts`; the file stays types-only, per D2a.
- `WorldModel` carries the Ending as a first-class member on the `playerId` precedent: `getEnding()`/`setEnding()` on `IWorldModel` and the class, dropped by `clear()`, written by `WorldSerializer.serialize`, restored by `deserialize`, synced back in `loadJSON`.
- `endStory(world, kind, opts?)` in `packages/stdlib/src/endings/`, beside `death/`. First-ending-wins (returns `undefined`, writes nothing, emits nothing). The phrase key rides as `endingMessageId`, never top-level `messageId` (GH #274).
- No consumer switched over — Phase 3's job.
- 10 new tests; 5 mutation probes, each failing its suite when the guard/write it targets is removed.

### Phase 3
- `endingStage` asks the world one question — does it carry an Ending — and stops with that kind. The three-source recomputation is gone: `emit-events`' `story.victory` scan and `TurnStageContext.victory` deleted, `isComplete?()` off the `Story` interface.
- The one ending the stage still declares is a death `detect-death` found that story policy did not revive, via `endStory(world,'defeat',{turn,cause})`.
- `triggerEnding` holds no implementation of its own: it delegates to `endStory` and returns `ISemanticEvent | undefined`; the `win`/`lose` statement pushes nothing when the story had already ended.
- `scheduler-constructs.storyOver()` reads `world.getEnding()`. `ChordRuntimeCore.turnNow()` is now the one turn implementation (timers delegate to it).
- Dungeo's hardcoded `isComplete(): return false` and the `completion-test-story` fixture's poll are both gone — the fixture declares through `endStory` like a real story.
- AC-1, AC-2 and AC-3 all satisfied here. Four mutation probes, each failing its suite.
- **Two deliberate behavior changes, neither moving a golden**: a Chord `lose` now ends as a *defeat* (it used to stop the engine as a victory, because `emit-events` only watched `story.victory`); and a hand-emitted `story.victory` no longer stops the engine by itself, since `endStory` is the declaring route and emits that event itself.

### Phase 4
- `GameEngine.derivePhaseFromEnding()` replaces the Phase 1 patch at both call sites (`loadSaveData`, `undo()`): `playing → stopped` via `stop(ending.kind)` when the restored save carries an Ending, `stopped → playing` via `resume()` when it does not, nothing in `empty`/`ready`. Both edges are ADR-345's existing ones — no state and no transition added.
- The `playing → stopped` direction goes beyond AC-5's two assertions on purpose: without it, GH #414 defect 2's reload path still lands a fresh playing engine on a finished world.
- AC-5: all three assertions met, including `grep -c "stopped') this.resume()"` returning **0** — deleted, not bypassed. AC-7: `stop('restart')` and `stop('victory')` still produce the same phase and neither records an Ending. AC-8: ADR-345 amended in the same work — header note, D9 pointer to ADR-347 D2, D15 note that the resume patch is retired.
- **Correction to ADR-347 D4b's premise**: it anticipated that ADR-345's D15 "describes the retired patch as a standing rule". It never did — Phase 1 recorded the rule as *owed* a D15 line that David never ruled in. The line D15 now carries answers that owed question ("it does not stand") rather than removing text that was there.
- Three mutation probes (each direction removed, plus the unconditional patch restored), each failing the suite.
- **`mutation-verification` found two real gaps, both acted on**: (1) the `playing → stopped` direction was an untested seam — `stop()`'s full cascade fires on a restore, and it happens to stay out of the restore's rendered response only because `META_STAGES` carries no `channelPacketStage`; now pinned by a new `platform-dispatcher.test.ts` case driving a real `RESTORE_REQUESTED` of an ended save into a playing engine. (2) `TestingExtension.restoreCheckpoint` (`extensions/testing/src/extension.ts:433`) replaces the world via `loadJSON` with no engine in hand, so a checkpoint restore can leave the phase and the world disagreeing — pre-existing, out of ADR-347's scope, **filed as GH #416**.

### Phase 5
- `storyEndingChannel` (`story-ending`, `replace`/`json`/`sparse`) reads `world.getEnding()` and carries the record. `endgame` untouched, so no golden moved. Third return shape is `null` — the clear signal when a reported Ending goes away.
- The browser renderer drives `input.disabled` + `data-story-ended` from the record and nothing else; an unrecognised payload leaves the input alone.
- The end-game prompt is derived, not written in the client: `EndGamePrompt` (`if.platform.prompt.ended`, registered in `lang-en-us`) replaces the story's prompt whenever the world carries an Ending, arriving on the existing `prompt` channel as resolved text.
- **`detectDeathStage` moved ahead of `render-prose`/`channel-packet`** and now owns the death declaration. It used to run after both, so a death — the commonest ending in IF — declared its Ending after the turn had already rendered and shipped its packet.
- **GH #414 defect 2 fixed by NOT suppressing the autosave.** The ended world is still persisted; Phase 4's seam stops the rebooted engine and `engineApplySave` syncs the input state from the world.
- AC-6 satisfied premise-first: channel fires (6 cases) → client disables with prose mocked empty (4 cases) → chain at 952. Real-path test per rule 13a drives a real engine and reads the actual `channel:packet`, including a death turn and a real `restore` at the end-game prompt.
- Five mutation probes, each failing its suite.
- **`mutation-verification` found one real client defect**: RESTART/RESET left the input disabled (the reboot reuses this DOM, and a fresh ChannelService has no prevValue to clear from). Fixed by syncing from the world on every `connectEngine`; pinned by a new `restart-reboot.test.ts` case.

## Two Rulings, Both Acted On (late this session)

1. **"obviously we need all outcomes reported"** — a death's Ending was being declared without emitting its `story.defeat` event: `detect-death.ts` discarded `endStory`'s return value. Fixed with a new `routeEndingEvent(context, event)` (`packages/engine/src/turn/detect-death.ts`) that delivers the event three ways — the event source, the turn's stored events, the engine's emitter — and refreshes `context.result.events`, the same shape `platform-operations` uses for its own completion events. Renders nothing (no template for `story.defeat` in `lang-en-us`) and moves no golden; two mutation probes (routing removed, turn-store write removed) each fail the suite.
2. **"make turn required"** — `IStoryEnding.turn` shipped in Phase 2 as `readonly turn?: number` under a stated deviation from ADR-347 D2d (no general declaring site had a turn to supply). David ruled it stays required. `IStoryEnding.turn` is now `readonly turn: number`, `endStory`'s `opts` is required to carry it, and `RuntimeHost.triggerEnding` takes `(world, ending, turn, messageId?)`. **ADR-347 D2d amended** ("D2d's `turn` contract reconciled" — Phase 2 had found the pinned contract internally inconsistent, shipped the deviation, and reported it; this is the reconciliation, not a new decision).

## ADRs Amended This Session
- **ADR-345** — two amendment blocks under session `c3f995`: D9 gains a pointer to ADR-347 D2 (which answers a question D9 never asked — who owns the *ending*, as against where the stop *reason* lives); D15 gains the record that the `loadSaveData`/`undo()` resume patch its own text described is **retired** by ADR-347 D3, not promoted to a standing rule. Written in the same commit as the code, per ADR-347 D4b, gated by its AC-8.
- **ADR-347** — D2d amended per the "make turn required" ruling above.

## Version Bump: 5.3.2 → 5.4.0

Bumped via `pnpm exec tsf version 5.4.0`, at David's ruling that a patch would have understated this release. Rationale carried into the version bump, not just the changelog:
- **Removed public surface**: `isComplete?()` off the `Story` interface; `victory` off `TurnStageContext`.
- **Changed signature**: `RuntimeHost.triggerEnding` now takes `(world, ending, turn, messageId?)`.
- **Three behavior changes**, none cosmetic: a Chord `lose` now ends as a *defeat* where it used to stop as a *victory*; a hand-emitted `story.victory` no longer stops the engine by itself; a death turn now emits `story.defeat` where it previously emitted nothing.
- **New public surface added**: `IStoryEnding`, `endStory`, `WorldModel.getEnding`/`setEnding`, the `story-ending` channel, `EndGamePrompt`.

All package versions confirmed at `5.4.0` post-bump (`packages/engine`, `packages/stdlib`, `packages/sharpee`, and siblings).

## Incident: an uncommitted file was reverted by `git checkout --`, then rewritten and re-verified

During a mutation probe, `git checkout -- packages/engine/src/turn/detect-death.ts` was run to undo the probe's own mutation — but the file was still uncommitted from this session's earlier work (the `routeEndingEvent` change), and the checkout reverted that too, silently. The loss was caught, `routeEndingEvent` and its `detect-death.ts` integration were rewritten, and the file was re-verified: `@sharpee/engine` test:ci, full `turbo run test:ci`, the Dungeo walkthrough chain, and both of Phase 3/5's mutation probes against `detect-death.ts` all green against the restored file.

**Lesson carried forward**: a probe must be restored from its own backup copy, never with `git checkout` on a tree that still has uncommitted session work — `git checkout --` cannot distinguish "the probe's edit" from "everything since the last commit" on the same file.

## Website — 5.4.0 release note, and the "Notes" header collision

- **The publish did not update the website, on either count.** `website/src/lib/versions.json` still read 5.3.2 — it is a derived artifact regenerated by `npm run sync` at prebuild, so a `./website/deploy.sh` would have picked 5.4.0 up, but nothing had run since the publish and the committed copy was stale. Regenerated to 5.4.0.
- **There was no `docs/releases/release-5.4.0.md` at all**, and the site's release notes are derived from that directory, so the published list stopped at 5.3.2. Written: the ADR-347 story, 15 `## Details` bullets, and an **Upgrading** section naming the breaking changes plainly (`isComplete?()` removed, `triggerEnding`'s new signature, `TurnStageContext.victory` removed, a Chord `lose` now ending as a defeat) plus the ruling that pre-release saves are not migrated. `sync-releases` now reports 8 releases with 5.4.0 newest.
- **David's finding: the roadmap/release-note design was goofy — "Notes is the header for both."** It was worse than that: the word did three jobs at three levels. A nav section titled `Notes` held a group titled `Releases` which held a page titled `Release notes`, whose file body then carried a `## Notes` heading — the rail read *Notes › Releases › Release notes*, and "Notes" named no category a reader could predict (a roadmap is not notes).
  - Section renamed **`Notes` → `Roadmap & Releases`**, named for its two contents rather than an invented umbrella. The prior session's reason for it being a *separate* section was sound — roadmap and releases both span Sharpee, Chord and Chord Writer, so filing either under one would misstate its scope — so only the name changed, and the comment records both.
  - **The two single-item groups are gone.** Each wrapped exactly one page and only added a level to the crumb trail; they are section-level `items` now, which `site-shell` already renders as flush-left peers. Breadcrumbs drop from three elements to two. `pagerFor` and `crumbsFor` were checked for section-level-item support before the change, not after.
  - **`## Notes` → `## Details`** in all seven existing release files, in `sync-releases.mjs`'s parser, and in `docs/releases/README.md`'s format spec.
- Verified: `npx tsc --noEmit` clean, `npm run build` succeeds with `/releases` and `/roadmap` prerendering. The site's 41 lint errors are pre-existing `react-hooks` complaints in the search components, none in the touched files.
- **Not deployed.** `./website/deploy.sh` was not run — the rail's new shape is a visual change David asked about before it goes live.

## Gates — re-verified fresh this run, at 5.4.0 (2026-09-11, post-bump)

All commands run directly by this summary pass, after every edit in the session (freshness per ADR-0019):
- `pnpm typecheck` — **77/77** (`Tasks: 77 successful, 77 total`)
- `pnpm exec turbo run test:ci --no-daemon` — **76/76**
- `./repokit build dungeo` (bundle already fresh, built 11:14, after all source edits) then `node dist/cli/sharpee.js --test --chain stories/dungeo/walkthroughs/wt-*.transcript` — **952 passed in 17 transcripts**, all goldens matched
- `pnpm test:scripts` — **14/14**
- `node dist/cli/sharpee.js --test stories/cloak-of-darkness/tests/transcripts/*.transcript` — **90 passed in 9 transcripts**
- `pnpm --filter '@sharpee/engine' test:ci` — **807 passed / 7 skipped** (84 files)
- `pnpm --filter '@sharpee/platform-browser' test:ci` — **153 passed** (16 files)
- Dungeo *unit* transcripts (`stories/dungeo/tests/transcripts/*.transcript`) — **1721 passed, 13 failed, 10 expected failures, 2 skipped** — within GH #411's known 13/20/18 band for that unseeded suite; none of the 13 failures are ending-related (dam/mailbox/troll-combat, per the plan's own trace).

## Integration Reality Statement (rule 13a — "the engine" in Phase 3's/4's names)

**ADR-347 Phases 2-5**
- **OWNED**: `packages/engine` (turn cycle, `GameEngine`), `packages/world-model` (Ending persistence), `packages/stdlib` (`endStory`, the `story-ending` channel), `packages/story-loader` (Chord's `triggerEnding`/`RuntimeHost`), `packages/platform-browser` (`BrowserClient`), and the CLI bundle `dist/cli/sharpee.js` this repo builds and ships.
- **EXTERNAL**: none — no third-party API or service is touched by this work.
- **REAL-PATH TEST**: `packages/engine/tests/integration/story-ending-channel.test.ts` drives a real `GameEngine` through `executeTurn` and reads the actual `channel:packet` (Phase 5). The Dungeo walkthrough chain and cloak-of-darkness transcripts run the real bundle (`dist/cli/sharpee.js`), the real Chord runtime, and the real story-loader — not a fixture stand-in. `platform-dispatcher.test.ts`'s new case dispatches a genuine `RESTORE_REQUESTED` into a real, playing `GameEngine` (Phase 4). `platform-browser/tests/restart-reboot.test.ts` and `story-ending-input.test.ts` drive the real `BrowserClient` end to end.
- **STUB JUSTIFICATION**: AC-6's second premise check (`story-ending-input.test.ts`) mocks prose content empty to isolate the disable-state assertion from rendering — backed by the real-path `story-ending-channel.test.ts` and the transcript chain, both of which exercise prose for real in the same phase.

## Open Items

- ~~The `turn?` deviation needs David's ruling or an ADR-347 D2d amendment before Phase 5 consumes the field~~ — **resolved this session** ("make turn required" ruling; D2d amended; `turn` is now non-optional throughout).
- ~~A death's Ending is declared without emitting its `story.defeat` event~~ — **ruled and fixed** this session ("obviously we need all outcomes reported").
- **GH #416** (open) — checkpoint restore (`extensions/testing`) bypasses the restore seam, so the engine phase and the world can disagree about the ending. Same defect class as the seam this plan fixed, explicitly out of ADR-347's Scope.
- ~~**GH #414**~~ — **CLOSED** 2026-09-11, after 5.4.0 was verified live on the registry. All three defects are fixed across the plan's five phases (defect 1 by Phase 1/aceb2e, defects 2 and 3 by Phases 4-5 this session). Closed with a comment explaining all three and why the reload fix was to persist the ended world faithfully rather than suppress the save. It was deliberately left open until the publish landed: it was reported from the field, so the fix was not real to the reporter until it was installable.
- ~~**Plan not yet archived**~~ — **archived** 2026-09-11: `plan-archive.sh meta-commands-work-after-ending` moved the directory to `docs/work/archive/` and released the `.current-plan` pointer, which it had named. The script listed files still naming the pre-archive path; all were session summaries, the ledger and ADR-347 — history rather than pointers — so they were left as written.
- **Awaiting David**: whether to run `./website/deploy.sh` so the 5.4.0 note and the renamed rail go live.
- **Awaiting David**: `scan-filer` filed GH #417-#423 at finalize, all seven for the stranded DevArch event logs — a category he has ruled ignorable, so the filer will reopen them every finalize while the logs sit there. Recommended closing them won't-fix and dropping `event-log-archival` from the scan set; not acted on.
- **Rough edge, not fixed**: a browser reload into an ended save disables the input but leaves a stale prompt label (no turn runs, so no `prompt` value is produced).
- **Noted, not fixed**: a headless Chord `win` records `turn: 0` (`core.turnNow()` falls back to `lastTickTurn` with no engine turn provider wired). Pre-existing `turnNow` semantics, shared with timers.
- `STORY_ENDING_FLAG` survives in `if-domain` with zero consumers (doc marked superseded). Deletion is David's call, not filed as a separate item.

## Files Modified

**Phase 2 + Phase 3**:
- `packages/if-domain/src/endings.ts`
- `packages/world-model/src/world/WorldModel.ts`, `WorldSerializer.ts`
- `packages/world-model/tests/unit/world/story-ending.test.ts` (new)
- `packages/stdlib/src/endings/end-story.ts`, `index.ts` (new); `packages/stdlib/src/index.ts`
- `packages/stdlib/tests/endings/end-story.test.ts` (new)
- `packages/engine/src/turn/ending.ts`, `emit-events.ts`, `context.ts`; `packages/engine/src/install/story.ts`
- `packages/story-loader/src/loader.ts`, `runtime/core.ts`, `runtime/statements.ts`, `runtime/timers.ts`, `runtime/scheduler-constructs.ts`
- `stories/dungeo/src/index.ts`
- Tests: `packages/engine/tests/unit/turn-stages.test.ts`, `tests/story.test.ts`, `tests/stories/completion-test-story.ts`, `packages/story-loader/tests/runtime.test.ts`, `tests/loader.test.ts`

**Phase 4**:
- `packages/engine/src/game-engine.ts`
- `packages/engine/tests/unit/engine-lifecycle-phase.test.ts`, `tests/unit/platform-dispatcher.test.ts`
- `docs/architecture/adrs/adr-345-the-engine-lifecycle-phase-is-an-explicit-concept.md`

**Phase 5**:
- `packages/stdlib/src/channels/standard.ts`, `channels/index.ts`
- `packages/if-domain/src/prompt.ts`
- `packages/lang-en-us/src/language-provider.ts`
- `packages/engine/src/turn/render-prose.ts`, `detect-death.ts`, `stages.ts`, `ending.ts`
- `packages/platform-browser/src/channels/story-ending.ts` (new), `channels/index.ts`, `BrowserClient.ts`
- Tests: `packages/stdlib/tests/channels/standard.test.ts`, `packages/platform-browser/tests/story-ending-input.test.ts` (new), `tests/restart-reboot.test.ts`, `tests/turn-events.test.ts`, `tests/shadow-renderer.test.ts`, `packages/engine/tests/integration/story-ending-channel.test.ts` (new), `tests/unit/turn-stage-order.test.ts`

**Two Rulings (late session)**:
- `packages/engine/src/turn/detect-death.ts` (`routeEndingEvent`)
- `packages/if-domain/src/endings.ts` (`turn` required)
- `packages/story-loader/src/*` (`RuntimeHost.triggerEnding` signature)
- `docs/architecture/adrs/adr-347-the-ending-is-an-explicit-concept.md` (D2d amendment)

**Version bump (5.3.2 → 5.4.0)**:
- All `packages/*/package.json` (version field), `packages/sharpee/package.json`
- `packages/sharpee/docs/genai-api/{index,engine,if-domain,presentation,stdlib,world-model}.md` (regenerated)

**Plan**:
- `docs/work/meta-commands-work-after-ending/plan.md` (all 5 phases DONE, Plan Status DONE) — since moved to `docs/work/archive/meta-commands-work-after-ending/`

**Website / release notes** (after the publish, uncommitted at the first finalize):
- `docs/releases/release-5.4.0.md` (new)
- `docs/releases/README.md` (format spec: `## Notes` → `## Details`)
- the seven existing `docs/releases/release-*.md` (heading rename only)
- `website/scripts/sync-releases.mjs` (parser keys off `## Details`)
- `website/src/lib/nav.ts` (section renamed and flattened)
- `website/src/lib/versions.json`, `website/src/lib/releases-data.json` (derived, regenerated)

## Notes

**Session duration**: overnight into morning, 2026-09-11 (started 04:38 CDT).

**Approach**: implement ADR-347 phase by phase against its own pinned contracts, running the full gate stack after each phase and mutation-probing every new guard/write in both directions before moving on. Two open questions from Phase 2 (the `turn?` deviation and the missing death event) were carried as reported deviations rather than silently resolved, and both were ruled on by David before Phase 5 closed.

**Verification gap**: this write-up could not close GH #414 or archive the plan directory — both writes were denied by the session's auto-mode permission classifier as "External System Writes." Both are otherwise ready (see Open Items).

---

## Session Metadata

- **Session**: c3f995
- **Status**: COMPLETE
- **Blocker**: N/A
- **Blocker Category**: N/A
- **Estimated Remaining**: N/A
- **Rollback Safety**: safe to revert — all changes are uncommitted at the time of this summary; `git diff`/`git checkout` on the files listed above fully reverts them, and the plan/ADR edits are ordinary tracked-file changes with no orphaned artifacts.

## Dependency/Prerequisite Check

- **Prerequisites met**: ADR-347 ACCEPTED (20/20 READY FOR IMPLEMENTATION) and ADR-345 D15 (Phase 1) both landed before this session started; the plan's phase breakdown and its "References consulted" list were already current.
- **Prerequisites discovered**: none — the `turn?` contract gap surfaced during Phase 2 implementation was resolved within this session (see Two Rulings), not left as an external dependency.

## Architectural Decisions

- **ADR-347** (ACCEPTED) — the Ending becomes an explicit, first-class `WorldModel` concept; this session implements Phases 2-5 of it in full. Amended this session: D2d's `turn` contract reconciled (required, not optional).
- **ADR-345** (ACCEPTED) — amended this session: D9 gains a pointer to ADR-347 D2; D15 gains the note that the resume patch it described is retired by ADR-347 D3.
- Pattern applied: capability/behavior-owns-mutation precedent (`endStory` follows `killPlayer`'s first-declaration-wins shape); channel-as-state-mode-sibling (ADR-347 D2d) rather than widening an existing prose channel.

## Mutation Audit

- Files with state-changing logic modified: `packages/world-model/src/world/WorldModel.ts` (Ending member), `WorldSerializer.ts` (persistence), `packages/stdlib/src/endings/end-story.ts` (declaring verb), `packages/engine/src/game-engine.ts` (`derivePhaseFromEnding`), `packages/engine/src/turn/detect-death.ts` (`routeEndingEvent`), `packages/platform-browser/src/BrowserClient.ts` (`syncEndingFromWorld`).
- Tests verify actual state mutations (not just events): **YES** (evidence: fresh runs performed directly in this pass, all after the session's last edit — `pnpm typecheck` 77/77, `turbo run test:ci` 76/76, `@sharpee/engine` 807 passed/7 skipped, `@sharpee/platform-browser` 153 passed, Dungeo chain 952/952, cloak-of-darkness 90/90, `pnpm test:scripts` 14/14, all captured 2026-09-11 in this pass). Every new guard/write across all four phases carries a mutation probe (removed in turn, each fails its suite) per the plan's own Phase outcomes above.

## Recurrence Check

- Similar to past issue? **NO** for the git-checkout incident specifically — grepped prior session summaries for "git checkout"; the two hits (`session-20260910-2242-main.md`, `session-20260911-0134-main.md`) are both deliberate, targeted checkouts of a known commit for a specific fix, not an accidental revert of uncommitted work. This is a new failure mode, not a recurrence.
- The `turn?`-deviation-then-ruling pattern (ship a deviation, report it, get a ruling next session) matches Phase 2's own prior handling of the same field — not a defect pattern, just this ADR's iteration style.

## Test Coverage Delta

- Tests added this session: 10 (Phase 2: `story-ending.test.ts` + `end-story.test.ts`) + 6 (Phase 4: `engine-lifecycle-phase.test.ts` +5, `platform-dispatcher.test.ts` +1) + 5 (Phase 5: `platform-browser` +5) + new integration file `story-ending-channel.test.ts` + `standard.test.ts` channel cases — exact net count not independently re-tallied against a pre-session baseline in this pass, so treat the per-phase deltas above as the traceable figures.
- Tests passing before → after (evidence: fresh runs, 2026-09-11, this pass): `@sharpee/engine` 796 passed/7 skipped (end of Phase 1) → **807 passed/7 skipped** (end of Phase 5, +11). `@sharpee/platform-browser` 148 → **153 passed** (+5, per Phase 5's own outcome note). Dungeo walkthrough chain held at 952/952 throughout — the gate that proves no rendered output moved.
- Known untested areas: the browser-reload stale-prompt-label rough edge (Open Items); GH #416's checkpoint-restore seam.

---

**Progressive update**: Session completed 2026-09-11 11:20 CDT

## Activity Log (auto-captured)
```
[09:45:59] BUILD: Build passed — pnpm --filter '@sharpee/world-model' test story-ending 2>/dev/null | tail -25
[09:46:02] BUILD: Build passed — pnpm --filter '@sharpee/stdlib' test end-story 2>/dev/null | tail -30
[09:46:51] BUILD: Build passed — pnpm --filter '@sharpee/world-model' test story-ending --run 2>/dev/null | tail 
[09:48:32] BUILD: Build passed — pnpm exec turbo run test:ci --no-daemon 2>/dev/null | tail -8
[09:48:33] BUILD: Build passed — pnpm --filter '@sharpee/world-model' test story-ending 2>&1 | tail -40
[09:48:36] BUILD: Build passed — pnpm --filter '@sharpee/stdlib' test end-story 2>&1 | tail -40
[09:49:56] BUILD: Build passed — pnpm test:scripts 2>/dev/null | tail -8
[09:56:49] BUILD: Build failed — pnpm --filter '@sharpee/engine' test turn-stages --run 2>/dev/null | tail -40
[09:59:13] BUILD: Build failed — pnpm exec turbo run test:ci --no-daemon 2>&1 | grep -E "Tasks:|Cached:|error|ERR
[09:59:18] BUILD: Build passed — pnpm --filter '@sharpee/story-loader' test runtime --run 2>&1 | grep -A 25 "inta
[10:01:03] BUILD: Build passed — pnpm exec turbo run test:ci --no-daemon 2>&1 | grep -E "Tasks:|ERR_|FAIL |failed
[10:02:14] BUILD: Build passed — pnpm test:scripts 2>/dev/null | tail -6
[10:05:06] BUILD: Build passed — pnpm --filter '@sharpee/engine' test:ci 2>&1 | tail -60
[10:05:22] BUILD: Build passed — pnpm --filter '@sharpee/story-loader' test:ci 2>&1 | tail -40
[10:06:03] BUILD: Build passed — pnpm exec turbo run test:ci --no-daemon 2>&1 | grep -E "Tasks:|ERR_|FAIL |failed
[10:07:20] BUILD: Build passed — node dist/cli/sharpee.js --test stories/cloak-of-darkness/tests/transcripts/*.tr
[10:08:15] BUILD: Build passed — pnpm --filter '@sharpee/stdlib' test:ci -- endings 2>&1 | tail -30
[10:08:45] BUILD: Build passed — pnpm test:scripts 2>&1 | tail -30
[13:44:02] BUILD: Build passed — pnpm exec turbo run test:ci --no-daemon 2>&1 | grep -E "Tasks:|ERR_|FAIL |failed
[13:45:41] BUILD: Build passed — grep -c "stopped') this.resume()" packages/engine/src/game-engine.ts; grep -n "d
[13:57:11] BUILD: Build passed — pnpm exec turbo run test:ci --no-daemon 2>&1 | grep -E "Tasks:|ERR_|FAIL " | hea
[13:57:32] BUILD: Build passed — python3 - <<'PY'
p='docs/work/meta-commands-work-after-ending/plan.md'
s=open(p)
[14:21:36] BUILD: Build failed — pnpm typecheck 2>&1 | grep -E "error|Tasks:" | head -10; pnpm --filter '@sharpee
[14:22:03] BUILD: Build passed — pnpm --filter '@sharpee/engine' test turn-stage-order --run 2>&1 | grep -A 20 "r
[14:23:50] BUILD: Build passed — pnpm exec turbo run test:ci --no-daemon 2>&1 | grep -E "Tasks:|ERR_|FAIL " | hea
[14:24:31] BUILD: Build passed — pnpm --filter '@sharpee/stdlib' test channels/standard --run 2>&1 | grep -B 2 -A
[14:25:39] BUILD: Build passed — pnpm --filter '@sharpee/engine' test story-ending-channel --run 2>&1 | grep -A 1
[14:25:51] TEST: Tests ran (status unverified) — cat packages/engine/vitest.config.ts 2>/dev/null | head -40; echo "=== registry 
[14:26:08] TEST: Tests ran (status unverified) — cat > /private/tmp/claude-501/-Users-david-repos-sharpee/d1839b09-b359-4efe-b2cf
[14:26:44] BUILD: Build passed — rm packages/engine/tests/integration/dbg.test.ts; pnpm --filter '@sharpee/engine
[14:27:07] TEST: Tests ran (status unverified) — cat > packages/engine/tests/integration/dbg.test.ts <<'EOF'
import { describe, i
[14:29:36] BUILD: Build passed — rm -f packages/engine/tests/integration/dbg.test.ts; pnpm typecheck 2>&1 | grep 
[14:31:00] BUILD: Build failed — node dist/cli/sharpee.js --test stories/cloak-of-darkness/tests/transcripts/*.tr
[14:32:03] BUILD: Build passed — pnpm --filter '@sharpee/engine' test story-ending-channel --run 2>/dev/null | ta
[14:35:22] BUILD: Build passed — cat -A /Users/david/repos/sharpee/stories/dungeo/walkthroughs/wt-17-endgame.gold
[14:37:53] BUILD: Build passed — pnpm --filter '@sharpee/engine' test story-ending-channel turn-stage-order 2>&1 
[14:37:59] BUILD: Build passed — pnpm --filter '@sharpee/stdlib' test channels/standard 2>&1 | tail -40
[14:38:01] BUILD: Build passed — cd /Users/david/repos/sharpee/packages/platform-browser && pnpm test story-endin
[14:42:19] BUILD: Build passed — pnpm --filter '@sharpee/platform-browser' test restart-reboot --run 2>&1 | grep 
[14:42:42] BUILD: Build passed — pnpm --filter '@sharpee/platform-browser' test turn-events --run 2>&1 | grep -A 
[14:42:54] BUILD: Build passed — grep -n "world:" packages/platform-browser/tests/turn-events.test.ts | head; ech
[14:42:59] BUILD: Build passed — grep -rn "function makeWorld" -A 12 packages/platform-browser/tests/*.ts package
[14:43:01] BUILD: Build passed — grep -rn "makeWorld" packages/platform-browser/tests/ | grep -v "connectEngine" 
[14:43:04] BUILD: Build passed — grep -rn "makeWorld" packages/platform-browser/tests/harness* packages/platform-
[14:43:07] BUILD: Build passed — grep -rn "makeWorld" packages/platform-browser/tests/turn-events.test.ts package
[14:45:18] BUILD: Build passed — pnpm typecheck 2>&1 | grep -E "error|Tasks:" | head; pnpm exec turbo run test:ci
[15:31:33] BUILD: Build passed — pnpm exec turbo run test:ci --no-daemon 2>&1 | grep -E "Tasks:|ERR_|FAIL " | hea
[15:33:13] BUILD: Build passed — pnpm --filter '@sharpee/engine' test story-ending-channel --run 2>/dev/null | gr
[16:05:48] BUILD: Build passed — pnpm exec turbo run test:ci --no-daemon 2>&1 | grep -E "Tasks:|ERR_|FAIL |Tests 
[16:16:09] BUILD: Build passed — pnpm exec turbo run test:ci --no-daemon 2>&1 | grep -E "Tasks:|ERR_|FAIL " | hea
[16:19:22] BUILD: Build passed — pnpm exec turbo run test:ci --no-daemon 2>&1 | tail -40
[16:19:37] BUILD: Build passed — pnpm --filter '@sharpee/engine' test:ci 2>&1 | tail -15
echo "=== platform-brows
[16:33:03] EDIT: File written — .commit-files
[16:33:17] EDIT: File written — .commit-msg
[16:33:49] GIT: Git operation — bash $HOME/.claude/scripts/git-commit.sh --push
[19:14:24] BUILD: Build passed — npm run build 2>&1 | tail -20
[19:19:28] EDIT: File written — .commit-files
[19:19:36] EDIT: File written — .commit-msg
[19:19:43] GIT: Git operation — bash $HOME/.claude/scripts/git-commit.sh --push
```
