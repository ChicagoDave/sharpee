# Session Summary: 2026-07-07 01:00 - main (ADR-209 snippets Phase 1)

## Goals
- Plan ADR-209 (room-description snippets) implementation + dungeo adoption.
- Phase 1 (go-ahead given): if-domain wire types, Seq decision, Assembler
  realization, shared marker-extraction helper, unit tests.

## Completed
- Session-start audit clean; project-profile.md regenerated (was 10 days stale).
- Plan written: `docs/work/adr-209-snippets/plan.md` (6 phases); plan-review
  found no conflicts against 10 references. `.current-plan` pointer set.
- **Phase 1 decision resolved by reading, not coding**: the `Sequence` phrase
  kind (`kind: 'seq'`) already exists in `packages/if-domain/src/phrase.ts`
  and the English Assembler already realizes it as
  `parts.flatMap(realizeToRuns)` — in-order, no joining punctuation; runs are
  joined with `''` and `collapseWhitespace` never inserts between-run spaces
  (verbatim runs exempt). **No new phrase kind, no Assembler change needed.**
  Phase 1 shrinks to: snippet wire types + `extractSnippetMarkers` helper in
  if-domain, decision comment in phrase.ts, tests.

## Key Decisions
- Reuse existing `Sequence` for ADR-209's Seq (the ADR's own escape hatch).

## Phase 1 COMPLETE
- `packages/if-domain/src/snippets.ts` (new): `SnippetText` / `SnippetEntry` /
  `SnippetMap` (ADR contract verbatim), `SNIPPET_MARKER_PATTERN`,
  `extractSnippetMarkers` (pure, dedup, first-appearance order); exported from
  `src/index.ts`.
- `phrase.ts`: Sequence doc comment records the ADR-209 reuse decision.
- Tests: `if-domain/tests/phrase/snippets.test.ts` (8) and
  `lang-en-us/tests/assembler/snippet-splice.test.ts` (5 — byte-exact study
  example AC-1 assembler-half, cycling AC-2, Empty absorption, empty-string
  entry, no-separator concatenation).
- Suites green: if-domain 87/87, lang-en-us 391/391; if-domain tsc --noEmit
  clean (use `node_modules/.bin/tsc` — bare `npx tsc` mis-resolves in this
  workspace).

## Phase 2 COMPLETE
- `RoomTrait.snippets?: SnippetMap` (roomTrait.ts — IRoomData + class +
  constructor); snippet types re-exported from the room leaf barrel so
  helpers/stories reach them via `@sharpee/world-model`.
- `RoomBuilder.snippets(map)` sugar in `packages/helpers/src/builders/room.ts`
  (helpers has NO test infra — builder sugar gets exercised by the Phase 4
  story transcript instead; deliberate deviation, no new infra without
  discussion).
- Serialization verified generic: `toJSON` spreads trait props, `fromJSON`
  stores plain data — round-trip test passes.
- Tests: `world-model/tests/unit/traits/room-snippets.test.ts` (4 — defaults,
  direct-trait construction with all four entry forms, runtime mutability,
  toJSON/fromJSON round-trip). Full suite 1322 passed / 10 skipped.
- `./repokit build` clean (rebuilt if-domain dists — package tsc resolves
  types from dist, not src; genai-api docs regenerated as build output).
- madge --circular: 9 cycles, byte-identical to pre-change baseline (verified
  via stash) — none involve traits/room.

## Phase 3 COMPLETE (with one recorded design deviation)
- **Deviation from plan letter (kept ADR intent)**: stdlib ActionContext has no
  language provider, so the resolver can't run at report time with AC-10
  intact. The scan/gate/resolve CODE lives in stdlib
  (`looking/snippet-resolver.ts`, pure + injectable world/provider seams); the
  CALL SITE is the engine's room-description handler (`prose-pipeline/
  handlers/room.ts`) — the single seam all three emitters flow through, with
  world + provider in hand. stdlib data builders attach `roomSnippets` (trait
  read stays stdlib-side) in looking-data, going.ts, switching_on.ts.
- lang-en-us: `parseVerbatim` passes pre-built phrase values through
  (isPhraseValue precedent) — `{verbatim:description}` accepts the Sequence.
- lang-en-us assembler: same-key Choice picks memoized per top-level realize
  pass (AC-8: duplicate marker → same text, one counter advance; distinct
  keys unchanged). `selectChoice` split into memo + `pickChoiceAlternative`.
- Tests: stdlib resolver 11 (AC-4/7/8/10 + unbound-marker log + purity);
  lang-en-us splice tests now 8 incl. end-to-end through REAL renderMessage +
  `if.room.description_body` byte-exact, AC-8 memo, distinct-key preservation;
  parseVerbatim passthrough test. Suites: stdlib 1314p/27sk, lang-en-us 394p,
  engine 473p/7sk, `./repokit build` clean.
- **Pre-existing dungeo failures found (NOT ours — stash-verified byte-identical
  on clean HEAD 5bc0c4ce)**: 13 failures / 12 transcripts (cage-puzzle,
  dam-puzzle×2, endgame-mirror, flooding, frame-after-thief, house-interior,
  implicit-take-test, navigation, rug-trapdoor, tiny-room-puzzle,
  trophy-case-scoring, wave-rainbow). Cause: 8c5ddc30 (2026-07-04, book-QA P2)
  excluded scenery from "You can see" lists; these transcripts assert scenery
  names in look output. This IS the dungeo-81 defect class — Phase 6 fixes by
  mentioning the scenery via snippets (and/or re-targeting assertions).
  Phase 6's "matches baseline" bar must use THIS 13-red HEAD baseline.

## Phase 4 COMPLETE — all 10 ACs have passing tests
- Engine: `src/snippet-validation.ts` (new) — `validateRoomSnippets` (AC-5,
  throws `SnippetValidationError` naming every (room, marker); called in
  `GameEngine.setStory` right after `initializeWorld`) +
  `lintUnusedSnippetEntries` (AC-6 logic). 9 unit tests; engine suite
  482p/7sk.
- devkit: `standalone/build.ts` gains an ADR-209 lint step — loads the
  compiled story (lazy transcript-tester), warns per unused entry, exits 1 on
  SnippetValidationError, silently skips if the story won't load. Added
  `@sharpee/engine` to devkit deps (lockfile diff = that one link).
  **Verified live**: planted `ghost` entry → `sharpee build` printed
  `Warning: room "Parlor": snippet entry 'ghost' matches no {snippet:ghost}
  marker`, exit 0; fixture removed after.
- Story vertical slice: `stories/concealment-test` (NOT a workspace member —
  builds standalone via tsc in its dir; run transcripts with
  `node dist/cli/sharpee.js --test --story stories/concealment-test <t>`).
  Study got initialDescription+description sharing a `clock` cycling snippet
  (AC-9); new Parlor room (east of hall) with cabinet (string), mantel
  (cycling 3), dust (duplicated marker, AC-8), trunk (`mentions` gate).
  `tests/transcripts/snippets.transcript`: 17/17 PASS first run — AC-1 shape,
  AC-2 cycle+wrap, AC-3 $save/$restore resumes mid-cycle, AC-4 gate
  out/resume (Q9: carried = still present; gate needs the trunk LEFT in the
  hall), AC-8, AC-9. Existing concealment.transcript still 21/21.
- **Finding (going action)**: `going` does NOT render initialDescription on
  first entry — only `looking` does (and going marks the room visited). So
  first-visit initial text shows only via a first LOOK. AC-9 test therefore
  lives in the starting room. Possibly a latent platform gap worth asking
  David about someday; not ADR-209 scope.
- **Pre-existing devkit test failure (NOT ours; verified at clean HEAD with
  identical error)**: `src/standalone/browser-build.test.ts` real-path
  scaffold test fails with esbuild "Could not resolve
  @sharpee/platform-browser" (package IS hoisted with dist present).
  Environmental; flagged to David.

## Phase 5 COMPLETE — findings doc written
- `docs/work/adr-209-snippets/dungeo-scenery-findings.md`: programmatic audit
  (bootstrap loadStory → 175 rooms → scenery vs prose correlation) found 24
  mechanical findings; cross-mapped against the 13 failing HEAD assertions.
- **P1 = 9 rooms** whose snippets fix failing transcripts: West of House
  (mailbox), Living Room (trophy case + rug w/ pushed-handler mutation), Tiny
  Room (door), Dam (bolt), Maintenance Room (one panel snippet covers panel +
  4 buttons), Dingy Closet (cage, mentions gate), Aragain Falls (rainbow),
  thief's lair (empty frame — DYNAMIC entity, needs Q7 handler mutation, id
  doesn't exist at init). Inside Mirror pole/panel = assertion retarget (prose
  already covers). troll-interactions drop-sword failure = RNG flake, not
  scenery.
- P2: 9 more candidates; Dam Base river + Vault/Small Room walls judged
  intentional omissions, skipped.
- Noted: switching_on auto-look lists scenery (no 8c5ddc30 filter) → will
  double-mention after snippets land; aligning it is a platform change
  needing its own nod.

## Phase 6 COMPLETE — dungeo unit suite back to green, walkthroughs pass
- Snippets added (P1, MDL-faithful wording where it exists):
  - `white-house.ts` West of House — mailbox (' There is a small mailbox
    here.').
  - `house-interior.ts` Living Room — trophycase + rug; Attic — table (P2,
    same file). `rug-push-interceptor.ts` mutates the rug entry post-push
    (moved-aside wording) — the Q7 handler pattern, live.
  - `underground.ts` Tiny Room — door (north wall).
  - `dam.ts` Dam — panel (authentic MDL DAM-ROOM text: panel + bolt +
    bubble); Maintenance Room — buttons (one snippet covers control panel +
    all four colored buttons; wording chosen to contain "control panel" and
    "blue button").
  - `well-room.ts` Dingy Closet — cage with `mentions` gate (id wired at
    object creation).
  - `frigid-river.ts` Aragain Falls — rainbow (direct RoomTrait-data
    construction, the dungeo-style path).
  - `maze.ts` Treasure Room — `frame: ''` shipped empty;
    `melee-interceptor.ts` fills it (`{ text, mentions: frame.id }`) when the
    thief dies and the frame spawns.
  - `endgame-mirror.transcript`: two assertions retargeted ("wooden pole",
    "mahogany") — Inside Mirror prose already covers both entities.
- **Results**: `./repokit build dungeo` clean. Unit suite: the 13 scenery
  reds are GONE — multiple fully-clean runs ("All tests passed", ~1695/9
  expected-fail/4 skipped). Two RNG flake events observed across 11 runs
  (one 2-fail, one 57-fail burst in a single transcript — thief/carousel
  class; no Choice/randomness exists in the dungeo snippets, all plain
  strings). Six consecutive clean runs after. **Walkthrough chain: 887/887
  first run** (one-good-run satisfied).
- P2 remaining (opportunistic, listed in findings doc): pedestal, glacier,
  hooks, Dusty Room safe/hole, velvet curtain, maze skeleton.

## Next Steps
- All six phases of docs/work/adr-209-snippets/plan.md are COMPLETE.
- Uncommitted: platform (if-domain, world-model, helpers, stdlib, lang-en-us,
  engine, devkit + lockfile), concealment-test story + transcript, dungeo
  story + 1 transcript edit, plan + findings docs. Awaiting David's
  commit/push call.
- Optional follow-ups for David: align switching_on auto-look scenery
  filtering (double-mention after snippets); going does not render
  initialDescription (latent gap); pre-existing devkit browser-build.test.ts
  env failure; book chapter for snippets someday.

## Session Metadata
- **Status**: COMPLETE (all 6 phases; uncommitted, awaiting commit decision)
- **Blocker**: none
