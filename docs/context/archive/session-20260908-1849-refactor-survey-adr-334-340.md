# Session Summary: 2026-09-08 - refactor/survey-adr-334-340

## Status: COMPLETE

## Goals
- Recap the previous session (David: `/devarch:recap`).
- Review John's Chord sample `docs/work/john-chord-samples/no-signal-home.story` and write a syntax review showing how each misuse should be written (David: "write a syntax review so John can see how it _should_ be done where he misused the language").
- Strip the pasted line-number gutter from the sample (David: "strip the line numbers from my copy/paste").

## Completed
- Sample compiles clean under Chord 3.6.0 (0 diagnostics) once the gutter and the IDE title line are stripped; the review is therefore about accepted-but-wrong usage and idiom, every item probed with `dist/cli/sharpee.js --exec … --seed 1` on 2026-09-08.
- `docs/work/john-chord-samples/no-signal-home.story` rewritten in place as raw source (1323 lines; line numbers unchanged from the listing).
- `docs/work/john-chord-samples/syntax-review.md` written: eight sections plus a "done right" close. Findings with evidence: `activate the target` / `board the target` shadow standard verbs game-wide (ADR-268 tier rule); a plain exit is two-way so the Maintenance Shaft → Engine Room link is a trapdoor overwritten by the Engine Room's own `north` line; entity-tier topic rows are scope-bound so `about the pressure hatch:` can never fire at Reed; the cargo bulkhead's key is offstage so the Cargo Hold is unreachable; `pryable` reads states declared on the desk instead of the trait, and the desk has two keys for one lock; the cables' tool gate is declared twice; `aka me, myself, self` is redundant; hand-typed `*** THE END ***` in ending phrases; a table of declared states nothing ever sets; on David's follow-up, a section on `{br}{br}` used where a blank line already breaks the paragraph (verified with a test story: blank lines work in inline `phrase` bodies under `on` clauses and in `define phrases en-US` entries), with the single `{br}` confirmed as the correct line-break form. On David's ruling ("note this as the official CLI test since John is on Windows and has no access to the Mac IDE"), a leading section names the author-side path: `sharpee compose --check` (gate-clean today), `sharpee play .` with piped stdin (verified), and `sharpee test .` on a hand-written tree document (verified: three hand-added turn cards pass, 5 cards / 7 assertions). David then identified the ADR he meant: ADR-307 AC-2 (tab and `sharpee test` consume the same `<story-id>.tests.json`); the review now records John's Windows CLI run as the official CLI-side check of that criterion. ADR-307 itself is untouched pending David's word on amending AC-2.

## Key Decisions
- None. No plan phase touched; the survey plan keeps the pointer.

## Open Items
- The refactoring-survey plan's HELD phases (3, 5, 14, 4b) and Phase 9 remain the next attended work, unchanged by this session.

## Files Modified
- `docs/work/john-chord-samples/no-signal-home.tests.json` — created by the first `./sharpee test` run (opening + boot cards); kept as the seed tree document the review's Windows section describes
- `docs/work/john-chord-samples/no-signal-home.config.json`, `docs/work/john-chord-samples/dist/` — generated by `./sharpee play` / `compose` probe runs; not review content, disposition is David's call
- `docs/work/john-chord-samples/no-signal-home.story` — gutter and title line stripped (was a numbered IDE listing; directory moved by David from `docs/work/actor-platform/` mid-session)
- `docs/work/john-chord-samples/syntax-review.md` — new
- `docs/context/.devarch-gate-485d98` — cleared after Session Start

## Notes
- Session started: 2026-09-08 18:36 CDT (session 485d98)
- Probe scripts (compile, map symmetry, unset states) live in the session scratchpad, not the repo.

---

## Session Metadata

- **Session**: 485d98
- **Status**: COMPLETE
- **Blocker**: N/A
- **Blocker Category**: N/A
- **Estimated Remaining**: N/A
- **Rollback Safety**: safe to revert (documentation and one sample story only; no source touched)

## Mutation Audit

- Files with state-changing logic modified: none (documentation and a sample `.story` only).
- Tests verify actual state mutations: N/A.

## Recurrence Check

- Similar to past issue? NO. The IDE-only recording gap for Windows authors is already the subject of ADR-341.

## Test Coverage Delta

- Tests added: 0 in the platform. One tree document (`no-signal-home.tests.json`, 2 cards) created beside John's sample by `./sharpee test`.

**Progressive update**: finalize invoked 2026-09-08 ~19:25 CDT; committing to `main` per the standing rule that docs and session records land on main, then returning to `refactor/survey-adr-334-340`.
