# Session Summary: 2026-09-14 - main

## Goals
- Pull `main` up to date with `origin/main` without losing local uncommitted state.
- Dispose of the orphaned `opensilver-vs-wpf-spike` plan per rule 18b after the pull repointed `.current-plan`.
- Run the ADR-351 open-questions interview and, if the Windows machine is available, close Q-1 (the Avalonia Phase 7 Windows check) for real.

## Phase Context
- **Plan**: `docs/work/avalonia-ide-evaluation/plan.md` (Avalonia + Velopack ("O6") evaluation — a single-codebase, natively-rendered shell for Chord Writer) — now archived, see Next Phase.
- **Phase executed**: Phase 7 — "Windows check (optional, separate machine, scheduling-dependent)" (Small tier)
- **Tool calls used**: 122 / 100 (session-state `docs/context/.session-state-6c19b3.json`)
- **Phase outcome**: Ran over budget — the 100-call budget was Phase 7's alone; this session also carried the `git pull`/stash sequence, a rule-18b plan disposition, and an ADR-351 interview round ahead of it.

## Completed

### `git pull` reconciliation (main, 8 commits behind, 0 ahead)
- Verified byte-identical overlap before pulling: `docs/architecture/adrs/adr-341-chord-writer-windows.md` and all seven staged `docs/work/adr-341-spike/` → `docs/work/archive/adr-341-spike/` renames diffed clean against `origin/main`. Only `.devarch/descriptor.json` (9.2.1 → 9.5.3) and `docs/context/.current-plan` were genuinely local.
- `git restore` was refused by the auto-mode classifier as irreversible; used `git stash push -u -m "pre-pull-6c19b3: local state superseded by origin/main"` (`stash@{0}`, still present and now redundant — see Notes) then `git pull --ff-only origin main`, a clean fast-forward `11a60b8f6 → a2e7329f4` (confirmed: `git log --oneline -3` shows `a2e7329f4` at HEAD). `.devarch/descriptor.json` was restored from the stash afterward (confirmed via `git diff`: `9.2.1` → `9.5.3`).
- Brought in the full IDE host-shape evaluation set (`opensilver-ide-evaluation/`, `avalonia-ide-evaluation/`, `web-ide-evaluation/`), ADR-349/350/351 (DRAFT), and a bootstrap/transcript-tester/branch-tester fix.

### Rule 18b — `opensilver-vs-wpf-spike` plan disposed as ABANDONED
- The pull repointed `.current-plan` to `docs/work/web-ide-evaluation/plan.md`, orphaning the local, untracked `opensilver-vs-wpf-spike` plan. David chose **Abandoned**.
- Confirmed in `docs/work/archive/opensilver-vs-wpf-spike/plan.md`: `**Plan Status**: ABANDONED (2026-09-14, session 6c19b3 …)`, all seven phases stamped `ABANDONED … never started`. Archived via `plan-archive.sh opensilver-vs-wpf-spike`.

### ADR-351 open-questions interview, Q-1 — Avalonia Phase 7 on Windows
- David chose "run Phase 7 now." `.current-plan` was repointed to `docs/work/avalonia-ide-evaluation/plan.md` (its Phase 7 was PENDING since 2026-09-13); the session gate `docs/context/.devarch-gate-6c19b3` had to be cleared mid-session (session-start step 4a had not run).
- **Q-1 — WebView2 response-supply door: ANSWERED YES.** Confirmed by direct read of `evidence/phase-7-windows-door.txt`: `AdapterCreated: handleType=...WebView2HwndAdapter`, `QI ICoreWebView2_3 hr=0x00000000`, `SetVirtualHostNameToFolderMapping(...) => hr=0x00000000`, then a real navigation to `https://sharpee-panes.invalid/index.html` reading back `document.title`/`location.origin`/a JS marker/`h1` text, all host-supplied, with no `HttpListener` in the process. `PaneHost` built on Windows with 0 warnings/0 errors (corroborated by the session's own event log, `docs/context/.devarch-events-6c19b3.jsonl`: repeated `"kind":"build","msg":"Build passed"` rows against `win-door`/`PaneHost` dotnet builds, timestamped 04:38–04:46Z, after the relevant edits). A first re-run reported a null platform handle — a timing artifact (queried before the adapter existed at `OnLoaded`); asked after the window opened, it returned correctly. Both readings are in the evidence; the later one is the finding.
- **Q-1, packaging: Velopack 1-for-1 on Windows** (0-for-1 on macOS). `vpk pack` produced `SharpeePaneHost-win-Setup.exe` (56,446,248 B), a portable zip, and a full nupkg (51,984,680 B) in 8.4 s; a 1.0.1 re-pack produced a 72,533-byte delta. `--azureTrustedSignFile` exists in vpk 1.2.0. Not established: any signed build or install run (needs David's Azure identity; `Setup.exe` was never executed).
- **Q-1, toolchain: does not port.** `tools/ide/vendor-toolchain.sh:298` writes a `#!/bin/sh` shim requiring `$root/node/bin/node`; `tools/ide/vendor/node/` ships only `darwin-arm64`/`darwin-x64` tarballs — no Windows Node asset exists. GH #448 sits downstream; the cost is identical under WPF, not an Avalonia-specific one.
- Not established by the phase: the play/testing panes (loopback re-run served `sharpee-docs` at 200 but 404'd on `sharpee-play`, because `branch-stories/fernhill/dist/web/fernhill` has never been built on this clone — a missing local artifact, entangled with the toolchain finding, not a separate Windows gap).
- Consequence recorded in ADR-351: D3 ("one host contract") now answers differently per platform — Windows gets a stable, port-free, token-free origin; macOS keeps its token-scoped loopback fallback.

### ADR-351 folds and Plan Status
- `docs/architecture/adrs/adr-351-chord-writer-host-shape.md`: Status line updated (Windows condition discharged, two of David's three D2 conditions remain), D2's Phase-7 discharge sentence added, D3 rewritten with the per-platform WebView2 finding, Consequences updated (×2), R5 matrix cell annotated with a note rather than rescored (deliberately — D5 makes the matrix David's; rescoring would move the weighted total 183 → 186 and change the ruling's margin), and the resolved Q-1 bullet removed from Open Questions. Confirmed via direct read: 5 Open Questions remain (Q-2 through Q-6), Status still `DRAFT`.
- `docs/work/avalonia-ide-evaluation/plan.md` Phase 7: PENDING → DONE with its three-question verdict inline (confirmed by direct read).
- All 8 phases (0–7) of this plan are now DONE (confirmed by reading every `- **Status**:` line). Per rule 4, its last phase closed this session, so **Plan Status was set to DONE** (this session's own edit, citing that Phase 6's deliverable-4 ADR draft was deliberately produced elsewhere — ADR-351, under `web-ide-evaluation`'s plan — not a gap in this plan). Per rule 4a, the plan was archived immediately: `bash ~/.claude/scripts/plan-archive.sh avalonia-ide-evaluation` → moved to `docs/work/archive/avalonia-ide-evaluation/` and released `.current-plan` (confirmed: the pointer file no longer exists, and `git status` shows the whole directory renamed under `archive/`).

## Key Decisions

### 1. Stash-and-restore over `git restore` for the pre-pull local state
The auto-mode classifier treats `git restore` as irreversible local destruction; `git stash push -u` plus a selective restore of only the genuinely-local file (`.devarch/descriptor.json`) achieved the same end state reversibly, and the stash is still recoverable if the byte-identical comparison turns out to have missed something.

### 2. R5 matrix cell left unrescored
Phase 7 undercuts the stated reason for Avalonia's R5 score (the WebView2 door beats the macOS loopback fallback it was scored against), but ADR-351 D5 assigns the weighted matrix to David alone. A note was added to the cell instead of a silent rescore; rescoring would have moved the weighted total from 183 to 186, changing the ruling's own margin without his say-so.

### 3. Avalonia plan closed as DONE and archived despite ADR-351 staying DRAFT
The plan's own scope (produce evidence for a parity column and decision record) is fully discharged across all 8 phases; the ADR question that evidence feeds is a separate document with its own rule-11a lifecycle. Treating the plan's completion as contingent on the ADR's acceptance would conflate two different artifacts' terminal states.

## Next Phase
Plan complete — all phases done; the plan is archived at `docs/work/archive/avalonia-ide-evaluation/`. `.current-plan` is unset. The live thread of work is not a plan phase but the **paused ADR-351 open-questions interview**: Q-2 (web-app fallback tier, deferred by David) and Q-3–Q-6 (felt comparison, Swift-app retirement, macOS bundle layout, requirement-list maintenance) all need macOS, where their evidence lives. Resume with `/devarch:adr-interview adr-351` on the Mac.

## Open Items

No new issue-store items were filed this session. The substantive follow-up is ADR-351's own Open Questions (Q-2 through Q-6), tracked by rule 11a directly on the ADR rather than duplicated into the issue store; see `docs/architecture/adrs/adr-351-chord-writer-host-shape.md`. Two small loose ends are left for David's own next action rather than filed as tracked work (see Notes): `stash@{0}` is fully redundant, and `tools/spikes/` is untracked build residue.

### Short Term
- None new.

### Long Term
- None new.

## Files Modified

**Documentation / ADR** (2 files):
- `docs/architecture/adrs/adr-351-chord-writer-host-shape.md` - Q-1 resolved and folded into Status, D2, D3, Consequences, and the R5 matrix note; five Open Questions (Q-2–Q-6) remain, Status stays DRAFT.
- `docs/work/archive/avalonia-ide-evaluation/decision.md` - new §12 "Phase 7 addendum — confirmed on Windows, with one correction" (path reflects the archive move made this session).

**Plan / phase records** (now under `docs/work/archive/avalonia-ide-evaluation/`, moved there by this session's own archive step):
- `plan.md` - Phase 7 PENDING → DONE with its verdict inline; `Plan Status` ACTIVE → DONE.
- `phase-7-windows-check.md` (new) - the Phase 7 record.
- `evidence/phase-7-windows-door.txt`, `evidence/phase-7-panehost-rerun-log.txt` (new) - raw evidence logs.

**Plan disposition** (1 file, now archived):
- `docs/work/archive/opensilver-vs-wpf-spike/plan.md` - Plan Status set to ABANDONED, all 7 phases stamped ABANDONED, per David's rule-18b choice.

**Local/session state** (2 files):
- `.devarch/descriptor.json` - `9.2.1` → `9.5.3` (restored from the pre-pull stash; a local DevArch version bump, not Mac-originated work).
- `docs/context/.current-plan` - repointed twice this session (web-ide-evaluation → avalonia-ide-evaluation), then removed when the Avalonia plan archived.

**Not committed to the repository (by design)**: spike code at `C:\Users\David\spikes\avalonia-ide\` (`win-door/` probe project, OS-aware `SpikePaths`, Windows platform-handle logging, a `VelopackApp.Build().Run()` call added to `PaneHost/Program.cs`) — kept out per the evaluation's evidence-inline discipline; only the record lives under `docs/work/`.

## Notes

**Session duration**: session-state `startedLocal` 2026-09-14 21:15 PDT; the session's own event log runs 04:15–04:47Z (~30 minutes of logged tool activity against 122 tool calls).

**Approach**: Verify-then-act throughout — byte-diffed every overlapping path before pulling, read evidence files directly rather than trusting narrated results, and treated the plan/ADR distinction as load-bearing when deciding what "done" meant for each artifact.

**Housekeeping left for David, not filed as tracked items**: `stash@{0}` ("pre-pull-6c19b3: local state superseded by origin/main") is fully redundant now that the pull and restore are confirmed clean — safe to drop. `tools/spikes/adr-341/` is untracked residue from C# sources the Mac's pull already retired elsewhere; disposition (delete vs. keep) is his call.

**Gap in session-state tracking**: `docs/context/.session-state-6c19b3.json`'s `files` array lists only two paths (the state file itself and `phase-7-windows-check.md`), far short of the actual file set this session touched — most of this session's edits were made through direct tool calls to files the hook tracker evidently didn't attribute, plus `git mv` operations from the archive script. `git status` was used as the authoritative file list instead, per the discrepancy-handling guidance for this section.

---

## Session Metadata

- **Session**: 6c19b3
- **Status**: COMPLETE
- **Blocker**: N/A
- **Blocker Category**: N/A
- **Rollback Safety**: safe to revert — nothing this session was committed or pushed; all changes (including the `git mv` moves from `plan-archive.sh`) remain in the working tree.

## Dependency/Prerequisite Check

- **Prerequisites met**: Windows 11 machine available and free for Phase 7 (plan's stated entry condition); .NET SDK present (10.0.401 — see Architectural Decisions for the version deviation); Avalonia 12.1.2, `Avalonia.Controls.WebView` 12.1.0, `Avalonia.AvaloniaEdit` 12.0.0 already resolved from Phase 0; `vpk` 1.2.0 installed this session; WebView2 Evergreen runtime 153.0.4234.32 present.
- **Prerequisites discovered**: the vendored toolchain has no Windows Node asset at all (`tools/ide/vendor/node/` ships only darwin tarballs) — a prerequisite gap for any future Windows toolchain work, not something this session could supply.

## Architectural Decisions

- ADR-351 (DRAFT, `docs/architecture/adrs/adr-351-chord-writer-host-shape.md`) — D3 amended: the WebView2/WKWebView response-supply contract now answers differently per platform, discharging one of D2's three conditions (Windows check). Two conditions remain (macOS bundle layout, felt comparison), both David's, both needing the Mac.
- **Deviation recorded, not silently accepted**: the transfer note pinned .NET SDK 10.0.300; this machine has 10.0.401 installed, with no `global.json` pinning either version. Recorded as a deviation in the phase record rather than treated as equivalent.
- Pattern applied: rule 18b disposition (Abandoned) for the orphaned `opensilver-vs-wpf-spike` plan; rule 4/4a plan-completion-and-archive for `avalonia-ide-evaluation` once its last phase closed; rule 11a Open-Questions interview mechanics for ADR-351 (one question at a time, folded immediately, no silent resolution).

## Mutation Audit

- Files with state-changing logic modified: N/A — no application source files in this repository were touched this session. All state-changing work (WebView2 navigation, file downloads, packaging) happened in the out-of-repo spike at `C:\Users\David\spikes\avalonia-ide\`, kept outside version control by design.
- Tests verify actual state mutations: N/A (no in-repo mutation code this session).

## Recurrence Check

- Similar to past issue? NO — this is the fourth and (per ADR-351) concluding host-shape evaluation phase in a series that has been running across `opensilver-ide-evaluation`, `avalonia-ide-evaluation`, and `web-ide-evaluation`; no new defect class was found, only the expected per-platform confirmation the phase was scoped to produce.

## Test Coverage Delta

- Tests added: 0 in this repository. The spike project's own build passed repeatedly (evidence: `docs/context/.devarch-events-6c19b3.jsonl`, multiple `"kind":"build","msg":"Build passed"` rows for `win-door`/`PaneHost`, 04:38–04:46Z, timestamped after the corresponding edits) and the Q-1 door check ran for real against the production WebView2 adapter (evidence: `docs/work/archive/avalonia-ide-evaluation/evidence/phase-7-windows-door.txt`, read directly).
- Known untested areas: signed Velopack build and an actual install run (need David's Azure Trusted Signing identity); the play and testing panes on Windows (blocked on `branch-stories/fernhill/dist/web/fernhill` never having been built on this clone).

---

**Progressive update**: Session completed 2026-09-14 21:47 (local)
