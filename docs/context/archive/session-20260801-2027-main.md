# Session Summary: 2026-08-01 20:27 - main (CDT)

## Goals
- Plan the ADR-294 transcript-tester rebuild (done — `docs/work/adr-294-rebuild/plan.md`, 5 phases, David approved).
- Fold plan-review advisories: ADR-293 D14 + ADR-282 superseded notes; NDJSON exit check in Phase 1 (done).
- **Phase 1 (CURRENT)**: transcript grammar rewrite — header provenance fields, removed-form parse errors (AC-4), `.golden` format reader/writer, typed provenance in `types.ts`, NDJSON data preserved. Parser-only; no bundle/repokit changes.

## Completed
- Session-planner wrote `docs/work/adr-294-rebuild/plan.md`; plan-review: no contradictions, 3 advisories, all folded.
- ADR-293 D14 amended (transcript-directive `[SEED:]` shape superseded by ADR-294 D3 header field).
- ADR-282 amended (Acceptance §2 + Consequences: `[OK: any]`/`[ENSURES]` removed per ADR-294 D4).
- `.current-plan` → `docs/work/adr-294-rebuild/plan.md`.

## Phase 1 COMPLETE (evidence inline, 2026-08-01 ~20:38 CDT)
- **Header run config (D3)**: `seed:`/`seeds:`/`channels:`/`events:`/`locale:`/`forces:` parsed into typed `TranscriptRunConfig` with defaults (`channels: ['main']`, `events: false`); duplicates, empty lists, non-integers, >MAX_SAFE_INTEGER seeds, `seed:`+`seeds:` conflicts, and below-`---` placement are all named parse errors. `transcript.seed`/`seedLineNumber` still populated (singular form), so `bundle-entry.js` chain-rule/`resolveSeed` work unchanged — bundle untouched this phase.
- **Removed forms (AC-4)**: body `[SEED:]`, `[WHILE:]`, `[END WHILE]`, `[ENSURES:]`, `[NAVIGATE TO:]`, `[OK: any]` + retry family `[RETRY:]`/`[END RETRY]`/`[DO]`/`[UNTIL]` (scope call: D4/D9 delete retry machinery wholesale) → parse errors naming form + replacement, checked BEFORE header parsing so the old above-`---` `[SEED:]` trap errors loudly. ADR-287 fenced payloads preserved; removed-form text inside literal blocks stays uninterpreted.
- **`.golden` format (D7)**: new `src/golden.ts` — `serializeGolden`/`parseGolden`/`parseGoldenFile`/`GoldenFormatError`. Strict v1 provenance (9 required keys, unknown/dup = error); turns verbatim; blank-line-separator symmetric strip (lossless for output ending in blanks); `• type {json}` event lines (raw-string JSON for byte-faithful round-trip) gated on `events: true`. Round-trip byte-identity pinned by test.
- **Evidence**: 118 tests passing (was 63) — `pnpm exec vitest run` in the package; `tsc --noEmit` clean; `git status` confirms surface = transcript-tester + docs only; `navigator.ts`/`condition-evaluator.ts` untouched (Phase 2 deletes them after David confirms). NDJSON exit check: aggregate.test.ts (12) green against new types — per-command status/line data intact.
- Corpus fact recorded for Phase 5: NO transcript in stories/ uses body `[SEED:]` (seeds were CLI-pinned); 40 files use other removed forms (49× ENSURES, 22× OK:any, 15× UNTIL/DO, 14× WHILE, 11× RETRY, 6× NAVIGATE).

## Key Decisions
- (carried) ADR-294 is the accepted spec; this plan implements ACs 1-9 only; ACs 10-14 follow-on gated on ADR-293 Phase C.

## Phase 2 COMPLETE (evidence inline, 2026-08-01 ~21:05 CDT)
- **Golden engine (runner.ts rewritten, ~700 lines vs 1621)**: tier selection (bless → record; `.golden` sibling → replay; else assertion tier); record writes provenance-stamped verbatim recordings (no write if any directive fails); replay verifies provenance + command list STATICALLY before executing (stale = named error, never a content diff), diffs output byte-for-byte with only the `Story vX (built …)` banner line masked (D6), stops at first divergence carrying `{recorded, actual}`; D5 unconditional directive failure in both tiers; D2 boundary — assertion-less command with no recording is a named failure.
- **Deleted (David confirmed)**: `navigator.ts` (357), `condition-evaluator.ts` (390), smart-runner control flow, `ok-any`/`contains-any`/`matches` evaluation, `updateExpected`/`filter` options.
- **Scope calls flagged for David**: `[IF:]`/`[END IF]`/`[REQUIRES:]` removed (59/3 corpus uses — condition evaluator gone; conditions can't vary at a pinned seed); `[OK: contains_any]`/`[OK: matches]` removed (49/0 uses — D2's retained list is exhaustive; fuzzy DSL = coping); `[GOAL:]`/`[END GOAL]` kept as structural labels (114 uses, migration-free). `[STATE:]`/`[EVENT:]`/`[EVENTS:]`/`[TODO]` retained.
- **One engine touch**: `SAVE_FORMAT_VERSION` exported from save-restore-service.ts (existing constant; D3 provenance needs it). Note: `tsf build` refreshed engine dist (CJS) but NOT dist-esm — needed `pnpm --filter engine build`; watch for stale dist-esm generally.
- **CLIs**: `--bless` in bundle-entry.js + cli.ts; validation errors now push error-status records (never dropped; exit code reflects them via aggregateTestRun — bundle's hand-rolled reduce replaced); chains abort at first non-passing member (one session); chain-rule message re-worded for `seed:` header.
- **Evidence**: 139 tests passing; `npx tsf build` + `./repokit build dungeo` green (bundle 3455296); fixture AC-1..AC-5 through the bundle vs dungeo at seed 42 all pass (see plan Phase 2 evidence line for the per-AC detail).
- **Known planned breakage**: the wt-* chain and ~40 unit transcripts use removed forms — corpus migrates in Phase 5 (D12 ordering); until then the dungeo transcript suites fail on parse errors BY DESIGN.

## Phase 3 COMPLETE (D8+D18; D15 paused) — evidence inline, 2026-08-01 ~21:20 CDT
- **D8 seed matrices**: `goldenPathFor(path, seed)` → `<name>.<seed>.golden`; bundle-entry runs a matrix transcript once per declared seed (`Seed: N (seeds:)`), `--seed`/`--vary` collapse it (runner enforces membership); chain+matrix is a loud error. AC-7 real-path: two recordings blessed; tampering 777's failed only 777's replay.
- **D18 divergence saves**: pre-turn engine save captured in memory each replayed command; on divergence written to `<transcript>.divergence.json` with restore hint (`--restore <path> --seed N`, `--restore` now accepts paths); green replay clears stale saves; capture failure degrades to no-file. AC-6 real-path: restored one command before the divergence, divergent turn reproduced 1114 bytes identical. `*.divergence.json` gitignored.
- **D15 channel scoping PAUSED** (plan's stop-and-flag rule): needs platform surface — bootstrap `CLI_CAPABILITIES` disables non-main channels in test mode and `LoadedGame` has no per-channel capture. Proposal drafted in plan Phase 3 evidence note (LoadedGame `lastChannels` + capability profile from `channels:` declaration). AC-8 waits.
- **Evidence**: 145 tests passing; `./repokit build dungeo` green (3458437); `tsf build --npm` green.
- **Mutation audit (Phase 3)**: clean — all six golden-dimensions tests GREEN (real file-state assertions); divergence write correctly gated, cleanup correctly scoped. Flagged gap closed same session: chain+matrix guard exercised through the shipped bundle (`seeds:` member in a `--chain` run → loud ADR-294 D8 error, exit 1); matrix loop already covered by the AC-7 bundle run.

## Phase 4 COMPLETE (evidence inline, 2026-08-01 ~21:35 CDT)
- **watch.ts**: classifyChange (watched transcript/goldens → that test; unwatched artifacts + saves/ + divergence files ignored; other story-dir changes → all), BlessPolicy (no prompt = never bless; `all` sticky), runCycle (bless offered only for golden-tier non-passes incl. stale errors; blessed goldens registered for suppression IMMEDIATELY — mid-cycle fs-event hole found and fixed), startWatch (debounced, serial, self-write window 2s).
- **bundle-entry**: `--watch` flag; non-chain path extracted to `runTranscriptFileFresh` (re-parses on rerun; fresh cache-purged story per run); TTY-gated readline bless prompt; `--watch --chain` named error.
- **AC-9 evidence** (headless driver): story dist edit reran both watched transcripts (3 seed-runs); golden tamper reran only its transcript with the diff; no prompt, nothing blessed, tampered golden untouched. Interactive path pinned by unit tests.
- 158 tests passing (watch suite run 3×, no flake); `tsf build --npm` green; repokit untouched; bundle 3469373.

## Phase 5 IN PROGRESS (evidence inline, 2026-08-01 ~22:10 CDT)
- **wt corpus migrated**: migrate-wt.js stripped removed grammar + resolved 55 IF blocks against a fresh old-runner ground-truth run at seed 42 (obtained via git stash → HEAD rebuild → verbose chain run, 944/0 → stash pop); resolve-loops.js spliced the 6 loop regions from the log; wt-13's fight re-derived empirically. `seed: 42` pinned on wt-01 (chain rule).
- **Story fixes at the new stream state**: wt-07 — thief camps Round Room during carousel routing, one `wait` and he leaves (probed via --exec from wt-06 save); wt-13 — disarm drops his stiletto making "knife" ambiguous, fight now: take stiletto + 7 attacks (seed-42 kill sequence), and grail/coins recovered here (thief had stolen them pre-wt-12; wt-12's 4 stale commands removed); wt-01 keeps one harmless post-kill attack (removing it would shift all downstream turn-draws).
- **All 17 goldens blessed** (952 turns); chain replay green ×2 in separate processes, byte-identical except the duration line (AC-1); endgame completes (Treasury of Zork, MASTER ADVENTURER; "Main game score: 0/650" display matches the old runner — pre-existing). ACs 2/3/6 re-verified on the corpus; AC-4 via the 17 unit files; AC-8 blocked on D15.
- **Unit suite (116 transcripts)**: 99 pass (1473 commands, 5 expected failures, 2 skips, ZERO failed commands); 17 error on removed forms (adr-287-fenced-literals, annotations, carousel, combat-disengagement, cyclops-magic-word, debug-combat, gdt-aa-score, maze-navigation, rank-survives-ceiling-change, save-restore-basic, smart-directives-basic, troll-blocking/combat/interactions/recovery/talking/visibility). Awaiting David's rewrite-vs-triage ruling.
- **Gates**: `./repokit verify` green through entropy gate + tsf npm (known 4.3.0 dry-run state); dungeo vitest 31/31.
- **New story-bug candidate (unfiled)**: attacking the staggered thief yields "Your attack has no effect on the nasty stiletto" — outcome message binds the weapon as target.

## Open Items
- Carried: post-293 coverage walkthrough; `IDebugEvent` dead tier; `ring-action.ts:141` bug unfiled; publish dry-run 4.3.0 state.
- ADR-282 earlier sections (D2 untagged-turn flow, Acceptance §1) still describe `[OK: any]` saves — left for a fuller sweep if David wants one.
- NEW follow-ons from Phase 5 closure (David ruled): 17 removed-form unit transcripts triaged as cleanup task (loud errors, list in Phase 5 section); other stories' transcript corpora unmigrated; D15 channel scoping + AC-8 pending platform decision; stiletto message bug filed as GH #206.

## Session Metadata (final)
- **Status**: ADR-294 core rebuild COMPLETE — Phases 1–5 all closed. ACs 1–7 + 9 verified (fixture and/or corpus); AC-8 blocked on D15 platform decision. Uncommitted: all phase work + migrated wt corpus + 17 goldens (committing is David's call).

## Files Modified
- `docs/work/adr-294-rebuild/plan.md` (new — plan + folds + Phase 1 COMPLETE)
- `docs/architecture/adrs/adr-293-choice-points-per-point-streams.md` (D14 note)
- `docs/architecture/adrs/adr-282-play-to-test-tagging.md` (Acceptance §2, Consequences)
- `docs/context/.current-plan`
- `packages/transcript-tester/src/types.ts` — TranscriptRunConfig, GoldenProvenance/GoldenEvent/GoldenTurn/GoldenRecording; seed docs updated
- `packages/transcript-tester/src/parser.ts` — header config parsing, removed-form interception, dead matchers deleted
- `packages/transcript-tester/src/golden.ts` (new) — .golden reader/writer
- `packages/transcript-tester/src/index.ts` — golden exports
- Tests: `golden-format` (new, 18), `header-config` (new, 21), `removed-forms` (new, 14); `seed-directive`/`ok-any`/`fenced-payloads` rewritten for new grammar

## Mutation Audit (Phase 2)
- `mutation-verification` ran on the rewrite (rule 15): **clean — no RED, no YELLOW.** All three mutation surfaces (golden bless write, `$save`, `$restore`) have tests reading the persisted artifact back off disk; the restore legacy-rejection gate is correctly asserted via the engine-never-called spy (gate-only contract); staleness gates verified as pre-execution (`engine.calls === []`).

## Session Metadata
- **Status**: IN PROGRESS — Phases 1–2 COMPLETE; Phase 3 (diff dimensions: seed matrices, channel scoping, divergence saves) next, pending David
- **Blocker**: N/A
- **Rollback Safety**: Phase 1 is parser/types/new-module only; bundle not rebuilt, corpus untouched, runner behavior unchanged (removed forms simply no longer reach it)
