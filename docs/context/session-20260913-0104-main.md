# Session Summary: 2026-09-13 - main

## Status: In Progress

## Goals
- Evaluate an OpenSilver IDE that matches the macOS Chord Writer (the WPF-vs-OpenSilver parity spike scoped in GH #438 and session b79c97, never started).

## Completed
- Session Start: recap presented, pre-session audit relayed, core concepts read, gate cleared.
- Context gathered: ADR-341 (D2 resolved WPF 2026-09-12), the archived spike parity table and decision record, GH #438/#439, the local OpenSilver re-host at `~/repos/SecretLetter2026`.
- GH #439 answered from `~/repos/SecretLetter2026/claude-rundown.md`: the re-host migrates the 2009 Silverlight 3 application (Textfyre.UI / Textfyre.VM / SecretLetter) to OpenSilver 3.3.3 — OpenSilver in its designed sweet spot; the IDE would be this project's first greenfield OpenSilver XAML. Not yet closed on GitHub.
- `session-planner` wrote `docs/work/opensilver-ide-evaluation/plan.md` (7 phases, capability before feel); pointer set; plan-review returned three advisory tensions, nothing blocking. David approved Phase 0 start.
- **Phase 0 in progress** (spike at `/Users/david/repos/spikes/opensilver-ide/hello/`):
  - `OpenSilver.Templates@3.3.3` installed; `dotnet new opensilverapp --usePhotino --mauiPlatforms macos windows --targetFramework net10.0` scaffolded four projects (Hello, Hello.Browser, Hello.Photino, Hello.MauiHybrid).
  - OpenSilver 3.3.3, OpenSilver.Photino 3.3.3 and OpenSilver.MauiHybrid 3.3.3 all ship `net10.0` lib targets (nuspec read from NuGet) — no .NET 8/9 install needed.
  - `dotnet build Hello.Photino` on .NET 10.0.300: Build succeeded, 0 errors, 2.3 s. `dotnet run` opened a native window titled "Hello" rendering the XAML "Hello, World!" — screenshot captured (scratchpad `photino-hello-window.png`). Photino.Native.dylib links AppKit + WebKit, so the desktop host is an NSWindow around a WKWebView with OpenSilver rendering XAML to DOM; C# runs on the native .NET runtime, not WebAssembly.
  - Browser target needed the `wasm-tools` workload (NETSDK1147); installed (10.0.111). `dotnet build Hello.Browser`: Build succeeded, 0 errors, 13 s. Dev server on :55592 served `dotnet.native.wasm` (14.7 MB, HTTP 200); Safari rendered "Hello, World!" — screenshot captured.
  - **Phase 0 DONE.** Record: `docs/work/opensilver-ide-evaluation/phase-0-prerequisites.md`; evidence PNGs under `evidence/`. Identified host: `OpenSilver.Photino` (plain .NET process, AppKit window + WKWebView on macOS, WebView2 on Windows, no workload); MAUI Hybrid (Mac Catalyst on macOS) recorded as fallback, unbuilt. Key finding for Phase 5: OpenSilver desktop hosts render XAML as DOM inside the OS web view — native process and window, web-rendered UI — which bears on ADR-341 D2's "not a web application in a window" ground.
  - GH #439 closed with the Silverlight-lineage evidence.
  - Health: Photino.NET 1335★ Apache-2.0, last push 2026-03-26; OpenSilver.Photino pins Photino.NET 3.2.3 while photino.Native's latest release is v4.0.22 (2025-01) — a stale pairing to record. OpenSilver repo pushed 2026-09-12.

- **Phase 1 DONE — PASS** (kill gate — native host capability check). Record: `docs/work/opensilver-ide-evaluation/phase-1-host-capability.md`; plan status updated. Details:
  - Toolchain: `tools/ide/vendor-toolchain.sh` run into `/Users/david/repos/spikes/opensilver-ide/toolchain-staging/` (exit 0, seal verified, signed, 175 MB); symlinked beside the Photino executable as `toolchain/`, the bundle shape.
  - Fixture: `~/Documents/OpenSilver Capability Check/` (dedicated test story + config sidecar, current Chord syntax). Note: `~/Documents/Aliens in Amberville/` uses retired syntax (`authors:` inline, `create the player`) and no longer composes — observed, not touched.
  - Host seam: `Hello/Hosting/{IHostServices,NativeHostServices,BrowserHostServices}.cs` compiled into the shared OpenSilver project; `HostServices.Current` selects by `OperatingSystem.IsBrowser()`; Photino launcher installs `NativeHostServices(toolchainRoot)`. Both targets build with it (Photino 0.8 s, Browser 2.2 s) — the codebase does not fork.
  - In-app auto run (`dotnet run -- --auto`): host info, file write/read round trip MATCH in the real Documents folder, vendored `node --version` → v22.23.1, `sharpee compose --json` exit 0 in ~100 ms with gate-clean IR, `sharpee build` streams 106 lines incrementally then exits 1 (see next).
  - Real-path tests `Hello.Host.Tests` (xunit, source-linked, no stubs): 9 passing, 0 failures, 1 s — includes timestamped incremental-delivery and cancellation-kills-process tests.
  - **Platform finding → GH #457**: the vendored toolchain cannot `build` any Chord story — `packages/character/package.json` has `files: ["dist"]` but `exports.import` → `./dist-esm/index.js`, so `pnpm deploy` ships no `dist-esm` and esbuild cannot resolve `@sharpee/character` from story-loader's ESM runtime. Six more packages share the shape (bootstrap, bridge, runtime, sharpee, branch-tester, transcript-tester). Filed, not fixed (platform change).

- **Phase 2 DONE — PASS** (web-pane hosting). Record: `docs/work/opensilver-ide-evaluation/phase-2-pane-hosting.md`. Test story retargeted to fernhill on David's instruction ("the only story to test with is fernhill"); the Phase 1 tests were retargeted too (compose fernhill, writes to the spike's `out/`), 9 passing. `PaneServer` (host-neutral custom-scheme resolver with serve-time script injection) + `PaneBridge` (iframe ↔ C# via OpenSilver Interop callback) + Photino `RegisterCustomSchemeHandler`. Auto run: Testing pane replayed fernhill's 31-card tree through the real client (31 turn records delivered), typed `inventory`, surface posted the 32-card document → written to `out/fernhill.tests.json` (fernhill untouched); Play accepted a typed `look`; Docs booted with 106 nav links. Finding: WKWebView's native `window.webkit.messageHandlers` is read-only, so the shim must replace `window.webkit` wholesale — panes detect hosts by a WebKit-specific shape (D3 contract note). Owned gap: no custom-scheme origin on the browser target.

- **Phase 3 DONE — measured PASS, felt judgment OPEN** (editor). Record: `docs/work/opensilver-ide-evaluation/phase-3-editor.md`. CodeMirror 6 (from `website/node_modules`) + the real Chord lexer bundled from `packages/chord/src/lexer.ts` (esbuild, 636 KB), served over a third scheme `sharpee-editor://`. On `out/measure-1755.story` (fernhill + 574 lines of ides-of-march; 1755 lines, 8,545 tokens): whole-document style pass 0.92 ms/pass (50-pass loop; WKWebView coarsens `performance.now()` to 1 ms), 200 typed chars at 1.09 ms each; select 897:8–30 → "phrase out-of-the-wind"; replace title → undo → content hash restored; wrap live. WPF spike's comparable figure 14.8–15.9 ms was tree-sitter+query, so the comparison is in kind, not like-for-like — recorded. Tree-sitter not attempted: no Chord grammar exists, no binding in the workspace, and the applicable one here is web-tree-sitter, not TreeSitter.DotNet. The 15-test host suite still passes (the one failing assertion was mine — `game.js` matched the page's leading comment — fixed to `src="game.js"`).

- **Phase 4 DONE — PASS** (shell, custom-drawn surfaces, appearance). Record: `docs/work/opensilver-ide-evaluation/phase-4-shell-and-appearance.md`. David's push ("three horizontal panes, one clearly for debugging") turned the phase into a mirror of `MainWindow.swift` in XAML with the real panes mounted: `Theme/ThemeManager.cs` (18 Theme.swift tokens, one brush each), `Shell/{TabStrip,EditorTabBar,WorldMapView,ProjectPane}.cs`, `MainPage.xaml(.cs)` (chrome band, rail, GridSplitter columns/rows, right + bottom tab strips, status bar with `sharpee --version`); harness kept under `--harness`. Drawing-model verdict: retained composition (WinUI 3's side), HtmlPresenter canvas as the escape hatch. Live flip probe: DOM rail rgb(220,224,232) → rgb(22,23,29) after one Apply() (0.9 ms), no rebuild — ADR-297 D2 PASS. World map fed by the real toolchain (`compose -o` + `world-index`). Screenshots light/dark in evidence. Two fixes on the way: Photino needs `SetUseOsDefaultSize(false)` for `SetSize` to apply; the `_logScroller` init-order NRE (first shell run rendered blank — root exceptions now printed to stdout by App.xaml.cs).

- **Phase 4 follow-ups from David's hands-on run**: (1) dividers stuck/froze → iframe drag guard in `PaneBridge` (pointer-events off on iframes during a XAML-page drag) — David confirmed "sliders work"; (2) "add the chrome macOS menu … has to be OS-agnostic" → `Shell/MenuBar.cs`, an in-app bar from Popups (OpenSilver 3.3.3 has no Menu control; only the toolkit's ContextMenu) mirroring MenuBuilder.swift, with ⌘/Ctrl shortcuts forwarded from the page and from inside panes; (3) "the index isn't implemented" → `Shell/IndexView.cs` off the retained IR (headline 13 rooms · 2 regions · 46 things · 4 people · 3 actions · 56 phrases; section tabs; span rows → editor select), stale-marked on edit, fed by compose-on-edit through `out/live/` (never the real story). Evidence: `phase-4-shell-{dark,light}-index.png`.

- **Phase 4, second hands-on pass** (David's punch list): double-click opens / single-click selects (as the outline view); multi-document tabs with close and buffer hand-off on switch; image viewer + audio player over a read-only `sharpee-story://` scheme; Testing badge removed (macOS badges only Problems/Game Errors/Diagnosis — the 31 was my invention); tab strip fits its width with truncation; World tab has Map/Reach/Incomplete (`Shell/WorldView.cs`). Noted for David: the shell composes on edit like ComposeScheduler.swift; it does not auto-build. Evidence: `phase-4-shell-dark-world-sections.png`, `phase-4-shell-light-image-viewer.png`. fernhill still untouched.

- **Phase 4, third hands-on pass**: (1) Build tab empty on first build → **no SynchronizationContext in the OpenSilver Photino host**; post-await code runs off the UI thread; fixed by dispatching every UI touch in BuildAsync (general rule for any OpenSilver shell); (2) Test Run hung → host duty implemented: `sharpee test <live story> --tree --capture-output --capture-world --json` relayed as runLine/runExit into the surface (TestRunner.swift's arguments); (3) audio Error → WKWebView media needs Range responses a Photino scheme delegate can't give; inlined as data: URLs (clips ≤ 32 KB), shipping host needs a range-capable origin. Side finding: vendored `build` succeeds for the in-repo fernhill because esbuild walks up to the repo's node_modules — commented on #457. Evidence: `phase-4-shell-light-build-tab.png`, `phase-4-shell-dark-test-run.png`, `phase-4-shell-light-audio-player.png`.

## Key Decisions
- **David, 2026-09-13: the desktop host is a .NET-native host, not Tauri.** The first planner brief carried Tauri forward from the Secret Letter precedent and session b79c97's notes; David caught it ("why would we pair Tauri with OpenSilver?"). With a .NET-native host, subprocess spawning, filesystem access, and the vendored Node toolchain are plain .NET calls, so the "kill on capability" phase shrinks to a native-host capability check rather than an IPC-bridge build. Planner re-briefed accordingly.

## Open Items
- Verify the exact OpenSilver desktop-host mechanism (MAUI Hybrid or otherwise) and its .NET SDK requirements in Phase 0 — this Mac has only .NET 10.0.300.
- Close GH #439 with the lineage evidence (plan Phase 0).

## Files Modified
- `docs/context/session-20260913-0104-main.md` - this file

## Notes
- Session started: 2026-09-13 01:04 CDT
- Environment verified: .NET SDK 10.0.300 only; Node 25.8.1; no OpenSilver `dotnet new` templates installed.
