# Session Summary: 2026-08-01 22:30 - main (CDT)

## Goals
- Triage + clean up the 17 unit transcripts erroring on ADR-294 removed grammar forms (follow-on from the ADR-294 rebuild closure).

## Completed
- **Triage** (David approved): A-delete (2), B-mechanical (5), C-drop-RETRY (6), D-empirical-at-seed (4).
- **Deleted**: `smart-directives-basic` (tested the removed ADR-092 IF/NAVIGATE machinery itself), `debug-combat` (RETRY/DO/UNTIL testbed; story surface covered by troll-combat).
- **Mechanical rewrites**: `gdt-aa-score` + `rank-survives-ceiling-change` (`[OK: any]` → real `[OK: contains]` — NOT `[SKIP]`, see finding below), `adr-287-fenced-literals` (IF-parity section removed — void without IF), `save-restore-basic` (`contains_any` → single `contains` at `seed: 42`), `annotations` (see finding).
- **RETRY-wrapper drops** (`seed: 42` pinned, one verification run each, all first-try green): `cyclops-magic-word`, `maze-navigation`, `troll-blocking`, `troll-interactions`, `troll-visibility`, `troll-talking`.
- **Empirical rewrites at seed 42** (probed via `--exec ... --seed 42`): `troll-combat` (blow 1 KO "dreamland", blow 2 kill "removes his head"), `combat-disengagement` (KO → flee → recovered → miss → wound persists on diagnose), `troll-recovery` (wake "stirs" fires on 3rd examine after return), `carousel` (post-button east lands Tea Room; bounce loop gone).
- **`stories/dungeo/CLAUDE.md`**: WHILE/NAVIGATE carousel section replaced with pinned-seed + `--exec` probing guidance; "run chain twice" thief-RNG advice updated (pinned seed = deterministic, only `--vary` varies); stale wt-10 WHILE reference fixed.
- **Evidence**: full unit suite `node dist/cli/sharpee.js --test stories/dungeo/tests/transcripts/*.transcript` → 114 transcripts, 1737 tests, 1725 passed, 10 expected failures, 2 skipped, ZERO failures (2026-08-01 ~22:25 CDT).

## Key Decisions / Findings
- **`[SKIP]` does not execute the command** (runner.ts `runCommand` early-return; inherited from old runner outside DO/WHILE). ADR-294's wording ("output deliberately not asserted") reads as execute-but-don't-assert — grey area, but behavior is inherited, not a regression. Consequence: `[OK: any]` on state-building commands must become real assertions, never `[SKIP]`.
- **Annotation `$` commands are not assertable**: processed out-of-band at transcript load (console output, wall-clock stamps); assertions attached to them are silently dropped. `annotations.transcript` is now smoke coverage + assertions on interleaved game commands, with an explanatory header note.
- **Zero-assertion commands fail in assertion tier** (runner.ts:497, D2 boundary) — every rewritten command carries a real assertion.

## Open Items
- **NEW bug filed as GH #207**: Low Room randomized exit (flag true, seed 42, `> east`) emits a franken-turn — Machine Room description + stale robot line ahead of the Tea Room description + "cannot get your bearings". Redirect appears to render the first-resolved destination before re-resolving. Old `contains_any` masked it. TODO comment pinned in `carousel.transcript` points at #207.
- Carried: D15 channel scoping / AC-8 platform decision; other stories' transcript corpora unmigrated; GH #206 stiletto message bug; post-293 coverage walkthrough; `IDebugEvent` dead tier; `ring-action.ts:141` unfiled; publish dry-run 4.3.0 state.

## Files Modified
- Deleted: `stories/dungeo/tests/transcripts/{smart-directives-basic,debug-combat}.transcript`
- Rewritten/edited: 15 transcripts in `stories/dungeo/tests/transcripts/` (listed above)
- `stories/dungeo/CLAUDE.md` (randomized-rooms + thief-RNG + Low Room sections)

## Session Metadata
- **Status**: COMPLETE — removed-form cleanup done; unit suite fully green. Uncommitted (committing is David's call).
- **Blocker**: N/A
