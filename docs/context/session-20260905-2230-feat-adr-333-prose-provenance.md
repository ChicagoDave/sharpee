# Session Summary: 2026-09-05 - feat/adr-333-prose-provenance

## Status: COMPLETE — chapter-before-room (fb9b60aee, feat/adr-333-prose-provenance); Secret Letter stub pass (126c2b5d9, feat/secret-letter-port); inline play editing (f3c5f13c0) then, on feat/adr-333-inline-play-edit: the facts stamp, topic-row editing with beside-the-character placement, shared-phrase repointing, failure notices, the three gestures (⌘ edit / ⇧⌘ go to code / ⌥⌘ override), ⌘F, and the contents-paragraph blank line — all confirmed by David in play ("confirmed") and finalized 2026-09-06 ~04:00 CDT.

## Goals
- David (2026-09-06): "I had pictured typing IN the play window" → "let's open a sub-branch and try it": inline editing of single-template paragraphs in Chord Writer's Play pane (ADR-333 D4c).
- David's ruling: "the chapter title should be announced before the room." Move the `story.chapter` card ahead of the turn's prose (ADR-330 D4 reversal) via a channel-registry registration position.

## Completed
- **Merge (David: "go")**: `feat/adr-333-inline-play-edit` merged into `feat/secret-letter-port` in the scratch worktree — `957970b58` (five conflicts: wares.chord keeps the port's stub default; ADR-333 takes the amended copy; `.current-plan` stays on the port plan; port session file takes the newer inline-branch copy; blog draft keeps the newer port copy; grubbers-market.chord merged cleanly with David's st-opener edit) + `ec0c62b91` (two tree claims repinned to David's opener line). Secret Letter tree 1468 cards passing on the merged story. Pushed to origin/feat/secret-letter-port. The port branch is now the single copy of Secret Letter with the editor on top.
- **"I added a response for 'ask stallkeeper about gems' and now you can ask that of any stallkeeper"**: a story-loader probe (deleted) showed the FIRST ask renders the shared greeting `st-opener` as the ask event's message, facts and all; ⌘-click edited the shared phrase in place. Ruling (David): editing that response must create a custom response for that specific stallkeeper. Fix: `Entity.topicRows` decoded (topics words + phrase keys per row; `topicCount` derived); `resolve` treats any story phrase rendered by an ask/tell about a known character+topic as topic context — own unshared row → edit in place; row's phrase shared with another character → `.topicRow(replacing:)` repoints that one `phrase <old>` line at `<entity>-on-<topic>` and adds the phrase after the block (a range replacement; `AppendEdit.length`; the coordinator writes through `replaceText`); no row → new row. ⇧⌘/⌥⌘ still reach the phrase. ADR-333 D4d amended. Resolver 30, coordinator 16 (repoint on disk, the other stallkeeper and the shared phrase untouched).
- **⇧⌘-click = "go to this code" (David: "We need a CMD key to 'go to this code' from the play area")**: the bridge posts `goToSource` (⇧); the coordinator skips the inline field and opens the editor at the source — phrase span, existing override, or for a topic-row reply the character's create block (navigateTo the entity span) — and arms the round as D4 did. Gestures now: ⌘-click edits in place, ⌥⌘-click overrides everywhere, ⇧⌘-click goes to the code. ADR-333 D4d sentence added. Coordinator 15, surface 10.
- **Blank line before "You can see … here." (David's ruling)**: root cause in `styles/base.css` — `p.main-entry--tight` zeroed its own `margin-bottom`, and the themes carry paragraph spacing on `margin-bottom`, so the description (tight under the room name) swallowed the gap before the contents paragraph; headless output always had `\n\n`. Fixed: tight zeroes only `margin-top`; the predecessor's bottom margin collapses via `p:has(+ p.main-entry--tight)`. Verified in Chromium by a new Playwright spec `tests/visual/prose-spacing.spec.ts` (computed margins + bounding boxes; the pane's `p:last-child` zeroing needed a trailing paragraph in the probe); `pnpm --filter @sharpee/platform-browser test:visual` → 13 passing. Devkit copies `base.css` from the package's `styles/` at story build, so the next story build in Chord Writer picks it up; no platform rebuild needed. A scratch BrowserClient probe (deleted) showed the contents paragraph is `prose-action-result`, not tight — the DOM was right, the CSS was wrong.
- **David's third play-test ("I can't find the change I just made 'gems'", "CMD-F should allow search")**: his other inline edits had landed (grubbers-market.chord `st-opener`, wares.chord `stall-lift-quietly` — uncommitted in the main repo's branch-stories/secret-letter), but no gems row anywhere and no report. A temporary reproduction against a scratch copy of the REAL Secret Letter (real compose, real editor) wrote the row correctly (`define topics for the gems stallkeeper` merge), so the loss was upstream: the window's `onInlineCommit`/`onEditRequest` guards dropped the round SILENTLY when the tree wasn't `.populated`, and `onUnresolved` only beeped. Fixes: the commit uses the target armed at the click and no longer needs a live compose (`commit(_:storyURL:ir: ComposeStoryIR?)`); every failure now shows a notice in the Play page (`PlayViewController.showNotice` → `__sharpeePlayToWrite.notice`, `PlayToWrite.describe(_:messageId:)` wording); an un-composed story says so at the click. ⌘F: the editor already used the find bar but no menu item reached it — Edit › Find submenu (Find… ⌘F, Find and Replace… ⌥⌘F, Find Next ⌘G, Find Previous ⇧⌘G, Use Selection for Find ⌘E, Hide Find Bar) via `performTextFinderAction:`. Repro test deleted, xcodegen regenerated. Coordinator 14, surface 10, resolver 26.
- **David's second play-test ("end phrase/newline/define phr")**: a stale-offset garble — placement used the compose span's line for the `create` block, and after he deleted the earlier addition and rebuilt, the buffer and that line disagreed. Fix: `PlayToWrite.createBlockEndLine(lines:entityName:)` finds the `create [the|a|an] <name>` block in the TEXT BEING EDITED and its last content line (up to the next column-1 line that is not a `##` comment); `topicRowEdits` no longer takes a span line; the coordinator stops reading the span for placement. New resolver test with a shifted, commented file; the append case now covers a character declared elsewhere. Resolver 26 passing.
- **David's play-test of the topic row (2 issues, both fixed)**: (1) the block landed at the end of the character's file — now `topicRowEdits(createEndLine:)` puts a new `define topics for` block and its phrase right after the character's `create` block (the IR entity span's last line), and a merged row's phrase right after the existing block's `end topics`; (2) clicking out of the inline field then back into it jumped to the command line — the client's document-level click handler (`InputManager`) refocuses `#command-input` on any click, so the field now stops propagation of mouse/pointer events too. Resolver 25, coordinator 13, surface 9 (new click-out/click-in test with the client's refocus mimicked in the fixture); real-path asserts the block sits between the stallkeeper's and Jack's create blocks and the merged phrase right after `end topics`.
- **Topic-row play-to-write (ADR-333 D1 facts + D4d), 3 phases DONE on feat/adr-333-inline-play-edit**: engine stamps `source.facts` (event primitives minus id/params/message/text/turn/`_*`) via domain-message/generic/examined, kept by the description stamp; stdlib carries it; browser exposes `data-source-facts`. IDE: ⌘-click on an ask/tell default reply about an IR-named character opens the inline field on the reply and Enter writes `define topics for <the name>` (`about "<topic>":` → `phrase <id>-on-<slug>`) merged into an existing block or opening one, plus the phrase, in the character's file; ⌥⌘-click keeps the everywhere override. Tests: engine 702, stdlib 1704, platform-browser 155, story-loader 1088 (real-path `adr-333-source-facts`), branch-tester 104, transcript-tester 282, channel-service 119; IDE resolver 24 / coordinator 13 / surface 8 / real path 5 (round trip + merge); full IDE suite 649 passing, 0 failures; Dungeo chain green; SL tree 1468. `./repokit build dungeo` exit 0. ADR-333 amended (D1, D4d, header). Lesson: after changing struct layouts / signatures in the app module, `xcodebuild clean` first — the incremental test bundle went stale and segfaulted on calls into the app module.
- **Secret Letter stub pass (port branch, scratch worktree `scratchpad/sl-port` on `feat/secret-letter-port`)**: all 126 stubs across 18 files rewritten as default lines inside the wrapper (25 in batch 1 — market, wares, disguise, Teisha — shown to David; 101 in batch 2 on "continue"). Test tree `secret-letter.tests.json`: 100-odd pinned fragments of old placeholder text repointed to the new lines (formatting preserved: indent 1, no ASCII escaping); `./sharpee test` (NODE_PATH to the repo's node_modules) → 1468 cards passing, 2643 assertions, 0 failing. Two authoring slips fixed along the way: a `\b` key match that crossed `change-outfit`/`change-outfit-instead`, and fragments that included a `{marker}` or the `Alley`/`Commerce Street` words shared with real prose. UNCOMMITTED in the worktree.
- **Inline play edit, Phase 1 DONE**: `PhraseName` decodes `strategy` + `variantCount`; `PlayToWrite.isInlineEligible/phraseName(for:)/inlineTemplate(source:span:)` + `InlineTemplate.replacement(for:)`; coordinator routes eligible clicks to `onInlineEditRequested` (no tab, not armed) and `commit(_:storyURL:ir:)` replaces the span in the editor buffer, arms, saves → build. Resolver 17, coordinator 10 passing (real editor, real temp file).
- **Phase 2 DONE**: Play page `beginInlineEdit` (textarea in the paragraph; Enter → `playEditCommit` bridge with text+history and restore; Shift-Enter newline; Escape restore), Swift handler → `onInlineCommit`, `beginInlineEdit(_:)` / `beginInlineEditInPlaySurface(_:)`, MainWindow wiring. Surface tests 7 passing.
- **Phase 3 DONE**: real-path tests 4 passing (2 new: a `first time` description and a `define phrase` body typed in Play, real build, replay, same turn shows the new text); full IDE suite 637 passing, 0 failures (was 622); ADR-333 D4c + header amendment written; `mutation-verification` clean (24 functions, no RED/YELLOW). Plan DONE → archived `docs/work/archive/inline-play-edit/`; pointer back on publish-readiness, its stamp repointed to the archive path.
- Session start: recap presented, `pre-session-audit` relayed verbatim, profile fresh (2026-09-04), core concepts read in full, gate cleared.
- Research (read, not assumed): the opener is announced on the first executed turn (browser: the initial `look`), and the browser renderer buffers prose until `preferred-layout` while `story.chapter` registers after every standard channel, so the card always lands below the prose.
- David's mid-turn question "where is my play-content editing feature?": `/Applications/Chord Writer.app` is 1.4.0 built Sep 4 21:13, before the play-to-write commit (fa80abaea, Sep 5 21:34); the Debug build at `~/Library/Developer/Xcode/DerivedData/Build/Products/Debug/Chord Writer.app` (Sep 5 23:44) carries it. Unreleased.
- `session-planner` → `docs/work/chapter-before-room/plan.md` (2 phases); publish-readiness stamped `Superseded by` (still live), pointer moved.
- **Phase 1 DONE**: `IChannelRegistry.add(channel, position?: { before })` + `ChannelRegistrationPosition` (if-domain, exported); `StdlibChannelRegistry.add` rebuilds its Map to insert a NEW id before the named one, throws on an unknown id, replaces an existing id in place; `registerChaptersChannels` registers before `room-name`. Tests: stdlib registry 14 passing (4 new), ext-chapters 8 (1 new), if-domain green. if-domain dist rebuilt; stdlib/ext-chapters tsc clean.
- **Phase 2 DONE**: story-loader `adr-330-chapters.test.ts` 12 passing (real engine manifest: `story.chapter` = banner+1 = room-name-1, before every prose channel and `preferred-layout`; empty registry → loud error; the old D4 case now seeds STANDARD_CHANNELS); platform-browser `chapter-before-room.test.ts` 2 passing on a REAL `BrowserClient` + `GameEngine` + compiled Chord fixture (turn 1: banner → card → room name → room description; `east`: market prose → street card → street room); full platform-browser 154 passing; channel-service 119 passing; story-loader/platform-browser tsc clean. ADR-330 D4 + D5 note amended (dated 2026-09-05, original text quoted), header amendment line; ADR-163 §7 amendment (registration position); website `chapters/content.mdx` rewritten; IDE docs tab rebuilt (164 pages).
- Two test-authoring slips in my own new browser test (wrong log id, then happy-dom lacks `:scope`) fixed and re-run; the product assertions passed on every run.

## Key Decisions
- David (2026-09-06): "the platform printed scenery should be a paragraph with the blank line" — a platform default flip, his call: the Play pane collapsed the gap between a room's description and the "You can see … here." line because `p.main-entry--tight` zeroed its own `margin-bottom` (the themes carry spacing on margin-bottom); the headless renderer always had the blank line. Fix in `packages/platform-browser/styles/base.css`: tight keeps its bottom margin; the predecessor's bottom margin collapses via `p:has(+ p.main-entry--tight)`. Stallkeepers → `a person, scenery` is David's ruling for the STORY, deferred ("story editing will come afterwards"); he is not worried about the two Secret Letter copies.
- Stubs carry a default line (David: "I would prefer we supply a default response the author can edit"; "As long as we identify generated text, it's fine… for the SL port, I'm okay with the helpers"): the `(TODO during play-testing — …)` wrapper stays as the identification of generated text; the brief lives in the `## DAVID:` comments; Gentry's 2009 lines are carried where they exist (cloak → dress swaps only), new lines drafted where the brief said none exists; conversation stubs become one narrated paragraph of the tree's opening in Gentry's words. Memory `feedback_stubs_carry_default_line` written.
- Inline editing edits the TEMPLATE (source at the span), never the rendered text; single arm + no strategy only; the editor stays the fallback for cycling/multi-arm phrases and new platform overrides. The round arms at commit (Escape leaves nothing). Prose spans replace the exact range with re-indented continuation lines; block spans replace body lines only, decided by reading the first line at the span (`define phrase` / `override message`), no IR change. ADR-333 D4c written after it worked.
- Registration position (`add(channel, { before })`) rather than a renderer special case or an order number; matches the ADR-298/ADR-300 registration-order precedent.
- The rule is uniform for every trigger kind.

## Open Items
- I-c8a56c-1 (carried): David's lines for the Chapter 6-9 placeholder beats and the DS38-39 conversion (Secret Letter port branch).
- I-c8a56c-2 (carried): GH #356, the stallkeeper patience counter — David's ruling pending.

## Files Modified
- `packages/if-domain/src/channels/{types,index}.ts`, `packages/stdlib/src/channels/registry.ts`, `packages/extensions/chapters/src/chapter-channel.ts`
- Tests: `packages/stdlib/tests/channels/registry.test.ts`, `packages/extensions/chapters/tests/chapters.test.ts`, `packages/story-loader/tests/adr-330-chapters.test.ts`, `packages/platform-browser/tests/chapter-before-room.test.ts` (new)
- Docs: `docs/architecture/adrs/adr-330-chord-chapters.md`, `docs/architecture/adrs/adr-163-channel-service-platform.md`, `website/src/app/chord/guide/flow/chapters/content.mdx`, `tools/ide/SharpeeIDE/Resources/docs-tab/{docs-index.json,pages/chord__guide__flow__chapters.html}` (rebuilt)
- Plans: `docs/work/archive/chapter-before-room/plan.md` (new; DONE and archived), `docs/work/publish-readiness/plan.md` (Superseded-by stamp), `docs/context/.current-plan`
- (this file)

## Notes
- Session started: 2026-09-05 22:30 CDT (session bc2998)
