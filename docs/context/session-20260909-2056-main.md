# Session Summary: 2026-09-09 - main

## Goals
- Resume Phase 1 of `docs/work/story-engine-role/plan.md` (ADR-343, `StoryEngine` role interface), which session 42e176 left BLOCKED after David accidentally typed `/q`.
- Resolve the block, finish Phase 1's outstanding step (regenerate the API reference), and close the phase.

## Phase Context
- **Plan**: `docs/work/story-engine-role/plan.md` — "Give `onEngineReady` a thirteen-member role interface (`StoryEngine`) instead of the whole `GameEngine` facade, closing the last import cycle into `packages/engine`'s facade and amending ADR-342's contract by one name." Implements ADR-343.
- **Phase executed**: Phase 1 — "Declare and wire `StoryEngine`; repoint every implementer" (Small)
- **Tool calls used**: 101 / 100 (session state)
- **Phase outcome**: Ran over budget (101/100; a rule-17 100% banner fired at 02:49:28 UTC)

## Completed

### Resolved the block
The finding: ADR-343 D4 removed six `if (engine.x)` probes from `loader.ts`'s `onEngineReady`. The ADR's Context read them as defending against engines lacking members, but no engine lacks them — they were what let `packages/story-loader`'s own tests drive the hook with hand-built one- and two-member object literals (9 sites with `{ getNpcService, getPluginRegistry }`, 6 with `{ getNpcService }`). Removing the probes failed 14 test files / 68 tests (`Cannot read properties of undefined (reading 'bind')` at `loader.ts:1056`).

### Test-double fix (route taken: complete double, role stays non-optional)
- `packages/story-loader/tests/helpers/stub-story-engine.ts` (new) — `stubStoryEngine(overrides?)` supplying all thirteen D1 members, each read returning the same instance for the double's lifetime as a real engine does; `recordingPluginRegistry(sink)` wraps the production `PluginRegistry` to also record install order for `extension-install.test.ts` (whose assertion depends on `getAll()`'s priority-sorted, i.e. run, order).
- Repointed all 19 `onEngineReady` call sites across 14 story-loader test files to the double, overriding only what each test asserts on. Every `as never` / `as unknown as Parameters<...>` cast at those sites is gone; `createNpcService` dropped from the imports of files that only had it for the ad-hoc stub.
- Recorded the as-built finding (why the probes existed, what replaced them) in ADR-343's Consequences section.

### API reference regeneration (outstanding step from the previous session)
`tsc --build --force packages/engine/tsconfig.json` then `node scripts/generate-genai-api.js`: `engine.md` gains the `StoryEngine` declaration under `install/story`, `GameEngine implements StoryEngine`, and the hook signature (verified via `git diff --stat`: +58/-... lines); `index.md` 2310 → 2354 lines, generated-for line 5.3.0 → 5.3.1.

### Plan closure
`docs/work/story-engine-role/plan.md`: Phase 1 Status CURRENT → DONE, completion record added, prior BLOCKED note demoted to a "Superseded record" for the trail. Plan Status rolled up to DONE (only phase in the plan) and the plan archived to `docs/work/archive/story-engine-role/` via `plan-archive.sh`; the `.current-plan` pointer (which named it) was released.

## Key Decisions

### 1. Non-optional role interface, complete test double (not reverting to optional members)
The previous session left this open with the platform half (ADR-343 D1-D6) done and only the story-loader tests failing. This session took the route the previous session's in-flight helper pointed at, plus David's "continue" — treated as directional but **not** a confirmed ruling on the record (see Open Items). The alternative (re-adding optionality to `StoryEngine` or the hook signature) was not taken; it would have reopened ADR-343 D1/D2 and reintroduced the probes D4 removed for the platform's own reasons, not the tests'. Fixing the tests' fixtures instead of the contract keeps ADR-343 as accepted and unamended.

### 2. `recordingPluginRegistry` wraps the real class rather than reimplementing it
`extension-install.test.ts` needed install order, which the production `PluginRegistry` doesn't expose. Wrapping the real class (append to a sink on each registration) rather than hand-rolling a fake keeps the double's behavior identical to production for everything except the one thing the test needs that production doesn't surface.

## Next Phase
Plan complete — all phases done (Phase 1 was the plan's only phase). No successor plan exists yet; the platform-side follow-ups from this and prior sessions live in the open-items ledger (below), not in a new plan phase.

## Open Items

### Short Term
- I-03237e-1: Confirm the story-loader test stubbing route: this session took the complete-double route (stubStoryEngine + recordingPluginRegistry) from the previous session's in-flight helper plus David's "continue", but David has not confirmed on the record which of the options put to him he wants; the platform half (ADR-343 D1-D6) stands either way, only the 14 test files would revert.
- I-42e176-1: Three story-loader tests import `PluginRegistry` from `@sharpee/engine`; repoint them to `@sharpee/plugins` and retire the engine re-export (ADR-342 Consequences).
- I-42e176-2: Two dead examples do not compile and import names that no longer exist: `packages/stdlib/examples/pushable-trait-with-handler.ts` (`IFEntity` from engine) and `packages/engine/examples/language-management.ts` (`createStandardEngine`). David's call whether to delete or fix.

### Long Term
- I-7f0471-1, I-7f0471-2: refactoring-survey ADRs 334-340 — Phases 10-12, 15-16 remain; package-by-package survey queue paused after devkit et al. (unchanged this session).

## Files Modified

**This session's own edit** (1 file):
- `packages/story-loader/tests/helpers/stub-story-engine.ts` — new test double (`stubStoryEngine`, `recordingPluginRegistry`)

**Plan/ADR bookkeeping this session**:
- `docs/architecture/adrs/adr-343-story-engine-role.md` — as-built finding added to Consequences
- `docs/work/story-engine-role/plan.md` (now `docs/work/archive/story-engine-role/plan.md`) — Phase 1 → DONE, completion record, Plan Status → DONE, archived

**Carried in the working tree from the previous session (42e176), part of the same uncommitted Phase 1 slice — not this session's work, but staged in the same eventual commit**:
- `packages/engine/src/install/story.ts`, `game-engine.ts`, `index.ts`, `tests/unit/module-layering.test.ts`, `tests/unit/public-surface.test.ts`
- `packages/story-loader/src/loader.ts` and 12 of its other test files (`adr-276-backstops`, `adr-289-d8`, `adr-328-npc-dialogue-scene`, `adr-330-chapters`, `channel-capability`, `channel-record`, `death-constructs`, `event-id-map-machine`, `extension-install`, `family-channels`, `hunger-loader`, `rank-ladder`, `state-machine-depth`, `zoo-surfaces-phase3`)
- Implementers: `stories/dungeo/src/index.ts` + 5 orchestration files, `stories/family-zoo-tutorial/src/index.ts`, `stories/channel-service-test/src/playable-story.ts`, `packages/devkit/fixtures/basic-story/src/index.ts`
- Three `docs/book/v2.0.0` code snippets + their three chapter markdown files
- `docs/architecture/adrs/adr-342-engine-package-contract.md` (D1 amendment)
- `packages/sharpee/docs/genai-api/engine.md`, `index.md` (regenerated this session)

## Notes

**Session duration**: ~10 minutes (21:00-21:10 CDT per the plan's own record; tool-call budget ran to 101/100)

**Approach**: Test-first repair — traced the failure to its actual cause (test fixtures relying on contract optionality) before choosing between amending the contract or fixing the fixtures, per the previous session's finding and the in-flight helper it left.

**Integration Reality check**: The phase name and package path (`packages/engine`) match the rule 13a keyword scanner literally, but this is a TypeScript interface/type-level refactor (declare a role interface, repoint call sites, regenerate docs) — no subprocess, runtime, sandbox, migration, or deploy is being integrated. Treated as exempt under the refactor-session carve-out; no Integration Reality Statement produced.

**Nothing is committed.** This summary and its file list cover Phase 1's full uncommitted diff (this session's slice plus the previous session's), per the plan's own note that the eventual commit is the user's call and will carry the whole phase, not a per-session slice.

---

## Session Metadata

- **Session**: 03237e
- **Status**: COMPLETE (unverified: the "IDENTICAL" byte-comparison and tsc-clean results reported for gate `story-engine-2` in the plan's completion record, which carry timestamps but no matching session event-log row and were not re-run in this pass)
- **Blocker**: N/A
- **Blocker Category**: N/A
- **Estimated Remaining**: N/A (phase complete; ledger carries the follow-ups)
- **Rollback Safety**: safe to revert (nothing committed; reverting the working tree returns to the BLOCKED state the previous session left, which is itself recoverable from `docs/work/archive/story-engine-role/plan.md`'s "Superseded record")

## Dependency/Prerequisite Check

- **Prerequisites met**: ADR-343 ACCEPTED (session 42e176); residue plan archived; the in-flight test-double helper and the platform-side D1-D6 work the previous session left uncommitted.
- **Prerequisites discovered**: None new this session — the story-loader test-fixture gap was already discovered and documented by the previous session; this session resolved it.

## Architectural Decisions

- ADR-343 (`StoryEngine` role interface): implemented in full this session (D1 through D6, AC-1 through AC-5 evidenced in the plan). Consequences section amended with the as-built finding that the six removed probes were a test-fixture accommodation, not a defense against real engines.
- Pattern applied: dependency-inversion / role-interface (DEVARCH.md rule 8, "dependencies flow inward") — `install/story.ts` now declares the contract implementers satisfy, rather than naming the concrete `GameEngine` class.

## Mutation Audit

- Files with state-changing logic modified: none — this session's substantive change is a test double (`stub-story-engine.ts`) and 14 test files repointed to it; no production mutation logic changed this session (the production `install/story.ts`/`game-engine.ts` changes were the previous session's).
- Tests verify actual state mutations (not just events): N/A — the work under audit is test infrastructure, not a mutating function. `mutation-verification` ran this session (confirmed via event log, 02:05:09 UTC) and reported clean: the six former probe sites are behavior-identical to before (`GameEngine` implemented all six unconditionally already), the double uses real non-mock defaults and the production `PluginRegistry`, and every assertion target survived the port unchanged. No RED/YELLOW patterns.
- If NO: N/A

## Recurrence Check

- Similar to past issue? NO — this is the first time an ADR's stated rationale for an optionality pattern was found to be defending the wrong thing (real engines vs. test fixtures); no prior session in the open-items ledger names this pattern.

## Test Coverage Delta

- Tests added: 0 new test cases (one new test helper file; existing tests repointed to it, not duplicated)
- Tests passing before: 1036/1104 (68 failing across 14 files) → after: 1104/1104, all passing (evidence: event log `docs/context/.devarch-events-03237e.jsonl` — "Build passed" rows at 2026-09-10T02:02:39Z for `pnpm --filter '@sharpee/story-loader' test` and 02:03:02Z for `@sharpee/engine` test, both after the last edit to `stub-story-engine.ts` at 01:58:43Z; reconfirmed by `mutation-verification`'s own re-run at 02:06:09Z/02:06:20Z)
- Devkit suite corroborated in this pass: 27 files / 177 passed / 1 skipped, `tsc --noEmit -p packages/engine/tsconfig.json` clean (both re-run directly during this summary-writing pass, exit 0)
- Known untested areas: the full `./repokit build dungeo` + byte-identical walkthrough/Chord-tree gate ("story-engine-2", IDENTICAL on dungeo-chain, tree-secret-letter, tree-fernhill, tree-ides-of-march) reported in the plan's completion record is not backed by a session event-log row and was not re-run in this pass — see Status qualifier above.

---

**Progressive update**: Session completed 2026-09-09 21:10 CDT (summary written 2026-09-09, ~21:53 CDT)
