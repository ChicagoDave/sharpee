# Session Summary: 2026-09-14/15 - main

## Status: In Progress (second finalize — the work below postdates commit a2e7329f4)

## Goals
- Execute web-ide evaluation **Phase 7** — "Parity column, decision record, and (on confirmation) the four-way ADR draft" (Medium, budget 200), completing the four-way comparison this track has been building toward across three evaluation plans.

## Phase Context
- **Plan**: `docs/work/web-ide-evaluation/plan.md` (ACTIVE, the `.current-plan` pointer).
- **Phase outcome**: **DONE — all four deliverables met.** (1)(2)(3) written first with (4) held at the gate; David then confirmed rule-11 ("yes but keep the details in the research and the ADR as a summary comparison of requirements") and the ADR was written to that shape. **Plan Status: DONE**, not archived — moving the directory is David's call.

## Completed

### Fresh check before writing anything
`./sharpee test branch-stories/fernhill/fernhill.story --tree` → 11 lines, **86 cards passing, 104 assertions passing, 222 commands (85 authored + 137 replayed)** — the exact baseline the in-page walker matched in three browsers. `branch-stories/fernhill/fernhill.tests.json` still hashes `069f424d…fe4de6`; `git status --short branch-stories/fernhill` empty.

### (1) `docs/work/web-ide-evaluation/parity-table.md` — the Web column
- The ADR-341 row set unchanged, filled row by row from Phases 0–6. Of the eighteen **YES**-flagged rows: **6 PASS**, **1 partial**, **1 void**, **3 not exercised**, **7 not built**. **None failed.**
- **The prior two columns are referenced, not re-copied.** The Avalonia table already carries OpenSilver's verbatim; a third copy would drift, and the plan names those tables as adjacent references.
- **New cell vocabulary: `void`** — the row's *question* does not arise for this shape (the title bar, D3's host contract, Check for Updates, the shell environment for subprocesses). Every void cell is argued; none is used to hide an untried row. `not exercised` added for the CodeMirror rows a prior column proved in the same build and this evaluation did not measure.
- Two rows added because they are plumbing in the other three shapes and this shape's own question: **Publish** and **Hatched stories**.
- Nineteen host findings that are not rows, each dated and traced to a phase.

### (2) `docs/work/web-ide-evaluation/decision.md` — the four-way record
- §1 extended to four columns (WPF / OpenSilver / Avalonia / Web), with five rows added: the ADR-307 round trip, running the tests, build and publish, hatched stories, and the author's files.
- §7 fills the options-doc dimension table's Web column with evidence.
- §11 dependency health checked at write time (`gh api`, `registry.npmjs.org`): esbuild 40,059★ MIT (`esbuild-wasm` latest **0.28.2**; the spike ran **0.27.2** to match this repo's own `^0.27.2` pin), CodeMirror packages MIT and active, fflate 3,009★ MIT and already a platform dependency. The real dependency named as the browser vendors' standards decisions, which no pin controls.

### (3) GH #438 commented
`issues/438#issuecomment-5674154214` — outcome, the recommendation, the counter-case, and the three new platform issues.

### (4) `docs/architecture/adrs/adr-351-chord-writer-host-shape.md` — DRAFT
- Written on David's rule-11 confirmation, to his shaping instruction: **one line per requirement per shape** across R1–R21 (the options doc's own requirement list), citing the four research directories for every measurement rather than copying one.
- **D5 makes that a decision**, so a later amendment cannot quietly turn the ADR into a second source of truth for a number that lives in a research record.
- **Status DRAFT** with six Open Questions (Q-1 browser matrix, Q-2 ADR-280 A1 for the web tier, Q-3 the felt comparison, Q-4 what happens to ADR-341, Q-5 the durability of a folder grant, Q-6 whether the Mac app stays indefinitely), so rule 11a forbids ACCEPTED. D2 carries the recommendation gated on Q-1/Q-2; D1, D4, D5, D6 and D7 hold whichever way D2 resolves.
- **ADR-341 is untouched.** D3 states only what acceptance *would* imply for its D2/D4, and Q-4 asks David how that amendment should be made.

## Key Decisions
0a. **David ruled the shape: Avalonia + Velopack.** After weighting the requirements he asked for a second dimension — a 0–4 satisfaction score for every requirement × every shape — and added **R22 platform reach** as a **gate** when the matrix showed the list could not answer ADR-341's own question (the native column scores a perfect 196 and reaches one platform). His reason: *"the Avalonia + Velopack score was as close to native as possible and still reach all three platforms."* **ADR-351 D2 is flipped to Avalonia**, conditional on Phase 7 (Windows), a solved macOS bundle layout, and his felt comparison. The web evaluation's own §8 recommendation is marked **not adopted** and deliberately **left as written**, because ADR-351 D2 names the web shape as the standing answer if a condition fails.
0b. **The arithmetic's own finding, now in the ADR:** the three cross-platform shapes **tie at 19 of 20 on the five weight-4 requirements**, each giving up its single point somewhere different — OpenSilver on a replay that under-ran, Avalonia on a pane door it does not have, Web on files it cannot reach outside Chromium. The whole 16-point Avalonia-to-Web spread is made in the ◕ 3 and ◑ 2 bands. And **the matrix does not separate Avalonia from OpenSilver** (183 vs 180, inside cell noise) — what separates them is ADR-341 D2/D4's own criteria, which the ADR now says outright.
0c. **David has a Windows machine available** ("I have a PC we can pull this to"), so `docs/work/avalonia-ide-evaluation/plan.md` Phase 7 moves from blocked-on-hardware to schedulable. Three things genuinely need it and cannot be inferred: the **WebView2** response-supply door (a Windows-only door splits ADR-341 D3 by platform), `Setup.exe` with Azure Trusted Signing, and the vendored toolchain's Windows launcher (`bin/sharpee` is a POSIX script; GH #448 is the other unknown). Everything else in that column is toolkit behaviour.
0. **David weighted the requirements, and the weighting changed the recommendation's shape.** He rejected the first ranking — *"not sure you're ranking them as I would. The file i/o is an important requirement"* — and set an importance value (0–4) on each of R1–R21 one at a time, correcting three drafted values: **R6 up to 4**, **R12 up to 3**, **R10 down to 1**. The defect he was pointing at was real and internal: the ADR's D4 said R1 decides while D2's grounds never weighed it, and a flat 21-row table hands the ranking to the reader. **ADR-351 now owns the weighting (D5), orders its table heaviest-first, and D2 is restated as a requirement ruling: a Chromium-class browser is a requirement of the web product, and the private-storage tier is a preview rather than a second tier of it.** R12 at 3 is what forces that: on the private tier a cloud adapter is the author's only route to their own files, and zero lines of one exist. The superseded ranking is recorded in ADR-351 D2 ground 4, not deleted.
1. **The recommendation: carry the pure web application forward as the shape that reaches Windows and Linux, keep the macOS Swift app as the native product it already is, and recommend against both .NET shapes as the Windows answer** — conditional on David's rulings on **Q2** (is "real folders in Chromium, private storage elsewhere" an acceptable product) and **Q3** (may a project live somewhere Finder cannot see it). Grounds in weight: it *removes* the Windows problem rather than solving it; it does not touch the Mac app, so the least-reversible decision in the track becomes unnecessary rather than deferred; the two product differentiators are its strongest cells; no 175 MB toolchain to seal — the wall Avalonia hit.
2. **This supersedes the Avalonia record's §7 on exactly the condition that record named** ("if the pure web application spikes well"), rather than quietly outranking it.
3. **Three predictions corrected rather than carried forward.** The Avalonia **274**-record over-run was correct behaviour and **OpenSilver's 31 was the under-run** (that host never fresh-booted the ten branch lines) — an unexplained result inside a product differentiator, now arithmetic. The options doc's `esbuild-wasm` estimate was **29% low** (12.88 MB, not ~10 MB). The plan's own `runner.ts` hypothesis was wrong, which makes #463 a packaging gap rather than a port.
4. **The counter-case is six paragraphs and is not decoration** — chiefly that the recommendation accepts the trade ADR-341 D2 and D4 were about, *after* a shape existed (Avalonia) that did not charge it; that the browser fork is a step, not a gradient; and that "keep the Swift app" is cheaper than Avalonia's answer and is still two codebases.
5. **Deliverable (4) was not pre-empted.** The four-way ADR draft is gated on an explicit rule-11 confirmation from David; ADR-341 is untouched either way. The ask is put to him at the end of this session's report.

## Open Items
- **The rule-11a open-questions interview for ADR-351 is offered and not started** — six questions, Q-1 and Q-2 being the ones D2 is gated on. Never start it unasked; never resolve a question without David.
- **Archiving `docs/work/web-ide-evaluation/`** — the plan is terminal; the move was not made.
- **#461** — whether `navigator.storage.persist()` makes a folder grant durable. The sharpest open question under the recommendation's first ground.
- **#463 / #464 / #465** — platform findings from Phases 3 and 5, filed not fixed (platform changes require discussion first).
- Owed regardless of the ruling (`decision.md` §12): the folder adapter at Phase 4's call sites; offline for the testing surface and the walker; the felt comparison; Avalonia's own Phase 7 (Windows), which folds back into its record, not this one.

## Files Modified
- `docs/work/web-ide-evaluation/parity-table.md` (new)
- `docs/work/web-ide-evaluation/decision.md` (new)
- `docs/architecture/adrs/adr-351-chord-writer-host-shape.md` (new, DRAFT)
- `docs/work/web-ide-evaluation/plan.md` — Phase 7 DONE (all four deliverables), **Plan Status: DONE**, not archived
- `docs/context/session-20260914-2204-main.md` (this file)

## Notes
- No `packages/` change, no Chord change, no spike code touched, nothing merged. **ADR-341 was not edited**; ADR-351 is new and DRAFT. This phase wrote documents and one GitHub comment.
- Rule 15 (mutation-verification) did not fire: every file written this session is `*.md`, which the rule excludes by path.
- Session start ran in full: recap, `pre-session-audit` (five-line block relayed verbatim), profile checked (3 days old, fresh), `docs/core-concepts/README.md` read, gate cleared.

## Part 2 — after the first finalize (2026-09-15)

### David ruled the shape, and then narrowed it
- On the weighted matrix he ruled **Avalonia + Velopack** ("as close to native as possible and still reach all three platforms"), and ADR-351 D2 was flipped to it with **R22 platform reach** added as a **gate** rather than a scored row.
- He then narrowed it further: **the native macOS app paired with Avalonia on Windows (and Linux?)**. That pairing deletes both of Avalonia's weak cells, because both are macOS artifacts — R15 = 2 was the bundle Velopack cannot seal, R5 = 3 was the loopback listener forced by macOS having no pane door. It also dissolves ADR-351 **Q-3, Q-4 and Q-5**. The cost, stated rather than buried: two codebases forever, and the matrix cannot see that cost — the same blind spot that let the native column score 196.
- **The `(and Linux?)` is what Phase 8 was opened to answer.**

### Phase 7 landed from the PC (session 6c19b3, commit bd2ab17a8)
- **The WebView2 door exists** — `IWindowsWebView2PlatformHandle` → `CoreWebView2` → `ICoreWebView2_3::SetVirtualHostNameToFolderMapping`, `hr=0x0`, a pane served from `https://sharpee-panes.invalid/` with no `HttpListener`. Avalonia's own API is Request-only on *both* backends, so this is a gap in Avalonia's abstraction rather than a platform difference.
- Velopack is **1-for-1 on Windows** after 0-for-1 on macOS. The genuine Windows gap is the **vendored toolchain**, which has no Windows Node asset at all.
- One caveat this session added: that 72,533-byte delta is measured against a package containing **no toolchain**, so it is not like-for-like with macOS's 8.11 MB against 99.8 MB.

### Phase 8 (Linux) — opened, scouted, and PASS on both answerable questions
- Plan reopened to ACTIVE for Phase 8 **without repointing `.current-plan`**, which a parallel session owns for ADR-349.
- **The door exists on Linux and is a custom URI scheme** — the mechanism ADR-341 D3 specifies and the Mac app already uses, where Windows offers a virtual-host analogue. Reached the same way Phase 7 reached its door: past Avalonia's API, through the public platform handle.
- **`vpk` produces an AppImage in 1.5 s**; 118,644-byte delta. Two build-host prerequisites found by failing: `mksquashfs` (structural) and `zstd` (whose absence *succeeds* into a bsdiff fallback at 8.5× the time, with Velopack's own "more prone to breaking" warning).
- **The toolchain gap is one gap across both non-Mac platforms** — `tools/ide/vendor/node/` carries only the two darwin tarballs.
- **A harness defect is in the record on purpose**: the probe's first run returned FAIL by interrogating the event-args object instead of the platform handle, so `libwebkit2gtk` was never called. That is the GH #435 pattern — a verdict about behaviour never exercised — caught before it entered the evaluation.

### Cross-session collisions, now demonstrated in both directions
1. **The issue store.** This Mac's detector updated **#424** at 03:59:23Z ("still present, 8 occurrences"); the PC's finalize closed it at 04:18:45Z ("not found by the recurrence scan"). Both were mechanically right from where they stood — `recurrence-count.sh` reads session summaries out of the working tree, and the two clones hold different sets. Last writer wins. The skill guards the UNREACHABLE case ("an unknown finding set is not an empty one"); a legitimately *partial* view does the same damage without tripping it. **#424 is still closed and I have not reopened it.**
2. **The working tree.** Commit **58f7a873b**, the parallel session's ADR-349 commit, swept up this session's Phase 8 record, evidence and plan update — filed under a message about location headings. Nothing was lost; the provenance is wrong.
3. **`.active-session` now reads `c35f3d`**, so `/devarch:finalize`'s step 1 resolves the wrong session's state file for this conversation.

### Owed
- ADR-351's Consequences still need the Linux paragraph, and D2's third condition (Linux) is answered on arm64 only.
- Phase 8 deliverable (4): the `linux/amd64` record run on the PC, the real panes over the scheme, signing, and running the AppImage.
- The macOS WKWebView re-test — `IAppleWKWebViewPlatformHandle.WKWebView` is public, so Phase 1's "no door" was reached by reasoning now wrong on two backends. Record accuracy, not a gate, under the pairing.
