# Session Summary: 2026-08-14 15:23 CDT - main

## Goals
- Revisit ADR-310 (character model in Chord) ahead of Fable 5's availability.
- Build a prior-art corpus on IF dialogue/conversation systems to strengthen it, under an explicit "reuse thoughts, not content" constraint given copyrighted sources.
- Fold verified findings back into the ADR and adversarially review the result.
- (Second half) Fix a New-Story-breaking bug in Chord Writer, ship a 1.0.1 release, and fix a prologue/banner styling and ordering defect reported from the Play pane.

## Phase Context
- **Plan**: `.current-plan` points to `docs/work/docs-consolidation/plan.md`, which is `**Plan Status**: DONE` — untouched this session.
- **Phase executed**: none — this was unplanned ADR work at the user's direction, no session plan was created.
- **Tool calls used**: 343 (from `.session-state-4e8fc1.json`; no budget was set since no phase was active). Note: `.session-state-4e8fc1.json` carries a stray `phase`/`phaseName` pair ("Roadmap schema and the seven known items") left over from an unrelated context — no plan resolves through `.current-plan` (empty) for either half of this session, so it is disregarded here.
- **Phase outcome**: N/A — not a planned phase.

## Completed

### Prior-art review
- Read four primary sources in full plus one secondary, under ground rules the user set mid-session (mechanisms in our own words, quotes only to pin a term of art, cite don't carry, source code read not copied, paywalled work used only via author-published secondary material): Emily Short's "NPC Conversation Systems" (IF Theory Reader 2011, pp. 331-358 — WebFetch 403'd on the IF Archive PDF, retrieved via curl + pdftotext); Ryan & Mateas "Simulating Character Knowledge Phenomena in Talk of the Town" (Game AI Pro 3, ch. 37); McCoy/Mateas/Wardrip-Fruin "Comme il Faut" (DAC 2009); Eric Eve's TADS 3 conversation how-to; and Versu via Short's own 2013 blog post (the IEEE paper is paywalled and was deliberately not pulled from third-party reposts).
- Wrote `docs/work/adr-310/prior-art.md` (518 lines, new): ground rules, per-source mechanism digests, eight findings F1-F8 mapped to ADR-310 decisions/open questions, a system-by-system verdict table on D1's novelty claim, ten recommended amendments, pass-2 candidates, and a Sources list.

### ADR-310 amendment
- Folded three findings into ADR-310 as new decisions D14, D15, D16, striking Open Questions 2, 4 and 7 in place with resolution notes (matching how Q6/Q8 were struck in the original session).
  - **D14** — a belief is `(holder, subject, facet) → value`; the shipped `_knowledge`/`_beliefs` maps at `packages/character/src/character-builder.ts:184-185` both lack a value slot, so two characters cannot disagree about what's true, only how sure they are. New Chord construct spelled `thinks` (not `believes`, which is taken by the shipped `believes()` method at line 359; not `holds`, taken by Chord's possession predicate at `packages/chord/src/parser.ts:134`). Scope line: one level of belief, no theory of mind, matching Versu's cut.
  - **D15** — topic tables (ADR-239's `define topics for …`) are the interface; the character model is the selector. They compose rather than compete. Recorded as code fact rather than ADR-status fact: `packages/stdlib/src` has no reference to `DialogueExtension`, so `character/src/conversation/dialogue-extension.ts`'s header claim of wiring into ASK/TELL is currently false — the socket doesn't exist yet.
  - **D16** — when phrasebook gates stack (D13), the innermost active context wins by total override, not score-blending, per TADS 3's twenty-year-shipped `ConvNode` behavior. A same-specificity tie is a compile-time diagnostic. Also fixes D8 implicitly: a conversation in progress suppresses NPC goal pursuit.
- Updated the Status line, added an amendment note to the Session section, and added a "Prior art review" pointer field.

### Adversarial review and correction
- Ran `/devarch:adr-review` on the amended ADR: score 3/18, verdict NEEDS WORK, 24 citations verified. Found three citation failures:
  - **C1**: the Context's central claim ("Nothing imports it... not one story") was false and was false when written — `stories/thealderman/src/npcs/index.ts:30` imports `ConversationBuilder` and builds six suspects across 862 lines, landed 2026-04-09 (`812a753e`), four months before the ADR. Not a pnpm-workspace member, not in `ts-forge.config.json` (unbuilt, untype-checked), but refactor sweeps still touch it.
  - **C2**: D14 as first drafted this session described only `_knowledge` and missed the parallel `_beliefs` map — conclusion held (neither has a value slot) but the analysis was incomplete, and the proposed `believes ... is ...` keyword collided with the shipped `believes()` method's existing meaning.
  - **C3**: the NPC manifest vocabulary was misattributed to `@sharpee/plugin-npc`; it actually lives in `packages/chord/src/stdlib-manifest.ts:119-123` and `src/manifests/npc.ts`, executed by `story-loader`.
- Applied all three corrections directly to the ADR (see diff in Files Modified). Verified clean in the same pass: 301 test cases across 19 files, version 5.0.0, cognitive-presets header quotes, all 8 preset names, ADR-145 step verbs, ADR-146 binary evaluation, ADR-144 vocabulary, Fernhill:667 and the while-midnight phrasebook.

### Chord Writer "New Story" bug — missing template resource
- Root cause: `StoryScaffold.swift` looked for `story.story.template` at `Bundle.main.resourceURL` root, but `tools/ide/project.yml` had no template resource entry at all — the file only ever reached `Contents/Resources` inside the opt-in vendored toolchain (ADR-279 D4) at `toolchain/devkit/templates/story-chord/`, three directories below the lookup and absent unless `SHARPEE_VENDOR_TOOLCHAIN=1`. New Story failed in every build configuration; no existing `StoryScaffoldTests` case caught it because every case (including `testRealTemplateScaffoldComposesClean`, despite its rule-13a comment) passes `templateDirectory:` explicitly, leaving the default-argument path with zero coverage.
- Fix: new `tools/ide/vendor-story-templates.sh` mirrors `packages/devkit/templates/story-chord` into `SharpeeIDE/Resources/story-templates` (precedent: `vendor-play-themes.sh`, including its "mirror not source" invariant); `project.yml` gains the folder resource, a sources exclude, and a non-optional prebuild phase; `StoryScaffold.swift` tries `story-templates/story.story.template` then falls back to the bare name so injected directories still work. Two new tests: `testScaffoldsFromTheAppBundleWithNoInjectedDirectory` (drives the default argument, asserts all three file writes — a `mutation-verification` finding) and `testTheAppBundleCarriesTheStoryTemplate`.

### Docs-tab prebuild blocker and a pre-existing red test
- Blocked mid-build: the docs-tab prebuild refused to bundle because website's `/chord-writer/download` page uses `<DownloadRow>`, which `tools/ide/web/docs-tab/src/mdx.mjs` cannot render. Presented three options; David chose excluding the page. Implemented `EXCLUDED_PAGES` in `build.mjs` plus page-level exclusion in `nav-bridge.mjs`'s `shippedNav`, carrying the same "a filter that matches nothing throws" discipline as the existing section/group filters.
- Discovery: `tools/ide/web/docs-tab/tests/nav-bridge.test.mjs` was already RED before this session — it hardcoded `excluded.size === 3`, and commit `1b1d5269` (this session's own earlier work, two sessions back, adding "What is Chord?" to Chord > Getting Started) grew that group to 4. Unnoticed because the docs-tab suite is not in `pnpm test:ci`. Updated the assertion to 5 and left a comment naming the constant-duplication hazard between `build.mjs` and the test.
- Also retargeted `DocsTabRealPathTests.testThePagerFollowsNavOrder`, which navigated to the now-excluded download page, to `/chord-writer/your-first-story` — the rule under test is the relabel, not which page is second.

### Version bump to 1.0.1
- Bumped Chord Writer to 1.0.1 / `CFBundleVersion` 2 at David's instruction: `tools/ide/project.yml` (`package.sh` reads this for DMG filenames), `website/src/lib/nav.ts`, `website/src/components/download-card.tsx` (both DMG filenames), and `website/src/app/chord-writer/download/content.mdx`, whose Sharpee half was also stale at 5.0.0 — the pull moved the platform to 5.0.1.

### Prologue/banner styling and ordering bug (screenshot report)
- Three distinct defects found in `packages/` (David approved fixing all three):
  1. `base.css` styled the PROSE path's banner class names (`p.game-title`, `story-version`, `sub-title`, `author-list`, `banner-spacer`) while ADR-300 D6/D12 had moved the banner to its own channel, whose renderer emits `sharpee-banner-*` names. Zero stylesheets matched — grep across `base.css`, `engine.css`, `decorations.css`, and all four themes returned nothing — so nothing was bold and everything fell back to `engine.css:147`'s `.sharpee-prose-pane p { margin-bottom: 0.8em }` instead of the intended flush stack. Added rules for the new class names, including the gap the discarded `banner-spacer` used to supply.
  2. `#sharpee-prologue` / `.sharpee-prologue` had no CSS at all and sit outside the prose pane, so `base.css:19`'s universal reset left them with no margins — no break before the banner, and multi-paragraph prologues collapsing together. Added region and inter-paragraph margins.
  3. Ordering: the banner rendered after the first room description. Root cause was `STANDARD_CHANNELS` registration order in `packages/stdlib/src/channels/standard.ts` — `prologueChannel` and `bannerChannel` sat after `preferredLayoutChannel`, which flushes buffered prose into main, so the opening always appended behind the turn. Moved both ahead of `PROSE_CHANNELS`, preserving ADR-300 D9's "preferred-layout after prose" invariant. The CLI was never affected (it renders the banner through the prose path's classed blocks) — verified by identical `--exec` output before and after.
- New test in `platform-browser` pins the actual defect class: it renders a banner and asserts every class the renderer emits has a rule in `base.css`.

### Verification pass
- stdlib channel tests: 53 passing. platform-browser: 141 → 142 passing. Chord Writer suite: 482 passing (was 480 + 2 new). docs-tab: 43 passing. Dungeo walkthrough chain: 952 passing across 17 transcripts, 0 failures (the regression baseline, confirming the channel reorder has no transcript impact). Website `npx tsc --noEmit`: exit 0.

### Installers (local only, none published)
- Built arm64 and x86_64 archives; verified inside each archived app (binary arch, bundled node arch, version, story-templates presence, vendored CSS fixes, vendored channel order). `ChordWriter-1.0.1-arm64.dmg` is complete — signed, notarized, stapled, sha256 written, contents verified by mounting.
- Mistake made and corrected: `package.sh --dmg-from` does not infer architecture from the app it is given — it uses the host slug unless `--arch` says otherwise. Running it on the Intel app without `--arch` wrote the Intel app into `ChordWriter-1.0.1-arm64.dmg`, destroying the already-stapled arm64 DMG (caught by mounting and running `lipo`, which reported x86_64). This is the same wrong-slice failure `package.sh:373` records from 2026-08-13. Both slices were rebuilt correctly with explicit `--arch`.
- `package.sh` bug found (unfixed, not blocking): its build path signs only Mach-Os under the vendored toolchain and never `Contents/Frameworks`, so `libswift_Concurrency.dylib` keeps its toolchain signature. Apple rejected submission `b37dab5e` as Invalid — "not signed with a valid Developer ID certificate" and "signature does not include a secure timestamp", both architectures. Every prior successful release went through Xcode's Distribute App, which re-signs embedded frameworks, so this code path had never been exercised end to end; the local `codesign --verify --deep --strict` passes because it checks that nested code has *a* valid signature, not that it is *ours*. Proposed fix (not applied, awaiting David): sign `Contents/Frameworks` before the outer bundle, and extend verification to assert every Mach-O carries the expected Team ID.
- Intel is still in flight — David got the Organizer upload working on the fourth attempt and is awaiting Apple.

### ADR-279 D7 answered — Sparkle auto-update
- David asked whether in-app updates were ever designed. They were: full Sparkle 2 auto-update from v1, his own ruling in the 2026-07-27 session (`fda0f0`), with appcast on sharpee.net, archives on GitHub Releases, an EdDSA key as a D3-class secret, and `package.sh` appcast steps. Implementation status checked this session: grep for `Sparkle`/`SUFeedURL` across `project.yml`, `package.sh`, and all Swift sources returns nothing — decided and unbuilt, which matters because 1.0.1 exists to fix a bug that makes New Story unusable and 1.0.0 users have no update path.

### Visitors skeleton removed from the ADR-310 branch
- Deleted `branch-stories/visitors/visitors.story` from `feat/adr-310-character-in-chord` (commit `6a18d12b`, **local only, not pushed**) at David's request — he is writing that story by hand. Done via a temporary git worktree to avoid switching branches with a dirty tree.
- Discovery while doing it: that branch has already accepted ADR-310 and resolved its open questions (`454f7beb`) and wired ADR-102's `DialogueExtension` seam (`ed9cefa2`). Its D14-D17 are different decisions from the D14-D16 added to `main` earlier this session (branch: demo-story-first / mood-feels-knows / define-fact / ResponseAction; main: belief addressing / topic-tables-as-interface / gate resolution order). Offered to renumber; David ruled "the branch is its own thing for now" — do not reconcile.

## Key Decisions

### 1. ADRs are reference, not imperative — corrected mid-review
The adversarial review's first draft graded "ADRs 141-146 are all DRAFT" as a finding and led the D15 gap with "ADR-102 is Proposed," gating findings on ADR status. The user corrected this (recurring feedback: "I've mentioned this before"). The draft-foundations finding was withdrawn entirely; the D15 gap was restated as a code fact (`packages/stdlib/src` has no `DialogueExtension` reference) rather than a status fact. New memory file written: `~/.claude/projects/-Users-david-repos-sharpee/memory/feedback_adrs_are_reference_not_imperative.md`, indexed in MEMORY.md. These are outside the repo and were not staged.

### 2. D12 deliberately left open despite a direct counter-argument in the literature
Short's chapter argues against invisible inference systems (her "Exposing the Mechanism (Partly)" section and Best of Three retrospective, where an invisible system produced a game that wasn't fun) — directly bearing on D12. It was left open for the ADR-310 interview anyway, because D12 is David's design ruling, not a literature lookup.

### 3. Versu sourced only via Short's own published account
The user's copyright constraint ("we can reuse thoughts, not content") ruled out the paywalled IEEE Versu paper; it was deliberately not obtained from third-party reposts, using Short's own 2013 blog post instead.

### 4. Exclude the download page from docs-tab rather than teach `mdx.mjs` about `<DownloadRow>`
Given three options for the docs-tab prebuild blocker, David chose excluding `/chord-writer/download` over extending the MDX renderer, keeping the docs-tab's rendering surface narrow rather than growing it per website component.

### 5. Fix all three prologue/banner defects together
The screenshot report surfaced one visible symptom (no break before the banner) but investigation found three independent root causes (missing CSS for the renamed banner channel, missing prologue region CSS, and channel registration order). David approved fixing all three in one pass rather than patching only the reported symptom.

### 6. ADR-310 branches left unreconciled
The `feat/adr-310-character-in-chord` branch already has its own D14-D17, distinct from the D14-D16 added to ADR-310 on `main` this session. David ruled the branch decisions stay as-is for now rather than being renumbered or merged with main's — a deliberate deferral, not an oversight.

### 7. Stop asking permission for routine archive builds
During the installer work, David noted "you've been building the archives for two days" — a correction to stop asking permission before routine DMG/archive builds. New memory written (outside the repo, not staged).

## Next Phase
No active plan governs this work — N/A.

## Open Items

### Short Term
- ADR-310's D14 has no rule for what happens when propagation delivers a value contradicting one already held (Talk of the Town compares evidence strength; the shipped model has `ConfidenceWord` and `resistance` and could compare either). This is a design call of D12's kind and belongs in the ADR-310 open-questions interview — flagged as the one gap that would actually block implementation.
- Open Questions 1, 3, 5 remain unresolved on ADR-310, plus D12 (held for interview) and the new D14 precedence gap.
- Intel `ChordWriter-1.0.1-x86_64.dmg` is awaiting Apple's notarization response; once it arrives, run `package.sh --dmg-from` with explicit `--arch x86_64` (the flag does not infer architecture from the given app).
- `package.sh`'s Contents/Frameworks signing bug is unfixed — it never re-signs embedded frameworks, so `libswift_Concurrency.dylib` fails Apple's submission check on both architectures. Proposed fix (sign `Contents/Frameworks` before the outer bundle; assert Team ID on every Mach-O) is awaiting David.
- Sparkle 2 auto-update (ADR-279 D7) is decided (2026-07-27, session `fda0f0`) but unbuilt — grep for `Sparkle`/`SUFeedURL` returns nothing across `project.yml`, `package.sh`, and Swift sources. Matters because 1.0.0 users have no update path to the 1.0.1 New-Story fix.
- The docs-tab and Chord Writer Swift suites sit outside `pnpm test:ci`, which is how the `nav-bridge.test.mjs` regression went unnoticed for two sessions.
- `packages/devkit/templates/browser/base.css` is a stale duplicate of the platform stylesheet (56 lines behind, dead path) — deletion needs David's confirmation, not yet done.
- The banner channel renderer appends without clearing its slot, unlike the prologue renderer — a re-emission on restart may duplicate the banner. Flagged, unverified.
- `release/` has accumulated stale DMGs and exported `.app` bundles needing a sweep.
- Nothing from this session was pushed; the ADR-310-branch deletion commit (`6a18d12b`) is local-only on `feat/adr-310-character-in-chord`.

### Long Term
- Pass-2 candidates recorded in `docs/work/adr-310/prior-art.md` for a future round of prior-art review.
- ADR-310's branch-local D14-D17 (`feat/adr-310-character-in-chord`) remain unreconciled with main's D14-D16 by explicit user ruling — revisit if/when the branch merges.

## Files Modified

All uncommitted (working tree) unless noted.

**Documentation** (2 files):
- `docs/architecture/adrs/adr-310-character-model-in-chord.md` - amended with D14/D15/D16, three open questions struck and resolved, Context/vocabulary corrections from the adversarial review (net +217/-28 lines)
- `docs/work/adr-310/prior-art.md` - new, 518 lines, prior-art corpus and findings

**Prologue/banner styling and ordering fix** (4 files):
- `packages/platform-browser/styles/base.css` - rules for `sharpee-banner-*` classes and `.sharpee-prologue` region/paragraph margins
- `packages/platform-browser/tests/channels/prompt-status-info.test.ts` - new test asserting every banner-renderer class has a `base.css` rule
- `packages/stdlib/src/channels/standard.ts` - moved `prologueChannel`/`bannerChannel` ahead of `PROSE_CHANNELS` in `STANDARD_CHANNELS`
- `packages/stdlib/tests/channels/standard.test.ts` - updated for the new registration order

**Chord Writer "New Story" template fix** (4 files, 2 new):
- `tools/ide/vendor-story-templates.sh` - new, mirrors `packages/devkit/templates/story-chord` into the app bundle
- `tools/ide/SharpeeIDE/Resources/story-templates/` - new, vendored mirror (2 files)
- `tools/ide/project.yml` - template folder resource, sources exclude, non-optional prebuild phase
- `tools/ide/SharpeeIDE/Workspace/StoryScaffold.swift` - tries `story-templates/story.story.template` then falls back to the bare name
- `tools/ide/SharpeeIDETests/StoryScaffoldTests.swift` - two new tests for the default-argument (app-bundle) path

**Docs-tab exclusion and pre-existing test fix** (4 files):
- `tools/ide/web/docs-tab/build.mjs` - `EXCLUDED_PAGES`, excludes `/chord-writer/download`
- `tools/ide/web/docs-tab/src/nav-bridge.mjs` - page-level exclusion in `shippedNav`
- `tools/ide/web/docs-tab/tests/nav-bridge.test.mjs` - fixed pre-existing red assertion (`excluded.size` 3 → 5), comment on the constant-duplication hazard
- `tools/ide/SharpeeIDETests/DocsTabRealPathTests.swift` - `testThePagerFollowsNavOrder` retargeted from the now-excluded download page to `/chord-writer/your-first-story`

**Docs-tab regenerated bundle** (3 entries, build artifacts):
- `tools/ide/SharpeeIDE/Resources/docs-tab/docs-index.json` - regenerated
- `tools/ide/SharpeeIDE/Resources/docs-tab/pages/chord-writer__download.html` - deleted
- `tools/ide/SharpeeIDE/Resources/docs-tab/images/chord-writer/app-icon.png` - new

**Version bump to 1.0.1 / 5.0.1** (3 files):
- `website/src/lib/nav.ts`, `website/src/components/download-card.tsx`, `website/src/app/chord-writer/download/content.mdx` - Chord Writer 1.0.1, Sharpee 5.0.1

**Platform build artifacts** (from `./repokit build dungeo`, 2 files):
- `stories/dungeo/src/version.ts`, `packages/sharpee/docs/genai-api/index.md`

**DevArch-managed, not authored by this session** (1 file):
- `.devarch/descriptor.json` - modified by DevArch itself

**Branch-local, committed (not pushed)**:
- `branch-stories/visitors/visitors.story` - deleted on `feat/adr-310-character-in-chord`, commit `6a18d12b` (local only)

**Outside repo, not staged**:
- `~/.claude/projects/-Users-david-repos-sharpee/memory/feedback_adrs_are_reference_not_imperative.md` - new memory file (first half)
- `~/.claude/projects/-Users-david-repos-sharpee/memory/project_chord_writer_release_split.md` - new memory file (second half): the Claude-builds-archive / David-distributes / package.sh-DMGs division of labor, per decision #7's correction
- `~/.claude/projects/-Users-david-repos-sharpee/memory/MEMORY.md` - indexed both new memory files
- `~/.claude/projects/-Users-david-repos-sharpee/memory/feedback_adr_312_branch_deferred.md` - read/consulted this session (per state-file `files` array) but last modified 2026-08-13, predates this session; unrelated to the ADR-310 branch (it names `feat/adr-312-cli-test-recording`) — not authored here

## Notes

**Session duration**: ~7 hours (start 15:23 CDT; last activity ~22:35 CDT per the `6a18d12b` commit timestamp and the final `MEMORY.md` edit).

**Approach, first half**: Read-corpus-then-fold — full reads of primary sources under explicit copyright ground rules, write findings to a standalone prior-art doc, fold only user-agreed amendments into the ADR, then adversarially review the amendment for citation accuracy before calling it done. No code was changed in this half; it was documentation-only, so no Behavior Statement or `mutation-verification` run applied.

**Approach, second half**: reactive bug-fix-and-ship work triggered by two user-reported defects (New Story failing; prologue/banner layout wrong from a Play-pane screenshot), each root-caused to more than the reported symptom before fixing (New Story: one missing bundle resource with zero default-argument coverage; the banner report: three independent defects in styling and channel ordering). Concluded with a version bump, a local install/verify pass on both Mac architectures, and one platform-branch cleanup (`branch-stories/visitors`) done via a scratch worktree to avoid disturbing `main`'s dirty tree.

---

## Session Metadata

- **Status**: COMPLETE (unverified: second-half test/build counts — stdlib channel tests, platform-browser, Chord Writer Swift suite, docs-tab, dungeo walkthrough chain, website `tsc`)
- **Blocker** (if any): N/A — the Intel DMG is waiting on Apple's notarization response, which is external latency, not a blocker on this session's work
- **Blocker Category**: N/A
- **Estimated Remaining**: N/A
- **Rollback Safety**: mixed — most changes are uncommitted working-tree edits on `main` (safe to revert); one commit (`6a18d12b`, deleting `branch-stories/visitors/visitors.story`) is local-only on `feat/adr-310-character-in-chord`, not pushed, and recoverable from history per the commit message; the arm64 DMG is a signed/notarized/stapled local build artifact outside git, and the Intel DMG rebuild is in flight

## Dependency/Prerequisite Check

- **Prerequisites met**: pre-session-audit ran and gate cleared at session start; project profile was 5 days old, within the 7-day window, so `dev-context-detector` was correctly skipped.
- **Prerequisites discovered**: the Chord Writer template-resource gap (item 1) and the docs-tab pre-existing red test (item 3) were both prerequisites this session had to clear before the intended work (New Story fix, docs-tab prebuild) could proceed — neither was anticipated at session start.

## Architectural Decisions

- ADR-310 (DRAFT, unaccepted, no implementation authorized): added D14 (belief addressing, `thinks` construct), D15 (topic tables as interface, character model as selector), D16 (innermost-context-wins gate resolution), resolving Open Questions 2, 4, 7.
- Pattern applied: cite-don't-carry sourcing discipline for copyrighted prior art (this session's own working rule, not a numbered platform ADR).
- Insight: "ADRs are reference, not imperative" — do not grade review findings or gate work on an ADR's Status/age/draft-ness; ground findings in code or stated user intent instead (see Key Decisions #1 and the new memory file).
- ADR-279 D4 (opt-in vendored toolchain) applied/exercised: the New Story bug traced directly to this decision's resource-path consequence not being carried into `project.yml`.
- ADR-279 D7 (Sparkle 2 auto-update): confirmed decided (2026-07-27, session `fda0f0`) but unbuilt — no code implements it; recorded as an open item rather than acted on this session.
- ADR-300 D6/D9/D12 (banner as its own channel; preferred-layout renders after prose; the CLI's classed-block rendering path) applied and preserved: the prologue/banner fix added CSS for D6's renamed classes and reordered channel registration without violating D9's ordering invariant.
- ADR-310 (branch-local, `feat/adr-310-character-in-chord`): already ACCEPTED there with its own D14-D17, distinct from main's D14-D16 added this session; left unreconciled by explicit user ruling (Key Decision #6).

## Mutation Audit

- Files with state-changing logic modified: `tools/ide/SharpeeIDE/Workspace/StoryScaffold.swift` (writes the three scaffolded story files to disk), `packages/stdlib/src/channels/standard.ts` (channel registration order, which determines render-output sequencing).
- Tests verify actual state mutations (not just events): YES for `StoryScaffold.swift` — `testScaffoldsFromTheAppBundleWithNoInjectedDirectory` asserts all three file writes and was itself written in response to a `mutation-verification` finding (item 1). For `standard.ts`, `standard.test.ts` was updated for the new order and the dungeo walkthrough chain (952 cases) is cited as the regression check, but that pass count carries the unverified marker above — no fresh corroborating run was performed in this write-up.

## Recurrence Check

- Similar to past issue? YES — the "ADRs are reference, not imperative" correction is explicitly a recurrence ("I've mentioned this before") of a pattern already tracked in memory (`project_adr_status_unreliable.md`, `project_ide_primacy_directive.md`).
- Separately: C1 (the false "nothing imports it" Context claim) is the reported eighth instance of a "proxy signal standing in for the real property" pattern the morning's pre-session-audit flagged as appearing in 3 of the last 5 summaries with no systemic audit yet run [reported by session, unverified — not independently corroborated against the audit's output in this write-up]. Consider a one-time audit of verification-claim provenance across recent ADR/review work per the pre-session-audit's own flag.
- Second-half recurrence: the `package.sh --dmg-from` wrong-architecture-slice failure (item 8) is an explicit same-day repeat of the failure `package.sh:373` already documents from 2026-08-13 — two occurrences of the same defect with a comment in the source naming the first, and the tool still does not infer architecture from the given app. Worth a one-time fix (validate the app's actual arch against `--arch`, or infer it) rather than relying on operator care a third time.
- Second-half candidate, not yet tracked: a test suite living outside `pnpm test:ci` letting a regression go unnoticed for multiple sessions (docs-tab's `nav-bridge.test.mjs`, red since commit `1b1d5269`, two sessions back) — no existing memory file covers this pattern; flagging as a candidate rather than a confirmed recurrence.

## Test Coverage Delta

- **First half**: no test changes. The adversarial `/devarch:adr-review` pass verified (did not add) 301 existing test cases across 19 files as clean/uncontested; no code paths were exercised or modified.
- **Second half**: tests added: 3 — `StoryScaffoldTests.testScaffoldsFromTheAppBundleWithNoInjectedDirectory`, `StoryScaffoldTests.testTheAppBundleCarriesTheStoryTemplate`, and one new `platform-browser` test asserting every banner-renderer class has a `base.css` rule. One pre-existing failing assertion fixed (`nav-bridge.test.mjs`'s `excluded.size`, 3 → 5), not a new test.
- **Second-half suite counts, as reported by the session** [reported by session, unverified — no session event log exists for this session (`docs/context/.devarch-events-4e8fc1.jsonl` not found) and no suites were re-run for this write-up]: stdlib channel tests 53 passing; platform-browser 141 → 142 passing; Chord Writer Swift suite 482 passing (480 + 2 new); docs-tab 43 passing; dungeo walkthrough chain 952 passing across 17 transcripts, 0 failures; website `npx tsc --noEmit` exit 0.
- Known untested areas: the banner-channel re-emission-on-restart duplication risk (Open Items) has no test either confirming or ruling it out.

---

**Progressive update**: Session completed 2026-08-14, last activity ~22:35 CDT (per the `6a18d12b` commit timestamp and the final `MEMORY.md` edit; `startedLocal` 15:23 CDT).
