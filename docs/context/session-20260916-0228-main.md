# Session Summary: 2026-09-16 - main

## Goals
- Unblock and finish Phase 3 of `docs/work/velopack-macos-bundle-layout/plan.md` — sign and
  notarize the relocated Velopack bundle end to end — which session f65b30 left BLOCKED on a
  missing credential and a required user go-ahead.
- Then Phase 4: write the Q-5 decision record.

## Phase Context
- **Plan**: `docs/work/velopack-macos-bundle-layout/plan.md` — now **DONE**, all four phases closed.
- **Phases executed**: Phase 3 (CURRENT → DONE), Phase 4 (PENDING → DONE).
- **Phase outcome**: Both closed. Q-5 answered YES on both readings the plan required to be stated separately.

## Completed

### Phase 3 — sign and notarize (DONE, sub-question 3 = YES)
- **The blocker dissolved rather than being solved.** The App Store Connect Issuer UUID that
  session f65b30 recorded as living "only in the `dc-notary` keychain profile" was committed in
  this repository the whole time, at `docs/work/archive/adr-279-chord-writer-packaging/plan.md:95`,
  with the key path and key id beside it. The keychain was never the route at all: notarytool keeps
  that profile in the data-protection keychain, which the `security` CLI cannot read
  (`security dump-keychain | grep -i notary` → empty, confirmed by David at his own prompt).
  Two credential reads were denied here by the auto-mode classifier; the xcode MCP server, which
  the prior session had hoped was an alternate route, was reachable this session
  (`SharpeeIDE.xcodeproj`, `windowtab1`) and is not one — it exposes project tooling, not keychain
  access. Routing the read through `ExecuteSnippet` was available and deliberately not taken.
- Artifact re-verified byte-identical to the signed record immediately before upload:
  101,786,359 bytes, `sha256 ded84ab02a90c4c5c57446e147f6c308fa857898218af1700236f1dd4e588ae2`.
- Submitted with David's explicit go-ahead through `tools/ide/notary-submit.py --wait`:
  `Successfully uploaded file`, id `cbcd0706-f65c-4f13-9460-e9be833044ca`, **Accepted** on the
  first submission. No orphan, no retry — the upload-line-before-id invariant the script exists
  to enforce held.
- `xcrun stapler staple` and `stapler validate` both exit 0. `spctl --assess --type execute -vv`
  → `accepted` / `source=Notarized Developer ID` / `origin=Developer ID Application: David
  Cornelson (RSNGKW5LNH)` — the exact inversion of the pre-notarization `rejected` /
  `Unnotarized Developer ID`, which was the correct state to be in at that moment.
- `codesign --verify --deep --strict` still clean after stapling, and the bundled toolchain still
  runs from inside the notarized bundle: `node v22.23.1`, `Sharpee 5.4.1 · Chord 3.6.0`.
- Installer shape settled as the `.app` zip, so §6.3's missing Developer ID Installer certificate
  never became a dependency.
- Record appended to `docs/work/velopack-macos-bundle-layout/evidence/phase3-signing-pre-notarization.txt`.

### Phase 4 — the Q-5 decision record (DONE)
- `docs/work/velopack-macos-bundle-layout/decision.md` written, citing all three phase records.
- **(i) Q-5 as literally worded — fixable by post-processing alone: YES.** No `vpk` version has a
  macOS layout option, so the fix sits entirely outside Velopack: 45 lines across `relocate.sh`
  and `patch-apphost.py`, plus one ordering constraint (relocate *before* `vpk pack`).
- **(ii) The condition D2 actually rests on — a solved macOS layout by any route: YES**, carried
  through the update-apply path and Apple's notary service to a stapled, Gatekeeper-accepted bundle.
- The divergence the plan warned about did not materialize, because there is no `vpk` option to
  create it. ADR-351's *"collapses to a better WPF for Windows"* clause is therefore **discharged**,
  not merely left unquoted — it was conditioned on (ii) being NO.
- GH #462 item (1) answered and **priced** by comment (issuecomment-5694389820); items (2)-(4) are
  untouched and keep the issue open.

## Key Decisions
- No NEW decision. Q-5's answer is a finding, and `decision.md` deliberately did not edit ADR-351.
- **ADR-351 amended at David's instruction** (after `decision.md` was written, not by it): D2's second
  condition discharged, D2 qualification 2 and the R15 matrix cell marked superseded, D4's reopening
  premise narrowed, the bundle-layout consequence repriced, Q-5 retired from Open Questions, and the
  amendment recorded in the Session section. **No score was changed and no requirement re-rated** —
  R15 stays at 2 per D5's rule that the weighting is David's; the ADR notes the evidence moved and
  the score is his call, the same treatment the Linux evidence gets against R22.
- ADR-351 remains **DRAFT**: Q-2, Q-3, Q-4 and Q-6 are still open, and D2's third condition (Q-3,
  the felt comparison) is now the only one outstanding.

### A new plan, at David's request
- `docs/work/chord-writer-avalonia-production/plan.md` — **12 phases** for a production Chord Writer
  on Avalonia + Velopack, written by `session-planner` at David's direct request and now named by
  `.current-plan`. Sequenced by dependency and risk-retirement: Phase 1 is the Q-3 felt-comparison
  gate (David's, and a hard stop if it returns NO); Phase 3 (Large, 400) is the toolchain
  portability blocker whose first deliverable is the estimate rather than code; Phase 10 audits
  shell parity against the Swift app's 17 feature folders and emits the follow-on phase list, with
  Phase 11 left deliberately unscoped until that audit exists; Phase 12 assembles the evidence and
  puts Q-4 (Swift app retirement) to David with a recommendation, retiring nothing itself.
- The planner's own adversarial review found one real gap and fixed it before hand-back: ADR-341 D5
  requires **generated** C# protocol types and the spike has none, so a hand-mirrored copy — which
  rule 8b and D5 both forbid — is now an explicit Phase 2 deliverable.
- Phases 3 and 9 touch `packages/`, so both carry David's sign-off as an entry condition per
  CLAUDE.md's platform-change rule. **No implementation is authorized; the plan is a plan.**
- `.current-plan` moved from the now-DONE `velopack-macos-bundle-layout/plan.md`. Rule 18b's
  disposition prompt correctly did not fire — every phase of the outgoing plan was already terminal.

## Next Phase
- **Phase 1 of the new plan**: David runs the notarized spike `.app` and records the felt comparison.
  Nothing downstream starts until that returns.
- The ADR-351 Q-5 edit was requested and is now done. One follow-up remains David's: whether
  this now-terminal plan directory is archived (`plan-archive.sh` would move it, and `decision.md`'s
  path is cited in a GitHub comment, so it was left in place rather than moved unasked).
- The other live thread is Phases 5-6 of `docs/work/adr-349-location-heading/plan.md`.

## Open Items

### Short Term
- 462: items (2) replay over-run, (3) editor light palette, (4) localStorage persistence across
  restart — this plan touched none of them.
- 474: Phase 4 shell probe validates only absolute host paths, not the bundled toolchain's
  relocated launch path (`pane/PaneHost/Shell/ShellWindow.axaml.cs:34-38`).
- `.current-plan` names this now-DONE plan. The `pre-session-audit` reported it as naming
  `docs/work/adr-349-location-heading/plan.md` and marked that `[verified]`; it is not — the
  pointer file reads `docs/work/velopack-macos-bundle-layout/plan.md`. Worth knowing the audit's
  verified marker was wrong on this point.

### Long Term
- None new.

## Files Modified
- `docs/work/velopack-macos-bundle-layout/decision.md` - new; the Q-5 decision record.
- `docs/work/velopack-macos-bundle-layout/evidence/phase3-signing-pre-notarization.txt` - notarization,
  stapling, Gatekeeper and post-staple runtime results appended, plus the credential-location finding.
- `docs/work/velopack-macos-bundle-layout/plan.md` - Phase 3 → DONE, Phase 4 → DONE, Plan Status → DONE.
- `docs/work/chord-writer-avalonia-production/plan.md` - new; the 12-phase production plan.
- `docs/context/.current-plan` - repointed to that plan.
- `docs/work/avalonia-ide-evaluation/decision.md` - §13 packaging addendum added (the macOS bundle
  is sealed, signed, notarized, accepted), plus four in-place amendments so a reader hitting §5, §7
  or §11 first is not misled: §5 marked superseded in part, its notarization-question verdict
  closed, §7's three conditions reduced to the felt comparison alone, and §11's two resolved items
  marked DONE with the narrower list that replaces them. The addendum also names, rather than
  silently fills, the fact that Phase 8 (Linux) still has no addendum in that record.
- `docs/architecture/adrs/adr-351-chord-writer-host-shape.md` - amended with the Q-5 resolution
  (8 edits; Status, D2, D2 qualification 2, R15 cell, D4, Consequences, Session, Open Questions).
- `docs/context/session-20260916-0228-main.md` - this file.

**Outside the repo**: `/Users/david/repos/spikes/avalonia-ide/out/signed-verify/ChordWriterAvaloniaSpike.app`
is now notarized and stapled in place. Nothing else in the spike changed.

## Notes

**Approach**: Resumed a blocked phase rather than re-deriving it. The credential hunt that blocked
the prior session was closed by grepping the repository, which is the finding most likely to save
time later and is recorded in both the evidence file and `decision.md` §6.

**No platform code touched.** No `packages/` source, no tests run (none applicable — this validated
an external packaging toolchain).

---

## Session Metadata

- **Session**: e923d3
- **Status**: COMPLETE
- **Blocker**: none
- **Blocker Category**: N/A
- **Estimated Remaining**: none for this plan.
- **Rollback Safety**: safe to revert — documentation and evidence only in-repo. The out-of-repo
  change (a stapled notarization ticket on a spike bundle) is not reverted by a git revert and does
  not need to be.

## Dependency/Prerequisite Check
- **Prerequisites met**: Developer ID Application certificate (RSNGKW5LNH); `AuthKey_MASFPJ29HJ.p8`
  at `~/.appstoreconnect/private_keys/`; Issuer UUID (from the repository, not the keychain);
  `aws` CLI and `openssl` for `notary-submit.py`; the signed Phase 3 artifact still on disk.
- **Prerequisites discovered but not available**: none. The xcode MCP server came back up this
  session and turned out to be irrelevant to the blocker.

## Architectural Decisions
None. `decision.md` answers a question; ADR-351 is left unedited by design.

## Mutation Audit
N/A — no state-changing platform code modified. Rule 15 did not fire: no source file with a
side-effect function was written.

## Recurrence Check
- Similar to past issue? **YES, and it is the interesting kind.** This is a second instance of a
  credential or prerequisite being recorded as unavailable without the repository being searched
  first. GH #430 (Documentation recurrence, 3 occurrences — stale records outliving the mechanism
  they describe) is the nearest category: a session summary asserted a credential's only location,
  and the next session inherited that assertion instead of checking it.
- Test Infrastructure recurrence (#435, 5 occurrences — gates reporting green without exercising
  real behavior) is worth noting *against* this session as a counter-example: rule 13a's real-path
  requirement was honoured, with one genuine submission to Apple rather than a dry run.

## Test Coverage Delta
- Tests added: none (no platform source changed).
- Tests passing before/after: not applicable — no suite run.
- Known untested areas: the x86_64 slice of this recipe is unexercised; the shipping `tools/ide/`
  integration of the relocation is unpriced (the spike is not Chord Writer); #474's coverage gap
  stands.

**Progressive update**: Session work complete 2026-09-16 (Phases 3 and 4 both DONE, plan DONE).
