# Session Plan: ISSUE-063 Phase 1 — Make getTrait Generic, Eliminate Trait-Access as-any Casts

**Created**: 2026-03-26
**Overall scope**: Fix the root cause of 1,035 `as any` casts by making `getTrait` generic so callers receive the correct trait type without casting. Then clean up trait-access casts in the worst-offending platform source files.
**Bounded contexts touched**: N/A — infrastructure/tooling (TypeScript type system, no domain behavior change)
**Key domain language**: N/A

## Phases

### Phase 1: Design and Implement Generic getTrait Signature
- **Tier**: Medium
- **Budget**: 250
- **Domain focus**: N/A — platform type infrastructure
- **Entry state**: Branch `issue-063-as-any-cleanup` checked out; `getTrait(type)` returns `ITrait | undefined` forcing callers to cast
- **Deliverable**: Updated `IFEntity` interface and implementation in `packages/world-model/src/entities/if-entity.ts` with a generic `getTrait<T extends ITrait>(type: TraitType): T | undefined` signature; optionally a `TraitTypeMap` if the type-map pattern is preferred after reading the current code
- **Exit state**: `getTrait` compiles without errors; no existing callers are broken (backward-compatible via type inference defaulting to `ITrait`)
- **Status**: CURRENT

### Phase 2: Clean Up Trait-Access as-any in Worst Offender Source Files
- **Tier**: Medium
- **Budget**: 250
- **Domain focus**: N/A — platform type cleanup
- **Entry state**: Phase 1 complete; generic `getTrait` is in place and builds cleanly
- **Deliverable**: `as any` casts removed from the three worst offenders — `game-engine.ts` (32 casts), `VisibilityBehavior.ts` (21 casts), `drinking.ts` (22 casts) — and any remaining high-count platform source files identified in the issue; full build passes
- **Exit state**: `as any` count in those files reduced to zero for trait-access patterns; `./build.sh` passes
- **Status**: PENDING

### Phase 3: Clean Up Remaining Source Files
- **Tier**: Small
- **Budget**: 100
- **Domain focus**: N/A — platform type cleanup
- **Entry state**: Phase 2 complete; worst offenders cleaned
- **Deliverable**: All remaining `as any` trait-access casts in non-test platform source files eliminated; `sharedData as any` and `world as any` patterns addressed where straightforward; build passes
- **Exit state**: `as any` count in `packages/` source files (excluding tests) materially reduced; baseline documented for Phase 4 (CI enforcement)
- **Status**: PENDING
