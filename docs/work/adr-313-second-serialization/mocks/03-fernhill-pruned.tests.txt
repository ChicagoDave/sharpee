story    fernhill
seed     42
format   1

# The projected file after an author edits it. Three distinct edits, each of
# which the format has to express unambiguously:
#
#   1. Long description claims DELETED, room-name claims kept. These cards
#      still have claims, so a replay never re-derives them (model.ts:69-72).
#   2. `take all` demoted to `skip` — it runs and asserts nothing, on purpose.
#      This is NOT the same as deleting its claims, which would read as a void.
#   3. `open gate` spliced in as a new turn with no claims — a VOID. The next
#      replay fills it (model.ts:334-337).

opening
    channel info.title is The Folly at Fernhill

boot
    contains Iron Gates

> north
    contains Gravel Drive

> north
    contains Fountain Court

    fork 1
        > west
            contains Greenhouse

    fork 2
        > east
            contains Boiler Shed

> open gate

> north
    contains Entrance Hall

> east
    contains Kitchen

> take all
    skip
