# Session Plan: Implement ADR-349 — the location heading is authored and composed per turn

**Created**: 2026-09-15
**Plan Status**: ACTIVE
**Overall scope**: Build the location-heading projection (`LocationHeadingBehavior.resolve` + the location-heading registry) in world-model, add the `room name` construct to Chord, wire story-loader's registration seam, cut the `location`/`room-name` wire over to the new projection (one-shot, D12), and add the auto-assertion refusal — discharging all 14 acceptance criteria of ADR-349.
**Bounded contexts touched**: World Model (location/visibility projection), Chord Story Language (grammar/analyzer/IR), Story Loader (compilation and registration seam), Engine (prose pipeline / room block), stdlib (channels, looking-data), if-domain (channel wire contract), channel-service / platform-browser (rendering), lang-en-us (English Assembler realization), branch-tester + transcript-tester (the auto-assertion policy and the assertion core it actually lives in — see Phase 5's owed ADR amendment).
**Key domain language**: location heading, `HeadingPart` (`place` / `enclosure` / `region` role), `LocationHeadingBehavior.resolve`, location-heading registry, the `room name` construct.

## References consulted
- `docs/architecture/adrs/adr-349-the-location-heading-is-authored-and-composed-per-turn.md` — the ADR this plan implements: 16 decisions (D1-D16a), 14 acceptance criteria, and a Scope line naming every package this plan may touch; D12 is a one-shot wire cutover with no compatibility path.
- `docs/architecture/adrs/adr-107-dual-mode-authored-content.md` — the `nameId` dual-mode pattern ADR-349 "revisits, not overturns"; `packages/engine/src/prose-pipeline/handlers/room.ts` already implements ADR-107 precedence for room descriptions and must keep doing so alongside the new heading path.
- `docs/architecture/adrs/adr-163-channel-service-platform.md` — channels are the universal UI wire (one concept: definition + closure); the `location` channel's payload-shape cutover (D12) must stay inside this contract.
- `docs/architecture/adrs/adr-165-renderer-architecture.md` — one renderer per channel, last-write-wins story override; the platform-browser `status.ts` renderer change must keep that shape rather than special-casing `location`.
- `docs/architecture/adrs/adr-174-decoration-and-prose-pipeline.md` — the bracket-decoration span+class wire shape authors already get for free; D6's "no component vocabulary" decision rests on this existing mechanism, not a new one.
- `docs/architecture/adrs/adr-255-message-override-acl.md` — the author message-override surface; D9 states this ADR deliberately does not extend it, so no phase may grow the `if.action.*` alias namespace or make `if.room.description_body` overridable.
- `docs/architecture/adrs/adr-289-chord-routing-decided-once.md` — D6 gates exits to rooms at compile time; ADR-349's D10 deliberately does not follow that shape (the construct is legal on rooms and enclosures alike, neither gated on the other's traits).
- `docs/architecture/adrs/adr-293-choice-points-per-point-streams.md` — walkthrough chains are deterministic at a pinned seed; the run-twice policy is retired. Governs how Phase 6's AC-5 regression run is executed (one run, no flakiness re-check).
- `docs/architecture/adrs/adr-340-testing-assertion-core.md` — the unified assertion core shared by `transcript-tester` and `branch-tester`; governs which harness AC-1's fixture and AC-3's optional runtime companion use (a branch-tester tree claim, never a `.transcript` — Dungeo walkthroughs are not a Chord design input).
- `docs/architecture/adrs/adr-350-vehicles-in-chord.md` — unscheduled research; confirms AC-4 stays a TypeScript REAL-PATH test (no Chord vehicle surface exists to write it against), and flags that `transparent` becomes load-bearing under this ADR's D5 independently of ADR-350's own timeline.
- `docs/context/project-profile.md` — TypeScript strict mode across packages, per-package Vitest (`pnpm --filter '@sharpee/<pkg>' test`), `./repokit build` as the platform build entry point, transcript regression via the CLI bundle, no external data layer / no back-compat shims.
- `docs/context/session-20260914-2322-main.md` — the session that accepted ADR-349: no open items, no blockers carried forward; recorded the transcript-tester/branch-tester harness split now in `docs/core-concepts/README.md`, which AC-3's "never a `.transcript`" note depends on.

## Review folds (2026-09-15, session 3c9f76)

`/devarch:plan-review` run independently of the planner, against all 12 references above. Four blocking findings, all folded below. Phase 1 came through clean and its code premises were re-verified: `LocationHeadingBehavior`, `location-heading-registry` and `compileLocationNames` are absent from `packages/`; `RoomTrait.regionId` (`roomTrait.ts:84`), `RegionTrait.parentRegionId` (`regionTrait.ts:25`), `WorldModel.assignRoom` (`:2090`) and `isInRegion` (`:2118`) all exist; `inVehicle` is exactly the three writes at `looking-data.ts:67,148,327`.

1. **Phase 5 named a file that does not hold the logic** (ADR-340 D1/A1) — rewritten against `assertion-core.ts:663` and the three writers that reach it; an ADR-349 Scope amendment is now recorded as owed.
2. **Phase 4 would have deleted ADR-107's `nameId` path for the room name** (`room.ts:73-81`; ADR-107 "ID takes precedence", ACCEPTED — IMPLEMENTED) — the fallback disposition is now written out and AC-3's structural test scoped to heading *parts*.
3. **Phase 4 changed the `location` payload without its `contentType`** (ADR-163 §3) — the `text` → `json` flip is now named.
4. **Phase 2's exit state was unattainable** — the `chord.ebnf` pin gate (`language-version.test.ts:106,121-127`) is now part of the deliverable, with no version move.

Two advisories, not folded. ADR-350's Recommendation asks for ADR-349's D5 and D4a to be settled with it rather than before it; it is RESEARCH — NOT SCHEDULED, and because `resolve` delegates to `getDescribableLocation` the transparency seam can move underneath the projection later, so this is a thing to watch rather than a blocker. And `HeadingPart.text` carries resolved prose out of `world-model`, against the project profile's "world-model emits message-ID-only events" convention — D11 decided that deliberately (the text is story-authored IR prose, not platform message text), and AC-3's structural test freezes it.

One evidence correction: the planner reported no matching open issues from `issues.sh list-open`. That projection returns 60 items and holds neither **GH #467** (lidless container) nor **GH #468** (the heading/status-line divergence AC-7 regression-tests), but `gh issue view` shows **both OPEN**, created 2026-09-15. The local store is stale against GitHub; #467 is non-blocking either way under ADR-349 Q-3.

## Phases

### Phase 1: The heading projection — `LocationHeadingBehavior.resolve` and the location-heading registry
- **Tier**: Medium
- **Budget**: 250
- **Domain focus**: World Model bounded context — the per-turn `HeadingPart` projection (D11) and its registration seam's world-model-side half (D16 contract 2/3).
- **Entry state**: `packages/world-model/src/world/VisibilityBehavior.ts:548-600` (`getDescribableLocation`, verified: room short-circuit, transparent-vehicle/open-container branch to `{ location: room, immediateContainer }`, opaque-vehicle/closed-container branch to self) is the only existing input; no `LocationHeadingBehavior` or location-heading registry exists yet (confirmed: no file matches under `packages/world-model/src/world/`).
- **Deliverable**: `packages/world-model/src/world/LocationHeadingBehavior.ts` exposing `static resolve(observer: IFEntity, world: WorldModel): ReadonlyArray<HeadingPart>` (D11's contract: `ownerId`, `text`, `role: 'place' | 'enclosure' | 'region'`), reading the place/enclosure from `getDescribableLocation` (never `getContainingRoom` — D4a) and the region chain from `RoomTrait.regionId` → `RegionTrait.parentRegionId` (`WorldModel.assignRoom`/`isInRegion`); `packages/world-model/src/location-heading-registry.ts` with `registerLocationName`, `lookupLocationName`, `clearLocationNames` keyed by entity id (D16 contract 2 — closures, not `IRCondition`s, modeled on `snippet-gate-registry.ts`'s Map-based, idempotent-last-wins, never-serialized shape); D16a part ordering (place, enclosure, regions innermost-to-outermost). **The D7/D16a fallback is reported, not rendered, here**: `resolve` returns an empty list when no contributor produced a part, and each consumer then renders what it renders today — which is what keeps ADR-107's `nameId` path alive (Phase 4's fold) and what makes the fallback a single decision at the consumer rather than a second one in `world-model`, which has no language provider to resolve a `nameId` with. Discharges **AC-9**'s arm selection and its all-fail/absent equivalence, unit-tested directly against the registry with no Chord fixture; AC-9's remaining half — that the heading *renders* the entity name rather than empty — is a consumer property and lands with Phase 4.
- **Exit state**: `pnpm --filter '@sharpee/world-model' test` passes, including unit tests that hand-register arms (closures) and assert `resolve`'s fallback and ordering behavior without any Chord or story-loader involvement.
- **Status**: **DONE 2026-09-15, session 3c9f76.** Built: `packages/world-model/src/location-heading-registry.ts` (Map-keyed by entity id, idempotent last-wins, never serialized — the lifecycle contract is in its header) and `packages/world-model/src/world/LocationHeadingBehavior.ts` (`resolve`, place → enclosure → regions innermost-to-outermost, region walk guarded at depth 64 against an author-written `parentRegionId` cycle). Both exported from the root barrel and `world/index.ts`. Evidence: `npx tsc --noEmit -p packages/world-model/tsconfig.json` exit 0; `pnpm --filter '@sharpee/world-model' test` — **88 files, 1540 tests passing**, including the new `tests/unit/world/location-heading.test.ts` (13 tests). Verified in those tests against a real `WorldModel`, not a stub: first-match-wins arm selection, the unconditional arm winning when conditionals fail, all-fail reporting identically to never-registered, live re-evaluation after a world change with no re-registration (D2), the four-part place/enclosure/region ordering through a transparent vehicle and a nested region pair, the silent-room/speaking-region maze case (D16a), an opaque vehicle taking the place slot and composing with nothing (D4a), and a region cycle terminating.
  **One reading recorded, not a change of decision**: D16a's "the entity name appears only when the whole heading would otherwise be empty" is implemented as `resolve` returning `[]`, with the entity-name rendering owed by the consumer. Written this way because `world-model` holds no language provider and so cannot honor ADR-107's `nameId`; putting the fallback in the projection would have silently dropped localized room names. Worth an ADR-349 note alongside Phase 5's owed Scope amendment.

### Phase 2: The `room name` Chord construct — grammar, analyzer, IR
- **Tier**: Medium
- **Budget**: 250
- **Domain focus**: Chord Story Language bounded context — an authored, per-entity, multi-arm `room name` block (D1, D15).
- **Entry state**: `packages/chord/chord.ebnf:858-859` already defines `phrase-override` with `"while" condition` and numbered-key derivation ("repeating `detail` keys (`<id>.detail.2`, …) are analyzer-derived") — the idiom this construct reuses; `packages/chord/src/analyzer.ts:3883,3902` (`collectEntity`) confirmed keying `analysis.duplicate-entity` on the lowercased entity name, independent of any heading text; `packages/chord/src/ir.ts:243-246` confirmed `IREntity.id`/`.name` unchanged by this work.
- **Deliverable**: grammar and analyzer support for a `room name` block on `create` (room, enterable enclosure, or region — every D4 contributor kind), emitting arms at `<entityId>.room-name`, `<entityId>.room-name.2`, `<entityId>.room-name.3`, … in the existing flat phrase table (D15 — `IREntity` gains no field, `IRPhrases` gains no shape), each an ordinary `IRPhrase` carrying its optional `condition`; analyzer gates for at most one unconditional arm, and only as the last arm (an earlier unconditional arm is a compile error, D15/D12 in the ADR's AC-12 numbering). **The `chord.ebnf` surface pin moves with the grammar**: new grammar changes `packages/chord/chord.ebnf`, and `packages/chord/tests/language-version.test.ts:121-127` compares its sha256 against `PINNED.ebnfSha256` (`:106`), so the chord suite is red until the pin is re-recorded with a dated comment in that file's existing convention. The version number does **not** move — `room name` folds into the unpublished 3.6.0 set, per the standing ruling that Chord/Sharpee versions move at publish rather than per landing (the same treatment `, one-way` got on 2026-09-03). Discharges **AC-12** (three-arm compile shape, declaration order, both negative compile-error cases).
- **Exit state**: `pnpm --filter '@sharpee/chord' test` passes — including `language-version.test.ts`, whose ebnf pin has been re-recorded — with fixtures for a multi-arm `room name` on a room, an enterable enclosure, and a region, plus both compile-error negatives; two entities with distinct names and identical `room name` text compile with no `analysis.duplicate-entity` diagnostic (the D1 half of AC-1 — analyzer-level only; the full maze transcript is Phase 6).
- **Status**: **DONE 2026-09-15, session 3c9f76.** Built: `room name [while <cond>]:` at `create` scope, parsed as a phrase-override under the reserved key `room-name` (`parser.ts` — `parsePhraseOverride` gained an alternate header, so the two spellings share one body reader), and numbered in the analyzer's existing repeat-key path, generalized from `detail`'s private counter to a per-key one — so the emitted keys are `<id>.room-name`, `<id>.room-name.2`, … and `IREntity`/`IRPhrases` gain nothing (D15, D16 contract 1). Four analyzer gates, all keyed on the phrase key rather than the spelling so `phrase room-name:` cannot bypass them: `analysis.room-name-duplicate-unconditional`, `analysis.room-name-dead-arm`, `analysis.room-name-variants`, `analysis.room-name-owner`. `chord.ebnf` gained the `room-name-block` production with its gate notes; the pin in `language-version.test.ts` was re-recorded to `37c9fdb6…99e2` with a dated comment, and the language version did **not** move. Evidence: `npx tsc --noEmit -p packages/chord/tsconfig.json` exit 0; `pnpm --filter '@sharpee/chord' test` — **78 files, 1157 tests passing**, including `language-version.test.ts` and the new `tests/adr-349-room-name.test.ts` (10 tests): the three-arm numbered shape in declaration order, per-arm conditions with the unconditional arm carrying none, no new IR field, all four gates firing on their own case, the legal arm order accepted, the block accepted on an enterable thing and on a region, and twelve distinct-named maze rooms sharing one heading with no `analysis.duplicate-entity`.
  **One gate beyond D15's two named negatives**: `analysis.room-name-variants` rejects `or`-separated variants inside a single arm. D15 names only the two unconditional-arm errors, but D11's contract carries one text per arm ("the winning arm's resolved prose") and Phase 1's `LocationNameArm.text` is a single string, so a multi-variant arm has nowhere to land. Gated rather than silently dropped, mirroring `analysis.detail-variants`. Worth an ADR-349 note with the other two owed amendments.
  **Rule 15 did not fire**: the touched functions are `parsePhraseOverride` and the analyzer's `collect()`, neither matching the side-effect name signal, and a compiler pass mutates no world state.

### Phase 3: The registration seam — story-loader wires Chord IR into the world-model registry
- **Tier**: Medium
- **Budget**: 250
- **Domain focus**: Story Loader bounded context — D16 contract 1 (chord → story-loader, already Phase 2's output) and contract 2 (story-loader → world-model, `compileLocationNames`).
- **Entry state**: Phase 1's registry API and Phase 2's numbered-key IR phrases both exist; `packages/story-loader/src/loader.ts:2453-2464` (the `detail` numbering loop) confirmed as the shape to imitate — `for (let i = 1; ; i++)` reading `<id>.detail`/`<id>.detail.N` until the first gap; `packages/story-loader/src/loader.ts:2368` and `:1304` confirmed as the existing closure-over-evaluator idiom (`() => this.evaluator.evalCondition(condition, { world })`) that keeps `world-model` from learning what an `IRCondition` is.
- **Deliverable**: `compileLocationNames(world)` walking `ir.entities`, reading each entity's numbered `room-name` keys, and registering closures via Phase 1's `registerLocationName` for any entity with at least one arm; region membership wired through `RoomTrait.regionId`/`RegionTrait.parentRegionId` so a region's `room name` block contributes to every member room's heading (D4/D16a) without the member needing its own arm. Discharges **AC-13** (region contribution, the maze-by-region case, and D16a's "no arm of its own renders the region's part alone" refinement) and **AC-14** (save/restore round trip: the loader re-registers on every story load, an in-game RESTORE reuses the registrations already in place, nothing is written to a save — tested by restoring and asserting the arm-appropriate heading renders, not the first arm permanently).
- **Exit state**: `pnpm --filter '@sharpee/story-loader' test` passes, including a save/restore-cycle test proving the registry survives an in-process RESTORE without ever serializing an arm predicate.
- **Status**: **DONE 2026-09-15, session 3c9f76.** Built: `compileLocationNames(world)` in `packages/story-loader/src/loader.ts`, run in the compile pass list beside `compileDetailChannels`. It reads each IR entity's numbered `<id>.room-name[.n]` keys until the first gap (the `detail` loop's shape), wraps every conditional arm as `() => this.evaluator.evalCondition(condition, { world })` — the closure idiom the snippet and slot-entry gates already use, so `world-model` never learns what an `IRCondition` is — and registers under the **world** entity id, which is what the projection resolves by. The pass clears the registry first: a second story in one process reuses entity ids, and a stale arm would otherwise answer for an entity whose own story declared none. An in-game RESTORE does not come through the pass, which is what leaves the live registrations in place. Evidence: `./repokit build dungeo` (needed first — `story-loader` reads world-model's built `.d.ts`, and Phase 1's exports were source-only); `npx tsc --noEmit -p packages/story-loader/tsconfig.json` exit 0; `pnpm --filter '@sharpee/story-loader' test` — **124 files, 1118 tests passing**, including the new `tests/adr-349-location-names.test.ts` (6 tests, all green on the first run). Discharged: **AC-13** both ways — a member room with no arm renders the region's part alone, and a member with its own arm renders place-then-region — and **AC-14**, which saves, restores into the same world with `loadJSON`, asserts the arm that held at save time still holds, and then flips the world to prove the predicate is still live rather than frozen. Also pinned: one arm per numbered key under the world id with the unconditional arm carrying no `holds`, nothing registered for a story with no `room name`, and live evaluation against the world rather than load-time state.
  **One gap found and closed by `mutation-verification` (rule 15)**: the first six tests would all have passed with the loader's own `clearLocationNames()` deleted, because the file's `beforeEach` cleared independently — the production guard was untested. A seventh test now loads two stories inside one test, asserts the second story reuses the first's entity id (so it fails loudly if id derivation ever stops colliding), and asserts the second `Well` has no arms and resolves to no parts. Probed in both directions: with the clear commented out, that test alone fails and the other six still pass; restored, all 7 pass. The same review confirmed the AC-14 shape is faithful to production — `save-restore-service.ts:217,335` both call `world.loadJSON` on the same live `WorldModel` the engine already holds.

### Phase 4: The one-shot wire cutover — both consumers call `resolve`, and nothing else does
- **Tier**: Large
- **Budget**: 400
- **Domain focus**: cross-cutting — Engine (prose pipeline), stdlib (channels, looking-data), if-domain (channel contract), channel-service, platform-browser, lang-en-us (English Assembler). This is D12's one-shot cutover: it must land as a single unit so the tree keeps building, with no `string`-shaped `location` payload left anywhere.
- **Entry state**: verified current state — `packages/engine/src/prose-pipeline/handlers/room.ts:69-90` builds the `room.name` block from `data.room?.nameId`/`data.roomName` (ADR-107 dual-mode), not from any projection; `packages/stdlib/src/channels/standard.ts:263-275` (`locationChannel`, `contentType: 'text'`, `mode: 'replace'`, `emit: 'always'`) calls `packages/stdlib/src/channels/world-helpers.ts:58-68` (`playerLocationName`, reading `world.getContainingRoom(player.id).name` — confirmed the `getContainingRoom` route D4a forbids); `packages/if-domain/src/channels/types.ts:287-297` (`ChannelProduceContext`, `world: unknown`, no language provider) confirmed as the shape `resolve`'s IR-registered-prose reliance (D11) must respect; `packages/channel-service/src/render-to-string.ts:260` (`renderStatusLine`) and `packages/platform-browser/src/channels/status.ts:22-31` (`createLocationChannelRenderer`, currently `typeof value !== 'string'` guard) confirmed as the two renderer call sites D12's Consequences section names; `packages/lang-en-us/src/assembler/english-assembler.ts:1-27` confirmed as the "SOLE authority" for cross-cutting realization (article, agreement, punctuation, whitespace, reference, case) that D13 assigns the parts-join to; `packages/stdlib/src/actions/standard/looking/looking-data.ts:67,148,327` confirmed as `inVehicle`'s only three writes, and a repo-wide grep confirmed zero reads.
- **Deliverable**: `room.ts`'s room-name block and `standard.ts`'s `locationChannel` both derive every heading *part* from Phase 1's `LocationHeadingBehavior.resolve` and from no other route (D3 — one projection, two consumers). **ADR-107's `nameId` path survives, in the fallback and only there**: `room.ts:73-81` resolves `data.room?.nameId` through `context.languageProvider` today, and `resolve` cannot do that lookup because `world-model` holds no language provider (D11; ADR-107 Decision Driver 4, "world-model must not depend on language provider"). D16a already narrows the entity-name fallback to "no contributor produced a part at all", and ADR-107's localized name *is* that entity name in its localized form — so when `resolve` returns no parts each consumer falls back to exactly the path it uses today (the block's `nameId` → literal chain, the channel's room name), which is what D7's "renders exactly as it does today" promises. That fallback is not a second derivation of the heading, and AC-3's structural test is written to say so: neither consumer may obtain a heading *part* by any route but `resolve`, while the no-part fallback is left alone; `if-domain/src/channels/types.ts`'s `location` payload changes from `string` to a structured `HeadingPart[]`-derived shape with no compatibility form kept alongside (D12), **and `locationChannel`'s own declaration moves with it** — `packages/stdlib/src/channels/standard.ts:266-272` is an `IOChannel<string>` with `contentType: 'text'`, and ADR-163 §3 makes `contentType` the wire-level shape contract (`text` = plain string), so it becomes a `json` channel of the new payload type, the shape `scoreChannel` already uses for a structured status value; `mode: 'replace'` and `emit: 'always'` are unchanged; the English Assembler realizes the parts as a `Sequence` — positional, no conjunction ("Top of Well, in the bucket", never "...and the bucket") — per D13; `render-to-string.ts`'s `renderStatusLine` and `platform-browser/status.ts`'s `createLocationChannelRenderer` are updated to the new payload shape; `looking-data.ts`'s three `inVehicle` writes are removed (D8, superseded by the resolve-based path). An Integration Reality Statement (rule 13a) is produced before AC-4 is declared complete, since it is REAL-PATH: the bucket test is driven through an assembled engine turn, not a stubbed composer. Discharges **AC-2** (state-varying heading, no look required), **AC-3** (structural unit test asserting both call sites use `resolve` and nothing else), **AC-4** (the bucket, TypeScript REAL-PATH per ADR-350's note that no Chord vehicle surface exists yet), **AC-6** (`grep -rn "inVehicle" packages` returns nothing), **AC-7** (the opaque-vehicle regression for GH #468 — written first against today's `getContainingRoom`-based divergence so it fails, then made to pass by the cutover), **AC-8** (the three transition moments: entering, leaving, and the bucket moving between rooms with the enclosure part surviving unchanged), **AC-10** (assembler joining, Sequence not PhraseList).
- **Exit state**: `pnpm --filter '@sharpee/world-model' test`, `pnpm --filter '@sharpee/stdlib' test`, `pnpm --filter '@sharpee/if-domain' test`, `pnpm --filter '@sharpee/engine' test`, `pnpm --filter '@sharpee/channel-service' test`, `pnpm --filter '@sharpee/platform-browser' test`, and `pnpm --filter '@sharpee/lang-en-us' test` all pass; `./repokit build` succeeds end-to-end with no package left on the old `string` payload.
- **Status**: CURRENT (since 2026-09-15)

### Phase 5: Branch-tester auto-assertion refusal for a varying heading
- **Tier**: Small
- **Budget**: 100
- **Domain focus**: Testing Intelligence bounded context — the `room-name-and-description` auto-assertion policy's static refusal (D14, Q-5).
- **Entry state**: `packages/branch-tester/src/auto-assertion.ts:34` confirmed `room-name-and-description` as the platform's effective default policy — but **the synthesis itself is not in that file**. ADR-340 D1 and Amendment A1 moved it into the assertion core: `synthesizePolicyAssertions` is defined at `packages/transcript-tester/src/assertion-core.ts:663` and only re-exported by `branch-tester/src/auto-assertion.ts:23`. Its signature is `(policy, actualOutput, channelValues)` — no world, no IR, no room identity — so it cannot see D14's static `while`-arm property without a new input. **Three writers pin a room name under a policy**, and the refusal must reach all three or the varying heading is pinned by whichever one the author used: `command-core.ts:574` (reached by the transcript world directly and by branch-tester's runner through `runCommand`, `branch-tester/src/runner.ts:245,259`), the IDE's record-time synthesis (`tools/ide/web/testing-surface/src/compose.ts:131`), and branch-tester's own boot-turn path (`synthesizeOpeningAssertions`, `auto-assertion.ts:132`, called at `runner.ts:276`). Phase 2's IR is where the static property comes from.
- **Deliverable**: `synthesizePolicyAssertions` gains an input carrying which entities have a varying heading (the IR-derived set, supplied by each caller; absent = today's behavior) and refuses to synthesize the room-name assertion for one of them — the description half is unaffected. `synthesizeOpeningAssertions` takes the same input for the boot turn. The refusal has **one spelling**, in the core, which is what `auto-assertion.ts`'s own header requires ("Anything either writer records comes from HERE — a second spelling of the synthesis is drift"). An unconditional `room name`, or a room with none, is pinned exactly as today. **Owed before this phase starts**: ADR-349's Scope line names `packages/branch-tester/src/auto-assertion.ts` and not `packages/transcript-tester`, so the ADR needs a Scope amendment recording where the assertion core actually lives. That is David's call, not this plan's — the alternative, duplicating the refusal at each of the three call sites to keep transcript-tester out of scope, is the drift the core exists to prevent and is not proposed here.
- **Exit state**: `pnpm --filter '@sharpee/transcript-tester' test` and `pnpm --filter '@sharpee/branch-tester' test` both pass, including both directions of **AC-11** — removing the refusal must pin the varying heading (proving the refusal is load-bearing), and over-applying it must not stop pinning the unconditional cases — and the transcript world's synthesis is unchanged when no varying-entity input is supplied, which is ADR-307's firewall restated as a test.
- **Status**: PENDING

### Phase 6: End-to-end acceptance sweep — the maze, and no regression
- **Tier**: Small
- **Budget**: 100
- **Domain focus**: cross-cutting verification — the two acceptance criteria that only make sense once every prior phase is in place.
- **Entry state**: Phases 1-5 all complete and each package's own test suite green; `./repokit build dungeo` available for the transcript bundle.
- **Deliverable**: a dedicated Chord test-story fixture (never Dungeo — Dungeo declares no `room name` and is not a design input for Chord work) with twelve distinct-named rooms sharing one identical `room name`, compiled and rendered through the full pipeline (Chord → story-loader → engine block + status channel) to confirm all twelve read the same heading with no `analysis.duplicate-entity` diagnostic; a full run of `node dist/cli/sharpee.js --test --chain stories/dungeo/walkthroughs/wt-*.transcript` (single run, deterministic at the pinned seed per ADR-293 — never re-run to check for flakiness) to confirm every existing transcript still passes unchanged, since no story declares a `room name` block. A short traceability note maps each of the 14 acceptance criteria to the test that discharges it, for the work summary.
- **Exit state**: **AC-1** (the maze) and **AC-5** (no regression) both green; all 14 ADR-349 acceptance criteria have a named, passing test.
- **Status**: PENDING
