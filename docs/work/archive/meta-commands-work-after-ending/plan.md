# Session Plan: GH #414 — meta commands work after an ending, and the browser client stops re-stranding the player

**Created**: 2026-09-11
**Plan Status**: DONE (all five phases, 2026-09-11)
**Overall scope**: Three stacked defects behind one symptom (typed `restart` dies at end-game, reload doesn't help). Phase 1 fixed the engine so a `stopped` engine accepts meta commands (ADR-345 **D15**). Phases 2-5 implement **ADR-347** — the ending becomes an explicit concept — which is where GH #414's remaining two defects are fixed. **Rewritten 2026-09-11 (session aceb2e) after ADR-347 was accepted**: the original single Phase 2 gated the browser autosave at its call site, which ADR-347's Q-1 ruling declined in favour of fixing defect 2 through the concept. One phase became four because the decision spans `if-domain`, `world-model`, `stdlib`, `engine`, `story-loader` and `platform-browser`.
**Bounded contexts touched**: Phase 1 was engine lifecycle only — no domain modeling. **Phases 2-5 are domain modeling**: ADR-347 introduces the Ending as an explicit concept owned by the world, which is a new member of the domain language rather than a guard against an existing one. That change was made after the plan was first written, and this line is corrected rather than left saying N/A.
**Key domain language**: the **Ending** (`IStoryEnding` — kind, turn, messageId, cause; absent while play continues) and its declaring verb `endStory`; `EnginePhase` (`empty`/`ready`/`playing`/`stopped`) as the separate *permission* fact; meta command vs. regular command; turn route (`context.route`); channel packet (ADR-163); autosave envelope.

## References consulted
- `docs/architecture/adrs/adr-347-the-ending-is-an-explicit-concept.md` — **ACCEPTED 2026-09-11, 20/20 READY FOR IMPLEMENTATION**, and the governing decision for phases 2-5. Its D2d pins the three contracts (`IStoryEnding`'s shape, `endStory`'s signature and rejection rule, the channel as a state-mode sibling), so no phase below invents them. Its eight ACs are the phases' exit conditions, mapped per phase. D4b obliges Phase 4 to amend ADR-345 in the same commit as the code.
- `docs/architecture/adrs/adr-345-the-engine-lifecycle-phase-is-an-explicit-concept.md` — D10 says `restart` is engine disposal and the four-state set (`empty→ready→playing⇄stopped`) is closed with no back-edge; **D15** (added 2026-09-11, this session) is the decision this plan implements: it narrows what `stopped` refuses without adding a fifth state or a back-edge, and D10 carries an in-place stamp saying it is *unchanged* by that narrowing — do not cite D10 as the amendment. **AC-10** is D15's acceptance criterion and is what Phase 1's tests must satisfy. D8/D8a establish the pattern this plan follows (`stop()` and `resume()` are phase-tolerant by exception, not by loosening D2's per-method strictness generally) and D8a explicitly forbids removing `resume()`'s tolerance of `playing`, which `bootstrap`/`branch-tester` depend on unconditionally — this plan's Phase 1 must not touch that. AC-2 pins `executeTurn` refusing every non-`playing` phase by name; Phase 1's change narrows that pin and must update the test that encodes it, not delete the guard.
- `docs/context/project-profile.md` — TypeScript strict mode with a shared `tsconfig.base.json`; `engine`'s `turn/` module is the ADR-334/342 reorganization this plan's Phase 1 works inside; all user-facing text lives in `lang-en-us`, engine/stdlib/world-model emit message-ID-only events (binds Phase 3's end-game prompt: no hardcoded English in the browser client's default text, a message ID if new prose is needed); mutation test assertions must verify actual state (`WorldModel`/engine phase), not "didn't throw" — binds every phase's Behavior Statement and test derivation.
- `docs/context/session-20260911-0134-main.md` — Open Items note GH #411 (103 of 117 Dungeo *unit* transcripts carry no `seed:` header, making that suite's failure count non-reproducible run to run) as a live, unrelated defect. This plan's gate is the **walkthrough chain** (`wt-*.transcript`, deterministic, 952 assertions across 17 transcripts), not the affected unit-transcript suite — noted so a failing unit transcript during this work is not mistaken for a regression this plan caused.

## Phases

### Phase 1: Engine — a `stopped` engine accepts a meta command, refuses everything else
- **Tier**: Medium
- **Budget**: 250
- **Domain focus**: `GameEngine.executeTurn` and the turn-stage router (`packages/engine/src/turn/runner.ts`, `stages.ts`, `parse.ts`) — the `EnginePhase` guard ADR-345 D2 put in place, now narrowed by D15 (added this session).
- **Entry state**: ADR-345 **D15** (added this session) states the ruling: a turn whose parse resolves to a meta command is accepted in `stopped`; world-mutating commands stay refused; `stopped` remains terminal and `restart` remains engine disposal. `executeTurn`'s current guard (`game-engine.ts:908-914`) refuses every turn while `stopped`, so `metaCommandStage` (`turn/meta-command.ts`) never runs at end-game.
- **Deliverable**:
  - `executeTurn` no longer refuses outright on `stopped`. It runs the turn far enough to parse and route (`SHARED_STAGES` through `parseStage`, which is the same shared prefix a meta command already runs through while `playing` — `undo-snapshot`'s `MetaCommandRegistry.isNonUndoable` already skips snapshotting for `restart`/`restore`/`quit`/`undo`/`save`, so no new snapshot-while-stopped case is introduced). If `context.route === 'meta'`, the turn continues into `META_STAGES` and the meta command runs exactly as it does while `playing`. If the route is not `'meta'` (a regular command, or a failed parse), the turn is refused at that point with the existing `stopped`-phase message ("The game has ended; call resume() to continue.") — refusal fires before `executeCommandStage`, so no world-mutating stage ever runs against a stopped engine.
  - Verify (do not assume) that `RESTART`, `RESTORE`, `QUIT`, `UNDO`, `SAVE` all route to `META_STAGES` from `stopped` the same way they do from `playing` — confirmed pre-session reading of `MetaCommandRegistry`'s default set (`packages/stdlib/src/actions/meta-registry.ts`) that all four are already registered; this phase re-confirms via a real-path test rather than by re-reading the registry.
  - Update the ADR-345 AC-2 rejection test for `executeTurn` (the one asserting a blanket `stopped`-phase refusal) so it pins the narrowed behavior instead of contradicting it — the AC-2 pin becomes "refuses a *regular* command while `stopped`," not "refuses every command."
  - `docs/architecture/adrs/adr-345-the-engine-lifecycle-phase-is-an-explicit-concept.md` D15 and AC-10 are already written (this session) — this phase's commit lands the code they describe; reconcile wording if the code diverges from what was drafted.
- **Exit state**: A `stopped` `GameEngine`, driven directly (no client), accepts `restart`/`restore`/`quit`/`undo`/`save` and refuses `look`/`take X`/any non-meta input with the existing message. `pnpm --filter '@sharpee/engine' test:ci` green. Dungeo walkthrough chain still 952/952 (an ending transcript reaching `stopped` and then issuing a meta command is new coverage the chain did not previously exercise — confirm whether `endgame-victory.transcript` or a sibling already types a post-ending command, and if none do, that gap is this phase's to close with a real transcript assertion, not a unit-test substitute, per rule 13a).
- **Status**: DONE (2026-09-11, session aceb2e)
- **Outcome**: `executeTurn` refuses only when the phase is not `playing` *and* the input is not something a stopped engine accepts; `acceptsWhileStopped` answers that before any stage runs, and refuses outright while an input mode is active. `resolveRoute` extracted from `parseStage` so the meta/turn rule has one implementation and two callers. `loadSaveData` and `undo()` return a *stopped* engine to `playing` over the existing back-edge. Gates, all green 2026-09-11: `pnpm typecheck` 77/77, `turbo run test:ci` 76/76, Dungeo walkthrough chain 952 passed in 17 transcripts (all goldens matched), `pnpm test:scripts` 14/14, `@sharpee/engine` 796 passed / 7 skipped (+6), `@sharpee/bootstrap` assemble-restart 4/4 (+1). Every new assertion mutation-probed in both directions.
- **`mutation-verification` findings, all three real and all three fixed**:
  1. The first undo-snapshot test proved nothing — it drove `look`, which is in `MetaCommandRegistry`'s non-undoable list (`meta-registry.ts:185-195`), so the stage skips it in every phase regardless of this change. Rewritten against `north`.
  2. An input mode still active at the ending would have consumed the line: `input-mode` runs *before* `parse` and `stop()` never clears the mode, so a line parsing as a meta command reached the handler — an unconstrained world write — before the route was consulted. `acceptsWhileStopped` now refuses while a mode is active, which also removes a chain-splitting divergence against `chainStage`.
  3. **RESTORE at the end-game prompt reproduced GH #414 one level deeper.** `loadSaveData` replaced the world but never touched the phase, so a restore loaded a live save into an engine that still refused every command — worse than the pre-change behavior, which refused `restore` outright. Fixed at `loadSaveData` and `undo()`, conditional on `stopped`: D8a's tolerance covers `playing` but not `ready`, and hosts do restore into an engine they never started (15 tests in the package do).
- **Owed**: the resume-on-restore/undo rule is a lifecycle behavior ADR-345 does not yet state. It uses the existing `stopped → playing` back-edge rather than adding one, so D10/D11 are unaffected — but it wants a D15 amendment line if David rules it in.
- **Corrections to this phase as planned**: (1) The ADR-345 AC-2 test needed **no** change — `engine-resume.test.ts:24` pins `executeTurn('look')`, a *regular* command, which is still refused; the plan assumed it pinned every command. (2) The planned "run `SHARED_STAGES` through `parseStage`, then refuse at the route switch" shape was **rejected on evidence**: `undo-snapshot` writes a snapshot, `turn-start` emits `turn:start`, and `input-mode` can advance the turn — all before the route is known. Refusing there would let a dead world overwrite the player's undo state. The refusal is therefore asked before any stage, at the cost of one extra parse on the accepted path.

### Phase 2: The concept — `IStoryEnding`, the world member, and the declaring verb
- **Tier**: Medium
- **Budget**: 250
- **Governing decision**: ADR-347 D1, D2, D2a, D2c, D2d. **Read the ADR before starting**; its D2d pins all three contracts, so none of them is this phase's to invent.
- **Domain focus**: `packages/if-domain/src/endings.ts` (the record type), `packages/world-model` (the member, its accessor, its serialization), `packages/stdlib` (the verb).
- **Entry state**: the ending exists as four unowned vocabularies — the world flag `STORY_ENDING_FLAG`, `story.victory`/`story.defeat`, `game.won`/`game.lost`, and `Story.isComplete()`. Nothing owns it.
- **Deliverable**: `IStoryEnding` beside the existing types in `endings.ts`, which stays types-only. A first-class `WorldModel` member with its accessor and serialization wiring, on the `getPlayer()`/`setPlayer()` precedent. `endStory(world, kind, opts?)` in stdlib beside `killPlayer`, setting the Ending and returning the blessed event, with **first-ending-wins** as its rejection rule (`killPlayer`'s idempotence precedent, `engine/src/turn/detect-death.ts:26-28`). No consumer is switched over in this phase.
- **Exit state**: AC-1, AC-3 and AC-4 satisfied. `pnpm typecheck` 77/77; the walkthrough chain unmoved at 952, because nothing reads the new member yet.
- **Status**: DONE (2026-09-11, session c3f995)
- **Outcome**: `IStoryEnding` added to `if-domain/src/endings.ts` (still types-only). `WorldModel` carries the Ending as a first-class member — `getEnding()`/`setEnding()` on `IWorldModel` and the class, dropped by `clear()`, written by `WorldSerializer.serialize` and restored by `deserialize`, synced back in `loadJSON` — all on the `playerId` precedent, field for field. `endStory(world, kind, opts?)` in `packages/stdlib/src/endings/`, beside `death/`, with first-ending-wins. No consumer switched over, as planned. Gates, all green 2026-09-11: `pnpm typecheck` 77/77; `turbo run test:ci` 76/76; Dungeo walkthrough chain 952 passed in 17 transcripts, all goldens matched (after `./repokit build dungeo`); `pnpm test:scripts` 14/14; 10 new tests (6 `world-model/tests/unit/world/story-ending.test.ts`, 4 `stdlib/tests/endings/end-story.test.ts`). Five mutation probes — first-wins guard, `loadJSON` sync-back, serializer write, `clear()` drop, `setEnding` kind guard — each removed in turn and each failed its suite.
- **Correction to this phase's exit state as planned**: **AC-1 and AC-3 are not this phase's to satisfy and were not claimed.** AC-1 requires `STORY_ENDING_FLAG` to appear only in world-model's accessor and if-domain's declaration, and AC-3 requires Chord and TypeScript to produce identical Endings — both need `loader.ts`'s `triggerEnding` routed through `endStory`, which is Phase 3's stated deliverable. The planned exit state contradicted this phase's own "No consumer is switched over in this phase." **AC-4 (first ending wins) is satisfied and tested here**; AC-1 and AC-3 move to Phase 3.
- **`mutation-verification` findings: no defects; one boundary flag and one contract question.**
  1. `loader.ts:1512,1530` still reads and writes `STORY_ENDING_FLAG` and nothing calls `endStory` — correct for a first slice, but until Phase 3 lands this is new plumbing with no live caller, so AC-1 must not be closed against it and Phase 3 must *remove* the flag writes rather than leave both paths live.
  2. The `turn?` deviation below, flagged as needing David's confirmation or an ADR amendment.
- **Contract deviation from ADR-347 D2d, deliberate and owed an amendment**: D2d pins `readonly turn: number` as required on `IStoryEnding`. Shipped as `readonly turn?: number`, because there is no source for it at a general declaring site — `WorldModel` carries no turn counter, `ActionContext` exposes no turn (`enhanced-types.ts` has none), and the only existing turn reader in this area is Chord's own `core.turnNow`/`turnProvider`. Requiring it would either force every TypeScript author to find a turn they do not have — the exact authoring cost D2b named and D2c exists to answer — or default it to `0`, which would make the field a lie at precisely the sites that omit it. Omission says "the declaring site did not know the turn"; `0` would say "turn zero." Chord supplies a real turn in Phase 3. **Needs a D2d amendment line or David's ruling before Phase 5 consumes the field.**

### Phase 3: The engine reads it, and the poll goes
- **Tier**: Medium
- **Budget**: 250
- **Governing decision**: ADR-347 D2b.
- **Domain focus**: `packages/engine/src/turn/ending.ts`, `packages/engine/src/install/story.ts`, `packages/story-loader/src/loader.ts`, `stories/dungeo/src/index.ts`.
- **Entry state**: Phase 2 merged. `endingStage` recomputes the ending every turn from three sources; `isComplete?()` is still on the `Story` interface.
- **Deliverable**: `endingStage` reads the Ending. `isComplete?()` drops off the `Story` interface (`install/story.ts:323`). Chord's `triggerEnding` routes through `endStory` rather than carrying its own implementation, exactly as `kill` routes through `killPlayer`. Dungeo's hardcoded `isComplete(): return false` (`index.ts:818-821`) and the `completion-test-story` fixture are updated — the only two non-Chord implementations in the tree, measured.
- **Exit state**: AC-2 satisfied. Walkthrough chain still 952 across 17 transcripts, all goldens matched — the gate that proves the rewrite changed no rendered output.
- **Status**: DONE (2026-09-11, session c3f995)
- **Outcome**: `endingStage` asks the world one question — does it carry an Ending — and stops with that kind. The three-source recomputation is gone: the `story.victory` scan in `emit-events` and `TurnStageContext.victory` are both deleted, and `isComplete?()` is off the `Story` interface. The one ending the stage still declares is a death `detect-death` found and story policy did not revive, via `endStory(world,'defeat',{turn,cause})` — so the engine stops because the world records a conclusion rather than because it recomputed one. `triggerEnding` holds no implementation of its own any more: it delegates to `endStory` and returns `ISemanticEvent | undefined`, and the `win`/`lose` statement pushes nothing when the story had already ended. `scheduler-constructs.storyOver()` reads `world.getEnding()`. Dungeo's hardcoded `isComplete(): return false` and the `completion-test-story` fixture's poll are both gone — the fixture now declares through `endStory` like a real story.
- **AC-1, AC-3 and AC-2 all satisfied here** (AC-1 and AC-3 moved from Phase 2, which could not satisfy them without switching a consumer over):
  - **AC-1**: `grep -rn "STORY_ENDING_FLAG" packages --include="*.ts"` returns two hits, both in `if-domain/src/endings.ts` — the declaration and its own header line. Zero consumers. The constant is kept (the file's values are frozen contract) with its doc rewritten to say it is superseded and unread; **it is a candidate for deletion, which is David's call, not this phase's.**
  - **AC-2**: `isComplete` appears nowhere in `packages/engine/src`, `packages/story-loader/src` or `stories/dungeo/src` outside Dungeo's unrelated `trivia.isComplete` state key. A real-path test drives a story to victory through `executeTurn` with no `isComplete` anywhere and asserts both the world's Ending and the stopped phase; a second test proves the poll is gone by giving a story an `isComplete(): true` it keeps to itself and asserting the engine stays `playing`.
  - **AC-3**: a test runs the cloak fixture's Chord `win` and a direct `endStory` on a second world and asserts the Endings are `toEqual` and the event types identical.
- **Gates, all green 2026-09-11**: `pnpm typecheck` 77/77; `turbo run test:ci` 76/76; Dungeo walkthrough chain 952 passed in 17 transcripts, all goldens matched; cloak-of-darkness 90 passed in 9 transcripts (the Chord story that actually uses `win`/`lose`); `pnpm test:scripts` 14/14. Four mutation probes — the death declaration, the Ending read, a restored `isComplete` poll, and a `triggerEnding` that re-implements the ending — each applied in turn and each failed its suite.
- **Two behaviors this phase changed on purpose, neither of which moves a golden**:
  1. **A Chord `lose` now actually ends the story as a defeat.** It used to set the flag, which `isComplete()` read, which stopped the engine as a **victory** — `emit-events` only ever watched for `story.victory`. No in-tree golden covered it.
  2. **A hand-emitted `story.victory` event no longer stops the engine on its own.** `endStory` is the declaring route and emits that event itself, so every real emitter now also records the Ending; accepting the bare event as a second declaration would be the fifth naming this ADR exists to remove. The one test that encoded the old behavior is rewritten to declare.
- **Noted, not fixed**: a headless Chord `win` records `turn: 0`, because `core.turnNow()` falls back to `lastTickTurn` when no engine turn provider is wired. That is `turnNow`'s existing semantics, shared with timers, not something this phase introduced.

### Phase 4: The restore seam, and Phase 1's patch retired
- **Tier**: Medium
- **Budget**: 200
- **Governing decision**: ADR-347 D3, D4, D4b; amends ADR-345.
- **Entry state**: Phase 3 merged. `loadSaveData` and `undo()` still carry Phase 1's `if (this.phase.name === 'stopped') this.resume()`.
- **Deliverable**: the phase derives from the Ending at the restore seam — a save carrying an Ending restores stopped, one without it restores to play. Phase 1's patch is **deleted**, not joined. `stopped` keeps its meaning and stays overloaded (D4). **ADR-345 is amended in this phase's commit** (D4b): D15's text stops describing the retired patch as a standing rule, and D9 gains a pointer to ADR-347.
- **Exit state**: AC-5, AC-7 and AC-8 satisfied — including the grep that proves the patch is gone rather than merely bypassed. ADR-345's own AC-4 and AC-7 still pass unmodified.
- **Status**: DONE (2026-09-11, session c3f995)
- **Outcome**: a private `GameEngine.derivePhaseFromEnding()` replaces the Phase 1 patch at both call sites (`loadSaveData`, `undo()`). It reads `world.getEnding()` and moves the phase to agree with the world the engine now holds: `playing → stopped` via `stop(ending.kind)` when the restored save carries an Ending, `stopped → playing` via `resume()` when it does not, and nothing at all in `empty` or `ready`. Both edges are ADR-345's existing ones (D10, D11) — `stop()`'s own forward edge and `resume()`'s back-edge — so no state and no transition is added. D5 is respected: this is the restore seam and nowhere else, which leaves the transcript-tester RETRY path (a deliberately revived dead player in a `playing` engine) exactly as ADR-345 D8a has it.
- **Beyond the two assertions AC-5 names**: the `playing → stopped` direction was implemented too, though AC-5 asks only about a stopped engine. Without it, GH #414 defect 2's reload path still lands a fresh `playing` engine on a finished world and the next turn re-discovers the ending — the disagreement, one seam later. D3's own sentence ("a restore of a save carrying one does not [play]") reads on both phases.
- **AC-5** — three assertions, all met: a save carrying an Ending restored into a stopped engine leaves it stopped (and still refuses `look`); a save with no Ending returns a stopped engine to play; and `grep -c "stopped') this.resume()" packages/engine/src/game-engine.ts` returns **0** — the patch is deleted, not bypassed. Two further cases pin the edges the AC does not name: a restore into a never-started engine leaves it `ready`, and a live save into an already-playing engine changes nothing (which is what keeps this a derivation rather than a flip).
- **AC-7** — `stop('restart')` and `stop('victory')` still produce the same phase, and neither records an Ending: `stopped` stays the permission fact, the Ending stays the narrative one (D4). ADR-345's own AC-4 (`stop()` tolerance) and AC-7 (closed state set) suites pass unmodified.
- **AC-8** — ADR-345 amended in this phase's work: a header note, a pointer on **D9** to ADR-347 D2 (the question D9 assumed rather than asked — who owns the *ending*, as against where the stop *reason* lives), and a note on **D15** recording that the resume patch is **retired** by ADR-347 D3 and that no standing rule about the restore seam should be read into D15. **Correction to D4b's premise**: D4b anticipated that ADR-345's D15 "describes the retired patch as a standing rule" — it never did. Phase 1 recorded the rule as *owed* an amendment line and David never ruled it in, so `grep -n "loadSaveData\|resume patch" adr-345…` returned nothing before this amendment. The line D15 now carries is the answer to that owed question — "it does not stand" — rather than the removal of text that was there.
- **Gates, all green 2026-09-11**: `pnpm typecheck` 77/77; `turbo run test:ci` 76/76; Dungeo walkthrough chain 952 passed in 17 transcripts, all goldens matched; cloak-of-darkness 90 passed in 9 transcripts; `pnpm test:scripts` 14/14; `engine-lifecycle-phase.test.ts` 19 passed (+5); `platform-dispatcher.test.ts` 12 passed (+1). Three mutation probes — each direction of the derivation removed in turn, and the unconditional Phase 1 patch restored — each failed the suite.
- **`mutation-verification` findings, both acted on**:
  1. **The `playing → stopped` direction was an untested seam.** `stop()` does not know it was called from a restore, so its full cascade (`game.ending`, `game.won`/`game.lost`, `game.ended`, `game:over`) fires. Tracing every caller showed no live path renders it twice or moves a golden — `META_STAGES` carries no `channelPacketStage`, and `meta-render` renders from `context.events`, which the cascade never reaches — but nothing pinned that, so it held by wiring accident rather than by contract. **Closed**: a new case in `platform-dispatcher.test.ts` drives a genuine `RESTORE_REQUESTED` of an ended save into a playing engine and asserts the phase followed the world *and* that the restore's own delivered response is `platform.restore_completed` alone, with the cascade visible only on the engine's event stream.
  2. **`TestingExtension.restoreCheckpoint` bypasses this seam entirely** (`extensions/testing/src/extension.ts:433` → `checkpoints/serializer.ts:103`, `world.loadJSON` with no engine in hand), so a checkpoint restore can leave the phase and the world disagreeing in both directions. Pre-existing, outside ADR-347's Scope, verified against the source and **filed as GH #416** on the same logic D4a used for the two dead string matches.

### Phase 5: The client — GH #414 defects 2 and 3
- **Tier**: Medium
- **Budget**: 250
- **Governing decision**: ADR-347 D3a, D2d's channel contract.
- **Domain focus**: `packages/stdlib/src/channels/standard.ts` (the new state-mode sibling), `packages/platform-browser/src/BrowserClient.ts` and `src/channels/`.
- **Entry state**: Phase 4 merged. The browser receives the ending as **prose** on the `endgame` channel (`standard.ts:504`, `contentType: 'text'`, rendered at `platform-browser/src/channels/index.ts:206`) and can print it but cannot act on it.
- **Deliverable**: a state-mode channel sibling carrying `IStoryEnding` — `endgame` keeps its text payload untouched, because widening it would delete rendered output and move the goldens (D2d). The browser drives the input box's disabled state and an end-game prompt from that record, never from prose, and restores the normal state when the Ending clears. Any new prose is a `lang-en-us` message id, not a string in `platform-browser`.
- **The autosave fix is NOT what Phase 2 originally planned, and this is the clearest place the concept changed the answer.** The old plan gated the autosave so an ended world was never persisted, restoring the player to their last live turn. With the Ending explicit, the honest fix is the opposite: **persist the ended world, restore it faithfully, and present it correctly** — the reload lands at the end-game prompt, where Phase 1 already made `restart` and `restore` work. Suppressing the save to dodge a loop was a workaround for not being able to represent the state; the state is now representable.
- **Exit state**: AC-6 satisfied, its two premise checks first (the sibling fires; the client disables with prose mocked empty), then the chain at 952. A real-path test per rule 13a — a genuine ending driven through `executeTurn`, asserting the persisted envelope and the DOM, not mocks.
- **Status**: DONE (2026-09-11, session c3f995)
- **Outcome**: `storyEndingChannel` (`story-ending`, `replace`/`json`/`sparse`) reads `world.getEnding()` and carries the record itself. `endgame` is untouched — still `text`/`event`/`sparse`, still rendering its notification, so no golden moved. The channel's third return shape is the one that matters at an end-game prompt: `null`, the clear signal, on the turn a reported Ending goes away, so a client re-enables what it disabled when an UNDO or RESTORE lands it back in a live turn. The browser's renderer (`platform-browser/src/channels/story-ending.ts`) drives `input.disabled` and a `data-story-ended` attribute from that record and nothing else; an unrecognised payload leaves the input alone rather than locking a player out of a story that overrode the channel.
- **The end-game prompt is derived, not stored, and not written in the client.** `appendPromptBlock` uses a new `EndGamePrompt` (`if.platform.prompt.ended`, registered in `lang-en-us`) whenever the world carries an Ending, so the prompt arrives on the existing `prompt` channel as resolved text and every host gets it at once — the same argument ADR-345 D15 used for putting the meta-command rule in the turn cycle rather than in each client. An UNDO back to a live turn restores the story's own prompt with no second rule to remember.
- **A stage moved, and it had to.** `detectDeathStage` ran *after* `render-prose` and `channel-packet`, so a death — the most common ending in IF — declared its Ending after the turn had already rendered and shipped its packet: the client would have been told about `win`/`lose` and never about dying. The declaration moved out of `endingStage` into `detectDeathStage`, and that stage moved ahead of the render. Its `requires` are unchanged and still satisfied; every site that can veto a death (event handlers in the executor, the plugin tick) still runs before it, so the live-state re-check is as final as it was. `endingStage` now declares nothing at all — it reads and stops. `turn-stage-order.test.ts`'s pinned list is updated, and its reordered-copy case now reports two violations because `detect-death` joined the set that depends on `platform-operations`.
- **GH #414 defect 2 is fixed by *not* suppressing the autosave**, which is the clearest place the concept changed the answer. The autosave is untouched: it still persists the ended world. What changed is that the world can now say it ended — so Phase 4's restore seam stops the rebooted engine, `engineApplySave` syncs the input's disabled state from the world directly (a restore runs between turns, so no packet is produced to carry it), and the reload lands at an end-game prompt where `restart` and `restore` work. The old plan's gate-the-save fix was a workaround for a state that could not be represented.
- **AC-6**, premise checks first, in order:
  1. The channel fires — `storyEndingChannel.produce` returns the record from a world carrying an Ending, stays `undefined` while none exists, and returns `null` when a reported one goes away (6 cases, `stdlib/tests/channels/standard.test.ts`).
  2. The client disables from the record with prose mocked empty — the renderer is driven with **no prose at all**, so a disabled box disabled from state (4 cases, `platform-browser/tests/story-ending-input.test.ts`).
  3. Then the chain: **952 passed across 17 transcripts, all goldens matched.**
- **Real-path test per rule 13a**: `engine/tests/integration/story-ending-channel.test.ts` drives a real `GameEngine` through `executeTurn` and reads the actual `channel:packet` — the ending turn carries the record and the end-game prompt; a *death* turn carries both too (the case the stage move exists for); an ordinary turn carries nothing; and a real `restore` typed at the end-game prompt clears it to `null` and restores the ordinary prompt.
- **Gates 2026-09-11**: `pnpm typecheck` 77/77; `turbo run test:ci` 76/76; Dungeo walkthrough chain 952 passed in 17 transcripts, all goldens matched; cloak-of-darkness 90 passed in 9 transcripts; `pnpm test:scripts` 14/14. Dungeo *unit* transcripts: 1721 passed, 13 failed, 10 expected failures — within the 13/20/18 band GH #411 records for that unseeded suite on an unchanged build, and every failure is a dam/mailbox/troll-combat case with nothing ending-related among them. Five mutation probes — the channel unregistered, the clear signal replaced by silence, the end-game prompt derivation removed, `detect-death` moved back after `channel-packet`, and the renderer's disable removed — each failed its suite. `@sharpee/platform-browser` 153 passed in 16 files (+5).
- **`mutation-verification` findings — one real client defect, fixed; two reported for David**:
  1. **RESTART and RESET left the input disabled.** Both reboot through `connectEngine`, which reuses this client, this DOM and this very input element, while the fresh engine's `ChannelService` starts with an empty `prevValues` map — so `story-ending` returns `undefined` (quiet) rather than its `null` clear signal, and a player who restarted from an end-game prompt landed in a brand-new game unable to type. **Fixed**: `connectEngine` now calls `syncEndingFromWorld()` on both paths, first connect and reboot alike, because the world is the fact in either case. Pinned by a new `restart-reboot.test.ts` case that connects an ended world, asserts the box is disabled, reboots onto a live one, and asserts it is typable again. Two hand-written partial world stubs (`turn-events.test.ts`, `shadow-renderer.test.ts`) gained `getEnding` — the client reads it on every connect now, so the stubs owe it an answer.
  2. **A death's Ending was declared without emitting its `story.defeat` event** — `detect-death.ts` discarded `endStory`'s return value, so a defeat-by-death recorded the Ending but never put the blessed event on the stream, where a Chord `lose` or a direct `endStory` call does. **Ruled and fixed** (David, 2026-09-11: "obviously we need all outcomes reported"): a new `routeEndingEvent` delivers the event three ways — the event source, the turn's stored events, the engine's emitter — and refreshes `context.result.events`, the same shape `platform-operations` uses for its completion events. An ending is an outcome, and the one ending the engine declares for itself must not be the one that records itself silently. The event renders nothing (no `message`, `text` or `messageId`, and no `story.defeat` template in `lang-en-us` — ADR-345 D14's trace), and no channel reads it, so nothing rendered moved: chain still 952 with all goldens matched. Two probes — the routing removed, and the turn-store write removed — each failed the suite.
  3. **The `IStoryEnding.turn` deviation from D2d is still open** (see Phase 2). Both current call sites do supply a turn, so nothing is broken; the pinned contract and the shipped type still disagree.
- **Known rough edge, not fixed**: on a browser *reload* into an ended save, the input is disabled but the prompt label still shows whatever the restored page had, because no turn runs and so no `prompt` channel value is produced. The player sees the restored transcript ending with the ending text and a dead input, which is coherent; naming the end-game verbs there would need a way to ask the engine to re-emit its channels between turns, which is a platform API this phase did not invent.

## Advisory finding from `plan-review`
**[TENSION] Phase 1's refusal is conditional-mid-pipeline, not up-front.** ADR-345 D2's general sentence is "one guard per method, so there is no order," and Phase 1 moves `executeTurn`'s refusal to *after* parse/route resolution. D15 is the ADR's own carve-out for exactly this, on the same footing as D8/D8a's named exceptions, so it is not a contradiction — but keep the refusal's shape visibly tied to D15 with a code comment at the guard, the way D8/D8a's exceptions are commented in `game-engine.ts`, so a future reader does not mistake it for D2 eroding generally.

## Workarounds this plan leaves alone
`transcript-tester` (`packages/bootstrap/src/index.ts:377-379`) and `branch-tester` (`packages/branch-tester/src/tree-walker.ts:332,360`) call `resume()` by hand after a `stopped` engine, tolerated per ADR-345 D8a specifically because `branch-tester` calls it unconditionally on every tree line. Neither workaround is retired by this plan: Phase 1 makes `stopped` accept meta commands, it does not change what `resume()` itself accepts or make `resume()` unnecessary for a harness driving a tree that dies mid-branch and keeps going. D8a's tolerance stays exactly as ruled.

## Not in this plan
- No change to `stdlib`'s `MetaCommandRegistry` — verified during Phase 1 investigation that `restart`/`restore`/`quit`/`undo`/`save` are already registered defaults; if that verification surfaces a gap, it is a Phase 1 finding to report, not a silently-added fourth phase.
- No `lang-en-us` phase of its own — if Phase 2's end-game prompt needs new prose, it is a small addition inside Phase 2's budget, not a separate session-sized unit.
