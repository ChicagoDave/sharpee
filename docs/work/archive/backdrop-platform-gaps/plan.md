# Session Plan: Fix the five platform gaps filed from the Secret Letter market-backdrops increment (GH #339-#343)

**Created**: 2026-08-30
**Plan Status**: DONE (2026-08-30, session 262648 — all five phases complete; Secret Letter tree 517 cards / 887 assertions passing at seed 1209)
**Overall scope**: Five `packages/` bug fixes surfaced 2026-08-30 while authoring `branch-stories/secret-letter/backdrops.chord`: region-membership testing (#339), a dispatch-precedence collision on colliding actor/target clauses (#340), a dead-on-arrival entering clause on non-room owners (#341), a missing proper-name marker for entities (#342), and a number-agreement bug in two sensory default messages (#343). All five are platform changes — CLAUDE.md requires discussion before implementation in `packages/`, so this plan is the discussion artifact: no phase starts until David reviews and approves it.
**Bounded contexts touched**: N/A — infrastructure/platform bug fixes (chord grammar/analyzer, story-loader dispatch, stdlib actions, lang-en-us message templates), not new domain modeling.
**Key domain language**: N/A (platform-internal fixes; the one story-facing concept is region membership, already named by ADR-236 as `containing`/`RoomTrait.regionId`).

## References consulted
- `docs/architecture/adrs/adr-236-chord-regions.md` — Consequences pin `containing` (region lists rooms) as the authoring shape; region membership lives on `RoomTrait.regionId`, not on world-model containment — the exact distinction #339's fix must respect, since `isWithin` currently walks containment.
- `docs/architecture/adrs/adr-325-chord-presence-and-duration.md` — D1 defines `is in <place>`/`location` semantics (always resolves to the containing room); D5 defines `landing` as the region's *put-destination* declaration, separate from membership — confirms #339's second finding (the analyzer's landing-gate conflates "is a place" with "has a landing") is a real conflation, not a misreading.
- `docs/architecture/adrs/adr-327-explicit-references.md` — Consequences: every clause line is meant to survive being read alone, with no two-actor ambiguity between an owner's own clause and another entity's clause about it. #340's bare-head-vs-target collision is exactly the ambiguity class this ADR closed for the *player-moves* case; #340 asks whether the same discipline extends to sensory bare-heads.
- `docs/architecture/adrs/adr-242-chord-proper-persons.md` — Consequences: `proper` currently renders persons bare and is loader-set only for the player (`loader.ts:1833`); #342 is the extension of the same convention to non-person entities, not a new naming concept.
- `docs/architecture/adrs/adr-228-interceptor-lifecycle-engine.md` — Consequences: standard actions are meant to run their interceptor path before hand-rolled lifecycle logic, with hand-rolling flagged as a review smell. #341 already has this path (`entering.ts` calls `runPreValidate` before its enterable gate); the fix is routing Chord clauses into it, not building a new mechanism.
- `docs/context/project-profile.md` — Active work note (as of 2026-08-30): "several open platform gaps were filed from Secret Letter session work this week (GH #335-#338) — grammar/binder limitations discovered while porting story content, not platform bugs introduced by story work"; #339-#343 continue that same pattern one session later. Two-harness split (`branch-stories/` uses the tree-document tester, not v1 transcripts) governs this plan's test phases.
- `docs/context/session-20260830-0527-feat-adr-321-world-index.md` (most recent session) — Open Items names all five issues verbatim as the source of this plan, with #341 additionally noted to deaden the market gates' committed hunted-entering refusal (`grubbers-market.chord:121`), and records what backdrops.chord routed around rather than shipped (six `enter <backdrop>` answers, the collapsed region conditions) — this plan's closing phase is that routed-around work coming back.

## Phases

### Phase 1: Region membership — `is in <region>` tests membership, not containment (#339)
- **Tier**: Small
- **Budget**: 100
- **Domain focus**: N/A — infrastructure. Touches `packages/chord` (analyzer's region-as-place gate) and `packages/story-loader` (`evaluator.ts` `case 'is-in'` / `isWithin`).
- **Entry state**: David has approved this plan. #339's probe evidence stands (seed 1209, 2026-08-30): `is in <region>` always false, including inside member rooms.
- **Deliverable**: `evaluator.ts`'s `is-in` case resolves region membership via `RoomTrait.regionId` (walking nested regions if the subject's room belongs to a sub-region), falling back to the existing containment walk for non-region places. A decision, recorded in this phase, on whether a membership test requires the `landing` line at all (ADR-325 D5 scopes `landing` to put-destinations, not membership) — and the analyzer's `analysis.region-not-a-place` gate updated to match that decision. `packages/story-loader` unit tests covering: player in a member room, player in a nested sub-region's room, player outside the region, and (if the landing-decoupling decision drops the requirement) a region with no `landing` line still testable with `is in`.
- **Exit state**: `is in <region>` resolves true for every member room and false elsewhere, verified by unit test; the landing/membership decoupling decision is on record either way; #339 fixed and ready to close.
- **Status**: DONE (2026-08-30, session 262648)
- **Decision recorded**: a membership test needs NO landing — `resolvePlace` takes a purpose (`destination` | `membership`); the `analysis.region-not-a-place` gate fires for destinations only, and `is in <region>'s location` STAYS gated in both modes (a region's location IS its landing, ADR-325 D5). Evaluator: a REGION-valued place routes through `world.isInRegion` (transitive, non-room subjects via containing room); non-region places keep the containment walk.
- **Progress 2026-08-30**: `packages/chord/src/analyzer.ts` (resolvePlace purpose flag; is-in lowers in membership mode), `packages/story-loader/src/evaluator.ts` (is-in region branch). Tests: `landing.test.ts` +3 cases (12 passing); new `packages/story-loader/tests/region-is-in.test.ts` (5 passing — direct member, nested, outside, container containment untouched, landing-less compile).

### Phase 2: Entering clauses on non-room owners reach the interceptor, not the room-arrival event (#341)
- **Tier**: Medium
- **Budget**: 250
- **Domain focus**: N/A — infrastructure. Touches `packages/story-loader` (`runtime.ts` `eventTriggerFor` ~line 694), `packages/stdlib` (`entering.ts`'s existing `runPreValidate` interceptor path at line 106), and possibly `packages/chord`'s analyzer if the refuse-at-load branch is chosen.
- **Entry state**: Phase 1 done or independently startable (no dependency between them — flag as parallelizable if David wants both fixed in the same session). #341's probe evidence stands: `on the player entering` on a thing loads clean, silently binds to room-arrival, and never fires; the market gates' hunted-entering refusal (`grubbers-market.chord:121`) is confirmed dead by the same mechanism.
- **Deliverable**: A routing decision recorded first — either (a) route a thing-owner's `entering` clause to the `entering` action's interceptor (the natural reading: "when the player enters THIS THING"), matching the pattern ADR-228 already established for the interceptor-before-lifecycle order, or (b) refuse a non-room, non-region `entering` clause at load, the same way `leaving` is already refused on a non-region owner. Whichever is chosen: `runtime.ts` `eventTriggerFor` stops silently mis-binding thing-owned `entering` clauses, and (if (a)) the interceptor path is wired to receive and fire them. `packages/story-loader` and `packages/stdlib` tests covering: a thing-owner's `entering` clause fires (or the load-time refusal fires, under (b)) instead of falling through to the stdlib default.
- **Exit state**: `enter <thing-with-entering-clause>` fires the clause (or refuses at load, per the chosen branch) instead of stdlib's generic "You can't enter the …"; #341 fixed and ready to close.
- **Status**: DONE (2026-08-30, session 262648)
- **Decision recorded**: branch (a), route to the interceptor — a THING's `entering` clause means "someone enters this thing" and rides the entering action's consultation like any gerund; rooms keep the arrival-event binding, regions the crossing events, and the `leaving` load-error now keys on "region-only verb" (`REGION_EVENT_TRIGGERS` minus `EVENT_TRIGGERS`) so it no longer swallows `entering`. The ebnf's event-verbs comment updated to the new contract.
- **Progress 2026-08-30**: `packages/story-loader/src/runtime.ts` (`eventTriggerFor` owner-aware; the region-verb guard exempts `entering`), `packages/chord/chord.ebnf` (comment). New REAL-PATH test `packages/story-loader/tests/entering-on-things.test.ts` (3 passing — a scenery refusal fires ahead of the enterable gate with the player unmoved; the `while` gate holds and calm falls to the stdlib default; an enterable's `after` phrase narrates on success), driven through `CommandExecutor.executeAsActor` per the adr-327-ac2 harness. Note learned there: a blocked turn returns `success: true` (success means the turn ran); refusals read from `if.event.entered { blocked, reason }`.

### Phase 3: Bare-head actor clause vs. target's own clause — dispatch precedence for colliding overrides (#340)
- **Tier**: Medium
- **Budget**: 250
- **Domain focus**: N/A — infrastructure. Touches `packages/story-loader` (`runtime.ts` `buildDispatchingInterceptor`) and `packages/stdlib` (`lifecycle-engine.ts` actor consultation, ~lines 72-90).
- **Entry state**: Independent of Phases 1 and 2 — can run in any order. #340's probe evidence stands: an actor bare-head (`on smelling`) colliding with a target's own clause (`on the player smelling`) yields "I don't understand that." instead of either arm firing.
- **Deliverable**: A precedence decision recorded first — the issue's own read is "target first seems right" (the target's own clause about itself outranks the actor's blanket bare-head), but this phase records David's actual ruling rather than assuming it. The dispatching interceptor and lifecycle-engine's actor consultation changed so a collision between the two override sources resolves to the chosen arm, never to the parser-failure fallback. `packages/story-loader` and `packages/stdlib` tests covering: bare-head actor clause alone (unchanged behavior), target clause alone (unchanged behavior), and the collision case resolving to the chosen precedence — plus, if resolvable in-scope, the same collision shape for a verb other than smelling (listening was the sibling ADR-327 D1 bare-head case) to confirm the fix isn't smell-specific.
- **Exit state**: The collision case resolves to one arm per the recorded precedence, never to "I don't understand that."; #340 fixed and ready to close.
- **Status**: DONE (2026-08-30, session 262648)
- **Decision recorded**: FIRST override in consultation order wins; a later consultation's override is dropped while its other effects (emits) still apply. Slots consult before the actor, so this is target-first by construction — the target's own clause outranks an actor bare-head — and it is the same first-wins rule the story-loader's per-owner clause merge already uses. The former hard error (which the executor surfaced as `command.failed` → "I don't understand that.") is retired; the `InterceptorReportResult` contract comment updated (its ADR-106 mirror punished two individually legal declarations). Plan review's ADR-327 TENSION stands as advisory: D1's bare-head exception is what produces the collision class; no ADR amendment made.
- **Progress 2026-08-30**: `packages/stdlib/src/actions/lifecycle/lifecycle-engine.ts` (`runPostReport` first-wins arbitration), `packages/world-model/src/capabilities/action-interceptor.ts` (contract comment). Tests: the stdlib hard-error case rewritten to first-wins + losing-emit-applies (`lifecycle-engine.test.ts`, 24 passing); new REAL-PATH `packages/story-loader/tests/actor-target-collision.test.ts` (2 passing — the loaf's clause wins over Jack's bare head; the bare head still answers an unclaused target). The spike that captured the root cause (the runPostReport throw wrapped as command.failed) is deleted.

### Phase 4: Proper-name marker for entities (#342) + lang-en-us sensory number agreement (#343)
- **Tier**: Small
- **Budget**: 100
- **Domain focus**: N/A — infrastructure. #342 touches `packages/chord` (`chord.ebnf`/analyzer, new surface) and `packages/story-loader` (`loader.ts`, extending the player-only `proper` handling at line 1833 to ordinary entities) plus the world-model identity trait's proper-noun flag. #343 touches `packages/lang-en-us` only — unrelated package, folded in here only because it is the smallest of the five and this is the lightest session.
- **Entry state**: Independent of Phases 1-3. #342's language-surface decision is **RULED (David, 2026-08-30, session 262648): the explicit `proper` kind word**, extending ADR-242 D2's person spelling to ordinary entities. The plan-review CONTRADICTION is resolved the review's way: the "no article = proper" inference option is dropped — ADR-242 D4 forecloses create-line inference deliberately, for every kind (the option as originally listed here also misread the person mechanism, which was never inference; GH #342 corrected in a comment). #343's probe evidence stands: `listen to shoppers` (a `plural` entity) renders "The shoppers isn't making any sound."; the issue also flags the sibling `smell` default as worth checking for the same class of error.
- **Deliverable**: (#342) Once David's syntax choice is recorded: the loader sets the identity trait's proper-noun flag from the chosen Chord surface, `chord.ebnf`/analyzer updated if a new kind word was chosen, and `packages/story-loader` tests confirming a proper-marked ordinary entity (e.g. "Grubber's Market") renders bare in a stdlib default message. (#343) A sweep of `packages/lang-en-us`'s sensory/default message templates (listening, smelling, and any sibling using the same conjugation pattern) for singular/plural agreement, fixed for every template found — not just the two named in the issue — with `packages/lang-en-us` tests covering a `plural` entity against each fixed template.
- **Exit state**: #342's decision is recorded and (if made) implemented and tested; #343's number-agreement sweep is complete and tested; both ready to close.
- **Status**: DONE (2026-08-30, session 262648)
- **Progress 2026-08-30 — #342**: the explicit `proper` kind word extended to every create block per David's ruling — `analysis.proper-person-only` retired (unconditional gate kept), the loader's trait-adjective pass applies the person shape (`properName: true, article: ''`) to any kind. Tests: `person-identity.test.ts` (chord) flipped to the extended contract (16 passing); new `packages/story-loader/tests/proper-things.test.ts` (2 passing). ADR-257 D5 pin re-recorded under the standing 3.5.0 (comment-only ebnf motion; landing history noted in `version.ts`); ratchet row appended to `chord-grammar-changes.md`.
- **Progress 2026-08-30 — #343**: root cause was hard-coded contractions after param subjects, not the conjugator — fixed by teaching `IRREGULAR_VERBS` the negative contractions (isn't/aren't, doesn't/don't, hasn't/haven't, wasn't/weren't) and templating the three offenders (`listening.no_sound`, `attacking.combat.not_hostile`, `giving.not_interested`) with `{verb:…}`; every singular rendering is byte-identical (no default flip). Tests: `verb-agreement.test.ts` +1 case (18 passing); lang-en-us 447 passing.

### Phase 5: Story-side acceptance — wire the routed-around backdrop content and confirm the tree
- **Tier**: Small
- **Budget**: 100
- **Domain focus**: N/A — story-content wiring in `branch-stories/secret-letter/`, not platform work; this phase closes the loop the platform fixes exist to open. Domain language from the story: the six `enter <backdrop>` answers, the market gates' hunted-entering refusal, the collapsed named region conditions.
- **Entry state**: Phases 1-4 done (or as many as David chose to ship — this phase wires whichever subset landed). `branch-stories/secret-letter/backdrops.chord` and `grubbers-market.chord` carry the routed-around content per the 2026-08-30 session's Open Items: the kept-aside entering texts, the named region conditions collapsible to one-line `is in` tests, and `grubbers-market.chord:121`'s committed hunted `enter gates` refusal.
- **Deliverable**: `backdrops.chord`'s named membership conditions (open-air, market-perimeter, within-grubbers, …) collapsed to one-line `is in <region>` tests per its own header note (#339). The six kept-aside `enter <backdrop>` answers wired as `entering` clauses, including `enter market` from six places and "You're already in the market." (#341). The market gates' hunted `enter gates` refusal (`grubbers-market.chord:121`) verified live under the `hunted` state (#341). Any entity names affected by #342's decision re-marked proper if that phase shipped. `secret-letter.tests.json` updated with tree-document lines covering the newly-wired behavior.
- **Exit state**: `./sharpee test branch-stories/secret-letter` passes at seed 1209 with the new assertions added (baseline: 498 cards / 857 assertions passing as of 2026-08-30); no `enter <backdrop>` command reaches stdlib's generic refusal where a story-side answer exists; all closed issues (#339-#343, whichever subset shipped) referenced in the commit.
- **Status**: DONE (2026-08-30, session 262648)
- **Progress 2026-08-30**: `./repokit build` fresh, then `backdrops.chord` — the Open-Air Market and Market Perimeter declared as real nested regions (`containing`, no landing lines — #339 makes membership free of them), `open-air`/`market-perimeter` collapsed to one-line region tests, the kept-aside `enter <backdrop>` answers wired (the stalls' "Customers on the outside!" behind the restored `at-a-stall`; the crowd's shove; the market's six-way ladder with the alley and tent walk-outs as authorial moves plus a `merc-held` guard mirroring the going block), and the market marked `enterable, proper` — enterable is mechanical (a clause's moves ride postExecute, which only a successful enter reaches; the ladder covers every room but the two walk-outs), proper is #342 visible in play ("You get into Grubber's Market.", "Grubber's Market is fixed in place."). `grubbers-market.chord:121`'s hunted `enter gates` refusal came alive with #341 (probed: Gentry's line). Tree: +19 cards / +30 assertions — the calm enter tour with `player.location` pins at every rung, the roof refusal, the hunted gates pair — **517 cards passing, 887 assertions passing, 0 failing** (seed 1209, 2026-08-30).
