# Session Plan: ADR-356 D4 — END STATE cards prove endings, plus D9's supersession notes and D5's remaining ratios

**Created**: 2026-09-26
**Plan Status**: DONE (both phases DONE, 2026-09-26; the plan directory stays in place — archiving is a human decision)
**Overall scope**: ADR-356's first cut (GH #520, `docs/work/testing-explorer/plan-20260925-520-adr356-first-cut.md`, DONE 2026-09-26) explicitly deferred D4, D9, and two of D5's three ratios to "the plan phase that lands D4" (ADR-356 D9's own phrase). This plan is that phase, split in two: **Phase 1** lands D4 itself — the optional ending id on a `TreeCard`, the walker change that stops a line where the story ends instead of reviving it, the IDE testing-surface END STATE affordance (moved in the same commit per rule 8b), and D9's three supersession notes plus the closing of ADR-353's Phase 16 on the ruling David already gave (ADR-356 Q-5) — and **Phase 2** lands D5's two remaining ratios (endings reached/declared, rooms entered/declared) now that D4 exists to supply them. **No implementation is authorized by this plan.** ADR-356 itself authorizes nothing (its own opening line), CLAUDE.md gates every `packages/` change on discussion first, and this plan is that discussion's input — David's explicit go-ahead is required before Phase 1 starts, and again before Phase 2 resumes if a session boundary falls between them (the same discipline the first-cut plan used for all four of its phases).
**Bounded contexts touched**: `@sharpee/branch-tester` (the tree document's wire format, the walker's line-ending behavior, the coverage report), `@sharpee/world-index` (two new static derivations — endings declared, alongside the existing `roomsOf` for rooms declared), `@sharpee/devkit` (the three-ratio report), `tools/ide/web/testing-surface` (the END STATE affordance — not under `packages/`, but the ADR's own Affected section requires it move in the same commit as the `tree-document.ts` field, so it is gated the same way). `packages/chord`, `packages/engine`, `packages/stdlib`, `packages/story-loader` are untouched — D4 replays real recorded play through the existing prefix-replay path, not `arrange()`.
**Key domain language**: END STATE card, a line that ends where the story ends, fork-from-an-earlier-card (still legitimate) vs. a card-after-an-END-STATE-card (MALFORMED), endings declared vs. reached, rooms declared vs. entered, one-shot repair (never a migration shim).

## References consulted
- `docs/architecture/adrs/adr-356-the-story-is-the-test-suite.md` — D4 (lines 117-129, the END STATE card mechanism and the walker change it requires); D5 (131-137, the three ratios and their denominators — endings from D4, rooms from the union of D3's arranged rooms and D4's replayed lines); D9 (149-157, the three supersession notes and their owner — "the plan phase that lands D4"); Affected (159-177, `branch-tester`'s `tree-walker.ts:368` and `tree-document.ts`'s optional field/version bump, `tools/ide/web/testing-surface`'s END STATE affordance "moves with `tree-document.ts` in the same commit" per rule 8b); End-to-End Scenario (179-195, the `fernhill-saved` replay/removal/unreached cases and the three-ratio denominators); Acceptance Criteria AC-6 (206, the literal test this plan's Phase 1 must satisfy); Consequences (211-222, the one-shot-repair framing for any tree with cards after an ending, and "ADR-353's Phase 16 has its ruling" naming this plan's phase as the closer); the 2026-09-26 measurement note (228, the enumerator's leaf-path count is 63 on fernhill, not the ADR's estimated 39 — Phase 2's rooms/endings denominators are read off the same measured IR, not the stale estimate).
- `docs/architecture/adrs/adr-353-the-testing-pane-visits-one-line.md` — Scope line (7, "D1–D3 and D5–D8 need no `packages/` change... none of them moves," now touched by this plan's Phase 1 exactly as ADR-356 D9 predicts); D7 (102, "the tree document, the CLI, the assertion core and the pinned seed do not change" — the note this plan owes records the one field this plan adds and confirms everything else on D7's list stands); D8 (104-106, the Phase 16 ruling is "a ruling only David gives... no session may close it by inferring it," answered by ADR-356 Q-5, not by this plan inferring anything); Consequences bullet "ADR-353's Phase 16 has its ruling" (216, naming this plan's phase as the one that "may then close Phase 16 on a ruling given rather than inferred").
- `docs/work/chord-writer-avalonia-production/plan.md` Phase 16 (lines 726-748, Status: PENDING) — the located phase ADR-353 D8 refers to. Its remaining open question ("ended" vs "failed" for a card reached after an ending) is exactly what ADR-356 Q-5 answered; Phase 1 closes it by quoting that ruling, not by inferring one from this plan's own design.
- `docs/work/testing-explorer/plan-20260925-520-adr356-first-cut.md` (DONE, all four phases) — the closed plan this one continues. Its "Explicitly out of scope" section (lines 188-193) is the literal handoff: D4, D9's three notes, and D5's endings/rooms ratios were deferred here by name, on the grounds that D9 itself assigns their ownership to "the plan phase that lands D4." Its Phase 4 design decision 4 (line 79 of the same file) is the source of the GH #524 follow-up this plan scopes out below.
- `docs/architecture/adrs/adr-340-testing-assertion-core.md` D5 (line 30, "the facade does not move... `<story>.tests.json` ... keep their names and shapes") — the note this plan owes records the one added optional field; every other item on D5's list (the CLI flags, `RunEventStream`, the `.transcript` grammar) is untouched by this plan.
- `docs/architecture/adrs/adr-321-world-index.md` D2 (static pass, no engine run, direct IR-type import so a schema change fails the build in the same commit) — the discipline Phase 2's two new derivations (endings declared, and reuse of the existing `roomsOf`) must follow, same as the first-cut plan's Phase 1 enumerator.
- `docs/architecture/adrs/adr-322-state-space-analysis-umbrella.md` D8 ("consume ADR-321's derivations, do not rebuild them") — the reason Phase 2's coverage extension reads `world-index`'s declared-endings/declared-rooms output rather than re-walking the IR in `branch-tester`.
- `docs/architecture/adrs/adr-293-search-executes-the-real-engine.md` D12 (real path only — no model runs) — D4's replay is the existing prefix-replay path through the real engine; Phase 1 must not introduce a second, model-based way to "check" an ending.
- `docs/architecture/adrs/adr-347-the-ending-is-an-explicit-concept.md` (the Ending as `WorldModel`-queryable state, `IStoryEnding` in `packages/if-domain/src/endings.ts`) — D4's assertion reads `world.getEnding()` (`packages/engine/src/game-engine.ts:1560` reads it the same way after a turn), never event-subscription or prose-diffing; `tools/ide/web/testing-surface/src/ending.ts`'s `EndingState`/`endingOf` pattern ("ask the question the platform answers in state," not a prose surrogate) is the precedent Phase 1's testing-surface affordance should follow rather than re-derive.
- `docs/context/project-profile.md` (2026-09-25, fresh) — per-package Vitest, the CJS-`dist`-vs-ESM-`dist-esm` dual build `world-index` and `branch-tester` both carry (a source change is invisible to `devkit`'s lazy `require('@sharpee/branch-tester')` until `dist/` rebuilds).
- `docs/context/session-20260925-1830-explorer-prototype.md` Open Items — I-524 (no run-event wire type for a derived branch outcome — this plan's Phase-1/2 scope decision below), I-522/I-523 (secret-letter findings, informational, untouched here), I-525 (next arrange shapes — a different, later plan's scope), I-242 (entity-topic fallback — unrelated), I-520 (the closed first-cut plan's own issue, already DONE).
- GH #524 ("Derived rule-test outcomes have no run-event wire type") — the outgoing plan's own follow-up note said a wire event "belongs with D4/D9's IDE work." This plan scopes it **out** (see "Explicitly out of scope" below) with the reasoning recorded there, rather than silently carrying it in or dropping it.
- `packages/branch-tester/src/tree-document.ts` (whole file header, lines 1-30; `TREE_DOCUMENT_VERSION` at line 39; `deserializeTreeDocument`'s version gate at lines 195-206) — the existing invariant this plan's version bump exercises exactly as designed: a newer document is `refused`, an older one is `malformed` and degrades to empty, and "additive fields arrive with a version bump, never silently" (line 26) is the file's own header rule, not a new one this plan invents.
- `packages/branch-tester/src/tree-walker.ts:355-403` — the prefix-replay loop and the `game.reviveEngine?.()` call at line 371 (comment block 368-370, "fork-on-the-death-card is a legitimate shape") that D4 requires this plan to remove/replace.
- `packages/world-index/src/story.ts:156` (`roomsOf`) — already exists and already returns declared rooms; Phase 2's rooms-declared denominator is this function, not new code.
- `packages/branch-tester/src/coverage.ts` header (lines 1-27) — states outright "the endings and rooms ratios depend on D4's END STATE cards, which a later plan lands," naming this plan's Phase 2 as its own stated successor.
- `packages/devkit/src/commands/test-derived.ts` (whole file) — the existing report/exit-code surface (0/1/3) Phase 2 extends rather than replaces.
- CLAUDE.md — "Platform changes require discussion first. Any changes to `packages/` ... must be discussed with the user before implementation" — the reason every phase below is gated on David's go-ahead exactly as the first-cut plan was; and "Never modify real stories for platform tests" / "Do not invent story content" — the reason Phase 2's fernhill/secret-letter measurement records whatever endings-reached ratio the existing authored tree lines produce (likely 0/2 on fernhill today, since no line yet plays through to an ending) rather than authoring a new winning line to make the number look better.

## Phases

### Phase 1: D4 — the END STATE card, the walker's line-ending change, the testing-surface affordance, and D9's notes
- **Tier**: Large
- **Budget**: 400
- **Domain focus**: `@sharpee/branch-tester` (`tree-document.ts`'s wire format and `tree-walker.ts`'s line-ending semantics) and `tools/ide/web/testing-surface` (the END STATE affordance, moved in the same commit per rule 8b). A line that ends in an ending IS that ending's walkthrough (D4) — no new artifact, no re-check of prose (D6), just an assertion that the replay's ending record matches the id the card declares.
- **Entry state**: ADR-356's first cut DONE (Phases 1-4 of GH #520, all shipped 2026-09-25/26). `TreeCard`/`TreeDocument` at `TREE_DOCUMENT_VERSION = 1`, no ending-related field. `tree-walker.ts`'s prefix-replay revives the engine after a card that ended the game (`:371`) and keeps running the line's own cards. No story's `.tests.json` yet authors a line that reaches an ending (confirmed for fernhill: no `win`/`lose`-adjacent marker in `branch-stories/fernhill/fernhill.tests.json` as of this plan's writing).
- **Deliverable**:
  1. **One new optional field on `TreeCard`** in `packages/branch-tester/src/tree-document.ts`: the ending id the card declares (naming open — `endingId` or `endsWith`, matching the file's existing naming register). No other change to the card or document shape (D6 — the tree's format does not otherwise move). `TREE_DOCUMENT_VERSION` bumps from `1` to `2` (the file's own header rule at line 26: "additive fields arrive with a version bump, never silently"); `validateDocumentShape` accepts the new field; `deserializeTreeDocument`'s existing version gate (lines 195-206) needs no new logic, since a document from the future is already `refused` and one from the past is already `malformed` — the invariant that already exists does the work, and there is no shim (per the project's no-backward-compatibility rule and the ADR's own "one-shot repair, not a shim" framing).
  2. **A one-shot repair of the committed `.tests.json` files, run once, not shipped as code**: every existing tree document under `branch-stories/*` (at minimum fernhill and secret-letter) has its `version` field hand-edited (or rewritten by a throwaway script, never a runtime migration path) from `1` to `2`, since the new field is optional and no existing card sets it — the documents are already valid under the new shape. Before doing so, **scan every existing line's replay for a card that already reaches an ending under today's revive-and-continue walker** (a card whose command triggered `win`/`lose`/`kill` with commands still recorded after it in the same line) and report any found to David for disposition — trimming, marking, or leaving as a fork point — rather than silently repairing or dropping them. This is the concrete instance of the ADR's Consequences bullet: "any existing tree with cards after an ending will report them MALFORMED at conversion... a one-shot repair, not a shim."
  3. **The walker change** in `packages/branch-tester/src/tree-walker.ts` (replacing the `game.reviveEngine?.()` call at `:371` and its surrounding comment `:368-370`): a line's own cards stop running at the first card whose command produces an ending (`world.getEnding()` becomes defined where it was not before that command) — the engine is left stopped, exactly as ADR-347 already models it, and the walker does not revive it. That stopping card is asserted against its declared ending id, per D4's own wording: "D4 replays each such line and asserts the ending record the engine produced matches the declared id; a line that stops short fails naming the ending." A **fork taken FROM an earlier card** (a `TreeBranch` on a card before the ending) stays legitimate exactly as today — D4 only changes what happens to the cards AFTER an ending is reached on the line's own spine. A card **recorded after** an END STATE card in the same line's cards array (not a fork, the literal next array entry) is reported as **MALFORMED** and not run — this is the new, named failure mode AC-6 requires ("A card placed after an END STATE card is MALFORMED, reported and not run").
  4. **The assertion**: for a line whose last executed card declares an ending id, the replay's `world.getEnding()` result (kind + `messageId`, per `packages/if-domain/src/endings.ts`'s `IStoryEnding` and `packages/engine/src/game-engine.ts:1560`'s read of it) is compared to the declared id; a match passes, a mismatch or an undefined ending fails **naming the ending that was not reached** (D4's own wording, and AC-6's "the same line with that card removed fails with a message naming the ending"). A line with NO card declaring an ending replays exactly as today (no D4 assertion added) — D4 only activates on lines the author has marked.
  5. **The testing-surface END STATE affordance**, in the same commit (rule 8b): the Testing tab shows "END STATE" on the card carrying the declared ending id and accepts no further typed command on that card — following the state-not-prose discipline `tools/ide/web/testing-surface/src/ending.ts`'s `EndingState`/`endingOf` already establishes for the play-time driver (D1's `ending.ts` reads `world.getEnding()` via the `story-ending` channel to decide whether to block input; the Testing tab's END STATE card reads the field the tree document itself now carries, since authoring is offline and has no live channel to read — the two surfaces answer the same question from two different data sources, by design, and neither infers it from prose).
  6. **D9's three supersession notes**, written into the touched documents (no Status line flips, per D9's own closing sentence):
     - A note beside ADR-353 D7 recording the one field ADR-356 D4 adds to the tree document's card shape; D7's other subjects (the CLI, the assertion core, the pinned seed) are confirmed untouched by this plan.
     - A note beside ADR-353 D8 (and its Phase 16 reference) quoting David's 2026-09-25 Q-5 ruling verbatim — *"an ending is a declarative state. A card would block additional commands in that card and show a message for 'END STATE'"* — recording that the ruling was given under ADR-356's interview, so Phase 16 closes on a ruling given, not inferred.
     - A note beside ADR-340 D5 recording the one added optional field on the card shape; every other item on D5's facade list (CLI flags, `RunEventStream`, the `.transcript` grammar, `<story>.tests.json`'s name) stands unchanged.
  7. **Close `docs/work/chord-writer-avalonia-production/plan.md` Phase 16**: its Status line moves from PENDING to DONE, with a note that its remaining deliverable — the ended-vs-failed presentation call — was ruled by David under ADR-356 Q-5 (quoted above), not inferred by this or any session, exactly as ADR-353's own Consequences bullet anticipates ("the plan phase that lands D4 may then close Phase 16 on a ruling given rather than inferred").
- **Exit state**: `packages/branch-tester` suite green including new tests for AC-6's three cases (a line whose declared-ending card passes; the same line with that card removed fails naming the ending; a card placed after an END STATE card is reported MALFORMED and not run) plus a case for "a declared ending no card carries appears in the report as unreached" (feeding Phase 2's denominator, not asserted as a failure here). Fixture stories under `packages/branch-tester/tests/fixtures/` carry the END STATE cases (never fernhill/secret-letter, which stay read-only measurement targets). `tsc --noEmit` clean on `branch-tester`; `dist`/`dist-esm` rebuilt. The testing-surface's own test suite green with the END STATE affordance covered; its bundle rebuilt. Every existing `.tests.json` under `branch-stories/*` reads as `version: 2`, `ok` (not malformed), after the one-shot repair; any line the scan found already reaching an ending mid-line has been reported to David and disposed of per his instruction before the walker change ships. The three D9 notes are committed beside their respective ADRs, with no Status line flipped. `docs/work/chord-writer-avalonia-production/plan.md` Phase 16 reads DONE. Behavior Statement produced for the walker's changed control flow and the new assertion function before their tests were written (rule 12); tests graded GREEN (rule 13).
- **Status**: DONE (2026-09-26, session 54fb33)
- **Landed**:
  1. `packages/branch-tester/src/tree-document.ts` — `TreeCard.ending?: string` (the id the story's `win`/`lose`/`kill` named; turn cards only), `TREE_DOCUMENT_VERSION` 1 → 2, validator accepts the field, and `endingIdsDeclaredBy(document)` (the "reached" side of D5's endings ratio, for Phase 2). No shim: version 1 reads as malformed by the existing gate.
  2. `packages/branch-tester/src/tree-walker.ts` — `flattenTreeLines` reports two new card-position defects (a card after an END STATE card; a branch from one), so a defective document runs nothing, matching the opening/boot rule. `reviveEngine` removed from `TreeWalkerGame` and never called. `assertionsOfCard` attaches the END STATE card's claim; `skip` does not suppress it.
  3. **The claim is a claim kind, not a walker-side read (amendment A1 below)**: `ending-assert` in `packages/transcript-tester` — `types.ts` (`endingId`), `assertion-core.ts` (`checkEndingAssertion`: passes when the world's Ending carries the id as `messageId` or `cause`; a miss names the declared ending and what the story did instead), `serializer.ts` (unserializable, like the four channel kinds), barrel export; `auto-assertion.ts`'s describer renders it `ending <id>` on the wire.
  4. `tools/ide/web/testing-surface` — `ending.ts` gains `endingIdOf` (messageId, else cause, off the `story-ending` channel); `main.ts` stamps the id on the turn the ended flag flips; `model.ts` sets it on append, fills it on bind (never overwrites), and `canBranch` refuses an END STATE card; `cards.ts` renders `· END STATE · <id>` in the card's meta line; bundle rebuilt into `tools/ide/SharpeeIDE/Resources/testing-surface`. Typing past an ending was already blocked by `storyEnded` (Phase 16's shipped half).
  5. D9's three notes written beside ADR-353 D7, ADR-353 D8 (quoting Q-5), ADR-340 D5; `docs/work/chord-writer-avalonia-production/plan.md` Phase 16 → DONE on the quoted ruling. No Status line flipped.
  6. **One-shot repair** (a throwaway script in the session scratchpad, never shipped): every committed tree document moved to version 2 — `branch-stories/{fernhill,ides-of-march,secret-letter}`, `branch-stories/secret-letter/prototypes/w10-dance`, `stories/thealderman/chord`, `docs/work/john-chord-samples/no-signal-home`. fernhill and w10-dance were not in the serializer's canonical key order, so their diffs are large but content-identical (the tab would rewrite them the same way on its next save). Lines whose LAST card reached a story-named ending under the old walker got that id: fernhill `main` (`south` → `fernhill-saved`) and `main/b5` (`wait` → `fuse-blast`); ides-of-march `main` (`east` → `ides-won`) and `main/b2` (`unmasked-lose`); secret-letter `main/b16` (`ride-out-caught`), `main/b34/b38/b40`, `b48`, `b49` (`mercenary-brutality`), `main/b34/b45` (`stepped-out-caught`), `main/b34/b47` (`ride-out-caught`), `main/b34/b50` (`pole-falls`).
- **Scan findings, reported for David's disposition (not repaired)**:
  - secret-letter `main` reaches a death at card 77 (`ne` → `defeat`, cause `death-at-mercenaries-chord-167-5`) and its remaining 71 cards fail against the stopped engine — **this is I-523's root cause** (the first failure is that card's `Grocery Stall` claim). The card is unmarked, so under D4 the line is not MALFORMED; it fails as before. Options are David's: trim the 71 cards, or fix the story so the recorded path survives.
  - secret-letter `main/b27`, `main/b28`, `main/b31` end on the same synthesized-cause death — no authored id, so no END STATE card can name them (Phase 2's "ending declared without an id" gap, seen from the tree side).
- **Evidence** (all run 2026-09-26, session 54fb33): `pnpm --filter '@sharpee/transcript-tester' test` → 349 passed (27 files); `pnpm --filter '@sharpee/branch-tester' test` → 183 passed (12 files; `tree-end-state.test.ts` is AC-6 on a real engine: a `win` and a `kill` declared by phrase key pass, the line with the winning card removed fails naming `box-opened`, the wrong id fails naming `defeat (cave-in)`, a card after an END STATE card is a defect with zero boots); `pnpm --filter '@sharpee/devkit' test test-tree-document test.test` → 14 passed; testing-surface `npx vitest run` → 140 passed (12 files, real-path and AC-2 sign-off included); `tsc --noEmit` clean on devkit and the surface, `tsc` builds clean for transcript-tester and branch-tester (dist + dist-esm). `./sharpee test branch-stories/fernhill` → tree 11/11 lines green including both END STATE lines, 86 cards passing, 106 assertions passing; exit 1 from the derived tier's two pre-existing I-242 failures, 33/63 branches — unchanged. `./sharpee test branch-stories/ides-of-march` → 4/4 lines green, exit 0. `./sharpee test branch-stories/secret-letter` → 566 cards passing, 72 failing (all 72 on `main` after card 77, as before), all eleven END STATE lines green; derived 358/721 with the same 12 failures as the first-cut measurement — no regression.
- **Not covered here**: `packages/bootstrap`'s `reviveEngine()` now has no caller and its doc comment (and `game-engine.ts`'s beside `derivePhaseFromEnding`) still describe the walker reviving — outside this plan's contexts, reported as a follow-up. The Swift fixture in `tools/ide/SharpeeIDETests/TestRunnerTests.swift` was bumped to version 2 but XCTest was not run this session.

### Phase 2: D5's remaining ratios — endings reached/declared, rooms entered/declared
- **Tier**: Large
- **Budget**: 400
- **Domain focus**: `@sharpee/world-index` (two declared-set derivations: endings, already-existing rooms), `@sharpee/branch-tester` (the coverage report, extended from one ratio to three), `@sharpee/devkit` (the `sharpee test` report, printing all three). Reachability's own denominator, completed: "reasonably complete" becomes three ratios, not one.
- **Entry state**: Phase 1 DONE — END STATE cards exist, the walker stops a line where the story ends and asserts the declared id, and `world.getEnding()` is the read surface for it. **Correction from Phase 1's scan**: fernhill's main line already reaches `fernhill-saved` on its last card and `main/b5` reaches `fuse-blast`, so fernhill's endings ratio will not read 0/2 — the planning assumption below was wrong; `dawn-comes` (the timer-driven `lose`) is the one no line reaches. `endingIdsDeclaredBy(document)` supplies the reached side. `packages/world-index/src/story.ts:156`'s `roomsOf(ir)` already returns every declared room; no new derivation needed for the rooms-declared side. `packages/branch-tester/src/coverage.ts`'s own header (lines 16-18) already states the endings/rooms ratios are this plan's job.
- **Deliverable**:
  1. **A new `endingsOf(ir): DeclaredEnding[]` (naming open) in `packages/world-index/src/story.ts`**, beside `roomsOf`, following the same static-pass discipline (ADR-321 D2): walk every `win`/`lose` statement the IR contains (the same `kind: 'win' | 'lose'` shape `packages/chord/src/ir.ts:1510-1511` already types, and the same statement roots `forEachStatementRoot` already walks in `statements.ts:249`), collecting each statement's `phraseKey` as its declared ending id, deduplicated, with its span. **Named edge case, not silently dropped**: a `win`/`lose` statement with a null `phraseKey` (grammatically legal) declares an ending with no id — D4's END STATE card can never name it, so it is reported in Phase 2's ratio as a distinct, separately-labeled gap ("an ending declared without an id"), not folded into the denominator as if it could ever be reached. Exported from `packages/world-index/src/index.ts` beside `roomsOf`.
  2. **`branch-tester`'s coverage report extended** (`packages/branch-tester/src/coverage.ts`, currently branches-only per its own header): given `endingsOf(ir)` and, per tree document, which lines carry an END STATE card (Phase 1) and whether that line's Phase-1 assertion passed, produce the **endings reached/declared** ratio and list every declared ending no line reaches (D5's own framing: "an ending no author has played through is unreached, and D5 reports it as such. That report is the finding, not a gap in the tool" — this ratio is expected to be low on a story with few authored winning lines, and that is correct, not a defect). Given `roomsOf(ir)` and the **union of D3's arranged rooms** (every room `arrange()` or the derived runner's placement terms visited during the branches suite — the derived-runner's `player.location`/`reach-subject` terms already compute this per branch, per the first-cut plan's Phase 3 design notes; it needs exposing as a set, not new computation) **and D4's replayed lines' rooms** (every room the player occupied during a tree line's replay, read off the world during `tree-walker.ts`'s existing turn loop — new instrumentation, since nothing tracks visited rooms today), produce the **rooms entered/declared** ratio and list every declared room neither tier reaches.
  3. **`packages/devkit/src/commands/test-derived.ts` and `test-tree-document.ts`** print all three ratios (branches, endings, rooms) in one report; the exit-code rule is unchanged (a branches or endings-assertion failure still exits non-zero; an unreached ending or an unentered room is a gap in the ratio, not a failure — D5a already settled that a SKIP/gap never fails the build, and an unreached ending is the same kind of fact).
  4. **The fernhill and secret-letter measurement, run and recorded verbatim** (never narrated from memory, matching the first-cut plan's own discipline): both stories' IR recompiled from source immediately before measuring (GH #519's lesson, already the practice `test-derived.ts` follows). Record the actual endings and rooms ratios for both. **Expected, and to be reported as a finding rather than treated as a defect to fix**: fernhill's endings ratio is likely 0/2 today, since no authored tree line yet plays through to `fernhill-saved` or `dawn-comes` (confirmed absent from `branch-stories/fernhill/fernhill.tests.json` as of this plan) — per CLAUDE.md's "do not invent story content" and D4's "the author has to provide the shape of the winning states," this plan does not author a new winning line to improve the number; it reports the ratio David's own authored tree produces.
- **Exit state**: `packages/world-index` suite green including `endingsOf` tests (a fixture story with two named endings and one unnamed `win`, asserting the count and the named gap for the unnamed one); two-run byte-identical, same discipline as `collectClauseBranches`. `packages/branch-tester` suite green including the extended coverage report's endings/rooms tests, each with a fixture asserting the ratio and the gap list by span/name. `packages/devkit` suite green including the three-ratio report output. `tsc --noEmit` clean on all three packages; `dist`/`dist-esm` rebuilt where consumed via `require()`. The fernhill measurement recorded verbatim in this plan's own outcome (mirroring the first-cut plan's Phase 4 format), including whatever endings/rooms ratios the existing authored material actually produces. `git status` clean over `branch-stories/fernhill` and `branch-stories/secret-letter` after both measurement runs (D6/AC-9's discipline, unchanged). Behavior Statements produced for `endingsOf` and the coverage extension before their tests (rule 12); tests graded GREEN (rule 13).
- **Status**: DONE (2026-09-26, session 54fb33)
- **Landed**:
  1. `packages/world-index/src/story.ts` — `endingsOf(ir): DeclaredEnding[]` beside `roomsOf`, a static pass over `forEachStatementRoot` (no cycle: `statements.ts` imports only chord types) collecting every `win`/`lose`/`kill` with its phrase key as the id, kind, statement, owner and line; named endings deduplicated by id (first declaration's line kept), unnamed ones kept apart with `id: null`. Exported from the barrel with its type. `kill` is counted as a declared ending because D4 lets a card name it by its `cause`.
  2. `packages/branch-tester/src/derived-runner.ts` — each booted branch records the player's room IR id after arranging and after its command (`DerivedOutcome.rooms`); `DerivedSuiteResult.roomsEntered` is their union. `src/tree-walker.ts` — `TreeDocumentRunResult.roomsEntered` (the room at boot, after every replayed command, after every card — read through the runner's observer while the game is in hand, since the runner's own snapshot names rooms rather than IR ids) and `endingsReached` (every ending id whose END STATE claim passed).
  3. `packages/branch-tester/src/coverage.ts` — `endingCoverageOf` (named endings only in the ratio; unnamed listed apart), `roomCoverageOf`, `formatCoverageSummary`; all exported.
  4. Gap sites honor the span's file: an ending or a branch in an imported `.chord` file is cited as `mercenaries.chord:167`, not as a line of the main story file (`siteOf` in `coverage.ts` had the same blind spot since Phase 4 of the first-cut plan; fixed here, one line).
  5. `packages/devkit/src/commands/test-derived.ts` takes the tree run's `endingsReached`/`roomsEntered` through a new `tree` option and prints all three ratios in one report after the branch rows; `test-tree-document.ts` passes them. Exit code unchanged: an unreached ending or an unentered room is a gap, never a failure (D5a).
- **The measurement, run through `sharpee test` and recorded verbatim** (2026-09-26, session 54fb33; each story recompiled from source by the command itself):
  - **fernhill** — `Branches exercised: 33 / 63` (unchanged); `Endings reached: 2 / 3`, `Not reached (1): fernhill.story:636 · dawn-comes (lose)`; `Rooms entered: 13 / 13`. Exit 1 from the two pre-existing derived failures (I-242).
  - **ides-of-march** — `Branches exercised: 21 / 80`; `Endings reached: 2 / 5`, not reached: `ides-of-march.story:90 · ides-no-book (lose)`, `:92 · ides-no-kemp (lose)`, `:94 · ides-nothing (lose)`; `Rooms entered: 5 / 5`. Exit 0.
  - **secret-letter** (informational) — `Branches exercised: 358 / 721`, `Derived failures: 12` (unchanged, I-522); `Endings reached: 4 / 5`, `Not reached (1): mercenaries.chord:167 · death-at-mercenaries-chord-167-5 (kill)`; `Rooms entered: 21 / 21`. Exit 1 (the main line, I-523).
  - **Finding on that unreached ending**: the id is compiler-generated (`kill the player` with an inline body and no key, `mercenaries.chord:167`, gets a key derived from its file and position), and three tree lines (`main/b27`, `b28`, `b31`) DO reach it. They were deliberately not stamped in Phase 1's repair: a positional key changes whenever a line above it moves, so an END STATE card carrying it would fail on the next unrelated edit. Whether the story should name that death (so the tree can claim it) is David's call — it is the one gap in secret-letter's endings ratio.
- **Evidence** (all run 2026-09-26, session 54fb33): `packages/world-index` `npx vitest run` → whole suite green including `tests/endings.test.ts` (4 tests: order/dedupe/owner/line, unnamed kept apart, two-run byte-identical, none declared); `pnpm --filter '@sharpee/branch-tester' test` → 190 passed (12 files; `derived-coverage.test.ts` gains the two ratios and the rendered summary with AC-6's unreached-ending line; `tree-walker.test.ts` pins rooms/endings tracking on the stub; `tree-end-state.test.ts` and `derived-runner.test.ts` assert both contributions off real worlds); `pnpm --filter '@sharpee/devkit' test test-derived test-tree-document test.test` → 17 passed (the derived-pass fixture prints `Endings reached: 0 / 0` and `Rooms entered: 1 / 1`). `tsc` builds clean for world-index, branch-tester and devkit; `tsc --noEmit` clean on branch-tester's test project. `git status --porcelain -- branch-stories stories docs/work/john-chord-samples` after all three measurement runs shows only Phase 1's version-2 repairs — nothing derived written (D6/AC-9).

## Explicitly out of scope (stated here per the task's own instruction, not silently dropped)

- **GH #524 (a run-event wire type per derived branch outcome)**. The outgoing first-cut plan flagged this as belonging "with D4/D9's IDE work"; this plan scopes it **out**, for a specific reason rather than by default: D4's Testing-tab affordance (Phase 1.5 above) reads the END STATE field directly off the tree document — a static read, no run and no wire event needed. D5's report (Phase 2) is a synchronous CLI/devkit print, already served by the existing stdout/stderr surface `test-derived.ts` has today. A structured per-branch run event would matter for *live* per-branch progress rendering in the Testing tab while a run is in flight — a `platform-browser`/wire-protocol addition with its own design questions (what the UI shows mid-run) that neither D4 nor D9 requires to be satisfied. It stays filed as GH #524, a follow-up for whenever live-progress rendering is itself scoped.
- **GH #521** (the `packages/chord` predicate-span gap) — unrelated to D4; still filed and tracked separately.
- **GH #525** (the next arrange shape to teach, ranked by secret-letter's SKIPPED count) — a scoping question for a future plan extending D2's floor, not this one.
- **Dungeo** (D8) — untouched, as ADR-356 states outright; this plan touches only Chord-story testing surfaces.
- **Any new artifact naming an ending** — rejected by the ADR itself (D4's "Rejected: a new per-ending artifact"); `WALKTHROUGH.txt` remains prose, never a test input, per David's own ruling quoted in the ADR.
- **Authoring new tree lines that reach an ending** — that is David's content-authoring work (D4: "the author has to provide the shape of the winning states"), not this plan's; Phase 2 measures whatever lines already exist.

## Amendments

- **A1 (2026-09-26, session 54fb33, Phase 1)**: the END STATE card's assertion lands as a claim kind in the assertion core (`ending-assert`, `packages/transcript-tester`) rather than as a walker-side read after `runTranscript`. Reason: the per-command row, the run-event wire (`sharpee test --json`), and the IDE run column all read the runner's rows, and a check made after the rows were announced would leave the wire saying the card passed while the line failed. ADR-356's Affected section already names transcript-tester for "any claim the effect catalog needs that the core lacks" (ADR-340 D3), so this widens the plan's touched contexts by one package the ADR itself lists — not a new decision.

## Plan-review hand-off

This plan carries a "References consulted" section (four ADRs plus a located open-plan phase, a session's Open Items, three GH issues, and several source files with line citations), so per the planner's own rule the `plan-review` skill runs automatically against it before David sees a final go/no-go summary.
