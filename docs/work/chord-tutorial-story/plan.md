# Session Plan: Chord Launch Tutorial Story (ADR-233 G3 — Q-3 ruling step 3)

**Created**: 2026-07-18
**Overall scope**: Write, transcript-test, and ship ONE narrative Chord tutorial — a NEW story built for Chord (not a Family Zoo port), bigger than a demo, exercising the David-selected pattern set recorded in `docs/work/chord-tutorial-story/pattern-catalog.md`'s SELECTION section — plus the teaching documents that walk an outside author from `npm i -g @sharpee/devkit` through building it themselves, pattern by pattern, linked from the live site nav. ADR-233's G3 gate closes only when the tutorial SHIPS (design + prose + transcripts + docs + nav link, all together) — this plan is that follow-on effort. This is the last content gate before G4 (release); G1 and G2 are both already confirmed SATISFIED.
**Bounded contexts touched**: `stories/<slug>/` (the new story itself — story-level, proceeds autonomously per CLAUDE.md), `website/src/pages/docs/tutorials/` + `website/src/layouts/DocsLayout.astro` (the teaching document(s) and nav link), `docs/reference/chord-grammar.md`/`docs/architecture/chord-grammar-changes.md` (cross-referenced, not modified, unless the tutorial surfaces a genuine platform gap — raised to David per CLAUDE.md, never silently bridged). No `packages/` changes are expected; if the tutorial's pattern-by-pattern build surfaces a real platform gap, STOP and raise it — do not work around it in story code.
**Key domain language**: the 32-pattern SELECTION set — the 30-pattern ☆ spine plus the two non-☆ browser-presentation rows (A1–A5, A7–A9, B1–B3, B5, B7, B9, C1–C4, D1–D3, D5, E1–E4, F1–F2, G1–G2, G4, G5 — catalog IDs are the traceability key used throughout this plan), the estate-mystery working shape (grounds + house, guard + patrol NPCs, boiler/greenhouse machine chain, timeline + fuse, scored discoveries, two endings), browser-first canonical playthrough (G4/G5 teach the full browser experience; logic beats stay transcript-testable), the author pipeline (`sharpee init`/`build`/`build --browser`/`test`/`play` — just shipped, `docs/work/chord-author-pipeline/plan.md`).

**Naming note**: this plan uses the placeholder `<slug>` for the story's own directory name (`stories/<slug>/`), its `.story` filename, and its site-nav tutorial slug (`/docs/tutorials/<slug>/`) throughout. Phase 1 fixes the actual slug (derived from David's title ruling) and that concrete name is substituted everywhere from Phase 2 onward — this placeholder is not a TODO left for the implementer to silently resolve on their own; it is fixed by Phase 1's stop-and-ask.

## References consulted
- `docs/architecture/adrs/adr-233-chord-go-live-gate.md` — G3's exact requirements: a new narrative Chord tutorial (reference ≠ tutorial), not a Family Zoo port, bigger than minimal, pattern-first per the Q-3 ruling's three steps, linked from the existing site nav, closes only on shipping; explicitly confirms ADR-232 (site restructure) and ADR-191 (playground) are NOT launch requirements — this plan must not grow into either.
- `docs/work/chord-tutorial-story/pattern-catalog.md` — the SELECTION section (David, 2026-07-18, session 80ff54): the must-include 30-pattern set, the browser-first ruling (G4+G5 in, canonical playthrough is browser-first, logic stays transcript-testable), the estate-mystery working shape from the coverage notes, and the C4/ADR-239-shipped note (topics are ordinary surface now, no platform work rides on this plan).
- `docs/reference/chord-grammar.md` — the full current Chord surface the tutorial teaches, read in full including the newest sections (Doors ADR-234/237/238, Topics ADR-239, Extension surface ADR-215/216 — machines, media sugar, channels, `client has`) — the tutorial must showcase these exact current spellings, not superseded or stale forms.
- `docs/architecture/chord-grammar-changes.md` — the append-only ratchet log; confirms R2 (`through` door exit-line) and R3 (keyless single-entity `with` config) as the newest landed forms this month — the catalog explicitly calls these "cheap to include and they showcase exactly what just landed," so the tutorial's door and tool-gate beats (A2/A3, B2) use the current ratcheted spelling, never the pre-ratchet keyed forms.
- `docs/work/chord-door-loading/plan.md` — house-style precedent: per-phase REAL-PATH test gates against a real loaded `WorldModel`/parser (not stubs), embedded stop-and-asks before breaking or unratified decisions land, a dependency summary distinguishing hard sequencing from parallelizable phases, and a closure phase that returns `docs/context/.current-plan` to the umbrella.
- `docs/work/chord-author-pipeline/plan.md` — house-style precedent AND the shipped pipeline this tutorial's docs teach: `sharpee init` scaffolds Chord by default (`--ts` opt-out), `sharpee build --browser` ships the `.story` SOURCE plus the client-side compiler and emits `dist/<story>.ir.json`, and the `DEVKIT_INTEGRATION=1` staged-tarball proof mechanism this plan's Phase 9 reuses (lighter-weight, scoped to one story, not a full clean-machine reproof — G2 already proved the pipeline generically).
- `docs/work/chord-go-live/plan.md` — the umbrella; Phase 7's exact G3 handoff text (catalog → selection → design, "the finished tutorial... is that plan's exit, and G3 closes only when it ships") and its noted budget-so-far (250, for the catalog/selection step only — this plan is the follow-on effort the umbrella explicitly deferred); the `.current-plan` pointer-return convention this plan's closure phase follows.
- `docs/context/session-20260718-0501-chord-foundations.md` — this session's state: ADR-239 (topics) shipped this session, closing the last audit ⚠️ and unblocking C4; G1 and G2 both CONFIRMED SATISFIED by David; the tutorial pattern-catalog spine selection was this session's sole remaining pre-plan open item, now resolved (the SELECTION section this plan builds from).
- `website/src/layouts/DocsLayout.astro` — the live site nav array (`Tutorials`, `Author Guide`, etc. sections) this plan's tutorial-docs phase must add an entry to, matching the existing `{ label, href }` shape exactly.
- `website/src/pages/docs/tutorials/family-zoo.mdx`, `.../cloak-of-darkness.mdx` — the site's existing tutorial-page conventions (frontmatter block, "The Game" / "What You'll Learn" table, "Prerequisites") the new tutorial page(s) follow structurally, adapted for a Chord-language and browser-first story rather than a TypeScript one.
- `CLAUDE.md` (project instructions) — story-level work (`stories/`) proceeds autonomously without a platform-change discussion; any genuine platform gap the tutorial surfaces is raised to David, never silently bridged; transcript-testing conventions (bundle CLI needs `--story stories/<slug>/<slug>.story`, `--chain` for walkthroughs, never disable story RNG — transcript logic gates are the remedy for phrase-strategy variation).

## Cross-cutting notes (apply to every content phase, 2–8)

- **Determinism discipline (G1 phrase strategies, no D4 seeded chance selected)**: the selected set includes `randomly`/`cycling`/`stopping`/`sticky`/`first-time` phrase strategies (G1) but explicitly excludes seeded chance (D4). Transcripts must never disable story RNG (project policy) — assert on structural/invariant postconditions (e.g. "one of these N variants appeared," `[ENSURES: ...]` on state, not exact text match) for any phrase-strategy-touched line, mirroring the combat-RNG "enough attempts" convention used elsewhere in this repo.
- **REAL-PATH discipline (rule 13a)**: every phase's test gate drives the real bundle CLI (`node dist/cli/sharpee.js --test --story stories/<slug>/<slug>.story ...`) or the real devkit build against the actual `.story` source — never a hand-written stand-in for the Chord compiler or loader.
- **Platform-gap discipline**: if any selected pattern doesn't load or behave as the catalog claims ("loadable from Chord today"), STOP, do not paper over it in story code, and raise it to David — this mirrors the "Chord as elegance oracle" and "raise gaps, don't bridge them" conventions.
- **Story-level autonomy**: per CLAUDE.md, `stories/` work proceeds without a platform-change discussion. If a phase discovers it actually needs a `packages/` change, that crosses into platform territory and needs the explicit go-ahead first (same as the author-pipeline and door-loading workstreams required).

## Phases

### Phase 1: Story design — title, premise, cast, map, puzzle chain (David-review checkpoint)
- **Tier**: Medium
- **Budget**: 250
- **Domain focus**: no code. A design document mapping every one of the 32 selected catalog patterns onto a concrete story beat.
- **Entry state**: SELECTION section of `docs/work/chord-tutorial-story/pattern-catalog.md` is final (it is — David ruled 2026-07-18).
- **Deliverable**: `docs/work/chord-tutorial-story/story-design.md` covering:
  - **Title and premise** — working title, one-paragraph premise, tone (the catalog's estate-mystery shape is the default working shape; deviations must justify themselves against pattern coverage, not just taste).
  - **Cast** — every NPC with its catalog role tagged (C1 patrol, C2 guard/follower, C3 give/show gate, C4 topics — who has a topics table and what's in it).
  - **Map** — every room/region with its catalog role tagged (A1 compass network, A2/A3 doors incl. one two-sided lockable, A4 dark room + light source, A5 blocked exit, A7/A8 regions with crossing reactions and region/story daemons, A9 first-visit prose).
  - **Object & mechanism inventory** — every object tagged to its pattern (B1 containers/supporters, B2 keyless tool gates — using the CURRENT R3 keyless `with` spelling, B3 concealment, B5 readable clues, B7 switchable boiler feeding the machine chain, B9 scenery discipline as a blanket craft rule, not a beat).
  - **Time/logic/state design** — the D-group (D1 fixed timeline, D2 fuse with stakes, D3 presence-gated recurring event, D5 the boiler's 3-stage `state-machines` depth) and E-group (E1 greenhouse vine state chain, E2 authored trait with data fields, E3 custom verb, E4 interception/refusal) mapped to specific beats, explicitly noting how D5 and E1 interact if the boiler and greenhouse are coupled (per the catalog's "machine chain" framing) or are kept independent (either is fine; state it).
  - **Scoring & endings** — F1 (where points attach and why), F2 (the two endings' exact win/lose conditions and how they relate to D2's fuse).
  - **Text & presentation plan** — G1 (which lines get which phrase strategy), G2 (which snippets gate on which prior discovery), G4/G5 (which rooms/objects get sound/image/channel treatment, and what each degrades to in text-only — `client has` gates named explicitly per asset).
  - **Story location decision (record explicitly, do not assume silently)**: the tutorial story lives at `stories/<slug>/` in-repo, following the `stories/friendly-zoo/` / `stories/cloak-of-darkness/` convention — built and regression-tested via `./repokit build` and the bundle CLI's `--story stories/<slug>/<slug>.story` flag (matching every other in-repo story gate). The teaching documents (Phase 9) separately walk an outside author through the equivalent `sharpee init`/`build --browser` devkit commands against a copy of the same story content — record this split (in-repo canonical copy for CI regression vs. devkit-taught commands for outside authors) so it isn't rediscovered ad hoc in Phase 9.
  - Final slug fixed (replaces `<slug>` in every later phase).
- **STOP-AND-ASK (before Phase 2 starts)**: present the design document to David for explicit sign-off on title, premise, cast, map, and puzzle chain — the ADR-first/no-silent-canon discipline this plan's prompt calls for. Do not begin story-file prose at scale until this checkpoint clears.
- **Exit state**: `story-design.md` exists, committed, and David has signed off on it (or amended it — amendments are folded back into the document before Phase 2 starts). The slug is fixed.
- **Status**: CURRENT

### Phase 2: World-shape skeleton — rooms, regions, doors, darkness, exits (A-group)
- **Tier**: Medium
- **Budget**: 250
- **Domain focus**: `stories/<slug>/<slug>.story` — the compass network, regions, doors, dark rooms, blocked exits, daemons. No objects/NPCs/scoring yet.
- **Entry state**: Phase 1's design document approved; slug fixed.
- **Deliverable**: The full room/region map from the design doc, implementing A1 (compass network + prose), A2/A3 (at least one `through`-form door, including the two-sided lockable-from-inside case per ADR-238), A4 (one dark room + its light source item, declared but not yet fully wired to B-group logic), A5 (one conditional blocked exit), A7 (regions with `containing` + crossing reactions both sides), A8 (a region-gated daemon and a story-level broadcast daemon), A9 (first-time arrival prose). Story builds via `./repokit build`.
- **Test gate (REAL-PATH)**: `tests/transcripts/*.transcript` covering compass navigation, the door's open/close/lock from both sides, the dark room's blocked actions before the light source is available, the blocked exit's condition flipping, and the region-crossing reactions firing on both sides. Run via `node dist/cli/sharpee.js --test --story stories/<slug>/<slug>.story tests/transcripts/*.transcript`.
- **Exit state**: The estate's full navigable shape loads and plays; `./repokit build` and the new story's atomic transcripts are green.

### Phase 3: Objects & concealment (B-group)
- **Tier**: Medium
- **Budget**: 250
- **Domain focus**: containers/supporters, keyless tool gates, concealment/searching, readable clues, the switchable boiler, scenery discipline — placed into Phase 2's rooms.
- **Entry state**: Phase 2 done — the rooms these objects populate exist.
- **Deliverable**: B1 (at least one container and one supporter with capacity config), B2 (at least two keyless tool-gate beats using the CURRENT R3 spelling — e.g. `cuttable with the knife`, `diggable with the trowel` — never the pre-ratchet keyed form), B3 (a concealed item revealed by `search`), B5 (a readable clue item), B7 (the boiler as a bare switchable device — its D5 state-machine depth is Phase 5's job, this phase only gets it on/off), B9 (a scenery pass over every object mentioned in room/object prose — a craft discipline applied throughout, not a single beat).
- **Test gate (REAL-PATH)**: transcripts covering container/supporter capacity limits, each tool-gate's success/failure without the tool, the concealed item's before/after `search`, reading the clue, and switching the boiler. R3 spelling asserted directly (a rejection test confirms the OLD keyed form is not accidentally reintroduced — grep the story source for `with key`/`with tool` as a lint step).
- **Exit state**: Every B-group object exists, is reachable, and its transcripts are green alongside Phase 2's.

### Phase 4: NPCs & conversation (C-group)
- **Tier**: Medium
- **Budget**: 250
- **Domain focus**: the patrol NPC, the guard NPC, give/show gating, and topic tables — independent of Phase 3 (only needs Phase 2's rooms), can be worked before or after Phase 3.
- **Entry state**: Phase 2 done.
- **Deliverable**: C1 (a patrolling NPC with a `route`), C2 (a guard NPC blocking a location, per the catalog's "classic obstacle" framing), C3 (give/show gating that changes the guard's or another NPC's state on receipt — e.g. a bribe), C4 (at least two `define topics` tables using the current ADR-239 table-block syntax with the comma-alias form, one entity-tier row and one free-text-tier row with aliases, plus a catch-all `on asking it` on at least one NPC to prove the miss path).
- **Test gate (REAL-PATH)**: transcripts covering the patrol's route over several turns, the guard's block and its lift condition, the give/show state change, and topic dispatch — hit (entity tier), hit (free-text tier with an alias spelling, not just the primary), and miss (catch-all fires, is not appended to a hit). Drive real `asking`/`telling` actions through the bundle CLI, not a stand-in.
- **Exit state**: All C-group NPCs are present, patrol/guard/topics all exercised REAL-PATH, transcripts green alongside Phases 2–3.

### Phase 5: Time, causality & the machine chain (D-group)
- **Tier**: Large
- **Budget**: 350
- **Domain focus**: the fixed timeline, the fuse, presence-gated recurring events, and the D5 flagship — the boiler's 3-stage `state-machines` depth (`use state-machines`, `define machine`). Depends on Phase 3 (the boiler object must already exist as a bare switchable).
- **Entry state**: Phase 3 done (boiler object exists) and Phase 2 done (rooms/regions the timeline and fuse reference exist).
- **Deliverable**: D1 (a fixed timeline — e.g. dusk falls, the house locks up at night, via `define sequence` / `at turn N` / `N turns later`), D2 (a fuse with real stakes — a countdown to a lose ending, `kill the player when` or equivalent), D3 (a presence-gated recurring event, at least one `, once` variant and one `while <cond>` recurring variant), D5 (the boiler's 3-stage startup as a `define machine` — roles, `starts <state>`, `state` blocks with `when <trigger>: <target>` transitions and `on enter`/`on exit` bodies, order-sensitive per the catalog's framing).
- **Test gate (REAL-PATH)**: transcripts covering the timeline firing on schedule, the fuse counting down and both defusing and NOT-defusing outcomes (the lose ending path — do not skip the failure branch), the recurring event firing correctly under its presence/`while` gate and not over-firing, and the boiler's full 3-stage machine walked through in the correct order plus a rejection of an out-of-order transition attempt.
- **Exit state**: The estate's full causal machinery (timeline, fuse, machine) runs correctly REAL-PATH; transcripts green alongside Phases 2–4.

### Phase 6: State, logic & custom mechanics (E-group)
- **Tier**: Medium
- **Budget**: 250
- **Domain focus**: entity state chains, authored traits with data fields, custom verbs, interception/refusal — depends only on Phase 2 (rooms), independent of Phases 3–5.
- **Entry state**: Phase 2 done.
- **Deliverable**: E1 (the greenhouse vine's multi-stage state chain — seedling → flowering → fruiting, forward-march per the catalog's framing), E2 (an authored trait with a keyed data field — e.g. `waterable with schedule the …` — feeding E1's transitions), E3 (a custom verb — e.g. `prune` or `wind` — via `define verb`/`define action`), E4 (an interception/refusal beat — an entity that vetoes a standard action with an authored message, troll-axe class).
- **Test gate (REAL-PATH)**: transcripts covering each vine-state transition in order (and a rejection test that an out-of-order `change` is refused if the design specifies forward-march-only), the custom verb's grammar and effect, and the refusal firing with its authored message rather than the stdlib default.
- **Exit state**: All E-group mechanics present and exercised; transcripts green alongside Phases 2–5.

### Phase 7: Scoring, endings & text polish (F-group + G1/G2) — full walkthrough chain
- **Tier**: Medium
- **Budget**: 300
- **Domain focus**: owner-attached scoring across everything built so far, both endings, phrase-strategy variety, and gated descriptive detail. This is the phase that ties Phases 2–6's content together into one playable arc.
- **Entry state**: Phases 2–6 all done — the full estate (world, objects, NPCs, machine chain, state logic) exists.
- **Deliverable**: F1 (score attached at multiple narratively-appropriate points — discoveries, puzzle solutions — not clustered arbitrarily), F2 (both endings wired: the win ending for solving the estate's secret, the lose ending tied to D2's fuse expiring), G1 (at least three lines using distinct phrase strategies — `randomly`, `cycling`, `sticky`/`first-time` — per the determinism discipline in Cross-cutting notes), G2 (at least one `{key}`-marker gated snippet whose appearance depends on a prior discovery, e.g. the mantel clue only after the diary is read).
- **Test gate (REAL-PATH)**: a full `walkthroughs/wt-*.transcript` chain (`--chain`) that plays the estate start to finish reaching the WIN ending, plus a second chain (or a branch transcript) reaching the LOSE ending via the fuse — both endings must be REAL-PATH exercised, not just declared in source. Score transcript asserts point totals at named milestones. Phrase-strategy lines asserted structurally per the determinism note.
- **Exit state**: The tutorial story is a complete, playable, scored game with both endings reachable; the full walkthrough chain is green; every one of the 32 selected patterns except G4/G5 (Phase 8) is now implemented and REAL-PATH tested.

### Phase 8: Browser presentation — media, channels, payloaded emits (G4/G5)
- **Tier**: Large
- **Budget**: 350
- **Domain focus**: `play sound/music`, `show image`, `define channel`, `client has`, payloaded `emit ... with` — the browser-first canonical playthrough layer. Depends on Phase 7 (final content) since media cues attach to specific finished beats.
- **Entry state**: Phase 7 done — the complete story exists and is stable.
- **Deliverable**: G4 (declared sound/image/music assets per Phase 1's presentation plan — ambient outdoor birdsong via a region-gated `play ambient`, the folly's photograph via `show image`, at least one `client has` degradation check gating a browser-only beat legibly in text mode), G5 (at least one payloaded `emit ... with` driving a custom client channel/panel — e.g. a discoveries panel populated via `define channel ... from event ...`).
- **Test gate (REAL-PATH, rule 13a — this is an owned integration: the browser client)**: (1) transcript-mode: confirm every media/channel beat degrades legibly in text (the `client has` gates fire correctly, no broken output); (2) real-browser proof: reuse the `g2-clean-machine-proof.mjs` playwright pattern (real Chromium, not a stub) to boot the story's `build --browser` output and assert observable client-side effects for at least one sound cue, one image cue, and the custom channel/panel (DOM/asset-load assertions, not a hand-rolled stand-in for the client renderer).
- **Exit state**: The browser-first canonical playthrough is real and proven; text-mode degrades gracefully; both proof forms (transcript + real browser) are green.

> **Phase 9 doc inputs from the outside review** (`stories/fernhill/chatgpt-review.md`, supplied by David 2026-07-18 — a fresh-eyes read of fernhill.story): (1) teach the THREE vocabulary tiers explicitly — core catalog adjectives (`concealed`, `switchable`), extension/manifest vocabulary (`follower`, `combatant`), and authored traits (`feedable`, `prunable`) — the reviewer could not tell which was which; (2) `phrase` is the one concept that "appears everywhere" and needs a first-class explanation early; (3) lead with the philosophy the reviewer articulated — Chord as a domain MODELING language, not a programming language with story syntax ("if you're reaching for `if`, you haven't found the domain abstraction yet"; facts about the world, not rules that run) — candidate preamble for the tutorial intro and chord-grammar.md; (4) the escape-hatch rule of thumb ("IF concepts belong in Chord; engine concepts in Sharpee; general computation in TypeScript — if Chord can't express an IF concept, improve Chord, don't drop to TS"), which matches the project's standing raise-gaps policy and deserves to be written down where authors see it; (5) note the reviewer's wished-for `unlockable by the key` sugar ALREADY EXISTS as R3's `lockable with the tarnished key` — the docs should headline the declarative-capability family so readers recognize it.

### Phase 9: Tutorial documentation + site nav link
- **Tier**: Medium
- **Budget**: 300
- **Domain focus**: `website/src/pages/docs/tutorials/` (new page(s)) and `website/src/layouts/DocsLayout.astro` (nav entry) — the teaching artifact half of G3, separate from the story itself.
- **Entry state**: Phase 8 done — the finished, playable, browser-proven story exists to document accurately.
- **Deliverable**:
  - Teaching document(s) walking an outside author from `npm i -g @sharpee/devkit` through `sharpee init` (Chord default), writing/reading the `.story` source pattern by pattern (organized by the catalog's A–G groups, cross-referencing `docs/reference/chord-grammar.md` sections directly), `sharpee build --browser`, `sharpee test`, and `sharpee play` — following the `family-zoo.mdx`/`cloak-of-darkness.mdx` structural convention (frontmatter, "The Game," a "What You'll Learn" table mapping each pattern group to where it appears, "Prerequisites") but scaled to this tutorial's larger pattern set. Page-count/structure (single long page vs. a chapter-per-group set under a new subdirectory) is an implementation decision made in this phase, not pre-decided by this plan — record the choice made and why.
  - Nav entry added to `DocsLayout.astro`'s `Tutorials` section, matching the existing `{ label, href }` shape.
  - **Doc-accuracy REAL-PATH check (rule 13a)**: run the actual `sharpee init` → `sharpee build --browser` devkit commands (not the in-repo `./repokit` build) against a fresh copy of the finished story content, confirming the tutorial's own documented commands work end to end for this specific story — a lightweight, story-scoped rerun of the G2 proof pattern, not a full clean-machine reproof (G2 already proved the pipeline generically).
- **Test gate**: the doc-accuracy check above passes; the new nav link resolves and the page(s) render (site build/dev-server check).
- **Exit state**: The teaching document(s) exist, are linked from the live site nav, and their commands are proven to work against the actual finished story.

### Phase 10: Closure — full regression, G3 confirmation, pointer return
- **Tier**: Small
- **Budget**: 150
- **Domain focus**: no new content — verification and handoff only.
- **Entry state**: Phases 1–9 all done.
- **Deliverable**: Full regression per house style — `./repokit build`; `chord`, `story-loader`, `stdlib`, `world-model`, `devkit` suites green; cloak 81/81 (`--story stories/cloak-of-darkness/cloak.story`); zoo 71/71 atomic + 56/56 chained; the new tutorial story's own atomic transcripts AND its full walkthrough chain (both endings) all green together in one pass. **Flag to David rather than silently editing**: ADR-233's G3 line ("closes only when the tutorial ships") is satisfied by this plan's completion — surface for confirmation, do not unilaterally mark it satisfied, mirroring the G1/G2 closure convention in the door-loading and author-pipeline plans. Update the umbrella's Phase 7 status note in `docs/work/chord-go-live/plan.md` recording this workstream's completion (mirroring the Phase 4/5/6 handoff notes already there).
- **Exit state**: David confirms G3 SATISFIED; `docs/context/.current-plan` is repointed to `docs/work/chord-go-live/plan.md`; only G4 (release, umbrella Phase 8) remains before Chord goes live.
- **Status**: PENDING

## Dependency summary
- Phase 1 gates everything else — no story prose at scale before its stop-and-ask clears.
- Phase 2 (world shape) gates Phases 3, 4, and 6 — each needs rooms to place its content in, but Phases 3, 4, and 6 are otherwise **independent of each other** and can run in any order (or be interleaved across sessions) once Phase 2 is done.
- Phase 5 depends on Phase 2 (rooms) AND Phase 3 (the boiler object must exist before it gains machine depth) — it does not depend on Phase 4 or 6.
- Phase 7 depends on Phases 2–6 ALL being done — it is the phase that ties the whole estate together into one scored, two-ending arc.
- Phase 8 depends on Phase 7 (media cues attach to finished beats).
- Phase 9 depends on Phase 8 (docs must describe the finished, browser-proven story accurately).
- Phase 10 is terminal — depends on all of 1–9.
- Total budget across all phases: 2,700 tool calls (Medium 250×5 + Medium 300×2 + Large 350×2 + Small 150) — larger than every prior go-live child workstream (extension-surface: 2,250; door-loading: 1,250; author-pipeline: 780) but consistent with G3 being explicitly "bigger than a demo" and the largest remaining pre-launch gate.
- Two explicit stop-and-ask/checkpoint points: Phase 1's David-review of title/premise/cast/map/puzzle-chain (blocking, before Phase 2), and Phase 10's G3-confirmation flag (blocking the umbrella's advance to G4, not self-ruled).

## Status tracking
### Phase 1: Story design — title, premise, cast, map, puzzle chain (David-review checkpoint)
- **Status**: DONE (2026-07-18, session 80ff54) — `story-design.md` written ("The Folly at Fernhill", slug `fernhill`; 13 rooms / 2 regions + folly, 3 NPCs, coupled D5→A5+E1 boiler/greenhouse chain, fuse finale, 8 scores, two endings; story location split recorded). Compared against the 124-pattern design catalog (web-save design-patterns page) at David's request — cheap wins folded in: B4 added to the selection (Verity's overcoat + carried letter) + an unnumbered red-herring beat; external pattern ids NOT referenced (ifwiki renumbering pending). **SIGNED OFF by David** (title/premise/cast/map/puzzle chain), same session.

### Phase 2: World-shape skeleton — rooms, regions, doors, darkness, exits (A-group)
- **Status**: DONE (2026-07-18, session 80ff54) — `stories/fernhill/fernhill.story` skeleton: 13 rooms + 2 regions (+ folly interior, regionless by design), all three doors (`through` form: cellar locked-by-default with the tarnished key; pantry `lockable, starts unlocked` two-sided; warped folly door), dark Cellar + oil lamp (`light-source, switchable`), the A5 frost-sealed exit reading LIVE boiler state (`north is blocked while the boiler is off:` — Phase 5's machine layers onto the same switchable), Grounds/House crossing reactions (entering + leaving), region wind daemon + story bell daemon (both chance-gated), Iron Gates `first time` prose, player + solicitor's letter. 4 transcripts, 65/65 green (incl. the full A3 hide beat: keyless lock from inside, door_locked refusal, unlock) (`--story stories/fernhill/fernhill.story`). Gotchas recorded: `containing` lists don't continue across lines via trailing comma (use additive `containing` lines); the negative assertion form is `[OK: not contains "…"]` — `[NOT: …]` is silently DROPPED when an `[OK:]` sits beside it; the first `look` still renders `first time` prose (first visit); the switch-on line for the boiler is its own flavor text ("hums to life").

### Phase 3: Objects & concealment (B-group)
- **Status**: DONE (2026-07-18, session 80ff54) — trunk (`max items 3`) + mantel (`capacity 2`) B1; crowbar→crate→opener→tin B2 chain in R3 spelling (explicit-instrument syntax: `open X with Y` — grep lint confirms zero pre-ratchet keyed forms); tarnished key concealed on the doormat + winding key concealed in the grey overcoat (search reveals both) + study curtains `hiding-spot with position behind` B3; overcoat wearable (B4, wears) + solicitor's letter carried-readable; diary page (`on reading it` prose form) + half-burned bank letter (`readable with text` config form — the red herring) B5; boiler B7 pinned in frost-seal; 15-entity B9 scenery pass (auction notice readable). 7 transcripts, 129/129 green; cloak/zoo gates re-proven. **PLATFORM FIX en route (David-approved)**: story-loader placement pass now constructs via `AuthorModel.moveEntity` — load-time contents of CLOSED containers were silently dropped (world.moveEntity refusal, no diagnostic; the TS path always used AuthorModel). New loader test `closed-container-contents.test.ts`; story-loader 275/275. Gotchas: placement lines need the article form (a possessive name like `Verity's overcoat` misparses as a trait adjective — renamed `the grey overcoat`); takes from containers/supporters phrase as "You take X from Y", not "Taken"; tool-openables require the explicit instrument.

### Phase 4: NPCs & conversation (C-group)
- **Status**: DONE (2026-07-18, session 80ff54) — Mrs Kettle (guard; conditional blocked exit on her `guarded` state — the beat that surfaced and then proved ADR-240's live derived state), sherry give-gate (`on giving it` + possession-conditioned `change`/override), Tobias (patrol route ×3, `wait-turns 5 and announces-movement true`) with the full ADR-239 topics table (entity-tier boiler row asked in-scope, free-text folly/fire alias row with body-form `change it to shaken`, bank red-herring row, catch-all shrug; Kettle's second no-catch-all table proves the stdlib default miss), folly-door unjam via `on opening it while tobias is not shaken`. npcs.transcript 41/41; all 8 fernhill transcripts 175/175. Gotchas: transcript `[UNTIL]` needs a `[DO]` opener; NPC announcements default OFF; arrival text "enters from the <dir>"; **flag: "The Tobias enters…" proper-name article bug (entityInfoFrom class, cosmetic, platform)**. Smoke the cat deferred to Phase 6 with her feedable trait (one entity, one phase).

### Phase 5: Time, causality & the machine chain (D-group)
- **Status**: DONE (2026-07-18, session 80ff54) — **D5 flagship**: `use state-machines` + `define machine the boiler works` (cold→filled→primed→running on turning-the-stopcock / pushing-the-primer / switching_on-the-furnace; on-enter bodies mirror machine truth onto the boiler's visible entity states AND speak; order violations get their own voices — `on pushing it while the boiler is cold → primer-dry`, `on switching_on it while cold-or-filled → boiler-clank`; turning needs an interceptor on the target, pushing needs `pushable` — both taught). **D2**: the fuse (`after entering it, once` lights it; `when the fuse becomes lit` anchored sequence + `3 turns later kill the player fuse-blast when the fuse is lit`; `cuttable with the garden shears` + `on cutting it` cut state; BOTH branches transcript-proven incl. the death). **D1**: `define sequence the long night` (`at turn 14` dusk + story evening→midnight; story `states:` added). **D3**: cellar drip (`on every turn`, cycling, presence-gated) + `on every turn while the oil lamp is lit, once` startle (proven non-repeating). frost-seal/npcs upgraded to the full startup order. 12 transcripts, 254/254.

### Phase 6: State, logic & custom mechanics (E-group)
- **Status**: DONE (2026-07-18, session 80ff54) — **E1** the vine (seedling→flowering→fruiting forward chain: the machine's `running` on-enter flowers it — the D5↔E1 coupling from the design — and pruning fruits it, `move`-ing the silver locket into the Greenhouse; per-state `select on its state` voices incl. the exhausted vine). **E2** `define trait feedable` (data `food: entity`, states peckish/fed, trait-scoped phrases, `the actor must have its food` gate) composed as `feedable with food the kipper` on Smoke (`a person, follower` — follows once seen; fed-state greenhouse nose hint `, once`, proven non-repeating after the follower's one-turn lag, caught with DO/UNTIL). **E3** two custom verbs: `define action pruning` (prune/trim) + `define action winding` (wind/wind up), each with reachable constraints and authored `otherwise refuse` voices — clause bodies live in composed traits `prunable`/`windable` (the loader's dispatch fail-fast teaches this itself: entity `on` clauses never fire on the dispatch path); the wound clock ticks with a chance-gated chime daemon. **E4** the furnace poker refuses taking while the boiler runs (troll-axe class, authored message). e-group (37) + smoke (full tool-chain feed, 134 w/ loop) transcripts; fernhill total 14 transcripts, 425/425.

### Phase 7: Scoring, endings & text polish (F-group + G1/G2) — full walkthrough chain
- **Status**: DONE (2026-07-18, session 80ff54) — **F1**: 8 owner-attached scores (key/cellar door/boiler/vine/Kettle/Tobias/fuse/deed = max 50, auto-summed) with awards living in the owners' own clauses (`after taking it, once`, topic-row `award truth, once`, the boiler's award folded into its `on switching_on it` via the D6 prohibition form `refuse when …:`). **F2**: the deed box (`openable with the silver locket` — R3 again) + the deed; WIN = `after entering it while the player has the deed` on the Iron Gates; THREE endings total proven REAL-PATH (win walkthrough 76/76 with a perfect 50; fuse blast kill; dawn-at-turn-130 lose with all three timeline beats on schedule). **G1**: randomly (wind) / cycling (drip, hiss) / first-time (cold-returns) asserted structurally. **G2**: the hall's `{mantel-hint}` marker snippet gated `while the diary page is read` (+ the photograph's `detail while` — the D9 gate renamed my `unread` to `folded` en route: states name what a thing IS). 15 atomic transcripts 456/456 + wt-01 walkthrough 76/76. **PLATFORM FINDINGS flagged, not fixed**: (1) `on X it` + `after X it` on the SAME owner+gerund — the dispatching interceptor's armFor first-match silently SHADOWS the second clause (either direction; worked around by single-clause folding, which reads better anyway); (2) a capped `[DO]…[UNTIL]` loop records a runner error but does NOT fail the transcript — a false-green source (the smoke hint sat on it until the dawn-lose exposed it; hint actually fires on the entry turn).

### Phase 8: Browser presentation — media, channels, payloaded emits (G4/G5)
- **Status**: DONE (2026-07-18, session 1e7652) — **G4**: four declared assets (`define sound night-wind`/`boiler-thump`, `define music dawn-theme`, `define image folly-photograph`; real WAV/PNG files under `stories/fernhill/assets/`), each beat `client has`-gated with its prose as the named degradation (Grounds crossing `play ambient`/`stop ambient`; machine `running` on-enter `play sound`; `after examining it` on the photograph `show image`; win clause `play music`). **G5**: the wound case clock's chime daemon emits `estate.clock with hour "evening"/"past midnight"` gated on the bare story-state conditions; `define channel clock` (`mode replace, gated by sidebar, from event estate.clock, take hour`); the story-side renderer lives in `stories/fernhill/src/browser-entry.ts` (the chord scaffold's designed seam) and mounts `#estate-clock` in the status bar. **Gate (both forms green)**: (1) `media-degrade.transcript` — every beat's prose degradation with no asset-name/event-key/payload leakage; fernhill 472/472 + wt-01 76/76, cloak 81/81, zoo 71/71 + chained re-proven; (2) `scripts/g3-fernhill-browser-proof.mjs` (g2 playwright pattern; build via the real devkit init→build --browser inside the repo, chord-build.test.ts precedent) — PASS, log at `docs/work/chord-tutorial-story/proof/g3-phase8-browser-proof.log`: boot (in-browser compile), image cue (`<img>` mounted + PNG fetched/painted), clock panel (`"The clock: past midnight"` — the panel also witnesses the turn-14 story-state flip), sound cue (WAV fetched on lighting the boiler). **PLATFORM SEAM RAISED, not bridged**: chord's `play ambient` lowers to a channel-less `media.ambient.play {src}` that no pre-registered channel consumes (stdlib's `ambient:<id>` channels filter on a `channel` field chord never emits; loader registers none) — ambient audio cannot reach the browser today; the story line ships and degrades correctly, browser assertion deferred to David's ruling. Also noted: repokit's `--browser` has no chord branch (in-repo chord stories build their page via the devkit path only). Gotchas: container takes phrase as "You take X from Y"; the image renderer mounts under `#img-layer-main`; resource-timing assertions need their own wait (fetch lags the DOM mount).
  > **Closure note (2026-07-18, session 1e7652, CONFIRMED by David)**: the raised ambient seam is CLOSED by ADR-241 (dynamic channels — ACCEPTED and implemented same session, `docs/work/chord-dynamic-channels/plan.md`). The browser proof now asserts the ambient beat (AC-1: `ambient:main` bed, `audio/night-wind.wav` fetched in real Chromium) with `fernhill.story` byte-identical; the repokit chord-browser note stands as a known parity gap, unchanged.

> **Phase 9 doc inputs from the outside review** (`stories/fernhill/chatgpt-review.md`, supplied by David 2026-07-18 — a fresh-eyes read of fernhill.story): (1) teach the THREE vocabulary tiers explicitly — core catalog adjectives (`concealed`, `switchable`), extension/manifest vocabulary (`follower`, `combatant`), and authored traits (`feedable`, `prunable`) — the reviewer could not tell which was which; (2) `phrase` is the one concept that "appears everywhere" and needs a first-class explanation early; (3) lead with the philosophy the reviewer articulated — Chord as a domain MODELING language, not a programming language with story syntax ("if you're reaching for `if`, you haven't found the domain abstraction yet"; facts about the world, not rules that run) — candidate preamble for the tutorial intro and chord-grammar.md; (4) the escape-hatch rule of thumb ("IF concepts belong in Chord; engine concepts in Sharpee; general computation in TypeScript — if Chord can't express an IF concept, improve Chord, don't drop to TS"), which matches the project's standing raise-gaps policy and deserves to be written down where authors see it; (5) note the reviewer's wished-for `unlockable by the key` sugar ALREADY EXISTS as R3's `lockable with the tarnished key` — the docs should headline the declarative-capability family so readers recognize it.

### Phase 9: Tutorial documentation + site nav link
- **Status**: DONE — documentation half (2026-07-18, session 1e7652); **site half ON HOLD by David's ruling same session** ("the plan is to redo the entire site — there's an ADR for it; this can hold"): the plan's `website/` references were STALE (the live Sharpee.net site is the static `site/` tree, and the whole site is being redone under the site-restructure ADR), so no nav link or site pages land now — the finished docs pour into the new site when it exists.
  - **Structure decision (recorded)**: chapter-per-group set — 8 pages at `docs/tutorials/fernhill/` (`index.md` overview + `world`/`things`/`people`/`time`/`state`/`endings`/`browser`), relative-linked, site-agnostic markdown.
  - All five ChatGPT-review inputs landed in the overview: the modeling-language preamble ("if you're reaching for `if`…"), the three vocabulary tiers table, `phrase` explained first-class before chapter 1, the escape-hatch rule of thumb (Chord/Sharpee/TypeScript), and the declarative-capability family HEADLINED (things chapter: "if you wish for `unlockable by the key`, you have it — `lockable with the tarnished key`").
  - Chapters teach current spellings only (R2 `through`, R3 keyless `with`, ADR-239 topic tables, ADR-240 live blocked exits, ADR-241 ambient beds/`define ambient|layer`/dynamic channels + generic panel).
  - **Doc-accuracy REAL-PATH check PASS**: fresh scaffold + finished story/assets/tests via the real devkit — `init` → `build` (compile gate) → `test` (all 16 transcripts, 472 green through the devkit loader path) → `build --browser` (page + assets verified). Story-scoped per plan; G2 covered the generic pipeline.
  - Six draft `.mdx` files briefly written under the stale `website/` tree were ported to the markdown set and removed (untracked, same-session artifacts).

### Phase 10: Closure — full regression, G3 confirmation, pointer return
- **Status**: DONE (2026-07-18, session 1e7652) — one-pass regression ALL GREEN: `./repokit build`; suites chord 370 / story-loader 287 / stdlib 1537(+27sk) / world-model 1390(+10sk) / devkit 39(+1sk) / engine 513(+7sk) / platform-browser 81 / channel-service 94(+2sk); cloak 81/81; zoo atomic + chained; fernhill 16 atomic transcripts all-pass (485 this run — DO/UNTIL chance-loop count variance, zero failures) + wt-01 76/76; the browser proof PASS'd earlier this session with its committed log. Umbrella Phase 7 note updated; `.current-plan` → `docs/work/chord-go-live/plan.md`. **G3 flagged to David, not self-ruled**: all clauses satisfied except the site-nav link, which rides the site-redo ADR per David's same-session ruling — awaiting his disposition of that one clause.
