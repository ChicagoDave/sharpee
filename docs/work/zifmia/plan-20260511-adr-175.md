# Session Plan: Zifmia Multi-User Product Rebuild (ADR-175)

**Created**: 2026-05-11
**ADR**: `docs/architecture/adrs/adr-175-zifmia-multiuser-product.md` — ACCEPTED
**Overall scope**: Greenfield implementation of the Zifmia multi-user web product at `tools/zifmia/`, from empty directory through all 14 acceptance criteria. Single Docker image, stateless-engine per ADR-164, HTTP command path, WebSocket for live coordination, SQLite default with Postgres adapter.
**Bounded contexts touched**: N/A — infrastructure/tooling (deployable application, not a library)
**Key domain language**: N/A (infrastructure framing); relevant contracts from consuming ADRs: TurnPacket, CmgtPacket, ChannelService, save_blobs, named_saves, room, identity, lock, presence

---

## Open Questions — Decide Before Each Affected Phase

These are ADR-175 "Open Questions for Implementation." They are surfaced under the phase where they become binding. A decision is needed **before that phase begins**, not during it.

| # | Question | ADR Recommendation | Binding In | Resolution |
|---|---|---|---|---|
| OQ-1 | Story upload UX: single-step install vs. staging gate | ADR: staging gate (upload → validate → promote) | Phase 5 | **RESOLVED 2026-05-11: direct install (one POST)** — single `POST /admin/stories` validates and installs atomically; 422 on first validation failure. See ADR-175 §Resolved Open Questions. |
| OQ-2 | Worker pool sizing: `os.cpus().length - 1` default, tune via env | RESOLVED 2026-05-11: env var name is `ZIFMIA_WORKER_COUNT` | Phase 3 | RESOLVED 2026-05-11 in Phase 3e. |
| OQ-3 | Admin UI location: bundled (role-gated routes) vs. separate admin port | ADR: bundled for v1; separate port is v2 option | Phase 5 | **RESOLVED 2026-05-11: bundled, role-gated** — `/admin/*` routes guarded by `isAdmin` middleware reading `identities.is_admin` column (new in Phase 5). Separate admin port deferred to v2. |
| OQ-4 | Web client framework: React (default) vs. vanilla web components | ADR: React default, but spike first; see Phase 2 | Phase 6 | RESOLVED 2026-05-11 in Phase 2. |
| OQ-5 | Save migration on bundle upgrade: admin "migrate" button vs. v2-only | ADR: recommend v2 (saves stay pinned to prior bundle) | Phase 4 | **RESOLVED 2026-05-11: v2-only** — rooms stay pinned to their original bundle for life; no migrate-saves admin button in v1. Preserves AC-5 invariant exactly. |
| OQ-6 | Audit log scope: which events to capture beyond story install/remove | Obvious: story CRUD, room kill, identity reset; commands out | Phase 5 | **RESOLVED 2026-05-11: ALL FOUR classes, including full command text** — `story.install/upgrade/remove`, `room.create/kill`, `identity.passcode_reset`, `command.submit` (detail JSON carries the full command string + turn + submitter). Engine-throw paths do NOT emit audit. Departure from ADR's "commands out" recommendation; rationale in ADR-175 §Resolved Open Questions. |

---

## Phases

### Phase 1: Repository Scaffold and Storage Adapter Foundation
- **Tier**: Medium
- **Budget**: 250 tool calls
- **ACs satisfied**: AC-1 (partial — `/health` reachable), AC-8 (adapter interface defined, SQLite implemented)
- **Entry state**: `tools/zifmia/` is empty; ADR-175 is ACCEPTED; `packages/channel-service`, `packages/world-model`, and `@sharpee/sharpee` build cleanly on `main`
- **Deliverables**:
  - `tools/zifmia/` package structure: `package.json` (name `@sharpee/zifmia`), `tsconfig.json`, `src/` tree skeleton
  - `StorageAdapter` interface covering: identity CRUD, room CRUD, save_blobs table (per-turn append + read-at-turn), named_saves CRUD, story library metadata + bundle blobs, chat log append/query, audit log append/query
  - `SqliteAdapter` implementing the interface (better-sqlite3 or bun:sqlite; decide before starting)
  - `PostgresAdapter` stub (interface-conformant, throws "not implemented" on all methods)
  - Schema migrations: single `migrate.ts` entry point; SQLite runs it on startup, Postgres runs it as a documented admin step
  - `GET /health` returning `{ ok: true, db: 'sqlite' | 'postgres', version: string }` — the minimal HTTP server wired to the adapter
  - `docker-compose.yml` and `Dockerfile` that pass `docker compose up` and reach `/health` within 30 s on a 2-vCPU host
  - Behavior tests: StorageAdapter contract suite (runs against SqliteAdapter; same suite reusable for Postgres via adapter injection)
- **Exit state**: `docker compose up` reaches `/health`; SqliteAdapter contract tests pass; PostgresAdapter compiles and passes the same suite when connected to a test Postgres instance; `tools/zifmia/` is committed
- **Status**: COMPLETE (2026-05-11)
- **Results**:
  - 23/23 tests passing (22 adapter contract + 1 `/health`)
  - `node dist/cli.js` smoke test: `/health` returns `{ok:true, db:"sqlite", driverVersion:"3.49.2", version:"0.1.0"}`; SIGTERM exits 0
  - AC-1 partial: `/health` reachable, body shape pinned; AC-8 interface: contract suite parameterized over adapter factory (PostgresAdapter stub conforms, Phase 7 fills in)
  - Deferred: `docker compose up` validation on a 2-vCPU VM — operator step, not unit-testable here

---

### Phase 2: Renderer Skeleton Spike — Framework Decision Gate
- **Tier**: Small
- **Budget**: 100 tool calls
- **ACs satisfied**: Unblocks AC-2, AC-3, AC-10 (all require a working client); gates OQ-4 decision
- **Entry state**: Phase 1 complete; `spikes/channel-io/` findings documented; ADR-165 renderer contract available in `packages/channel-service/src/renderer/`
- **Deliverables**:
  - A throwaway spike under `spikes/zifmia-renderer/` that implements the ADR-165 `Renderer` / `ChannelRenderer` contract for the multi-user transport shape
  - Two parallel sketches in the spike: React (matching `packages/platform-browser/`) and vanilla web components — enough to render a `main` channel text block, a `presence` roster, and a `chat` message
  - A findings note (`spikes/zifmia-renderer/findings.md`) recording: bundle size estimate, author-override ergonomics comparison, complexity to wire up WS events, recommendation
  - No production code written; spike is explicitly disposable
- **Open question resolved here**: OQ-4 (React vs. web components). David reviews `findings.md` and picks the framework **before Phase 6 begins**.
- **Exit state**: `spikes/zifmia-renderer/findings.md` exists and recommends a framework with trade-off reasoning; David has approved the choice; spike code committed as reference only
- **Status**: COMPLETE (2026-05-11)
- **Results**:
  - `spikes/zifmia-renderer/findings.md` records the outcome. React/WC sketches discarded as a dead-end (David surfaced that Shoelace was archived 2026-03-24, then pointed at System 6 as evidence the platform already has a structurally distinct theme working without a framework).
  - **OQ-4 resolved**: no framework. Zifmia matches the `packages/platform-browser/` pattern — plain-TS managers + ADR-170 component vocabulary + native HTML5 (`<dialog>`, `<menu role="menubar">`) + CSS-class theming. Five existing themes (DOS Classic, Modern Dark, Retro Terminal, Paper, System 6) work for free once Zifmia's DOM uses the same classes.
  - **Phase 6 prerequisite added**: ADR-176 (multi-user vocabulary addendum to ADR-170) — extends the 22-class component vocabulary with ~10 multi-user-specific classes. Recommend drafting at Phase 5 exit so Phase 6 starts with the contract pinned. Does not block Phases 3, 4, 5.

---

### Phase 3: Identity, Room, and Turn-Execution Core
- **Tier**: Large
- **Budget**: 400 tool calls
- **ACs satisfied**: AC-9 (transport split), AC-11 (identity rejection), AC-13 (engine-throw recovery), AC-14 (performance baseline)
- **Entry state**: Phase 1 complete; storage adapter passes contract tests; OQ-2 resolved (`ZIFMIA_WORKER_COUNT`)
- **Sub-phase breakdown** (per 2026-05-11 commit-cadence decision):
  - **3a — Identity + session middleware**: schema extension for sessions, `POST /identity/register`, `POST /identity/login` (AC-11), session middleware, scrypt-based passcode hashing — **COMPLETE 2026-05-11** (46/46 tests passing; smoke test confirms identical 401 body for wrong-passcode and nonexistent-handle paths)
  - **3b — Room CRUD + state stub**: `POST /rooms`, `GET /rooms`, `GET /rooms/:id/state` (returns empty CMGT until 3c lands) — **COMPLETE 2026-05-11** (57/57 tests passing; smoke test confirms story-library gating, public-lobby semantics, and auth gating on state route)
  - **3c — Turn lifecycle integration**: `POST /rooms/:id/command` with full stateless engine execution (load save_blob → instantiate WorldModel → execute → snapshot → commit), AC-13 engine-throw recovery, AC-14 perf baseline against Dungeo
    - **3c.i — Stateless turn executor + fixture bundle**: COMPLETE 2026-05-11 (64/64 tests passing; new `executeTurnStatelessly` + `loadStoryFromBundle` + 7 behavior tests against a real esbuild-produced `.sharpee` bundle; Integration Reality Statement recorded — no stubs)
    - **3c.ii — `POST /rooms/:id/command` route + AC-13 engine-throw**: COMPLETE 2026-05-11 (73/73 tests passing; new `command.ts` route with typed precondition errors mapped to 404/410/500; AC-13 verified against a real `crashing-story` fixture whose `onEngineReady` throws — no stubs; route confirms 500 `turn_failed` + zero save_blob rows in the failure path)
    - **3c.iii — AC-14 perf baseline against Dungeo**: COMPLETE 2026-05-11. Harness at `tools/zifmia/perf/baseline-look.ts` + `pnpm perf:baseline`. Dungeo p95 **14.24 ms direct / 16.06 ms HTTP** on Apple M4 + Node 25, well under the 100 ms target. Unblocking required a `tsf` change — added `esmExtensions` option that rewrites relative imports in emitted JS to include explicit `.js`/`/index.js` (Node-ESM strict resolver requires them). See `docs/work/zifmia/perf-baseline.md`. Re-baseline against the 2-vCPU container before AC-14 final sign-off.
  - **3d — WebSocket server**: chat / lock / presence / turn:broadcast / command_echo message kinds, AC-9 transport-split assertion
    - **3d.i — WS plugin + auth handshake + subscription registry + AC-9 rejection**: COMPLETE 2026-05-11 (84/84 tests passing; new `src/server/ws/` module with `@fastify/websocket` registered on the shared Fastify instance; `?token=` query-string auth validated against the sessions table; in-process `SubscriptionRegistry` with forward + reverse indices and `removeAll` cleanup on close; `room:subscribe`/`room:unsubscribe` lifecycle ACKed; **AC-9 verified** — inbound `command:submit` returns `error code: transport_split` with zero save_blob writes; 11 behavior tests cover handshake auth, lifecycle, AC-9, and malformed-frame handling).
    - **3d.ii — Chat + presence**: COMPLETE 2026-05-11 (94/94 tests passing; `chat:send` validates body + room existence + subscription, persists via `appendChatMessage`, fans `chat:message` to ALL subscribers (sender included for canonical-echo pattern); subscribe now requires the room exist (returns `room_not_found` otherwise); `presence:roster` delivered to joiner with deduped identity list; `presence:joined`/`presence:left` broadcast to OTHER subscribers on first-conn/last-conn transitions per identity (multi-tab opens are silent); 10 new behavior tests including the multi-tab dedup case).
    - **3d.iii — Locks + turn:broadcast + command_echo**: COMPLETE 2026-05-11 (104/104 tests passing; new `LockRegistry` with per-room single-holder semantics keyed on connection — `tryAcquire`/`release`/`forceRelease`/`releaseAllFor`; WS `lock:acquire`/`lock:release` handlers broadcast `lock:state` on transitions, send to requester only on contended/idempotent acquire; implicit release on disconnect + on unsubscribe; HTTP `POST /rooms/:id/command` now fans out `command_echo` then `turn:broadcast` to subscribers whose identity ≠ submitter on success, force-releases the lock and broadcasts `lock:state { holder: null }` to ALL subscribers (submitter included); **AC-13 verified end-to-end** — on engine throw, no `command_echo`, no `turn:broadcast`, but `lock:state { holder: null }` still fires so observer inputs unlock; 6 new WS lock tests + 4 new HTTP fan-out tests.)
  - **3e — Worker pool**: `ZIFMIA_WORKER_COUNT` bounded pool, AC-14 multi-worker test — **COMPLETE 2026-05-11** (117/117 tests passing; new `WorkerPool` (`src/server/worker-pool.ts`) — counting semaphore with FIFO queue, capacity from `ZIFMIA_WORKER_COUNT` or `max(1, cpus-1)` default, slot release in `finally` so throws don't leak slots; wired into `command.ts` so every `executeTurnStatelessly` runs under `pool.run(...)`; `ZifmiaServerHandle.workerPool` exposed for inspection/injection; 11 unit tests cover cap/FIFO/throw-release/env parsing; 2 route-level tests assert max-in-flight cap and slot release on failure; companion `pnpm perf:concurrent` harness records local-M4 multi-worker baseline in `perf-baseline.md` — under 8-room load the JS event loop and SQLite WAL are the bottleneck, not the pool cap; 2-vCPU container measurement still gates AC-14 sign-off.)
- **Deliverables**:
  - `POST /identity/login` — ADR-161 `{handle, passcode}` → `{id, handle, sessionToken}`; `401 { error: 'invalid_credentials' }` on bad creds (no handle/passcode distinction)
  - `POST /rooms` — room create with story selection; `GET /rooms` — lobby list
  - `GET /rooms/:id/state` — returns CMGT manifest + transcript replay from in-blob window; used for page-load and WS-reconnect recovery
  - `POST /rooms/:id/command` — full stateless turn lifecycle: acquire lease → load latest save_blob → instantiate WorldModel (ADR-164 invariant: no WorldModel held past this request) → run engine → snapshot → DB commit → release lease → return TurnPacket; engine-throw path: no DB commit, no turn:broadcast, lock:state {holder: null} broadcast, return 500 {error: 'turn_failed'}
  - Worker pool: `WorkerPool` class bounded by `os.cpus().length - 1` (default) or `ZIFMIA_WORKER_COUNT` env; each worker holds no per-room state between requests
  - WS server wired: `chat:send/message`, `lock:acquire/release/state`, `presence:update`, `turn:broadcast`, `command_echo`; `command:submit` over WS returns protocol-error frame (AC-9)
  - Session token validation middleware applied to all protected routes
  - Behavior tests:
    - `POST /identity/login` — valid creds return token; wrong passcode returns 401 same body; nonexistent handle returns 401 same body (AC-11)
    - `POST /rooms/:id/command` happy path — TurnPacket returned, save_blob row written, turn:broadcast sent to non-submitter WS, lock released (AC-9)
    - `POST /rooms/:id/command` engine-throw path — 500 returned, no save_blob written, no turn:broadcast, lock:state {holder: null} broadcast (AC-13)
    - WS command:submit rejected (AC-9)
    - Performance: `look` on Dungeo under 100 ms p95 on a 2-vCPU SQLite container (AC-14); record baseline number in `docs/work/zifmia/perf-baseline.md`
- **Exit state**: AC-9, AC-11, AC-13, AC-14 pass; turn execution is the definitive stateless path; no WorldModel reference outlives a request; all behavior tests green
- **Status**: COMPLETE (2026-05-11) — all sub-phases 3a–3e shipped; 117/117 tests passing; AC-9, AC-11, AC-13 verified against real code paths; AC-14 single-room baseline at 16 ms p95 HTTP on M4 (2-vCPU container retest still required for final AC-14 sign-off — same posture carried forward from 3c.iii).

---

### Phase 4: Save Lifecycle and Transcript Truncation
- **Tier**: Medium
- **Budget**: 250 tool calls
- **ACs satisfied**: AC-4 (crash recovery), AC-6 (save lifecycle), AC-7 (transcript truncation)
- **Entry state**: Phase 3 complete; `save_blobs` and `named_saves` schema in place; turn execution writes save_blobs rows
- **Sub-phase breakdown**:
  - **4a — Transcript truncation + state route hydration**: COMPLETE 2026-05-11 (133/133 tests passing; new `ZifmiaSaveEnvelope` (`src/engine/save-envelope.ts`) wraps engine `ISaveData` with a per-room transcript window; `appendAndTruncate(prior, next, window=1000)` is the truncation primitive; `TurnExecutorInput` now takes `submitter` (route fills from `request.identity`) and an optional `transcriptWindow` for test injection; `GET /rooms/:id/state` returns the latest blob's transcript window verbatim (empty stub when no save_blob exists); hard cutover — `decodeEnvelope` rejects raw-`ISaveData` pre-4a blobs with `unsupported envelopeVersion` (no back-compat per project policy); 10 envelope unit tests + 6 transcript-truncation/state-route tests; AC-7 truncation verified at window=5 with 12 turns → exactly [8..12].)
  - **4b — Named save endpoints**: COMPLETE 2026-05-11 (146/146 tests passing; new `src/server/saves.ts` registers `POST /rooms/:id/saves` (body `{label, atTurn?}`; label trimmed 1–80; atTurn defaults to latest save_blob; explicit atTurn validated against `getSaveBlobAt`) and `GET /rooms/:id/saves` (auth-gated; returns adapter's `listNamedSaves` newest-first). NO `DELETE` endpoint — per policy, saves are room-scoped and the room owns save lifecycle (saved as `feedback_no_save_delete` memory). Errors: 400 `invalid_body` (malformed label/atTurn); 400 `turn_not_saved` (explicit atTurn with no blob); 400 `no_turns_yet` (atTurn omitted on a zero-turn room); 404 `room_not_found`; 401 (no session). 13 behavior tests: latest-turn default, label-trim, explicit-atTurn happy + reject, no-turns-yet, ten malformed body variants, 404 + 401, list ordering, list empty, DELETE-route-absent.)
  - **4c — Restore endpoint**: COMPLETE 2026-05-11 (158/158 tests passing; destructive rollback semantics chosen explicitly — restore truncates forward history rather than branching. New `src/server/restore.ts` registers `POST /rooms/:id/restore` taking `{saveId}` only (matches ADR-175 line 313); resolves the saveId to its `atTurn`, acquires the per-room lease, validates the target save_blob exists and its envelope decodes (corrupt-blob bail-out leaves history untouched), then calls new adapter method `truncateRoomHistory({roomId, keepThroughTurn})` which atomically deletes named_saves where atTurn > target THEN save_blobs where turn > target in a single SQLite transaction (FK-safe order). Returns 200 `{turn: target, blocks: [], events: []}` — clients refetch `GET /state` for the transcript window. New `RoomRestoredMessage` WS kind broadcast to non-submitter subscribers (`{atTurn, by, savedLabel}`); `lock:state {holder: null}` to everyone. Error map: 400 invalid_body / 404 room_not_found / 404 save_not_found / 409 save_room_mismatch / 500 restore_failed (target missing or undecodable; lock still releases). 9 route tests (rollback semantics, post-restore turn numbering, body validation, all error paths, WS fan-out with submitter-exclusion). 3 adapter-contract tests for `truncateRoomHistory` (past-cutoff truncation, idempotence below cutoff, other-room isolation). PostgresAdapter stub updated to satisfy interface.)
  - **4d — Compaction job**: COMPLETE 2026-05-11 (175/175 tests passing; new adapter method `compactRoomSaveBlobs(roomId)` deletes save_blob rows that aren't the latest turn AND aren't referenced by any named_saves row — single atomic SQLite statement with `MAX(turn)` subquery, transaction-wrapped, returns `{deleted: count}`; new `src/server/compaction.ts` worker module with `parseCompactionEnabled`/`parseCompactionInterval` env parsers + `startCompactionWorker({adapter, enabled, intervalMs, scheduler?})` that returns `{running, sweepNow(), stop()}`. Off by default — `ZIFMIA_COMPACTION_ENABLED=true|1|yes` to opt in; `ZIFMIA_COMPACTION_INTERVAL_MS` tunes the period (default 5 min). Overlapping ticks are skipped; per-room errors reported via `onError` but don't abort the sweep; `stop()` drains the in-flight sweep. Server bootstrap wires it into `ZifmiaServerHandle.compaction` and stops it on close. NO lease acquisition — adapter atomicity + SQL serialization are sufficient; compaction never blocks turn execution. 5 adapter contract tests (preserves latest+named, idempotent, empty room, room isolation) + 12 worker unit tests (env parsing, disabled-but-manual-sweep, periodic ticks via fake scheduler, overlap-skip with slow adapter, per-room error containment, stop-drains-in-flight, intervalMs validation). PostgresAdapter stub updated.)
  - **4e — Crash-recovery integration tests**: COMPLETE 2026-05-11 (180/180 tests passing; new `tests/integration/crash-recovery.test.ts` with 5 file-backed tests across two describe-blocks. Integration Reality Statement recorded — OWNED = SqliteAdapter, executor, save envelope, routes; EXTERNAL = none in v1; REAL-PATH TESTs use a tmpdir-backed SQLite file with two `SqliteAdapter` instances bracketing a graceful close + reopen; no adapter stubs, no executor stubs. AC-4 post-commit suite: (a) committed turns + transcript window survive close+reopen and the next /command continues at turn=N+1; (b) named saves persist and `/restore` from a pre-restart save works after reopen; (c) session tokens persist (sessions table durability). AC-4 pre-commit suite: (a) crashing-story 500 leaves no save_blob row, reopen sees clean state, healthy rooms continue working with no leaked locks; (b) five back-to-back crashes accumulate no rows and the room row remains intact and open. Compaction explicitly disabled in integration tests so background ticks don't race the specific row-count assertions. Fixed a pre-existing flake in saves.test.ts ordering assertion — added a 5 ms gap between named-save creates so `created_at DESC` is deterministic under load.)
- **Deliverables**:
  - `POST /rooms/:id/saves` — name a save slot: validates atTurn exists in save_blobs, writes named_saves pointer row
  - `GET /rooms/:id/saves` — list named saves
  - `POST /rooms/:id/restore` — load save_blob at atTurn, run a "restore" turn (world re-initialized to that blob), write new save_blob row, return TurnPacket; restore is a full turn lifecycle (load → execute → snapshot → commit)
  - Transcript truncation: per-turn blob append trims in-blob transcript to 1000 entries; absolute turn counter preserved separately (world scoring capability); mid-session join via `GET /rooms/:id/state` replays the 1000-entry window
  - Compaction job: periodic background task GCs save_blob rows with no named_saves reference and older than the transcript window; implemented but off-by-default in v1 (env flag to enable)
  - Save format: v3 read/write (ADR-164 Decision 7); v2 blobs load with empty transcript per ADR-164 AC-4
  - Crash recovery verification: force-kill mid-turn (before DB commit) → restart → reconnect → room loads at prior save; force-kill post-commit → restart → reconnect → committed turn replays in transcript (AC-4)
  - Behavior tests:
    - Named save round-trip: save → container restart → restore → correct world state (AC-6)
    - Save persists across restart (AC-6)
    - 1500-turn session: in-blob transcript caps at 1000; mid-session join sees 1000 entries (AC-7)
    - Crash pre-commit: room recoverable at prior baseline (AC-4)
    - Crash post-commit: committed turn visible in transcript after restart (AC-4)
- **Open question surfaced**: OQ-5 — does admin get a "migrate saves to new bundle version" button? Confirm v2-only before Phase 5 begins.
- **Exit state**: AC-4, AC-6, AC-7 pass; all behavior tests green; save_blobs and named_saves are the definitive persistence surface
- **Status**: COMPLETE (2026-05-11) — all sub-phases 4a–4e shipped; 180/180 tests passing; AC-4, AC-6, AC-7 verified against the real persistence boundary (file-backed SQLite, two-adapter close+reopen, no stubs); save lifecycle is the definitive persistence surface with envelope + transcript window + named saves + destructive restore + off-by-default compaction GC; ready for Phase 5 (admin + story library) and Phase 6 (web client).

---

### Phase 5: Story Library, Admin UI, and Audit Log
- **Tier**: Large
- **Budget**: 400 tool calls
- **ACs satisfied**: AC-1 (full — admin installs story via UI), AC-5 (story install/upgrade/remove), AC-8 (partial — adapter swap documented), AC-12 (bundle validation)
- **Entry state (2026-05-11)**: Phases 1–4 COMPLETE; 180/180 tests passing; all four binding OQs RESOLVED:
  - **OQ-1**: direct install (one POST)
  - **OQ-3**: bundled admin, role-gated via `identities.is_admin`
  - **OQ-5**: v2-only (no save migration in v1)
  - **OQ-6**: full audit scope — story CRUD + room create/kill + passcode reset + `command.submit` (full command text)
- **Deliverables**:
  - **Schema additions**:
    - `identities.is_admin` boolean column (default 0); migration handled by adapter `migrate()`
    - Bootstrap CLI: `zifmia grant-admin <handle>` (one-shot, documented in deploy guide)
  - **Admin middleware**: `adminMiddleware({ adapter })` — composes with `authMiddleware`, then asserts `identity.is_admin === true`; rejects non-admin with 403 `{error: 'forbidden'}`. Applied to every `/admin/*` route.
  - **Story library routes** (direct install per OQ-1):
    - `POST /admin/stories` — multipart upload + JSON metadata. Validation sequence (structure → signature → IFID → format version) short-circuits on first failure; on any failure no library row written, no blob stored; 422 `{error: 'invalid_bundle', detail: 'bad_structure'|'bad_signature'|'missing_ifid'|'unsupported_format'}` (AC-12).
    - `PUT /admin/stories/:id` — upgrade: new bundle replaces active pointer; in-flight rooms finish on old bundle version (saves pinned to bundle_version); new rooms use new version.
    - `DELETE /admin/stories/:id` — soft-remove: blocks new room creation; existing rooms continue.
    - `GET /admin/stories` — list installed stories (admin view).
  - **Room admin routes**:
    - `DELETE /admin/rooms/:id` — admin kill: soft-close (`closedAt`); existing transcript preserved.
  - **Identity admin routes**:
    - `POST /admin/identities/:id/passcode_reset` — admin-initiated reset; generates a new passcode hash + invalidates existing sessions.
  - **Audit log**:
    - `appendAuditEntry` called from every admin route AND from the command route on success (NOT on engine-throw — only successful turns are audited).
    - Action names (canonical, dotted):
      - `story.install`, `story.upgrade`, `story.remove`
      - `room.create` (called from POST /rooms route, not just admin), `room.kill`
      - `identity.passcode_reset`
      - `command.submit` — `detail` JSON: `{roomId, turn, command, submitter: {identityId, handle}}`
    - `GET /admin/audit` — list audit entries with pagination (`?sinceTs=`, `?limit=`); admin-only.
  - **Admin UI**: role-gated routes (bundled with player UI; same port); pages: story library, active rooms, audit log viewer, room kill action. Implementation deferred to Phase 6 (web client phase) — Phase 5 ships HTTP routes + tests; the UI rendering layer rides on top.
  - **Behavior tests**:
    - Bundle validation: truncated → 422 bad_structure; valid + missing IFID → 422 missing_ifid; v2-format → 422 unsupported_format; after each failure, library row count and blob count unchanged (AC-12).
    - Direct install happy path: 201 + library row + blob persisted + audit row appended.
    - Upgrade: new rooms get new bundle; in-flight rooms load saves from prior bundle version; audit row.
    - Remove: new room creation blocked; existing room continues; audit row.
    - Admin middleware: non-admin identity → 403; admin → through.
    - Passcode reset: admin reset invalidates old sessions; audit row.
    - Command audit: `POST /command` success path → audit row with full command text; engine-throw path (crashing-story) → no audit row.
    - `GET /admin/audit`: returns appended rows in reverse-chronological order; paginates correctly.
- **Exit state**: AC-1 (full), AC-5, AC-12 pass; admin HTTP surface complete; audit log captures all six action classes; admin UI rendering deferred to Phase 6; all behavior tests green.
- **Status**: PENDING

---

### Phase 6: Web Client and Two-User End-to-End
- **Tier**: Large
- **Budget**: 400 tool calls
- **ACs satisfied**: AC-2 (two-user end-to-end), AC-3 (WS reconnect), AC-10 (lock-on-typing)
- **Entry state**: Phases 1–5 complete; framework choice from Phase 2 approved; all server routes and WS message kinds operational
- **Deliverables**:
  - Web client using the approved framework (React or vanilla web components), built and served as static assets from the same container and port
  - ADR-165 Renderer/ChannelRenderer contract wired to multi-user transport: HTTP for turn submission and state load; WS for chat, lock, presence, turn:broadcast, command_echo
  - Client pages: lobby (room list), room creation, room view (main channel, chat panel, presence roster, command input, save management)
  - Lock-on-typing: input focus → `lock:acquire`; other participants see read-only input + "A is typing…" within 200 ms; disconnect → implicit release → inputs re-enabled within 1 s; submit → HTTP POST, then WS turn:broadcast to others + lock release (AC-10)
  - WS reconnect: disconnect/reconnect triggers `GET /rooms/:id/state` (CMGT + transcript replay) then fresh WS subscription; no lost packets observable (AC-3)
  - Client holds no authoritative state: browser refresh recoverable entirely from `GET /rooms/:id/state` (ADR-175 invariant)
  - Author-override entry point: documented extension point for per-story UI customization (ADR principle: platform ships defaults authors can override)
  - Playwright end-to-end tests (patterns from `legacy/tools-server`):
    - Two browsers, same room: each sees the other in presence roster; commands echo via command_echo; chat messages delivered; identical narrative in main channel (AC-2)
    - Browser A disconnects mid-session; reconnects; CMGT + transcript replayed; no lost packets (AC-3)
    - Browser A focuses input → Browser B sees read-only + "A is typing…" within 200 ms; A disconnects without submitting → B sees input re-enabled within 1 s (AC-10)
- **Exit state**: AC-2, AC-3, AC-10 pass via Playwright; client is author-customizable; all behavior tests green
- **Status**: PENDING

---

### Phase 7: Postgres Adapter, Adapter Swap, and Hardening
- **Tier**: Medium
- **Budget**: 250 tool calls
- **ACs satisfied**: AC-8 (adapter swap full), AC-4 (crash recovery with Postgres), AC-14 (performance re-baseline with Postgres)
- **Entry state**: Phases 1–6 complete; PostgresAdapter stub from Phase 1 exists; all ACs except AC-8 and AC-14-Postgres pass
- **Deliverables**:
  - `PostgresAdapter` fully implemented: all StorageAdapter interface methods, Postgres advisory lock for room lease (`pg_try_advisory_lock(room_id)`), schema migrations via a documented `migrate` command
  - Adapter swap documented admin step: `ZIFMIA_DB_URL=postgres://...` env change + `docker compose run zifmia migrate` command; no code change required
  - Data migration tool: SQLite → Postgres export/import covering identities, rooms, save_blobs, named_saves, chat history, audit log, story library metadata and bundles
  - Horizontal scaling notes in `docs/zifmia/deployment.md`: N containers + Postgres + load balancer; each container stateless across requests; advisory lock is the single-writer guarantee
  - Behavior tests (adapter contract suite reused from Phase 1, now run against Postgres):
    - All SqliteAdapter contract tests pass with PostgresAdapter (AC-8)
    - SQLite → Postgres migration: existing rooms, identities, and saves transfer; post-migration instance serves the same rooms (AC-8)
    - Crash recovery under Postgres: same AC-4 scenarios pass
    - Performance re-baseline: `look` on Dungeo under 100 ms p95 on Postgres adapter (AC-14 extended); record in `docs/work/zifmia/perf-baseline.md`
- **Exit state**: AC-8 passes; Postgres adapter passes full contract suite; perf baseline updated; all 14 ACs green across the full test suite
- **Status**: PENDING

---

## AC Coverage Map

| Acceptance Criteria | Phase |
|---|---|
| AC-1 — Single-container deploy + `/health` | Phase 1 (partial) → Phase 5 (full) |
| AC-2 — Two-user end-to-end (presence, chat, narrative) | Phase 6 |
| AC-3 — WS reconnect non-event (CMGT + transcript replay) | Phase 6 |
| AC-4 — Crash recovery (pre/post-commit) | Phase 4 |
| AC-5 — Story install / upgrade / remove | Phase 5 |
| AC-6 — Save lifecycle (name, restore, cross-restart) | Phase 4 |
| AC-7 — Transcript truncation at 1000 turns | Phase 4 |
| AC-8 — Adapter swap (SQLite → Postgres) | Phase 1 (interface) → Phase 7 (full) |
| AC-9 — Transport split (HTTP command, WS fanout only) | Phase 3 |
| AC-10 — Lock-on-typing (200 ms acquire, 1 s implicit release) | Phase 6 |
| AC-11 — Identity rejection (401 same body for both failure modes) | Phase 3 |
| AC-12 — Bundle validation (422 shapes, no partial write) | Phase 5 |
| AC-13 — Engine-throw recovery (500, no commit, lock released) | Phase 3 |
| AC-14 — Performance baseline (<100 ms p95 on 2-vCPU SQLite) | Phase 3 (SQLite) → Phase 7 (Postgres) |

---

## Sequencing Notes

- Phases 1 → 3 → 4 → 5 are a strict dependency chain (each phase's entry state requires the prior).
- Phase 2 (renderer spike) can run in parallel with Phase 3 if David approves it early — it is read-only and produces no production code. It must complete and produce a framework decision before Phase 6 begins.
- Phase 6 (web client) depends on Phases 1–5 but not Phase 7.
- Phase 7 (Postgres) depends on Phase 6 being green (no regression before hardening).
- The compaction job (Phase 4) is gated behind an env flag; it does not block any other phase.

---

## Test Strategy Summary

Tests are not deferred to a final "QA phase." Every phase ships its own behavior tests and integration tests as part of the phase deliverables. Playwright end-to-end tests are introduced in Phase 6 (they require a running client). The performance baseline (AC-14) is recorded as a regression-tracking artifact in `docs/work/zifmia/perf-baseline.md` starting in Phase 3.

The StorageAdapter contract suite introduced in Phase 1 is the single test suite that validates both adapters — it runs against SqliteAdapter in Phase 1 and against PostgresAdapter in Phase 7. No duplicate test code between adapter implementations.
