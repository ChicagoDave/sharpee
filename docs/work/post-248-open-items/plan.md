# Session Plan: Post-ADR-248 open-items sweep

**Created**: 2026-07-21 (session 99aee6 continuation)
**David's go-ahead**: recorded — David has already said "proceed" on this sweep. Two explicit in-flight gates remain inside it (dead-code deletion confirm, Phase 3; ADR-247 implementation go-ahead, beyond Phase 4's plan-authoring deliverable) — this blanket "proceed" authorizes the sweep's own phases, not those two gated actions.
**Baseline**: branch `chord-foundations`, commit `2ccc2767` — chord 444, story-loader 308, engine 524, lang-en-us 430, platform-browser 86, dungeo chain 888/888, full `--browser` build clean (all green per `docs/context/session-20260721-0912-chord-foundations.md`).
**Overall scope**: Close out the four remaining post-ADR-248 open items recorded in `docs/context/session-20260720-2125-chord-foundations.md` (session 171837) and reconfirmed still parked in the 2026-07-21 session: (1) dispose of dungeo's residual module-level state — record the audited-benign majority as-is and remove the audit-flagged dead code, with a confirm gate before deletion; (2) wire armoured/thealderman's browser entries onto the ADR-248 reboot callback in place of the `window.location.reload()` fallback; (3) fix the `ext-testing` package's broken `"import"` exports condition; (4) author (not implement) the ADR-247 (`getContents` worn-items default flip) implementation plan as this plan's final phase, since ADR-247 is a platform world-model API change with a 64-site call audit — large enough to be its own plan, gated on David's separate go-ahead before any of its phases run.
**Out of scope**: the royal-puzzle-basic RNG flake — RULED accepted 2026-07-21 (memory: one-good-run-rule addendum; no transcript logic gate needed, wt-14 chain coverage suffices; NOT a regression to chase). ADR-245/ADR-250 phrasebook work — DONE, its own plan (`docs/work/adr-245-phrasebooks/plan.md`) is fully COMPLETE, this plan repoints `.current-plan` away from it. zifmia (its loader adopts the ADR-248 factory contract at its own design time, per the ADR's Consequences). Generalized `import <file>` and packaged-book tooling (ADR-245 follow-ons, separately parked). ADR-247's actual *implementation* (Phase 4 here authors its plan only).
**Bounded contexts touched**: N/A — infrastructure/platform-lifecycle cleanup and packaging work (dead-code disposition, browser client wiring, npm package build output, plan authoring). No domain modeling; no `docs/ddd/notation.yaml` in this repo (confirmed absent), so DDD decomposition does not apply — phases are named in plain technical terms per the project's own "Dungeo residual module-level state," "browser reboot wiring," "ext-testing packaging," and "ADR-247 implementation plan" language.
**Key domain language**: N/A (see above) — project terms used throughout: factory-only story contract (ADR-248), reboot callback (`BrowserClientConfig.reboot`), one-good-run rule (walkthrough-chain acceptance), `getCarriedAndWorn` partition (ADR-247).

## References consulted
- `CLAUDE.md` (project instructions) — "Never delete files without confirmation. Not even 'to get a build working'" (gates Phase 3's dead-code removal on an explicit confirm step); "Platform changes require discussion first. Any changes to `packages/`... must be discussed with the user before implementation" (gates Phase 2's ext-testing fix on presenting the diff first, and gates ADR-247's actual implementation beyond Phase 4's plan-authoring); "Never auto-retry failed builds or tests... report the error and WAIT" (governs every verification step below); story-level changes (`stories/`) can proceed autonomously — covers Phase 1 and Phase 3 without a platform-discussion gate.
- `docs/architecture/adrs/adr-248-restart-via-platform-events.md` — ACCEPTED. Decision 2 defines the reboot pattern Phase 1 wires armoured/thealderman onto (`BrowserClientConfig.reboot` = the story's own `start()`, confirmed restart deletes the autosave then defers reboot to after the current turn completes, forced onto the new-game branch). Decision 3 (factory-only story export, no module-level mutable state — "a browser bundle cannot re-evaluate modules") is the standard Phase 3's dead-code disposition applies to dungeo's remaining module-level sites. Consequences note zifmia "adopts the pattern at design time" — confirms zifmia is correctly out of scope here.
- `docs/architecture/adrs/adr-247-getcontents-worn-default.md` — ACCEPTED. Decision requires a **committed 64-site audit table** before the flip lands, deletion of `ContentsOptions.includeWorn` and `ClothingTrait` outright (no opt-out surface), and a new `getCarriedAndWorn()` partition method. Acceptance Criteria section is the exact shape Phase 4's authored plan must satisfy. This is why Phase 4 scopes ADR-247 as its own plan rather than absorbing implementation here — the audit alone is session-sized work before any code changes.
- `docs/context/session-20260720-2125-chord-foundations.md` (session 171837) — source of all four open items and their exact framing: the module-state audit result (20 benign monotonic `eventCounter` counters + 9 safe-reseeded refs, "no live hazards"; dead code with the hazard pattern in `handlers/basket-handler.ts` never-registered actions, river-handler/gas-room-handler dead setters), the armoured/thealderman reboot-fallback + tsconfig DOM drift note, the ext-testing packaging workaround location (`packages/bootstrap`'s vitest config alias), and "ADR-247 implementation still unblocked/unscheduled (separate work)."
- `docs/context/session-20260721-0912-chord-foundations.md` (most recent session) — reconfirms all four items still parked under "Post-ADR-248 open-items sweep" in its Open Items / Next section; confirms the ADR-245 plan is fully COMPLETE (justifying the `.current-plan` repoint) and the current green baseline this plan starts from.
- `docs/work/adr-248-restart-reboot/plan.md` (Phase 3: "platform-browser — client-owned reboot") — the implementation record for the reboot-callback pattern; Phase 1 here follows its precedent rather than re-deriving it, applied to the two stories that plan's Phase 2 (7-story factory migration) did not finish wiring onto `reboot`.
- `docs/context/project-profile.md` — confirms the dual-build convention Phase 2 should match (`"build": "tsc --build && tsc -p tsconfig.esm.json"`, per sibling packages e.g. `packages/stdlib`) rather than inventing a new one; confirms TypeScript strict mode and CommonJS/project-reference conventions relevant to the tsconfig DOM-drift investigation in Phase 1.
- Memory: one-good-run-rule (royal-puzzle addendum, ruled 2026-07-21) — governs why the RNG flake is out of scope and why Phase 3's post-deletion verification only needs one clean walkthrough-chain run, not a flake hunt.
- Memory: no-get-it-done-assumptions — governs Phase 3's confirm gate: surface the exact dead-code inventory and STOP for David's explicit confirm rather than assuming "dead code plus a passing suite" is license to delete.

## Phases

### Phase 1: armoured/thealderman — wire the ADR-248 reboot callback
- **Tier**: Small
- **Budget**: 100
- **Domain focus**: Story-level (`stories/armoured`, `stories/thealderman`) — autonomous per CLAUDE.md, no platform-discussion gate.
- **Entry state**: Both stories' `browser-entry.ts` construct `BrowserClient` without a `reboot` field, so a confirmed RESTART falls through to `BrowserClient.dispose`'s `window.location.reload()` fallback (`packages/platform-browser/src/BrowserClient.ts:628-629`) instead of the ADR-248 reboot path. `stories/armoured/src/browser-entry.ts` already calls `createStory()` per-boot inside `start()` (factory contract satisfied) but has no `reboot:` line. `stories/thealderman/src/browser-entry.ts` is in the same shape. `stories/thealderman/tsconfig.json` has no `"DOM"` in its `lib` array (only `["ES2022"]`) despite `browser-entry.ts` referencing `document`, `HTMLInputElement`, `HTMLDialogElement` — the "pre-existing tsconfig DOM drift" from the 171837 audit. `stories/armoured/tsconfig.json` extends `tsconfig.base.json` (needs checking for the same gap).
- **Deliverable**:
  1. Add `reboot: () => start()` to the `BrowserClient` constructor config in both `stories/armoured/src/browser-entry.ts` and `stories/thealderman/src/browser-entry.ts`, matching the `stories/fernhill/src/browser-entry.ts` / `docs/work/adr-248-restart-reboot/plan.md` Phase 3 pattern exactly (comment citing ADR-248, same as fernhill's).
  2. Investigate the tsconfig DOM drift on both stories (start with thealderman's missing `"DOM"` lib entry; confirm or rule out the same gap in armoured, which extends the shared `tsconfig.base.json` but sets no `"lib"` override of its own — root `tsconfig.base.json` itself sets no `"lib"` array either, so this is likely a per-story gap, not a base-config one). Fix by adding a local `"lib"` override (e.g. `["ES2022", "DOM"]`) to each story's own `tsconfig.json` — a story-scoped file, autonomous per CLAUDE.md. **Boundary**: if investigation instead shows the fix requires editing the shared root `tsconfig.base.json` (a file every `packages/` and `stories/` consumer inherits), that is a platform change — stop and discuss with David before touching it, per CLAUDE.md's platform-discussion rule; do not silently widen this story-level phase into a platform-config change.
  3. Decide, in-plan, whether cloak-of-darkness's 11 pre-existing stale unit tests (agent-verified against the stash baseline, 171837) belong in this phase or stay parked. Recommendation to record: they are a separate story (`cloak-of-darkness`) with a pre-existing failure class unrelated to the reboot-wiring fix in this phase's scope — leave parked, note explicitly in the phase's completion record rather than silently dropping it.
  4. Build and smoke-verify both stories (`./repokit build` for each, or the equivalent per-story browser build) and confirm no new type errors.
- **Exit state**: armoured and thealderman both reboot via the ADR-248 callback path (verified by triggering RESTART against each and observing the story's own `start()` re-run rather than a full page reload — a transcript-level restart test if one exists for these stories, otherwise a manual/documented check since neither story currently has restart transcript coverage per the 171837 note on "zero-test stories"). Both stories build clean with no DOM-lib type errors. The cloak stale-test disposition is recorded (parked, with reason), not silently dropped.
- **Status**: COMPLETE (2026-07-21 — both entries wired with the cloak ADR-248 pattern (module client singleton + reboot: () => start() + first-boot-only construct/initialize); DOM lib added locally to both tsconfigs; armoured fully typechecks (incl. dropping the stale modalOverlay property — removed from DOMElements at ADR-170); thealderman browser-entry.ts typechecks clean — its 6 remaining errors are pre-existing `.topic()` calls against a removed character-package conversation API, PARKED as recorded story-port drift alongside cloak's 11 stale unit tests. NOTE: cloak's own tsconfig also lacks DOM (repo-wide story pattern, out of this phase's scope))

### Phase 2: ext-testing — fix the broken `"import"` exports condition
- **Tier**: Small
- **Budget**: 100
- **Domain focus**: Platform package (`packages/extensions/testing`, published as `@sharpee/ext-testing`) — platform change, CLAUDE.md's discussion-first rule applies: present the chosen fix as a small diff in-conversation before applying.
- **Entry state**: `packages/extensions/testing/package.json`'s `exports["."]["import"]` points at `./dist-esm/index.js`, but the package's build script (`"build": "tsc"`) never produces a `dist-esm/` directory — only `dist/` (CJS). `packages/bootstrap/vitest.config.*` carries an alias (`'@sharpee/ext-testing': resolve(__dirname, '../extensions/testing/dist/index.js')`) that routes around the broken condition rather than fixing it.
- **Deliverable**: Present the chosen fix in-conversation (per CLAUDE.md's platform-discussion norm) before editing. Preferred fix, matching the sibling-package dual-build convention confirmed in `docs/context/project-profile.md` (e.g. `packages/stdlib`'s `"build": "tsc --build && tsc -p tsconfig.esm.json"`): add a `tsconfig.esm.json` to `packages/extensions/testing/` (ESM-emitting variant of its existing `tsconfig.json`) and change its `package.json` `"build"` script to `"tsc --build && tsc -p tsconfig.esm.json"`, so `dist-esm/` is actually produced and the `"import"` condition resolves to a real file. Alternative (if the sibling-convention diff turns out non-trivial for this package's structure): drop the `"import"` condition from the exports map entirely, since nothing outside the workspace currently needs an ESM entry point. Whichever is chosen, then remove the `packages/bootstrap/vitest.config.*` alias workaround.
- **Exit state**: `@sharpee/ext-testing`'s exports map resolves correctly under both `require` and (if kept) `import` without an alias workaround. The bootstrap vitest alias is deleted. `packages/bootstrap` and `packages/extensions/testing` test suites both run and pass without the alias in place.
- **Status**: COMPLETE (2026-07-21 — chose the sibling dual-build: new tsconfig.esm.json + build script `tsc && tsc -p tsconfig.esm.json` + removed the ts-forge esm skip (the ESM build passes clean — the skip wasn't protecting anything); bootstrap vitest alias removed; bootstrap 34/34 green resolving through the real exports map. NOTE: ext-testing itself has zero test files — its `test` script always failed; pre-existing, recorded)

### Phase 3: Dungeo residual module-level state — disposition + confirm-gated dead-code removal
- **Tier**: Medium
- **Budget**: 250
- **Domain focus**: Story-level (`stories/dungeo`) — autonomous per CLAUDE.md for the audited-benign disposition and for presenting the deletion inventory; the deletion itself requires David's explicit confirm per CLAUDE.md's "never delete files without confirmation" (no exception for "the audit already says it's dead" — that is exactly the no-get-it-done-assumptions trap: STOP and surface, don't assume the audit is standing confirmation).
- **Entry state**: The 171837 session audit (`docs/context/session-20260720-2125-chord-foundations.md`) already classified every remaining module-level state site in `stories/dungeo`: 20 benign monotonic `eventCounter` counters + 9 safe-reseeded refs (no live hazards — ADR-248's factory contract is satisfied because these reset correctly per-boot) and three dead-code sites carrying the pre-ADR-248 hazard pattern without live risk (unreachable code, not a runtime bug): `stories/dungeo/src/handlers/basket-handler.ts` plus `stories/dungeo/src/actions/basket/{lower-basket-action.ts,raise-basket-action.ts,index.ts,types.ts}` (confirmed this session: `lowerBasketAction`/`raiseBasketAction` are exported from `actions/basket/index.ts` but never imported or registered in `stories/dungeo/src/index.ts` — genuinely dead, not just under-audited), `stories/dungeo/src/handlers/river-handler.ts`'s module-level `waterRoomIds` set, and `stories/dungeo/src/handlers/gas-room-handler.ts`'s module-level `gasRoomId` ref.
- **Deliverable**:
  1. **(a) Record the disposition for the 29 benign sites**: no code change. Write a short disposition note (in this plan's phase record, or a `docs/work/post-248-open-items/dead-code-disposition.md` companion file) stating: these counters/refs are module-level but reset correctly per story-instance boot, migrating all 29 to instance/closure state is churn without a hazard per the 171837 audit, and they are left as-is. Cite the audit.
  2. **(b) Present the exact dead-code inventory to David and get explicit confirm before deleting anything.** The inventory: the 5 basket-puzzle files listed above (confirmed never registered — `LOWER BASKET`/`RAISE BASKET`/`TURN WHEEL` are not reachable commands in the shipped story), plus the two dead setters (`river-handler.ts`'s `waterRoomIds`, `gas-room-handler.ts`'s `gasRoomId` module-level state, if confirmed dead-not-live during this phase's own re-check — re-verify both are genuinely unused after ADR-248's dungeo migration, since the 171837 audit is now roughly a day old and dungeo has had further changes since). Do not delete anything until David confirms.
  3. Once confirmed, delete the confirmed-dead files/code and any now-orphaned imports or references (e.g. wherever `basket-handler`'s exports or the basket action pair might still be referenced in barrels or docs).
  4. Verify: `stories/dungeo` unit suite green, dungeo walkthrough chain (`node dist/cli/sharpee.js --test --chain stories/dungeo/walkthroughs/wt-*.transcript`) — one clean run satisfies the one-good-run rule (royal-puzzle RNG flake excluded from this verification per the out-of-scope ruling above).
- **Exit state**: The 29-site benign disposition is recorded and cited. The confirmed-dead code is either deleted (with dungeo suite + one clean walkthrough-chain run as proof) or explicitly deferred if David does not confirm — either outcome closes this open item, it does not stay silently parked.
- **Status**: COMPLETE (2026-07-21 — benign disposition RECORDED (29 sites stay as-is per the 171837 audit); David confirmed ALL FOUR deletions via the gate: gas-room-handler.ts, actions/basket/ (4 files), basket-handler.ts, river-handler dead setter lines. Zero dangling references (grep-verified); full repokit build clean; dungeo suite 31/31; walkthrough chain 921/921 first run)

### Phase 4: Author the ADR-247 implementation plan
- **Tier**: Small
- **Budget**: 100
- **Domain focus**: Planning pass, not implementation — platform change (world-model `getContents`/`getCarriedAndWorn`/`ClothingTrait` deletion), so David's go-ahead gates everything the authored plan would later do; this phase's own deliverable (the plan document) does not touch `packages/` code.
- **Entry state**: ADR-247 (`docs/architecture/adrs/adr-247-getcontents-worn-default.md`) is ACCEPTED with all three Open Questions resolved. Its Decision mandates a committed 64-site call-audit table before the flip lands, deletion of `ContentsOptions.includeWorn` and `ClothingTrait` outright, and a new `getCarriedAndWorn(holderId)` partition method — this is a world-model API change with cross-platform call sites (world-model, stdlib, story-level GDT debug command), correctly sized as session-or-larger on its own per this plan's brief.
- **Deliverable**: Run a fresh planning pass (invoke session-planner again, or produce the plan directly following the same discipline) that decomposes ADR-247's implementation into session-sized phases and writes `docs/work/adr-247-getcontents-worn-default/plan.md`. The authored plan must sequence at minimum: (1) produce the mandatory 64-site audit table (classifying each site everything / held-only-migrate-to-`.carried` / doesn't-matter) as a committed artifact, gated for David's review before any code changes; (2) implement `getCarriedAndWorn()` and delete `ContentsOptions.includeWorn`/the `getAllContents()` `includeWorn` plumbing; (3) delete `ClothingTrait` (barrels, `implementations.ts` rehydration table, `all-traits.ts`, the GDT `do.ts` case, the trait file); (4) migrate `inventory.ts` off its narrow `includeWorn: true` fix onto `getCarriedAndWorn()`; (5) new tests per ADR-247's Acceptance Criteria (worn item visible in LOOK/EXAMINE, partition correctness, save/restore survival) plus a full package-suite + typecheck verification. This phase does **not** run any of those authored phases — authoring the plan is the entire deliverable.
- **Exit state**: `docs/work/adr-247-getcontents-worn-default/plan.md` exists with session-sized phases, a References-consulted section citing ADR-247 (and this plan/ADR-248 where the factory-contract touches call sites), and a first phase marked CURRENT. This plan's `.current-plan` pointer is **not** repointed to it automatically — that is a separate decision for David to make once he gives the go-ahead to start ADR-247 implementation (platform change gate, per CLAUDE.md).
- **Status**: COMPLETE (2026-07-21 — docs/work/adr-247-getcontents-worn-default/plan.md authored, 6 phases, plan-review clean (advisory tensions only, recorded in-plan). Key grounding find: the REAL getContents call-site count is 176 across 102 files (platform ~89 + stories ~87, dungeo 75/45) — the ADR's stated 64 is stale; drift documented. All 6 phases PENDING behind David's explicit ADR-247 go-ahead; .current-plan untouched)

## Sequencing note
Phases 1 and 2 are independent of each other and of Phase 3 — either can run first, or in parallel across sessions. Phase 3 is sequenced after them not because it depends on their output, but because it carries the confirm gate the user asked to keep explicit and separate from the cheaper, ungated work. Phase 4 runs last because it depends on nothing from Phases 1-3 either, but its own output (a plan for platform-changing work) is the natural hand-off point out of this sweep.
