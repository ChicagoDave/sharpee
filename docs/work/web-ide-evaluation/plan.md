# Session Plan: Pure web application ("O1/O2") evaluation — the fourth and final shape for Chord Writer

**Created**: 2026-09-14
**Plan Status**: ACTIVE
**Pointer handoff (rule 18b)**: complete — David's disposition of `docs/work/avalonia-ide-evaluation/plan.md` was **still live** (David, 2026-09-14, session 3b49f8): it now carries a `**Superseded by**: docs/work/web-ide-evaluation/plan.md` stamp, its Phases 0–6 are done, and its **Phase 7 (Windows check) stays PENDING and it remains resumable at exactly that phase**. No phase of that plan is marked DONE or ABANDONED by this handoff, and it is not archived. `docs/context/.current-plan` now names this plan.
**Overall scope**: An evaluation, not a build — the fourth and final candidate in the four-way comparison (native Swift/WPF, OpenSilver on Photino, Avalonia + Velopack, and this one), and the only one with no evidence yet. **Scope ruling from David, 2026-09-14, binding on this plan: strictly O1/O2 — nothing installed.** Real folders via the File System Access API where the browser allows (Chromium today), private storage (OPFS) elsewhere, cloud adapters named as known cost rather than spiked. **O3 (a local companion process) is explicitly OUT of scope** — a phase that finds itself reaching for a companion process has found a finding, not a fallback. Output is a filled Web column on the ADR-341 parity table, a decision record extending the Avalonia record's four-way frame with real evidence in the Web column, and — only on David's explicit ADR-worthy confirmation (rule 11) — the four-way ADR draft that has been deferred pending exactly this evidence. ADR-341 itself is not edited by this plan.
**Bounded contexts touched**: N/A — infrastructure/tooling evaluation, no domain behavior change. Product surface judged is the existing macOS `tools/ide/SharpeeIDE` parity table (rows reused, not invented per requirement or capability); no Chord change, no `packages/` change (platform changes require discussion first).
**Key domain language**: N/A in the DDD sense — this evaluation's own vocabulary, carried from the two prior evaluations and extended for a host that is not a process: "the kill phase" (the one question whose failure ends the evaluation before UI work — here, storage, not rendering); "the pane collapse" (in a browser the page IS the host, so D3's three-web-pane contract, WebView response-supply, and host-shape counterfeiting all stop being questions this shape has to answer); "storage adapter" (brainstorm §5's `open/list/read/write/delete/watch` interface, with `LocalFolderAdapter` over the File System Access API and `OPFSAdapter` as the two adapters this plan actually spikes; cloud adapters are named cost, not spiked, per the options doc's own ruling); "tier boundary" (brainstorm §6 — where this shape honestly stops, most plausibly at hatched TypeScript stories, and a phase should name that rather than route around it); "felt comparison" (David's own judgment, not inferred from measurement, deferred exactly as in the prior two evaluations).

## References consulted
- `docs/work/web-chord-writer/options-20260913-web-app-shapes.md` — §1 R1–R21 (requirements against the host, with a browser-clean measurement per row); §2 (the File System Access API is Chromium-only through Safari 26.6 and Firefox 155, dated 2026-09-13 — the fork this plan's kill phase exists to verify firsthand rather than cite); §3 O1/O2 descriptions and the O1+O2 hybrid named as "what 'pure web app' most plausibly means as a product" — the shape this plan actually builds; §5 the ordered O1 kill list this plan's phase order follows; §6 the four-way judging frame this plan's decision record fills for the Web column; §7 Q2–Q5 carried below as open questions (Q1 is answered: strictly O1/O2).
- `docs/work/web-chord-writer/brainstorm-20260813-web-chord-writer.md` — §4.1 the iCloud finding (no third-party web API for iCloud Drive), flagged 2026-08-13 and never verified — this plan verifies it rather than re-citing it; §5 the storage adapter interface (`open/list/read/write/delete/watch`, `OPFSAdapter` as the working copy, not just a fallback) this plan's Phase 4 implements; §6 the hatch tier-boundary framing this plan's Phase 5 must resolve or name.
- `docs/work/avalonia-ide-evaluation/plan.md` — the immediately preceding plan and structural model: its Evidence discipline section (spike code OUTSIDE the repo, evidence inline never cited, fernhill read-only with an empty `git status --short` check after every phase, rule-13a Integration Reality framing applied to named phases, a kill phase before any UI work) is reused verbatim in shape below, adapted to a host with no process at all.
- `docs/work/avalonia-ide-evaluation/decision.md` — §6 the four-way dimension table, Web column marked "still unspiked" — this plan's Phase 6 fills it; §7 the recommendation explicitly stated as "provisional against O1/O2," which is the reason this evaluation exists; §9 "if the pure web application spikes well" as a named condition that would change the standing recommendation.
- `docs/work/opensilver-ide-evaluation/parity-table.md` — the canonical row set (from `docs/work/archive/adr-341-spike/parity-table.md`) and the PASS/partial/not built/Spike? vocabulary this plan's Phase 6 parity column reuses verbatim; the "host findings that are not rows" table format for properties of the web host itself (no change-notification API, per-session permission re-prompt, service-worker offline) that are not macOS parity-table rows.
- `docs/architecture/adrs/adr-341-chord-writer-windows.md` — D3 (the three-web-pane host contract — this plan's Phase 2 records that a browser host collapses the question rather than answering it, since the page is already the host); D4/AC-5 (one grammar — CodeMirror 6 over the compiler's own lexer, already proven in the OpenSilver spike at 0.92 ms, is the row this plan's Phase 2 reuses rather than re-proves from scratch); D7 (installer and update channel — a pure web app has no installer, which this plan's Phase 6 records as a dimension answer, not a gap); AC-1 (the parity table this plan's Phase 6 column feeds).
- `docs/architecture/adrs/adr-280-chord-writer-project-model.md` — Amendment A1 (the story's own title names a real, editable folder at `~/Documents/<Story Title>/` — this is the promise Phase 1's kill question tests directly, and the promise Q3 asks whether the web tier may relax).
- `docs/architecture/adrs/adr-307-testing-tree-model-v2.md` — the tree-document round trip (tree in, replay, typed turn, document out; every played turn a card, no ranges or ticks) this plan's Phase 3 reproduces in-page with no host process at all, following the exact discipline the OpenSilver and Avalonia evaluations already proved on two other hosts. `docs/architecture/adrs/adr-306-testing-play-surface-revamp.md` — **Status: ACCEPTED, SUPERSEDED IN PART by ADR-307** (the range/tick model and its ticking-specific click-through rulings; "the rest of the revamp stands as built") — cited only for the surviving parts (the testing surface itself, "no golden path, just a tree of transcripts"); Phase 3 builds on ADR-307's always-recording tree, never on the superseded range/tick design.
- `docs/architecture/adrs/adr-297-ide-appearance.md` — the dual-palette live flip this plan's Phase 6 answers with CSS custom properties and `prefers-color-scheme`, per the options doc's R17 assessment that the panes already do this.
- `docs/architecture/adrs/adr-191-play-it-now-browser-playground.md` — ACCEPTED precedent for browser-clean compile (ADR-233 G2 shipped and proven in real Chromium, 2026-07-18) and CodeMirror 6 as the chosen playground editor — this plan's Phase 2 and Phase 6 build on proven ground rather than open questions.
- `docs/context/session-20260914-0716-main.md` (newest session file) — Open Items: the ADR is deferred on evidence, not priority, and returns "when the web spike has evidence of the same kind" — the exact deliverable this plan produces; the felt comparisons owed from the Avalonia evaluation are unrelated to this plan's scope and are not re-opened here.

## Evidence discipline (applies to every phase)
Every acceptance-gating claim in this plan's phase records carries the command run (or the exact browser/version/OS exercised) and its result, dated, inline — never "see the spike" (project memory: evidence is inline, never cited). Spike code lives OUTSIDE this repository at `/Users/david/repos/spikes/web-ide/` (sibling of `/Users/david/repos/spikes/opensilver-ide/` and `/Users/david/repos/spikes/avalonia-ide/`); only the record — this plan, the phase records, the filled parity column, the decision document, and `evidence/` (screenshots, HAR-style request logs) — lives under `docs/work/web-ide-evaluation/`.

**Test story**: fernhill (`branch-stories/fernhill`), read-only. Every phase reads its real folder and, where a phase writes (the tree-document round trip, a compose diagnostic cache), it writes to a **copy** of fernhill under `/Users/david/repos/spikes/web-ide/fixtures/fernhill/`, never to `branch-stories/fernhill` itself. `git status --short branch-stories/fernhill` must stay empty after every phase. No real story is ever modified; no ADR references appear in any test story text.

**Rule 13a Integration Reality**: Phase 1 (real folder access — the kill phase), Phase 3 (the test-tree round trip run entirely in-page), and Phase 5 (`esbuild-wasm` bundling) are integration phases. Each needs a REAL-PATH test against the real browser API, the real `branch-tester` logic (not a hand-rolled walker stand-in), and the real `esbuild-wasm` package — no stub of `showDirectoryPicker`, no mocked `FileSystemDirectoryHandle`, no hand-written replacement for OPFS, no fake bundler. If a real path cannot be exercised in a real, installed browser on this machine (for example, a browser this Mac cannot run), the phase records that as a finding — it does not substitute a stub and call the integration proven. Browser versions actually exercised (not versions read off caniuse) are recorded inline, dated, in every phase touching §2's fork.

**Browsers available for real-path testing on this machine**: Phase 0 establishes exactly which of Chrome, Edge, Safari, and Firefox are installed and at what version before any later phase depends on a specific one's behavior — the options doc's caniuse table (2026-09-13) is a starting hypothesis, not a substitute for checking this machine directly.

## Phases

### Phase 0: Prerequisites, browser matrix, and scaffold
- **Tier**: Small
- **Budget**: 100
- **Domain focus**: Establishing the ground every later phase depends on — which browsers this machine can actually exercise, a minimal static-site scaffold with no backend, and the one factual question (iCloud) flagged in 2026-08-13 and never verified, closed before it can silently shape Phase 4's scope.
- **Entry state**: No web-ide spike exists anywhere in `/Users/david/repos/spikes/`. fernhill exists at `branch-stories/fernhill`, untouched. Nothing installed for this evaluation on this machine yet.
- **Deliverable**: (1) A dated inventory of Chrome, Edge, Safari, and Firefox actually installed on this Mac, with exact versions, superseding the options doc's caniuse-sourced table for this evaluation's own claims. (2) A minimal static scaffold at `/Users/david/repos/spikes/web-ide/app/` — no framework required beyond what compiling `.story` in-page needs (`@sharpee/chord`, `@sharpee/story-loader`, `@sharpee/platform-browser` per the options doc's already-verified browser-clean grep) — served over `http://localhost` for local development only (a plain static file server, the same role `npm run dev` plays for any static site) since `file://` cannot grant File System Access permissions in most browsers. This is dev-loop plumbing, not O3: nothing it serves answers a compose, build, or test request — every one of those runs in-page, and the server exists only because a browser will not grant filesystem permissions to a `file://` origin. (3) The iCloud question (brainstorm §4.1) verified directly — check Apple's current developer documentation for any third-party web API surfacing iCloud Drive (CloudKit JS's actual scope, any newer surface) and record the finding with its source and date, closing or reaffirming the 2026-08-13 flag rather than re-citing it. (4) A copy of fernhill at `/Users/david/repos/spikes/web-ide/fixtures/fernhill/` for every later phase to read and write against.
- **Exit state**: The exact browser versions this evaluation's later phases will test against are known and recorded; the scaffold serves a blank page over `http://localhost` (not `file://`); the iCloud finding is either reaffirmed with a current source or corrected, either way ending its "never verified" status; fernhill's copy exists and is never the tracked one.
- **Status**: DONE 2026-09-14, session 3b49f8 — record at `phase-0-prerequisites.md`, probe readouts at `evidence/phase-0-probe-{safari-26.6.2,firefox-155.0.1}.json`. All four deliverables met. **The inventory blocks Phase 1 as written**: no Chromium-family browser is installed on this Mac (Safari 26.6.2 and Firefox 155.0.1 only, each verified to expose no `showDirectoryPicker`), so the kill phase's subject cannot be exercised. Not a kill of O1 — a missing install, and David's call. OPFS is present and writable in both, so Phases 2–6 are unblocked against the O2 route.

### Phase 1: Real folder access — the kill phase (Integration Reality, rule 13a)
- **Tier**: Medium
- **Budget**: 250
- **Domain focus**: The one fact that forks every option in the options doc's §2 — whether the File System Access API lets this page open, hold, and write back to the author's real `~/Documents/<Story Title>/` folder — verified in real installed browsers on this machine rather than read off caniuse. This is O1's kill question #1 and it is a storage question, not a rendering one, which is the structural difference from every prior evaluation's kill phase.
- **Entry state**: Phase 0's scaffold serves over `http://localhost`. Browser inventory from Phase 0 is known.
- **Deliverable**: In each browser Phase 0 found installed: (a) `showDirectoryPicker()` invoked by a user gesture, pointed at `fernhill`'s spike copy; (b) the returned `FileSystemDirectoryHandle` persisted to IndexedDB; (c) the page reloaded and the handle reacquired — recording exactly what permission UI appears (a silent reacquire, a one-click reprompt, or a full re-pick) and whether it differs session to session; (d) a real write through the handle (editing `fernhill.story`'s copy) verified by an independent read — either the Finder, if the browser is on this Mac and the handle points at a real path, or a second, separate read through the API; (e) the identical sequence attempted in Safari and Firefox, with the actual behavior recorded (a clean `TypeError`/absence of `showDirectoryPicker`, a partial implementation, or something caniuse's snapshot did not anticipate) — not assumed from the options doc's 2026-09-13 table.
- **Exit state**: A named PASS/FAIL per browser for open, persist, reacquire, and write, dated and with the exact version from Phase 0's inventory. If Chrome and Edge both pass cleanly, O1's real-folder promise holds for the Chromium tier exactly as the options doc predicted, and the plan proceeds to Phase 2 with that promise proven rather than assumed. If Chrome or Edge show a rougher permission story than the options doc implied (for example, a full re-pick rather than a one-click reprompt), that is a genuine finding for the decision record's Web column, not a phase failure — the phase only fails if no Chromium browser on this machine can complete the round trip at all, which would be a kill of O1 outright and would force the evaluation to O2-only for every browser.
- **Status**: DONE 2026-09-14/15, session 3b49f8 — record at `phase-1-real-folder-access.md`, evidence at `evidence/phase-1/` (ten JSON files posted by the page itself, plus the reprompt dialog screenshot). **PASS on every sub-deliverable in Edge 153.0.4234.32**, including the session-to-session question the plan asked for. The permission ladder: pick once (one dialog) → first browser restart (one dialog that **names the folder rather than re-picking it**, offering "Allow on every visit") → every restart thereafter silent (`granted`, 1.6 ms, no UI). A page *reload* is silent throughout. Real write verified from outside the browser (30,608 = 30,559 + 49) and the fixture restored to its baseline hash. Safari 26.6.2 and Firefox 155.0.1 refuse with a clean `TypeError`. **The product answer: the author is asked twice, ever** — better than the options doc's framing, which assumed the reprompt recurs.

### Phase 2: Compose and Play in-page — the pane collapse
- **Tier**: Medium
- **Budget**: 250
- **Domain focus**: R3 (compose) and R5 (play) answered by a page that already has no host to negotiate with — the D3 three-web-pane contract, WebView response-supply doors, and host-shape counterfeiting that consumed the largest single finding in both prior evaluations simply do not apply here, because the IDE page and the pane are the same document. This phase names that collapse explicitly rather than silently skipping the rows.
- **Entry state**: Phase 1 has a working folder-access route (Chromium at minimum) or an OPFS fallback is used if Phase 1 killed the folder route entirely. fernhill's spike copy is reachable through whichever storage adapter Phase 1 proved.
- **Deliverable**: (1) fernhill composed in-page: `@sharpee/chord`'s compiler invoked directly (no subprocess, no `sharpee compose` shell-out) against fernhill's real `.story` and its `.chord` imports read through the folder handle, using a browser-side import resolver in place of devkit's `makeFsImportResolver`; diagnostics with spans returned and displayed. (2) fernhill played in-page: the composed IR handed directly to `story-loader` and `platform-browser` in a sandboxed `<iframe>` (R19 isolation, per ADR-191's already-proven pattern) — no `dist/web/<id>/` bundle built or served, confirming the options doc's claim that play needs no build step in this shape. (3) CodeMirror 6 wired over the same `.story` file, tokenized by the real compiler lexer (`packages/chord/src/lexer.ts`, bundled) — reusing the OpenSilver spike's already-proven approach (0.92 ms whole-document style pass on the 1755-line measurement file) rather than re-deriving it, and re-measured here only to confirm it holds in this host with no native process backing it.
- **Exit state**: A named PASS/FAIL for in-page compose with real spans, in-page play with no bundle, and the editor at a timing comparable to the OpenSilver figure — plus an explicit paragraph stating that D3's host contract, the response-supply question, and the `window.webkit.messageHandlers` counterfeiting problem are void for this shape, not solved by it.
- **Status**: DONE 2026-09-15, session 3b49f8 — record at `phase-2-compose-and-play.md`, evidence at `evidence/phase-2/`. **PASS on all three deliverables, unattended** (Phase 1's persistent grant let the page reacquire the folder handle silently — the first practical dividend of that result). Compose in-page: **11.8 ms**, gate-clean, 114 KB IR, no subprocess — against 89 ms for the `sharpee compose` round trip the other shapes paid. Import resolver proven on a two-file probe, fragment spliced at the import site (ADR-251 D4). Play: IR handed to a sandboxed iframe, **no bundle built and nothing served**, 66 entities, and a typed `look` rendering fernhill's real opening prose. Editor: **0.82 ms style pass / 0.83 ms per char** on the same `measure-1755.story` the other three measured (8,545 tokens, same lexer) — **the fastest of the four shapes**; undo round trip restores the exact hash. **The pane collapse is stated explicitly**: D3's host contract, the response-supply question and the `window.webkit` counterfeiting are *void* for this shape, not passed. Two platform findings: `CompileOptions.importResolver` is **synchronous** while every browser filesystem API is async, so a browser host must pre-walk the import graph (a real seam, recorded not fixed); and the runtime is **not browser-clean without three build-time `process.env` defines** (`browser-core.ts:747-749`), whose absence builds clean and dies at boot. Bundles: 1.33 MB compose + 3.31 MB play, unminified — the first-load cost of the shape whose install story is "a URL".

### Phase 3: The test tree, in-page — the ADR-307 round trip with no host process (Integration Reality, rule 13a)
- **Tier**: Medium
- **Budget**: 200
- **Domain focus**: R6 — the Testing surface's tree-of-cards UI, already a web app, now driving the actual `@sharpee/branch-tester` walker in the same page rather than shelling out to `sharpee test --tree --json`. The one node-bound file in that package (`runner.ts`, the I/O shell) is what a browser host replaces; the walker, tree-document model, auto-assertion, and channel-assertion logic (already measured browser-clean, brainstorm §2) port unmodified.
- **Entry state**: Phase 2's in-page compose produces IR for fernhill. Phase 1's storage route (folder or OPFS) can read and write fernhill's spike copy.
- **Deliverable**: The real `tools/ide/web/testing-surface` pane loaded in-page (not in an iframe over a scheme — directly, since Phase 2 established there is no separate host to serve it from); fernhill's tree document (31 cards, seed 42, matching the fixture the two prior evaluations used) injected; the tree replayed through the in-page `story-loader`/`platform-browser` client from Phase 2, driven by `branch-tester`'s real walker running in a Web Worker or the main thread (not Node); one typed turn; the whole document written back to the spike's fernhill copy through Phase 1's storage adapter — never to `branch-stories/fernhill`.
- **Exit state**: A named PASS/FAIL with the resulting card count and any turn-record count named explicitly (the Avalonia evaluation found an unexplained 274-vs-31 over-run in this exact path on a different host; this phase records its own count without assuming it will match either prior number, and flags a discrepancy if the write comes back wrong rather than treating "wrote something" as success).
- **Status**: CURRENT (since 2026-09-14)

### Phase 4: Storage adapters — the O1+O2 hybrid this evaluation actually builds
- **Tier**: Medium
- **Budget**: 200
- **Domain focus**: The scope ruling made concrete — brainstorm §5's storage interface (`open/list/read/write/delete/watch`) with a `LocalFolderAdapter` over Phase 1's File System Access route and an `OPFSAdapter` for every browser Phase 1 could not reach, unified behind one interface so the rest of the app (Phases 2–3, already built) does not know which adapter is live. Cloud adapters (Google Drive `drive.file`, OneDrive/Graph) are **named as known cost, not spiked** — the options doc states plainly that "the OAuth work is not a spike question; it is known cost," and this phase respects that ruling rather than re-litigating it with a partial OAuth implementation.
- **Entry state**: Phase 1's per-browser verdict is known. Phase 2 and Phase 3 work against whichever adapter Phase 1 proved for the browser under test.
- **Deliverable**: (1) The adapter interface implemented as a TypeScript module with both concrete adapters. (2) `OPFSAdapter` proven as a genuine working copy, not just a fallback: fernhill's spike copy imported into OPFS (a zip import, since OPFS has no picker), edited, and exported back to a zip — exercised in a browser where Phase 1 found no folder access (Safari or Firefox, per whatever Phase 0 found installed), or in Chrome/Edge deliberately routed to OPFS if no non-Chromium browser is available on this machine. (3) The adapter selection logic: which adapter a given browser gets, and whether the choice is automatic or author-visible. (4) One paragraph naming the cloud-adapter boundary explicitly: what `drive.file` or Graph would need (named, not built), and why this phase does not build it.
- **Exit state**: A named PASS/FAIL for the interface working identically over both adapters from Phases 2–3's call sites (the same compose/play/test code path exercised against both, per the honest test of a real abstraction — not one adapter with the other stubbed out and never exercised). The cloud-adapter boundary is recorded as a stated cost, not a silent gap.
- **Status**: PENDING

### Phase 5: Build, publish, and the hatch tier boundary — `esbuild-wasm`
- **Tier**: Medium
- **Budget**: 250
- **Domain focus**: R4/R9/R10 — the one place this shape needs a bundler at all, since play (Phase 2) needs no build. `esbuild-wasm` in a Web Worker over a virtual filesystem holding prebuilt platform ESM (the ADR-178 baseline set, published as a versioned static asset per ADR-191 Mode B's sketch) is a claim to measure, not assume, per the goal framing — size, cold start, and whether the real Chord toolchain (including a TypeScript hatch) actually transpiles under it.
- **Entry state**: Phase 2's in-page compose produces IR. Phase 4's storage adapter can read fernhill's spike copy and write a produced zip.
- **Deliverable**: (1) `esbuild-wasm` loaded lazily on first Publish, measured cold-start and load size against the options doc's ~10 MB estimate. (2) `browser-entry` bundled with the `@sharpee/*` platform ESM against the virtual FS, producing a `dist/web/<id>/`-equivalent output entirely in-page. (3) The zip built with `fflate`'s `zipSync` (already browser-clean per the options doc), downloaded, unzipped, and opened from `file://` to confirm it plays standalone — the actual acceptance bar for R9. (4) A TypeScript hatch (ADR-210 §5.6) attempted through the same `esbuild-wasm` pipeline exactly as `hatch-transpile.ts` does today, with the real outcome recorded: either it transpiles correctly (closing brainstorm §6's Option 1) or it does not, in which case the phase states the tier boundary plainly — "hatched stories need Chord Writer for Mac" — as brainstorm §6's Option 2, rather than routing around the failure with a half-working substitute.
- **Exit state**: A named PASS/FAIL/PARTIAL for bundling, a measured cold-start and bundle-size number dated inline, a working standalone zip proven by opening it from `file://`, and an explicit ruling — supported by the phase's own evidence — on which side of brainstorm §6's tier boundary hatches land for this shape. This is squarely Q4 from the options doc §7, and this phase is where the technical half of that question (can it even work) gets answered; whether it should ship that way remains David's call, carried into the open questions section below.
- **Status**: PENDING

### Phase 6: The shell — appearance, offline, settings, and second window
- **Tier**: Medium
- **Budget**: 250
- **Domain focus**: The remaining parity-table rows a web host answers structurally differently from a native one — R13 (offline via PWA, a dimension no prior shape had to name), R14 (settings via `localStorage`/IndexedDB), R17 (appearance via CSS custom properties and `prefers-color-scheme`, which the panes already do per the options doc), R18 (a second window via `window.open` and `BroadcastChannel` or shared OPFS state rather than a second native window), and the project pane as a list/tree view over whichever storage adapter Phase 4 proved for the active browser.
- **Entry state**: Phases 1–5 have each recorded a named verdict. The in-page shell from Phases 2–3 exists to build chrome around.
- **Deliverable**: (1) A minimal service worker caching the static assets and the platform ESM bundle, tested by loading the page once, going offline (DevTools network throttling or airplane mode), and reloading — confirming R13's PWA claim rather than assuming service workers "just work." (2) ADR-297's dual-palette live flip via CSS custom properties and `prefers-color-scheme`, measured the same way the prior two evaluations measured it (a token mutated, a pixel or DOM value read before and after, timed) so the Web column of the four-way table is comparable in kind to the other three. (3) The project pane as a list/tree view over the active storage adapter, mirroring the typed-artifact-group structure (ADR-280 D1) the other two evaluations built. (4) A second window opened via `window.open` sharing state with the first through `BroadcastChannel`, proven by a message sent from one and observed in the other. (5) Settings (last project, open documents, active tab) persisted to `localStorage` or IndexedDB and confirmed to survive a reload.
- **Exit state**: A named PASS/FAIL/PARTIAL for each of offline, appearance flip, project pane, second window, and settings — with the appearance-flip timing recorded in the same units the WPF/OpenSilver/Avalonia records used, so Phase 7's table cell is not an estimate.
- **Status**: PENDING

### Phase 7: Parity column, decision record, and (on confirmation) the four-way ADR draft
- **Tier**: Medium
- **Budget**: 200
- **Domain focus**: Assembling Phases 0–6's evidence into the artifact this whole track has been waiting on — the Avalonia decision record's own words: "the ADR is deferred on evidence, not on priority… it returns when the web spike has evidence of the same kind." This phase is where that evidence lands.
- **Entry state**: Phases 0–6 each have an inline evidence record (PASS, FAIL, or PARTIAL — no phase left silently unresolved).
- **Deliverable**: (1) `docs/work/web-ide-evaluation/parity-table.md` — the existing row set (via `docs/work/opensilver-ide-evaluation/parity-table.md`'s already-filled OpenSilver column and `docs/work/avalonia-ide-evaluation/parity-table.md`'s Avalonia column as adjacent references) with a new **Web** column filled row by row from Phases 0–6's evidence — never inventing new product surface, and explicitly marking the rows this shape structurally cannot answer the same way (an installer row, for instance, becomes "none — a URL is the install," per D7's dimension answer, not a blank). (2) `docs/work/web-ide-evaluation/decision.md` — extending `docs/work/avalonia-ide-evaluation/decision.md` §1's per-control table to a **fourth** column (WPF / OpenSilver / Avalonia / Web) and its §6 four-way dimension table's Web column from "still unspiked" to evidence, with a recommendation stated in weighted order and the honest counter-case, matching the discipline all three prior decision records held to. (3) GitHub issue #438 commented with the outcome — the same umbrella issue every prior decision record used. (4) **Only on David's explicit ADR-worthy confirmation (rule 11)**: the four-way ADR draft the Avalonia record's §9 named as pending exactly this evidence — never assumed, never pre-empted; ADR-341 itself is not edited by this plan either way. If the draft carries open questions, the rule-11a interview offer follows normally.
- **Exit state**: David has a four-column parity table, a decision record with the Web column filled and the counter-case honestly stated, and (if confirmed ADR-worthy) a draft ADR to review — completing the four-way comparison this whole track has been building toward across three separate evaluation plans.
- **Status**: PENDING

## Open questions carried into this evaluation (not blocking, tracked for Phase 7)
- **Q2 — the browser matrix, options doc §7.2.** "Is 'real folders in Chrome and Edge; private storage elsewhere' an acceptable product, or must Safari authors have the same product as Chrome authors?" Phase 1 settles what is technically true on this machine; whether the resulting asymmetry is an acceptable *product* is David's call, carried into Phase 7's decision record rather than resolved by this plan.
- **Q3 — ADR-280 A1, options doc §7.3.** "For a web tier, may the project live somewhere the author cannot see in Finder (OPFS, a cloud tree), or is the real folder non-negotiable everywhere?" Phase 4 builds and proves the OPFS adapter regardless of the answer, since the scope ruling already names OPFS as the non-Chromium tier; whether that is an acceptable trade against A1's promise, or only a stopgap until every browser supports real folders, is David's ruling to make once Phase 4's evidence exists.
- **Q4 — hatches, options doc §7.4.** "`esbuild-wasm` in the page, or 'hatches need the desktop app'?" Phase 5 answers the technical half (does the real transpile pipeline actually work under `esbuild-wasm`); whether to ship the tier boundary even if it technically works — brainstorm §6's case that "hatches need Chord Writer for Mac" gives the desktop app a clear reason to exist — is a product decision Phase 7 states as pending rather than makes.
- **Q5 — iCloud, brainstorm §4.1, options doc §7.5.** Flagged 2026-08-13, never verified. **Not deferred by this plan** — Phase 0 verifies it directly as a factual check, closing the flag one way or the other before any other phase depends on the answer.
- **Q6, options doc §7.6 (relation to the macOS app) and Q7 (which spike first) are answered by this plan's existence** — this is the second spike (Avalonia ran first), and §6's "third product, or the thing that eventually replaces both" question is the same felt-comparison-deferred pattern the other two evaluations used; Phase 7's decision record states it as open rather than resolving it, consistent with how the OpenSilver and Avalonia records both left it.
- **GH #438** — the umbrella four-way issue every prior decision record has commented; Phase 7 continues that thread rather than opening a new one.
- **Owed items from the Avalonia evaluation** (macOS bundle layout, the Phase-1 replay over-run, the editor's light-palette wiring, `localStorage` across an app restart, GH #457, GH #458) are unrelated to this plan's scope and are not re-opened here.
