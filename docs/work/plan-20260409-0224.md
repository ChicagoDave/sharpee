# Plan: Fix SonarQube Issues in Character Package

## Context

PR #74 fails the SonarQube quality gate with a D Reliability Rating (requires A). The issues are all in `packages/character/` — the NPC behavior chain code from ADR-141/142/144/145/146. There are 3 bugs, 8 critical complexity issues, ~25 major code smells, and ~30 minor items. Fixing the 3 bugs should flip the reliability rating to A. The rest are code quality improvements.

## Approach: Four Passes

### Pass 1: Bugs (fixes the quality gate)

3 items, all quick:

| File | Line | Fix |
|---|---|---|
| `dialogue-extension.ts` | 141, 259 | Ternary returns same value both branches. Both are `resolution.topic.name`. For `related` type, should use `resolution.via.name`. Fix both `handleAsk` and `handleTell`. |
| `propagation.test.ts` | 108 | `.sort()` → `.sort((a, b) => a.localeCompare(b))` |

### Pass 2: Readonly + Unused (mechanical, ~30 items)

All in one sweep across files. Add `readonly` to ~25 class members that are assigned at construction and never reassigned. Remove 7 unused imports and 5 useless assignments. These are search-and-replace fixes.

**Files:**
- `character-builder.ts` — 3 readonly
- `conversation/builder.ts` — 3 readonly
- `conversation/constraint-evaluator.ts` — 2 readonly
- `conversation/dialogue-extension.ts` — 3 readonly, merge duplicate import, drop unused `contradiction` var
- `conversation/lifecycle.ts` — 1 readonly
- `conversation/topic-registry.ts` — 1 readonly
- `conversation/acl.ts` — remove unused `Coherence` import
- `goals/builder.ts` — 7 readonly
- `goals/goal-activation.ts` — 1 readonly
- `goals/pathfinding.ts` — 1 readonly
- `influence/builder.ts` — 3 readonly
- `propagation/propagation-types.ts` — 1 readonly
- `propagation/propagation-evaluator.ts` — remove useless `listeners` assignment
- `tick-phases.ts` — 2 readonly, remove useless `restored` assignment
- `stdlib/npc-service.ts` — 1 readonly
- 7 test files — remove unused imports and useless assignments

### Pass 3: Minor code smells (~15 items)

- `influence/influence-duration.ts` L40 — refactor `track()` to take an options object (8 params → 1)
- `influence/influence-duration.ts` L66 — flip negated condition
- `influence/influence-duration.ts` L161 — use optional chain
- `conversation/topic-registry.ts` L226 — collapse else-if
- `influence/influence-evaluator.ts` L216-222 — collapse duplicate switch cases with fallthrough
- `goals/step-evaluator.ts` L116-122 — collapse duplicate switch cases
- `goals/step-evaluator.ts` L172, L200 — extract shared helper for duplicate functions
- `conversation/lifecycle.ts` L347, L370 — extract shared helper for duplicate functions
- `goals/pathfinding.ts` L55 — add clarifying comment (false positive, intentional reversal)

### Pass 4: Cognitive complexity (8 functions)

Extract helpers to reduce nesting. No logic changes — just structural refactoring.

| File | Function | Complexity | Fix |
|---|---|---|---|
| `dialogue-extension.ts:121` | `handleAsk` | 18 | Extract `resolveAndSelectResponse()` helper |
| `topic-registry.ts:130` | `resolve` | 22 | Extract `findRelatedRedirect()` for Phase 2 |
| `goal-activation.ts:67` | `evaluate` | 21 | Extract `checkNewActivations()` and `evaluateInterruptions()` |
| `pathfinding.ts:93` | `findNextRoom` | 23 | Extract `isAccessible()` predicate, unify seed + BFS loops |
| `influence-evaluator.ts:106` | `evaluatePassiveInfluences` | 17 | Early-continue instead of nesting; extract `evaluateInfluenceOnTarget()` |
| `propagation-evaluator.ts:189` | `findEligibleFacts` | 22 | Extract `meetsAudienceRequirement()` and `meetsTendencyFilter()` |
| `tick-phases.ts:130` | `createGoalPhase` | 22 | Extract `executeTopGoal()` helper |
| `tick-phases.ts:205` | `createPropagationPhase` | 36 | Extract `handleSpeakerPropagation()` and `recordTransfer()` |
| `tick-phases.ts:365` | `createInfluencePhase` | 21 | Extract `handleInfluenceResult()` helper |

## Execution Order

1. **Pass 1** first — fixes the quality gate, can be verified immediately
2. **Pass 2** next — mechanical, low risk, high volume
3. **Pass 3** — minor structural improvements
4. **Pass 4** last — complexity refactoring, higher risk, needs test verification

## Verification

After each pass:
- `npx tsc --noEmit -p packages/character/tsconfig.json` — no type errors
- `pnpm --filter '@sharpee/character' test` — all tests pass

After all passes:
- Push and check SonarQube re-analysis on PR #74
