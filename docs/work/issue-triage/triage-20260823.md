# Issue triage — 2026-08-23

79 open at pull time. Redo of yesterday's pass (`triage-20260822.md`), one day
and one ADR-325 implementation round later.

## Movement since yesterday

Yesterday opened at 83 and this pass opens at 79 — but the churn underneath is
larger: **12 closed** (staleness pass: #280, #249, #250, #251; Tier 1: #273,
#192, #295, #290; Tier 2 import seam: #287, #288, #301, #302) and **8 filed**
(#303, #304, #305–#310). The list is finally being read down faster than it
grows.

## Age

| Month | Count |
| --- | --- |
| 2026-04 | 5 |
| 2026-05 | 2 |
| 2026-06 | 11 |
| 2026-07 | 9 |
| 2026-08 | 52 |

## Staleness pass (verified 2026-08-23, this session)

**The never-closed-fix pattern recurred within 24 hours.** Six of the eight
new issues — #305–#310, the ADR-325 implementation set — were filed early
2026-08-23 and landed by this branch's own commits the same morning
(74fa6192 at 04:28 for #305/#306/#307; ffe06101 at 05:21 for
#308/#309/#310, each with dedicated test files: `counter.test.ts`,
`landing.test.ts`, `movement-clauses.test.ts`, plus the three
story-loader runtime suites). Still open ten hours later.

Verified at HEAD before closing, not taken from the commit messages:

- `pnpm --filter @sharpee/chord test` — 986 passing (66 files), 2026-08-23 15:50 CDT
- `pnpm --filter @sharpee/story-loader test` — 604 passing (84 files), same run
- `./sharpee test branch-stories/secret-letter` — 91 cards passing, 103
  assertions passing, 2026-08-23T20:51:05Z — the real path: the rewritten
  `mercenaries.chord` exercises the plural possessive, possessive
  location/here/offstage, named-turn timers, `when … expires` / `when the
  player moves` clauses, and the inline `kill the player` body in a live story.

**All six close on evidence. Net: 73 open.**

No other closure candidates surfaced: yesterday's pass already swept the
plausible ones, and the eight it confirmed still-open are unchanged. One
partial noted for later: #245 (nine Fernhill defects) includes "win text
prints twice", which #274 fixed on 2026-08-18 — the issue needs a per-defect
re-verify at next touch, not a close.

## Clusters (of the 73)

### A. IDE / Chord Writer — 24, still a third of the list

#129, #188, #193, #194, #195, #196, #198, #238, #252, #253, #254, #281, #282,
#283, #284, #289, #291, #292, #293, #294, #296, #297, #298, #299

Yesterday's worst bugs (#295, #290) are gone; what remains is feature work and
the parked testing-UX set. Nothing here is a defect that loses data or wedges
the product.

### B. Chord language — 12

#197, #200, #263, #267, #268, #269, #272, #285, #286, #300, #303, #304

(#272 — the unreliable-rumors idea — was missed by yesterday's clustering;
counted here now.) Four still want ADRs (#197, #200, #267, #302's successor
work is done); #269 stays blocked on ADR-316; #267/#268 ride the held
Reflections track.

### C. Testing / transcripts / devkit CLI — 10

#202, #224, #231, #239, #240, #243, #244, #245, #246, #255

### D. Engine / runtime defects — 11

#79, #82, #97, #106, #108, #120, #206, #241, #242, #275, #278

### E. API surface audit — 6

#135, #136, #137, #138, #139, #143 — filed 2026-06-20 as one audit, now two
months untouched. Same ruling as yesterday: one sweep or close all six.

### F. Web client / publish — 1

#248 — the only survivor; #249/#250/#251 closed yesterday.

### G. Docs / archives / housekeeping — 6

#86, #94, #213, #214, #215, #247

### H. Build / toolchain — 3

#130, #131, #132

## The finding that matters most

**The Secret Letter port is the QA instrument.** All eight new issues trace to
the port round: #305–#310 are the ADR-325 work it demanded (filed, built, and
verified inside one day), #304 is a live renderer bug its tree document caught,
and #303 came from outside review of the conversation layer it exercises.
Meanwhile the pre-existing backlog barely moved on its own. The cheapest way to
keep finding real bugs is to keep porting — and to run the close-on-commit pass
yesterday's addendum recommended, since the fix now reliably lands before the
issue is a day old.

## Recommended order

### Tier 1 — the phrase-rendering seam, while the port is standing on it

| # | Why |
| --- | --- |
| **#304** | `refuse <key>` on a `randomly` phrase prints the literal word "variants" — the refusal renderer stringifies the Choice payload instead of selecting an arm. Live bug, found by the port's own tree document; the story carries a single-arm workaround that should not become permanent. |
| **#286** | `{phrase-key}` interpolates in a description body but prints literally inside a `define phrase` body — the other half of the same seam, and the mechanism #303's item 2 is waiting on evidence about. |

Both are `packages/` work — flagging for discussion before implementation, per
the platform-change rule. They are small, adjacent, and the port gives an
immediate real-path check for each.

### Tier 2 — phrase affordances as one feature (carried)

#281, #282, #283, #284 — unchanged from yesterday: four filings, one design
(tap to edit, hover to define, hold to reveal, live Read tab). One session,
one ADR.

### Tier 3 — park the testing-UX cluster (carried, still unlabelled)

#193, #194, #198, #239, #240, #243, #244, #252, #253, #254

Yesterday recommended labelling these against the planned revamp; no label was
applied. Recommend actually doing it this time (a `testing-ux-revamp` label)
so the next triage pass can skip them wholesale instead of re-reading ten
issues.

### Tier 4 — decide the API audit batch as a batch (carried)

#135–#143. Now the oldest untouched cluster on the list. Schedule one sweep or
close all six.

### Deprioritise

#86, #94, #213, #214, #215, #247 — unchanged.

### Blocked / held, not actionable

- #269 — blocked on ADR-316 elapsed-time semantics.
- #267, #268 — roadmap ADRs tied to the held Reflections track.
- #303 items 2–3 — by their own text, waiting on evidence from the port's
  380-quip conversation corpus. Item 1 (compound strategies) is independently
  actionable when wanted.
