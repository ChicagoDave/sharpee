# Session Plan: Package-by-package refactoring survey implementation (ADR-334..340, GH #382, #385)

**Created**: 2026-09-08
**Plan Status**: ACTIVE
**Overall scope**: Implement the seven ACCEPTED refactoring-survey ADRs (334 engine, 335 story-loader, 336 chord, 337 stdlib, 338 world-model, 339 character, 340 transcript-tester/branch-tester) and the two issue-only cleanups (#382 lang-en-us, #385 parser-en-us), applying the code-documentation-sweep header convention (#384) to every header touched. Every ADR requires D5/D6-style byte-identical output after each phase — this is structural reorganization only, no domain-behavior change, except ADR-337 D1's one declared exception (refusal-order changes, HELD for David's per-line disposition).
**Bounded contexts touched**: N/A — infrastructure/tooling refactor across `packages/`. Byte-identical structural reorganization; no domain behavior change in any phase except the one declared, HELD exception in ADR-337 D1 (see "When DDD Does NOT Apply" — refactors that do not change domain behavior).
**Key domain language**: N/A — this plan uses each ADR's and the umbrella's own terms (stage, sub-step, pass, builder, descriptor, lifecycle hook, assertion core) rather than invented ones.

## References consulted
- `docs/work/refactoring-survey/assessment-20260907-umbrella.md` — the umbrella's recommended phase order (cost/risk, not side), the three ADR-340 facts (ADR-302 D15 amendment + two code comments, `synthesizePolicyAssertions` consolidation, browser-safe `export type` constraint), the two ADR-338 facts (seven-site `AuthorModel` count with the kept working spelling, `canMoveEntity` kept + cached-bind proxy snippet), the `requires` idiom for every ordered list this work introduces, and the header convention (logic first, `References:` block after) applied to every header a phase rewrites.
- `docs/architecture/adrs/adr-334-game-engine-turn-pipeline.md` — D1-D7, AC-1..6: turn stage list, one platform dispatcher, dead-class removal, facade unchanged, byte-identical phases, discuss-first authorization (satisfied tonight, scope limited to this ADR).
- `docs/architecture/adrs/adr-335-story-loader-decomposition.md` — D1-D7, AC-1..6: thirteen runtime-section modules, core mutable-fields assertion + restore reset, extension construction into the registry, one curated alias table with a derived Chord-side set, facade unchanged, byte-identical phases.
- `docs/architecture/adrs/adr-336-chord-analyzer-structure.md` — D1-D7, AC-1..6: exhaustive `stmt.kind` switches, per-line entity builders, `ANALYSIS_PASSES` as data, section split explicitly deferred (not planned here), facade unchanged, byte-identical (IR + play) phases.
- `docs/architecture/adrs/adr-337-stdlib-lifecycle-and-validator.md` — D1-D8, AC-1..6: one lifecycle call site at the executor with the multi-object remainder declared, one validation pipeline, confirmed-deletion list, docs correction, the one declared byte-identical exception (refusal order, decided per line, never by default), 27-row skipped-test resolution table, discuss-first authorization.
- `docs/architecture/adrs/adr-338-world-model-surface-and-dead-subsystems.md` — D1-D8, AC-1..6: `AuthorModel` as a cached-bind proxy view (not a subclass — the registries are instance fields), confirmed-deletion list for dead subsystems, character-model trait mutators recorded as the exception, trait-registration test, discuss-first authorization.
- `docs/architecture/adrs/adr-339-character-tick-sub-steps.md` — D1-D6, AC-1..5: seven sub-step modules moved beside their subsystems, two `stdlib/npc` modules moved home to `character` by `git mv` (a move, not a deletion), `CHARACTER_TICK_SUB_STEPS` as data, facade unchanged, byte-identical phases (the three Chord trees only — Dungeo has no character-model NPCs).
- `docs/architecture/adrs/adr-340-testing-assertion-core.md` — D1-D7, AC-1..5: one assertion-core module owned by `transcript-tester`, ten byte-identical functions moved outward first, eight diverged functions decided function-by-function with reasons recorded, a structural no-redefinition test, browser-safe headers, discuss-first authorization, sequenced before the testing-UX revamp.
- `docs/proposals/code-documentation-sweep.md` — the four-part header convention (purpose with no citations, public interface, owner context, `References:` block) this plan applies to every header it touches; P-6 defers the full package sweep to after each survey plan lands, so this plan's header rewrites are scoped to files it actually touches, not a package-wide sweep.
- GitHub issue #382 (`ChicagoDave/sharpee`) — lang-en-us: four unreferenced message tables (~330 lines, delete-after-confirmation), stale text-service comments (rewrite, no confirmation needed), two unreferenced grammar types (remove-or-keep-deliberately, David's call).
- GitHub issue #385 (`ChicagoDave/sharpee`) — parser-en-us: dead `tryMatchRule` matcher (175 lines, delete-after-confirmation), six unreferenced exports (remove-or-keep-deliberately), three skipped tests (implement/delete/note-gap, David's call); two long methods named as out-of-scope candidates for a later pass, not this plan.
- `docs/context/project-profile.md` — TypeScript strict mode and the layer-based per-package source structure this plan's moves must preserve; the `test:ci` / transcript-chain / branch-story-tree three-gate testing shape this plan's byte-identical checks reuse; "platform changes require discussion first" and this plan's own scope as that discussion's outcome for these seven ADRs and two issues only.
- `packages/stdlib/CLAUDE.md` — the real action-directory convention ADR-337 D4 corrects the doc to match (not every action has all six files), the capability-dispatch decision tree this plan's ADR-337 phases must not disturb, and the "world-state verification, not events alone" test-assertion standard this plan's own tests must meet.
- `packages/world-model/CLAUDE.md` — root barrel discipline (two barrels, rebuild `dist`/`dist-esm`) that ADR-338's deletions and ADR-339's `git mv` moves must observe when they touch exports.
- `docs/context/session-20260908-0127-main.md` (most recent session) — its Long Term open item records the survey's implementation as "unscheduled" per a prior-session ruling; David's instruction opening this session ("open a refactor branch and go ahead with the refactor work") explicitly supersedes that and is why this plan exists tonight.
- `/Users/david/repos/sharpee/CLAUDE.md` — never delete files without confirmation (every deletion below is a HELD phase naming the exact list), never auto-retry a failed build or test (every phase's exit state below says STOP, not fix-and-rebuild), and platform (`packages/`) changes require discussion first — tonight's instruction is that discussion's outcome, scoped to exactly these seven ADRs and two issues.

## Item-to-phase trace

| ADR / Issue | Decision(s) | Phase | HELD? |
|---|---|---|---|
| ADR-336 | D1 (exhaustive switches) | 1 | no |
| #382 | stale text-service comments | 2 | no |
| #382 | 4 message tables (delete), 2 unreferenced grammar types (keep/remove) | 3 | **HELD** |
| #385 | header convention (no other non-deletion item named) | 2 | no |
| #385 | dead `tryMatchRule` (delete), 6 unreferenced exports (keep/remove), 3 skipped tests | 3 | **HELD** |
| ADR-338 | D4 (trait-registration test), IParser migration, interfaces/ audit table | 4 | no |
| ADR-338 | D2 (extensions/, examples/, interfaces/parser.ts deletion), D7 (10-row skip table) | 5 | **HELD** |
| ADR-339 | D1, D2, D3 (all of it — the lightest plan in the survey) | 6 | no |
| ADR-338 | D1 (proxy view), D3 (ADR-310 D17 amendment) | 7 | no (depends on 5, 6) |
| ADR-340 | D1's first phase (read-only per-function diff table) | 8 | no |
| ADR-340 | D3 (fixture + no-redef test), D1 (moves + divergence decisions), D4 (headers), D2 (cleanup) | 9 | no (divergence decisions recorded with reasons, not HELD — not on the enumerated David-only list, and the byte-identical gate is the backstop) |
| ADR-336 | D2 (entity builders), D3 (`ANALYSIS_PASSES`) | 10 | no (D4 explicitly deferred — no phase) |
| ADR-335 | D4 (alias derivation), D2 (core assertion + restore reset), D3 (extension registry) | 11 | no |
| ADR-335 | D1 (thirteen runtime-section modules) | 12 | no |
| ADR-337 | D2 (one validation pipeline), D4 (docs correction) | 13 | no |
| ADR-337 | D1 (refusal-order per-line disposition), D3 (deletion list), D7 (27-row skip table) | 14 | **HELD** |
| ADR-334 | D3 (one platform dispatcher), D4 (dead `TurnEventProcessor` class removed — a block deleted from a file that stays, not a file deletion, per D4's own instruction) | 15 | no |
| ADR-334 | D1, D1a, D2 (turn stage list, `TURN_STAGES`/`META_STAGES`) | 16 | no |

## Phases

### Phase 0: Baseline capture
- **Tier**: Small
- **Budget**: 60
- **Domain focus**: N/A — establishes the regression net every later phase's byte-identical check compares against.
- **Entry state**: Branch `refactor/survey-adr-334-340` created from `main` at `aa1c81901`. A platform build is running in the background from the prior session — **wait for it to finish before running any command in this phase**; do not start a second build concurrently.
- **Deliverable**: Once the background build is confirmed finished, run and record (command, pass/skip count, date, output captured to `docs/work/refactoring-survey/baseline/`):
  1. `./repokit build dungeo`
  2. `node dist/cli/sharpee.js --test --chain stories/dungeo/walkthroughs/wt-*.transcript` (walkthrough chain)
  3. `node dist/cli/sharpee.js --test stories/dungeo/tests/transcripts/*.transcript` (unit transcripts — record the known-failure baseline per the project memory "Distinguish transcript test suites"; do not treat pre-existing unit-transcript failures as new)
  4. `./sharpee test branch-stories/secret-letter --tree`, `./sharpee test branch-stories/fernhill --tree`, `./sharpee test branch-stories/ides-of-march --tree`
  5. `pnpm --filter '@sharpee/engine' test`, `'@sharpee/story-loader'`, `'@sharpee/chord'`, `'@sharpee/stdlib'`, `'@sharpee/world-model'`, `'@sharpee/character'`, `'@sharpee/transcript-tester'`, `'@sharpee/branch-tester'`, `'@sharpee/lang-en-us'`, `'@sharpee/parser-en-us'` — the ten packages this plan touches
  A table in this plan's progress notes records every count with its command and date; every later phase's byte-identical check compares against this table, not a re-run of it.
- **Exit state**: Baseline counts and captured output exist and are recorded inline in this plan. If any baseline command fails outright (not "known skips," an actual failure), **STOP and report — do not fix and rebuild** (CLAUDE.md: never auto-retry). Line-number citations in the ADRs above were verified this session against `6ad2faeba`; current `HEAD` (`aa1c81901`) has already drifted by tens of lines in `runtime.ts`, `loader.ts`, and `analyzer.ts` (measured this session) — every phase below re-`grep`s for its target before editing rather than trusting a cited line number verbatim.
- **Baseline (2026-09-08, session 4a2d5f, branch `refactor/survey-adr-334-340` at `aa1c81901`; `./repokit build dungeo` exit 0, bundle 4,418,816 bytes)** — raw outputs captured under the session scratchpad `baseline/` directory; every later phase runs the same script (`run-gates.sh <dir>`) and `compare-gates.sh baseline <dir>`, which strips only the per-block `(NNNms)` wall-clock suffixes before a byte diff and compares vitest on its summary counts:

  | gate | command | result |
  |---|---|---|
  | Dungeo chain | `node dist/cli/sharpee.js --test --chain stories/dungeo/walkthroughs/wt-*.transcript` | exit 0, "All tests passed"; coverage 17 of 33 points fired |
  | Dungeo unit transcripts | `node dist/cli/sharpee.js --test stories/dungeo/tests/transcripts/*.transcript` | exit 1, **12 failing (known baseline, project memory)**; coverage 18 of 33 |
  | secret-letter tree | `./sharpee test branch-stories/secret-letter` | 566 cards passing, 965 assertions passing; 3854 commands |
  | fernhill tree | `./sharpee test branch-stories/fernhill` | 86 cards passing, 104 assertions passing; 222 commands |
  | ides-of-march tree | `./sharpee test branch-stories/ides-of-march` | 39 cards passing, 49 assertions passing; 75 commands |
  | chord | `pnpm --filter '@sharpee/chord' test` | 75 files, 1136 passing |
  | story-loader | | 118 files, 1083 passing |
  | world-model | | 85 files, 1504 passing, 10 skipped |
  | character | | 51 files, 599 passing |
  | stdlib | | 127 files, 1695 passing, 27 skipped |
  | engine | | 68 files, 682 passing, 7 skipped |
  | transcript-tester | | 23 files, 282 passing |
  | branch-tester | | 8 files, 104 passing |
  | parser-en-us | | 25 files, 328 passing, 3 skipped |
  | lang-en-us | | 28 files, 452 passing |

  The `--tree` flag in the deliverable's item 4 is not needed: `./sharpee test branch-stories/<story>` runs the tree document (the directory decides the harness, ADR-302 D16). Counts differ from the ADRs' 2026-09-07 figures (chord 1136 vs 1158, secret-letter 566 cards vs 1468) because those were measured on `feat/secret-letter-port`; this branch is off `main`, and this table is the baseline for it.
- **Status**: DONE (2026-09-08, session 4a2d5f)

### Phase 1: ADR-336 D1 — exhaustive `stmt.kind` switches
- **Tier**: Small
- **Budget**: 100
- **Domain focus**: Chord compiler structure — compile-time exhaustiveness only, no output change.
- **Entry state**: Phase 0 baseline recorded.
- **Deliverable**: The six `stmt.kind` switches (`analyzer.ts`: `checkConversationTargets`, `collectInlineTexts`, `checkPhaseOrder`, `resolveStatement`; `story-loader/src/runtime.ts`: `execStatements`; `story-loader/src/select-ids.ts`) each gain a `default` assigning to a `never`-typed binding with an unreachable throw; kinds a switch deliberately ignores get explicit `case` labels falling through to `break` instead of a bare default. No output change — this is AC-1's compile-time proof only.
- **Exit state**: `pnpm --filter '@sharpee/chord' test` and the story-loader suite at baseline counts or higher; a scratch addition to the `Statement` union fails `tsc` in all six switches (verify, then revert the scratch addition — do not leave it). If the suite count drops, **STOP and report — no fix-and-rebuild.**
- **Outcome (2026-09-08, session 4a2d5f)**: all six switches gain the `never` default; the three that ignored kinds (`checkConversationTargets`, `collectInlineTexts`, `select-ids` `walk`) list every ignored kind as an explicit `case`, and `checkPhaseOrder`'s "ended" default lists its ten kinds and keeps the same body. Both unions have 29 kinds (`Statement`: 28 interfaces, `CounterMutateStmt` carrying `'raise' | 'lower'`; `IRStatement`: 29 members, ADR-336's "23" was a miscount). `tsc --noEmit` clean in chord and story-loader. **AC-1 evidence**: a scratch `{ kind: 'scratch-kind' }` member added to `Statement` fails chord's `tsc` in all four analyzer switches (`analyzer.ts` 2505, 3668, 6080, 6603, each `TS2322 … not assignable to type 'never'`); the same member added to `IRStatement`, with chord's types re-emitted, fails story-loader's `tsc` in both of its switches (`runtime.ts:4400`, `select-ids.ts:83`); both scratch edits reverted (`git checkout`, grep for the scratch kind → 0 in both files) and chord's `dist` re-emitted clean. Suites: chord 1136 passing, story-loader 1083 passing (baseline counts). **Gate**: `./repokit build dungeo` exit 0; `compare-gates.sh baseline phase1` — three Chord trees byte-identical, Dungeo chain identical apart from its total `Duration` line, unit transcripts identical apart from `Seed: … (clock)` lines on the transcripts that pin no seed (both now stripped by the filter as wall-clock noise), all ten suites at baseline counts.
- **Status**: DONE (2026-09-08, session 4a2d5f)

### Phase 2: #382 / #385 — non-deletion cleanup
- **Tier**: Small
- **Budget**: 70
- **Domain focus**: N/A — comment and header correction, no logic change.
- **Entry state**: none (independent of Phase 1).
- **Deliverable**: Rewrite the four stale text-service references in `lang-en-us` (`language-provider.ts:5`, `:68`, `actions/quitting.ts:26`, and the fourth `grep` hit) per the header convention — state what the provider is now, not what it used to support; apply the code-documentation-sweep convention to any header this phase touches.
- **Exit state**: `grep -rni 'text service\|text-service' packages/lang-en-us/src` → 0; `pnpm --filter '@sharpee/lang-en-us' test` at baseline or higher; Dungeo chain byte-identical (comment-only change, so this should be a no-op check, not a re-run of the full suite — spot-check one walkthrough). **STOP, don't retry, on any mismatch.**
- **Outcome (2026-09-08, session 4a2d5f)**: `language-provider.ts` gains a convention-shaped header (purpose, public interface, owner, `References:` ADR-174 and ADR-158) replacing the "enhanced for text service" line; the `loadCoreMessages` doc comment now says what the messages are and who resolves them; the two `quitting.ts` comments say what the messages are for. `grep -rni 'text service\|text-service' packages/lang-en-us/src` → 0. lang-en-us suite 452 passing (baseline). Dungeo check: Phase 1's gate run (bundle rebuilt after these edits) is byte-identical apart from wall-clock lines, which covers this comment-only change. #385's header item: `english-grammar-engine.ts`'s header is touched in the HELD Phase 3 alongside its deletion, not here, so one edit covers the file once.
- **Status**: DONE (2026-09-08, session 4a2d5f)

### Phase 3: #382 / #385 — decisions for David
- **Tier**: Small
- **Budget**: 70
- **Domain focus**: N/A — a confirmation gate, per CLAUDE.md's never-delete-without-confirmation rule.
- **Entry state**: Phase 2 done (so the non-deletion items are already handled and this phase is deletions/keep-or-remove/skip-resolution only).
- **Deliverable**: Post the exact list, for David's confirmation before this phase executes anything:
  1. **Delete** (lang-en-us): `eventMessages`/`eventMessageFunctions` in `src/data/events.ts` (~192 lines), `failureMessages`/`systemMessages` in `src/data/messages.ts` (~163 lines) — confirm `IFEvents` and `ActionFailureReason` (same files) still have users before touching them.
  2. **Keep-or-remove, David's call** (lang-en-us): `EnglishToken`, `EnglishPrepositionProperties` in `grammar.ts`.
  3. **Delete** (parser-en-us): `tryMatchRule` in `english-grammar-engine.ts:135-309` (175 lines; `tryMatchRuleWithFailure` is the live one and stays).
  4. **Keep-or-remove, David's call** (parser-en-us): `DirectionWords`, `DirectionAbbreviations`, `getDirectionWord` in `direction-mappings.ts`; `MatchAttemptResult`, `ExtendedMatchOptions`, `MatchResult` in `english-grammar-engine.ts`.
  5. **Skip-test resolution, three rows, David's call** (parser-en-us): "should handle pattern mismatch", "should return multiple candidates", "should choose highest confidence pattern" — each implemented, deleted, or replaced with a one-line named-gap note.
  Once David confirms, this phase executes the confirmed deletions and the confirmed skip-test dispositions in one pass.
- **Exit state**: `pnpm --filter '@sharpee/lang-en-us' test` and `'@sharpee/parser-en-us' test` at baseline or higher; `grep -rn 'test.skip\|it.skip\|describe.skip' packages/parser-en-us/tests` → 0 (all three resolved one way or another); Dungeo chain and the three Chord trees byte-identical. **This phase does not run until David confirms the list. STOP and wait — do not infer confirmation.**
- **Outcome (2026-09-08, session 71ed1a; David: "confirm both")**: every candidate re-verified against HEAD before the edit. (1) The dead set was larger than the list: nothing imports `lang-en-us/src/data/events.ts` at all, and the barrel exports only `parserErrors` and `getParserErrorMessage` from `data/messages.ts` — so `events.ts` is deleted whole (its `IFEvents` copy included; the live constant is world-model's) and `ActionFailureReason`, `failureMessages`, `systemMessages` (260 lines) leave `messages.ts`, whose header now describes what remains. (2) `EnglishToken`, `EnglishPrepositionProperties` removed. (3) `tryMatchRule` deleted (175 lines). (4) `getDirectionWord` removed with `DisplayNames`, its only consumer's only reason to exist; `DirectionWords`/`DirectionAbbreviations` lose `export` (read by `parseDirection`); `ExtendedMatchOptions`, `MatchResult` removed; **`MatchAttemptResult` is not dead** — it is `tryMatchRuleWithFailure`'s return type — so it loses `export` and stays. (5) All three skipped tests deleted: "take in box" parses against the real grammar (validation refuses it, not the parser), bare "put down" parses as `put` with a missing object (carried to GH #388), `parseWithErrors`'s test predates the engine by its own comment. **Gate**: `tsc --noEmit` clean for both packages; lang-en-us 452 passing (baseline); parser-en-us 328 passing, 0 skipped (baseline 328 + 3 skipped); `grep skip packages/parser-en-us/tests` → 0; `./repokit build dungeo` exit 0 (bundle 4,402,865 bytes); `compare-gates.sh` — Dungeo chain, seeded unit suite, and the three trees IDENTICAL; every other suite at its recorded post-phase count.
- **Status**: DONE (2026-09-08, session 71ed1a)

### Phase 4: ADR-338 D4 — trait-registration test, `IParser` migration, `interfaces/` audit
- **Tier**: Medium
- **Budget**: 180
- **Domain focus**: world-model surface — a pinning test and a contract-ownership fix, no deletion yet.
- **Entry state**: Phase 0 baseline recorded (independent of Phases 1-3).
- **Deliverable**: (a) A test under `tests/unit/traits/` asserting every `TraitType` key (excluding the three category constants) has an `implementations.ts` entry whose class reports that type, and that the root barrel and `traits/index.ts` export every trait class and behavior — per `packages/world-model/CLAUDE.md`'s barrel-discipline note, this is exactly the check the project memory says is a trap. (b) The four `IParser` importers (`engine/src/command-executor.ts:21,143,162`, `engine/src/parser-interface.ts`, `engine/src/platform-operations.ts`; the fourth is `world-model/src/interfaces/parser.ts` itself — `core/src/extensions/types.ts` defines `IParserExtension` and does not import `IParser`, corrected by plan-review 2026-09-08) migrate to `Parser`/`BaseParser` from `@sharpee/if-domain` — confirmed the member sets match by reading both interfaces first. (c) An audit table (in this plan's progress notes) of the rest of `src/interfaces/` (`command-executor.ts`, `command-processor.ts`, `command-validator.ts`, `language-provider.ts`, `index.ts`) against `if-domain`, marking each "sole definition, stays" or "duplicate, candidate for deletion" — candidates feed Phase 5's delete list, they are not deleted here.
- **Exit state**: The new test passes and fails by name when a trait's `implementations.ts` entry or barrel export is removed in a scratch check (verify, then revert the scratch removal). `pnpm --filter '@sharpee/world-model' test` and `'@sharpee/engine' test` at baseline or higher. Dungeo chain and the three Chord trees byte-identical (the `IParser`→`Parser` migration is a type-only change if the member sets match; if they do not, **STOP and report the mismatch — do not silently widen either interface**). **STOP, don't retry, on any gate failure.**
- **Outcome (2026-09-08, session 4a2d5f)**:
  - **(a) DONE** — `tests/unit/traits/trait-registration.test.ts` (three cases: every `TraitType` key has an implementation whose class reports that type; every implementation class is in both barrels; every class or behavior a `src/traits/**/*Trait.ts` or `*Behavior.ts` file exports is in both barrels, compared by identity after importing the file). Clean: 3 passing. **AC-5 evidence**: with `[TraitType.EDIBLE]: EdibleTrait` commented out of `implementations.ts`, fails "no TRAIT_IMPLEMENTATIONS entry for: TraitType.EDIBLE ('edible')"; with `export * from './traits/edible/index.js'` commented out of `src/index.ts`, fails "not exported from src/index.ts: EdibleBehavior (edible/edibleBehavior.ts)"; both reverted (`git checkout`), final run 3 passing. A first draft derived class names from filenames and misreported `combatant/combatantBehavior.ts` (its class is `CombatBehavior`, reaching the root through `behaviors/index.ts:21`) and `vehicle/vehicleBehavior.ts` (helper functions, no class); the identity comparison replaced it. No barrel gap exists today. world-model suite 1507 passing, 10 skipped (baseline 1504 + 3); engine 682 passing (baseline).
  - **(b) NOT DONE — mismatch, reported**: `IParser.parse` returns `CommandResult<IParsedCommand, IParseError>` (`interfaces/parser.ts:16`); `if-domain`'s `BaseParser.parse` returns `any` (`parser-contracts/parser-types.ts:14`) and `Parser` adds `tokenize` and four optional engine hooks. Migrating engine's three importers to `Parser` would widen the parse result to `any` at the engine's one call site (`command-executor.ts:274`) and require every engine parser to expose `tokenize`; migrating to `BaseParser` loses the typed result. Neither is a type-only change, so per this phase's own exit state it stops here. **For David**: (i) tighten `BaseParser.parse` in `if-domain` to the typed result (if-domain already owns `IParsedCommand`'s shape? — verify) and migrate; (ii) keep `IParser` in world-model and strike it from ADR-338 D2's deletion list; or (iii) move `IParser` verbatim to `if-domain` as the typed base. Recorded in Phase 5's HELD list as a decision, not a deletion.
  - **(c) DONE — audit table** of `src/interfaces/` at `aa1c81901`:

    | file | exports | reachable from | importers outside the directory | if-domain owner | verdict |
    |---|---|---|---|---|---|
    | `parser.ts` | `IParser` | `interfaces/index.ts` → root | engine `command-executor.ts`, `parser-interface.ts`, `platform-operations.ts` | `BaseParser` / `Parser` (member sets differ, above) | decision (b) |
    | `command-executor.ts` (18 lines) | `ICommandExecutor` | root | none (`grep` across `packages/*/src`, `tools/*/src`, world-model tests) | none | dead — HELD delete candidate |
    | `command-processor.ts` (18 lines) | `ICommandProcessor` | root | none | none | dead — HELD delete candidate |
    | `command-validator.ts` (18 lines) | `ICommandValidator` | root | none | none | dead — HELD delete candidate |
    | `language-provider.ts` (93 lines) | `ILanguageProvider`, `ILanguageProviderRegistry` | **nothing** — not exported by `interfaces/index.ts` or the root | none | `LanguageProvider`, `ParserLanguageProvider` | dead and unreachable — HELD delete candidate |
    | `index.ts` | barrel | root | — | — | shrinks with the above |

  Gate: no shipped source changed in this phase (one test file added), so the Dungeo chain and the three trees are the Phase 1 run's; not re-run.
- **Status**: DONE for (a) and (c); (b) carried to Phase 5 as a decision (2026-09-08, session 4a2d5f)

### Phase 5: ADR-338 — decisions for David
- **Tier**: Small
- **Budget**: 80
- **Domain focus**: N/A — a confirmation gate.
- **Entry state**: Phase 4 done (its audit table feeds this phase's delete list).
- **Deliverable**: Post the exact list, for David's confirmation before this phase executes anything:
  1. **Delete**: `src/extensions/` (`base-extension.ts`, `loader.ts`, `manager.ts`, `registry.ts`, `types.ts`, `index.ts` — 1,100 lines, exported from the root barrel, constructed by nothing); `src/examples/event-handler-registration.ts` (174 lines, unexported, imported by nothing); `src/interfaces/parser.ts` — **now a decision, not yet a deletion**: Phase 4 found `IParser` and `if-domain`'s `BaseParser`/`Parser` differ in member sets (typed result versus `any`; `tokenize`), so the migration stopped per its exit state; David picks (i) tighten `BaseParser.parse` in if-domain and migrate, (ii) keep `IParser` and strike it from D2, or (iii) move `IParser` verbatim to if-domain — the file is deleted only under (i) or (iii); **and the three dead command interfaces plus the unreachable language-provider file Phase 4's audit found**: `src/interfaces/command-executor.ts`, `command-processor.ts`, `command-validator.ts` (18 lines each; exported from the root, imported by nothing anywhere, no if-domain twin) and `src/interfaces/language-provider.ts` (93 lines; not exported by `interfaces/index.ts` or the root, so unreachable; if-domain's `LanguageProvider` is the live contract) — with the three barrel lines in `interfaces/index.ts`.
  2. **Skip-test resolution, ten-row table, David's call**: `scope/magic-sight.test.ts` (4), `scope/window-visibility-fixed.test.ts` (3), `scope/darkness-light.test.ts` (1), `unit/world/get-in-scope.test.ts` (1), `integration/wearable-clothing.test.ts` (1) — each implemented, deleted, or replaced with a one-line named-gap note.
  Once David confirms, this phase executes the confirmed deletions (including the barrel-line removals) and the confirmed skip-test dispositions in one pass.
- **Exit state**: `packages/world-model/src/extensions/`, `src/examples/`, `src/interfaces/parser.ts` absent; `grep -rn IParser packages/*/src` → 0; `grep -rn 'it.skip\|test.skip\|describe.skip' packages/world-model/tests` → 0; `pnpm --filter '@sharpee/world-model' test` at baseline or higher (minus the ten former skips, now resolved one way or another); Dungeo chain and the three Chord trees byte-identical. **This phase does not run until David confirms both lists. STOP and wait.**
- **Outcome (2026-09-08, session 71ed1a; David: "confirm both")**: (1) Deleted: `src/extensions/` (six files, 1,100 lines; the only outside `ExtensionRegistry` hit is core's own unrelated class), `src/examples/event-handler-registration.ts`, and the four audit-found files `src/interfaces/command-executor.ts`, `command-processor.ts`, `command-validator.ts`, `language-provider.ts`; the root barrel's extension line and `interfaces/index.ts`'s three lines removed; `interfaces/index.ts`'s header now says what the directory holds. **`IParser` stays** (David, option ii): its result types `IParsedCommand`, `IParseError`, `CommandResult` live in `world-model/src/commands/` and `if-domain` sits below world-model, so neither (i) nor (iii) is possible without moving the command types too; ADR-338 Amendment A2 records the ruling and withdraws AC-2's `grep IParser → 0` line; the engine's three importers are untouched. (2) Ten-row table: the eight tests whose titles name the architectural reason (`magic-sight` ×4, `window-visibility-fixed` ×3, `darkness-light` ×1 — every one asserts `getVisible` honors a `ScopeRule`, which the platform rejects) **deleted**; `wearable-clothing` "items in pockets visibility" **un-skipped and kept** (passes as written); `get-in-scope` "deeply nested items" **deleted** — un-skipped it fails (`getInScope` stops one container level down), carried to GH #389. **Gate**: `tsc --noEmit` clean for world-model and engine; world-model 1512 passing, 0 skipped (1511 + the pockets test); `grep skip packages/world-model/tests` → 0; `./repokit build dungeo` exit 0 (bundle 4,394,151 bytes); regenerated `genai-api/world-model.md` has no `Extension*` entry (the one remaining match is the phrase "ADR-090 Extension" in a doc comment); `compare-gates.sh` — Dungeo chain, seeded unit suite, and the three trees IDENTICAL; every other suite at its recorded post-phase count.
- **Status**: DONE (2026-09-08, session 71ed1a)

### Phase 6: ADR-339 — character tick sub-steps come home
- **Tier**: Medium
- **Budget**: 250
- **Domain focus**: `@sharpee/character` — the tick's sub-steps move beside the subsystems they drive; `stdlib/npc/` becomes exactly the decision layer core-concepts describes.
- **Entry state**: Phase 0 baseline recorded (independent of Phases 1-5, though sequenced here per the umbrella's cost/risk order — "the lightest plan in the set").
- **Deliverable**: (a) **D3 first**: the seven spread calls in `createCharacterModelPhase` become an exported `CHARACTER_TICK_SUB_STEPS` array of `{ name, run, requires }` (the `requires` idiom — five of the seven sub-steps read/write the shared `surface` accumulator per the umbrella's Elegance section; state each entry's real dependency, not just list order), iterated by the phase; a new case in `character-model-phase.test.ts` asserts the seven names in order, that a `requires` violation would fail by name, and that one tick runs each exactly once; the header's contract sentence moves onto the array as its doc comment, in the header-convention shape (logic first, `References:` block after). Amend ADR-339 with a short note under D3 recording that `CHARACTER_TICK_SUB_STEPS` carries `requires`. (b) **D1**: each of the seven sub-step bodies moves out of `tick-phases.ts` into its subsystem's directory (`conversation/scene-sub-step.ts` + `applySeizedExchange`, `goals/goal-sub-step.ts` + `executeNpcGoals`, `influence/influence-sub-step.ts` + `handleInfluenceResults`, `propagation/propagation-sub-step.ts`, `act-detection/observe-sub-step.ts`, `arbiter/decay-sub-step.ts`, `conversation/arrival-sub-step.ts`) — no body changes, largest first (scene, goal, influence, propagation, arrival); shared helpers (`recordTransfer`, `authorNarratesArrival`, `mutation`) go with their first caller or into a `tick-support.ts` beside the tick, decided by fan-in as the ADR specifies. Each new module gets a rule-9, convention-shaped header. (c) **D2**: `git mv packages/stdlib/src/npc/lucidity-decay.ts packages/character/src/arbiter/lucidity-decay.ts` and `git mv packages/stdlib/src/npc/character-observer.ts packages/character/src/act-detection/character-observer.ts` with their tests moved alongside — this is a move preserving content, not a deletion, and may run without confirmation per this plan's hard constraints; update the one import (`CharacterMessages` from `@sharpee/stdlib`, already imported that way elsewhere in the tick) and remove the four re-exports from `packages/stdlib/src/npc/index.ts`.
- **Exit state**: `tick-phases.ts` contains no `run*SubStep`/`runArrivalReactions` body; `packages/stdlib/src/npc/` contains no `lucidity-decay.ts` or `character-observer.ts`; `grep -rn 'processLucidityDecay\|observeEvent\|DefaultStateTransitions' packages/stdlib/src` → 0; `pnpm --filter '@sharpee/character' test` and `'@sharpee/stdlib' test` at baseline or higher. `./sharpee test branch-stories/secret-letter --tree` and `branch-stories/ides-of-march --tree` byte-identical (both carry character-model lines); `branch-stories/fernhill --tree` byte-identical (proves nothing else moved). **STOP, don't retry, on any gate failure or count drop.**
- **Outcome (2026-09-08, session 4a2d5f)** — three checkpoints, each gated on the three trees byte-identical (`./sharpee test branch-stories/<story>` after `pnpm --filter … build`; the `--tree` flag is not a flag of that command):
  - **D3 (`b843c077f`)**: `CHARACTER_TICK_SUB_STEPS` as `{ name, requires, run }` with a uniform `TickSubStepRun` argument (the seven bodies keep their signatures; each entry's `run` adapts); `subStepOrderViolations`; `tests/tick-phases/sub-step-order.test.ts` (five cases: names in order, no violations, a named violation on a swapped copy, a missing requirement named, one tick over two modeled NPCs runs each once in order via `vi.spyOn` on the entries). Header rewritten in the sweep shape. Character 604 (599 + 5). Behavior Statement for `createCharacterModelPhase` given in conversation.
  - **D1 (`78b249a77`)**: the split done by a script keyed on measured line numbers with an assertion at every boundary, bodies copied verbatim, `export` added to each sub-step's own `function` line only. Seven modules plus `tick-support.ts` (context, surface, `ArrivedFact`, `createEvent`, `sceneWrappable`, `emptySceneTickSurface`); `buildRoomGraph` with its only caller in `goals/goal-sub-step.ts`; helpers by fan-in as the ADR specifies (the two the ADR named as functions were locals). `tick-phases.ts` 1635 → 321 lines, re-exporting the three shared types so existing importers keep one path. AC-1: `grep -c 'function run' tick-phases.ts` → 0; each of the seven defined once in its subsystem. Character 604.
  - **D2 (this commit)**: `git mv` of `lucidity-decay.ts` → `arbiter/`, `character-observer.ts` → `act-detection/`, and the 35-case test → `tests/act-detection/`; `CharacterMessages` now from `@sharpee/stdlib`; both subsystem barrels export the moved names; the decay and observe sub-steps import locally; `stdlib/src/npc/index.ts` drops the two re-export blocks; the integration test imports from the subsystem paths. AC-2: `grep -rn 'processLucidityDecay\|observeEvent\|DefaultStateTransitions' packages/stdlib/src` → 0. stdlib 1660 (1695 − 35 moved), character 639 (604 + 35). Both `tsc --noEmit` clean.
  - ADR-339 gains Amendment A1 recording `requires`, the support module, and the counts.
- **Status**: DONE (2026-09-08, session 4a2d5f)

### Phase 7: ADR-338 D1 + D3 — the proxy view and the recorded exception
- **Tier**: Medium
- **Budget**: 220
- **Domain focus**: world-model — `AuthorModel` becomes a cached-bind proxy of the live world; the character-model trait's mutators are recorded as the deliberate exception to "behaviors own mutations."
- **Entry state**: Phase 6 done. (Plan-review 2026-09-08: ADR-338 D6's order D4, D2, D1, D3 is by diff size, not dependency — `AuthorModel.ts` references nothing under `extensions/` or `examples/` — so this phase runs ahead of the HELD Phase 5 rather than waiting on a confirmation David cannot give tonight; the inversion is recorded here and in ADR-338's amendment note when D1 lands.) Phase 6 done (ADR-338 D3's amendment paragraph names the tick's sub-steps as the mutators' callers, so D3 must land after ADR-339 D2, per the umbrella's Alignment finding).
- **Deliverable**: (a) `createAuthorModel(world: WorldModel): AuthorModel` returns a `Proxy` over the live `WorldModel` instance (not a subclass — the registries are instance fields, so a second instance would not see the registrations the four construction sites make during `initializeWorld`). The `get` trap forwards every member to the live world, **binding once per member per view and caching the result** (the umbrella's snippet: a `Map<PropertyKey, unknown>` cache, only function values cached, non-function members always read through live), except the two bypasses (`createEntity`, `moveEntity` — same bodies as today) and the three helpers (`populate`, `addTrait`, `removeTrait`). **`canMoveEntity` joins the override list and keeps returning `true`** — the umbrella's recorded decision, preserving the author model's promise that a placement never fails; record this as a one-line comment on the override, not silently. The four construction sites (`story-loader/src/loader.ts:489`, three `helpers/src/builders/*.ts`) become `createAuthorModel(world)`. **The three Dungeo sites keep `new AuthorModel(dataStore, world)` as a working spelling** — the class constructor returns the proxy, so the spelling still works; this is not a Dungeo edit. The 89 delegations and the `AuthorModel` class body (minus the constructor-returns-proxy shim) are deleted from `AuthorModel.ts`; the file's header states why `Object.create` was rejected (writes would shadow the world's own fields, since it has no private fields) so a future reader does not "simplify" it. `IWorldModel` is unchanged (Q-1's ruling stands). (b) ADR-310 gains a short amendment paragraph under D17 stating the character-model trait's eleven mutators live on the trait by design (a behavior would hold no state and only forward, the shape D1 just deleted from `AuthorModel`), naming the tick's sub-steps (Phase 6) as the mutators' callers; `docs/core-concepts/README.md`'s "Behaviors vs Actions" section gains one sentence naming the same exception; `characterModelTrait.ts`'s header states it too, in the convention shape.
- **Exit state**: `grep -c 'this.worldModel\.' packages/world-model/src/world/AuthorModel.ts` → 0; a test registers an event handler and a capability through the author view and asserts the live `WorldModel` holds them (the case a subclass draft would have failed); the existing `AuthorModel` tests pass unchanged; `pnpm --filter '@sharpee/world-model' test` at baseline or higher. Dungeo chain and the three Chord trees byte-identical (the Secret Letter tree's boot and the helpers' builders placing items through the author view are the real-path test). **STOP, don't retry, on any gate failure.**
- **Outcome (2026-09-08, session 4a2d5f)**:
  - **D1**: `AuthorModel.ts` rewritten (658 → ~190 lines): `createAuthorModel(world)` returns a `Proxy` over the live world; overrides are `createEntity` and `moveEntity` (bodies verbatim, closing over the shared store), **`canMoveEntity` kept returning true**, and the three helpers; everything else forwards bound-once-per-member with a `Map` cache, non-function members read live. `AuthorModel` is the type `WorldModel & AuthorHelpers` plus a constructor shim, so all **twelve** construction sites (not seven: the loader, three helpers builders, three Dungeo regions, Dungeo's GDT context, `extensions/testing`'s debug context, the devkit fixture, and the doc example) compile unchanged. `grep -c 'this.worldModel\.'` → 0. The 13 existing `AuthorModel` cases pass unchanged; four new cases: a capability registered through the view is on the live world, the view is the same instance (`instanceof WorldModel`, shared entity identity), one bound function per member per view, and a placement into a closed container succeeds through the view while `world.canMoveEntity` says no. world-model 1511 passing (1504 + 3 + 4); engine 682.
  - **D3**: ADR-310 gains the D17 amendment paragraph naming the eleven mutators and their callers (the tick sub-steps first); `docs/core-concepts/README.md` names the exception in one sentence under "Behaviors vs Actions"; `characterModelTrait.ts`'s header states it.
  - **Gate**: `./repokit build dungeo` exit 0; Dungeo chain IDENTICAL; three trees IDENTICAL; every suite at baseline or explained (world-model +7, character +42 = 35 moved + 5 order + 2 influence, stdlib −35). **The unit-transcript suite differed** (12 → 19 failing): traced, read-only, to `royal-puzzle-basic.transcript`, which pins no seed — at the failing run's own clock seed (1788864161240) the thief lunges in the maze on `south`, knocks the player out on `take card`, the engine ends the game in defeat, and the next command reports "Engine is not running"; alone it passes 21 of 21, and the exec replay of its commands is clean. Not a regression. **Gate change recorded**: from this phase on the unit suite runs with `--seed 1` so runs are comparable; the seed-1 capture on this tree (1693 passing, 32 failing, 3 transcript errors) is the reference for later phases, and the clock-seed capture is kept beside it. The chain and the trees remain the byte-identical gates that pin their own seeds.
  - Rule 15: `mutation-verification` ran on Phase 6's changes and found one gap, the influence sub-step's wiring with no tick-level state assertion; `tests/tick-phases/influence-sub-step.test.ts` now drives a room-range passive influence through the real tick and asserts the target trait's `influencesInForce`, then expiry by separation on the next tick (2 cases, passing).
  - ADR-338 gains Amendment A1.
- **Status**: DONE for D1 and D3 (2026-09-08, session 4a2d5f)

### Phase 8: ADR-340 — read-only per-function diff table
- **Tier**: Small
- **Budget**: 100
- **Domain focus**: N/A — analysis only, no code change; can run independently of every other phase.
- **Entry state**: none.
- **Deliverable**: A table (in this plan's progress notes) of the 20 functions defined in both `transcript-tester/src/runner.ts` and `branch-tester/src/runner.ts` (or wherever `grep -n 'function evaluateStateExpression\|function checkAssertion\|...'` finds them at current `HEAD`, since line numbers have drifted), each row: function name, similarity score (re-measured, not copied from the ADR's 2026-09-07 numbers), which behavior is kept for the ten diverged/near-identical ones and why, and — where one side missed a fix the other received — which test now pins the corrected side. Confirm the ADR's finding that `synthesizePolicyAssertions` is defined in both and is the one the IDE's testing-surface build reaches through branch-tester (`tools/ide/web/testing-surface/build.mjs`), so it belongs in the shared core.
- **Exit state**: Table complete and inline in the plan; no code changed, no gate re-run needed (read-only). No STOP condition — this phase cannot fail a build.
- **Outcome (2026-09-08, session 4a2d5f)**: `docs/work/refactoring-survey/adr-340-function-diff.md` — the table (36 rows), the five keep decisions, the synthesis-engine comparison, and the twenty-one-name core set for Phase 9.
- **Status**: DONE (2026-09-08, session 4a2d5f)

### Phase 9: ADR-340 D3, D1, D4, D2 — one assertion core
- **Tier**: Large
- **Budget**: 350
- **Domain focus**: the seam between `transcript-tester` and `branch-tester` — one evaluator of the claim language both testing worlds write in.
- **Entry state**: Phase 8's diff table complete — `docs/work/refactoring-survey/adr-340-function-diff.md` (2026-09-08, session 4a2d5f): fifteen functions shared verbatim (fourteen byte-identical, one differing only by `export`; four have converged since the ADR's count of ten), five diverged with a keep decision each (`errorResult` and `runCommand` as supersets, `checkAssertion`'s channel arm from branch-tester over the structured capture, `runAssertion` and `openingResult` staying as each runner's own), `synthesizePolicyAssertions` byte-identical across `runner.ts` and `auto-assertion.ts`, and `aggregateTestRun` defined in both packages. Twenty-one names for the core; no missed fix expected to move a gate. **Design settled for this phase but not executed unattended (2026-09-08, ~06:30 CDT, session 4a2d5f)** — three facts read this session: (1) resolution: `tsconfig.base.json` uses `moduleResolution: node`, which ignores `exports` subpaths, so the core is reached three ways, exactly as `@sharpee/sharpee/runtime-surface` is today: a `"./assertion-core"` entry in `packages/transcript-tester/package.json` `exports` (which has none yet; add `"."` beside it) for Node, a `paths` entry plus a `../transcript-tester` reference in `packages/branch-tester/tsconfig.json` for `tsc` (`packages/runtime/tsconfig.json:9-11` is the pattern), and one `alias` line in `tools/ide/web/testing-surface/build.mjs:43-48` for the browser bundle; vitest's `workspaceAliases()` already maps every subpath to `src/`. (2) The core module must be browser-safe: transcript-tester's `types.ts` is type-only, so the core may import types from it, but never the barrel. (3) **`runCommand` is not a move**: branch-tester's version (the keep decision) calls `endingFrom` and `captureWorldSnapshot`, both branch-tester-only tree helpers, so lifting it means injecting those through `RunnerOptions` — a signature design, with both `CommandResult` types unified to the superset. The same holds for `checkAssertion`'s channel arm, which calls `checkChannelAssertion` from `channel-assert.ts`. And the IDE's committed bundle (`tools/ide/SharpeeIDE/Resources/testing-surface/surface.js`) is regenerated by the alias change. Recommendation: run this phase with David at the keyboard, in this order — D3's fixture test first (read-only against the current copies; it will fail by design until D1 lands), then the fifteen verbatim functions plus `synthesizePolicyAssertions` and `aggregateTestRun` (mechanical), then `errorResult`, then the two that need injection as their own decision.
- **Deliverable**: (a) **D3 first**: a structural test in `branch-tester` asserting its runner defines no function by any name in Phase 8's shared-function fixture and imports each from `@sharpee/transcript-tester`; a second copy of any listed name fails the test by name. (b) **D4**: rule-9 headers on `parser.ts` and `branch-tester/src/runner.ts` in the convention shape; the `Streamable*` types and `ReporterOptions` removed or kept with a stated reason (David's call if ambiguous — otherwise "remove, unreferenced" per the ADR's own finding). (c) **D1**: `@sharpee/transcript-tester` exports an `assertion-core` module (no Node import — `fs`/`path` stay in the seven modules that already have them) carrying the ten byte-identical functions first (zero-risk moves), then the eight diverged functions per Phase 8's table, each move a recorded decision; `packages/branch-tester/package.json` gains `@sharpee/transcript-tester` as a dependency and its runner imports the core; `branch-tester/src/types.ts`'s re-exports from transcript-tester become `export type` so the browser bundle never tries to resolve a package whose barrel imports `fs`. Amend ADR-307 with a one-line note beside its "Untouched" statement (`:23-24`) recording that transcript-tester now exports the assertion core branch-tester consumes; amend ADR-302 D15 with the same note; rewrite the two code comments that state the now-false premise (`tools/repokit/src/repo.ts:54-59`, `packages/branch-tester/src/auto-assertion.ts:8-10`) in the same phase. (d) **D2**: the golden tier, the tree/walker/auto-assertion/Chord-claim code stay where they are — no cleanup expected beyond what D1's moves leave behind.
- **Exit state**: `grep -n 'function evaluateStateExpression\|function checkAssertion\|function checkEventAssertion\|function executeDirective\|function configureRandomInstruments' packages/transcript-tester/src packages/branch-tester/src` finds each name once, under `transcript-tester/src`; the D3 test fails by name when a shared function is redefined in branch-tester (verify, then revert the scratch redefinition); `pnpm --filter '@sharpee/transcript-tester' test` and `'@sharpee/branch-tester' test` at baseline or higher; the IDE testing-surface build (`tools/ide/web/testing-surface/build.mjs`) still resolves with no source change. Dungeo chain, unit transcripts, and the three Chord trees report the same counts and byte-identical output. **STOP, don't retry, on any gate failure or count drop.**
- **Outcome (2026-09-08, session 13688e; David: "go" on the four calls, "go" on the rebuild)**: **D3 first** — `packages/branch-tester/tests/assertion-core-ownership.test.ts` + `tests/fixtures/assertion-core-names.json` (26 names): no `src/*.ts` declares a pinned name, every use is bound from an `@sharpee/transcript-tester` specifier (import or export-from). Against the copies: 52 failed by name. **Phase 8's table corrected**: its extractor cut a function at the first line whose braces balanced, so `evaluateStateExpression` (signature `): { matches; details? } {`) was misread as identical — branch-tester's carried the `story.state =` and Chord `the X is Y` forms (GH #355) on three `@sharpee/story-loader` constants — and `runTranscript` was never comparable (each runner's facade; stays). Also `configureRandomInstruments` uses `path.basename` and `executeDirective` uses `fs`/`path`, so one browser-safe module was not possible. **Four calls made attended**: (1) two modules — `src/assertion-core.ts` (browser-safe evaluator + synthesis; `./assertion-core` package subpath, `typesVersions` for `tsc` consumers) and `src/command-core.ts` (Node: `runCommand`, directives, instruments, results), both barrel-exported; (2) the Chord state forms are a `StoryStateKeys` parameter (`RunnerOptions.storyStateKeys`; branch-tester exports `CHORD_STORY_STATE_KEYS` from the loader's constants), so transcript-tester gains no dependency and the evaluator stays bundleable; (3) `channel-assert.ts` moved whole with its test (ADR-340 D2 narrowed by Amendment A1; branch-tester's barrel re-exports the three names); (4) `runCommand` moved with `endingFrom`/`worldEntityRef`/`captureWorldSnapshot`, which read only the engine seam — no injection. **D1 as built**: transcript-tester's `types.ts` is the superset (tree fields optional, four channel kinds added; the serializer names those four as unserializable so the `.transcript` grammar is unchanged); branch-tester's `types.ts` is `export type *` from the core; branch-tester's runner is `runTranscript`/`openingResult`/`runAssertion` (1500 → 321 lines); `auto-assertion.ts` re-exports the two synthesis functions; `aggregateTestRun` is transcript-tester's; `allAssertionsOf` (unused) went with the rewrite. `errorResult` spreads `tier`/`goldenPath` conditionally; `runCommand` evaluates channels over `lastChannelValues ?? lastChannels`. **D4**: `parser.ts` and branch-tester's `runner.ts` headers; `Streamable*` ×3 and `ReporterOptions` kept (parameter types of barrel-exported methods) and now barrel-exported. IDE: alias in `build.mjs`, `tsconfig.json`, `vitest.config.ts`; `surface.js` regenerated (diff: the `proseTextLinesOf` banner moves to `transcript-tester/src/assertion-core.ts`, body identical). ADR-340 Amendment A1; ADR-307 note beside "Untouched"; ADR-302 D15 amended; `tools/repokit/src/repo.ts` comment rewritten. mutation-verification's three gaps closed in `transcript-tester/tests/assertion-core.test.ts` (9 tests: forms without keys fall through, flattened fallback, serializer throw). **AC-1 evidence**: scratch `normalizeOutput` in branch-tester's runner → "normalizeOutput is redefined in src/runner.ts" + "used … without an import", 2 failed; reverted → 53 passed; five-name grep finds each once under `transcript-tester/src`. **Gate**: first `./repokit build dungeo` failed in devkit's `tsc` (branch-tester's declaration shim re-exported a subpath `moduleResolution: node` cannot see → `any`); fixed with `typesVersions` (resolution trace + type probe + clean-tsbuildinfo devkit check), rebuilt on David's word: exit 0, bundle 4,369,112 bytes. `compare-gates.sh baseline phase9`: Dungeo chain, seed-1 unit suite, and the three trees **IDENTICAL**; chord 1136, story-loader 1083, lang-en-us 452 at baseline; world-model 1512, character 641, stdlib 1664, engine 685, parser-en-us 328/0 skipped at their recorded post-phase counts; transcript-tester 25 files **314** (282 + 23 moved + 9 new), branch-tester 8 files **134** (104 − 23 moved + 53 D3). IDE testing-surface suite 91 passing, `tsc` clean.
- **Status**: DONE (2026-09-08, session 13688e)

### Phase 10: ADR-336 D2, D3 — per-line entity builders and the named pass list
- **Tier**: Large
- **Budget**: 320
- **Domain focus**: the Chord compiler — `buildEntity` becomes a runner over declared builders; `Analyzer.run`'s 45 passes become data.
- **Entry state**: Phase 1 done (D1's exhaustiveness lands first per D6's own order).
- **Deliverable**: (a) **D3 first**: `Analyzer.run`'s 45 calls become an exported `ANALYSIS_PASSES` constant, each entry `{ name, run, requires }` (the `requires` idiom — e.g. `resolveOverrideGates` requires `buildTimers`; `buildChapters` requires `buildEntity`, `buildTimers`); `run` iterates it; a test asserts the list order satisfies every `requires` and that one corpus compile runs every pass exactly once in list order — the GH #359 defect class becomes a failing test naming both passes when `resolveOverrideGates` is moved above `buildTimers` in a scratch check (verify, then revert). Amend ADR-336 with a short note under D3 recording that `ANALYSIS_PASSES` carries `requires`. (b) **D2**: `packages/chord/src/analyzer/entity/` holds one module per entity-block line kind (role/player lines, `starts`, `proper`/pronouns, the ADR-310 character lines, placement, `landing`, `first time`, exits, the rest), each `(decl, entity, context) => void`; `buildEntity` becomes the runner creating the `IREntity`, calling `ENTITY_LINE_BUILDERS` in a declared order, returning it; builder order pinned by a test that fails by name when a builder is moved before one it reads from; each builder's header carries the ADR that added its line kind in the `References:` block, convention-shaped. Builder-by-builder from the last branch of the 920-line method upward.
- **Exit state**: `buildEntity` contains no line-kind logic; `ANALYSIS_PASSES` exists with the order test passing; `pnpm --filter '@sharpee/chord' test` at baseline or higher. Compiled IR of every story under `packages/chord/tests`, `packages/story-loader/tests`, and `branch-stories/` byte-identical (JSON) to Phase 0's baseline; the three Chord trees byte-identical; diagnostic-code set unchanged (a `grep`-derived list, checked against Phase 0's baseline). **STOP, don't retry, on any gate failure.**
- **Status**: PENDING

### Phase 11: ADR-335 D4, D2, D3 — the alias table, the core's mutable fields, the extension registry
- **Tier**: Medium
- **Budget**: 220
- **Domain focus**: `story-loader` — the seam-facing fixes that don't move the 5,000-line runtime yet.
- **Entry state**: Phase 0 baseline recorded (independent of Phases 1-10 except the sequencing note below).
- **Deliverable**: (a) **D4 first** (a derivation, no runtime code moves): `./repokit aliases` (beside `grammar`, `tools/repokit/src/commands/`) derives `@sharpee/chord`'s `message-alias-catalog.ts` (names only) from `story-loader`'s curated `message-alias-map.ts`; `./repokit aliases --check` joins `./repokit verify` beside the grammar freshness gate; the existing bijection test (`message-alias-map.test.ts:29-55`) stays. ADR-255 gains a one-line amendment note under D2 recording the derivation. (b) **D3**: `ExtensionRegistration` gains an `installFromIR(ir, world, engine, core)` slot; the five loader methods and two inline blocks for the four config-dependent extensions (hunger, chapters, state machines, combat/NPC adjectives) move into their own registry entries; `loader.ts` imports no `@sharpee/ext-*` or `@sharpee/plugin-state-machine` package afterward; the engine-ready path becomes one loop over `ir.uses` calling the slot. (c) **D2**: the runtime header's false invariant ("holds no turn-scoped mutable fields") is rewritten to state the real split — re-entrancy guards and per-turn buffers (named), asserted empty at the top of each per-turn entry point, and two cross-turn counters (`eventSeq`, `lastTickTurn`); `ChordStory.onWorldRestored` gains one call to `RuntimeCore.resetAfterRestore(turn)`; a guard left set between turns throws, naming the field — this lands **before** Phase 12's section moves, per D6's order, so a move that leaks a guard is caught immediately.
- **Exit state**: `./repokit aliases` regenerates the catalog with no diff at HEAD; adding a row to the map without regenerating fails `--check` naming the catalog; `loader.ts` has no `@sharpee/ext-*`/`plugin-state-machine` import; a story with `use hunger` and one with `use state-machines` boot and behave unchanged (existing story-loader tests); a test leaving a guard set and entering the next turn throws naming the field; `pnpm --filter '@sharpee/story-loader' test` at baseline or higher. Three Chord trees byte-identical. **STOP, don't retry, on any gate failure.**
- **Status**: PENDING

### Phase 12: ADR-335 D1 — thirteen runtime-section modules
- **Tier**: Large
- **Budget**: 400
- **Domain focus**: `story-loader` — the largest mechanical move in the survey; the runtime's own ruler comments become the module boundaries.
- **Entry state**: Phase 1 done (ADR-336 D1 must land before this phase touches `runtime.ts:4168`'s statement switch, per the umbrella's one sequencing dependency), Phase 10 done (same reason — D1's exhaustiveness and D2's builder split both touch the statement-handling area this phase relocates), Phase 11 done (D2's guard assertion must be in place before a section move can leak a guard, and D4/D3 are D6's stated predecessors).
- **Deliverable**: `packages/story-loader/src/runtime/` gains thirteen modules, one per ruler-marked section (`bind`, `event-clauses`, `move-clauses`, `on-clauses`, `topic-tables`, `dialogue`, `conversation-threads`, `dispatch-verbs`, `scheduler-constructs`, `timers`, `derived`, `statements`, `phrases`), each taking its section's fields with it — no method changes body. `RuntimeCore` carries the five shared helpers (`dialogueTurn`, `rawEvent`, `placeOf`, `narrated`, `sourced`) and the truly shared state. Each section exports `bind<Section>(core, ir, world): void`; `bind` calls the thirteen in today's order. **The `requires` idiom applies to the bind order too** (the umbrella's instruction to amend ADR-335 D1): the order is pinned by an exported list with per-entry `requires` (which sections' fields or registrations an entry depends on) and a test that asserts the order satisfies every `requires` and that a corpus load calls each `bind<Section>` exactly once. Amend ADR-335 with a short note under D1 recording that the bind order carries `requires`. `ChordRuntime` remains the facade the loader constructs; it delegates. Order: largest self-contained sections first (`buildTopicArm`, `buildDispatchAction`), then dialogue and thread sections, then the rest. Every new module gets a rule-9, convention-shaped header.
- **Exit state**: `packages/story-loader/src/runtime/` contains thirteen modules; `runtime.ts` contains the facade and `RuntimeCore` only; `grep` of `from './'` across `runtime/` shows only `core` and existing package helpers; `pnpm --filter '@sharpee/story-loader' test` at baseline or higher. Three Chord trees byte-identical after **each** section move (not just at the end — D6 says section by section). `bootstrap`, `devkit`, `branch-tester`, `transcript-tester` compile without edit. **STOP, don't retry, at the first section move that breaks a gate — report which section and which line changed, do not continue to the next section.**
- **Status**: PENDING

### Phase 13: ADR-337 D2, D4 — one validation pipeline, corrected docs
- **Tier**: Medium
- **Budget**: 180
- **Domain focus**: `stdlib` — the seam-facing, unattended-safe half of the ADR.
- **Entry state**: Phase 0 baseline recorded (independent of Phases 1-12).
- **Deliverable**: (a) `resolveWithSelection` (`command-validator.ts:385-660`) is removed along with its golden test case; the five-step `validate` pipeline is unchanged; if a future caller needs "re-validate with explicit selections," the ADR names the shape (`validate(command, selections?)`) but this phase does not add it speculatively — nothing calls `resolveWithSelection` today. (b) `docs/core-concepts/README.md` and `packages/stdlib/CLAUDE.md` are corrected to state the real per-action file convention (`<name>.ts` + `index.ts` always; `-events.ts` when the action emits its own event types; `-messages.ts`/`-data.ts`/`-types.ts` only when warranted) — no action directory is restructured to match the old text; a sample of five actions named in this plan's progress notes is checked against the corrected text.
- **Exit state**: `resolveWithSelection` does not exist; `grep -rn resolveWithSelection packages tools stories` → 0; the remaining validator golden tests pass unchanged; `pnpm --filter '@sharpee/stdlib' test` at baseline or higher (minus the one removed golden case). Dungeo chain and the three Chord trees byte-identical. **STOP, don't retry, on any gate failure.**
- **Outcome (2026-09-08, session 4a2d5f)**: (a) `resolveWithSelection` removed from `CommandValidator` — the class method (doc through closing brace, 281 lines), the interface member, and its parameter type `EntitySelections` (its only use; never exported by the validation barrel) — by an asserting line-range script; the golden test's `describe('resolveWithSelection (Disambiguation)')` block (218 lines, six cases) removed the same way. `grep -rn resolveWithSelection packages tools stories` → 0. `validate` untouched; no `selections?` parameter added. stdlib `tsc --noEmit` clean; 1654 passing (1660 − 6). (b) `docs/core-concepts/README.md` "Action Structure" and `packages/stdlib/CLAUDE.md` line 5 state the measured convention (57 actions: `<name>.ts` and `index.ts` always; `-events.ts` 46, `-messages.ts` 21, `-data.ts` 12, `-types.ts` 7) and that a two-file action is complete; sample checked against the text: `taking/` (all six), `telling/` (two), `cutting/` (messages only), `attacking/` (data, events, types), `about/` (events only). ADR-337 gains Amendment A1. **Gate**: `./repokit build dungeo` exit 0; `compare-gates.sh` — Dungeo chain, seeded unit suite, and the three trees all IDENTICAL; every suite at baseline or explained.
- **Status**: DONE for D2 and D4 (2026-09-08, session 4a2d5f)
### Phase 14: ADR-337 — decisions for David and D1's landing
- **Tier**: Medium
- **Budget**: 160
- **Domain focus**: `stdlib` — the one phase in the survey expected to change output, and the one whose disposition is entirely David's.
- **Entry state**: Phase 13 done.
- **Deliverable**: Post the exact lists, for David's confirmation before this phase executes anything:
  1. **D1's refusal-order diff.** Implement `CommandExecutor.runPhases` looking up `actionLifecycleDescriptors` and running the four hooks (`runPreValidate`/`runPostValidate`/`runPostExecute`/`runPostReport`/`runOnBlocked`) around each action's phases in a scratch commit; run the Dungeo chain and the three Chord trees; list every line that differs from Phase 0's baseline, with the action and the refusal that now speaks first or last. For each: is it accepted as the new uniform order, or does the action need a declared `earlyRefusal?(context): ValidationResult | undefined` on its descriptor (run before `runPreValidate`, taking the action's `blocked` path with `runOnBlocked` still consulted)? Also decide, in one line: **is `earlyRefusal` named for "the slot resolver could not resolve"** (the umbrella's suggested criterion, matching `going`'s structural case) **or something narrower/broader** — this is a question, not a default (per the ADR's own text and this plan's hard constraints). The five multi-object actions (`taking`, `dropping`, `putting`, `inserting`, `removing`) keep their per-item `runMultiObjectValidate`/`Execute`/`Report` calls untouched — they are the declared remainder, not part of this diff.
  2. **D3's deletion list**: `pushing-original.ts` (398 lines, `actions/standard/pushing/`) and the four `.removed` files under `actions/removed/` (`answering.ts.removed`, `asking.ts.removed`, `telling.ts.removed`, `using.ts.removed` — 663 lines total).
  3. **D7's skip-test resolution, 27-row table**: `entering-golden` (4), `exiting-golden` (5), `opening-golden` (3), `dropping-golden` (2), `inventory-golden` (2), `throwing-golden` (2), `taking-golden` (1), `wearing-golden` (1), `witness-system` (3), `command-validator-golden` (1 — the case Phase 13 already removed), `integration/meta-commands` (3) — each implemented, deleted, or replaced with a one-line named-gap note.
  Once David confirms all three, this phase lands D1 with the confirmed dispositions (including any `earlyRefusal` declarations), deletes the confirmed files, and resolves the skipped tests per the confirmed table.
- **Exit state**: `grep -rl 'resolveLifecycle\|getLifecycleState\|runPreValidate\|runPostValidate\|runPostExecute\|runPostReport\|runOnBlocked' packages/stdlib/src/actions/standard` → 0; a test asserts the hook sequence fires once each for `take lamp` and none for `score`; the Dungeo troll-axe interceptor test still passes; the five multi-object actions still call all three per-item primitives (structural test); the five deleted files are absent; `grep -rn 'test.skip\|it.skip\|describe.skip' packages/stdlib/tests` → 0; `pnpm --filter '@sharpee/stdlib' test` at baseline or higher. Every differing Dungeo-chain/Chord-tree line from the D1 diff has a recorded disposition in this plan; lines not on that list still byte-identical. **This phase does not run past the diff-and-list step until David confirms all three items. STOP and wait — do not infer confirmation, do not land D1 speculatively.**
- **Outcome, item 1 (2026-09-08, session 71ed1a; David: "recommendations are approved" — the two descriptor fields, D5 amended after)**: D1 built and gated. **The refusal-order diff is empty**: with every action's pre-`resolveLifecycle` refusal now behind preValidate and onBlocked consulted for refusals it never saw, the Dungeo chain, the seeded unit suite, and the three Chord trees are byte-identical to baseline (`compare-gates.sh`, two runs: after the first cut and after the conversation-action fix). No line needs `earlyRefusal`; the field is on the type, no descriptor uses it, and the criterion question stays open until a line needs it. As built (ADR-337 Amendment A2): `ActionLifecycleDescriptor` gains required `reportEventType` (string, or a function for putting's `put_in`/`put_on`) and `blockedEventType`, plus optional `earlyRefusal`; the call site is stdlib's `lifecycle/phase-runner.ts` (`runValidatePhase`/`runExecutePhase`/`runReportPhase`/`runBlockedPhase`), which `CommandExecutor.runPhases` calls — one implementation the executor runs on a real turn and 58 test files (31 stdlib, 26 story-loader, 1 character) call on a built context, instead of the copy each test harness would otherwise need; `contracts.runsOwnHooks` names attacking (postExecute mid-execute is the combat resolution; postReport precedes death events) and the four conversation actions (postValidate, postExecute, postReport only when no exchange or thread grips the input — ADR-320 D16; the topic table's occurrence bump lives in postValidate, found by 5 story-loader and 1 character failures on the first cut); `contracts.handlesMultiObject` names taking, dropping, putting, removing (the D4 loop; `blocked` keeps its all-fail branch); inserting has no per-item loop — it runs putting's hooks around its delegated phases itself. 605 plumbing lines gone from 40 actions; the six-name import block gone from 30. `attacking.validate` reads the executor-resolved state via `getLifecycleState` for its "no combat interceptor" refusal. AC-1's grep is a named list of ten files, pinned by `packages/stdlib/tests/unit/actions/lifecycle-call-site.test.ts` (3 cases: every descriptor names both event types; no action calls a hook without a declared contract, and every declared caller still calls one; only the four D4 actions import the primitives and each calls all three). `packages/engine/tests/lifecycle-call-site.test.ts` (3 cases): `take lamp` fires preValidate, postValidate, postExecute, postReport once each in order and the take lands; a preValidate veto reaches onBlocked and nothing moves; `score` consults nothing. `packages/stdlib/CLAUDE.md` states the call site and tells tests to drive phases through the runner. **Gate**: `tsc --noEmit` clean for stdlib and engine; stdlib 1657 passing, 27 skipped (1654 + 3); engine 685 (682 + 3); story-loader 1083, character 641 (both baseline); world-model 1512; `./repokit build dungeo` exit 0; genai reference regenerated with the new fields and no re-added API (#391 workaround: stdlib and character force-rebuilt first). Items 2 (D3 deletions) and 3 (D7 table) remain HELD below.
- **Outcome, items 2–3 (2026-09-08, session 957700; David: "confirm both")**: **D3**: `pushing-original.ts` and `actions/removed/{answering,asking,telling,using}.ts.removed` deleted (`git rm`); nothing imported any of them (`pushing/index.ts` exports only `pushing.ts` and `pushing-events.ts`), so the bundle and the genai reference are unchanged by the deletion. **D7, method**: all 27 tests un-skipped and run once against HEAD before any edit — 5 pass as written, 22 fail — and each row took one of D7's three verbs from what the failure showed. **The 27-row table** (file — test — verb — reason):
  1. `entering-golden` — already inside target — **deleted** — used the removed `TraitType.ENTRY`; the World State test "should NOT move player when already inside target" covers it with `ENTERABLE`.
  2. `entering-golden` — entry is blocked — **deleted** — passed, but only because a container without `ENTERABLE` refuses `not_enterable`, which "target is not enterable" already asserts; "blocked entry" was an EntryTrait concept.
  3. `entering-golden` — maximum occupancy — **deleted** — never built: `too_full` is declared but no `validate` branch emits it.
  4. `entering-golden` — custom prepositions — **deleted** — `EnterableTrait.preposition` is `'in' | 'on'`; "should enter enterable supporter" covers `on`.
  5. `exiting-golden` — no location set — **deleted** — `createRealTestContext` refuses an actor with no location; the `nowhere_to_go` branch is covered by "container has no parent location".
  6. `exiting-golden` — container is closed — **deleted** — it moved the player into an already-closed crate, which `canMoveEntity` refuses; the World State test "should NOT move player when container is closed" does it right (close after entering).
  7. `exiting-golden` — exit is blocked — **deleted** — never built: `cant_exit` is declared but no branch emits it; the `ENTRY` trait it used is gone.
  8. `exiting-golden` — vehicle with ENTRY trait — **deleted** — "should exit from container" is the same case under the current trait.
  9. `exiting-golden` — custom prepositions correctly — **deleted** — never built: exiting derives `out of`/`off` from the trait type; `under`/`behind` are not in the model.
  10–12. `opening-golden` — separate revealed events / proper atomic events / multiple revealed events — **note** — `if.event.revealed` is the opened→revealed chain's (ADR-094, `src/chains/opened-revealed.ts`), covered by `tests/unit/chains/opened-revealed.test.ts`; one comment in the golden file replaces the three.
  13. `dropping-golden` — dropping inside a closed container — **note** — ADR-043 names the "trapped in the trunk" convention; `ContainerBehavior.canAccept` refuses closed containers from any side, so nothing implements it; the comment names the gap.
  14. `dropping-golden` — dropping while not in a room — **deleted** — the "car" had no container trait, so `canMoveEntity` left the player in the room; with one it is "should drop item in open container".
  15. `inventory-golden` — weight data when weight limit — **deleted** — empty body; inventory emits no weight fields (`inventory.ts:77` TODO).
  16. `inventory-golden` — pattern: weight calculation — **deleted** — referenced an undefined variable; same gap.
  17. `throwing-golden` — miss moving actor — **implemented** — the "implementation bug" in the title was the test's: `agility` must live in `ActorTrait.customProperties`, which `ActorBehavior.getCustomProperty` reads; retitled "should miss an agile actor, who ducks".
  18. `throwing-golden` — NPC catches thrown item — **implemented** — same fix for `canCatch`.
  19. `taking-golden` — too heavy — **implemented** — weight capacity is `capacity.maxWeight` on the actor's `CONTAINER` trait and weight is `IdentityTrait.weight`; the test used a never-built `inventoryLimit` and a weightless boulder.
  20. `wearing-golden` — not held and not in room — **implemented** — the refusal an out-of-scope item gets is `scope.not_known` (the scope check precedes `not_held`); retitled "out of scope (in another room)".
  21–23. `witness-system` — action witness / movement witness / partial witness level — **implemented** — pass as written; un-skipped.
  24. `command-validator-golden` — action without object in parsed command — **deleted** — the parser returns null for bare "take", so the validator never sees it; the bare-verb parse question is GH #388's.
  25–27. `integration/meta-commands` — `author.parser_events` / `author.validation_events` / `author.system_events` — **deleted** — never built; `src/actions/author/` holds only `trace`.
  Orphaned imports removed (`AuthorModel`, `EntityType` in opening; `EntityType` in dropping). **Exit state**: `grep -rn 'test.skip\|it.skip\|describe.skip' packages/stdlib/tests` → 0; the five files absent; stdlib `tsc --noEmit` clean; stdlib **1664 passing, 0 skipped** (1657 + 7 un-skipped; 20 removed from the skipped count). **Gate**: `./repokit build dungeo` exit 0 (bundle 4,350,654 bytes), genai reference unchanged; `compare-gates.sh baseline phase14c` — Dungeo chain, seeded unit suite, and the three Chord trees IDENTICAL; against last session's post-D1 run the only count change is stdlib's. ADR-337 Amendment A3 records D3 and D7 as built.
- **Status**: DONE

### Phase 15: ADR-334 D3, D4 — one platform dispatcher, the dead enrichment funnel removed
- **Tier**: Medium
- **Budget**: 200
- **Domain focus**: `engine` — the two smallest, lowest-risk diffs in ADR-334, landed first per its own D6 order.
- **Entry state**: Phase 0 baseline recorded. (ADR-334 is explicitly "last, and only if reached" per the umbrella — this phase and Phase 16 are the lowest priority in this plan; they may not be reached this session.)
- **Deliverable**: (a) **D3**: the three inline `PlatformEventType` switches (`processMetaPlatformOperation`, `processPlatformOperations`, and whichever the third is at current `HEAD`) and the dead, never-constructed `PlatformOperationHandler` collapse into one module owning all six operations including `AGAIN`, with one completion-event mapping and one error mapping; the meta-command path and the turn path call the same dispatcher with a parameter distinguishing "one operation" from "drain the pending list." (b) **D4**: the two `processEvent` funnels become one stage-level function taking the source (player action or plugin) as a parameter; the unused `TurnEventProcessor` class and its factory are removed from `turn-event-processor.ts` and `index.ts` — **this deletes a class from a file that stays and an export line, not a file** (`processEvent` itself, which two tests import directly, stays), so it runs without a separate confirmation gate per this plan's hard constraints, exactly as ADR-334 D4 orders it and AC-3 pins the result.
- **Exit state**: `grep -rn 'switch (.*PlatformEventType\|case PlatformEventType' packages/engine/src` → one module; the six paired meta/turn assertions (save, restore, quit, restart, undo, again) match; `GameEngine` has no `platformOpHandler` or `turnEventProcessor` field; `TurnEventProcessor`/`createTurnEventProcessor` are not exported from `@sharpee/engine`; the two tests that import `processEvent` directly still pass; `pnpm --filter '@sharpee/engine' test` at baseline or higher. Dungeo chain and the three Chord trees byte-identical. **STOP, don't retry, on any gate failure.**
- **Status**: PENDING

### Phase 16: ADR-334 D1, D1a, D2 — the turn becomes a stage list
- **Tier**: Large
- **Budget**: 400
- **Domain focus**: `engine` — the highest-ceremony change in the survey; the widest blast radius.
- **Entry state**: Phase 15 done (D6's own order: D3/D4 first, then D1 stage by stage).
- **Deliverable**: `packages/engine/src/turn/` holds one module per stage of the ~25-phase `executeTurn` body; a single exported `TURN_STAGES` constant names them in order (**the `requires` idiom applies here too** — the umbrella's instruction to amend ADR-334 D1 — each stage's entry states what it must follow, not just where it sits in the array), plus `META_STAGES` sharing the parse/platform-operations/prose-rendering/channel-packet stages and omitting the turn-only ones. A stage is `{ name, run(context): Promise<StageOutcome> }`, `StageOutcome` is `'continue' | 'stop'`; the parse stage sets `context.route` and the runner selects the list. `turn-stage-order.test.ts` asserts both lists in order, drives one regular turn and one meta command, and asserts every stage of the chosen list ran exactly once and none of the other list's exclusive set ran. `executeTurn` becomes the runner: builds the context, runs the chosen list, returns the result — no phase logic remains inline. Amend ADR-334 with a short note under D1 recording that `TURN_STAGES`/`META_STAGES` carry `requires`. Order: stage by stage from the ends of the 426-line method inward, per D6, with the byte-identical check after **each** stage extraction, not just at the end.
- **Exit state**: `TURN_STAGES` and `META_STAGES` exist with the order test passing; a regular turn (`take lamp`) runs every `TURN_STAGES` stage exactly once; `score` runs every `META_STAGES` stage exactly once and none of the turn-only stages; `executeTurn` contains no phase logic; `./repokit build dungeo` regenerates `genai-api/engine.md` with no removed/renamed public `GameEngine` member; the seventeen `new GameEngine` call sites are untouched in the diff; `pnpm --filter '@sharpee/engine' test` at baseline or higher (708+). Dungeo chain and the three Chord trees byte-identical after **each** stage extraction. **STOP, don't retry, at the first stage extraction that breaks a gate — report which stage and which line changed, do not continue to the next stage.**
- **Status**: PENDING
