# Session Plan: Chord Language Reference (author-facing documentation)

**Created**: 2026-07-14
**Overall scope**: Write an author-facing reference for Chord (ADR-210, v1 locked 2026-07-14, shipped npm 3.0.0) — every construct explained in plain language with a working, compile-checked example, organized by what authors do rather than by grammar production. Source of truth: `docs/reference/chord-language.md`, plus a rendered `site/chord-reference.html` page linked from `site/chord.html`. Every example snippet is backed by a fixture `.story` file that passes (or, for migration/error examples, is asserted to fail with) the compiler's load-time gates via `@sharpee/chord`'s `compile()`.
**Bounded contexts touched**: N/A — documentation-only work (no domain behavior changes; DDD decomposition does not apply per the planner's own rules). Domain-accurate terminology is still drawn directly from the Chord grammar and design doc throughout.
**Key domain language**: Chord, `.story`, Story IR, load-time gates, hatch, phrase — the vocabulary is the subject matter of this deliverable, not incidental to it.

## References consulted
- `docs/architecture/adrs/adr-210-story-language.md` — ACCEPTED; Chord's grammar is a one-way ratchet governed by `docs/architecture/chord-grammar-changes.md`, and `docs/reference/chord-grammar.md` is deliberately the drift-proof, parser-table-sourced grammar source (Consequences: "documentation cannot drift"). This plan's `chord-language.md` must therefore be a narrative companion, not a second grammar source — its own drift protection is the compile-check harness (every example still runs against the real `@sharpee/chord` parser), not code generation. Also confirms: nothing platform depends on `@sharpee/chord` (browser-safe, pure compiler), so a docs-side verification script requiring the built package does not violate the Direction rule.
- `docs/context/project-profile.md` — confirms `pnpm`/TypeScript/Vitest conventions, that `site/` is Astro/plain-HTML publishing surface built separately from `docs/book`, and the "Test assertions — insufficient" mutation-signature entry for the Publishing domain (a chapter claimed "verified" without a re-run of its actual check is the failure mode this plan's compile-check harness exists to prevent).
- `docs/context/session-20260714-1008-main.md` (most recent session) — this morning's session built `site/chord.html` (landing page, already linking out to `chord-grammar.md`/`chord.ebnf`/`cloak.story`/`zoo.story` as GitHub raw links) and noted two open items this plan must account for: (1) the `.current-plan` pointer was stale, pointing at the finished `chord-zoo-surfaces` plan — this plan's write corrects that; (2) `site/` changes from that session are local-only, uncommitted — Phase 7 of this plan touches the same files (`site/chord.html`, `site/components.js`) and must diff-check rather than assume a clean base.
- `docs/reference/chord-grammar.md` (read in full — the coverage checklist) — every production and layout rule enumerated in Phase 1's checklist derivation traces to this file: story header, `create` block lines (aka/states/score/composition/placement/wears/exits/blocked-exits/phrase-override/first-time/on-clause/prose), `define phrase`/`phrases`/`verb`/`text`/`condition`/`trait`/`action`/`hatch`/`sequence`, the full statement set, condition grammar, and the "Removed from the top level" + "Analyzer readings" sections that Phase 5's migration/diagnostics chapter must cover accurately (top-level `when`/`once`/`every`, `define flag`, `if`/`else`/`end if`, top-level `define score` all removed with fix-it diagnostics, not just silently gone).
- `docs/work/story-language/design.md` (structure read — section headers, §§1–8) — normative source with worked examples (§3: Cloak of Darkness complete, stdlib traits, Friendly Zoo NPC/timeline/custom-actions) and rationale (§1 the seven givens, §2.5 "no counting", §2.6 phrases, §5 compile-to mapping and verification strategy). Phases 2–5 must consult specific sections in full when drafting the corresponding chapter, not rely on this planning pass's header-level skim.
- `packages/devkit/src/commands/compose.ts` (read in full) — confirms the public verification surface this plan's harness reuses: `require('@sharpee/chord').compile(source)` returns `{ ast, ir, diagnostics, ok }`; diagnostics carry `.story` line/column/severity/code. `sharpee compose --check` is the CLI-level equivalent authors will actually run; the doc's Tooling chapter (Phase 5) should present `--check` as the primary author workflow and the plan's own harness as the doc-maintenance-time equivalent of the same call.
- `site/chord.html` (read in full) — the existing landing page's conventions this plan's `site/chord-reference.html` must match: dark-default `data-theme`, `<div class="page"><main class="content">` shell, `<pre><code>` for snippets (real excerpts, not invented syntax), `<aside id="sidebar">`/`<footer id="footer">` filled by `components.js`, and its existing "Reference" section (`chord-grammar.md`, `chord.ebnf`, `cloak.story`, `zoo.story` links) which Phase 7 extends with a link to the new page rather than duplicating.
- `site/components.js` (read in full) — the shared sidebar builder; "The Chord Language" link already exists (`chord.html`) under Getting Started. Phase 7 adds the new reference page's link here (single source for the site nav — do not hand-duplicate the sidebar markup elsewhere).
- `packages/chord/src/index.ts` (read in full) — the compiler's public interface: `parse(source)` (AST + diagnostics) and `compile(source)` (AST + IR + diagnostics, `ok` gates IR use). This is the exact call the verification harness (Phases 1 and 6) will make from a plain Node script against the built `dist/index.js` — no platform package is modified to build this deliverable.
- `packages/chord/tests/fixtures/` (directory enumerated, not individually read) — confirms the existing precedent this plan's own fixtures extend: `cloak.story`, `traits-basic.story`, `each-*.story`, `zoo-*.story` as positive examples; `gates/*.story` and `malformed/*.story` as the precedent for intentionally-failing fixtures (used by Phase 5's migration/diagnostics chapter, which needs "this used to work, now it's a load error" examples).

## Phases

### Phase 1: Outline, coverage checklist, and harness scaffold
- **Tier**: Small
- **Budget**: 100 tool calls
- **Domain focus**: Chord's full construct surface, reorganized from grammar-production order (`chord-grammar.md`'s order) into author-task order.
- **Entry state**: Source materials available and current (`chord-grammar.md`, `chord.ebnf`, `design.md`, ADR-210 all synced 2026-07-14 to the locked v1 parser); `@sharpee/chord` built (`dist/index.js` present, same build `compose.ts` uses).
- **Deliverable**:
  1. `docs/work/chord-language-reference/coverage-checklist.md` — every production/line-form in `chord-grammar.md` (story header fields; all `create-line` forms; all `define-*` forms; the full `statement` set; the `condition`/`predicate`/`value-expr` grammar; the removed-construct list) mapped to exactly one planned chapter/section in the outline below. No construct may be left unmapped — this file is the acceptance check for "every construct explained" at the end of Phase 6.
  2. `docs/reference/chord-language.md` skeleton — headers only, one-line placeholder per section, organized by author task, not grammar layer:
     - **Building your world**: story header; `create` blocks and composition (kind nouns vs. traits, `with` settings, conditional traits); prose, markers, and `{br}`; exits and blocked exits; states and score ownership lines; the `first time` room description (Z1); per-entity phrase overrides (CP3/Z3b).
     - **Giving things behavior**: `on`/`after` clauses (verb binding, role binding, `every turn`, `while`, `once`, `before`/`after` ordering); conditions (is-a/is-in/is-here/has/holds/wears/can see/reach, `any`/`no` quantifiers, `one chance in N`, boolean combinators); `must` requirements and `refuse`; the statement set (`phrase`/`emit`/`set`/`change`/`move`/`remove`/`award`/`win`/`lose`, the statement-final `when` suffix).
     - **Branching, iteration, and progression**: `select on`/`select` with strategy adverbs; ordinal blocks (`first time` … `tenth time`); `each` blocks (E1–E3, `the match` binder); score lines across owners; win/lose endings; `define sequence` and step anchors.
     - **Defining vocabulary and text**: `define condition`; `define phrase`/`define phrases <locale>` (strategies, `while` gate, verbatim); `define verb … means`; `define text … from` (text hatch) and `define action|behavior … from` (later hatch kinds); `define trait`; `define action`.
     - **Tooling**: `sharpee compose` (`--check`, `-o`), reading diagnostics, migrating from removed constructs (top-level `when`/`once`/`every`, `define flag`, `if`/`else`, top-level `define score`).
  3. `docs/work/chord-language-reference/fixtures/` directory created with one smoke-test fixture (e.g. a two-line valid story) and `docs/work/chord-language-reference/verify-examples.mjs` — a Node script that `require()`s the built `@sharpee/chord` (same resolution `compose.ts` uses), calls `compile()` on every `*.story` file in `fixtures/`, and reports `ok`/diagnostics per file. Script also accepts a manifest format (JSON or frontmatter convention, decided here) for fixtures that are *expected to fail* with a specific diagnostic code (needed by Phase 5's migration examples) — decide and document the convention now so Phases 2–6 use it consistently.
- **Exit state**: Checklist file complete with no unmapped constructs; skeleton committed with every section header in place; harness script runs clean against the one smoke fixture, proving `compile()` is callable from outside `packages/chord` without modifying any platform package.
- **Status**: COMPLETE (2026-07-14) — checklist (~110 rows), skeleton (§1–§6 + appendix), harness verified on both paths: `smoke.story` compiles clean, `migration/removed-flag.story` fails with the manifest-asserted `parse.removed-flag`. Manifest convention documented in `verify-examples.mjs` header. ADR-210 §Consequences amended same day (grammar-doc drift mechanism) per plan-review's STALE ADR finding.

### Phase 2: World-building chapters
- **Tier**: Medium
- **Budget**: 250 tool calls
- **Domain focus**: `create` and the story header — how an author declares the world.
- **Entry state**: Phase 1 skeleton and harness exist; coverage checklist's "Building your world" rows identified.
- **Deliverable**: Full prose + working example for every "Building your world" section listed in Phase 1: story header fields; kind-noun-vs-trait composition and `with` settings (including a conditional-trait example, e.g. `dark while …`); prose blocks (paragraph-break vs. `{br}` hard break, the create-block blank-line-ends-composition rule); markers; exits including at least one blocked exit with a `while` guard; `wears`/placement forms (`in`, `on`, `starts in`); `states:`/`states, reversible:` and `score … worth N` at story/create scope; the `first time` room-description block (Z1); a per-entity `phrase` override with a strategy and an `or`-variant (CP3/Z3b). Each example becomes one fixture under `docs/work/chord-language-reference/fixtures/world/` and passes `verify-examples.mjs`.
- **Exit state**: All "Building your world" sections in `chord-language.md` are complete prose with passing examples; `coverage-checklist.md` rows for this chapter are checked off.
- **Status**: COMPLETE (2026-07-14) — §1.1–1.2 and §2.1–2.10 written; 9 world fixtures + smoke all compile clean (11/11 with the migration fixture); 15 doc code blocks verified verbatim against their cited fixtures (early run of Phase 6's traceability check); 28 checklist rows checked with fixture citations. Iteration note: first harness run caught missing `end on`/`end after` terminators and a leading-punctuation phrase variant (`analysis.fragment-not-bare`) — fixed as ordinary example iteration.

### Phase 3: Behavior chapters I — reactions, conditions, requirements, and effects
- **Tier**: Medium
- **Budget**: 250 tool calls
- **Domain focus**: `on`/`after` clauses, the condition grammar, `must`, `refuse`, and the core statement set — the vocabulary an author uses to make things react.
- **Entry state**: Phase 2 complete (examples in this chapter reference entities/traits/states established there or in new small fixtures).
- **Deliverable**: Full prose + working example for: `on <verb> it` vs `after <verb> it` (with the react-only restriction on `after` — no `refuse`/`must` inside, demonstrated as a documented constraint, not just asserted); role binding (`WORD "anything" "as" "the" WORD`); `every turn` (`on` only); `while`, `, once`, and `before`/`after` ordering modifiers; the full predicate set (is-a/is-in/is-here/has/holds/wears/can see/reach) with one example each; `any`/`no` quantifiers over a declared open condition and the closed-condition-selection failure mode; `one chance in N`; boolean `and`/`or`/`not` precedence; `must` requirement forms (`be a/an`, `be in`, `be any <condition>`, `be <value>`, `have`/`hold`/`wear`, `see`/`reach`) and why `must not …` doesn't exist (`refuse when` is the prohibition form — show both side by side); the statement set (`phrase`, `emit`, `set`, `change`, `move`, `remove`, `award`, `win`, `lose`) each with a working example, plus the statement-final `when` suffix on the statements that support it. Fixtures under `docs/work/chord-language-reference/fixtures/behavior/`.
- **Exit state**: All "Giving things behavior" sections complete with passing examples; checklist rows checked off.
- **Status**: COMPLETE (2026-07-14) — §3.1–3.8 written; 8 behavior fixtures (incl. `closed-condition.story` expected-fail asserting `analysis.closed-condition-selection`); 19/19 fixtures match expectation; 28 doc blocks verbatim-verified; 35 checklist rows checked. Analyzer findings folded into the doc as documented constraints: roles are action-defined (`taker`, not arbitrary words); story states are referenced as bare words in conditions (`while dusk`, not `the story is dusk`); open conditions cannot test states on free `it` (`analysis.unknown-value`) — kind/place/possession predicates only.

### Phase 4: Behavior chapters II — branching, iteration, and progression
- **Tier**: Medium
- **Budget**: 250 tool calls
- **Domain focus**: `select`, ordinals, `each` blocks, scoring, endings, and `define sequence` — the vocabulary an author uses to shape a story over time.
- **Entry state**: Phase 3 complete (this chapter's examples build on the statement set and condition grammar already documented).
- **Deliverable**: Full prose + working example for: `select on <value>` (arm-per-state dispatch, using the sawdust-message pattern from `site/chord.html`'s own example as a model, re-derived from a fixture — do not merely copy the site excerpt without a fixture backing it); `select <STRATEGY>` with each of `randomly`/`cycling`/`stopping`/`sticky`/`first-time` demonstrated and the retirement of `ordered`/`once` as adverbs (cross-reference to Phase 5's migration chapter, don't re-explain there); ordinal blocks (`first time` … `tenth time`) inside a statement body, distinguished from the Phase 2 `first time` room-description block (same keywords, different position — call this out explicitly, it's a real ambiguity risk for authors); `each <condition-name> … end each` (E3) with `the match` binder, at least one nested `each`, and the `must be any <name>` membership form; `score`/`score … worth N` demonstrated at each legal owner (story, create, trait, action) with one worked "why owner-scoped" explanation; `win`/`lose` endings; `define sequence` with all three step-anchor forms (`at turn N`, `N turns later`, `when <name> becomes <state>`). Fixtures under `docs/work/chord-language-reference/fixtures/flow/`.
- **Exit state**: All "Branching, iteration, and progression" sections complete with passing examples; checklist rows checked off.
- **Status**: COMPLETE (2026-07-14) — §4.1–4.7 written; 7 flow fixtures (`select-on`, `select-strategy`, `ordinals`, `each`, `scoring`, `endings`, `sequence`) all compile clean; harness now 26/26 matching expectation; 43 doc `story` blocks verbatim-verified against their cited fixtures; 13 §4 checklist rows checked. All four score owners (story/create/trait/action) demonstrated in one fixture. Runtime semantics for the five strategy adverbs, ordinal occurrence-matching, and `each`/`the match` re-confirmed against `story-loader/src/runtime.ts` before drafting. Iteration note: harness caught two bare params (`{plant}`/`{pot}`) needing the formatter-chain article form (`{the plant}`/`{the pot}`) — fixed as ordinary example iteration.

### Phase 5: Definitions, tooling, and migration chapters
- **Tier**: Medium
- **Budget**: 250 tool calls
- **Domain focus**: the `define` family (vocabulary an author adds to the language itself), `sharpee compose`, and how to read a diagnostic.
- **Entry state**: Phases 2–4 complete (this chapter's examples may reference traits/actions defined earlier, and its migration examples reference statement forms already explained).
- **Deliverable**: Full prose + working example for: `define condition`; `define phrase` (with `while` gate CP1', each strategy, `verbatim` mode showing preserved line structure); `define phrases <locale>`; `define verb … means <pattern>` including a slotted pattern `(something)`; `define text X from "…"` (text hatch, name `br` reserved) and `define action|behavior X from "…"` (later hatch kinds) with a minimal accompanying TS stub shown but not fixture-compiled as a hatch load (hatch *binding* is out of scope for a pure-`compile()` harness — call this out explicitly as a documented limitation, not silently skipped); `define trait` (data fields incl. `optional`/`one of`/`starts`, `states`, `score`, `phrases`, `on`-clauses); `define action` (grammar patterns with cardinality `→`, scope constraints `the WORD must be WORD`, `must`-line, `score`, `refuse without`/`refuse when`/`otherwise refuse`, `phrases`, body statements). A **Tooling** section on `sharpee compose <file> [--check] [-o <ir.json>]` (exit codes, stdout/stderr split). A **Diagnostics and migration** section: a table of the common load-time gate categories (`parse.*` removed-construct fix-its for top-level `when`/`once`/`every`, `define flag`, `if`/`else`, top-level `define score`; representative `analysis.*` gates — closed-condition-selection, unknown-value, negated-requirement, irreversible-state) each with a before/after pair — the removed form and its owner-attached replacement — backed by *expected-to-fail* fixtures using Phase 1's manifest convention (asserting the specific diagnostic code fires, not that the fixture passes). Fixtures under `docs/work/chord-language-reference/fixtures/define/` and `fixtures/migration/`.
- **Exit state**: All remaining chapters in `chord-language.md` complete; `coverage-checklist.md` fully checked off — no unmapped construct remains.
- **Status**: COMPLETE (2026-07-14) — §5.1–5.8 and §6.1–6.3 + Appendix written; 7 new `define/` fixtures compile clean + 8 new `migration/` expected-fail fixtures assert their exact `parse.*` codes (manifest updated); harness now **40/40** matching expectation; **56/56** doc `story` blocks verbatim-verified; `coverage-checklist.md` **fully checked off** (every construct mapped and backed). Doc status banner flipped SKELETON → DRAFT. Migration table drawn from `chord-grammar-changes.md`; every removed-construct code + fix-it message captured live from the compiler before drafting (`parse.removed-{when,once,every,flag,flag-field,if,score}`, `parse.phrase-strategy-retired`). Hatch binding documented as an explicit harness limitation (compile validates the `define … from` line + marker, not the TS module load). Iteration notes: `verbatim` cannot splice at a description marker (`analysis.verbatim-marker`) — shown via a `phrase` statement instead; gated description fragments written bare to avoid the `analysis.fragment-terminator` warning.

### Phase 6: Full compile-check verification sweep
- **Tier**: Medium
- **Budget**: 250 tool calls
- **Domain focus**: proving the deliverable's core claim — every example in `chord-language.md` actually compiles (or, for migration examples, actually fails with the documented code) against the real v1 parser.
- **Entry state**: Phases 1–5 complete; `chord-language.md` has no placeholder sections remaining; every code block has a corresponding fixture under `docs/work/chord-language-reference/fixtures/`.
- **Deliverable**: 
  1. Extend `verify-examples.mjs` (or add a companion script) to do the traceability check both directions: every fixture is referenced by at least one doc example (no orphan fixtures), and every fenced `.story`-flavored code block in `chord-language.md` maps to a named fixture (no unverified snippet). A simple convention (e.g. an HTML comment `<!-- fixture: world/create-basic.story -->` immediately preceding each code block) is sufficient — decide the exact marker in this phase if Phase 1 left it open.
  2. Run the full sweep and report N/N fixtures passing (or failing-as-expected for migration fixtures) — a summary table appended to `docs/work/chord-language-reference/coverage-checklist.md` or a new `docs/work/chord-language-reference/verification-report.md`.
  3. Per CLAUDE.md's build/test discipline: this is example-content verification against an already-built platform package, not a platform build — ordinary example fixes (typos, missing `end` lines) are expected iteration within this phase. If the compiler flags what looks like a genuine platform defect (not an example mistake), stop, report it, and wait rather than working around it in the doc.
- **Exit state**: 100% of examples verified (pass or expected-fail as documented); the verification report is committed alongside the doc so the claim is checkable by anyone, not just trusted.
- **Status**: CURRENT

### Phase 7: Site render and linking
- **Tier**: Small
- **Budget**: 100 tool calls
- **Domain focus**: publishing the reference to `site/`, matching the existing hand-written HTML conventions.
- **Entry state**: Phase 6 complete — `docs/reference/chord-language.md` is content-final and fully verified. Before editing `site/chord.html` or `site/components.js`, re-check their current on-disk state against this morning's session (both are noted there as possibly still uncommitted/local-only) rather than assuming a clean baseline.
- **Deliverable**: `site/chord-reference.html` — a rendered/transcribed version of `chord-language.md` following `site/chord.html`'s exact conventions (dark-default `data-theme`, `.page`/`.content` shell, `<pre><code>` snippets pulled verbatim from the same fixtures Phase 6 verified — not retyped), with the site's existing per-page structure (title, meta description, `<aside id="sidebar">`, `<footer id="footer">`, `components.js`/`theme.js` script tags). `site/chord.html`'s existing "Reference" list gains a link to `chord-reference.html` (placed first, before the GitHub links, since it's the in-site page). `site/components.js`'s sidebar gains a link to the new page (either as a new nav entry or as a sub-item under the existing "The Chord Language" link — decide based on how the sidebar already nests, e.g. the Family Zoo step-tabs pattern, or keep it flat if nesting isn't precedented for non-tutorial pages).
- **Exit state**: `site/chord-reference.html` live, linked from both `site/chord.html` and the shared sidebar; an HTML tag-balance check (the same lightweight Python `html.parser` check used in the 2026-07-14 morning session) passes on all touched site files; `docs/reference/chord-language.md` and `site/chord-reference.html` agree in content coverage (spot-checked, not necessarily byte-identical prose).
- **Status**: PENDING

## Notes for the implementer

- No package under `packages/` is modified by any phase — `docs/`, `site/`, and the doc-scoped fixtures/scripts under `docs/work/chord-language-reference/` are the only write surfaces, so CLAUDE.md's platform-change discussion gate does not apply to this plan. If a phase discovers it needs a fixture inside `packages/chord/tests/fixtures/` (e.g. to get a construct combination the analyzer only accepts in a larger story context), stop and discuss — that would cross into a platform-package change.
- Never auto-retry a failed build (CLAUDE.md) — this plan's Phase 6 draws the line explicitly between ordinary example-content iteration (fine) and a suspected platform defect surfaced by the compiler (stop and report).
- `docs/architecture/chord-grammar-changes.md` (governance log, not read in full during this planning pass — read it when drafting Phase 5's migration chapter) is the authoritative history of every removed/added construct with rationale; use it, not memory, when writing the before/after migration examples.
- Keep `docs/reference/chord-grammar.md` and `docs/reference/chord.ebnf` untouched — this plan adds a companion narrative document, it does not edit the grammar reference.
