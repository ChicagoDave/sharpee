# Plan: ADR-295 Computed Exits — implementation

Source: `docs/architecture/adrs/adr-295-computed-exits.md` (ACCEPTED 2026-08-01). Fixes GH #207; GH #208 is a separate dependency for rendered ordering, NOT in this plan's scope.

Ruling (David, 2026-08-01): Phase 3 implements the puzzle for **Sharpee, not MDL** — only the puzzle logic must match. No spin-flag unification; gates stay `RoundRoomTrait.isFixed` (Round Room) and `dungeo.carousel.active` (Low Room).

## Phase 1 — world-model seam

- `src/traits/room/computedExitContract.ts`: `IComputedExitDeclaration { candidates: string[] }`; carrier contract (`computedExits?: Partial<Record<DirectionType, IComputedExitDeclaration>>` | `computedExitsAll?: IComputedExitDeclaration`).
- `src/capabilities/exit-resolver-binding.ts`: `ExitResolver`, `ExitResolution` types + binding map, mirroring `interceptor-binding.ts` (per-world, idempotent last-wins, never serialized).
- `WorldModel.registerExitResolver(traitType, resolver)` + lookup, beside the other two registries.
- `RoomBehavior.getComputedExitDeclaration(room, direction)` (pure; duck-typed trait scan) and `RoomBehavior.resolveExit(room, direction, ctx)` (called-once contract; candidate warn-and-honor; unregistered-resolver warn + static fallback per D3).
- Root barrels (leaf, traits, root) + rebuild dist/dist-esm.
- Unit tests with a synthetic test trait (never dungeo fixtures): existence via declaration alone; resolveExit dispatch/fallback/off-candidate warn; no registry consultation from the pure helper.

**AC**: world-model tests green; ADR Acceptance 6-7 behaviors covered at unit level.

## Phase 2 — going integration (stdlib)

- `validate`: existence per D3 (static exit OR declaration).
- `execute`: single `resolveExit` call, result in sharedData; move once on `kind:'exit'`; no move on `'blocked'`; static path on `undefined`.
- `report`: resolver events forwarded verbatim ahead of the arrival description; `'blocked'` mirrors the `too_dark` shape (`if.event.went` + resolver messageId, no description).
- D7: destination interceptor slot resolves to no entity for computed directions (source + door unchanged).
- `going-data.ts`: delete `findSourceRoomAndExit` reverse scan; builders read `sharedData.previousLocation`; mapHint from the actual source room's static exit when present.
- Action tests assert world state (ADR Acceptance 2, 5, 8): validate draws nothing; blocked leaves location unchanged; `fromRoom` correct under redirect.

**AC**: stdlib suite green; no behavior change for static-only rooms (existing going tests untouched and green).

## Phase 3 — dungeo swap

- `CarouselExitTrait` (`dungeo.trait.carousel_exit`): data = candidates, point name, narration messageIds, gate descriptor (Sharpee-native: Round Room `isFixed`, Low Room `dungeo.carousel.active`). Two instances; one registered resolver.
- Delete both exit daemons, both tracking daemons, the entry daemon + both prevLocation state keys; Low Room entry "compass spinning" message becomes an `if.event.actor_entered` handler.
- Remove the #207 TODO in `carousel.transcript`; re-record it and Round-Room/Low-Room-crossing walkthroughs at their pinned seeds (draw-order change expected to move goldens; ADR Acceptance 4 budgets this).
- Verify ADR Acceptance 1 (the #207 repro, single arrival) — noting #208 still governs rendered order of bearings vs description.

**AC**: full unit suite + walkthrough chain green at pinned seeds; #207 closable.

## Build/test per phase

`./repokit build dungeo` (with `--skip` to resume), then bundle-based transcript runs (`node dist/cli/sharpee.js --test ...`). No auto-retry on failures — report and wait.
