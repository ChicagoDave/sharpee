# Win 2 / 5 — a grammatical list, for free

The dynamic case: a runtime set of objects becomes a correctly punctuated list —
serial comma, "and", per-item article, plurals — and stays correct when the set changes.

```text
Create a clean, minimal editorial-infographic image, single idea. Flat vector style,
light background (#F7F8FA), accent indigo (#4B47B8), neutral grays. Whitespace-heavy.
Sans-serif prose, monospace for the template line. Aspect ratio 16:9. Badge top-right:
"Win 2 / 5 · Lists".

Top, centered: ONE monospace template card, {slot:here} highlighted indigo:
   You see {slot:here}.

A thin arrow DOWN to one finished sentence on a clean card, larger font, with the
connectives ("," and "and") and the articles ("a", "an") highlighted in indigo:
   You see a brass key, an apple, and three gold coins.

To the right, a small faded "remove one item" note with a second output showing the
list re-forming correctly with no dangling comma:
   You see a brass key and three gold coins.

Title across the top: "A list that punctuates itself."
One gray caption bottom-center: "Serial comma, 'and', a/an, and plurals are applied at
render time. Add or drop an item and the grammar stays right — no dangling commas."

Keep it to: template card → one full list → one shorter list. Nothing else.
```

Notes: the value is the open-ended set the author can't enumerate. Drop-an-item is the
clincher — hand-written lists break here constantly.
