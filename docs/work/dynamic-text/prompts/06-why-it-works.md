# Capstone — why all of it stays correct

Ties the five wins together: every one is correct because a single Assembler owns every
cross-cutting grammar concern, working from a typed tree instead of a glued string.

```text
Create a clean, confident editorial-infographic image with one hero element. Flat
vector style, light background (#F7F8FA), accent indigo (#4B47B8), neutral grays. Lots
of whitespace. Sans-serif for prose. Aspect ratio 16:9. Badge top-right:
"Why it works".

Left column: a compact stack of the five wins as tiny faded result chips (gray, small):
   "an umbrella"
   "a brass key, an apple, and three gold coins"
   "the knight draws her sword"
   "forty-two coins · the third floor"
   "variation that resumes after a save"

A thin arrow from the stack points right into a single glowing indigo prism / lens,
bold label inside "ONE ASSEMBLER". Around it, five evenly spaced, uncrowded tags:
"article", "agreement", "punctuation", "reference", "case".

Title across the top: "Five wins, one authority."
One gray caption bottom-center, two short lines:
"A message becomes a typed tree of atoms, not a glued-together string."
"One Assembler resolves all grammar — so the author never has to."

Footer tag, bottom-right, light gray: "Sharpee phrase algebra · ADR-192".

Keep it sparse: the faded win stack, the prism, its five tags. No pipeline plumbing.
```

Notes: this is the one "architecture" image, but framed by the payoff (the five wins on
the left), so it reads as "why these are possible," not as plumbing.
