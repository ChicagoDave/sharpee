# Session Plan: ADR-247 implementation — `getContents()` worn-items default flip

**Created**: 2026-07-21 (session 99aee6 continuation)
**Authored by**: post-248-open-items plan Phase 4 (this document is that phase's deliverable). Not a re-plan of that sweep.
**GATE — DO NOT START ANY PHASE BELOW WITHOUT DAVID'S EXPLICIT GO-AHEAD.** This is a platform change under `packages/world-model` (consumed by `packages/stdlib`, `packages/engine`, `packages/event-processor`, `packages/extensions/*`, and every story). Per `CLAUDE.md`: "Platform changes require discussion first. Any changes to `packages/`... must be discussed with the user before implementation." Authoring this plan is not that discussion — it is the artifact the discussion will use. Phase 6 additionally carries its own explicit file-deletion confirm gate (`ClothingTrait` + `ContentsOptions.includeWorn`), independent of and in addition to the platform go-ahead.
**`.current-plan` pointer**: intentionally **not** repointed here. `docs/context/.current-plan` continues to resolve to `docs/work/post-248-open-items/plan.md` until David gives the go-ahead to begin ADR-247 implementation; repointing it is his decision to make at that time, not an automatic side effect of authoring.
**Overall scope**: Implement ADR-247 in full — commit a per-site call audit, flip `WorldModel.getContents()`/`getAllContents()` to return worn items unconditionally, add `getCarriedAndWorn(holderId)`, migrate every consumer per its audited disposition, delete `ContentsOptions.includeWorn` and `ClothingTrait` outright, and land a green full regression (platform suites + dungeo walkthrough chain + fernhill transcripts).
**Bounded contexts touched**: N/A — infrastructure/platform-API framing per this project's own convention (no `docs/ddd/notation.yaml`; CLAUDE.md's Logic Location table is the operative decomposition). Layers touched: **world-model** (the `getContents`/`getAllContents`/`getCarriedAndWorn` API and `ClothingTrait`), **stdlib** (standard actions, `inventory.ts` primarily), **engine** (turn-cycle/prose-pipeline consumers), **event-processor** (effect/handler consumers), **extensions** (`basic-combat`, `testing`), **story** (dungeo primarily, plus cloak-of-darkness, friendly-zoo, armoured, channel-service-test).
**Key domain language**: `getContents()` (direct-children query, world-model), `getAllContents()` (recursive variant), `getCarriedAndWorn()` (new partition method: `{ carried, worn }`), `ContentsOptions.includeWorn` (deleted), `ClothingTrait` (deleted — `WearableTrait` becomes the sole worn-state source), "held-only" / "everything" / "doesn't-matter" (the three per-site audit dispositions ADR-247 mandates).

## References consulted
- `CLAUDE.md` (project instructions) — "Never delete files without confirmation. Not even 'to get a build working'" gates Phase 6's `ClothingTrait` file deletion; "Platform changes require discussion first... must be discussed with the user before implementation" gates every phase below on David's go-ahead (recorded in this header, not assumed from the post-248 sweep's blanket "proceed"); "Never auto-retry failed builds or tests... report the error and WAIT" governs every verification step; the Logic Location table confirms `getContents`/`getCarriedAndWorn` belong in world-model and consumers (stdlib/engine/event-processor/story) adapt to it, not the reverse.
- `docs/architecture/adrs/adr-247-getcontents-worn-default.md` — ACCEPTED, all three Open Questions resolved 2026-07-20. Decision: flip the default unconditionally (no opt-out surface), delete `ContentsOptions.includeWorn`, add `getCarriedAndWorn(holderId): { carried, worn }`, delete `ClothingTrait` in the same change. Acceptance Criteria (audit table committed before the flip lands; both deletions verified gone from source/barrels/rehydration/GDT; three named new tests; all suites green) is the exact shape this plan's phases target. This plan is the authored artifact ADR-247's own Session note anticipates ("the broad flip... is gated on this ADR reaching ACCEPTED" — ACCEPTED is satisfied; implementation is gated separately per the header above).
- `docs/architecture/adrs/adr-189-entity-type-default-traits-registry.md` — cited by ADR-247's Parent line as the sibling precedent ("a type keeps the behavior its name promises" — a query default should behave the way its name implies: `getContents` returning less than all contents is the same class of surprise ADR-189 closed for trait defaults). No contradiction with this plan; confirms the flip's direction is consistent with prior platform precedent.
- `docs/work/post-248-open-items/plan.md` (Phase 4, and its own References list) — the parent task: recorded that ADR-247 is "correctly sized as session-or-larger on its own," specified the minimum phase sequence (audit → API change → consumer migration → `ClothingTrait`/`includeWorn` deletion + regression) this plan's phase shape follows, and confirmed the `.current-plan` pointer must stay on the sweep plan until David's separate go-ahead.
- `docs/context/project-profile.md` (2026-07-16) — confirms Vitest 3.x per-package `test` scripts (`vitest` / `vitest run`, used below for suite verification), the dual-build convention (`tsc --build` + `tsc -p tsconfig.esm.json`, relevant if `if-domain`'s `ContentsOptions` type edit needs a rebuild across dependents), TypeScript strict mode, and the 4-file action convention in `stdlib` (relevant to Phase 3's consumer-migration file shape).
- `packages/world-model/CLAUDE.md` — "Items are portable by default" (the principle ADR-247's flip extends to `getContents`, per ADR-247's own Context section); Root Barrel Discipline (adding a trait touches the leaf barrel, `traits/index.ts`, and `src/index.ts` — Phase 6's `ClothingTrait` deletion is this procedure in reverse, at the same three sites: `src/traits/clothing/index.ts`, `src/traits/index.ts:25`, `src/index.ts:56`).
- Memory: one-good-run-rule (royal-puzzle addendum, ruled 2026-07-21) — governs Phase 5's and Phase 6's dungeo walkthrough-chain acceptance bar: **one clean `--chain` run** of `stories/dungeo/walkthroughs/wt-*.transcript` satisfies the gate; thief/combat/carousel/royal-puzzle-basic RNG variance is not a regression to chase after this change. Also governs the non-chain unit-transcript suite's acceptance bar: deterministically green **except** the troll-family combat transcripts (`troll-combat`, `troll-blocking`, `troll-recovery`, `troll-visibility`, `troll-interactions`, `debug-combat`), which follow the same one-good-run treatment as the chain (a `dungeo.melee.villain_attack` kill → `game.ended` cascade is the accepted flake signature; anything else failing in those six files is a regression).

## Grounding — real call-site count vs. the ADR's stated 64

ADR-247's Context section cites "a census (2026-07-20, non-test source) counts **64 `getContents(` call sites**." A fresh count against the current `chord-foundations` tree (`grep -rn "getContents(" packages/ stories/ --include="*.ts" --include="*.tsx"`, excluding `dist/`, `dist-esm/`, `node_modules/`, `*.test.ts`/`*.spec.ts`, and `tests/`/`__tests__/` directories) finds:

**176 non-test call sites across 102 files** — nearly 3x the ADR's figure. **This drift is recorded here, not silently absorbed**: the ADR's 64 was almost certainly scoped to `packages/` only and/or a narrower grep pattern; even a `packages/`-only recount comes to **89 sites / 51 files** (see breakdown below), still 39% over 64. This 176-count re-grounding prompted David's 2026-07-21 amendment to ADR-247 itself: the full-census clause is SUPERSEDED — only actor-scoped sites are audited (Phase 1, amended); the non-actor majority cannot change behavior under the flip (worn state exists only on actors) and is covered by the regression surface.

By package (unique call sites / unique files):

| Package/story | Sites | Files |
|---|---|---|
| `stories/dungeo` | 75 | 45 |
| `packages/stdlib` | 34 | 25 |
| `packages/world-model` | 31 | 11 |
| `packages/extensions` (`basic-combat`, `testing`) | 9 | 6 |
| `stories/cloak-of-darkness` | 7 | 3 |
| `packages/transcript-tester` | 5 | 2 |
| `packages/event-processor` | 4 | 2 |
| `packages/engine` | 3 | 2 |
| `stories/friendly-zoo` | 2 | 1 |
| `stories/armoured` | 2 | 1 |
| `stories/channel-service-test` | 1 | 1 |
| `packages/story-loader` | 1 | 1 |
| `packages/queries` | 1 | 1 |
| `packages/devkit` | 1 | 1 |
| **Total** | **176** | **102** |

`stories/fernhill` (Chord-authored) has **zero** direct `getContents(` call sites — it only exercises the API transitively through engine/stdlib. It is still a regression target in Phase 6 because those transitive paths change behavior.

`ContentsOptions` is defined once, in `packages/if-domain/src/contracts.ts:47-51`, and re-exported through `packages/world-model/src/world/index.ts`; `AuthorModel.getContents`/`getAllContents` (`packages/world-model/src/world/AuthorModel.ts:188-200`) are thin pass-throughs to `WorldModel` — both are touched by the Phase 3 API change but were not separately counted in the 176 (they don't call `getContents(` themselves in a way the grep would double-count as a "site needing disposition"; they're plumbing, not callers).

`ClothingTrait` today: imported and registered in `packages/world-model/src/traits/implementations.ts` (rehydration table) and `packages/world-model/src/traits/all-traits.ts` (registry + `isClothingTrait` type guard), checked directly in `WorldModel.getContents()` (`WorldModel.ts:983`), exported from two barrels (`src/traits/index.ts:25`, `src/index.ts:56`, both via `./clothing/index.js`), and referenced by name (string literal `'clothing'`, not the trait class) in `stories/dungeo/src/actions/gdt/commands/do.ts:144`. Three world-model test files also reference it (`tests/unit/traits/clothing.test.ts`, `tests/integration/wearable-clothing.test.ts`, `tests/integration/trait-combinations.test.ts`, plus a fixture in `tests/fixtures/test-entities.ts`) and one devkit fixture (`packages/devkit/fixtures/basic-story/src/world-setup.ts`) — all of which Phase 6 must also resolve (delete/update, not silently leave red).

## Phases

### Phase 1: Targeted audit — actor-scoped `getContents` sites (AMENDED SCOPE, 2026-07-21)
- **Tier**: Small
- **Budget**: 100
- **Domain focus**: The ADR-247 amendment (David, 2026-07-21) superseded the full 176-site census with a targeted audit: worn state exists only on actors, so the flip is a no-op by construction for room/container/supporter holders. Audit ONLY the actor/player-scoped `getContents`/`getAllContents` calls (grep-driven: calls whose holder is the player, an actor entity, or actor-typed variables — expect a dozen or two, dominated by stdlib inventory/burden/visibility paths and dungeo NPC/thief code).
- **Deliverable**: `docs/work/adr-247-getcontents-worn-default/audit.md` — the committed actor-scoped table (site → everything / held-only → `.carried` / doesn't-matter), plus one line recording the amendment and the non-actor coverage argument (regression surface, not tabulation).
- **Exit state**: audit.md committed; every actor-scoped site classified; the handful of held-only sites identified for Phase 4/5 migration.
- **Status**: COMPLETE (2026-07-21 — audit.md committed: 60 actor-scoped sites classified (everything 40, fixed-by-flip 10, held-only 9, doc-only 1); the broad grep found 20 beyond the pre-listed 40, incl. the caller-missed giving.ts:204 capacity migration)

### Phase 2: REMOVED (2026-07-21 — ADR-247 amendment)
- The full story-package census phase is deleted by the amended audit scope: non-actor sites cannot change behavior under the flip and are covered by the regression surface (per-package suites, dungeo chain 921-test baseline, fernhill transcripts). Phase numbering below is retained (Phases 3–6 unchanged) to keep the review's cross-references valid.

### Phase 3: World-model API change — `getContents()`/`getAllContents()` flip + `getCarriedAndWorn()` + unit tests
- **Tier**: Medium
- **Budget**: 250
- **Domain focus**: `packages/world-model` only (`WorldModel.ts`, `AuthorModel.ts`, `packages/if-domain/src/contracts.ts` for the `ContentsOptions` type, `VisibilityBehavior.ts` for its now-redundant explicit `includeWorn: true` sites).
- **Entry state**: `docs/work/adr-247-getcontents-worn-default/audit.md` committed (Phase 2 exit). No consumer code touched yet — this phase changes world-model's behavior; downstream held-only call sites will get MORE items than they want until Phase 4/5 migrate them. That is an expected, temporary mid-implementation state internal to this plan, not a shippable checkpoint — do not commit-and-pause here with red consumer suites; Phase 3's own commit should be scoped so `packages/world-model`'s own suite is green even though `stdlib`'s/`stories/dungeo`'s may not be yet (cross-package suites are verified starting Phase 4).
- **Deliverable**:
  1. `WorldModel.getContents()` (`WorldModel.ts:964-991`): remove the `if (!options.includeWorn)` filter block entirely — worn items are no longer excluded, unconditionally. `options.includeWorn` becomes dead (read by nothing); leave the `ContentsOptions.includeWorn` field itself in place for now (its removal is Phase 6's confirm-gated deletion, per the task's explicit phase shape) — but the behavior no longer depends on it.
  2. `WorldModel.getAllContents()` (`WorldModel.ts:1080-1099`): remove the `isRoot` special-casing that forces `includeWorn: true` only for recursive calls — now unnecessary since the base `getContents()` always includes worn items.
  3. Add `getCarriedAndWorn(holderId: string): { carried: IFEntity[]; worn: IFEntity[] }` to `WorldModel` — partitions `getContents(holderId)`'s direct children by `WearableTrait.isWorn` (per ADR-247 Consequences, `ClothingTrait`'s worn-check folds into `WearableTrait`-only ahead of its Phase 6 deletion — do not add a new `ClothingTrait` check here). Empty `worn` array for holders with no wearables.
  4. Add the same method to `AuthorModel` as a pass-through, matching its existing `getContents`/`getAllContents` delegation pattern (`AuthorModel.ts:188-200`).
  5. `VisibilityBehavior.ts:353,405`: leave the explicit `includeWorn: true` in place (harmless-redundant per Phase 1's disposition) or strip it — pick one and note the choice; either is correct, don't spend a review cycle on it.
  6. New tests per ADR-247's Acceptance Criteria, added to `packages/world-model/tests/`: (a) a worn item appears in `getContents()`/`getAllContents()` output where visibility says it should (update/extend `wearable-clothing.test.ts` and `trait-combinations.test.ts`, which currently assert the OLD filtered behavior at the `includeWorn`-keyed lines found in the grounding grep above — e.g. `wearable-clothing.test.ts:272,279,463,464` and `trait-combinations.test.ts:241,244,279` assert `carriedOnly`/`everything`/`wornOnly` splits keyed to the old option; rewrite these to assert the new unconditional-include behavior and the new `getCarriedAndWorn()` partition instead of deleting coverage); (b) `getCarriedAndWorn()` partitions correctly (mixed carried+worn inventory, all-worn, all-carried, empty); (c) the partition survives save/restore (extend `trait-rehydration.test.ts:80-92`, which already tests `includeWorn: true` survives rehydration — add a `getCarriedAndWorn()`-specific assertion post-restore).
  7. `packages/world-model` suite green (`pnpm --filter '@sharpee/world-model' test`), typecheck clean.
- **Exit state**: `getContents()`/`getAllContents()` return worn items unconditionally. `getCarriedAndWorn()` exists on both `WorldModel` and `AuthorModel`. The three ADR-mandated new test categories exist and pass. `packages/world-model`'s own suite and typecheck are green. `ContentsOptions.includeWorn` still exists in the type (unused) — not yet deleted (Phase 6).
- **Status**: COMPLETE (2026-07-21 — getContents/getAllContents filter deleted (worn always included); getCarriedAndWorn added to IWorldModel/WorldModel/AuthorModel/IWorldQuery; ContentsOptions.includeWorn removed; new tests: getcontents-worn.test.ts (flip + visible-contents + partition) and rehydration roundtrip)

### Phase 4: Consumer migration — platform packages, per Phase 1's audit
- **Tier**: Medium
- **Budget**: 250
- **Domain focus**: `packages/stdlib` (34 sites/25 files, `inventory.ts` primary), `packages/engine`, `packages/event-processor`, `packages/extensions/{basic-combat,testing}`, `packages/transcript-tester`, `packages/story-loader`, `packages/queries`, `packages/devkit`.
- **Entry state**: Phase 3 complete — world-model API flipped, `getCarriedAndWorn()` available, `packages/world-model` suite green. `docs/work/adr-247-getcontents-worn-default/audit-platform.md` (or the merged `audit.md`) gives the per-site disposition to execute.
- **Deliverable**: For every platform site marked **held-only** in the audit, migrate to `world.getCarriedAndWorn(id).carried`. For **everything** and **doesn't-matter** sites, no code change (verify the new default already gives correct behavior; drop now-redundant explicit `includeWorn: true` passes where harmless to simplify, e.g. `transcript-tester/src/condition-evaluator.ts:287`). `stdlib/src/actions/standard/inventory/inventory.ts:59-63` specifically: replace its narrow `getContents(player.id, { includeWorn: true })` fix with `getCarriedAndWorn(player.id)`, print both `carried` and `worn` lists per ADR-247's Decision example, remove the now-stale comment documenting the old bug. Run each affected package's own test suite as its migration completes (`pnpm --filter '@sharpee/stdlib' test`, `@sharpee/engine`, `@sharpee/event-processor`, and the extension/tooling packages' `vitest`/`vitest run` scripts).
- **Exit state**: Every platform held-only site uses `getCarriedAndWorn().carried`. `inventory.ts` uses the partition method, not a raw `includeWorn` flag. All platform package suites (`world-model`, `stdlib`, `engine`, `event-processor`, `extensions/basic-combat`, `extensions/testing`, `transcript-tester`, `story-loader`, `queries`, `devkit`) green + typecheck clean.
- **Status**: COMPLETE (2026-07-21 — held-only → .carried: actorBehavior capacity/weight/volume x4 (transitive source fix), giving.ts recipient capacity, multi-object-handler carried-scope x2, taking.ts container count; deleted-option: inventory.ts adopts getCarriedAndWorn, transcript-tester + VisibilityBehavior x2 drop the arg; stdlib 1559, engine 524 green)

### Phase 5: Consumer migration — story packages, per Phase 2's audit (dungeo primary)
- **Tier**: Large
- **Budget**: 400
- **Domain focus**: `stories/dungeo` (75 sites/45 files — combat interceptor, room handlers, liquid/container/tie/inflate actions, GDT commands), plus `stories/cloak-of-darkness`, `stories/friendly-zoo`, `stories/armoured`, `stories/channel-service-test`.
- **Entry state**: Phase 4 complete — all platform consumers migrated and green, so any story-level test failure from this point is attributable to story code, not platform drift. `docs/work/adr-247-getcontents-worn-default/audit-stories.md` (or `audit.md`) gives per-site disposition. Any sites Phase 2 flagged as ambiguous must be resolved (David's ruling obtained) before this phase touches them.
- **Deliverable**: Migrate every story held-only site to `getCarriedAndWorn().carried`, same discipline as Phase 4. Priority order given dungeo's density: `melee-interceptor.ts` (combat targeting — likely wants held-only weapon selection, worn armor probably shouldn't be "drawn"), `fill-action.ts`/`pour-action.ts`/`tie-action.ts`/`inflate-action.ts`/`deflate-action.ts` (liquid/rope/object-manipulation actions — verify worn containers/ropes aren't accidentally now offered as fill/tie targets if that's semantically wrong), `objects-action.ts`/`commanding-action.ts` (room-info/NPC-command listings — likely "everything," worn items should appear), `tiny-room-handler.ts`/`endgame-trigger-handler.ts` (event handlers — check per-site). Run `stories/dungeo`'s non-chain unit-transcript suite (`stories/dungeo/tests/transcripts/*.transcript`) and one clean run of `node dist/cli/sharpee.js --test --chain stories/dungeo/walkthroughs/wt-*.transcript` (one-good-run rule; royal-puzzle-basic RNG variance excluded per memory). The unit-transcript suite is expected deterministically green **except** the six troll-family combat transcripts (`troll-combat`, `troll-blocking`, `troll-recovery`, `troll-visibility`, `troll-interactions`, `debug-combat`), which follow the same one-good-run treatment as the chain — a `dungeo.melee.villain_attack` kill cascade in those six is the accepted flake, not a regression; any other failure there, or any failure outside those six files, is a regression. Build and test cloak-of-darkness, friendly-zoo, armoured, channel-service-test similarly (each has far fewer sites — verify per-site then run each story's own transcript/unit suite).
- **Exit state**: Every story held-only site migrated. `stories/dungeo` unit-transcript suite green (troll-family six excepted per the one-good-run rule), one clean walkthrough-chain run. Other four stories build clean with their own test surfaces green.
- **Status**: COMPLETE (2026-07-21 — no story code changes needed: every story actor-scoped site classified 'everything'/fixed-by-flip (dungeo has no worn items in play); dungeo unit 1782 +9 expected, chain 887/887 clean, fernhill 503 green)

### Phase 6: `ClothingTrait` + `ContentsOptions.includeWorn` deletion (confirm-gated) + full regression
- **Tier**: Medium
- **Budget**: 250
- **Domain focus**: Final cleanup across `packages/world-model` (trait deletion, barrel discipline per `packages/world-model/CLAUDE.md`), `packages/if-domain` (`ContentsOptions` type), `stories/dungeo` (GDT `do.ts` string case), plus platform-wide regression.
- **Entry state**: Phases 3-5 complete — the flip has landed everywhere, every consumer migrated, all package/story suites green except for the vestigial `ClothingTrait`/`includeWorn` surface itself (still present but unused/no-op). **STOP here and present the exact deletion inventory to David for explicit confirm before deleting anything** — per CLAUDE.md's "never delete files without confirmation" and the no-get-it-done-assumptions discipline (ADR-247 having ACCEPTED status is not standing confirmation for this session's specific deletion act; the two are separate).
- **Deliverable**:
  1. **Present the inventory**: `ContentsOptions.includeWorn` field (`packages/if-domain/src/contracts.ts:49`) and every reference to it left after Phases 3-4 (should be none in source by now — confirm via re-grep); `ClothingTrait` file (`packages/world-model/src/traits/clothing/clothingTrait.ts` + its dir), its registrations in `implementations.ts` (rehydration table) and `all-traits.ts` (registry + `isClothingTrait` guard), its two barrel re-exports (`src/traits/index.ts:25`, `src/index.ts:56`), the `WorldModel.getContents()` `ClothingTrait` check (already removed in Phase 3's filter-block deletion — confirm no orphaned import remains), the GDT `do.ts:144` `'clothing'` string case, and the three world-model test files + one devkit fixture that reference `ClothingTrait` (need updating, not left red: `tests/unit/traits/clothing.test.ts` — likely deleted outright since it tests the deleted trait directly; `tests/integration/wearable-clothing.test.ts` and `tests/integration/trait-combinations.test.ts` — already rewritten in Phase 3 to test the new behavior, confirm no residual `ClothingTrait` references; `tests/fixtures/test-entities.ts` and `packages/devkit/fixtures/basic-story/src/world-setup.ts` — update fixtures to use `WearableTrait` only).
  2. Once David confirms: delete `ContentsOptions.includeWorn` from `packages/if-domain/src/contracts.ts`. Delete the `ClothingTrait` file/directory, its two registrations, its two barrel exports, and update the GDT `do.ts` case (remove or repoint to `WearableTrait`-based introspection, per whatever the case's purpose was — read it first, don't guess).
  3. Full regression: `./repokit build dungeo` (platform + story build clean, per CLAUDE.md's build workflow — use `--skip <pkg>` to resume if iterating), every platform package suite (`world-model`, `stdlib`, `engine`, `event-processor`, `extensions/*`, `transcript-tester`, `story-loader`, `queries`, `devkit`), `stories/dungeo` unit suite + one clean `--chain` walkthrough run, and a fernhill smoke pass (`node dist/cli/sharpee.js --test stories/fernhill/tests/transcripts/*.transcript` — fernhill has no direct `getContents` call sites but exercises the changed behavior transitively through engine/stdlib; `containers.transcript` is the most relevant single file if a full pass is too large for this phase's budget).
- **Exit state**: `ContentsOptions.includeWorn` and `ClothingTrait` do not exist anywhere in source, barrels, rehydration tables, test fixtures, or the GDT command (ADR-247's Acceptance Criteria, verbatim). `./repokit build dungeo` clean. All package suites green + typecheck clean. Dungeo walkthrough chain: one clean run. Fernhill: transcript pass green (full or the `containers.transcript` representative subset, noted which). ADR-247 implementation is COMPLETE.
- **Status**: COMPLETE (2026-07-21 — ClothingTrait deleted (dir + 5 world-model touchpoints + GDT bare-string case; zero external refs, authorized ADR Q3 + go-ahead + surfaced inventory); includeWorn already gone in Phase 3; world-model tests migrated ClothingTrait→WearableTrait (fixture + clothing/wearable-clothing/trait-combinations, 3 assertions inverted to worn-included); full regression GREEN: world-model 1400, stdlib 1559, engine 524, story-loader 308, lang-en-us 430, platform-browser 86, dungeo unit+chain, fernhill; ./repokit build dungeo --browser clean)

## Sequencing note
Phases 1 and 2 (audit) must both complete and the combined `audit.md` must be committed before Phase 3 starts — this is ADR-247's own Acceptance Criteria, not a planning preference. Phase 3 (API change) must complete before Phases 4/5 (consumer migration) start, since migration targets the new `getCarriedAndWorn()` method. Phases 4 and 5 are independent of each other in principle (platform vs. story consumers) but are sequenced platform-first here so Phase 5's dungeo verification isn't chasing platform-side regressions at the same time — a Phase 5 that finds platform-audit gaps should return to Phase 4's scope, not patch around it locally. Phase 6 runs last unconditionally: it is both the confirm-gated deletion and the only phase that asserts full-system green, so nothing else can follow it in this plan.
