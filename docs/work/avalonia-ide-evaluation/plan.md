# Session Plan: Avalonia + Velopack ("O6") evaluation — a single-codebase, natively-rendered shell for Chord Writer

**Created**: 2026-09-13
**Plan Status**: ACTIVE
**Pointer handoff (rule 18b)**: complete — `docs/context/.current-plan` names this plan as of 2026-09-14. David's disposition of `docs/work/opensilver-ide-evaluation/plan.md` was **still live**: that plan carries a `**Superseded by**` stamp naming this one, its Phase 5 stays CURRENT and Phase 6 PENDING, and it remains resumable at exactly the phase it reached.
**Overall scope**: An evaluation, not a build. Determine, on evidence, whether Avalonia (XAML + C# on .NET, own Skia renderer, no web view for the chrome) packaged with Velopack can serve as a single codebase reaching Windows, macOS, and Linux for Chord Writer's shell — extending the three-shape comparison already on record (native Swift/WPF, OpenSilver on Photino, a web application written up but not spiked) to a fourth. Output is a filled Avalonia column on the ADR-341 parity table, a decision record with inline evidence extending the OpenSilver decision's per-control table to four columns, and — only on David's explicit ADR-worthy confirmation (rule 11) — a draft ADR. ADR-341 itself is not edited by this plan.
**Bounded contexts touched**: N/A — infrastructure/tooling evaluation, no domain behavior change. Product surface judged is the existing macOS `tools/ide/SharpeeIDE` parity table (rows reused, not invented); no Chord change, no `packages/` change.
**Key domain language**: N/A in the DDD sense — the evaluation's own vocabulary, carried over from the OpenSilver evaluation and extended: "capability parity" (native host capability: subprocess, filesystem, toolchain — expected trivial for a plain .NET process) versus "control parity" (editor feel, custom drawing, pane hosting); "the kill phase" (the one question whose failure ends the evaluation before UI work); "response supply" (whether `WebResourceRequested` lets the host answer a pane's request, D3's contract, here undocumented and unverified); "felt comparison" (David's own judgment of typing feel and shell fidelity, not inferred from measurement).

## References consulted
- `docs/architecture/adrs/adr-341-chord-writer-windows.md` — D3 (the host contract for the three web panes: serve pane files, serve the story's own bundle, answer subprocess-result requests; "a pane that can only be served on Windows by a second copy is the failure this decision exists to prevent"); D7 (installer + update channel: MSIX-with-appinstaller vs. conventional installer + updater, Azure Trusted Signing under David's account — Velopack is D7's second option, cross-platform, and this plan's Phase 5 is where D7 gets a live answer for a third shape); AC-1 (the parity table is the acceptance bar this plan's Phase 6 column feeds) through AC-7 (a signed installer built and verified on a clean machine — this plan's Phase 5/7 analog); Scope (ADR-341 excludes OpenSilver from its own Scope and does not open an ADR on keeping alternate targets reachable — Avalonia is further outside that Scope still, so this plan's ADR question in Phase 6 is explicitly a new-ADR question, never a same-ADR edit).
- `docs/work/opensilver-ide-evaluation/decision.md` — §1 (the per-control WPF-vs-OpenSilver table this plan's Phase 6 extends to a fourth column, not a new table); §5 (names Avalonia, unevaluated, as "the candidate that offers the first [one codebase] without the second [web rendering]" — the exact premise this plan tests); §8 (dependency-health table this plan's Phase 0 must refresh with dated numbers, not reuse stale ones); §9 (owed items — GH #457 vendored-toolchain packaging gap and GH #458 fixture disposition apply identically here and are not re-litigated, only re-noted if hit again).
- `docs/work/opensilver-ide-evaluation/parity-table.md` — the row set and the "Spike?" flagging method this plan's Phase 6 column reuses verbatim (never inventing new product surface); the ordered spike list (editor → pane hosting → project tree → chrome/appearance → tab strip → World map) whose capability-first ordering this plan's phase order follows, adapted to O6's own kill list.
- `docs/work/opensilver-ide-evaluation/phase-1-host-capability.md` — the nine real-path xunit tests (toolchain paths, vendored Node execution, compose through the sealed shim including its rejection case, streamed-stdout timing, cancellation, Documents read/write including a refusal case, browser-target refusal) this plan's Phase 2 reproduces against the Avalonia host, "expected trivial... but recorded, not assumed" per the goal framing; GH #457 (vendored toolchain cannot build an out-of-repo story) as a known open platform defect this plan does not fix if hit again.
- `docs/work/opensilver-ide-evaluation/phase-2-pane-hosting.md` — the pane-hosting design (custom scheme vs. localhost, script injection timing, the WebKit-specific `window.webkit.messageHandlers` shape a non-WebKit host must counterfeit) and the ADR-307 Testing round trip (tree document in, replay, typed turn, document out) this plan's Phase 1 must reproduce under `Avalonia.Controls.WebView`, including the same host-shape finding if it recurs.
- `docs/work/web-chord-writer/options-20260913-web-app-shapes.md` — §3 "O6 — Avalonia + Velopack" (dependency versions and health, the `WebResourceRequested` response-supply question as O6's kill question, D7/Velopack correspondence, App Sandbox non-issue since Chord Writer spawns Node); §5 "O6 — the kill questions, in order" (the six-step list this plan's phases follow in sequence); §6 (the four-way judging-dimension table this plan's Phase 6 decision record must fill for the Avalonia column).
- `docs/context/session-20260913-1613-main.md` (newest session file) — Open Items: the plan pointer still names the OpenSilver evaluation and the rule-18b disposition is pending (carried into this plan's header above, not resolved by it); GH #458 (orphaned Phase 1 fixture folder disposition) and OpenSilver's own Phase 6 (Windows re-check) remain open and unrelated to this plan's scope, noted so this plan does not silently re-open or duplicate them.

## Evidence discipline (applies to every phase)
Every acceptance-gating claim in this plan's phase records carries the command run and its result, dated, inline — never "see the spike" (project memory: evidence is inline, never cited). Spike code lives OUTSIDE this repository at `/Users/david/repos/spikes/avalonia-ide/` (sibling of `/Users/david/repos/spikes/opensilver-ide/` and `/Users/david/repos/spikes/adr-341/`); only the record — this plan, the phase records, the filled parity column, the decision document, and `evidence/` (screenshots, logs) — lives under `docs/work/avalonia-ide-evaluation/`. The vendored toolchain comes from `tools/ide/vendor-toolchain.sh` into a staging directory; the OpenSilver spike's `toolchain-staging/toolchain` (175 MB) may be reused rather than re-vendored. GH #457 (vendored toolchain cannot build an out-of-repo story) is a known open platform defect, not something this plan fixes if it recurs.

**Test story**: fernhill (`branch-stories/fernhill`), read-only. Its already-built `dist/web/fernhill/` bundle is served unmodified; its `.story`, `.tests.json`, and folder are never written. `git status --short branch-stories/fernhill` must stay empty after every phase. Everything the spike writes goes to `/Users/david/repos/spikes/avalonia-ide/out/`. No real story is ever modified; no ADR references appear in any test story text.

**Rule 13a Integration Reality**: Phase 1 (pane hosting — the kill phase) and Phase 2 (subprocess/folder/vendored-Node round trip) are integration phases. Each needs a REAL-PATH test against the real `sharpee` shim, the real vendored Node, the real `~/Documents/<Story Title>/` folder, and fernhill's real bundle — no stub of `sharpee`, no mocked filesystem, no placeholder Node, no hand-written replacement for `Avalonia.Controls.WebView`'s response-supply mechanism. If Phase 1's real path cannot be exercised on the real WebView backend, the phase records that as the finding — it does not substitute a stub and call the integration proven.

**Notarization risk (carried explicitly into Phase 5)**: project memory records that Apple's `notarytool` crashes on upload from this Mac and the working path is the REST route via `tools/*/notary-submit.py` (single PUT, verdict in ~1 minute). Velopack's macOS packaging drives `notarytool` via `--notaryProfile`. Phase 5 must record which path actually worked here — `vpk pack` with signing and notarization deferred, then `notary-submit.py` over the produced artifact; or a direct check of whether `vpk`'s `notarytool` invocation succeeds on this machine where the bare CLI does not. Signing identity is David's Developer ID, already used by `tools/ide/release-all.sh`. David distributes; Claude builds — this plan never publishes or distributes an artifact.

## Phases

### Phase 0: Prerequisites, dependency health, and scaffold
- **Tier**: Small
- **Budget**: 100
- **Domain focus**: Toolchain verification — confirms Avalonia 12.1.x, `Avalonia.Controls.WebView` 12.1.0, and Velopack's `vpk` CLI are reachable on this Mac's .NET 10 SDK (10.0.300), and that AvaloniaEdit's compatibility against Avalonia 12 (a stated watch item — AvaloniaEdit 11.4.1 predates Avalonia 12.1.2 by several months per the options doc) is checked rather than assumed, before any later phase depends on it.
- **Entry state**: No Avalonia project exists anywhere in `/Users/david/repos/spikes/`. This Mac has .NET SDK 10.0.300 (confirmed working for the OpenSilver spike). No Avalonia templates installed. `vpk` CLI not installed. Dependency versions as recorded 2026-09-13 in the options doc (Avalonia 12.1.2, AvaloniaEdit 11.4.1, Velopack 1.2.0, `Avalonia.Controls.WebView` 12.1.0) are several hours old at plan-write time and must be re-checked with `gh api` at phase-run time, not copied forward silently.
- **Deliverable**: (1) A refreshed dependency-health table (`gh api` per repo, dated) confirming stars/last-push/license/latest-release for `AvaloniaUI/Avalonia`, `AvaloniaUI/AvaloniaEdit`, `velopack/velopack`, and the `Avalonia.Controls.WebView` NuGet package version, at phase-run time. (2) `dotnet new` templates for Avalonia 12.1.x installed and the exact version combination recorded (Avalonia app template version, target framework, RID). (3) A minimal "hello window" Avalonia app at `/Users/david/repos/spikes/avalonia-ide/hello/` building and running on macOS, with a screenshot as evidence. (4) `Avalonia.Controls.WebView` 12.1.0 added to the hello app and a blank `NativeWebView` control shown on-screen (no pane content yet — that is Phase 1) to confirm the package resolves and initializes on this host before Phase 1 depends on it. (5) `vpk` CLI installed (`dotnet tool install -g vpk` or the documented equivalent) and `vpk --version` recorded. (6) AvaloniaEdit 11.4.1 added to a throwaway view and confirmed it at least loads against Avalonia 12.1.2 (build succeeds, control renders) — full editor functionality is Phase 3's job, this is only the compatibility gate the options doc flagged as a watch item.
- **Exit state**: A working Avalonia scaffold builds and runs on this machine with `NativeWebView` initializing and AvaloniaEdit loading without a version conflict, or the phase record states plainly which of these fails, with the blocking error inline, and Phase 1 does not start until resolved (a build-level AvaloniaEdit/Avalonia-12 incompatibility would demote Phase 3 to "CodeMirror in a NativeWebView pane" from the start rather than discovering it mid-phase-3). The exact version combination that works is recorded for every later phase to reuse.
- **Status**: DONE (2026-09-14) — all six deliverables met; record: `phase-0-prerequisites.md`. Two premises corrected: `Avalonia.AvaloniaEdit` **12.0.0** exists on NuGet (published 2026-04-08, floor `Avalonia >= 12.0.0`, `net10.0` target group), so the AvaloniaEdit-vs-Avalonia-12 watch item is closed at build level; and `vpk` on this machine requires `DOTNET_ROOT=/opt/homebrew/Cellar/dotnet/10.0.300/libexec` on every invocation (Phase 5 must carry it).

### Phase 1: NativeWebView pane hosting — the kill phase (Integration Reality, rule 13a)
- **Tier**: Medium
- **Budget**: 250
- **Domain focus**: ADR-341 D3's host contract (serve pane files, serve the story's own `dist/web/<id>/` bundle, answer subprocess-result requests) seen from a fourth host shape after `WKWebView` (macOS), `WebView2` (WPF spike), and Photino's custom-scheme iframes (OpenSilver spike). This is O6's own named kill question: whether `Avalonia.Controls.WebView`'s `WebResourceRequested` event lets the host **supply** the response on macOS (WKWebView backend) — undocumented in Avalonia's own reference, though the assembly carries `SetResponse`, `AddWebResourceRequestedFilter`, `SetVirtualHostNameToFolderMapping`, and a `customScheme` string per today's `strings` check.
- **Entry state**: Phase 0's scaffold builds with `NativeWebView` initializing. No pane-hosting code exists yet in the Avalonia spike.
- **Deliverable**: fernhill's `dist/web/fernhill/` bundle and both IDE-owned panes (`tools/ide/web/testing-surface`, `tools/ide/web/docs-tab`) served inside the Avalonia app via `NativeWebView`, tried in this order: (a) a response supplied from `WebResourceRequested` on the macOS (WKWebView) backend; (b) if (a) fails, a custom scheme via `SetVirtualHostNameToFolderMapping` or the `customScheme` string found in the assembly; (c) if both fail, the recorded fallback — a localhost origin with a session token, named explicitly as a fallback rather than silently substituted. Whichever mechanism works, the phase also proves: `localStorage` surviving an app restart; `WebMessageReceived` delivering messages both directions (host→page via `InvokeScript`, page→host via `WebMessageReceived`); and the full ADR-307 Testing round trip — fernhill's tree document (31 cards, seed 42) injected, replayed through the real client, one typed turn, the whole document written back to the spike's `out/` directory (never to `branch-stories/fernhill`). Each of these is a REAL-PATH test per rule 13a — no stub of `Avalonia.Controls.WebView`, no hand-written replacement for the response-supply mechanism, no mocked message channel.
- **Exit state**: A named PASS/FAIL per pane, with the mechanism (a, b, or c) recorded for each. If none of (a)/(b)/(c) works on the macOS backend — a genuine FAIL, not "awkward" — this is a kill of D3-as-evaluated for O6: the phase record states this plainly, and Phases 3–5 either do not proceed on pane hosting specifically (the editor and drawing-model phases can still run independently, since they do not depend on pane hosting) or proceed with the localhost-fallback shape explicitly carried forward as O6's answer to D3, not glossed over. Phase 6's decision record cites this phase's verdict directly.
- **Status**: DONE (2026-09-14) — record: `phase-1-pane-hosting.md`. Mechanism (a) response supply FAIL and (b) custom scheme/virtual host FAIL on the macOS backend (both names exist only as internal Windows COM interop; `setURLSchemeHandler:forURLScheme:` is not bound at all); (c) token-scoped loopback origin PASS, carrying all three panes, both messaging directions, and the full ADR-307 round trip (31 → 32 cards, seed 42). Tests 11 passing. Two items owed: the replay over-runs (274 records vs the OpenSilver host's 31, unexplained — §5) and `localStorage` across an app restart is untested.

### Phase 2: Subprocess, folder, and vendored Node (Integration Reality, rule 13a)
- **Tier**: Small
- **Budget**: 100
- **Domain focus**: The plain .NET capability half of "capability parity" — `System.Diagnostics.Process` with streamed stdout, real `~/Documents/<Story Title>/` read/write, and locating a vendored Node binary beside the app bundle. Expected trivial for a .NET process (the same primitives WPF and OpenSilver's Photino host both already proved), and recorded rather than assumed, per the goal's explicit framing.
- **Entry state**: Phase 0's scaffold exists. No subprocess, filesystem, or Node-location code exists yet in the Avalonia spike. The OpenSilver spike's `toolchain-staging/toolchain` (175 MB, already vendored and signed) may be symlinked in rather than re-vendoring.
- **Deliverable**: The nine real-path tests from `phase-1-host-capability.md`'s `Hello.Host.Tests` reproduced as xunit tests against the real Avalonia host code: toolchain paths resolve only when files exist; vendored Node runs and reports its version; `compose --json` through the sealed shim returns gate-clean IR for a real fixture story (never a real story — a dedicated fixture under Documents, as the OpenSilver phase used, or that same fixture reused); compose of a missing story exits non-zero with stderr; stdout lines arrive incrementally, not batched at exit; cancellation kills the process tree; write-then-read round-trips in the real Documents folder; write into a missing directory is refused; and (this host's equivalent of) a browser/non-native target's capability refusal, if a non-desktop Avalonia target is in scope for this check — otherwise noted as not applicable and why.
- **Exit state**: All reproduced tests pass with the same evidence discipline (`dotnet test`, exit code, pass count, dated), or a genuine failure is recorded with its exact error — not waved through as "should be fine, it's a .NET process." GH #457 is not re-triggered by this phase's own `compose`/`build` calls unless the phase happens to invoke `build`; if it does and hits the same vendored-toolchain packaging gap, the phase records the recurrence and does not attempt a fix (platform change; discuss first).
- **Status**: DONE (2026-09-14) — record: `phase-2-host-capability.md`. All nine real-path tests pass first run (suite now 20 passing, 0 failures): vendored node `v22.23.1`, `compose --json` gate-clean against the reused Documents fixture, non-zero exit + stderr for a missing story, incremental stdout (≥600 ms spread), cancellation kills the process tree, real Documents round trip, missing-directory refusal, browser-head refusal (tested as a class — no WebAssembly head built). Not one line of the seam references Avalonia. GH #457 recurs identically and is recorded, not fixed.

### Phase 3: Editor
- **Tier**: Medium
- **Budget**: 250
- **Domain focus**: What a code editor is, concretely, on Avalonia — the parity table's first-spiked, highest-weight row in every prior evaluation (D2's own ordering: "the editor first"), and the one-grammar question (ADR-341 D4, AC-5) an Avalonia shell must answer explicitly rather than by default.
- **Entry state**: Phase 0 confirmed whether AvaloniaEdit loads cleanly against Avalonia 12.1.2. Phase 2 passed (the editor's save/reload needs the same file read/write Phase 2 proved).
- **Deliverable**: A working editor surface, tried in this order per the options doc: (a) AvaloniaEdit driven by the compiler's own lexer (`packages/chord/src/lexer.ts`), via whatever binding gets tokens from a TypeScript lexer into a `DocumentColorizingTransformer` (a bundled JS-to-embedded-runtime bridge, or a C# port — if a C# port is the only route found, the phase must say so plainly, since a second hand-written Chord tokenizer is exactly the "second definition of Chord syntax" risk ADR-341's Context calls out, and D4/AC-5's "one grammar" question is not satisfied by it); (b) if (a) is not reachable in-budget, CodeMirror 6 in a `NativeWebView` pane bundling the real lexer, as the OpenSilver spike did (proven pattern, reuses Phase 1's pane-hosting mechanism if it passed). Either route is measured against the same 1755-line file the OpenSilver and WPF spikes used (fernhill + ides-of-march head; the OpenSilver spike's `out/measure-1755.story` may be reused), on: whole-document style-pass timing, per-character typed cost, span selection and scroll-to-span from outside the editor, programmatic undoable text replacement (hash-verified round trip), gutter/line numbers, and word wrap.
- **Exit state**: A named verdict on which route was built (AvaloniaEdit-native or CodeMirror-in-pane) and why, a timed measurement comparable in kind to the WPF (14.8–15.9 ms) and OpenSilver (0.92 ms) numbers on the same file, and an explicit statement of how this plan's route answers or reopens D4/AC-5's one-grammar question. **The felt judgment (native-feeling vs. "a web page in a box," if route (b) was taken) is David's** — this phase measures and reports, it does not render that verdict.
- **Status**: DONE (2026-09-14) — record: `phase-3-editor.md`. **Route (a)** taken: AvaloniaEdit natively drawn, colored from the real `packages/chord/src/lexer.ts` bundled as an NDJSON service under the vendored Node — no C# port of the lexer, so D4/AC-5's one-grammar question is answered without a second grammar (the curated keyword/property display sets are a third copy of a display list, not of the grammar — stated plainly in §1). Route (b), CodeMirror in a pane, was not needed. Measured on the same 1755-line file: 7.96 ms per whole-document style pass, 7.41 ms per typed character, against WPF's 14.8–15.9 ms and CodeMirror's 0.92/1.09 ms; breakdown 0.49 ms lexer + 3.93 ms bridge + 3.86 ms index rebuild, neither of the latter two inherent. Select/scroll, undoable replace with hash round trip, gutter and live word wrap all pass. **Felt judgment reserved to David.**

### Phase 4: Custom-drawn surfaces and appearance
- **Tier**: Medium
- **Budget**: 250
- **Domain focus**: The parity table's custom-drawn rows and ADR-297's live appearance flip, on Avalonia's own drawing model — `Render(DrawingContext)`, WPF's idiom, the exact ground David named as deciding WPF over WinUI 3 (ADR-341 D2) and the one OpenSilver could not offer (retained composition instead, per `decision.md` §1). This phase is where O6's central promise (WPF's drawing model, but cross-platform) either holds or does not.
- **Entry state**: Phase 0 complete. Phases 1–3 need not have passed for this phase to run in reduced form (drawing-model research is independent of pane hosting and the editor route), but a full felt comparison against the shipping app benefits from Phases 1 and 3 having already mounted real panes and a real editor into the shell this phase builds chrome around.
- **Deliverable**: The tab strip (left-aligned text tabs, accent top bar, count badges), the editor's tab bar (dirty-dot indicator), and the World map (fed by real `sharpee compose` and `sharpee world-index` output against fernhill, two levels, connections, one displaced-room case if fernhill has one — reusing the OpenSilver spike's approach of feeding real analyzer output rather than synthetic data) built as `Render(DrawingContext)` overrides driven by `AffectsRender<T>`; the project pane as a list/tree view over the real folder; ADR-297's dual-palette live flip measured at the pixel/brush level (a token mutated, a screenshot or DOM/visual-tree diff before and after, timed); `NativeMenu` wired for the macOS menu bar (the OpenSilver Phase 4 gap — no native `Menu` control — does not apply here per the options doc); and the `MainWindow.swift` shell (chrome band, rail, splitters, right and bottom panels, status bar) mirrored enough — with Phase 1's panes and Phase 3's editor mounted if they passed — for a felt comparison against the shipping AppKit app.
- **Exit state**: A named verdict on whether Avalonia's `Render(DrawingContext)` behaves as advertised — genuinely immediate-mode like WPF, not retained composition wearing WPF's method name — with line-count or complexity evidence comparable to the OpenSilver Phase 4 measurement (~500 lines across four surfaces there); the live-flip result stated PASS/FAIL/PARTIAL; `NativeMenu` PASS/FAIL on macOS. **The felt comparison of the whole shell against the shipping app is David's** — this phase mounts the shell and reports what was built and measured, it does not render the verdict.
- **Status**: DONE (2026-09-14) — record: `phase-4-shell-and-appearance.md`. **`Render(DrawingContext)` is WPF's model, measured**: one call per invalidation, zero while idle over 8 frame waits, re-entered on `AffectsRender` and `InvalidateVisual` — not per-frame, not a composed tree. Four surfaces built (tab strip, editor tab bar, World map, project pane; 453 lines against OpenSilver's ~500) with the World map fed by real world-index output (13 rooms, 12 connections, 3 doors, 2 levels, 1 displaced). ADR-297 flip **PASS at the pixel level** — `RenderTargetBitmap` pixel #DCE0E8 → #16171D, `Apply(22 tokens)` 0.06 ms, and **zero re-records**, which contradicts `decision.md`'s prediction that immediate-mode pays for the flip. `NativeMenu` **PASS** (File, Story in the macOS menu bar) where OpenSilver had none. Felt comparison reserved to David; two screenshots are the evidence for it.

### Phase 5: Velopack packaging on macOS
- **Tier**: Medium
- **Budget**: 200
- **Domain focus**: ADR-341 D7's open installer/update-channel choice, answered for this shape by Velopack directly — `vpk pack` producing a signed, notarized `.app` carrying the vendored toolchain, with delta updates.
- **Entry state**: Phase 0's `vpk` CLI is installed. A buildable Avalonia app exists from whichever of Phases 1–4 completed (packaging does not require every prior phase to have passed — a minimal shell with the vendored toolchain embedded is sufficient to exercise packaging, and the phase record says exactly which build was packaged).
- **Deliverable**: `vpk pack` run on macOS producing a `.app` with the vendored toolchain (from Phase 2) embedded inside it; Developer ID signing using the identity already used by `tools/ide/release-all.sh`; notarization attempted via `vpk`'s documented `--notaryProfile` path first, with the actual outcome recorded (pass, or the same `notarytool` upload crash the project memory already records); if `vpk`'s path fails the same way, the fallback — `vpk pack` with signing only, notarization deferred, then `tools/*/notary-submit.py`'s REST route run directly against the produced artifact — attempted and its outcome recorded; install and launch of the packaged app on this machine; a version-N-to-N+1 update cycle exercised (`vpk` produces a new release, the app checks and applies it) with the delta package size recorded; sizes and timings for the pack, sign, notarize, and update steps all inline with dates. David distributes; Claude builds — no artifact from this phase leaves this machine or gets distributed.
- **Exit state**: A named PASS/FAIL for packaging, for each of signing/notarization/install/launch/update, with whichever notarization path actually worked recorded plainly (this is the one D7 answer this whole evaluation track has been missing across all three prior shapes' Windows-side D7 questions, and it is exercised here on macOS since Avalonia's macOS leg is not deferrable per the goal framing).
- **Status**: DONE (2026-09-14) — record: `phase-5-velopack-packaging.md`. Packaging **PASS** (34.5 s; toolchain rides at `Contents/MacOS/toolchain`, vendored node runs from the bundle); install and launch **PASS** (the packaged .app runs the whole Phase 4 shell); delta updates **PASS and strong** — 1.0.0 → 1.0.1 is **8.11 MB against a 99.8 MB full package**. Signing **FAIL**, four routes, one structural cause: Velopack's macOS bundler puts all 225 entries — data files and the 175 MB toolchain — flat in `Contents/MacOS/`, and `codesign` will not seal that; no `vpk` flag can move a file out of it. Notarization **not reachable** (an unsealed bundle cannot be notarized) — nothing was submitted to Apple, and the `notarytool` vs `notary-submit.py` question stays open for this shape. No `Developer ID Installer` certificate exists, so the `.pkg` cannot be signed either.

### Phase 6: Parity column, decision record, and (on confirmation) a new ADR draft
- **Tier**: Medium
- **Budget**: 200
- **Domain focus**: Assembling the evidence from Phases 0–5 into the artifact this evaluation exists to produce — extending the three-way comparison to four, on the same frame `decision.md` §6 already used and the options doc §6 extended.
- **Entry state**: Phases 0–5 each have an inline evidence record (pass, fail, or partial — no phase left silently unresolved). The Windows check (Phase 7) need not have run yet — this phase's decision record states plainly that the Windows column is pending, mirroring how the OpenSilver decision record carried its own Phase 6 as a stated condition rather than waiting on it.
- **Deliverable**: (1) `docs/work/avalonia-ide-evaluation/parity-table.md` — the existing `docs/work/archive/adr-341-spike/parity-table.md` rows (reused via `docs/work/opensilver-ide-evaluation/parity-table.md`'s already-filled OpenSilver column as the adjacent reference) with a new **Avalonia** column filled row by row from Phases 0–5's evidence — never inventing new product surface. (2) `docs/work/avalonia-ide-evaluation/decision.md` — extending `docs/work/opensilver-ide-evaluation/decision.md` §1's per-control table to a fourth column (WPF / OpenSilver / Avalonia, with the fourth reusing the same row set), using the options doc §6 dimension table (codebases, author's files, toolchain shipped, install/update, rendering, drawing model, editor, web panes, offline, macOS app fate, where the deciding risk sits) filled for the Avalonia column with this plan's own evidence, a stated recommendation with grounds in weighted order, and the honest counter-case — not a one-sided pitch, matching the discipline both prior decision records held to. (3) GitHub issue #438 commented with the outcome — the same umbrella issue the OpenSilver decision record used, since this is the same four-way question returning with a fourth column filled, not a new issue. (4) **Only on David's explicit ADR-worthy confirmation (rule 11)**: a draft ADR — never assumed, never pre-empted — that the four-way evidence now supports; ADR-341 itself is not edited by this plan either way. If the draft carries open questions, the rule-11a interview offer follows normally.
- **Exit state**: David has a four-column parity table, a decision record with the Avalonia column filled and the counter-case honestly stated, and (if confirmed ADR-worthy) a draft ADR to review. Nothing is merged, ADR-341 is untouched, GH #438's disposition is recorded.
- **Status**: CURRENT (since 2026-09-14)

### Phase 7: Windows check (optional, separate machine, scheduling-dependent)
- **Tier**: Small
- **Budget**: 100
- **Domain focus**: Whatever Phases 0–6 proved on macOS, checked once on the actual Windows target — Velopack's `Setup.exe` path with the Azure Trusted Signing flag (D7's signing ruling), the `WebView2` backend of `NativeWebView` (untested here — only WKWebView is exercised on macOS), and the vendored toolchain's Windows launcher.
- **Entry state**: Phase 6's decision record exists. The Windows machine used by sessions 89f9e0/b79c97/OpenSilver Phase 6 is available. (Not run for any of the four shapes yet — this is a separate, scheduling-dependent phase for every shape, not specific to Avalonia.)
- **Deliverable**: Phase 1's pane-hosting mechanism, Phase 2's capability tests, and Phase 5's packaging re-run on Windows with the same evidence discipline, confirming or correcting that the macOS-measured results are not macOS-specific artifacts — in particular whether `WebResourceRequested`'s response-supply behavior differs between the WebView2 and WKWebView backends, since the assembly strings that suggested the mechanism exists were read from the same cross-platform assembly but Phase 1 only exercised the WKWebView side.
- **Exit state**: The decision record gains a "confirmed on Windows" or "Windows-specific gap found" addendum; this phase's evidence folds back into Phase 6's record rather than standing alone.
- **Status**: PENDING — scheduling depends on Windows-machine availability; may run out of order relative to Phase 6 if the machine becomes free earlier.

## Open questions carried into this evaluation (not blocking, tracked for Phase 6)
- ~~**The response-supply mechanism is unverified until Phase 1 runs.**~~ — **answered in Phase 1**: there is no response-supply or custom-scheme door on the macOS backend, so O6 answers D3 over a token-scoped loopback origin, carried forward explicitly as its answer rather than glossed over. Whether the WebView2 backend differs is Phase 7's question. Original wording: The options doc names `SetResponse`, `AddWebResourceRequestedFilter`, `SetVirtualHostNameToFolderMapping`, and a `customScheme` string as evidence the plumbing exists in the assembly, but explicitly states this is not confirmed working end-to-end. Phase 1 is the only place this gets settled, and its outcome gates whether O6 answers D3 as evaluated or falls back to a localhost-origin shape.
- ~~**Whether AvaloniaEdit's release lag behind Avalonia 12 is a build-time blocker or a live-but-stale dependency**~~ — **answered in Phase 0**: the lag is a GitHub release-tag artifact only; `Avalonia.AvaloniaEdit` 12.0.0 is on NuGet and builds clean against Avalonia 12.1.2 with zero warnings. Phase 3 still chooses the editor route on its merits, but not under a version-conflict risk. Original wording — Phase 0 answers the build question; Phase 3 answers whether it's usable in practice or CodeMirror-in-pane is the better route regardless of whether AvaloniaEdit loads.
- ~~**Whether a C# port of the Chord lexer … counts as violating D4/AC-5's "one grammar" principle**~~ — **moot: no port was written.** Phase 3 reached route (a) through an NDJSON bridge to the real lexer under the vendored Node, so the question does not arise for O6. Original wording: — this plan does not resolve that question, it only states plainly in Phase 3's record which route was taken and lets Phase 6's decision record carry the tension forward, the same way the OpenSilver decision record surfaced (not resolved) D4's rewrite implications.
- **Whether the macOS felt comparison in Phase 4 is decided in this evaluation or deferred** — **still open, and now actionable**: Phase 4 built the shell and captured it in both palettes (`evidence/phase-4-shell-dark.png`, `phase-4-shell-light.png`) with the real panes, editor, World map and native menu mounted, so the comparison needs David rather than more spike work. Original wording: whether it is decided here or deferred, mirroring how the OpenSilver evaluation deferred the "replace the Mac app" question to a felt comparison
- **GH #457** (vendored toolchain cannot build an out-of-repo story) and **GH #458** (orphaned OpenSilver Phase 1 fixture folder) are known open items from the prior evaluation, unrelated to this plan's scope unless Phase 2 or Phase 5 happens to hit the same toolchain-packaging gap — noted here so this plan does not silently re-discover or re-file either.
- **The pointer handoff itself** (this plan header) — not an evaluation question, but tracked here since it blocks this plan from becoming the session's active plan until the main session resolves the OpenSilver plan's rule-18b disposition.
