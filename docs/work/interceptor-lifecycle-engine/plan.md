# Session Plan: Interceptor Lifecycle Engine (ADR-228)

> You are an interactive fiction platform design expert and expect all changes to adhere to aligning Sharpee and Chord for full IF logic access. No shortcuts. No bespoke hacks.

**Created**: 2026-07-16
**Overall scope**: Turn the ADR-118 interceptor lifecycle from a hand-copied convention (14 drifted implementations, 19 silently-dead surfaces) into a shared stdlib mechanism per ADR-228's rulings D0-D8. Touches world-model (API signature change), stdlib (new engine + 33 action migrations/declarations), and story-loader (Chord fail-fast + a `while`-gate lowering bug).
**Bounded contexts touched**: N/A — this is platform/engine infrastructure work (a shared execution mechanism replacing a hand-copied convention), not new domain modeling. No `docs/ddd/notation.yaml` exists in this project.
**Key domain language**: N/A (technical framing per session-planner guidance — infrastructure work with no notation file). Vocabulary in use throughout: interceptor, descriptor, lifecycle hook (preValidate/postValidate/postExecute/postReport/onBlocked), veto, wired action, delegation seam.

## References consulted
- `docs/architecture/adrs/adr-228-interceptor-lifecycle-engine.md` — the accepted ADR; source of every ruling (D0-D8) and the sequencing skeleton this plan follows.
- `docs/architecture/adrs/adr-118-stdlib-action-interceptors.md` — origin of the hook vocabulary and interceptor interface; its example code is superseded by D1/D2 where they contradict it.
- `docs/architecture/adrs/adr-208-interceptor-registry-engine-owned.md` — the binding-map storage (per-world, idempotent, last-registration-wins) is unchanged by this work; only the lifecycle *execution* and the `onBlocked` signature change.
- `docs/architecture/adrs/adr-090-entity-centric-action-dispatch.md` — capability dispatch (lowering/raising full-delegation) stays untouched; the interceptor engine must not encroach on capability-dispatch actions.
- `docs/architecture/adrs/adr-051-action-behaviors.md` — superseded by ADR-052, kept only as historical context; the four-phase action pattern it names is carried forward by later ADRs and remains the pattern the engine calls into (validate/execute/report/blocked).
- `docs/work/adr-118-hook-audit/audit.md` — per-action gap detail (which of the 14 have which path/entity skips; which of the 19 are unwired and their plausibility ranking) that each migration/declaration phase below is scoped against.
- `docs/work/adr-118-hook-audit/decisions.md` — rationale per fork (D0-D8); D7/D8 fix shapes are rulings, not forks, and are folded directly into phases 3 and 7.
- `docs/context/project-profile.md` — confirms build/test commands (`./repokit build dungeo`, `dist/cli/sharpee.js --test`, `pnpm --filter '@sharpee/stdlib' test`), the 4-file action convention, TypeScript strict mode, and the "one-good-run" walkthrough rule baked into every phase's verification step below.
- `docs/context/session-20260716-1330-chord-foundations.md` — most recent session's open items: David's rulings on D0-D8 (all recommendations accepted) and the goal "plan the implementation," status IN PROGRESS; no other blockers or deferred work recorded.

## Phases

### Phase 1: Lifecycle engine module + world-model signature change + pinning tests
- **Tier**: Large
- **Budget**: 400
- **Domain focus**: New stdlib module owning resolve → preValidate → postValidate → postExecute → postReport → onBlocked (D1-D4's spec, implemented exactly once); `ActionInterceptor.onBlocked` signature change in world-model (D2).
- **Entry state**: ADR-228 accepted; no shared engine exists; each of the 14 wired actions still hand-rolls its own lifecycle; `ActionInterceptor.onBlocked` returns a bare `CapabilityEffect[] | null`.
- **Deliverable**:
  - New module under `packages/stdlib/src/actions/` (e.g. a `lifecycle/` subdirectory) implementing: veto-only guard semantics (D1 — only `result.valid === false` blocks; the reserved `{valid: true, force: true}` extension documented but NOT implemented); structured `onBlocked` handling (D2 — `{ override?, emit? }`, event type survives, `null`/`{}` means standard); fixed-order all-entities consultation with first-veto-wins (D3 — direct object → indirect/instrument → implicit entities, each with its own sharedData); a shared multi-object helper implementing per-item resolve→preValidate→postValidate→postExecute→postReport→onBlocked (D4).
  - Declarative descriptor type(s) actions will supply: which command entities carry interceptors under which action ids (incl. implicit-entity resolvers), multi-object item-to-lifecycle mapping, and an explicit special-contract flag mechanism (for attacking's postExecute-replaces-combat, needed by Phase 3).
  - `ActionInterceptor.onBlocked` signature change in `packages/world-model/src/capabilities/action-interceptor.ts` to `{ override?, emit? } | null`; update the two existing consumers of the old shape found in stories (search `stories/` for onBlocked implementations before assuming none exist).
  - Pinning tests for the engine itself (not per-action): veto-only semantics (a hook returning `{valid: true}` does not skip later hooks/checks), onBlocked override-vs-emit-vs-null, fixed-order multi-entity consultation with first-veto-wins, multi-object per-item lifecycle (including onBlocked per failed item).
- **Exit state**: Engine module exists and is unit-tested in isolation (no action wired to it yet); `ActionInterceptor.onBlocked`'s new signature compiles across world-model; the two existing onBlocked story implementations (if any) are migrated to the new shape so the type change doesn't leave dead code; `./repokit build dungeo` succeeds; `pnpm --filter '@sharpee/stdlib' test` and the world-model equivalent are green.
- **Status**: COMPLETE (2026-07-16, session 3ed9fb) — four story onBlocked implementations found and migrated (melee, glacier-throwing, sphere-taking, gas-room-entry), not two; world-model suite has 22 PRE-EXISTING darkness/light failures verified identical on clean HEAD (flagged to David — not from this phase); stdlib suite 1366 green incl. 17 engine pinning tests; affected Dungeo transcripts one-good-run clean.

### Phase 2: Migrate the 14 wired actions — group A (single/dual-entity actions)
- **Tier**: Large
- **Budget**: 400
- **Domain focus**: taking, dropping, eating, examining, reading, closing, opening — the simpler half of the 14 (no second-entity gaps beyond D3's normal consultation, no special-contract flags). Fixes the D1/D2 semantic splits (B4 in the audit) and taking/dropping's multi-object bypasses (D4, part of B1).
- **Entry state**: Phase 1's engine and descriptor types exist and are pinned; these 7 actions still hand-roll their lifecycle inline with the documented drift (taking: onBlocked never fires on multi-object paths; dropping: multi path skips all 5 hooks AND uses short-circuit-on-truthy guard semantics per audit B4; examining/reading: hardcoded action-id strings, append-not-replace onBlocked — the correct D2 direction already, just not routed through IFActions constants; taking/eating/examining/reading/closing/opening: already canonical veto-only guard semantics per audit B4, so D1 migration for these six is mechanical — only dropping's guard semantics actually change).
- **Deliverable**: Each of the 7 actions rewritten to supply a descriptor and call the shared engine at its four phase boundaries; hand-rolled lifecycle code deleted from each action file; D7.4/D7.5 normalization folded in for these actions (IFActions constants, one sharedData key convention, dead-local cleanup) since the engine makes it moot; taking and dropping gain full multi-object lifecycle coverage including onBlocked per failed item (closing the take-all/drop-all gaps named in the audit).
- **Exit state**: All 7 actions wired through the engine with zero hand-rolled lifecycle code remaining in them; existing unit tests updated for the (documented, ADR-blessed) observable ordering change — taking's postValidate now runs after ALL standard validation; `pnpm --filter '@sharpee/stdlib' test` green; relevant transcripts (rug/trapdoor, troll-axe white-hot take-block per audit C, any take-all/drop-all transcripts) pass via `node dist/cli/sharpee.js --test`; walkthrough chain run once per the one-good-run rule (RNG flakes not regressions).
- **Status**: COMPLETE (2026-07-16, session 3ed9fb) — no unit-test churn was needed (nothing pinned the old ordering/guard semantics); stdlib suite 1368 green; walkthrough chain 876/876 first run; white-hot axe verified through the engine. Note: API 529 overload killed the three migration subagents mid-phase — eating/examining/reading/closing/opening/dropping were migrated inline in the main session instead.

### Phase 3: Migrate the 14 wired actions — group B (multi-entity actions + D7 path-skip repairs)
- **Tier**: Large
- **Budget**: 400
- **Domain focus**: entering, pushing, putting, switching_on, throwing, attacking, going — the actions with second-entity gaps (weapon, door, item-side, single-winner) and the D7-blessed path-skip repairs. Fixes the live trophy-case scoring bug (D4) and the four D7 path-skip items.
- **Entry state**: Phase 2 complete; group A actions prove the descriptor pattern; these 7 actions still have: putting's ALL-5-hooks-dead multi-object bypass (live trophy-case bug — `TrophyCasePuttingInterceptor` is postExecute-only, "put all in case" awards no score); dropping-class gaps already fixed in Phase 2 do NOT apply here — putting's specific container-only/item-dead gap does; throwing's target-else-item single-winner rule; attacking's weapon/instrument never consulted; going's door never consulted, dark-destination path returns before postReport hooks.
- **Deliverable**: All 7 actions wired through the engine with D3's full entity consultation (throwing: target AND item hooks both run, item-side sharedData carries the container per D3's sub-decision; putting: item-side hook now receives the container in sharedData, matching D3; attacking: weapon/instrument now consulted, postExecute/postReport unconditional across combatant/non-combatant branches via an explicit descriptor special-contract flag — not a comment — for the postExecute-replaces-combat rule; going: door + source + destination all consulted, both postReport hooks run before the dark-destination early return). Putting's multi-object path now runs the full per-item lifecycle (D4), fixing the trophy-case bug structurally. Throwing's capability-path ordering fixed per D7.2 (capability behavior first, then interceptor postExecute/postReport).
- **Exit state**: All 14 wired actions now run through the shared engine (zero hand-rolled lifecycle code remains anywhere in stdlib); the trophy-case "put all in case" scoring bug is fixed and covered by a transcript; `pnpm --filter '@sharpee/stdlib' test` green; affected transcripts pass via `node dist/cli/sharpee.js --test` including a walkthrough chain run (`--chain`) per the one-good-run rule; going/attacking/throwing observable-ordering changes have transcript coverage per ADR-228's Consequences section.
- **Status**: COMPLETE (2026-07-16, session 3ed9fb) — trophy-case fix proven by new `tests/transcripts/trophy-case-put-all.transcript` (put all in case → score 7 = egg 5 + canary 2); stdlib 1371 green (one test updated: throwing's single-winner pin → both-fire per D3-B); walkthrough chain 869/869 first run; engine gained a `seedData(ctx, entity, multiObjectItem?)` third param so shared slots (trophy case) seed per-item context in D4 loops. Ordering notes recorded: going's destination-entry hooks now precede standard door checks (canonical D3 chain); throwing declares item-before-target per the published D3-B order.

### Phase 4: Declare descriptors for the 19 unwired actions — high-plausibility set
- **Tier**: Medium
- **Budget**: 250
- **Domain focus**: drinking, pulling, touching, switching_off, searching, unlocking, locking, wearing, taking_off — the audit's VERY HIGH/HIGH author-plausibility unwired actions (the eating.ts pattern is the precedent to follow, per decisions.md D5 recommendation-context).
- **Entry state**: Phase 3 complete; the engine is proven across 14 migrated actions; these 9 actions have zero interceptor consultation today — any Chord `on drinking it` etc. registers and is silently dead per the audit's Chord seam.
- **Deliverable**: A declarative descriptor for each of the 9 actions (no hand-rolled lifecycle — descriptor-only, since the engine already exists); switching_off wired to close the switching_on/switching_off asymmetry named in the audit; locking/unlocking wired using LockableBehavior alongside the already-wired opening/closing; wearing/taking_off wired, folding taking_off's existing ad-hoc trait-flag refusals (taking-off.ts:122-128) into the new interceptor path where applicable rather than leaving both mechanisms live.
- **Exit state**: 9 of the 19 previously-dead actions are wired; `pnpm --filter '@sharpee/stdlib' test` green; new pinning/unit tests exist for at least one representative interceptor per newly-wired action (mirroring eating-interceptor.test.ts); `./repokit build dungeo` succeeds; no Dungeo transcripts currently depend on these actions being unwired (verify via grep before declaring done).
- **Status**: COMPLETE (2026-07-16, session 3ed9fb) — 9 actions wired by 3 parallel agents, 36 new interceptor pinning tests; grep confirmed no story registers interceptors under the nine ids (thealderman's if.action.searching hit is a grammar alias only); stdlib 1415 green; walkthrough chain 876/876. Also landed this pass: the 5 Phase-3 mutation-verification test gaps (putting multi-object container hooks via real action, seedData 3-arg pin, going dark-path postReport, attacking postExecuteReplacesCore contract + weapon slot, throwing capability-then-hooks order). taking_off's execute-phase refusal folding flagged as an OPEN question for David (wired around, not restructured); wearing/taking_off both-mechanisms note carried there.

### Phase 5: Declare descriptors for the remaining unwired actions + D6 delegation seams + talking live-bug fix
- **Tier**: Medium
- **Budget**: 250
- **Domain focus**: climbing, giving, showing, talking, removing, inserting, smelling, listening, hiding, exiting — the remaining 10 unwired actions, plus D6's delegation-seam ruling (removing consults removing+taking; inserting consults inserting then putting) and the dead `TrollTalkingInterceptor` registration (talking wired closes this live Dungeo bug).
- **Entry state**: Phase 4 complete; 9 of 19 unwired actions now declared; the remaining 10 include design-decision cases the audit flagged (exiting's affected entity is implicit — current container, never a parsed object; giving's item-side is fully dead with no validate-phase block possible today; smelling has no clean seam at all — audit notes MED-HIGH plausibility but "no interceptor AND no trait-driven custom scent").
- **Deliverable**: Descriptors for all 10 remaining actions, including exiting's implicit current-container entity (per Phase 1's implicit-entity resolver mechanism, same pattern as going's door/source/destination). Removing's descriptor consults `if.action.removing` AND `if.action.taking` on the item per D6-B (specific id first), closing the TrollAxe REMOVE-FROM bypass named in the audit. Inserting's descriptor consults `if.action.inserting` then delegates into putting's hooks per D6-B. Talking wired through the engine, reactivating the existing `TrollTalkingInterceptor` registration (troll "can't hear you" MDL-canon message when incapacitated) — verify this fixes the dead-registration bug with a new talk-to-troll transcript (audit notes none exists today). Giving's item-side descriptor declared even though no validate-phase block is possible yet (document the limitation rather than silently omitting the item side).
- **Exit state**: All 19 previously-unwired actions are now declared and wired (33/33 entity-keyed standard actions covered per ADR-228's Consequences); the troll-talking live bug is fixed and covered by a new transcript; D6's both-ids-fire rule is documented for authors (a trait registers under exactly one id to avoid double-mutation) somewhere authors will find it (stdlib CLAUDE.md or a code comment on the descriptor type); `pnpm --filter '@sharpee/stdlib' test` green; relevant transcripts pass via `node dist/cli/sharpee.js --test`; walkthrough chain run once per one-good-run.
- **Status**: COMPLETE (2026-07-16, session 3ed9fb) — 33/33 covered. D6-B: removing consults removing+taking (TrollAxe REMOVE-FROM bypass pinned closed by removing-interceptor.test.ts); inserting consults inserting-id then delegates into putting's hooks (order pinned). exiting resolves its implicit current-container. Both-ids rule documented in stdlib CLAUDE.md + descriptor docs. Troll-talking fixed end-to-end: new `tests/transcripts/troll-talking.transcript` (8/8) — required a Dungeo grammar addition (`talk to :target`/`speak to :target` → if.action.talking) because CORE GRAMMAR HAS NO TALK PATTERN (talking was player-unreachable in any story without custom grammar — flagged to David as a parser-en-us platform question). Dungeo's bespoke talk_to_troll story action now redundant for its 3 literal phrasings (flagged, not removed). stdlib 1455 green (99 files); walkthrough chain 885/885.

### Phase 6: D5 registry export + Chord loader fail-fast
- **Tier**: Medium
- **Budget**: 250
- **Domain focus**: stdlib exports the authoritative set of interceptor-consulting action ids, derived mechanically from the descriptor table built in Phases 1-5 (never hand-maintained); the Chord story-loader (`packages/story-loader/src/runtime.ts`, routing decided in `packages/chord/src/analyzer.ts:1055`) rejects unknown gerunds and lowering/raising at load time.
- **Entry state**: Phases 1-5 complete; all 33 entity-keyed standard actions have descriptors; nothing today validates that a Chord `on <gerund> it` clause names a stdlib action that actually consults interceptors — typos and constants-with-no-action (tasting, kissing, waving, turning, setting, emptying, consulting, jumping) register and silently die; lowering/raising accept dead registrations despite being full-delegation capability actions by design.
- **Deliverable**: A stdlib export (mechanically derived from the descriptor table — the descriptor table IS the source of truth, per ADR-228 D5) listing every wired action id. Story-loader diagnostic at load time: reject `on <gerund> it` for gerunds not in the exported set, with a load-time diagnostic (mirroring the existing role-clause "not wired yet" precedent named in decisions.md D5); reject lowering/raising specifically with a pointed message directing authors to a capability behavior / Chord dispatch action instead of a generic "unknown gerund" error.
- **Exit state**: A typo'd or unimplemented gerund in a `.story` file produces a load-time diagnostic instead of a silent no-op; lowering/raising produce the pointed capability-dispatch message; existing valid Chord stories (examining/reading, feeding/petting per the audit's "current Chord exposure is safe" note, plus every story exercising the newly-wired actions from Phases 4-5) still load and pass their transcripts; `pnpm --filter '@sharpee/chord' test` and story-loader's test suite green; `./repokit build dungeo` succeeds.
- **Status**: PENDING

### Phase 7: D8 — Chord `while`-gate lowering fix
- **Tier**: Medium
- **Budget**: 250
- **Domain focus**: `packages/story-loader/src/runtime.ts` — three lowering sites (`buildInterceptor`, `buildTraitInterceptor`, `buildCapabilityBehavior`) where `clause.condition` (the `while` gate) is evaluated too late or not at all, letting gated refusals fire when the gate is false.
- **Entry state**: Phases 1-6 complete (this fix is independent of the stdlib wiring program but lands in the same session plan because 100% coverage makes `while`-gated refusal clauses reachable on the 19 newly-wired actions per decisions.md D8's "interaction with the coverage program" note). Today: `buildInterceptor`'s preValidate runs `findRefusal` before `clause.condition` is checked (leaks gated refusals); `buildTraitInterceptor` never evaluates `clause.condition` at all (refusals, mutations, AND reports fire unconditionally) and never checks `clause.once`; `buildCapabilityBehavior` has the same two gaps as the trait-interceptor route.
- **Deliverable**: At all three sites, evaluate `clause.condition` at the top of preValidate/validate, before `findRefusal` runs. Set/honor `chordSkip` and `clause.once` uniformly across entity and trait paths (today only the entity path checks `once`, at runtime.ts:348). Gate evaluation point per D8's ruling: once per firing, at validate time (double-evaluation in preValidate and postValidate within one action is safe since no mutation occurs between them — document this in a code comment at each site so save/restore and future phases can't drift from it).
- **Exit state**: Pinning tests exist for: while-false + leading-refusal on both routes (entity clause and trait clause); trait-clause `, once` honored (occurrence counter behaves identically to the entity path); a regression run of the Cloak-of-darkness stumble suite (the only existing runtime `while`-gate test, `story-loader runtime.test.ts:89`) still passes. `pnpm --filter '@sharpee/story-loader' test` green; `node dist/cli/sharpee.js --test` against cloak-of-darkness transcripts passes; `./repokit build dungeo` succeeds (confirms no cross-story regression from the shared runtime.ts change).
- **Status**: PENDING
