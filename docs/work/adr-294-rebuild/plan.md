# Session Plan: ADR-294 transcript-tester ground-up rebuild (ACs 1-9 core)

**Created**: 2026-08-01
**Overall scope**: Rebuild `packages/transcript-tester` in place on the golden-transcript model (ADR-294 D1-D9, D10, D14, D15, D18): whole-session record/diff/bless replacing the nondeterminism-coping machinery (navigator retries, `WHILE`/`ENSURES`, `[OK: any]`), now that ADR-293 Phases A+B make output byte-identical at a pinned seed. Scope is bounded to Acceptance Criteria 1-9 (the core rebuild); ACs 10-14 are gated on ADR-293 Phase C (`materialize`/trace) and are an explicit follow-on arc, not part of this plan.
**Bounded contexts touched**: N/A — infrastructure/tooling (a CLI testing tool rebuild; no `docs/ddd/notation.yaml` exists in this repo and none of this work introduces domain modeling)
**Key domain language**: golden (a recorded whole-session baseline), bless (the reviewed act of overwriting a recording), provenance (seed/derivation/save-format/channels/events/locale/forces recorded with a golden), assertion tier (the retained per-command DSL for unit intent), divergence save (the real save written at the last matching turn on a failed replay) — ADR-294's own vocabulary, not a DDD model

## References consulted
- `docs/architecture/adrs/adr-294-golden-transcripts-tester-rebuild.md` — the spec this plan implements: D1-D9/D10/D14/D15/D18 are in scope, D11-D13/D16/D17/D19/D20 and ACs 10-14 are explicitly out of scope (gated on ADR-293 Phase C), D9 requires the rebuild in place under the existing package name with no compatibility layer.
- `docs/architecture/adrs/adr-293-choice-points-per-point-streams.md` — parent determinism substrate this rebuild depends on: D7 stream states ride the save behind a *versioned* reader (no hard break — the same discipline applies to `.golden` provenance mismatches, D3), D13 the acceptance bar is byte-identical rendered text, D14 the seed is always injectable/reported/silent to players. Phases A and B are complete (commits through `1bc77944`); this plan is the D12 arc's step 3.
- `docs/architecture/adrs/adr-277-ide-integrated-testing.md` — kept per ADR-294 D9: the NDJSON wire contract (`sharpee test --json`), top-level `walkthroughs/` directory layout with filename-order chaining, and `.transcript` as an editable document type. New CLI surface (`--bless`, `--watch`) must not break this contract.
- `docs/architecture/adrs/adr-287-transcript-fenced-payloads.md` — kept in the assertion tier per ADR-294 D2/D9: the literal `text` / `end text` fenced-payload grammar, additive to the existing `.transcript` grammar, one grammar shared by the tester and the IDE. Phase 1's parser rewrite must preserve this form untouched.
- `docs/architecture/adrs/adr-282-play-to-test-tagging.md` — the existing "bless" concept (per-turn tag, round-trip is the invariant) that ADR-294 D1 generalizes to whole-session. This plan's `--bless` should read as the natural extension of that verb, not a new one.
- `docs/architecture/adrs/adr-290-test-creation-as-an-atomic-mode.md` — status DRAFT, "do not implement." Its Amendment A1 already retargets its future output artifact to ADR-294 goldens; this plan does not implement ADR-290's IDE flow, but Phase 2's `.golden` format is the artifact ADR-290 will eventually produce, so its shape should not be revisited casually later.
- `docs/architecture/adrs/adr-187-devkit-author-only-split-inrepo-build.md` — two build CLIs split by audience: `./repokit` (platform, owns build/bundle/verify/test:npm) is where transcript testing lives; `./sharpee`/devkit is the author tool and is not in scope for new CLI surface. New flags belong in `scripts/bundle-entry.js` and `packages/transcript-tester/src/cli.ts`, not devkit.
- `docs/context/session-20260801-1122-adr-293-phase-a-arc.md` — most recent session. Confirms ADR-293 Phase A (PR #205 merged) and Phase B (commit `1bc77944`, walkthrough chain byte-identical at seed 42, 944 passed / 0 failures) both complete — this plan's entry condition. Carried open items (post-293 coverage walkthrough, `IDebugEvent` dead tier, `ring-action.ts:141` unfiled bug, publish dry-run 4.3.0 state) are background noise, not blockers for this plan. Also records the exact rebuild motivations this plan addresses: directive errors swallowed without `--stop-on-failure`, `[OK: any]` masking a stuck-player failure, `[SEED:]` silently ignored before the `---` separator.
- `docs/context/project-profile.md` — Vitest 3.x is the test framework; TypeScript strict mode (`strict`, `noImplicitAny`, `noImplicitReturns`) applies to all new code; `@sharpee/transcript-tester` is a publishable npm package, so `tsf build --npm` regression is part of "complete" for any phase that changes its public surface (`src/index.ts` exports).

## Platform touch-points outside `packages/transcript-tester` (flagged per CLAUDE.md's platform-discussion gate — already satisfied by ADR-294's acceptance, but called out per phase below since each is a concrete file, not just the package)
- `scripts/bundle-entry.js` — currently `require('../packages/transcript-tester/dist/index.js')` and calls `transcriptTester.runTranscript(game, ...)` directly; owns its own CLI flag parsing (`--seed`, `--restore`, `--chain`, `--test`, `--exec`, `--play`) independent of the package's own `src/cli.ts`. New flags (`--bless`, `--watch`) need parsing and threading here, not just in the package.
- `tools/repokit` — no dedicated transcript-run command exists today (`./repokit` owns build/bundle/verify/test:npm/clean; transcript testing is invoked directly via `node dist/cli/sharpee.js --test`). Expected impact for ACs 1-9 is minimal-to-none; each phase below states explicitly whether `tools/repokit` needs a change or is confirmed untouched.
- `packages/engine` — expected untouched. Divergence saves (Phase 3, D18) and provenance's save-format field (Phase 2, D3) both consume the existing `engine.save()` / `engine.restore()` contract that ADR-293 Phase A's fixup already wired through `ISaveRestoreHooks` (commit history: `runner.ts`'s `$save`/`$restore`, `bundle-entry.js`'s `restoreNamedSave()`). No new engine capability is anticipated; if one is discovered mid-phase, stop and flag it per CLAUDE.md's platform-discussion rule before implementing.

## Phases

### Phase 1: Transcript grammar rewrite — header provenance, removed-form parse errors, `.golden` format **[COMPLETE — 2026-08-01, session e30007]**
- **Evidence**: `pnpm exec vitest run` in `packages/transcript-tester` — 118 passing (63 before; new suites: golden-format 18, header-config 21, removed-forms 14; seed-directive/ok-any/fenced-payloads rewritten for the new grammar). `tsc --noEmit` clean. `git status`: change surface is `packages/transcript-tester` + docs only — no `scripts/bundle-entry.js`, no `tools/repokit`, `navigator.ts`/`condition-evaluator.ts` untouched. Scope note: the retry-family forms (`[RETRY:]`, `[END RETRY]`, `[DO]`, `[UNTIL]`, `[END WHILE]`) were also removed alongside the plan's named five — D4/D9 delete the retry machinery wholesale, and loop forms parseable while `[WHILE:]` errors would be incoherent.
- **Tier**: Medium
- **Budget**: 250
- **Domain focus**: `.transcript` source parser (`packages/transcript-tester/src/parser.ts`, 726 lines today) and `packages/transcript-tester/src/types.ts` (329 lines); new `.golden` format reader/writer.
- **Entry state**: ADR-293 Phase B complete (confirmed — walkthrough chain byte-identical at seed 42, commit `1bc77944`). Existing 63 unit tests in `packages/transcript-tester/tests/` pass (`pnpm --filter '@sharpee/transcript-tester' test`).
- **Deliverable**:
  - Header grammar: `seed:`, `seeds:` (D8), `channels:` (D15, default `main`), `events:` (D6, default `false`), `locale:` (D19 hook, default the story's primary), `forces:` (D13 hook, parsed but not yet acted on — `(none)` today) alongside existing `title:`/`story:`.
  - `[SEED:]` body-positional directive removed; a transcript using it is a parse error naming the form and its replacement (the `seed:` header field) — AC-4.
  - `[WHILE:]`, `[ENSURES:]`, `[NAVIGATE TO:]`, `[OK: any]` removed from the grammar; each produces a parse error naming the removed form and its replacement (AC-4). ADR-287's `text`/`end text` fenced-payload grammar is preserved untouched (regression: existing `fenced-payloads.test.ts` and `seed-directive.test.ts` still pass, rewritten for the new header seed placement where they assert the old positional form).
  - New `.golden` format reader/writer implementing ADR-294's exact block (`# sharpee golden v1` header, `key: value` provenance lines, `---` separator, `> command` + verbatim output per turn, optional `• type {json}` event lines when `events: true`).
  - `types.ts` gains the provenance shape (seed, `SEED_DERIVATION_VERSION`, save-format version, story, channels, events, locale, forces) as a typed structure, not loose strings.
- **Exit state**: Parser and golden-format round-trip (write then read reproduces the same structure) covered by unit tests. Every removed form has a dedicated test asserting the exact parse-error message names the form and its replacement. The rewritten `types.ts` preserves the per-command status and line-number data the ADR-277 NDJSON wire contract (`sharpee test --json`) consumes — verified at phase exit by running the existing NDJSON emitter path against the new types (ADR-277's requirement that this data comes out of the runner/parser as data, not scraping). `navigator.ts` and `condition-evaluator.ts` are NOT yet deleted (their callers in `runner.ts` are removed in Phase 2) — Phase 1 only stops the *parser* from producing directives those modules consume. `pnpm --filter '@sharpee/transcript-tester' test` green. No change to `scripts/bundle-entry.js` or `tools/repokit` in this phase (parser-only; confirm by diff review at phase end).

### Phase 2: Core golden engine — record, diff, `--bless`, provenance staleness, unconditional failure **[COMPLETE — 2026-08-01, session e30007]**
- **Evidence**: package suite 139 passing (`pnpm exec vitest run`; golden-runner 16 new); full `npx tsf build` + `./repokit build dungeo` green (bundle 3455296 bytes — smaller after deletions); fixture AC pass through the shipped bundle vs dungeo at seed 42: AC-1 bless + two separate-process replays exit 0; AC-2 tampered line → exactly that diff, `--bless` repairs, file diff = the one line; AC-3 derivation bump → "stale recording — re-bless … derivation recorded 2, runtime 1" exit 1; AC-4 `[WHILE:]` → parse error naming replacement, nothing executes; AC-5 `$restore ghost` → named failure exit 1 without `--stop-on-failure`. `navigator.ts`/`condition-evaluator.ts` deleted (David confirmed in-session). Scope extensions flagged: `[IF:]`/`[END IF]`/`[REQUIRES:]` and `[OK: contains_any]`/`[OK: matches]` joined the removed set (evaluator deletion + D2's exhaustive retained list); `[GOAL:]` survives as structural label. One engine touch: `SAVE_FORMAT_VERSION` export (existing constant, D3 provenance requires it).
- **Tier**: Large
- **Budget**: 400
- **Domain focus**: `packages/transcript-tester/src/runner.ts` (1621 lines — the bulk of the rebuild), `src/reporter.ts` (451 lines), `src/cli.ts` (404 lines); deletion of `src/navigator.ts` (357 lines) and `src/condition-evaluator.ts` (390 lines) per ADR-294 D4/D9.
- **Entry state**: Phase 1 complete — new grammar and `.golden` format types/parser exist and are tested in isolation.
- **Deliverable**:
  - Golden-tier execution path: a transcript with a recording (`.golden` sibling exists) replays and diffs against it (D1); without one, `--bless`-mode creates the initial recording. Diff covers the story's rendered output only by default (D6) — excludes harness chrome (PASS/FAIL columns, durations, file paths) and the build-date banner line; nothing else is normalized.
  - `--bless` overwrites the recording (D1) — CLI flag threaded through both `packages/transcript-tester/src/cli.ts` and `scripts/bundle-entry.js` (the actual entry point per CLAUDE.md's "always use the bundle" rule).
  - Provenance stamping and mismatch detection (D3): a recording whose provenance (seed, `SEED_DERIVATION_VERSION`, save-format version, channels/events/locale/forces set) disagrees with the runtime fails with a named "stale recording — re-bless" error, never a raw content diff.
  - Unconditional failure (D5): any failed directive (`$restore` of a missing/legacy save, a rejected snapshot) fails the transcript regardless of `--stop-on-failure`; that flag only controls whether the *run* continues to other transcripts.
  - Assertion tier retained per D2: `[OK: contains "…"]`, `[OK: not contains]`, `[OK]` + text block (ADR-287), `[FAIL:]`, `[SKIP]` — all still evaluated per-command for transcripts with no recording.
  - Delete `navigator.ts` and `condition-evaluator.ts`; remove their call sites from `runner.ts` (this is a file deletion under CLAUDE.md's "never delete files without confirmation" — confirm with David before deleting, even though ADR-294 D4/D9 already mandates it as accepted platform decision).
- **Exit state**: Against a small fixture transcript (not yet the full corpus — that is Phase 5):
  - AC-1: two consecutive replays in separate `node` processes both pass with zero diffs.
  - AC-2: editing the fixture's expected room text breaks exactly the golden containing that text; `--bless` repairs it; the `.golden` diff shows exactly the text change.
  - AC-3: a hand-bumped provenance field (e.g. mismatched save-format version) fails with the named "stale recording — re-bless" error.
  - AC-4: each removed form (already covered by Phase 1's parser tests) is re-verified end-to-end — nothing executes past the parse error.
  - AC-5: a `$restore` of a missing save fails the transcript with a named error and nonzero exit, without `--stop-on-failure`.
  - `pnpm --filter '@sharpee/transcript-tester' test` green; `./repokit build dungeo` green (confirms `scripts/bundle-entry.js` wiring compiles into the bundle).

### Phase 3: Diff dimensions — seed matrices, channel scoping, divergence debugging **[COMPLETE (D8+D18) — 2026-08-01, session e30007; D15 CLOSED 2026-08-03, session 7dd736 — see closure addendum below]**

> **D15 closure (2026-08-03, session 7dd736)**: David approved the recorded proposal
> (design refined in-session: `◦ <id> <line>` capture lines in the golden, gated on
> provenance `channels:` exactly like the `events:` precedent; capability profile
> derived from each declared channel's `IOChannel.gatedBy` — the mapping already
> existed, no table needed; structured payloads as key-sorted single-line JSON;
> absence diffed; assembly/declaration mismatch is a named failure). Implemented in
> `packages/bootstrap` (`channels` option, `lastChannels` capture, gatedBy flip,
> unknown-channel rejection), `packages/transcript-tester` (serializer/parser,
> runner capture + channel-naming diff, `assembledChannels` guard), and
> `scripts/bundle-entry.js` (both test paths thread channels). Evidence: bootstrap
> 39 passing incl. real-engine gated-channel capture; tester 219 passing; **AC-8
> real-path through the bundle** — `channels: main, score` transcript blessed,
> replayed green, tampered `◦ score` line failed naming `channel 'score' diverged`
> with the D18 divergence save written; dungeo chain + unit corpus green at seed 42
> with **zero golden churn** (main-only recordings byte-identical by construction);
> `tsf build --npm` green. ADR-294's core (ACs 1–9) is now fully closed.
- **Evidence**: suite 145 passing; typecheck clean; `./repokit build dungeo` green (bundle 3458437); `tsf build --npm` green (all publishables staged). AC-7 through the bundle vs dungeo: `seeds: 42, 777` blessed `ac7.42.golden` + `ac7.777.golden` (one run per seed, `Seed: N (seeds:)`); tampering 777's recording failed only 777's replay — 42 stayed green. AC-6: tampered `read leaflet` turn → divergence at that turn, `ac.divergence.json` written; `--exec "read leaflet" --restore <path> --seed 42` landed one command before the divergence (leaflet in hand) and reproduced the turn **1114 bytes identical**. Green replays clear stale divergence saves; `*.divergence.json` gitignored; `--restore` accepts paths. **D15 (channel scoping) paused per this plan's flag rule**: capturing non-main channels needs a platform surface — the CLI capability profile disables `statusBar` etc. (bootstrap `CLI_CAPABILITIES`), so those channels never produce packets in test mode, and `LoadedGame` exposes no per-channel capture. Proposal for David: (1) `LoadedGame` gains per-turn `lastChannels` (flattened per channel id from the `channel:packet` payload); (2) golden runs assemble the game with a capability profile derived from the transcript's `channels:` declaration. Runner keeps its named "not yet supported (D15)" guard until then; AC-8 waits on this.
- **Tier**: Large
- **Budget**: 400
- **Domain focus**: extending Phase 2's diff engine along three independent axes declared in provenance (`runner.ts`, `reporter.ts`).
- **Entry state**: Phase 2 complete — `--bless`, provenance staleness, and unconditional failure verified against a fixture.
- **Deliverable**:
  - D8 seed matrices: `seeds: A, B` produces one recording per seed (`<transcript>.<seed>.golden` or equivalent sibling naming — decide and document the naming convention in `.golden` format notes); each replay diffs only against its own seed's recording.
  - D15 channel scoping: a transcript may declare `channels: main` (default), `channels: main, status`, or a non-text channel; the recording captures only the declared channels (per ADR-163, channels carry all story→UI signals) and replay diffs only those.
  - D18 divergence debugging: on a replay mismatch, the runner writes a real save (3.0.0 format — world, turn counter, stream states, via the existing `engine.save()` contract) at the last matching turn, named `<transcript>.divergence.json`, and reports the exact restore command. Divergence saves are working artifacts, never committed (confirm `.gitignore` covers them).
- **Exit state**:
  - AC-7: `seeds: 42, 777` produces two recordings; each replay diffs only against its own.
  - AC-8: a status-line-only change breaks a `channels: main, status` golden and does not break a default `channels: main` golden (both run against the same fixture story to isolate the variable).
  - AC-6: a deliberately induced divergence (e.g. an intentionally wrong expected-output edit) writes the divergence save; `--restore` of it lands one command before the divergence; replaying the divergent command reproduces the divergent output byte-for-byte.
  - `tsf build --npm` regression check run once for the package (its public surface — `src/index.ts` exports — may have grown for the new options); confirm no other publishable package regresses.

### Phase 4: Watch mode — targeted reruns, inline bless, never-unattended **[COMPLETE — 2026-08-01, session e30007]**
- **Evidence**: new `src/watch.ts` (classifyChange / BlessPolicy / runCycle / startWatch — decision logic injectable, host CLI supplies run/prompt/log); `--watch` threaded through `scripts/bundle-entry.js` (non-chain runner extracted to `runTranscriptFileFresh`, reused by initial run and reruns; story reloads are cache-purged per run via bootstrap ADR-248, so story edits take effect); `--watch --chain` is a named not-yet error (chain reruns land with the corpus migration). Suite 158 passing (13 watch tests incl. two live-fs tests, run 3× no flake); `tsf build --npm` green; `tools/repokit` untouched (git status). AC-9 real-path (headless driver, piped stdio): story dist edit → "rerunning 2 transcript(s)" (3 seed-runs — ac + ac7 matrix); golden tamper → "rerunning 1 transcript(s)" only, diff shown, **no bless prompt, nothing blessed, tampered golden untouched on disk**. Interactive-confirm path covered by unit tests (y/n sticky-all; bless rerun; self-write suppression — a real mid-cycle suppression hole was found and fixed: bless registers its golden write immediately, not after the cycle). No new mutation surface (watch only orchestrates; rule-15 audit not re-run — the writers were audited in Phases 2–3).
- **Tier**: Medium
- **Budget**: 250
- **Domain focus**: new file-watching entry point (`packages/transcript-tester/src/cli.ts` and/or a new `watch.ts`), threaded through `scripts/bundle-entry.js`'s `--watch` flag (D14).
- **Entry state**: Phase 2's `--bless` flow and Phase 3's diff dimensions both work end-to-end against a fixture.
- **Deliverable**: `--watch` re-runs only the transcripts affected by a change — a story source edit reruns that story's suites; a single `.transcript`/`.golden` edit reruns that one test. Failures present as diffs with an inline bless affordance at the CLI (`bless? [y/n/all]`). An unattended watch run (no TTY, or no response) never blesses anything — it only reports.
- **Exit state**: AC-9 — with `--watch` running against the fixture story, editing a story source file re-runs only that story's affected suites and reports diffs; confirm nothing is blessed without an explicit y/all response (test both an interactive-confirm path and a headless/no-input path that reports-only). No change anticipated to `tools/repokit` (confirm at phase end — `--watch` is a `sharpee`/bundle-level dev-loop concern, not a `repokit verify` gate, since D16's `verify` coverage gate is out of this plan's scope).

### Phase 5: Corpus migration and full acceptance pass **[COMPLETE — 2026-08-01, session e30007]**
- **Closure**: David ruled the 17 removed-form unit transcripts are triaged as a follow-on cleanup task (ADR-294 D10 — not a migration gate); they fail loudly, never silently. Story-bug filed: GH #206 (stiletto no-effect message). Other stories' corpora (cloak-of-darkness, friendly-zoo, family-zoo-tutorial, fernhill, test stories) NOT migrated — named follow-on. AC-8 + D15 channel scoping remain the one open ADR-294 core item, paused on the platform decision recorded under Phase 3.
- **Status note**: wt migration COMPLETE — all 17 members golden (952 recorded turns; chain replays green twice, byte-identical excl. timing; endgame completes: Treasury of Zork, THE END). Ground truth from a HEAD-stash old-runner run at seed 42 (944 passed / 0 failed). Story fixes en route: wt-07 thief-camp `wait` (thief departs instead of killing); wt-13 fight rewritten (his dropped stiletto makes "knife" ambiguous → take stiletto, 7-blow seed-42 kill); grail/coins moved from wt-12 takes to wt-13 loot recovery (the thief had already stolen them). ACs 1/2/3/6 re-verified on the real corpus (AC-2: 55 prose lines, nothing else, across two golden generations; AC-6: restored replay reproduced divergent prose); AC-4 = 17 removed-form unit files error loudly with exit 1; AC-5/7 stand on fixture evidence, AC-9 on Phase 4; AC-8 blocked on D15. Unit suite: 99/116 pass (1473 commands, 0 failed), 17 error on removed forms — **David's rewrite-vs-triage ruling pending**. Story-bug candidate: melee outcome message "Your attack has no effect on the nasty stiletto" mis-names the weapon as target vs the staggered thief.
- **Tier**: Large
- **Budget**: 400
- **Domain focus**: applying Phases 1-4 to the real `stories/dungeo` corpus (D10) and running the complete AC-1..AC-9 acceptance bar against it, not just fixtures.
- **Entry state**: Phases 1-4 complete and independently verified against fixture transcripts/stories.
- **Deliverable**:
  - Run each of the 17 `wt-*` walkthrough chains (`stories/dungeo/walkthroughs/wt-*.transcript`) through the rebuilt runner in record mode at seed 42 (`--chain`), review the recorded output once, `--bless`. These become golden tier (D10's policy line).
  - Audit `stories/dungeo/tests/transcripts/*.transcript` (~100 unit transcripts, assertion tier per D10 — stays assertion-tier wholesale, not promoted to golden) for use of removed forms (`WHILE`/`ENSURES`/`NAVIGATE TO`/`OK: any`/body-positional `SEED`). Each hit is either mechanically rewritten to the new header-seed / retained-DSL form, or triaged as a pre-existing known failure and left out of the blessing path per D10's explicit exclusion (its cleanup is a separate follow-on task, not a migration gate) — confirm which category each hit falls into with David before rewriting, since bulk transcript edits are exactly the kind of change CLAUDE.md's "discuss story-level changes" latitude assumes is autonomous but bulk-deletes/rewrites of test fixtures should still be reviewed given the "never delete files without confirmation" rule.
  - Full acceptance pass, evidence inline per project convention (dated, command + result):
    - AC-1: `wt-*` chain records clean at seed 42, two separate bundle processes replay with zero diffs.
    - AC-2: a deliberate room-description edit breaks exactly the goldens containing that prose; `--bless` repairs; `git diff` of the `.golden` files shows exactly the prose change.
    - AC-3 through AC-9: re-verified against the real corpus (not just the Phase 2-4 fixtures) at least once each.
  - `./repokit build dungeo` green; `./repokit verify` green (grammar → control bytes → ADR-293 D6 entropy gate → `tsf build --npm`); dungeo unit suite green (`pnpm exec vitest run`, not `pnpm --filter … test` which starts watch mode per the prior session's note).
  - Explicitly out of scope for this phase, flagged as follow-on: migrating `cloak-of-darkness`, `friendly-zoo`, `family-zoo-tutorial`, and `fernhill` transcript corpora (named in ADR-294's header as eventual scope but not exercised by this plan's fixture/acceptance work) — note their status (migrated or not) at phase end so a follow-on plan can pick them up.
- **Exit state**: All 17 `wt-*` chains are golden with committed `.golden` recordings; the dungeo unit-transcript suite's removed-form usages are resolved or explicitly triaged as known-failures (list them); AC-1 through AC-9 all pass against the real dungeo corpus with evidence inline; `./repokit verify` and `tsf build --npm` both green. This closes ADR-294's core rebuild — ACs 10-14 remain a named follow-on arc gated on ADR-293 Phase C.

## Status

Plan approved by David 2026-08-01 (session e30007). Phase 1 COMPLETE (same session); Phases 2-5 PENDING. Phase 2 is next: core golden engine (record/diff/`--bless`), including the David-confirmation gate before deleting `navigator.ts`/`condition-evaluator.ts`.
