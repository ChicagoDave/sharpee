# Session Plan: Implement ADR-344 D1–D8 — the player role-holder invariant at the install seam

**Created**: 2026-09-10
**Plan Status**: ACTIVE
**Overall scope**: Implement ADR-344 decisions D1–D8 only. D9 already shipped (`f3d3d24a3`) and is out of scope. The work adds an install-time guard (`validate-role-holder`) that refuses an unplaced, trait-less, or non-playable role holder; brings the engine test fixture and nine story-less engine tests into compliance instead of special-casing them; removes the engine's fabricated pre-install player everywhere it is constructed (bootstrap, bridge, runtime, and ~20 test call sites); removes the loader's first-declared-room placement fallback (with an ADR-289 D4 amendment); flips `ActorTrait.isPlayable`'s default to `false` and wires the loader to write it from Chord's `playable` flag (with an ADR-327 D9 pointer amendment); and records, without fixing, the two remaining unguarded install-step handoffs (`initializeWorld`, `story-initialize` — AC-7 requires they stay unfixed here). AC-1 through AC-8 are the phase gates; AC-9 is already met and is not re-verified except by inspection in Phase 5's final sweep.
**Bounded contexts touched**: N/A — infrastructure/install-pipeline and entity-trait invariant enforcement, not new domain modeling. No `docs/ddd/notation.yaml` exists in this repo and this work introduces no new domain concepts (it enforces an existing one, the player role, at a seam that didn't check it); framed in plain technical terms per the "DDD does not apply" rule, consistent with the sibling ADR-293/294 plans in `docs/work/`.
**Platform change**: every phase below touches `packages/` (engine, world-model, story-loader, bootstrap, bridge, runtime, helpers) and therefore needs David's go-ahead before implementation, per CLAUDE.md's platform-change rule. Each phase's entry state below states exactly what it touches so approval can be per-phase rather than blanket.

## References consulted
- `docs/architecture/adrs/adr-344-player-role-holder-at-the-install-seam.md` — the authority for this plan; ACCEPTED, D1–D8 unimplemented, D9 MET; nine ACs gate the eight phases below, each naming the exact command that decides it.
- `docs/architecture/adrs/adr-289-chord-routing-decided-once.md` (D4, lines 396–424) — currently keeps "the first-declared-room fallback only for a player with no placement line at all" (line 414–416); D5 strikes that clause by amendment, in the implementing commit, before AC-4 can be marked met.
- `docs/architecture/adrs/adr-327-explicit-references.md` (D9, `change the player to`, line 260) — the `playable` create-block flag's loader write was scoped here and never landed; D7 completes it and owes D9 a pointer amendment, in the implementing commit, before AC-5 can be marked met.
- `docs/architecture/adrs/adr-334-game-engine-turn-pipeline.md` (Consequences, AC-4) — sets the project's own precedent for this kind of change: verify byte-identical Dungeo walkthrough-chain and Secret Letter tree pass counts after each phase, evidence inline (command, count, date). This plan's phase-boundary gating follows that precedent.
- `docs/context/project-profile.md` — pnpm workspace, Vitest per package, strict TS; `pnpm exec turbo run test:ci` for the platform suite; transcript/tree suites are separate manual gates, matching the per-phase test commands below.
- `docs/context/session-20260910-0458-plover-fixes-20260910.md` (Open Items) — flags the same class of defect ADR-344's "third spanking" describes (un-typechecked `.template`/tutorial consumers). Not this plan's scope, but it is the reason Phase 1 explicitly excludes `tutorials/familyzoo/{v1.5.0,v2.0.0}` and `stories/_archive/thealderman-ts` from the `new GameEngine(...)` call-site sweep (version-pinned editions and an archived story — never patched piecemeal, per standing project direction) rather than silently including them.

## Evidence gathered this session (feeds phase sizing, not itself an AC)

- `grep -rln "new GameEngine(" packages stories tutorials --include="*.ts"` → **23 call sites**, wider than ADR-344's Consequences section, which describes D6's blast radius as "bounded" to `bootstrap/src/index.ts`, `setup-test-engine.ts`, and the nine story-less engine tests. All 23 were read this session; every one fabricates a throwaway `player`/`placeholder` actor at construction and either (a) discards it immediately after `installStory`, or (b) never installs a story at all (`presence-tagging.test.ts`, the two `processEvent`-unit describes aside). Three of the 23 (`tutorials/familyzoo/v1.5.0`, `tutorials/familyzoo/v2.0.0`, `stories/_archive/thealderman-ts`) still call the retired `setStory` — they are excluded from this plan (version-pinned tutorial editions and an archived story; not patched piecemeal per standing project direction), leaving **20 in scope for Phase 1**.
- The nine story-less engine test files (D3, verified against `setupTestEngine`/`createMinimalStory` usage without `installStory`): `actor-turn-plugin.test.ts`, `command-executor.test.ts`, `debug-xyzzy.test.ts`, `engine-random-service-forcing.test.ts`, `integration/adr-296-trap-scenario.test.ts`, `integration/command-history.test.ts`, `introspect-interceptors.test.ts`, `save-restore-hooks-merge.test.ts`, `unit/adr-296-transaction-stamping.test.ts`.
- The six hand-written protagonist sites for D7's opt-in: `stories/concealment-test/src/index.ts:48`, `stories/armoured/src/index.ts:109`, `stories/family-zoo-tutorial/src/index.ts:268`, `stories/channel-service-test/src/playable-story.ts` (`ensurePlayerTraits`, ~line 129), `packages/devkit/fixtures/basic-story/src/index.ts:93`, and six files under `packages/engine/tests/stories/` (`action-test-story.ts`, `completion-test-story.ts`, `complex-world-test-story.ts`, `event-test-story.ts`, `game-state-test-story.ts`, `minimal-test-story.ts`), all constructing `new ActorTrait({ isPlayer: true })` or bare `new ActorTrait()` with no `isPlayable`.

## Sequencing rationale

D1's third condition (`isPlayable`) is decorative until D7 lands (today's default is `true`, so it never fires); D7's flip makes every un-opted-in actor non-playable, which D1 would then refuse at install. Phases are ordered so the tree is green at each boundary rather than only at the end:

1. **D6+D3** first — the engine no longer accepts a constructed player anywhere, and the fixture/story-less tests comply. This is the one phase every later phase's test runs sit on top of.
2. **D1+D2+D4** next — the seam gains its guard while `isPlayable` is still trivially `true` everywhere, so the guard is exercised (placement, `ActorTrait` presence) without the flip's blast radius landing at the same time.
3. **D5** — the loader fallback removal is independent of 1–2 (different package, different invariant) and could run before or after them; it is sequenced third for narrative continuity, not a hard dependency.
4. **D7** last of the code phases — the default flip and the loader write must ship in the same commit (a flip without the write breaks every Chord protagonist; a write without the flip is a no-op), and it is safest to make once D1's guard already exists to catch any protagonist the opt-in sweep missed.
5. **AC-8 wrap-up** — full-tree verification, AC-7/D8 confirmation by inspection, evidence compilation.

## Phases

### Phase 1: The player always comes from the story (D6)
- **Tier**: Large
- **Budget**: 400
- **Domain focus**: install-pipeline / engine construction contract — `GameEngine` accepts no player at construction; `Story.createPlayer` is the only source.
- **Entry state**: ADR-344 ACCEPTED; David has approved this phase's platform touch (`packages/engine`, `packages/bootstrap`, `packages/bridge`, `packages/runtime`, plus test-only edits in `packages/story-loader/tests` and `packages/platform-browser/tests`); current suite green as a baseline (`pnpm --filter '@sharpee/engine' test` — record the baseline count before touching anything).
- **Deliverable**:
  - `packages/engine/src/game-engine.ts`: remove `player` from the constructor's `options` type (`:229`); `GameContext.player` becomes `IFEntity | undefined`; `updateScopeVocabulary` (`:1286`) returns early when unset instead of dereferencing `.id`; `switchPlayer`'s `oldPlayer` read (`:797`) treats absence as a first assignment (matches `movePlayerRoleVocabulary`'s existing `IFEntity | null | undefined` signature at `playerRole.ts:53`).
  - `packages/bootstrap/src/index.ts:274`: stop fabricating a `'player'` actor; `boot()` constructs `GameEngine` without a `player` field and relies on `installStory` alone.
  - `packages/bridge/src/bridge.ts` and `packages/runtime/src/bridge.ts`: same removal (fabricated `player`/`this.world.createEntity('player', 'actor')` immediately before construction).
  - Every other `new GameEngine(...)` call site with a fabricated or pass-through player (grep-verified list, 20 sites after excluding the three named exclusions): `stories/armoured/src/browser-entry.ts`, `stories/dungeo/src/browser-entry.ts`, `packages/story-loader/tests/helpers/boot-engine.ts` (also drop the now-unneeded `placeholder` create/remove dance), `packages/story-loader/tests/helpers/boot-turns.ts`, the seven `packages/story-loader/tests/*.test.ts` files that go through those helpers, `packages/engine/tests/unit/engine-parser.test.ts`, `packages/engine/tests/sound/engine-wiring.test.ts`, `packages/engine/tests/sound/integration.test.ts`, `packages/platform-browser/tests/capture-parity.test.ts`. `packages/engine/tests/unit/presence-tagging.test.ts` needs more than a mechanical drop: it never calls `installStory` today (a bespoke `world.createEntity('You', EntityType.ACTOR)` with no `ActorTrait`) — convert `buildEngine()` to construct a minimal temp `Story` (world built in `initializeWorld`, actor placed and returned from `createPlayer`) and call `engine.installStory(story)`, the same shape D3 asks of the nine story-less engine tests below.
  - `packages/engine/tests/test-helpers/setup-test-engine.ts`: `setupTestEngine()` stops creating a player and a room of its own (drop the `world.createEntity('You', ...)` / `world.setPlayer` / test-room block); `createMinimalStory()` builds and places its actor (with `ActorTrait`) in `initializeWorld` and returns it from `createPlayer` as a lookup, per D3.
  - The nine story-less engine test files listed in "Evidence gathered" install a generated `createMinimalStory()` (or an equivalent local temp story) via `engine.installStory(...)` instead of hand-building a player. `command-executor.test.ts:40`'s direct `story.createPlayer(world)` call outside any install is replaced by an `installStory` call.
  - **The devkit browser templates** — `packages/devkit/templates/browser/browser-entry.ts.template:96`, `chord-browser-entry.ts.template:188`, and `playground-entry.ts.template:134` all construct `GameEngine` with a `player` field. They are author-facing (scaffolded by `init-browser.ts:156`; `playground-entry` also drives the playground build via `browser-core.ts:919`) and are invisible to `tsc` — outside devkit's `include: ["src/**/*"]` and suffixed `.template`. The D9 guard added in `f3d3d24a3` (`browser-build.test.ts` → `runBuildCommand` → `npx tsc`) is what catches them now, so devkit's suite belongs in this phase's exit, not Phase 5's.
  - **The three docs that teach the constructor** — `packages/sharpee/README.md:51`, `packages/engine/README.md:63` and `:101`, and `packages/engine/docs/language-migration-guide.md` (six occurrences at `:56, :61, :74, :88, :113, :148`, on an older positional `new GameEngine(world, player, config, languageProvider)` signature that predates the options object). Left alone they document a constructor that no longer exists. Update the snippets to the post-D6 shape; the migration guide's positional examples are already historically wrong and should move to the options object at the same time.
  - **Explicitly excluded, not touched this phase**: `tutorials/familyzoo/v1.5.0/src/browser-entry.ts`, `tutorials/familyzoo/v2.0.0/src/browser-entry.ts` (version-pinned editions, tutorial drift is tracked separately as #224 — never patched piecemeal), `stories/_archive/thealderman-ts/src/browser-entry.ts` (archived). All three still call the retired `setStory`, which is a pre-existing, unrelated breakage — report it, do not fix it here.
- **Exit state**: `grep -rn "new GameEngine(" packages stories --include="*.ts"` shows no site passing a `player` field (outside the three excluded files); `EngineOptions` has no `player` member; `npx tsc --noEmit` at repo root exits 0; `pnpm --filter '@sharpee/engine' test`, `--filter '@sharpee/story-loader' test`, `--filter '@sharpee/bootstrap' test` (if it has a suite), `--filter '@sharpee/bridge' test`, `--filter '@sharpee/runtime' test`, `--filter '@sharpee/platform-browser' test` and `--filter '@sharpee/devkit' test` all pass at (or above) the pre-phase baseline count (devkit's suite is what type-checks the scaffolded browser templates); no `new GameEngine(` in the three docs or three templates still passes a `player`. `node dist/cli/sharpee.js --world-json --story stories/cloak-of-darkness/cloak.story` (after `./repokit build dungeo` or the relevant package rebuild) shows the protagonist at `a01` with no unaccounted `a02` phantom (AC-3's own check). This phase reroutes `bootstrap`, which every CLI-run story installs through — so, per ADR-334's own Consequences/AC-4 precedent for changes of this shape, also run `node dist/cli/sharpee.js --test --chain stories/dungeo/walkthroughs/wt-*.transcript` (one run, byte-identical at the pinned seed) before calling this phase done, not only at the final Phase 5 sweep. Evidence for AC-2 and AC-3 recorded inline (command, count/output, date).
- **Scope narrowed 2026-09-10**: D3 and every test-suite conversion moved to Phase 2. This phase is the engine change and its production consumers only. **Suite-green is not this phase's gate** — removing the fabricated player exposes 70 pre-D10 test files at once, and converting them is Phase 2's named deliverable. Phases 1 and 2 together restore green; neither does alone.
- **Progress (2026-09-10, session 7776c8)** — DONE except the docs/templates item. Landed: `game-engine.ts` builds `GameContext` inside `installStory` behind a private throwing getter, with `GameContext.player` left non-optional (ADR-344 D6 as amended this session — the original `IFEntity | undefined` mechanism would have forced nine turn-path readers across six files to handle a case that cannot occur); `player` removed from the constructor options; `startedAt` kept as a construction-time field so `metadata.started` still means construction. `packages/bootstrap/src/index.ts`, `packages/bridge/src/bridge.ts`, `packages/runtime/src/bridge.ts` stop fabricating a placeholder actor, and bootstrap's compensating `removeEntity` cleanup (`:316-318`) is deleted with its cause. Evidence: `./repokit build dungeo` exit 0 (2026-09-10 02:10 CDT); per-package `tsc --noEmit` for bootstrap/bridge/runtime — 0 errors each. Two findings recorded against the ADR while doing it: the root `npx tsc --noEmit` is near-vacuous (`files: []`, four project references) and consumers type-check against `dist` declarations, so `./repokit build` replaces it in AC-3 and AC-8; and D6's "every Chord story carries the stray entity" claim was wrong — bootstrap already removed it, bridge and runtime did not.
- **Completed 2026-09-10 02:50 CDT**: the three devkit browser templates (`browser-entry`, `chord-browser-entry`, `playground-entry`) stop fabricating a player and drop the now-unused `EntityType` import; `packages/sharpee/README.md:52` and `packages/engine/README.md:64, :102` show the player-free constructor. `packages/engine/docs/language-migration-guide.md` was **not** rewritten — it documents a completed, unrelated migration and both sides of its Before/After pairs use a positional constructor that predates the options object, so rewriting them would falsify the history; it carries a historical-document banner instead, naming ADR-344 D6 and pointing at the engine README for current shape.
- **Exit evidence (2026-09-10 02:50 CDT)**: `./repokit build dungeo` exit 0. The D9 guard (`pnpm --filter '@sharpee/devkit' exec vitest run src/standalone/browser-build.test.ts`) passes 2/2 — the scaffolded `browser-entry.ts` type-checks against the new constructor, which is the guard catching a second, unrelated API change. Per-package `tsc --noEmit`: bootstrap, bridge, runtime — 0 errors each. `grep -rn "new GameEngine(" packages stories --include="*.ts" --include="*.template" --include="*.md"` (excluding `node_modules`, `dist`, `_archive`, pinned tutorials): 34 sites, of which **zero production sites** still pass a `player`; the 10 that do are test files (7 story-loader, 2 engine sound, 1 platform-browser `capture-parity.test.ts:222`) and are Phase 2's deliverable by this phase's rescope, plus 6 deliberately-preserved historical samples in the migration guide.
- **Not green, by design**: 70 test files fail with `No story installed`. That is Phase 2's named work, not a regression — see the rescope note above.
- **Defect found after this phase was first closed (2026-09-10 03:10)**: `emitGameEvent` read the context through the throwing getter, but install steps emit before the context exists — every story install threw, breaking the production CLI. Declaring the phase done on `./repokit build dungeo` exit 0 was the error: a successful build says nothing about whether a story can boot. AC-3's own check (`--world-json` against `cloak.story`) would have caught it and was not run. Fixed in this phase; AC-3 now verified for real — protagonist at `a01`, id sequence contiguous (`a01, o01-o04, r01-r03`), no phantom gap, and `--exec "look"` plays a turn.
- **Status**: DONE (2026-09-10, session 7776c8 — reopened and re-closed the same session after the defect above)

### Phase 2: The story-less engine tests (D3)
- **Tier**: Small
- **Budget**: 150
- **Domain focus**: the handful of engine tests that construct an engine and use it without installing a story.
- **CORRECTION 2026-09-10 03:15** — this phase was created at Large/400 on a measurement of "70 test files across five packages, 341 failures, the unfinished half of ADR-327 D10". **That sizing was wrong, and the cause was a defect in Phase 1's own change, not pre-existing debt.** `emitGameEvent` read `this.context.currentTurn` through the throwing getter, but `emit-story-loading` and `emit-story-loaded` emit *during* `runInstallSteps`, before the context is constructed — so every story install threw, and the failure cascaded into every test that installed a story. It also broke the production CLI outright (`assembleGame → boot → installStory → Fatal error`), which `./repokit build` exit 0 could not detect because compiling is not booting. Caught by `commit-remote`'s test gate, which correctly refused to be waived; found via `@sharpee/helpers`' REAL-PATH bundle test, a package this plan had never listed as affected. Fixed by reading the backing field with a turn-1 default — the value the constructor used to supply — restoring the prior bucketing exactly.
- **Entry state**: after that fix, five of the six suites are back at baseline: story-loader 1104, bootstrap 52, platform-browser 148, devkit 177/1 skipped, helpers 13. Engine is 34 failed / 730 passed / 7 skipped across **11 files**, all one message (`No story installed`). That is the real D3 scope, and it is close to what ADR-344 D3 estimated all along ("the nine engine test files").
- **Deliverable**:
  - **Seven files that never call `installStory`** — `tests/actor-turn-plugin.test.ts`, `tests/debug-xyzzy.test.ts`, `tests/integration/adr-296-trap-scenario.test.ts`, `tests/integration/command-history.test.ts`, `tests/save-restore-hooks-merge.test.ts`, `tests/unit/adr-296-transaction-stamping.test.ts`, `tests/unit/presence-tagging.test.ts`. Each installs a generated temp story. `presence-tagging.test.ts` is the non-mechanical one: it builds a bespoke `world.createEntity('You', EntityType.ACTOR)` with no `ActorTrait`.
  - **Four files that install a story but have individual tests touching the engine first** — `tests/game-engine.test.ts` (25 installs), `tests/integration.test.ts` (12), `tests/unit/story-install-order.test.ts` (11), `tests/unit/engine-failure-reports.test.ts` (1). Find the specific cases, not a blanket edit.
  - `setup-test-engine.ts` per D3: `setupTestEngine()` stops creating a player and a room; `createMinimalStory()` builds and places its actor in `initializeWorld` and returns it from `createPlayer` as a lookup.
- **Exit state**: engine suite at or above its 764 passed / 7 skipped baseline; the other five suites unchanged; `./repokit build dungeo` exit 0; `node dist/cli/sharpee.js --exec "look" --story stories/cloak-of-darkness/cloak.story` boots and plays; one Dungeo walkthrough-chain run byte-identical at the pinned seed. Evidence inline.
- **Progress (2026-09-10 03:45, session 7776c8)** — engine 34 failed → **15 failed / 749 passed / 7 skipped** across 6 files. Landed: `setupTestEngine()` no longer creates a player (D3); new `setupTestEngineWithStory()` installs `createMinimalStory()` and returns the story's player; `createMinimalStory(options)` builds a room, a placed actor **with an `ActorTrait`**, and optional test objects in `initializeWorld`, returning that actor from `createPlayer` as a closure lookup; nine story-less files switched to the installing helper. **D6a added on David's ruling — `start()` requires a story** (`Cannot start: no story installed — call installStory() first.`), and `game-engine.test.ts:208`'s "should start without a story" became a rejection test.
  - **One self-inflicted detour worth recording**: switching the helper first produced 228 failures, not fewer. Cause was a single stale line — `setup-test-engine.ts:64` still passed `player` to `new GameEngine({...})` after the variable above it was deleted, a `ReferenceError` every one of the helper's 37 consumers inherited. It was a Phase 1 D6 site missed because that sweep excluded test files. Removing it took 228 → 16.
  - **Remaining 15, per-site not sweepable.** Every file except `presence-tagging` already installs stories, so a blanket helper switch would double-install; each failure is a specific call site reaching the engine before installing. `presence-tagging.test.ts` (5, constructs directly, 0 installs), `story-install-order.test.ts` (3, 11 installs), `engine-failure-reports.test.ts` (2, 1 install), `engine-resume.test.ts` (2, 3), `player-death-routing.test.ts` (2, 2), `integration.test.ts` (1, 12). Causes: 6× `Cannot start: no story installed`, 4× `No story installed` (context), 4× `Cannot read properties of undefined (reading 'id')`, 1× `expected undefined to be defined`. Several likely turn on D6a and want checking rather than rewriting.
- **Status**: CURRENT (since 2026-09-10) — partially complete, 15 failures remain

### Phase 3: The install seam validates the role holder (D1, D2, D4)
- **Tier**: Medium
- **Budget**: 250
- **Domain focus**: install-pipeline invariant — the player role holder must be located, actor-trait'd, and playable before the engine commits to it.
- **Entry state**: Phase 1 landed and green (engine constructs with no player; fixtures and story-less tests comply). David has approved this phase's platform touch (`packages/engine/src/install/`).
- **Deliverable**:
  - New step `validate-role-holder` in `packages/engine/src/install/`, modeled on `validate-room-snippets.ts`'s shape (a pure function plus an `InstallStep` wrapper), `requires: ['create-player']`, inserted into `STORY_INSTALL_STEPS` (`packages/engine/src/install/steps.ts`) between `createPlayerStep` and `listenerTraitStep`.
  - The step throws (does not return a diagnostic) naming the entity and the failed condition, checking in order: (1) `world.getLocation(player.id)` is defined — "nowhere to play" wording, no fallback placement; (2) the entity carries an `ActorTrait`; (3) that trait's `isPlayable` is `true`. Per project convention, the ADR number goes in a code comment above the throw, never in the message text.
  - `packages/engine/src/install/story.ts`: `Story.createPlayer`'s doc comment states all three conditions the returned entity must satisfy (D2), restoring in contract form the half of the pre-refactor comment ADR-344 found dropped.
  - Tests (D4): one rejection test per condition (unplaced, no `ActorTrait`, not playable) plus one positive test asserting a compliant story's installed holder is placed, actor-trait'd, and playable. Each test must fail if the step is absent, reordered out of `create-player`→`validate-role-holder`→`listener-trait` position, or drops a condition (AC-1's self-verifying bar).
- **Exit state**: `pnpm --filter '@sharpee/engine' test` green including the four new tests; `STORY_INSTALL_STEPS`'s order test (`unit/story-install-order.test.ts`) updated to include the new step name in position. Evidence for AC-1 and AC-6 recorded inline (AC-6 is PREMISE-DEPENDENT on AC-1 landing in this same phase, so read the doc comment after the step exists, not before).
- **Status**: PENDING

### Phase 4: One placement rule (D5, ADR-289 D4 amendment)
- **Tier**: Medium
- **Budget**: 250
- **Domain focus**: Chord loader — the role holder places the same way every other entity does; no seam-specific fallback.
- **Entry state**: David has approved this phase's platform touch (`packages/story-loader/src/loader.ts`) and the ADR-289 amendment. No hard dependency on Phases 1–2, but sequenced third for narrative continuity.
- **Deliverable**:
  - Re-run the measurement before touching code: grep `packages/chord/tests/fixtures/`, `stories/`, and `branch-stories/` for `change the player to` and confirm the 85/64/19/0 split (85 sources, 64 explicit placement, 19 relying on the fallback, 0 shipped stories) still holds at the commit in hand; record the re-measurement inline with date. If the split has moved, stop and report before proceeding — this is a PREMISE-DEPENDENT acceptance criterion (AC-4).
  - Remove `finalizeRoleHolder`'s first-declared-room fallback (`packages/story-loader/src/loader.ts:775-782` at the time of reading; confirm current line numbers before editing).
  - Add an explicit placement line (`starts in <room>` or equivalent) to each of the 19 `packages/chord/tests/fixtures/` files the re-measurement confirms still relies on the fallback. **Post the list of files to be edited and wait for confirmation before editing any of them**, per CLAUDE.md's "never delete without confirmation" spirit extended to this bulk, ADR-driven edit — these are fixture files whose content is changing, not being deleted, but the list-and-wait discipline applies to any bulk edit David hasn't seen file-by-file.
  - `docs/architecture/adrs/adr-289-chord-routing-decided-once.md` D4: strike the clause "keeping the first-declared-room fallback only for a player with no placement line at all" (currently lines 414–416), leaving the rest of D4 (the `in`/`on`/`starts in` unification) untouched. Written in the same commit as the loader change. ADR-289's Status stays ACCEPTED — this is an amendment, not a supersession.
  - A new refusal test: a Chord story whose protagonist has no placement line fails installation (via Phase 2's `validate-role-holder` step, condition 1).
- **Exit state**: `pnpm --filter '@sharpee/story-loader' test` green; `./sharpee test branch-stories/fernhill` passes; `node dist/cli/sharpee.js --test --chain stories/dungeo/walkthroughs/wt-*.transcript` runs byte-identical at its pinned seed (single run, per project convention). Evidence for AC-4 recorded inline (re-measurement command + counts + date; test run + date; walkthrough chain command + pass count + date).
- **Status**: PENDING

### Phase 5: `playable` reaches the runtime (D7, ADR-327 D9 amendment)
- **Tier**: Large
- **Budget**: 400
- **Domain focus**: the player role's eligibility — `ActorTrait.isPlayable` becomes opt-in, matching Chord's own `playable` semantics, and the loader is the one place that writes it.
- **Entry state**: Phases 1–2 landed (the seam guard exists and constructs no player itself), so a protagonist the opt-in sweep misses fails loudly at install rather than silently misbehaving at `switchPlayer`. David has approved this phase's platform touch (`packages/world-model`, `packages/story-loader`, `packages/helpers`) and the six story/fixture edits. **The default flip and the loader write ship in the same commit** — flipping without the write breaks every Chord protagonist; writing without the flip is a no-op (the "half-measure to avoid" the ADR names explicitly).
- **Deliverable**:
  - `packages/world-model/src/traits/actor/actorTrait.ts:154`: `isPlayable`'s class default changes `true` → `false`.
  - `packages/story-loader/src/loader.ts` (the `'person'` case, currently ~`:1633-1634`): immediately after `entity.add(new ActorTrait())`, write `isPlayable` from `irEntity.isPlayable`.
  - `packages/helpers/src/builders/actor.ts`: add a `.playable()` method (a `_playable` flag defaulting `false`, applied to the `ActorTrait` construction in `build()`). It does **not** default to `true` for its own construction — an author writes `actor('yourself').playable().build()` explicitly.
  - Opt-in each of the six hand-written protagonist sites so they set `isPlayable: true` (directly on `new ActorTrait({...})` where the story hand-builds the trait, or via `.playable()` wherever a site is migrated to the builder — no site is required to migrate to the builder as part of this phase, only to stay playable):
    - `stories/concealment-test/src/index.ts:48`
    - `stories/armoured/src/index.ts:109`
    - `stories/family-zoo-tutorial/src/index.ts:268`
    - `stories/channel-service-test/src/playable-story.ts` (`ensurePlayerTraits`)
    - `packages/devkit/fixtures/basic-story/src/index.ts:93`
    - all six files under `packages/engine/tests/stories/` (`action-test-story.ts`, `completion-test-story.ts`, `complex-world-test-story.ts`, `event-test-story.ts`, `game-state-test-story.ts`, `minimal-test-story.ts`)
  - `docs/architecture/adrs/adr-327-explicit-references.md` D9: a pointer amendment recording that the loader's `isPlayable` write was completed here (ADR-344 D7), in the same commit as the code. ADR-327's Status stays ACCEPTED.
  - Tests: a world-model test asserting the new default; a story-loader test asserting the written flag for both `playable` and absent-`playable` Chord characters; an engine test driving `switchPlayer` both ways (playable succeeds, non-playable throws the existing guard at `game-engine.ts:793`); a helpers test that `actor(...).build()` is non-playable while `actor(...).playable().build()` is playable.
- **Exit state**: green suites for `pnpm --filter '@sharpee/world-model' test`, `--filter '@sharpee/story-loader' test`, `--filter '@sharpee/engine' test`, `--filter '@sharpee/helpers' test`, and the six hand-written stories/fixtures (build + relevant story test/transcript for each). `node dist/cli/sharpee.js --test --chain stories/dungeo/walkthroughs/wt-*.transcript` byte-identical at its pinned seed (single run). Any Chord story beyond Dungeo with a `playable` protagonist (at minimum `stories/cloak-of-darkness`, `branch-stories/fernhill`) installs and switches correctly. Evidence for AC-5 recorded inline.
- **Status**: PENDING

### Phase 6: Tree green end-to-end, D8/AC-7 confirmed, evidence compiled
- **Tier**: Small
- **Budget**: 100
- **Domain focus**: closure — no new code, verification and record-keeping only.
- **Entry state**: Phases 1–4 landed and each individually green.
- **Deliverable**:
  - `npx tsc --noEmit` at repo root, full run.
  - `pnpm --filter '@sharpee/engine' test`, `--filter '@sharpee/story-loader' test`, `--filter '@sharpee/world-model' test`, `--filter '@sharpee/helpers' test`, `--filter '@sharpee/devkit' test` (AC-8's named suites) run together as a final confirmation, not just per-phase.
  - `node dist/cli/sharpee.js --test --chain stories/dungeo/walkthroughs/wt-*.transcript` — one run, byte-identical at the pinned seed (do not re-run to check for flakiness, per project convention).
  - AC-7/D8 confirmed by reading `STORY_INSTALL_STEPS`: exactly one step (`validate-role-holder`) added by this plan; `initializeWorld` and `story-initialize` remain unguarded, matching D8's recording. No code change for this bullet.
  - Compile the nine-AC evidence table (AC-1 through AC-9, AC-9 already met) into the ADR itself or a closing note, each line naming the command run, what it returned, and the date — per the project's "evidence inline, never cited" convention.
- **Exit state**: all eight ACs (AC-1–AC-8) have inline evidence; AC-9 reconfirmed by inspection only. Plan phases all marked DONE.
- **Status**: PENDING

## Notes for the implementing session

- **Never auto-retry a failed build or test.** If any phase's verification command fails, report the failure and stop — do not loop fix→rebuild without David's instruction.
- **Never delete a file without confirmation.** Phase 3's fixture edits are additive (a placement line), not deletions, but post the file list and wait before editing regardless, since it is a 19-file bulk change David has not seen individually.
- **No CI gates.** All verification above is local commands run by the implementing session, not a new CI step.
- Line numbers cited above (`game-engine.ts:229/797/1286/793`, `loader.ts:775-782/1634`, `actorTrait.ts:154`, story file line numbers) were read at commit `87f06800c` / this session's `HEAD`; confirm they still hold before editing, since earlier phases in this same plan shift some of them (e.g., Phase 1's edits to `game-engine.ts` may shift Phase 4's line references).
