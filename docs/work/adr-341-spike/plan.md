# Session Plan: ADR-341 D2 spike phase — WPF vs WinUI 3 for Chord Writer on Windows

**Created**: 2026-09-08
**Plan Status**: DONE (2026-09-12, session `89f9e0`) — all six phases closed. ADR-341 D2 is resolved on evidence: **the toolkit is WPF**, David's tie-break after both toolkits passed their spikes. `decision.md` carries the record, ADR-341 D2 and ADR-154's deferral bullet are both amended, and D8's order moves to its next step (ADR-182 in the Swift app) as its own plan.
**Superseded by**: ~~`docs/work/archive/refactoring-survey/plan.md`~~ (2026-09-08, session 4a2d5f — rule 18b "still live": David, "okay until then, open a refactor branch and go ahead with the refactor work"; this plan stays exactly where it is, Phase 0 CURRENT with the machine arriving 2026-09-11 and Phase 1 DONE, resumable when `.current-plan` returns here). **Discharged 2026-09-11, session 89f9e0**: the refactoring-survey plan reached a terminal status and was archived, the pointer was released, and `.current-plan` now names this plan again — which is the condition the stamp was written to wait for. The stamp is struck rather than deleted because it is the record of why this plan sat unadvanced for three days.
**Windows application directory**: `tools/winide` — named by David, 2026-09-11, session 89f9e0 ("adr-341 starts in tools/winide"), which is the naming ADR-341's Scope delegates to this plan. **Resolved the same session**: the Scope line's "no abbreviations" qualifier was flagged as contradicted by the name, and David ruled ("winide is fine, amend the ADR scope"). ADR-341's Scope is amended accordingly — the directory is named in the ADR itself, the qualifier is struck, and AC-2's `<windows directory>` placeholder is resolved to `tools/winide` so the check is a command that runs. The reason recorded there: `tools/ide` is the macOS app, and a mirror (D1) should read as one at a glance.
**Takes the pointer from**: `docs/work/archive/publish-readiness/plan.md` — dispositioned DONE by David 2026-09-08 (session 4a2d5f, rule 18b option 1: "mark it done and archive it, the three carry forward as issues"; GH #387 filed for the outside-repo proof, #332/#350 and #317 already open, #224 already deferred) and archived the same session. `.current-plan` names this plan as of 2026-09-08. The Secret Letter port plan stays PAUSED and does not take the pointer back (David: "we're on main for now").
**Overall scope**: Execute ADR-341 D2 — the first step of D8's order, and the only thing this plan authorizes. Build, in both WPF and WinUI 3, every control the parity table (AC-1) marks as possibly problematic; verify the three assumptions ADR-341 names in Context and D3/D6; check D7's MSIX packaging question; and write the toolkit decision to `docs/work/adr-341-spike/decision.md` with per-control evidence, plus the one-line ADR-154 note the ADR's Consequences assign to this phase. Output is evidence, not product — nothing here ships, and nothing here is ADR-182, the D5 generator, the D3 host contract, or the D-order's later shell work.
**Bounded contexts touched**: N/A — this is a technology spike under `tools/spikes/adr-341/` (never `packages/`) plus a plan-owned decision record. No domain modeling; no production code path changes on either platform. Spike code is exempt from `documentationStandard: always` (`.devarch/descriptor.json`) beyond a header line stating it is a spike (per this plan's own scope note, consistent with rule 9a's floor: it is not a new public interface being shipped).
**Key domain language**: N/A — infrastructure/tooling spike, not domain-affecting software work (see "When DDD Does NOT Apply"). Phase names below use ADR-341's own terms (D2, D3, D6, D7, AC-1) rather than invented ones.

## References consulted
- `docs/architecture/adrs/adr-341-chord-writer-windows.md` — the governing ADR (ACCEPTED 2026-09-08, GH #386): D2 names the spike's method and order (editor first, then whatever AC-1 flags); D3 names the WebView2 resource-request-hook assumption; ~~D6 names the ARM64-emulation assumption (checked once, not a gate)~~ — **D6 amended 2026-09-11 (session 89f9e0): Windows on ARM is deferred indefinitely and the assumption is withdrawn, so D6 names no check this plan owes**; D7 names the MSIX packaging check that decides installer format later, not in this plan; AC-1 sets the parity-table shape this plan's Phase 1 must produce; Consequences assign the ADR-154 note to "the plan phase that records the D2 spike result" — this plan's closing phase.
- `docs/architecture/adrs/adr-154-sharpee-ide.md` — line 150 ("A specific implementation technology (Electron vs Tauri vs native). Implementation choice is deferred to the implementation plan.") is the deferral bullet this plan's final phase annotates; verified by direct read, not by ADR-341's citation alone.
- `docs/architecture/adrs/adr-297-ide-appearance.md` — the appearance-mirror system (dynamic light/dark tokens, user override) a Windows chrome spike must reproduce; read for what "the appearance mirror" in D2 concretely names.
- `docs/architecture/adrs/adr-280-chord-writer-project-model.md` (as amended A1) and its ADR-341 citation — real folders at `Documents\<Story Title>\`, the filesystem assumption D7's MSIX check tests against.
- `docs/context/project-profile.md` — no CI gates beyond the publishing workflow (this plan adds none); TypeScript strict / pnpm conventions do not apply to the spike's C# code but the "verify active maintenance before recommending" convention (project memory, folded into the profile's dependency posture) governs Phase 2 and Phase 6.
- `docs/context/session-20260908-0127-main.md` — most recent session; records ADR-341's acceptance and that its work is its own plan, starting with the spike phase, "not scheduled" — this plan is that scheduling. No open item in it bears on this plan's scope.
- `docs/work/web-chord-writer/brainstorm-20260813-web-chord-writer.md` §2, §3, §12.3 — superseded on the desktop-shell question by ADR-341, but its measurements stand: §2 confirms `@sharpee/chord`, `@sharpee/engine`, `@sharpee/story-loader`, `@sharpee/platform-browser` are browser-clean (0 Node-bound files each), which is why the three panes need no rewrite for a Windows host; §3 named the editor as the one real fork, unchanged by ADR-341; §12.3's toolchain-size measurement (165M/177M, 2026-08-13) is superseded by ADR-341's own re-measurement (171M/187M, 2026-09-08) — this plan cites the ADR's figure, not the brainstorm's, per rule "ADR Status lines are unreliable" applied symmetrically to superseded measurements.

## Phases

### Phase 0: Decide and provision the spike environment
- **Tier**: Small
- **Budget**: 100
- **Domain focus**: N/A — infrastructure decision, not domain modeling.
- **Entry state**: ADR-341 ACCEPTED (GH #386). No Windows environment provisioned for this work yet. David develops on macOS; every subsequent phase needs Windows to run C#/.NET, WPF, WinUI 3, and WebView2.
- **Deliverable**: A written decision, recorded in this plan's progress notes (not a separate ADR — this is an execution-environment choice, not a constraint on future sessions), naming which of the three options David picked and why, plus the provisioned environment itself:
  1. **A local Windows VM** (e.g., Parallels/UTM/VMware on the Mac, or a cloud Windows VM) — can verify interactive editor feel, WebView2 resource-request behavior, and MSIX install/run; cannot cheaply verify Windows-on-ARM emulation unless the VM host is itself Apple-silicon-hosted x64-on-ARM (which is a different emulation layer than Windows-on-ARM's own x64 emulation and does not substitute for it).
  2. **A physical Windows machine** — can verify everything a VM can, plus is the only option that can verify Windows-on-ARM emulation for real if the physical machine is a Windows-on-ARM device (e.g., a Snapdragon-based laptop); without one, the ARM assumption stays unverified by this plan and is flagged as such in Phase 6, not silently passed.
  3. **A GitHub Actions Windows runner** — build-only: can verify the tree-sitter binding compiles and loads headlessly, can run the MSIX packaging check's filesystem behavior, can run WebView2 in headless/offscreen mode if the check can be scripted that way; **cannot** judge interactive editor feel (auto-indent responsiveness, bracket-match highlighting under real keystrokes, scroll performance) — the ADR's own language ("how it felt") names a judgment this option cannot deliver.
  This phase does not decide for David; it presents the three options with what each can and cannot verify, gets David's pick (and, if he picks more than one — e.g., a runner for build-only checks plus a VM for the editor-feel judgment — records the split), and provisions the chosen environment(s): .NET SDK (matching D2's ".NET" — verify current LTS at provisioning time), a WebView2 runtime install, and `git` access to this repository (or a worktree) on the Windows side.
- **Exit state**: Environment decision recorded; chosen environment(s) provisioned and confirmed reachable (a trivial `dotnet --version` or equivalent run, evidence inline: command, output, date); `tools/spikes/adr-341/` scaffold directory created (empty save for a header `README.md` stating it is spike code, exempt from the repository's documentation standard beyond that header, never shipped, and not under `packages/`).
- **Progress (2026-09-08, session 4a2d5f)** — Decision: **option 2, a physical x64 Windows machine** (David: "I guess I need a Windows machine"). Reasoning recorded from the conversation: AWS ships no license-included Windows 11 AMI and a retail key carries no right to shared-tenancy cloud (Dedicated Host plus Software Assurance or VDA required); Azure's Marketplace Windows 11 works for dev/test but bills by the hour with the license folded in, and a one-vCPU size is too slow for an interactive spike; a local arm64 VM on the Mac (Parallels or UTM with a retail Windows 11 key) would judge editor feel and verify the ARM assumption but cannot run Phase 2's x64 Node check or Phase 5's x64 MSIX check, which are about what ships. A physical x64 machine covers every phase except the Windows-on-ARM emulation check, which stays unverified and is flagged in Phase 6 per this phase's option text. Sizing: four cores and sixteen gigabytes is comfortable; Windows 11 Home suffices (developer mode and MSIX sideloading are available on Home). Machine identified the same session: a Getorli mini PC, Ryzen 5 6600H (six cores, twelve threads, x64), 16 or 32 GB LPDDR5 (David weighed the 16 GB model; either is above the sizing line), 1 TB NVMe, Windows 11 Pro preinstalled; arrives Friday 2026-09-11. On arrival: confirm the preinstalled Windows 11 edition is activated (Pro or Home both suffice), turn on developer mode (Settings › System › For developers), then install the .NET SDK (current LTS), Visual Studio or Build Tools with the .NET desktop and Windows App SDK workloads, and confirm the WebView2 runtime is present. The exit state's `dotnet --version` evidence is not yet recorded. **Claude Code runs on the Windows machine itself** (native install with Git Bash, not WSL — the spike's toolchain, WinUI 3 deployment, and MSIX packaging are Windows-side), and David judges the editor feel at that machine's keyboard, on a monitor or over RDP from the Mac. **DevArch on Windows** (David, 2026-09-08: "this goes back to my need to get DevArch installing cleanly on Windows"): this plan is the first Sharpee work to run a DevArch session on Windows. Intent: DevArch installs cleanly first, and Phase 0's exit evidence includes the SessionStart hook output on that machine. Explicit fallback so the spike never waits on the installer: if DevArch is not ready when the machine is, the spike phases run under plain Claude Code on Windows with no hooks, and each session writes its phase record into this plan by hand at session end. Nothing in the spike depends on DevArch; only the bookkeeping does. The DevArch-on-Windows work itself is tracked in DevArch's own repository, not here.
- **Progress (2026-09-11, session 89f9e0)** — **The machine arrived and this session runs on it.** Environment reported by the harness: Windows 11 Pro 10.0.26100, `PROCESSOR_ARCHITECTURE=AMD64`, primary shell PowerShell 5.1 with Git Bash beside it, repository at `C:\Users\David\repos\sharpee`. **DevArch installs and runs cleanly on Windows**: the SessionStart hook fired with `[Session ID: 89f9e0]`, the previous-session pointer, and `[Capabilities: base]`, so Phase 0's explicit fallback (spike phases run under plain Claude Code with hand-written phase records) is not needed and is retired. Claude Code is the native Windows install, not WSL, as this phase's own text required.
  - **Provisioning, measured before any install** (2026-09-11 17:52–18:04): WebView2 Evergreen runtime **already present at 153.0.4234.32** (read from `HKLM:\SOFTWARE\WOW6432Node\Microsoft\EdgeUpdate\Clients\{F3017226-…}`), which satisfies the phase's WebView2 requirement with no action and is the runtime Phase 2's assumption check 2 and Phases 3/4's pane-hosting spikes run against. `dotnet` absent; no Visual Studio or Build Tools (`vswhere.exe` not found); developer mode off (`AllowDevelopmentWithoutDevLicense` unset).
  - **.NET SDK — installed, and this is the phase's exit evidence.** `winget install --exact --id Microsoft.DotNet.SDK.10` → `Successfully installed`; `dotnet --version` → **`10.0.401`**, `dotnet --list-sdks` → `10.0.401 [C:\Program Files\dotnet\sdk]`, 2026-09-11 18:04. .NET 10 is the current LTS, which is what this phase's "matching D2's '.NET' — verify current LTS at provisioning time" asked for, and it is the LTS ADR-341's Context names WPF as shipping with. Note for later sessions: `dotnet` is not on the Git Bash `PATH` inherited by an already-running shell; `"$ProgramFiles\dotnet\dotnet.exe"` resolves it until a new shell picks the machine `PATH` up.
  - **Developer mode enabled**: `AllowDevelopmentWithoutDevLicense = 1` written to `HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\AppModelUnlock`. This is what Phase 5's MSIX sideloading needs.
  - **Visual Studio Community 2026 (18.10.0) instead of Build Tools — a deliberate substitution, David's call.** David asked mid-session, "should I install VS community?", against this phase's own text, which named "Visual Studio or Build Tools". Community was recommended and taken. The reason is this plan's own acceptance standard, not preference: Build Tools is headless MSBuild with no XAML designer, no WinUI 3 templates, and no XAML Hot Reload, and Phases 3 and 4 turn on D2's "how it felt" — a judgment reached far faster with a designer and hot reload than by authoring XAML blind in two toolkits. Community is free for an individual on his own project and is a strict superset of Build Tools, so the substitution widens the approved install rather than departing from it. Installed with `--add Microsoft.VisualStudio.Workload.ManagedDesktop` (.NET desktop, i.e. WPF) `--add Microsoft.VisualStudio.Workload.Universal` (Windows app development, i.e. WinUI 3 / Windows App SDK / MSIX) `--includeRecommended`.
  - **Directories created.** `tools/spikes/adr-341/` with the header `README.md` this phase's exit state specifies (spike code, exempt from `documentationStandard: always` beyond that header, never shipped, not under `packages/`). Separately, `tools/winide/` was created as the **reserved and deliberately empty** Windows application directory, carrying a `README.md` that states it stays empty until D8's order reaches the shell and that the spike deliberately does not live there. **David ruled the two stay separate** (2026-09-11, choosing "Spike stays separate" over collapsing them): throwaway evidence does not sit in the product tree, which keeps D2's "output is evidence, not product" true of the filesystem and not only of the prose.
  - **`.current-plan` repointed** to `docs/work/adr-341-spike/plan.md`. No rule 18b disposition was owed: the pointer was absent (released when `meta-commands-work-after-ending` was archived 2026-09-11), so nothing was displaced.
  - **Visual Studio verified** (2026-09-11 18:1x, exit code 0): `vswhere -products *` reports **Visual Studio Community 2026, 18.10.12201.205**, at `C:\Program Files\Microsoft Visual Studio\18\Community`. Both requested workloads present — `vswhere -requires Microsoft.VisualStudio.Workload.ManagedDesktop` and `-requires Microsoft.VisualStudio.Workload.Universal` each resolve the install. MSBuild at `…\18\Community\MSBuild\Current\Bin\MSBuild.exe`.
  - **ADR-341's Scope amended** (David, "winide is fine, amend the ADR scope"): the ADR now names `tools/winide` outright, strikes its "no abbreviations" qualifier with the reason recorded, and resolves AC-2's `<windows directory>` placeholder to `tools/winide` so that acceptance check is a runnable command.
  - **Gap found, carried to Phase 5 rather than fixed here: the Windows 10/11 SDK is not installed.** `C:\Program Files (x86)\Windows Kits` contains only `NETFXSDK`; a recursive search for `makeappx.exe` finds nothing, and `microsoft.windowsappsdk` is not in the NuGet cache. `--includeRecommended` on the Universal workload did not pull the Windows SDK component. This does not block Phase 0 (whose exit evidence is `dotnet --version`) and may not block Phase 4 either, since modern SDK-style WinUI 3 projects obtain `makeappx`/`signtool` through the `Microsoft.Windows.SDK.BuildTools` NuGet package rather than a Kits install — **which is itself an assumption, not a measurement, and Phase 5 must check it before concluding the packaging tools are present.** If the NuGet route does not cover Phase 5's MSIX check, the fix is one `winget install` adding the Windows SDK component; recorded here so a later session does not rediscover it as a mystery build failure.
- **Status**: DONE (2026-09-11, session 89f9e0) — every exit-state item met: the environment decision was already recorded 2026-09-08 (option 2, a physical x64 Windows machine), the machine is provisioned and this session runs on it, `dotnet --version` → `10.0.401` is the inline evidence with command, output and date, and `tools/spikes/adr-341/` is scaffolded with its header README. Two facts beyond the exit state: DevArch runs cleanly on Windows, retiring this phase's fallback clause, and the Windows SDK gap above is carried to Phase 5.

### Phase 1: Build the parity table (AC-1) — the control inventory the spike list is drawn from
- **Tier**: Small
- **Budget**: 100
- **Domain focus**: N/A — inventory of the macOS app's product surface, not new domain modeling. Independent of Phase 0 — this phase reads only the Swift source tree, needs no Windows access, and can run before, after, or in parallel with Phase 0's environment provisioning. Sequenced second here only for narrative order in this document.
- **Entry state**: `tools/ide/SharpeeIDE` is the reference implementation (106 Swift files, 18,921 lines per ADR-341's Context, verified this session by directory listing). No parity table exists yet.
- **Deliverable**: `docs/work/adr-341-spike/parity-table.md` — one row per feature under ADRs 277, 280, 281, 284, 285, 297, and the Play, Test, Docs, and World Index panes (AC-1's own list), each row carrying a Windows-status column seeded to "not yet built" and a "possibly problematic on Windows" mark where applicable. Seed the "possibly problematic" marks from the custom views already confirmed present this session (verified by directory listing, 2026-09-08):
  - **Editor** (`Editor/`): `EditorViewController.swift`, `LineNumberRulerView.swift`, `TabBarView.swift`, `ChordLexer.swift`, `SyntaxHighlighter.swift`, `AutoIndenter.swift`, `BracketMatcher.swift` — a custom AppKit text editor with no off-the-shelf Windows equivalent. **Flagged.**
  - **Project** (`Project/`): `ProjectTreeViewController.swift`, `ProjectPaneViewController.swift`. **Flagged** (D2 names the project tree explicitly).
  - **WebView2 pane hosting** (Play, Test, Docs): `Play/PlayViewController.swift`, `Play/PlayHeaderView.swift`, `Play/RightPanelViewController.swift`, `Play/IndexView.swift`, `Play/ErrorDiagnosisView.swift`, `Play/WebBundle.swift`, `Play/PlayURLSchemeHandler.swift` (confirmed `WKURLSchemeHandler` conformance by read); `TestingSurface/TestingSurfaceViewController.swift`; `Docs/DocsTabViewController.swift`, `Docs/DocsTabSchemeHandler.swift`. **Flagged** (D2 and D3 both name WebView2 hosting explicitly — this is the seam D3's host contract eventually formalizes, but this phase only needs whether WebView2's resource-request hook can serve a bundle directory, which is Phase 2's assumption check).
  - **Window chrome / appearance mirror** (ADR-297): the app's dynamic light/dark token system and user-override menu. **Flagged.**
  - **Other custom views** (list from directory read, mark each "possibly problematic" pending Phase 0's environment and David's own read of which look non-trivial to reproduce): `Build/BuildPanelView.swift`, `Build/BuildSettingsViewController.swift`, `Build/GameErrorsView.swift`, `Build/BottomPanelViewController.swift`, `Compose/ProblemsView.swift`, `Publish/PublishView.swift`, `Settings/SettingsWindowController.swift`, `World/WorldMapView.swift`, `World/WorldReachView.swift`, `World/WorldView.swift`, `World/WorldFindingTable.swift`, `World/WorldCandidateCard.swift`, `World/WorldIncompleteView.swift`, `UI/StoryTitleBarViewController.swift`, `UI/TabStripView.swift`, `UI/ThemedPane.swift`, `Launch/LandingPageViewController.swift`, `Launch/CreateStoryViewController.swift`, `MainWindow.swift`.
  - **Not spiked here** (rows exist for parity tracking but are not "possibly problematic" — they are plain subprocess plumbing or standard controls): the seven subprocess sites (`Build/BuildRunner.swift`, `Build/ShellEnvironment.swift`, `Compose/ChordVersionCheck.swift`, `Compose/ComposeRunner.swift`, `Project/IntrospectionRunner.swift`, `Test/TestRunner.swift`, `World/WorldIndexRunner.swift`) and `Updates/UpdateController.swift` (Sparkle — its Windows counterpart is D7's update-channel question, not a UI-control spike).
- **Exit state**: `parity-table.md` committed under `docs/work/adr-341-spike/`; every row from AC-1's category list present; the "possibly problematic" column populated, becoming the authoritative list Phases 3 and 4 spike against (this phase's exit state is Phases 3/4's entry state for "what to build").
- **Outcome (2026-09-08, session 4a2d5f)**: `docs/work/adr-341-spike/parity-table.md` written from a read of every UI-bearing Swift file's header, the menu builder, and the two panel controllers (106 files listed, `d06e511ae`). Rows grouped by surface: app shell and chrome, launch, project pane, editor, the eight right-panel tabs (Build, Play, Testing, Index, Diagnosis, Documentation, Publish, World with its Map, Reach, Incomplete views), the bottom panel, build/test/compose plumbing, workspace and story model, persistence and settings, protocol and shared panes. Windows column seeded "not built" throughout. The spike list is six controls in D2's order: the editor, WebView2 hosting (Play, Testing, Documentation as three cases), the project tree, window chrome and appearance (split layout, title band, dynamic tokens, the pin), the reusable tab strip, and the World map's custom drawing. Two parity facts recorded for the shell plan: ADR-285's asset manager is not in the macOS source (the Assets group is a folder lens), and ADR-277's transcript editor was retired by ADR-306/307, so no transcript row exists.
- **Status**: DONE (2026-09-08, session 4a2d5f — the table is written; it is committed at the session's next push. Ran before Phase 0's provisioning, which waits on the machine arriving 2026-09-11)

### Phase 2: Verify the three named assumptions and select spike candidates
- **Tier**: Medium
- **Budget**: 250
- **Domain focus**: N/A — dependency verification, not domain modeling.
- **Entry state**: Phase 0's environment provisioned and reachable. Independent of Phase 1's exact row contents — none of the three assumptions below depend on which controls end up flagged, though Phase 2 also selects the editor-control candidates Phases 3/4 will build against, which is easier once Phase 1's editor row is confirmed.
- **Deliverable**: Three assumption checks, each recorded with command, result, and date (evidence inline per the repository's evidence rule):
  1. **A .NET tree-sitter binding exists and loads a grammar.** Identify the candidate binding(s) (ADR-341's Context calls this "an assumption here" without naming a specific package), verify current maintenance status per the project's "check dependency health before recommending" rule, install it in the spike scaffold, and load any grammar (a TypeScript grammar is sufficient for this check — the Chord grammar itself is ADR-182's unimplemented artifact and is out of scope here) to confirm the binding actually parses. Record pass/fail; a fail does not block the spike, it changes what the editor spike can test (a custom hand-written lexer becomes the fallback the record notes, mirroring D4's own contingency language).
  2. **WebView2's resource-request hook can satisfy the D3 host contract.** Build the smallest possible WebView2 host (in whichever toolkit is fastest to scaffold — this check is toolkit-agnostic since both WPF and WinUI 3 host the same `WebView2` control) that intercepts a resource request and serves a small directory of files over a custom origin, the way `PlayURLSchemeHandler.swift` and `DocsTabSchemeHandler.swift` do on macOS (both confirmed `WKURLSchemeHandler` conformers by read this session). Confirm `localStorage` persists and relative URLs resolve against the custom origin — the two properties D3's Context names as what the WKURLSchemeHandlers currently guarantee.
  3. ~~**The vendored x64 Node runs acceptably under Windows-on-ARM emulation.** Checked once, not a gate (D6's own wording). If Phase 0's environment includes a Windows-on-ARM device, run the vendored x64 Node build under it and record whether it launches and executes a trivial script; if no ARM environment was provisioned, record that explicitly as unverified rather than silently passing it — Phase 6 must not report this assumption as checked if it was not.~~ **Withdrawn 2026-09-11, session 89f9e0** (David: "we indefinitely deferred ARM on Windows"). ADR-341 D6 is amended the same session: Windows on ARM is deferred indefinitely, the ADR asserts nothing about how the vendored x64 Node behaves there, and so there is no assumption to check. This is not the same as unverified — **do not carry it forward as a gap.** The struck text is kept because it is what Phase 0's environment-option analysis was written against (option 2's "covers every phase except the Windows-on-ARM emulation check"), and that reasoning is now moot rather than wrong. **This phase has two named assumptions, not three.**
  Also select and health-check (per project memory, dependency health verified before recommending) the editor-control candidates Phases 3/4 will build against: for WPF, the mature third-party code-editor ecosystem ADR-341's Context names generally (name specific candidates at spike time — AvalonEdit is the most commonly cited mature option, verify its current maintenance status rather than assuming it); for WinUI 3, the three options ADR-341's Context names (the Scintilla port, which the ADR already characterizes as "early work in progress" — verify that characterization still holds; the Win2D control; Monaco hosted in WebView2 — check each for current maintenance).
- **Exit state**: Both remaining assumptions checked (pass or fail) and recorded with evidence; editor-control candidates for both toolkits named and health-checked, ready for Phases 3/4 to build against. (Was "all three" before assumption 3 was withdrawn 2026-09-11.)
- **Outcome (2026-09-11, session 89f9e0)**: `docs/work/adr-341-spike/assumption-checks.md` — both remaining assumptions **PASS**, and the editor-control candidates are named and health-checked. Evidence inline there with commands, versions and dates; two runnable spikes under `tools/spikes/adr-341/`.
  - **Assumption 1 — .NET tree-sitter binding: PASS** on four sub-checks. `TreeSitter.DotNet` 1.3.0 (MIT, 2026-01-22) ships `win-x64` natives *and* 30 grammars in one package with zero dependencies, targets `netstandard2.0` so WPF and WinUI 3 both consume it. Loads by name and parses; the **query API returns correct captures with byte ranges**, which is what a highlighter is built from; **loads a grammar from an arbitrary path** — the exact route ADR-182's Chord grammar will take; incremental reparse costs **1 µs** on a four-line document — **a figure Phase 3 corrected the same session**: in a real editor over a 1755-line file the cost is **14.8–15.9 ms per keystroke**, because re-running the highlight query over the whole tree is O(document), not O(edit). The parser is fast; the naive whole-document query is not, in either toolkit. See the strikethrough in `assumption-checks.md`. Dormant candidates (`TreeSitterSharp`, `tree-sitter`, both last published 2023-11) rejected on dates.
  - **Assumption 2 — WebView2 resource-request hook: PASS** on four sub-checks. `AddWebResourceRequestedFilter` + `WebResourceRequested` (SDK 1.0.4191.47, 2026-08-28) serves a pane directory over a custom HTTPS origin, resolves and *applies* the pane's relative URLs, answers a request **entirely from memory** (the subprocess-results half of D3's contract, which a file-only hook would have failed), and keeps `localStorage` **across a process restart** — tested with a fixed user data folder over two passes, since a per-run temp folder would have produced a meaningless green.
  - **Editor controls named.** Phase 3 builds against **`AvalonEdit` 6.3.1.120**, whose external-tokenizer support was *verified by reflection on the shipped assembly*: `IHighlighter` and `DocumentColorizingTransformer` are both public, so tree-sitter captures can drive colouring and D4's one-grammar rule holds. Phase 4 builds against **`WinUIEdit` 0.0.5-prerelease**, with `WinUI.Monaco` as a recorded fallback.
  - **One finding Phase 6 must weigh, recorded as evidence and not as a decision**: WPF has a mature, free, externally-tokenizable *native* editor control today; WinUI 3's only native option still self-declares "not production ready… breaking API changes are very likely" (verified from the shipped 0.0.5 README — ADR-341's characterization holds 14 months on), and its better-maintained alternatives are **Monaco in WebView2, which is not a native editor** and conflicts with D4 and with D3's reservation of WebView2 for the three panes. D2 rules that the toolkit whose spikes pass is the toolkit, and no spike is built yet — a candidate that reads badly here and performs well in Phase 4 beats this table, which is the whole reason D2 orders spikes instead of argument.
- **Status**: DONE (2026-09-11, session 89f9e0) — exit state met: both assumptions checked with evidence, editor-control candidates named and health-checked for both toolkits, Phases 3/4 have something concrete to build against.

### Phase 3: WPF spike — every flagged control, editor first
- **Tier**: Large
- **Budget**: 400
- **Domain focus**: N/A — technology spike.
- **Entry state**: Phase 1's parity table names the flagged controls; Phase 2's assumption checks and candidate selection give this phase a tree-sitter binding result (or fallback) and a named editor-control candidate to build the editor spike against.
- **Deliverable**: In WPF, on .NET (current LTS, confirmed at Phase 0), build each control Phase 1 flagged, in this order (D2's own order): (1) the editor — the chosen candidate control (or a minimal custom `TextBox`/`RichTextBox`-based editor if no candidate survives Phase 2) wired to the tree-sitter binding if it loaded, with highlighting, auto-indent, bracket-matching, and a line-number gutter attempted, each recorded as built/partial/failed; (2) the project tree — a `TreeView`-based mirror of `ProjectTreeViewController`'s behavior (expand/collapse, selection, basic icons); (3) WebView2 hosting of a pane — reusing Phase 2's resource-request-hook spike, now loading one of the three actual panes (`tools/ide/web/docs-tab` or `testing-surface`'s build output is the natural first choice since both are already standalone web builds); (4) window chrome and the ADR-297 appearance mirror — light/dark dynamic resource dictionaries with a user-override toggle; (5) whatever else Phase 1 flagged, budget permitting — if the remaining flagged rows do not fit this session's budget, this phase's exit state records which were reached and defers the rest to a follow-on phase this plan appends, rather than silently shrinking scope. Each spike's record is inline in `docs/work/adr-341-spike/wpf-spike.md`: what was built, what failed, and how it felt (D2's own language — the editor's interactive responsiveness is a felt judgment, not just a build-succeeded checkbox, which is why Phase 0's environment choice matters here specifically).
- **Exit state**: `wpf-spike.md` records a pass/partial/fail per flagged control with evidence (screenshots are not required by this plan but are useful; command/build output and a written felt-assessment are); every row Phase 1 flagged is either addressed here or explicitly carried to a follow-on phase named in this phase's own closing note.
- **Progress (2026-09-11, session 89f9e0)** — `docs/work/adr-341-spike/wpf-spike.md`. **All six flagged controls BUILT in one window**, `tools/spikes/adr-341/wpf-spike/`, ~700 lines across 7 files, `dotnet build -c Release` exit 0 / 0 errors. Not six toy apps: the macOS app is one window and its feel is the feel of the pieces working together.
  - **Editor** — `AvalonEdit` coloured entirely from `TreeSitter.DotNet` query captures, **no `.xshd` syntax definition anywhere in the spike**, which demonstrates D4's one-grammar rule rather than asserting it; a Chord `highlights.scm` drops into the same capture-to-token table unchanged. Loaded with the real `packages/engine/src/game-engine.ts`: **1755 lines, 1694 spans**, rendering verified by screenshot. `AutoIndenter` and `BracketMatcher` ported behaviour-for-behaviour from the Swift originals (2-space unit, indent after `([{`, balanced scan preferring the char before the caret, no string/comment skipping — matching the Swift v1's own stated limit) so the spike measures the toolkit and not a different algorithm.
  - **Appearance (ADR-297) verified by pixel sampling, not by eye**: restarted under each pin, `railBackground` measured **#16171D / #DCE0E8**, `projectBackground` **#262832 / #E6E9EF**, `editorBackground` **#1E1F26 / #EFF1F5** — exact against `Theme.swift` on all three, both palettes. The pin persists under the same value name the macOS app uses (`SharpeeAppearance`) and is applied in `OnStartup` before the window is constructed, which is D2's "applied at launch before the window builds".
  - **One defect**: the OS non-client title bar does not follow the appearance — free on macOS via `NSApp.appearance`, an explicit per-window `DwmSetWindowAttribute(DWMWA_USE_IMMERSIVE_DARK_MODE)` on Windows. One call, but a real instance of the mirror paying for what the original got for nothing; Phase 4 must check whether WinUI 3 has the same gap.
  - **The finding that corrects Phase 2**: live reparse is **14.8–15.9 ms per keystroke** on the 1755-line file, against the 1 µs this plan quoted from Phase 2's four-line micro-benchmark. The parser is not the cost — re-running the highlight query over the whole tree is O(document), not O(edit). Corrected in `assumption-checks.md` as a visible strikethrough rather than a silent edit, and carried into Phase 4 as a measurement both toolkits are held to on the same file.
- **Closing note (2026-09-11, session 89f9e0)** — David used the build, reported eight user-visible defects in succession (dead rail icons, un-closeable tabs, wrong-language highlighting, invisible project tree, missing window chrome, right panel ignoring the appearance, a left panel that never fully collapsed, and a crash), then asked: *"what's the goal here? given everything we built on on the MacOS side, recreating the same views shouldn't be this hard."* **Every defect was this spike's own code; none was a fact about WPF** — the table in `wpf-spike.md` names each cause, including the crash (`NullReferenceException` in AvalonEdit's `RunTransformers`, from a null highlighter pushed into `LineTransformers` once grammar-less files were made legal) and the missing chrome (I was moving his window to screenshot it while he used it). All are fixed. The pattern is the finding: a spike whose defects are all its author's has stopped producing toolkit evidence, and `session-checkpoint` independently reached the same read — D4's screening question was settled in Phase 2 by assembly reflection, so no build was required to answer it.
  - **The felt assessment was not taken**, on David's ruling ("close phase 3 and move to phase 4"). Recorded in `wpf-spike.md` as a decision with its reason rather than as an unmet exit condition, because the toolkit decision turns on whether **WinUI 3** has a usable native editor at all, not on refining a toolkit that cleared every bar this spike set.
  - **One datum routed away from this plan**: "recreating the same views shouldn't be this hard" is evidence about the **cost of D1's mirror** — six controls at this fidelity took a session and produced eight defects, against a parity table with far more than six rows. That belongs in the shell plan's estimate, not in a toolkit comparison; the same six in WinUI 3 would produce a different eight defects and say nothing more about the choice.
- **Status**: DONE (2026-09-11, session 89f9e0) — closed on the evidence gathered, per this phase's own escape hatch and David's explicit ruling. All six flagged controls built and demonstrated; `wpf-spike.md` records pass/defect per control, the ADR-297 pixel match, the DWM title-bar gap, and the reparse measurement. The felt assessment is recorded as deliberately not taken.

### Phase 4: WinUI 3 spike — every flagged control, editor first
- **Tier**: Large
- **Budget**: 400
- **Domain focus**: N/A — technology spike. Same structure as Phase 3, same entry dependencies, independent of Phase 3 itself (the two toolkit spikes do not depend on each other's outcome and could run in either order, or by different sessions in parallel if David works both — noted here so this plan does not imply WPF must be tried first).
- **Entry state**: Same as Phase 3 — Phase 1's flagged list, Phase 2's assumption/candidate results.
- **Deliverable**: The same five-item build list as Phase 3, in WinUI 3 on the Windows App SDK (1.8 stable per ADR-341's Context; note if 2.0 preview is evaluated instead and why), against the editor-control candidate(s) Phase 2 selected for WinUI 3. Recorded inline in `docs/work/adr-341-spike/winui3-spike.md` in the same shape as `wpf-spike.md` so Phase 6 can compare the two directly, row by row.
- **Exit state**: `winui3-spike.md` records a pass/partial/fail per flagged control with evidence, same standard as Phase 3's exit state, including the same explicit carry-forward note if budget did not cover every flagged row.
- **Scope tightened before starting (2026-09-11, session 89f9e0)**, on Phase 3's closing lesson and `session-checkpoint`'s read: Phase 3 built six controls at product fidelity and every defect it produced was its author's, not WPF's. Rebuilding six controls at that fidelity in WinUI 3 would produce a different set of author defects and **discriminate nothing**. This phase therefore builds what actually differs between the toolkits, in this order:
  1. **The editor — the decisive one.** Does `WinUIEdit` (0.0.5-prerelease) build, run, and accept **external** styling driven by tree-sitter through Scintilla's container-lexing messages? WPF answered yes via a typed public interface. If WinUI 3's answer is no, or only through a web view, D4 decides the toolkit on its own.
  2. **Custom drawing.** WPF handed the tab strip and the World map a retained-mode `OnRender`/`DrawingContext`. WinUI 3 has no equivalent and needs Win2D or shape composition. This is a genuine, measurable difference in what the two toolkits ask of the same two controls.
  3. **Window chrome and appearance.** WPF needed an explicit `DwmSetWindowAttribute` for the title bar. WinUI 3 owns its chrome and probably does not — confirm, because it is a point in WinUI 3's favour and the record should carry both directions.
  4. **The same reparse measurement on the same file** (`game-engine.ts`), so the ~15 ms whole-document figure is comparable rather than re-derived.
  5. **WebView2 hosting** — confirm only. Both toolkits host the identical control; Phase 2 already proved the hook. Anything beyond "it loads the pane" is duplicated work.
  **Not built**: the project tree (WinUI 3 ships a `TreeView`; nothing to learn), tab close boxes, pane collapse, document sets, save/reload. Those were product fidelity in Phase 3 and would be product fidelity here.
- **Outcome (2026-09-11, session 89f9e0)**: `docs/work/adr-341-spike/winui3-spike.md`. Built at `tools/spikes/adr-341/winui3-spike/` — unpackaged WinUI 3 on Windows App SDK **2.4.0** (ADR-341's Context records "1.8 stable, 2.0 preview"; that snapshot has aged, no decision changes), `dotnet build -c Release` exit 0.
  - **The decisive question came back LEVEL.** `WinUIEdit` drives tree-sitter through Scintilla **container lexing** — `SetILexer(0)`, `StartStyling`/`SetStyling`, `StyleSetFore` — with no built-in lexer active, verified by screenshot in both palettes. **D4 is satisfiable on both toolkits, so the editor does not decide the choice**, which is the opposite of what Phase 2's table implied.
  - **A second Phase 2 correction**: that table called WinUIEdit's styling reachable only "through a raw message interface rather than a typed extension point". Reading the shipped `.winmd` shows **1003 typed WinRT members**, the whole Scintilla surface; `SendMessage` is the escape hatch, not the route. The original was inferred from the README instead of measured — the exact failure `docs/core-concepts` names. Struck through in `assumption-checks.md`.
  - **The style-pass measurement settles Phase 3's correction from the other side**: WinUI 3 **13.5–15.4 ms** against WPF's 14.8–15.9 ms on the same 1755-line file — statistically identical across two different editor controls and two different styling APIs. The O(document) query was the harness, not either toolkit.
  - **A Phase 3 prediction was wrong and is retracted**: Phase 3 guessed WinUI 3 would theme its title bar for free where WPF needed `DwmSetWindowAttribute`. It does not — an unpackaged WinUI 3 `Window` leaves the caption light under a Dark pin (measured `#EEF4F9`) and needs `AppWindow.TitleBar` work. **Both toolkits need explicit work; Phase 6 must not carry this as a difference.** Worse, WinUI 3 themes from `RequestedTheme` rather than a custom token table, so ADR-297's dual-palette system has to be wired to it explicitly or the pin is ignored entirely — which it was, on the first run.
  - **The real differences, which do not all point one way.** *For WPF*: immediate-mode `OnRender`/`DrawingContext` against WinUI 3 composing every drawn mark as a retained element with a layout pass (paid on every custom control in the parity table); a live theme flip for free via `{DynamicResource}` where WinUI 3 must rebuild visuals; **105 MB against 152 MB** working set; `dotnet new wpf` against a hand-written `.csproj` (the App SDK ships no CLI templates). *For WinUI 3*: Microsoft's stated direction, and a fully typed Scintilla projection.
  - **Answers Phase 0's open question for Phase 5**: the packaging tools arrive via NuGet (`Microsoft.Windows.SDK.BuildTools`, transitively at 10.0.26100.4654), so the missing Windows 10/11 SDK install is not the blocker it looked like.
- **Status**: DONE (2026-09-11, session 89f9e0) — the narrowed scope is complete and `winui3-spike.md` records pass/partial per item on the same shape as `wpf-spike.md`, so Phase 6 can compare row by row. **On the spikes as run, both toolkits pass**, which D2 did not anticipate; Phase 6 decides on the accumulated smaller differences and David breaks the tie if it is one.

### Phase 5: D7 packaging check — vendored Node and real project folders under MSIX
- **Tier**: Small
- **Budget**: 150
- **Domain focus**: N/A — packaging/filesystem check, not a UI spike. Independent of Phases 3 and 4 — this check does not need a working editor or project tree, only a minimal app shell (either toolkit, or no UI at all) packaged as MSIX. Can run any time after Phase 0.
- **Entry state**: Phase 0's environment provisioned. (This entry state also named Phase 2's vendored-Node-under-ARM-emulation result as informative input; that assumption was withdrawn 2026-09-11 with ADR-341 D6's amendment, so this phase's only entry dependency is Phase 0. The MSIX check here is an x64 check on an x64 target, which is the whole of what D6 supports.)
- **Deliverable**: A minimal MSIX-packaged test app that (a) carries a vendored Windows x64 Node build (a Windows counterpart of `tools/ide/vendor-toolchain.sh`, confirmed this session to vendor `node-v{version}-{arch}` plus a matching `@esbuild/{platform}` package, 171M of 187M installed on macOS per ADR-341's Context) and confirms it launches and runs a trivial script from inside the packaged app's install location; (b) writes to and reads from `Documents\<Story Title>\` — ADR-280's real-folder project model as amended A1 — from inside MSIX's packaged filesystem, checking whether MSIX's virtualized filesystem/broker behavior lets a packaged app read and write ordinary user-visible folders the way the macOS app's sandboxless model does. Recorded in `docs/work/adr-341-spike/msix-packaging-check.md` with command, result, and date.
- **Exit state**: A clear pass/fail/partial on whether MSIX's packaged filesystem is compatible with ADR-280's real-folder model as-is, or requires a filesystem capability declaration, or fails outright — this result decides between MSIX-with-appinstaller and a conventional-installer-plus-appcast-updater, per D7, but **the choice itself is made by the later shell plan, not here**; this phase records the evidence only.
- **Outcome (2026-09-12, session 89f9e0)**: `docs/work/adr-341-spike/msix-packaging-check.md`. **MSIX is viable — both checks PASS**, under a **real signed package installed to `C:\Program Files\WindowsApps`** (`IsDevelopmentMode: False`, `SignatureKind: Developer`), not a developer loose registration.
  - **(a) The vendored toolchain runs from the packaged install location.** A real `node-v22.23.1-win-x64` `node.exe` (83 MB — the version read from `tools/ide/vendor-toolchain.sh`, not chosen) executed from inside `WindowsApps`: exit 0, reporting `v22.23.1`/`x64`. No shim, no copy-to-temp, no extraction step. This was the check most likely to fail, since `WindowsApps` is ACL-locked and has historically been hostile to executing bundled binaries.
  - **(b) ADR-280's real folders survive packaging unchanged.** `Documents\<Story Title>\` was written and read with **no redirection** (the resolved path contains neither `Packages\` nor `VirtualStore\`), and — the test that actually matters — a sentinel written by the packaged app was read back by an **ordinary non-packaged process**, since the author's editor, `git` and sync client all live outside the package. `runFullTrust` is what buys this; no `documentsLibrary`, no `broadFileSystemAccess`.
  - **Three runs, deliberately**: unpackaged baseline (proves the harness detects identity rather than always passing), loose `-Register` (packaged identity but **not** the ACL-locked path — recorded rather than discarded because it is the run that *looks* conclusive and is not), then the signed install. Chain: `makeappx pack` → 39 MB from a 109 MB payload → `signtool sign /fd SHA256` → `Add-AppxPackage`.
  - **One finding for the shell plan**: the child process's **cwd is `C:\windows\system32`** under packaging, where the unpackaged baseline inherited the project directory. Harmless here, but the macOS app has **seven** subprocess sites, and any that assume their working directory would break once packaged — the Windows shell must set `WorkingDirectory` explicitly at each.
  - **Not established, and named as such**: the update channel (D7's actual point of difference between its two candidates), Azure Trusted Signing (run 3 used a locally-trusted self-signed cert), Store submission, and a real composed/built/playable Chord project — AC-4 is the shell plan's.
  - **Cleanup**: the test package was uninstalled and the self-signed certificate removed from `LocalMachine\TrustedPeople` and `CurrentUser\My` — a spike must not leave a signing certificate trusted on the machine.
- **Status**: DONE (2026-09-12, session 89f9e0) — exit state met with a clear PASS on both halves, evidence inline with commands, versions and dates, and an Integration Reality Statement (rule 13a) recording that the acceptance result comes from the real path with **no stub of any owned dependency**.

### Phase 6: Toolkit decision, dependency-health reconfirmation, and the ADR-154 note
- **Tier**: Small
- **Budget**: 100
- **Domain focus**: N/A — synthesis and record-writing.
- **Entry state**: Phases 1 through 5 complete (or explicitly closed with a carried-forward note per Phase 3/4's own escape hatch — this phase does not silently wait forever; if a phase's scope overran and was carried forward, Phase 6 either waits for the follow-on or, if David chooses, decides on the evidence gathered so far and notes the gap).
- **Deliverable**: `docs/work/adr-341-spike/decision.md`, containing: (1) a per-control results table, one row per Phase 1's flagged control, WPF result and WinUI 3 result side by side, drawn directly from `wpf-spike.md` and `winui3-spike.md`; (2) the toolkit decision — the toolkit whose spikes passed is the toolkit, per D2's own rule; if both pass, or both partially fail, the record states that explicitly and names what broke the tie (David's call, recorded as his, not inferred); (3) the **two** assumption results from Phase 2, carried into this record verbatim (three before the ARM-emulation assumption was withdrawn 2026-09-11; `decision.md` must not list it as an unverified gap, because ADR-341 D6 as amended asserts nothing about ARM for it to be a gap in); (4) the D7 MSIX result from Phase 5, carried into this record verbatim, explicitly marked as evidence for the later shell plan's decision, not a decision made here; (5) a reconfirmation of dependency health at decision time (WPF on .NET, WinUI 3 / Windows App SDK 1.8 stable and 2.0 preview, the editor control chosen, the tree-sitter .NET binding, WebView2 runtime distribution) — re-check each briefly since Phase 2's initial check may be session(s) old by the time this phase runs; (6) the one-line note ADR-341's Consequences assign to this phase, added beside ADR-154's deferral bullet at `docs/architecture/adrs/adr-154-sharpee-ide.md:150`, pointing at ADR-341 and this decision record.
- **Exit state**: `decision.md` committed; `adr-154-sharpee-ide.md` carries its one-line note; ADR-341's D2 is closed on evidence. No other ADR's Status changes (per ADR-341's own Consequences). This plan's own `Plan Status` is set to DONE once this exit state is reached, and the main session's rule-18b handling of `publish-readiness/plan.md` is what actually repoints `.current-plan` here or elsewhere — this plan does not touch that pointer itself.
- **Outcome (2026-09-12, session 89f9e0)**: `docs/work/adr-341-spike/decision.md` written — per-control comparison, both assumption results, the D7 MSIX evidence, and a dependency-health re-check at decision time. **ADR-154's note is written** (`adr-154-sharpee-ide.md:150`): the deferral is CLOSED as native on both desktop platforms, pointing at ADR-341 and this decision record. That note was written despite the open tie below, because ADR-154 deferred *Electron vs Tauri vs native* and D2 already closes that as native C# on .NET whichever toolkit wins. No other ADR's Status changed.
  - **D2's mechanical rule does not resolve this: BOTH TOOLKITS PASS.** Every spike passed on both, and the editor question — the one D2 ordered spiked *first* and the one Phase 2's table implied would decide it — came back **level**. D4 is satisfiable either way. This phase's own text governs that case: the record states it explicitly and the tie is **David's call, recorded as his, not inferred**. It is left open here on purpose.
  - **Three things do discriminate, all toward WPF**: the drawing model (AppKit's `drawRect:` maps onto `OnRender`/`DrawingContext`; WinUI 3 has no equivalent and turns every drawn mark into a retained element — a cost multiplier on every custom surface in the parity table, not a one-time difference); ADR-297 D2's *immediate* appearance change, which WPF gives free via `{DynamicResource}` and WinUI 3 requires rebuilding visuals for; and editor-control maturity (MIT AvalonEdit at 17.4M downloads against a `0.0.5-prerelease` whose README says "breaking API changes are very likely"). Memory (105 vs 152 MB) and scaffolding friction agree but are not load-bearing.
  - **The strongest counter-case is recorded too**, because a record that only argues one way is the thing D2 wrote its rule against: WinUI 3 is Microsoft's direction (`WindowsAppSDK` pushed the day of the check) while AvalonEdit's upstream has been quiet **282 days**. Those are different risks — a stable platform with a possibly-unmaintained editor, against an evolving platform with an immature one — not one better and one worse. The named mitigation is RoslynPad as an active downstream consumer plus Actipro as a paid fallback; WinUIEdit has no *native* fallback, only Monaco-in-WebView2, which conflicts with D4.
  - **What would overrule the recommendation, named explicitly**: if the Windows app should look like a modern Windows 11 app rather than a mirror of the macOS one, D1's premise weakens and WinUI 3's Fluent defaults start earning their cost. **That is a product question for David, not an engineering one.**
  - **A shared risk surfaced that belongs to neither toolkit**: `TreeSitter.DotNet` (35★, one maintainer, last published 232 days ago) is load-bearing for D4 on *both* native shells and is the least-supported dependency in the set. Worth watching whichever way the tie breaks.
- **Tie broken (2026-09-12, session 89f9e0): WPF.** David: *"wpf, agreed on the drawing model"* — endorsing the ground the record weighted first, that AppKit's `drawRect:` maps onto `OnRender`/`DrawingContext` while WinUI 3 turns every drawn mark into a retained element, a cost paid on every custom surface in the parity table rather than once. Recorded in `decision.md` as his ruling, with the recommendation and its counter-case left unedited so a later reader sees what the choice was made *against*, not only what it was made for. **ADR-341 D2 amended** the same session to carry the resolution, why the mechanical rule did not decide it (both toolkits passed; the editor question came back level), and the two Context claims the spikes corrected.
- **Status**: DONE (2026-09-12, session 89f9e0) — exit state met: `decision.md` written and the toolkit ruled, `adr-154-sharpee-ide.md:150` carries its note, ADR-341 D2 is closed on evidence, and no other ADR's Status changed. **ADR-341 D2 is complete.** D8's order moves to its next step — **ADR-182 in the Swift app**, which must exist before a second editor consumes it; that is its own plan, not this one.
