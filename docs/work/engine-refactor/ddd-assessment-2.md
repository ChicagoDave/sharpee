You checked it and I couldn't. Five payoffs, five refutations — save already serializes exactly the session, bootstrap already gives one engine per playthrough, the guards are Phases 4 and 5, and the registry is a module-global that only moving it fixes, so #393 owns it whether or not a Session type ever exists. I mis-attributed that last one.

The structural error on my side: I argued from a benefits list instead of from the one argument that survives contact with the code. You found that argument yourself and stated it better than I did — a typed handoff between assembly and playthrough in place of an ordering convention. Everything else I listed was either already true or already scheduled, which means it was decoration.

And your disposition is better than my framing. "Its own ADR after the plan closes, judged by whether the IDE's Play surface or the bridge protocol gets simpler" is a scoped decision with an owner and a falsifiable test. I offered a breakthrough; you turned it into a dated question with a criterion. That's the correct handling of a concept that's real but not yet load-bearing.

The one thing I'd add is about making "builds toward Session without committing to it" checkable rather than asserted. It comes down to one property of the Phase 4 install steps: does each step mutate `this.*`, or does it contribute to a value the engine then adopts? If `StoryLoader` produces something — call it a load result — and the engine assigns from it, a later Session constructor consumes the same value and the cutover is mechanical across those five sites. If the steps write engine fields directly, Phase 4's assembly half is the current design with better names, and the later ADR is a redesign that has to relitigate the ordering. That's a cheap constraint to impose now and it's the only thing that decides whether the claim holds.

Related, and available immediately at no public-surface cost: make the install sequence declarative the way `TURN_STAGES` already is. The ordering constraints currently live in comments and line order in `setStory` — "registered BEFORE initializeWorld", "the WORLD is built first" — which is prose guarding an invariant. A named ordered list makes the assembly/playthrough boundary legible without a type, in the house style you already established for the turn. That's most of what I wanted from Session, for none of the cutover.

On `turnEngine()`, your answer dissolves the question. If its width is the seventeen unmoved helper bodies under D5's "private stays private", it's a lagging indicator, not a design decision, and Phase 1 is the fix. My only caveat: as the helpers move, re-judge the getters that remain rather than letting them ride. Some of them are live mutable handles — `turnEvents`, `soundBuffer`, `pendingPlatformOps` — and those stay wrong-shaped even after the surface shrinks.

`PlayerRole` — your version is sharper than mine. "The flag is serialized with the entity while `playerId` is serialized with the world, so a save carries two sources of truth" is the actual defect; I only said three representations. When you file it, I'd put the failure mode in the issue text rather than the redundancy, because redundancy arguments age in a backlog and failure modes get scheduled. The concrete one: `syncPlayerState` treats `world.getPlayer()` as canonical at restore, so a story that sets `ActorTrait.isPlayer` directly without going through `switchPlayer` produces a save whose two sources disagree, and behavior then splits by which one a given call site happens to read. Cross-package, discuss-first, agreed — but that's the sentence that gets it prioritized.

On the validators, you're right and I was wrong. Snippet binding and combatant health aren't checkable until every entity exists; that's not an aggregate failing to protect an invariant, it's a class of invariant that only has meaning at a build boundary. Free functions are the right implementation. What Phase 4 adds is naming that boundary — as install steps they become a phase in the model rather than two calls that happen to sit in the middle of `setStory`. That's the real improvement, and it's already in the plan.

Which leaves Phase 2 as the only thing in my review that was both correct and not already yours.