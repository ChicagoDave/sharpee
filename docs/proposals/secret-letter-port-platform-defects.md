# Proposal: Platform defects surfaced by the Secret Letter port

**Status**: PLANNED — **11 PLANNED, 4 DONE (P-1..P-4, plan Phase 1, 2026-09-06), 1 REJECTED (P-5, tracked as publish-readiness P-11), 0 ACCEPTED, 0 PROPOSED.** `session-planner` ran 2026-09-06, session 24532e ("plan proposal secret-letter-port-platform-defects"), writing `docs/work/secret-letter-port-platform-defects/plan.md` (nine phases) and flipping all fifteen ACCEPTED items to PLANNED per ADR-0008. Acceptance history: `proposal-review` ran 2026-09-06, session 24532e (4 blocking: P-5 DUPLICATE of publish-readiness P-11, P-6(b) and P-9 alternatives contradict ADR-330 / ADR-245, P-14 DECISION-IN-DISGUISE pending an ADR-325 amendment; 8 advisory). Acceptance walked item by item with David the same session — the review's rewords were applied at acceptance (P-6, P-9), every "fix it or diagnose it" alternative was resolved by David's pick (P-7 narrate, P-8 resolve, P-10 declared-state-wins, P-13 splice, P-15 ship both), issue 275 joined P-6 as case (d), and ADR-325 Amendment W1 was written and ACCEPTED for P-14.
**Origin**: issue set — the twenty open GitHub issues filed against the platform while the Secret Letter port built Chapters 1-11 (2026-08-24 through 2026-09-05), plus one older devkit gap (issue 255) in the same testing seam; content summarized per item below. Triaged from the full 86-issue backlog in session 24532e (David: "triage the GH backlog for a set of changes we can put into a proposal", then "go").
**Date**: 2026-09-06
**Session**: 24532e

Every item here has a reproduction in `branch-stories/secret-letter/` and a
story-side workaround already carried in the port. The set is cut by root
cause, not by issue: where two issues share a cause they are one item with two
acceptance checks. Two language-extension issues the triage held back
(run-on phrases and `takes no time` — issues 363 and 263) are design items and
go to a second proposal; bare-verb grammar scoping (issue 317) already lives in
`publish-readiness-defects.md` P-21, pending its ADR-087/267 amendment. Not in this set: issue 347
(multi-NPC conversations, ADR-320 D10 — a feature build with its own plan; note
`publish-readiness-defects.md` records D10 as built while the issue says unbuilt,
a disagreement to settle before that issue is planned),
issues 344 and 356 (Secret Letter content, David's rulings), and — no longer — issue 275
(the character model's clock-mirror off-by-one, found by the theatre story),
which David added to P-6 as its case (d) at acceptance.

## Items

### P-1: A tree document can assert on the `story.chapter` channel
- **Done when**: `channelIdsReferencedBy` in branch-tester resolves an assertion id to the longest registered channel id that prefixes it (or the registry knows `story` as a namespace), so a card with `channels: [{ id: "story.chapter.title", contains: ["Chapter VI"] }]` loads and passes against the chapters extension; a branch-tester test pins it. (Issue 369: today every id is reduced to its first segment, so the one channel that says which chapter the story is in is the one a tree cannot read — chapter-row triggers go untested, which is how P-6's `visits` case shipped unnoticed.)
- **Status**: DONE (2026-09-06, session 24532e — plan Phase 1; #369 closed: bootstrap `resolveDeclaredChannelId` + `capturedChannels`, branch-tester `splitChannelClaimId`; bootstrap 10, branch-tester 114, devkit tree 6 passing); was PLANNED (plan Phase 1, session 24532e)

### P-2: A dotted state claim can name a multi-word entity
- **Done when**: the runner's dotted-claim head accepts a kebab-case IR id and a Chord-spelled multi-word name (`silver brooch.location = player` and `silver-brooch.location = player` both resolve the same entity), with the same lookup the `the silver brooch is worn` form already uses; a branch-tester test pins both spellings. (Issue 375: the head is matched by `\w+`, so every entity whose name has a space has no spelling for its location or any other property; the port's Chapter 9 leaves the jewel, gown, dagger and coin locations unpinned. Review note: ADR-307's issue-355 addendum documents the head as single-word; that paragraph is updated in the same landing.)
- **Status**: DONE (2026-09-06, session 24532e — plan Phase 1; #375 closed: full dotted head + IR-id lookup, ADR-307 addendum sentence corrected; branch-tester 114 passing); was PLANNED (plan Phase 1, session 24532e)

### P-3: The bundle CLI resolves Chord imports
- **Done when**: `node dist/cli/sharpee.js --exec "look" --story branch-stories/secret-letter/secret-letter.story --seed 7` (the README's own command) runs, and `scripts/make-story-artifacts.mjs` regenerates `secret-letter.tests.json` from it; a repo-level script test under `scripts/__tests__/` drives the bundle against an imported story. (Issue 352: `loadChordStory` in the bundle provides no import resolver where devkit's compose, test and play all do, so every story with an `import` fails the load-time gate. Review note: publish-readiness listed the issue as deliberately out because it is the in-repo harness rather than the author path — a scope exclusion, not a rejection.)
- **Status**: DONE (2026-09-06, session 24532e — plan Phase 1; #352 closed: the bundle compiles with devkit's `makeFsImportResolver`; `scripts/__tests__/cli-chord-import.test.ts` 3 passing; the README's secret-letter command runs. The `secret-letter.tests.json` regeneration named here is plan Phase 9's); was PLANNED (plan Phase 1, session 24532e)

### P-4: `sharpee test --tree` honors the root transcript's declared channels
- **Done when**: `loadAuthorGame` takes a channels option and test-tree passes `spec.channels` through to `assembleGame`, so a `[CHANNEL: …]` assertion written from play captures identically under `--tree`; a devkit test runs a tree whose root declares an extra capture channel and asserts on it. (Issue 255: the call at `test-tree.ts` passes only entry and seed, and the loader has no channels parameter, so the field cannot reach bootstrap on this path at all.)
- **Status**: DONE as built (2026-09-06, session 24532e — plan Phase 1; #255 closed as superseded: `test-tree.ts` was replaced by the ADR-307 cutover (31ef79b28), `test-tree-document.ts` already derives the capture set from the document's claims and `loadAuthorGame` already takes `channels`; devkit `test-tree-document` 6 passing); was PLANNED (plan Phase 1, session 24532e)

### P-5: Every Chord clause bound to an action on an entity is consulted, not only the highest-priority one
- **Done when**: (a) an entity composing two traits that each carry an `on the player talking` clause fires both refusals in the documented order (issue 332's fruit stallkeeper: with the mercenaries absent and the stall chaotic, `talk to stallkeeper` prints `cleaning-up`'s refusal, not the greeting); (b) a trait's guarded `on the player asking while <cond>` with the guard false does not consume the owner's `define topics` table (issue 350's dance partner: `ask second partner about the weather` while the partner is `dancing` serves the topic row); the loader's story tests pin both, and Fernhill's Tobias (a clause that is the intended catch-all beside a topic table) stays green. (Cause, both issues: `WorldModel.getInterceptorForAction` sorts an entity's bindings by priority and returns only the first; the Chord loader registers one merged interceptor per trait and action, so on an entity with two Chord traits for the same action exactly one is consulted and the rest are silently dead.)
- **Status**: REJECTED (David, 2026-09-06, session 24532e) — DUPLICATE of `publish-readiness-defects.md` P-11 (issues 332 and 350), which is PROPOSED there pending ADR-118 Amendment 1 (DRAFT, one open question: own block first or composed traits first) and held by the publish-readiness plan's Phase 6a. The live action is that amendment's open-questions interview; nothing is planned from this item.

### P-6: Chord's authored-move narration and event order is audited once, and four cases are fixed against it
- **Done when**: a short document under `docs/architecture/`, titled for authored-move narration and event order (not the closed publish-readiness tick-order audit), states, for an authored move, the order of (arrival narration, the destination's entering clauses, watchers, chapter triggers) and which events an offstage move raises; and four cases pass as tests: (a) an authored `move the player` narrates the room header and description before the destination's `after the player entering` text, matching the walked order and ADR-326 D5's addendum (issue 367: today the reaction text prints above the room, on `enter hole`, `follow bobby`, `follow olmer`); (b) `begins when the player visits <room> for the first time` fires when that room's own entering clause moves the player on in the same turn, by riding the arrival event as ADR-330 states rather than reading the end-of-action location (issue 368: Chapter VI's row could never begin); (c) `move <entity> offstage` raises `when <entity> moves`, and ADR-325 D3h's "rides the actor-moved event `going` emits" wording is amended in the same landing to cover authored moves (issue 373: the gallows watcher on the butler never fired); (d) a subject change made by the player asking a different topic seizes an authored `when the subject changes:` initiative row — the scenes sub-step's `subjectChangedTurn` stamp and its `clockTurn` comparison are put on one scale (issue 275: the mirror advances at phase entry, so an action-side stamp of T is compared against T+1 and never matches; only tick-side stamps could ever fire); story-loader tests pin (a)-(c), a character test pins (d). (Sessions 20260904-2310 and 20260905-1007 both flagged (a)-(c) as one recurrence class — dispatch order surfacing as one-off issues per chapter — and recommended one audit over more individual issues. Review 2026-09-06: the "or refuse the trigger" and "or document it as not a move" alternatives were dropped as contradicting ADR-330 and freezing an accident in ADR-325; David added issue 275 as case (d).)
- **Status**: PLANNED (David, 2026-09-06, session 24532e — with both rewords and issue 275 added as case (d); planned into `docs/work/secret-letter-port-platform-defects/plan.md` Phase 2, session 24532e)

### P-7: Region daemons and offstage-owned timers narrate from where the guide says they do
- **Done when**: (a) a region's `on every turn` clause fires only while the player is in a member room, as the regions guide states (issue 365: with the player in the Street, a daemon on the `Inner` region flipped Toresal to night and pulled Bobby into a non-member room every turn, silently); (b) a timer owned by an unplaced entity speaks its turn bodies wherever the player is (issue 372: the raid's `overheard` timer ran and expired on schedule but none of its five rows spoke; re-owning it to the player spoke all five); story-loader tests pin both. (Review 2026-09-06: (a) is a defect against ADR-236 D4's "presence = the player is in the region"; the compile-time-warning alternative for (b) was dropped — David chose narrate-from-the-player.)
- **Status**: PLANNED (David, 2026-09-06, session 24532e — (b) as narrate-from-the-player; planned into `docs/work/secret-letter-port-platform-defects/plan.md` Phase 3, session 24532e)

### P-8: `select on <entity>` inside a clause body runs the matching arm
- **Done when**: issue 370's winch (`select on the gaudy chandelier` with `when raised` / `when lowered` arms over a reversible state) changes the state and speaks the arm's phrase on `turn winch` — the entity subject resolves to its declared state, as the EBNF's `select on value-expr` already admits; a story-loader test pins it. (Today the block compiles clean and runs no arm; the port rewrote it as five guarded statements with a pivot state. Review 2026-09-06: the refuse-and-diagnose alternative was dropped — David chose resolve-the-state.)
- **Status**: PLANNED (David, 2026-09-06, session 24532e — resolve-the-state; planned into `docs/work/secret-letter-port-platform-defects/plan.md` Phase 4, session 24532e)

### P-9: A `stopping` phrase's progress belongs to the phrase, not the call site
- **Done when**: two clauses sharing one `stopping` phrase (issue 371's butler: `give letter to butler` then `show letter to butler`) speak the first arm once and then the second, and the same holds for `first-time` and `cycling`; a story-loader test pins it and the phrase reference states the rule. ADR-245 already places variant counters on the phrase entry ("cycling/first-time/sticky firing counters belong to the entry, not the bare key"), so per-call-site progress is the defect this restores. (Review 2026-09-06: the per-site-semantics fallback sentence was dropped as contradicting ADR-245.)
- **Status**: PLANNED (David, 2026-09-06, session 24532e — with the reword; planned into `docs/work/secret-letter-port-platform-defects/plan.md` Phase 5, session 24532e)

### P-10: A declared state wins over a colliding platform word, never a silent shadow
- **Done when**: `states: fresh, seen` on an entity works as declared — conditions on `seen` hold after `change … to seen` — on the rule issues 336 and 361 apply, that a declared name wins over the platform's own reading of the word; a story-loader test pins it, and a chord test pins that the analyzer resolves the entity's declared states ahead of the colliding platform notion. (Issue 366: `x coin` twice printed the base description both times with no error; renaming the states to `shiny, examined` fixed it. Review 2026-09-06: the collision-diagnostic alternative was dropped — David chose declared-state-wins.)
- **Status**: PLANNED (David, 2026-09-06, session 24532e — declared-state-wins; planned into `docs/work/secret-letter-port-platform-defects/plan.md` Phase 4, session 24532e)

### P-11: A declared name containing `and` or `&` reads in conditions and `change`
- **Done when**: `while the Sandler and Sons is new` and `change the Sandler and Sons to greeted` parse, on the rule issue 336 already gave possessives — the whole phrase spelling a declared entity wins over the connective split; a chord parser test pins both `and` and `&`. (Issue 361: the declaration, exit line and placement accept the name; only conditions and `change` refuse it. Shops, firms and pairs are named this way in ordinary prose.)
- **Status**: PLANNED (David, 2026-09-06, session 24532e — planned into `docs/work/secret-letter-port-platform-defects/plan.md` Phase 4, session 24532e)

### P-12: A timer read is accepted on `phrase detail while` as it is on a clause head
- **Done when**: `phrase detail while the player's pole-destruction has started:` compiles where the same read already passes on `on the player listening` and `after the player entering while …`; a chord analyzer test pins it, and the same test records whether a bare entity state word (`phrase detail while huddled:`) is accepted or refused by design. (Issue 359: `analysis.unknown-timer` on the detail line only.)
- **Status**: PLANNED (David, 2026-09-06, session 24532e — planned into `docs/work/secret-letter-port-platform-defects/plan.md` Phase 5, session 24532e)

### P-13: `{phrase}` markers splice in entity descriptions as they do in room prose
- **Done when**: a scenery entity whose description carries `{lantern-night}The lampposts {lantern-tail}.` renders the gated phrases on `x lanterns` exactly as a room description does; a story-loader test pins it. (Review 2026-09-06: the refuse-and-diagnose alternative was dropped — David chose splice; the publish-readiness precedent that refused a marker was phrase-in-phrase, not description prose.) (Issue 364: today the marker text prints verbatim and nothing warns; the port carries Bobby's presence lines as gated markers in each room's description instead of on Bobby because a `phrase present:` override cannot vary by condition either.)
- **Status**: PLANNED (David, 2026-09-06, session 24532e — splice; planned into `docs/work/secret-letter-port-platform-defects/plan.md` Phase 5, session 24532e)

### P-14: A runtime statement puts a garment on an actor or takes it off
- **Done when**: `make <actor> wear <item>` and `make <actor> take off <item>` land as ADR-325 Amendment W1 decides (W1a-W1g: `move`-family puts; the item moves to the actor through the move lifecycle then `WearableBehavior.wear`; `take off` leaves it in hand; `the player` is a legal actor; `analysis.wear-not-wearable` / `analysis.wear-actor-not-person` at compile time; additive IR kinds), so that Commerce Street's arrival block reads `make the player wear the woolen cap` and `while the woolen cap is worn` reads true afterward without the player typing `wear cap`; the grammar changelog, the guide's statements page and `genai-api` record both statements; story-loader tests pin wear, take off, the already-worn and not-worn no-ops, and the two diagnostics. (Issue 360: `wears` is declaration-only and `move` can only hand the item over; the source's own block does `now the player wears the gray cloak`. Review 2026-09-06: DECISION-IN-DISGUISE — the statement's spelling is language surface; the ADR-325 amendment was written and accepted in the same session, so the item is accepted under ADR-0009. The acting statement is not the answer: ADR-329 D1 excludes `the player` as an actor and acting narrates; this is a put, D7's family.)
- **Status**: PLANNED (David, 2026-09-06, session 24532e — after ADR-325 Amendment W1, ACCEPTED the same session; planned into `docs/work/secret-letter-port-platform-defects/plan.md` Phase 6, session 24532e)

### P-15: `sleeping` and `waking` ship as standard actions
- **Done when**: stdlib ships `sleeping` and `waking` as four-phase standard actions with the stock English refusals ("You aren't tired", "You're already awake") in lang-en-us and the parser's verb patterns (`sleep`, `go to sleep`, `go to bed`, `lie down`; `wake`, `wake up`), so `on the player sleeping` loads and a Chord story reacts to them without a `define action`; stdlib tests pin both actions, and the Secret Letter's Maiden House drops its `define action sleeping` in favour of `on the player sleeping`. (Issue 362: the ids are declared, the loader refuses the clause with a good message, and nothing in the guide distinguishes a standard action from a reserved id. Review 2026-09-06: the drop-the-ids alternative was dropped — David chose ship-both.)
- **Status**: PLANNED (David, 2026-09-06, session 24532e — ship both as standard actions; planned into `docs/work/secret-letter-port-platform-defects/plan.md` Phase 7, session 24532e)

### P-16: An unplaced entity never wins scope over a carried one
- **Done when**: with `the dress` unplaced and `the daydress` (aka `dress`) in the player's inventory, `wear dress` puts the day dress on and never reaches the offstage entity's intercept, and the same rule holds for every action's `:item` slot (an entity with no location is out of scope, and a carried match wins); stdlib scope tests pin it. (Issue 374: `x dress` already excludes the unplaced entity; wearing does not.)
- **Status**: PLANNED (David, 2026-09-06, session 24532e — planned into `docs/work/secret-letter-port-platform-defects/plan.md` Phase 8, session 24532e)
