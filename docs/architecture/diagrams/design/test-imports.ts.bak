/**
 * Simple build test for engine package
 * This file tests if basic imports work
 */

// Test core imports
import { EventEmitter } from '@sharpee/core';

// Test world-model imports  
import { WorldModel, IFEntity } from '@sharpee/world-model';

// Test event-processor imports
import { EventProcessor } from '@sharpee/event-processor';

// Test stdlib imports
import { 
  ActionRegistry,
  standardActions,
  vocabularyRegistry,
  LanguageProvider
} from '@sharpee/stdlib';

// Basic test
console.log('Imports successful');
console.log('Standard actions:', standardActions.length);
console.log('Vocabulary registry:', typeof vocabularyRegistry);
