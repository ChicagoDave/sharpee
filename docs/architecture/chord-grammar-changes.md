# Chord Grammar Changes — Owner-Approved Log

Governance log for the Chord story language grammar (ADR-210, Open Question 4).
The grammar is a closed set with one canonical form per concept (Given 7).
Any addition or change to the grammar beyond what `docs/work/story-language/design.md`
records as normative requires David's explicit approval, logged here as a dated
entry — a one-way ratchet: entries are never removed, only superseded by later
entries.

**What counts as a grammar change:** a new keyword, statement, selector form,
strategy adverb, kind noun, trait adjective, or any second way to express an
existing concept. Renames and removals count too.

**What does not:** new phrase keys, story content, loader internals, IR schema
changes that don't alter surface syntax, and diagnostics wording.

## Entries

| Date | Form (syntax) | Rationale | Example | Decision |
|------|---------------|-----------|---------|----------|
| 2026-07-10 | **Prose block is the only phrase-text form** — the same-line value in `define phrase` / `phrases <locale>` entries (both the quoted and the bare-text variants) is removed (parse error `parse.phrase-text-form` with a fix-it pointing at the block form). Quoted strings remain for non-prose atoms only: story header strings, hatch module paths. | Given 7 (one canonical form per concept): the quoted one-liner duplicated the prose block and the choice between them tracked line length, not meaning. Raised by David against cloak.story §3.1. | `stumble:` ⏎ `  Blundering around in the dark isn't a good idea!` | Approved (David, 2026-07-10). Supersedes design.md §3.1's quoted examples; §3.1 amended. |
| 2026-07-10 | **Blank line inside a prose block = paragraph break.** A blank line no longer terminates the block while the following line is still indented to the block's level; each paragraph maps to its own text block downstream. In `create` blocks, all consecutive bare paragraphs form the description. | Prose-first: authors type paragraphs the way prose works; the platform's text-block unit *is* the paragraph. | Two indented paragraphs under a room → two-paragraph description | Approved (David, 2026-07-10). |
| 2026-07-10 | **`{br}` built-in marker = hard line break** (flush stacking, no paragraph gap) inside phrase/description prose. `br` is reserved: the unbound-marker gate whitelists it; a story cannot declare a phrase or hatch named `br`. | Verse/signs/addresses need tight line stacking; markers are Chord's one inline-escape form, so this adds a concept, not a second form. | `Here lies the grue.{br}It could not see in daylight.` | Approved (David, 2026-07-10). |
| 2026-07-11 | **Open conditions as selections** — `define condition` (existing form, unchanged) now has two classified uses: a condition whose body never references `it` is CLOSED (a truth test, as today); one that references `it`/`its` is OPEN — a selection criterion over entities. New reference form: `any <condition-name>` in `when`-rule target position and in thing-positions (`has`/`holds`/`wears` objects); `each <condition-name>` reserved for iterate/cardinality positions when those constructs land. Never-guess gates: an open condition as a bare `while` test with no `it` in scope is a load error; `any <name>` naming a closed condition is a load error. | David proposed named selection criteria for `when` clauses (2026-07-11); extending `define condition` instead of adding a `define selection` declaration keeps one form per concept (Given 7) and reuses the closed condition kit as the criteria language — no new expression grammar. | `define condition small-animal: it is pettable and it is in the Petting Zoo` ⏎ `when the player pets any small-animal` | Approved (David, 2026-07-11 — "I'd extend define condition"). Condition names stay single hyphenated tokens (key namespace: phrases/flags/scores/conditions; prose multi-word names remain entity-only) — David, 2026-07-11. Implementation: reference-form analysis in Phase B phase 3, evaluation with the loader phases; `each` form deferred to the iterate/cardinality constructs. |
| 2026-07-10 | **Conditional blocked exits** — `<direction> is blocked while <condition>: <phrase-key>` in `create` room blocks; the refusal applies only while the condition holds (evaluated per movement attempt / recompute, like `dark while`). The unconditional form is unchanged. | Dungeo conversion sweep (docs/work/chord/dungeo-conversion.md §5.2): ~10 transformer-based movement gates and 8 refusal pseudo-actions collapse into this one declarative, load-checkable form. Approved for Phase B scope even though Zoo doesn't need it. | `east is blocked while the player has the basket: too-wide` | Approved (David, 2026-07-10). |
| 2026-07-11 | **Ownership-package removals (Phase C; givens 8+9).** Removed, each with a parse-error fix-it naming its replacement: top-level `when` rules (→ `on`/`after` clauses on the owner), top-level `once <cond>` rules (→ `, once` clause modifier), top-level `every N turns` rules (→ story-owned schedule forms; `define sequence` stays), `define flag` (→ states or derived conditions), the `flag` trait-field type (→ trait-declared states), `if`/`else`/`end if` (→ `must` guards, statement `when` suffix, `select`), `refuse when` with top-level `not` (→ `must`, gate `analysis.negated-requirement`), and top-level `define score X worth N` (→ owner-attached `score … worth N`; CP5). Unchanged-only-extended list in ratchet-drafts.md §Unchanged. Removals land only together with the replacement entries below (one package; both stories migrate inside Phase C). | Givens 8+9; DDD review; decisions 3/6/7/8. | `define flag after-hours starts false` → parse error with story-states fix-it | Approved (David, 2026-07-11 — Phase C P1 checkpoint, "Approve all as drafted"; CP5 = remove `define score`). |
| 2026-07-11 | **D1 — State adjectives**: `open`, `closed`, `locked`, `unlocked`, `on`, `off`, `worn`, `lit` join the condition kit as `is [not] <adj>` predicates, read live from world trait state (OpenableTrait.isOpen, LockableTrait.isLocked, SwitchableTrait.isOn, the worn relation, computed light). Same closed-catalog governance as capability adjectives. | Leg 1: derivable facts must be derivable or authors shadow them into flags (`gate-closed`). | `south is blocked while the staff gate is closed: staff-gate-blocked` | Approved (David, 2026-07-11). |
| 2026-07-11 | **D2 — Story states**: `states:` line in the story header; the story starts in the first declared state; `change the story to <state>`; bare state names remain valid condition refs. | Leg 2: story phases are a state machine, not flags. | `states: open, after-hours` | Approved (David, 2026-07-11). |
| 2026-07-11 | **D3 — `on`/`after` clause split**: `on <verb> it` intercepts (may `refuse`; its `phrase` is the primary message); `after <verb> it` reacts (`refuse` is a parse error inside `after`; its `phrase` appends). Terminators `end on`/`end after`. Mechanism (CP2, spike-confirmed 2026-07-11): standard verbs → interceptor postExecute/postReport append; dispatch verbs → after-clauses compile into the dispatch action's report phase (loader-internal; interceptor hooks empirically do NOT fire on the dispatch path; zero platform changes). | Decision 1: the §5.4 routing boundary drawn on the page. | `after entering it` ⏎ `  award visit` ⏎ `end after` | Approved (David, 2026-07-11 — CP2 loader-internal confirmed). |
| 2026-07-11 | **D4 — `states, reversible:`**: state sets are forward-only by default (backward `change` = load error `analysis.irreversible-state`); the single-word modifier `reversible` permits any declared-to-declared transition. | Decision 2: back-transitions are a defined capability. | `states, reversible: hungry, content` | Approved (David, 2026-07-11 — word `reversible` confirmed). |
| 2026-07-11 | **D5 — `, once` clause-header modifier**: gates a whole `on`/`after`/every-turn clause to one lifetime firing; `first time` body ordinals stay (different scopes, both canonical at their scope). | Decision 4. | `on every turn while after-hours, once` | Approved (David, 2026-07-11). |
| 2026-07-11 | **D6 — `must` requirements**: `<subject> must <predicate>: <phrase-key>` as a `define action` line and a body statement; failure ⇒ refusal with the key. Companion gate: `refuse when` with top-level `not` = load error `analysis.negated-requirement`, must-form fix-it. `refuse without`/`otherwise refuse` unchanged. | Decision 6 ("single closed"): one form per polarity. | `the actor must have its food: no-feed` | Approved (David, 2026-07-11). |
| 2026-07-11 | **D7 — statement `when` suffix**: `<statement> when <condition>` (legal on `award`/`phrase`/`emit`/`move`/`change`/`win`/`lose`) executes only if the condition holds; not a block form. Positionally distinct from select-arm `when <value>` (statement-final vs arm-header) — homonym noted. | Decision 7: the moment-conditional replacement for removed `if`; keyword freed by the when-rule removal. | `award farewell when the player can see it` | Approved (David, 2026-07-11). |
| 2026-07-11 | **D8 — trait-declared states**: `states[, reversible]: …` as a `define trait` line; every composer gets the set; resolution across the composer's full trait set (another composed trait may reference them — `restless` reads feedable's `hungry`); same state name from two composed traits = load error `analysis.state-collision` with rename fix-it. The `flag` field type is removed from trait data. | Decision 5: booleans gone at every scope. | `define trait feedable` ⏎ `  states, reversible: hungry, content` | Approved (David, 2026-07-11). |
| 2026-07-11 | **D9 — three-ring boolean-state gate** (pattern detection over every declared state set): ring 1 `analysis.boolean-state` — literal `true`/`false`/`yes`/`no`; ring 2 `analysis.shadow-state` — platform pairs (`open`/`closed`, `locked`/`unlocked`, `on`/`off`, `lit`/`unlit`, `worn`/`unworn`), fix-it: compose the owning trait; ring 3 `analysis.negated-state` — negation-shaped pairs (`not-`/`un-`/`non-`/`no-` prefixes, `-less`, shared-stem pairs), encouraging fix-it: "a state names what the thing IS, never the absence of another state" with the `fed`→`hungry`/`content` worked example. All errors. | Decision 8 + clarification: catch pos/neg states, encourage real states. | `states: fed, unfed` → ring-3 error | Approved (David, 2026-07-11). |
| 2026-07-11 | **D10 — sequence step anchors**: `when <owner> becomes <state>` joins `at turn N`/`N turns later` as a step header; the step arms on the transition; later relative steps chain from its firing; `at turn N` stays absolute from story start. | Decision 9: timelines chain off phases, not just the wall clock. | `when the story becomes after-hours` | Approved (David, 2026-07-11). |
| 2026-07-11 | **D11 — owner-scoped narration** (semantics entry, no new syntax): story-owned schedules broadcast; entity-owned every-turn clauses fire only when the player is in the owner's location (presence, not sight); `, once` cannot be consumed unwitnessed; RNG conditions in entity clauses roll only on stage. | Decision 10: a phrase's reach follows its owner. | goats' confession fires when the player arrives | Approved (David, 2026-07-11). |
| 2026-07-11 | **D12 — owner-attached scores**: `score <name> worth N` as a line in `create` blocks (any entity), `define trait` blocks, `define action` blocks, and the story header; identities are owner-scoped (goats' `fed` ≠ rabbits' `fed`); max score = load-time sum; `award <name>` in an owner's clauses resolves to the owner's score. Top-level `define score` is removed (CP5). | Decision 3: the owner provides the namespace. | `create the zoo map` ⏎ `  score collected worth 5` | Approved (David, 2026-07-11 — CP5 remove). |
| 2026-07-11 | **D13 — sequence-scope mutation rule**: sequences (story-owned) may mutate world state — `change` on the story or any named entity, and `move` — plus `phrase`/`award`/`emit`/`win`/`lose`; they bind no `it`. Supersedes the proposal's narrower leg-2 recommendation. | CP6: the approved sketch is the spec where the two disagreed. | `change the pygmy goats to hungry` from `define sequence feeding time` | Approved (David, 2026-07-11 — CP6 confirmed). |
| 2026-07-11 | **Given 9 adopted — stickiness: all behaviors belong to "something."** Floating rule forms (top-level `when`, `once`, `every`) are slated for replacement by owner-attached clauses (on-clauses on entities; schedules/states on the story object); orphan global phrase keys migrate to per-entity phrases. Like the Given 8 entry below, this records the decision — the replacement surfaces (story object, clause modifiers, reaction-vs-interception marking) are the Phase C design package (docs/work/chord/phase-c-ownership-proposal.md); removals land together with replacements. Type definitions (`define trait`/`define action`) stay top-level: they declare kinds, not instances of behavior. | Phase B DDD review + David's principle statement (2026-07-11): "all behaviors belong to 'something'. Stickiness is a foundational aspect of DDD." Chord shipped a sticky system and an unsticky system in parallel; every review smell lives in the unsticky half. | `when the player enters the Aviary … end when` → on the Aviary: `on entering it …` | Approved as Given 9 (David, 2026-07-11 — "Promote it"). Implementation deferred to Phase C design. |
| 2026-07-11 | **`define flag` removed — global true/false flags forbidden (Given 8).** The declaration, `set <flag> to true/false`, and bare flag refs in conditions leave the grammar. Facts are either derived (conditions over world state) or owned (data/`states:` on an object). Replacement surfaces are design work before removal lands: state adjectives in the condition kit (`is open/closed/locked/on` — kills the `gate-closed` shadow-flag class) and story-scoped state (the "story object" direction, under exploration) for phases like `after-hours`. Zoo migrates when those exist; removal + replacements ship together so no story is stranded. | Phase B DDD review (docs/work/chord/ddd-review.md §3.1a): every global boolean shipped in Zoo was a disguised state machine — a shadow copy of entity state, a flattened story phase, or a lifecycle-less period. | `define flag after-hours starts false` → (removed; future: `states:` on the story) | Approved as Given 8 (David, 2026-07-11 — "global true/false flags are forbidden"). Implementation deferred to the replacement-design phase; this entry records the decision, not a landed grammar. |
| 2026-07-10 | **`verbatim` phrase modifier** — rides the `define phrase <name>, <modifier>` comma slot beside the strategy adverbs (mutually exclusive with them); the phrase's text is exempt from whitespace collapse (maps to phrase-algebra `whitespace: 'verbatim'`). | Preformatted text (ASCII maps, letters) cannot survive prose collapse; the modifier slot already exists. | `define phrase treasure-map, verbatim` | Approved (David, 2026-07-10). |
| 2026-07-12 | **D6 extension — `refuse when <condition>: <phrase-key>` as a body statement** (clause/trait/action bodies), same form as the define-action line in a new position; parse error inside `after` clauses (reactions cannot refuse, per D3); the D6 negated-requirement gate applies in body position too. | Removing `if` (ownership-package removals, 2026-07-11) left body-level prohibitions inexpressible — the carrying-limit case. Shipped in Phase C P2 as an implementation finding; ratified here. | `refuse when the taker holds the flimsy basket: hands-full` | Approved (David, 2026-07-12 — ratified as part of D6's as-shipped state). |
| 2026-07-12 | **E1 — `any <open-condition>`: existential condition** (each package; a REBUILD — the reserved form's parse code did not survive the ownership package's when-rule removal). Legal wherever a condition is legal (`while`, statement `when` suffixes, `refuse when`, `must` predicates): true iff some world entity satisfies the named OPEN condition (enumeration domain: all entities, player and rooms included — the condition filters); false over the empty set. Never-guess: `any <closed-condition>` is a load error; a bare open condition in truth position stays `analysis.open-condition-truth`, its fix-it now naming a live form. | The open-conditions entry (2026-07-11) reserved the reference form; the each package (proposal: docs/work/chord/each-iteration-proposal.md, checkpoints resolved 2026-07-12) lands its live hosts. | `define condition alarm-trigger: it is a treasure and the player holds it` ⏎ `on entering it while any alarm-trigger` | Approved (David, 2026-07-12 — each-package P1 sign-off). |
| 2026-07-12 | **E2 — `no <open-condition>`: negated existential**, its own positive spelling (never `not any` — one form per polarity, the D6 precedent); true over the empty set. Universals ride the no-dual: the condition names the violation (`no stray-treasure`) — there is deliberately NO `every` form (David, 2026-07-12). `refuse when no <name>: <key>` is legal (positive form); `refuse when not any <name>` stays a negated-requirement error. | Victory-shaped set-emptiness tests without vacuous-truth semantics or a domain/predicate split. | `define condition stray-treasure: it is a treasure and it is not in the trophy case` ⏎ `award endgame when no stray-treasure` | Approved (David, 2026-07-12 — each-package P1 sign-off). |
| 2026-07-12 | **E3 — `each <open-condition> … end each`: body-position iteration block.** Hosts: `on`/`after` clause bodies, action bodies, trait clause bodies, sequence steps — never top-level (Given 9); the block inherits its host clause's presence gate, `, once`, and RNG staging. Binder: **`the match`** (new value form) is the iterated entity; `it` keeps meaning the clause owner; nested `each` binds innermost; `the match` outside an `each` body = `analysis.match-outside-each`. Enumeration in creation order (deterministic for transcripts and RNG); empty set = no-op; body takes the normal statement kit (`refuse` legality follows the host: legal in `on`, error in `after`). Never-guess: `each <closed-condition>` is a load error. Fenced OUT of the language by the same decision: numeric cardinality/comparisons (`holds <n> or more`, `is at least <n>` — Given 5) and inline descriptions (named conditions only). | Bulk state changes and per-match narration without authored counting; design.md §2.4's `iterate through each <description>` sketch is superseded by this named-condition form. | `after feeding it` ⏎ `  each hungry-neighbor` ⏎ `    change the match to content` ⏎ `  end each` ⏎ `end after` | Approved (David, 2026-07-12 — each-package P1 sign-off). |
| 2026-07-12 | **Phase C ownership package — as-shipped reconciliation** (no new syntax; final state of the 2026-07-11 entries). All removals + D1–D13 landed as approved across Phases 2–5; both shipping stories (cloak, zoo) migrated; Cloak gate 81/81, Zoo gate 79/79. As-shipped notes: (1) event-verb register is the gerund — `after entering it` (D3's `<verb> it` clause form); the catalog still holds exactly one event verb, `entering`. (2) The `any <condition>` reference form (open-conditions entry, 2026-07-11) is DORMANT — its only host position (when-rule targets) was removed by this package; it revives with the `each`/iterate backlog. (3) Duplicate same-trigger clauses on one owner: load error keyed strictly for interceptor/dispatch-routed verbs (keyed registration = real masking), and by span-free condition fingerprint for event-verb clauses (condition-differentiated clauses are legal; identical conditions error) — approved by David 2026-07-11 during P3. (4) `select on` arm checking stays arm-validity-only, extended over trait scopes; a missing-arm completeness gate was not requested and would need its own entry. | Phase C P6 close-out: the log's Phase 1 drafts flipped to their final as-shipped state, deviations dated per the append-only discipline. | — | Recorded (Phase C P6, 2026-07-12); underlying approvals dated 2026-07-11/12 above. |
| 2026-07-12 | **Each package (E1–E3) — as-shipped reconciliation** (final state of the three 2026-07-12 entries above; per the append-only discipline they stay as written). E1/E2/E3 landed as approved across P2–P4 (chord frontend → analyzer/IR → loader evaluator/runtime; fixture-story e2e, no shipping-story migrations — Cloak 81/81 and Zoo 79/79 confirmed untouched through the rebuilt bundle). As-shipped notes, each individually decided by David during P3 (2026-07-12): (1) **`must be any <name>` parses with MEMBERSHIP semantics** — E1's "must predicates" leg shipped as `<subject> must be any <open-condition>: <key>`, the subject satisfying the named open condition (its `it` bound to the subject); standalone-name rule, same as the condition-position quantifiers; IR carries a `satisfies` condition kind (a dated addition to the proposal §3.5 contract). `must be no <name>` is `parse.must-negative` — a negated requirement in disguise, the D6 stance. (2) **`match` is a reserved declaration name** (`analysis.reserved-name` on an entity name, alias, trait data field, or grammar slot) — the binder resolves before entity lookup exactly as `it` does; multi-word names containing the word stay legal. (3) Enumeration domain pinned in implementation: all IR-declared entities in DECLARATION order (= the loader's instantiation order — deterministic across save/restore, the AC-5 guarantee inside `each` bodies); `any` = false / `no` = true / `each` = no-op over the empty set; the pre-mutation match-set snapshot makes the report pass visit the execute pass's set (§5.4). (4) `is` comparisons with `the match` as subject skip static state validation (the set is unknowable — any entity may match); the runtime resolves the word live. Evaluator side (not grammar): `is a` classification landed, over IR kind nouns — platform kinds only (room/container/person/supporter are all the loader instantiates; `treasure`-style story kinds remain future scope with the loader's kind set). | Each package P5 close-out: E1–E3 flipped to as-shipped, deviations dated per the append-only discipline (Phase C P6 precedent). | `the gift must be any penned-crate: not-penned` | Recorded (each package P5, 2026-07-12); underlying approvals dated 2026-07-12 above and in each-iteration-proposal.md §6.5. |
| 2026-07-12 | **Z1 — `first time` prose block in room `create` blocks**: a `first time` block whose body is bare prose is the first-VISIT description; following bare paragraphs remain the standard description. Compiles to `RoomTrait.initialDescription` — the platform's exact semantics (first `look` shows it, later looks show the standard text). Never-guess: `second time`/`third time` at create scope are load errors (no platform field); `first time` prose on a non-room is a load error until a platform surface exists. Z2 markers are legal in BOTH texts and share entries + counters (ADR-209 q5 / ADR-211 Q6, AC-9). | Reuses the existing ordinal concept (`first time` body blocks) at a new scope rather than minting a keyword (`initially`) — Given 7. | `create the Zoo Entrance` ⏎ `  a room` ⏎ `  first time` ⏎ `    Your family piles out of the car…` ⏎ ⏎ `  You stand before the wrought-iron gates…` | Approved (David, 2026-07-12 — zoo-surfaces proposal, re-confirmed in ratchet-drafts v2). |
| 2026-07-12 | **Z2 — description markers splice strategy phrases; fragments are BARE; the platform owns every separator** (ADR-211, accepted 2026-07-12). Room description prose (either text) may carry `{<key>}` naming a `define phrase` with a strategy; every variant is a bare fragment — a variant beginning with punctuation/whitespace is a load error with the fix-it "write the fragment bare — the separator is platform-owned"; a clause-site fragment ending in a sentence terminator is a lint warning. `nothing` as a variant's ENTIRE body is the explicit empty variant (reserved in that position; blank variants are never guessed). Joining is site-determined at render, never authored: clause site → `, `, sentence site → ` `, empty → nothing (ADR-211 Decision 2). NEW grammar position: an optional trailing `while <condition>` on the `define phrase` header gates the fragment (CP1') — a presence condition (`is in <room>`, Z4 `is here`) compiles to ADR-209 `mentions` data; ANY other condition registers on the ADR-211 gate seam (Q4: full generality, no presence-only error). Loader compiles marker sites onto ADR-209 storage/keying: counters key `(roomId, marker)`; the same phrase emitted via a `phrase <key>` statement keeps its own `('chord', key)` counter and gets NO separator (ADR-211 Q6/AC-8). Never-guess: a `verbatim` phrase at a description marker is a load error; an unresolvable `{key}` stays `analysis.unbound-marker`; a single-variant plain phrase at a marker site compiles to a string entry. | David (2026-07-12): "the author shouldn't care about splicing in commas that connect to an existing sentence. The phrase evaluator should understand how to handle that" — the concern that produced ADR-211 (Q1 full unification: the same bare-fragment rule governs the TS surface). Zero new definition syntax; the strategy-phrase kit already expresses variant selection. | `define phrase pins, cycling` ⏎ `  and a spinning rack of enamel pins wobbles by the register` ⏎ `or` ⏎ `  nothing` ⏎ `end phrase` — prose: `…and postcards{pins}. A large…`; gated: `define phrase keeper-note, cycling while the zookeeper is here` | Approved (David, 2026-07-12 — ADR-211 Q1/Q2/Q4 + ratchet-drafts v2 CP1'). |
| 2026-07-12 | **Z3 — occupant lifecycle channel family**: reserved phrase keys on entities, all optional, platform-PULLED (emitting one via a `phrase` statement in a body is a load error; channels are never pushed): `present` — feeds the room description's `{slot:here}` (ADR-195 S1) while the owner is in the described room, one contribution per owner, bare content, slot owns joining, contribution order = declaration order; `entered` — narrated on witnessed arrival into the player's room; `exited` — narrated on witnessed departure to another room; `disappeared` — narrated when the owner leaves play while the player is in its room. All firing witnessed-only per D11. Channel phrases may carry a strategy adverb with `or`-variants (`phrase present, cycling:` — CP3; the Z5 adverb set). All four keys' runtime ships (CP4), loader-internal (`move`/removal paths). The keys join the `br`/`match` reservation class (`analysis.reserved-name`). | David: "the author writes ALL the versions of the zookeeper" — arrival, presence, departure, vanishing are one narration family. Zero new syntax beyond the reservations; zoo.story's four dormant `presence` blocks migrate one word to `present`. | `create the zookeeper` ⏎ `  a person` ⏎ `  in the Main Path` ⏎ ⏎ `  phrase present:` ⏎ `    Sam the zookeeper is here, jingling a ring of keys.` ⏎ ⏎ `  phrase exited:` ⏎ `    Sam heads off, keys jingling.` | Approved (David, 2026-07-12 — CP3 keys + channel adverbs now; CP4 all four). |
| 2026-07-12 | **Z3b — `phrase detail while <condition>:` on entities**: each gated block contributes its text to the examine `{slot:detail}` while the condition holds (`it` = the owner). Multiple `detail` blocks per owner are legal (independent contributions, declaration order — the one place a key repeats). Full condition kit (CP5'): `while it is on` → `SwitchableTrait.detailWhenOn`, `while it is lit` → `LightSourceTrait.detailWhenLit` (the shipped trait fields); ANY other condition registers a loader-registered state-clause provider — `while` on prose is never artificially restricted (the Q4 rule). `phrase detail` with no `while` is a load error (unconditional detail belongs in the description). `detail` joins the reserved-key class. | Trait-config prose (`with detailWhenOn = …`) would put prose on a composition line; a gated channel phrase keeps text in phrase position. CP5' chose full generality for consistency with ADR-211 Q4. | `create the flashlight` ⏎ `  light-source, switchable` ⏎ ⏎ `  phrase detail while it is on:` ⏎ `    It clicks faintly as it powers up.` | Approved (David, 2026-07-12 — CP5' full condition kit). |
| 2026-07-12 | **Z4 — `here` deictic joins the condition kit** (ADR-211 Q3): `while <entity> is here` is a well-formed condition wherever `while` parses — one predicate, one choke point, two evaluation contexts: on a description-fragment gate `here` denotes the DESCRIBED room (compiling to `mentions` when it is the marker's own room — ADR-211 AC-11, byte-identical to `is in <this room>`); everywhere else it denotes the player's current room (D11 `playerPresentAt` / transitive containment). Never-guess: `here` with a non-entity subject is a load error; an entity with no location evaluates false. | Q3 resolution: minted now alongside `is in <room>`, not deferred; kit-wide keeps the condition language uniform (no context-forbidden spellings) and both evaluation primitives already exist. | `define phrase keeper-note, cycling while the zookeeper is here` — or in a rule: `on every turn while the parrot is here` | Approved (David, 2026-07-12 — ADR-211 Q3; kit-wide scope per ratchet-drafts v2). |
| 2026-07-14 | **Z6 — `remove <entity>` statement**: takes the entity out of play entirely — compiles to `world.removeEntity`; ADR-213's pre-removal observers fire; a witnessed `phrase disappeared:` on the entity narrates per Z3 (player's room = the entity's last containing room; unwitnessed removals narrate nothing and consume nothing, D11). Removal is permanent — no statement restores a removed entity. Legal wherever `move` is (clause/action/trait bodies, sequence steps — joins D13's sequence mutation kit) and takes the D7 statement `when` suffix; entity references resolve as `move`'s do (`it` = clause owner, `the match` inside `each`). Orphaning is deliberately NOT this statement (no null `move`; parked-nowhere is not an author concept). Never-guess: `remove the player` is a load error (`analysis.remove-player` — the platform defines no post-removal player semantics; ADR-213 pins player removal as out of the `disappeared` channel's scope). | Chord's statement roster had no way to take an entity out of play (`move` requires a real destination, runtime.ts:937-944); every shipped disappearance was TS-side. ADR-213 (ACCEPTED 2026-07-13) built the platform signal at the `removeEntity` choke point; its Q3 minted the statement now, spelled `remove` to match the one platform API (Given 7 — one form; no `destroy` synonym). Ships with zoo-surfaces Phase 3. | `after buying it` ⏎ `  remove the last balloon` ⏎ `end after` — witnessed, the balloon's `phrase disappeared:` renders; gated: `remove the crumbs when the seagull is here` | Approved (David, 2026-07-14 — "transcribe", incl. the D7/D13 kit memberships and the remove-player load error; approval-in-principle ADR-213 Q3, 2026-07-13). |
| 2026-07-12 | **Z5 — strategy adverbs mirror the Choice selectors 1:1** (ADR-211 Q5/Decision 4): the adverb set becomes `cycling | stopping | randomly | sticky | first-time` → selectors `cycling | stopping | random | sticky | firstTime`. Semantics: cycling = in order, wraps; stopping = in order, stays on last; randomly = seeded random each render; sticky = seeded random once, locked forever (save-persistent); first-time = variant 1 on first render, variant 2 thereafter. RENAMES/REMOVALS: `ordered` and `once` retire as phrase-strategy adverbs — each a load error naming its replacement (`stopping` / `first-time`, ADR-211 AC-13); the RULE modifier `, once` (D5) is a different grammar site and still parses. Applies everywhere strategies are legal, including Z3 channel phrases. The loader's STRATEGY_SELECTOR table is rewritten to this table — the single implementation. | Q5: `ordered` didn't say it stops; `once` didn't say "first text once, then the second" and collided with the D5 rule modifier; mirroring removes the mapping table from the author's head. Zero story migration — the only shipped phrase-strategy adverb is `randomly`. | `define phrase mood, sticky` ⏎ `define phrase intro, first-time` — `define phrase hint, ordered` → load error naming `stopping` | Approved (David, 2026-07-12 — ADR-211 Q5). |
| 2026-07-14 | **F1 — `enterable` trait adjective**: a bare adjective in a `create` block composing the world-model `EnterableTrait` (default config, preposition `in`); the player may `enter` the thing. Composes onto any thing, including `container`/`supporter` (an enterable bench). **`enterable` is always explicit** — no kind is enterable by default and there is no per-kind enterability config (one adjective = one trait, no special cases). | ADR-218 §1a (child of ADR-214 parity umbrella): closes the `entering` action gap. `EnterableTrait` already existed in the world model but had no Chord surface. | `create the cage, container, enterable` | Approved (David, 2026-07-14 — ADR-218 W1 go-ahead; "always explicit" per the 2026-07-14 ADR-218 interview). |
| 2026-07-14 | **F2 — `climbable` trait adjective**: a bare adjective composing the world-model `ClimbableTrait` (default config); the player may `climb` the object. | ADR-218 §1a (child of ADR-214 parity umbrella): closes the object-`climbing` action gap. `ClimbableTrait` already existed in the world model but had no Chord surface. | `create the ladder, climbable` | Approved (David, 2026-07-14 — ADR-218 W1 go-ahead). |
| 2026-07-17 | **G1 — `drinkable` trait adjective**: a bare adjective composing the world-model `EdibleTrait` with `liquid = true` — the marker that routes the entity to the drinking action instead of eating. Composes order-independently with `edible` (either order keeps the liquid flag; `edible` alone stays solid food). | Chord go-live G1 mechanical shortlist (ADR-233; parity audit 2026-07-17): drinking was one of the 5 ❌ action gaps — no Chord path set `EDIBLE.liquid`, so no loaded entity could ever be drunk. | `create the potion` ⏎ `  drinkable` | Approved (David, 2026-07-17 — per-item shortlist sign-off, session 615882). |
| 2026-07-17 | **G2 — `concealed` marker adjective**: sets `IdentityTrait.concealed = true` on the composed entity — hidden from normal view until the searching action reveals it. A marker like `plural`: no trait of its own, no config. | Same shortlist: searching parsed and ran from Chord but its payoff (revealing concealed items) was unreachable — zero Chord surfaces set the flag. Closing it upgrades searching ⚠️ → ✅ with zero further platform work. | `create the key` ⏎ `  in the Shed` ⏎ `  concealed` | Approved (David, 2026-07-17 — per-item shortlist sign-off, session 615882). |
| 2026-07-17 | **G3 — `hiding-spot` trait adjective**: composes the world-model `ConcealmentTrait` (ADR-148). Bare form = the actor may hide at any position (`behind`, `under`, `on`, `inside`; quality `good`); optional `with position <word>` narrows to exactly one position, any other word = load error naming the four. Multi-position narrowing and quality config are future extensions, not guessed at. | Same shortlist: the hiding action's grammar half already shipped (`hide behind/under/in :target` supplies the position via semantics); the only missing piece was an adjective mapping to ConcealmentTrait. Closes a ❌ action gap. | `create the curtain` ⏎ `  hiding-spot with position behind` | Approved (David, 2026-07-17 — per-item shortlist sign-off, session 615882). |
| 2026-07-17 | **G4 — `openable` gains `with tool <entity>` config** (Defect D3 fix): the loader now reads the tool config and stamps `OpenableTrait.toolId` with the resolved WORLD id (shared pending-ref mechanism, cuttable precedent — never the raw name string). Previously the form compiled and was **silently ignored**. | Same shortlist, defect class "silent drop": the platform's opening tool gate (ADR-230 D3b) existed but Chord's config never reached it. | `create the crate` ⏎ `  openable with tool the crowbar` | Approved (David, 2026-07-17 — per-item shortlist sign-off, session 615882). |
| 2026-07-17 | **Shortlist platform closures (recorded here for the audit trail; no new Chord syntax)**: (1) Defect D1 — `pushable`/`pullable` loader cases added (catalog accepted both since Phase A; the loader threw a misleading "not a v1 adjective" `LoadError`, a compile↔load contract break). Default config: button-style push, lever-style pull. (2) Bare `cut`/`slice`/`chop :target` and `dig :target` player-command grammar added to parser-en-us (SHARPEE-GAP — untooled cuttables/diggables were unreachable for TS and Chord alike; tooled forms keep priority 110). (3) **`on turning it` is now live**: stdlib's turning action rewrote from the full-delegation factory to cutting's dual-surface shape (capability behavior XOR interceptor) with a `turningLifecycle` descriptor in the ADR-228 D5 registry — the dead-gerund gate derives from that registry, so the existing clause form simply stops erroring. No new grammar in any of the three. | Chord go-live G1 mechanical shortlist (ADR-233): every remaining mechanical item individually signed off; these three change platform surfaces, not the Chord grammar. | `on turning it` ⏎ `  phrase crank-groans` ⏎ `end on` — now loads and fires | Approved (David, 2026-07-17 — per-item shortlist sign-off, session 615882). |
| 2026-07-17 | **D5a — `starts <state>` initializer clause**: on a composition line, `starts` followed by one of the six accepted state words (`locked`, `unlocked`, `closed`, `open`, `off`, `on`) sets the paired trait's initial-value field (`isLocked`, `isOpen`, `isOn`). Reuses the `starts` word Chord already owns with one-token lookahead: `starts in <place>` stays placement; an unknown word after `starts` is its own parse error (`parse.starts-state`). The analyzer enforces pairing (`analysis.starts-state-pairing`): `starts locked`/`unlocked` require `lockable` composed on the same entity, `starts closed`/`open` require `openable`, `starts off`/`on` require `switchable` — mismatch is a load-time error. Table-driven (chord `STARTS_STATE_PAIRINGS`, loader field mapping) so future stateful traits extend the tables, not the code. The shadow-state ratchet survives intact: the state adjective is an *initializer* only, never stored story state — `locked`/`open`/`on` stay derivable. | ADR-231 D5: Chord had no way to declare initial trait state (a safe that starts locked, a lantern that starts on); per-trait config alternatives (`with locked true`) were rejected as non-generalizing — one surface for every stateful platform trait, present and future. Also the author's explicit escape hatch (`starts open`) that D5b's closed-by-default flip relies on. | `create the safe` ⏎ `  a container, openable, lockable with key the brass key, starts locked` | Approved (David, 2026-07-17 — ADR-231 D5a ruling IS the owner approval; PIN 5 pinned the diagnostic and the dispatch shape). |
| 2026-07-18 | **R1 — `region` kind noun (ADR-236 D1)**: `region` joins KIND_NOUNS; a region is a normal entity the loader builds onto the shipped platform seam (`world.createRegion` + `world.assignRoom` — `RoomTrait.regionId` is never set directly). A region block composes `aka` and a description like any entity; placement lines (`in`, `on`, `starts in`) on a region block are a load error (`analysis.region-placement`, mirror of ADR-234 D3's door-placement gate) — a region's "location" IS its member list. | ADR-236: regional simulation (weather over the outdoor rooms, a mansion clock) belongs to a named room group, not a hardcoded room-ID set; the ADR-149 platform half shipped with zero Chord surface (the elegance-parity signal was dungeo's hand-rolled `assignRooms`). | `create the Forest` ⏎ `  a region` ⏎ `  containing the Clearing, the Forest Path` | Approved (David — ADR-236 ACCEPTED 2026-07-17, session d2863f; D9 carries this entry). |
| 2026-07-18 | **R2 — `containing <name list>` region body line (ADR-236 D2/D3)**: the only membership form — the region names its rooms; no room-side `in the Forest` form exists (Given 7). Additive across multiple `containing` lines; Oxford `and` welcome. Members are rooms (membership via `assignRoom`) or regions (nesting: the member's `parentRegionId` is set; membership is transitive via the shipped ancestry walk — zero new grammar). Never-guess gates, each with its own diagnostic: unresolved member (`analysis.unknown-entity`), member neither room nor region (`analysis.region-member-kind`), double direct membership including an ancestor+descendant listing (`analysis.region-double-membership`), region with two parents (`analysis.region-two-parents`), memberless region — hard error, no warning tier (`analysis.region-memberless`), containment cycle (`analysis.region-cycle`), `containing` on a non-region block (`analysis.region-containing-host`). | Same ADR: David's ruling language is literally "a named list of rooms"; `regionId`/`parentRegionId` are single-valued, so every ambiguity is unanswerable — refused at compile, never guessed. | `containing the Mines, the Round Room` | Approved (David — ADR-236 ACCEPTED 2026-07-17, session d2863f; memberless-hard ruling same session). |
| 2026-07-18 | **R3 — crossing reactions on region blocks; `leaving` joins EVENT_VERBS (ADR-236 D6)**: region blocks are a new legal home for the existing reaction clause form — `after entering it` (existing verb, new host) and `after leaving it` (`leaving` = catalog growth, this row) bind to the region's shipped crossing events (`if.event.region_entered`/`region_exited`), fired per boundary actually crossed (`getRegionCrossings` is the source of truth — entering a nested child from inside the parent does NOT fire the parent's reaction; entering it from outside fires both). `while <condition>` composes as on any reaction clause. `leaving` exists ONLY on region blocks: on any other owner it is a load error naming the region-only rule, never a silently-dead binding. | ADR-236 D6, ruled both-sides in-gate, symmetric (David, 2026-07-17, session d2863f): going.ts has emitted the crossing events since ADR-149 with zero Chord consumers. | `after leaving it` ⏎ `  phrase open-sky-relief` ⏎ `end after` | Approved (David — ADR-236 ACCEPTED 2026-07-17, session d2863f; D9 carries this entry). |
| 2026-07-18 | **R4 — story-owned `on every turn` (ADR-236 D7)**: the existing `on every turn [while <cond>][, once]` clause becomes legal in the story header block's indented body, alongside the story-owned lines it already carries (`states`, `score … worth N`). Owner = the story itself: one scheduler daemon per clause with **no presence gate** (fires every turn wherever the player is — the background clock for the whole game); narration broadcasts (D11 satisfied trivially — the story is everywhere). `it` is NOT bound in a story clause — referencing `it`/`its` is the compile error `analysis.story-clause-it` (the analyzer's unbound-referent class; the case cannot arise in any other clause home since every other host has an owner entity). Any other clause form in the header is `parse.story-clause`; `after every turn` keeps its own existing error. | ADR-236 D7, ruled in-gate (David, 2026-07-17, session d2863f): story-as-owner is established precedent; no floating rule returns (Given 9 holds); closes the global half of the audit's "no story-global daemon surface" finding with behavior. | `story "…" by "…"` ⏎ `  on every turn while stormy` ⏎ `    phrase storm-howl` ⏎ `  end on` | Approved (David — ADR-236 ACCEPTED 2026-07-17, session d2863f; D9 carries this entry). |
| 2026-07-18 | **REMOVAL — `define behavior <name> from "<module>"` (ADR-235 D2)**: the CapabilityBehavior hatch is a parse error (`parse.removed-behavior-hatch`) with a fix-it naming the two live paths — author the behavior in-language (`define trait <name>` with `on <verb> it` clauses, composed on the entity) or ship a full action (`define action <name> from "<module>"`). The loader's `boundBehaviors` map and the `behavior` hatch kind are deleted. | Ruled by David (ADR-235 Q-1, 2026-07-17): the declaration carried no trait/action binding key, so a bound behavior structurally could never fire — a form that compiles but cannot work is the exact class the never-guess gates exist to kill. | `define behavior crowd-control from "./stunts.ts"` → parse error + fix-it | Approved (David — ADR-235 ACCEPTED 2026-07-17, session 615882; D2 is the ruling). |
| 2026-07-18 | **`use <extension>` header line + manifest-gated vocabulary (ADR-215)**: `use <name>` is a static line in the story header's indented body, one trusted platform extension per line (duplicate = `analysis.duplicate-use`; unknown name = `analysis.unknown-extension` at compile, LoadError at load; top-level placement gets a header fix-it). Each `use` (a) triggers the extension's runtime registration from the loader's trusted, runtime-bundled registry — no author code, so a `use`-only story stays pure IR — and (b) admits that extension's static vocabulary manifest (pure data in `packages/chord/src/manifests/`) into the composable catalog. A manifest adjective referenced without its `use` is `analysis.extension-not-used`; its `with`-fields are a closed, typed set (`analysis.extension-config-key`/`-value`). Manifest names and loader-registry names are pinned equal by the manifest-conformance test. | ADR-214 §5 audited the plugin/extension system as entirely TS-registered with zero Chord surface; ADR-215 designed `use` + static manifests (analyzer stays platform-free — it consumes data, never extension code); ADR-235 D3 ruled the full surface in the go-live gate. | `story "…" by "…"` ⏎ `  use combat` | Approved (David — ADR-215 ACCEPTED 2026-07-14, session ae2a61; gate slice S3 ruled ADR-235 Q-2, 2026-07-17). |
| 2026-07-18 | **Combat vocabulary — `combatant` / `weapon` trait adjectives with typed `with`-stats (ADR-215)**: under `use combat`, `combatant` and `weapon` compose on `create` lines using the EXISTING `with <field> <value> [and …]` data grammar (no new value grammar; ADR-215's comma-separated example is normalized to the existing `and` separator — one grammar across the language, Given 7). `combatant` fields: `health`/`max-health` (routed to the REQUIRED HealthTrait per ADR-226 — auto-attached), `skill`, `base-damage`, `armor`, `attack-power`, `defense`, `experience-value` (numbers), `hostile`, `can-retaliate`, `drops-inventory`, `is-undead` (`true`/`false`). `weapon` fields: `damage`, `skill-bonus` (numbers), `is-blessed`, `glows-near-danger` (`true`/`false`). Loading a `use combat` story registers `registerBasicCombat` (attacking interceptor + NPC combat resolver); a real attack resolves through it (AC-1). | Combat was the audit's only entirely-unreachable subsystem: the platform shipped `CombatantTrait`/`WeaponTrait`/`CombatService` with zero Chord path. The typed-fields-in-manifest pattern is the general form for non-boolean trait state in Chord. | `create the troll` ⏎ `  a person, combatant with health 20 and skill 40 and hostile true` | Approved (David — ADR-215 ACCEPTED 2026-07-14, session ae2a61; combat spelling resolved same session). |
| 2026-07-18 | **`[ … ]` config list values (ADR-215)**: a `with` field may take a bracketed, comma-separated list of entity names (`patrol route [the Gate, the Yard, the Shed]`), resolved to entity IDs at compile. Legal ONLY where an extension manifest declares a `list` field — on any other adjective it is `analysis.config-list-host`; an empty list is a parse error; unresolved entries hit the standard unknown-entity gate. Deliberately narrower than the ADR-216 statement-payload arrays (a separate grammar surface, landing with payloaded `emit`). Lexer grows `[`/`]` tokens. | The NPC behavior library's `patrol` route (and `allowed-rooms`/`forbidden-rooms`) need an ordered room list; a name list is the smallest form that expresses it. | `patrol with route [the Gate, the Yard, the Shed]` | Approved (David — ADR-215 ACCEPTED 2026-07-14, session ae2a61; NPC depth resolved same session). |
| 2026-07-18 | **CORE NPC behavior vocabulary — `guard`, `passive`, `wanderer`, `follower`, `patrol` (ADR-215 Q4)**: the standard NPC behavior library is always-available trait-adjective vocabulary — the NPC plugin auto-wires at load and there is NO `use npcs` (a `use npc` line is `analysis.extension-core`); this is the deliberate owner-ruled exception to one-`use`-per-extension. Params ride the `with`-data grammar: `wanderer with move-chance 50` (a Chord percentage, converted to the platform's 0-1 fraction by the loader), `follower with immediate true`, `patrol with route [ … ] and loop true and wait-turns 1`. NpcTrait fields compose on every behavior adjective: `can-move`/`announces-movement` (`true`/`false`), `allowed-rooms`/`forbidden-rooms` (lists). Movement behaviors default `can-move true`; factory behaviors register per-entity (`chord.npc.<id>`); custom per-turn logic remains `on every turn`. NOT surfaced: `NpcTrait.goals` — structured Goal objects have no config spelling; surfacing them is a future conversation, never a guess. | ADR-215 Q4 (owner call, 2026-07-14): NPCs are common enough to be core; the fully-built stdlib behavior library had zero Chord path. | `create the keeper` ⏎ `  a person, patrol with route [the Gate, the Yard]` | Approved (David — ADR-215 ACCEPTED 2026-07-14, session ae2a61). |
| 2026-07-18 | **`define machine … end machine` under `use state-machines` (ADR-215; spelling A ratified by David 2026-07-18)**: the ADR-119 depth — `role <name> is <entity>` bindings, `starts <state>`, `state <name>[, terminal]` blocks carrying `when <trigger>[ while <condition>]: <target>` transition lines and `on enter`/`on exit` statement bodies. Triggers: `<gerund> <entity/role name>` (action on a target — a declared role name lowers to the platform's `$role` binding), `event <dotted.key>`, a condition, or a single bare word (declared condition/story state wins, else an action gerund — the parser is vocabulary-free, the analyzer resolves). Machines are story-owned: `it` in a body is `analysis.story-clause-it`; `define machine` without the `use` is `analysis.extension-not-used`; unknown states/targets, duplicate machines/roles, and missing `starts`/`end machine` all gate. The EXISTING `states:`/`select`/`change` surface stays unconditional — this construct is additive depth, never a replacement. Lowering: platform id `chord.machine.<slug>`, Chord conditions ride as ADR-119 custom guards over the shared evaluator, bodies as custom effects through the runtime statement executor. Trigger gerunds match performed action ids at runtime (statically unvalidated — the action catalog is open). | ADR-215: the full ADR-119 depth (named machines, onEnter/onExit, terminal, `$role`) was TS-only; the gate slice ruling (ADR-235 Q-2, S3) put it in the go-live gate. Spelling chosen from three candidates (define-machine block / owner-nested / flat lines). | `define machine drawbridge` ⏎ `  role winch is the rusty winch` ⏎ `  starts raised` ⏎ `  state raised` ⏎ `    when turning the winch: lowering` ⏎ `end machine` | Approved (David — spelling A ratified 2026-07-18, session 501cac; ADR-215 ACCEPTED 2026-07-14 defers spellings to this row). |
| 2026-07-18 | **Payloaded `emit` (ADR-216)**: `emit <event> [with <field> <value> [and …]] [when <cond>]` — the load-bearing presentation primitive. Values: literals (numbers/quoted strings), value expressions (entity refs → world ids, `the player's location`-style live reads, `true`/`false` → booleans; unquoted words resolve as the language always resolves words — plain strings are QUOTED), `[ … ]` arrays, and `{ <field> <value>, … }` nested objects (lexer grows `{`/`}`; comma-separated inside brackets/braces, `and` at the flat level — the create-data separator, same normalization as the combat row; ADR-216's comma-at-top-level example normalized). Payload keys pass VERBATIM into `event.data`. Also fixes the latent dotted-event mangle: `emit media.sound.play` previously emitted the type `media . sound . play`; event segments are now dotted keys. The empty-payload emit is unchanged. | ADR-216: the runtime hard-coded `data: {}` — even `emit media.sound.play` could not carry its required `src`; every media/custom channel was unreachable. All Phase-5 media sugar lowers onto this. | `emit media.sound.play with src "chime.ogg" and channel "sfx"` ⏎ `emit media.image.show with hotspots [ { id "well", target the well } ]` | Approved (David — ADR-216 ACCEPTED 2026-07-14, session ae2a61; payload form resolved same session, separators normalized per Given 7). |
| 2026-07-18 | **Typed media sugar + declared assets (ADR-216)**: statements `play sound <asset>`, `play music <asset> [looping]`, `stop music`, `show image <asset> [in <layer>]`, `hide image`, `play ambient <asset>` / `stop ambient`, `transition <kind>`, `clear` — each lowers AT COMPILE TIME onto a payloaded `media.*` emit (no runtime surface of its own; `when` composes). Assets declare once and reference by name: `define sound|image|music <name> from "<file>"` — a DATA reference (static path), never a hatch: it does not set `hasHatches` and keeps the pure-IR profile (the discriminator is the `define` subject, per the ADR). Gates: unknown asset (`analysis.unknown-asset`, nearest-match suggestion), kind mismatch (`analysis.asset-kind`), duplicate name (`analysis.duplicate-asset`). `play ambient` plays SOUND assets (an ambient loop is a sound file). `media.image.preload` and anything exotic remain reachable via raw payloaded `emit`. | ADR-216: ergonomics over the Phase-4 primitive — a central typo-checked asset manifest, one place to change a path; the full media surface becomes authorable. | `define sound chime from "audio/chime.ogg"` ⏎ `play sound chime` ⏎ `show image map in background` | Approved (David — ADR-216 ACCEPTED 2026-07-14, session ae2a61; sugar + asset forms resolved same session). |
| 2026-07-18 | **`define channel … end channel` + `client has <capability>` (ADR-216; channel spelling A ratified by David 2026-07-18)**: a custom channel is a declarative JSON data projection — keyword body lines `mode replace|append|event`, optional `gated by <capability>`, `from event <event.key>`, `take <field>[, …]` — lowered to a real IOChannel whose produce takes the turn's last matching event and projects ONLY the taken fields; registered through the existing `Story.registerChannels` hook (re-declaring a standard channel id is the platform's documented story-override; duplicating a story channel is `analysis.duplicate-channel`). Renderers stay platform/extension territory: the trusted registry's `registerChannels` slot is live for `use`d extensions (no bundled extension registers one yet — structurally possible, unexercised). `client has <capability>` is a new condition form reading the LIVE negotiated client capabilities (new engine accessor `getClientCapabilities()` — the pre-flagged ADR-215/216 platform seam, one read-only getter); capability words are the closed platform flag set in Chord spelling (`sound`, `split-pane`, … — 15 flags incl. `layers`, which the ADR grounding wrongly said didn't exist), validated at compile (`analysis.unknown-capability`), lowered to camelCase keys; `client` is reserved in condition-subject position; without a negotiated client every gateable flag reads false (text-only default). | ADR-216: channels close the declarable half of the presentation surface; `client has` lets stories degrade deliberately instead of relying on silent gating. | `define channel compass` ⏎ `  mode replace` ⏎ `  gated by images` ⏎ `  from event chord.compass.updated` ⏎ `  take heading, target` ⏎ `end channel` — and `phrase roar-text when not client has sound` | Approved (David — channel spelling A ratified 2026-07-18, session 501cac; ADR-216 ACCEPTED 2026-07-14 defers the spelling to this row). |
| 2026-07-18 | **Player block composes like any entity (Gap-2 ruling)**: trait adjectives and `starts` states on `create the player` now land on the player entity through the shared loader path (`a person, combatant with health 30 and skill 10` composes CombatantTrait + HealthTrait) — previously they compiled and were SILENTLY DROPPED. Gates: only `a person` is a legal player kind noun (a no-op — the player is already an actor; anything else is `analysis.player-kind`); NPC behavior adjectives are refused (`analysis.player-behavior` — the player is not driven by the NPC service). No new syntax — a silent-drop defect class closed, recorded for the audit trail. Companion rulings same session: the flat `with` separator is ratified as `and` (ADR-215/216 comma examples were illustrative; commas stay the separator inside `[ ]`/`{ }`); the engine's `getClientCapabilities()` accessor is kept as-shipped; the extension renderer-registration leg stays unexercised until a bundled extension needs a novel renderer. | Surfaced during ADR-215 Phase 2 (the NPC guard test could not give the player combat traits from the story); ruled by David 2026-07-18 with the three companion dispositions. | `create the player` ⏎ `  a person, combatant with health 30 and skill 10` | Approved (David, 2026-07-18, session 501cac — four-gap ruling). |
| 2026-07-18 | **R2 — `through the <door>` exit-line tail (ADR-234 D1/D2)**: `<direction> to the <room> through the <door>` is the ONE door relationship form — direction explicit on the line, the reverse inferred as the opposite direction (`connectRooms`'s established convention), no matching line needed in the far room (a mirrored line is legal but must agree exactly: same pair, opposite direction, stated at most once). `through` references a DECLARED door and never creates one; `through` becomes a reserved word on exit-line destination names. The door block itself is pure entity declaration — kind, traits, description, phrases, clauses; placement lines on it are refused (its location IS its room pair; the loader places it in the declaring room). Gates: `parse.exit-through`, `analysis.door-through-kind`, `analysis.door-multi-pair`, `analysis.door-pair-mismatch`, `analysis.door-unconnected` (hard, no warning tier), `analysis.door-placement`. Loader leg: door composes SceneryTrait + OpenableTrait (closed; `starts open` overrides) and wires through `connectRooms(room1, room2, direction, doorId)` — the ADR-237 D4 one-wiring-path primitive. (`analysis.door-through-kind` is an implementation addition to the ADR's drafted gate list: `through` naming a non-door entity is refused at compile rather than surfacing as the loader's DoorTrait throw.) | ADR-234 D1 (R1 `between` struck by David 2026-07-17 before ever entering the grammar); D2 never-guess direction; D3 gates. | `north to the Hall through the oak door` | Approved (ADR-234 ACCEPTED 2026-07-17; row lands with the Phase 2 implementation, 2026-07-18). |
| 2026-07-18 | **R3 — keyless single-entity `with` config, uniform (ADR-234 D6; Q-1 ruling for lockable, extended to all single-entity configs by David 2026-07-17, session 615882; breaking migration confirmed at implementation 2026-07-18)**: an article directly after `with` (or after `and` between settings) starts the entity-name value for the adjective's single entity config — `lockable with the iron key`, `cuttable with the knife`, `diggable with the shovel`, `openable with the crowbar`. The old keyed forms (`with key X`, `with tool X`) are parse errors (`parse.removed-config-keyword`) with fix-its naming the new form (one form per concept, Given 7). Carve-outs, both keyword-kept: word-valued configs (`hiding-spot with position behind`, `readable with text …` — a bare word after `with` never triggers the rule) and authored-trait NAMED data fields (`feedable with food the handful of feed` — the keyword is the field name; key words precede the article, so first-position-article never fires). IR: the keyless value lowers to config key `''`, valueKind `name`; loader reads it via `entityConfigValue` for lockable keyId / openable+cuttable+diggable toolId. Breaking in-gate migration executed: all fixtures/tests (`lockable-key`, `cuttable`, `quickwin-adjectives`, `starts-state`, `door-actions`, `zoo-phase-c.story`) + `stories/friendly-zoo/zoo.story`; cloak had no keyed usage; zoo golden churn verified as exactly `"key": "key"` → `""` + span shrink. | ADR-234 D6/AC-6; Given 7 (one form per concept). | `lockable with the iron key` | Approved (ADR-234 ACCEPTED 2026-07-17; migration re-confirmed by David 2026-07-18, session d02586). |
| 2026-07-18 | **`define topics for <entity> … end topics` — the ask/tell topic table (ADR-239 D3 as amended / D4)**: a define-family block declaring the entity's closed, compile-visible table of topics + responses — any number of `about` rows in two tiers: `about the <entity>:` (entity tier — the platform's quiet `topicEntityId` resolution) and `about "<text>"[, "<text>" …]:` (free-text tier — comma-separated declared aliases; the COMMA separator was ruled by David 2026-07-18, superseding the ADR example's `or`). A response is a one-line statement or an indented statement body (`it` = the owner; the full statement kit, including the `phrase` declare-and-emit prose sugar, owner-scoped). One block per entity; the same table serves `ask` and `tell` alike (D1); matching is normalized whole-topic lookup — case-insensitive, leading article stripped, never fuzzy (D4); a miss falls to the owner's unfiltered `on asking it` catch-all, and a hit fully owns its response (D5) — NO on-clause grammar changes at all. Gates: `parse.topics-for`/`-row`/`-alias`/`-colon`/`-response`/`-empty`/`-end`; `analysis.duplicate-topic` (same entity or same normalized quoted text within a table, aliases included), `analysis.topic-entity-collision` (quoted entry vs the name/aka of an entity used in an entity-tier row of the same table — per-table, order-independent), `analysis.duplicate-topics-block` (second block for the same owner), `analysis.topics-host` (person-kind owners only). IR: rows lower onto the owner as `IREntity.topics` (additive — `topics: []` on every entity; runtime consumption is the workstream's Phase 2). | ADR-239: the parity audit's last two ⚠️ rows (asking/telling — "Chord has zero topic surface"); D4's never-guess table discipline (declared vocabulary, compile-gated overlaps, no runtime tie-breaks); the ISSUE-074 append-vs-override lesson decided at design time (D5). | `define topics for the porter` ⏎ `  about the sword: phrase sword-reply` ⏎ `  about "treasure", "the hoard": phrase treasure-reply` ⏎ `end topics` | Approved (David — ADR-239 ACCEPTED + D3 table-block amendment 2026-07-18, session d02586; comma alias separator ruled 2026-07-18, session 80ff54; row lands with the Phase 1 implementation). |
| 2026-07-18 | **`in <channel-word>` tail on the ambient forms (ADR-241 D3)**: `play ambient <asset> [in <word>]` and `stop ambient [in <word>]` — the word names an ambient BED; omitted, both forms mean the default bed `main` (`ambient:main`, mirroring `image:main` — Q-1), and the bare `stop ambient` stops the default bed ONLY (Q-2; a stop-all, if ever wanted, gets its own future spelling). Lowering always stamps `channel` onto the `media.ambient.play`/`stop` payload — the field stdlib's `createAmbientChannel` produce filters on, closing the Fernhill Phase 8 finding that `play ambient` compiled but was silently inaudible in the browser. Channel words beyond the implied set must be declared (companion row below) — `analysis.unknown-channel` with a nearest-match suggestion (Q-3, never-guess); the same gate covers `show image … in <layer>` words beyond the platform's pre-registered `background`/`main`/`overlay` (implied like the `main` bed). | ADR-241 (dynamic channels ruled by David 2026-07-18, session 1e7652); mirrors the shipped `show image … [in <layer>]` spelling — no second preposition. | `play ambient night-wind in wind` ⏎ `stop ambient in wind` | Approved (ADR-241 ACCEPTED 2026-07-18, session 1e7652; row lands with the Phase 1 implementation). |
| 2026-07-18 | **`define ambient <word>` / `define layer <word>` — named family channel declarations (ADR-241 D2, Q-4)**: one-liners beside the asset declarations, declaring an ambient bed / an image layer by its author word. The registered ids (`ambient:<word>`, `image:<word>`) are an implementation detail — the colon form is never author-facing; `define channel` stays a plain-word, full-bodied data projection (one construct, one meaning). Declared and implied-and-used family channels join `ir.channels` with the additive `family: 'data'|'ambient'|'layer'` discriminator (every pre-existing entry reads as `data`); the loader registers family entries through stdlib's `createAmbientChannel`/`createImageChannel` builders. Gates: `parse.channel-name` (missing name / trailing text — the declaration is one word), `analysis.duplicate-channel` (per-family namespace; the same word across families, or as a data-channel name, is legal — the ids differ). | ADR-241 D2 (Q-4 ruling: declarations say what the thing IS, matching the `define sound …` asset precedent, instead of namespaced ids in `define channel`). | `define ambient wind` ⏎ `define layer floorplan` | Approved (ADR-241 ACCEPTED 2026-07-18, session 1e7652; row lands with the Phase 1 implementation). |
| 2026-07-19 | **H1 — `proper` trait adjective (ADR-242 D1)**: person-only, unconditional — `a person, proper` marks the person proper-named; the loader constructs IdentityTrait `properName: true, article: ''` (the player's own shape), so the name renders bare in every context ("You can see Tobias here."). The first kind-scoped entry in TRAIT_ADJECTIVES — the analyzer gains a kind-scope check keyed to `proper` only (deliberately not generalized). Gates: `analysis.proper-person-only`, `analysis.proper-conditional` (identity is not turn state — the specific message, not the loader's generic conditional-composition error). The create-line article stays noise for identity (D4 — recorded divergence from Inform 7's inference rule: explicit spelling over reading intent out of `create the …` vs `create …`). | ADR-242 (the "a Tobias" quirk — the loader's `article: undefined` pin, flagged since ADR-237 Phase 1, closed by D2/D3). | `create Tobias` ⏎ `  a person, proper, patrol with route […]` | Approved (ADR-242 ACCEPTED 2026-07-19, session b381db; row lands with the Phase 1 implementation). |
| 2026-07-19 | **H2 — `pronouns <word>` person body line (ADR-242 D5)**: beside `aka`, one line naming the person's pronoun set — a standard set (`he`/`she`/`it`/`they`) or a `define pronouns` set name. Loader maps it to IdentityTrait.pronounSet; the phrase chain's pronoun atoms render the set's forms. ABSENT MEANS ABSENT (Q-2 ruling): no default is injected — an undeclared person keeps the platform's by-number fallback ("it" singular), uniform with things; pronouns are declared, never inferred. Gates: `analysis.unknown-pronouns` (nearest-match suggestion — the `analysis.unknown-channel` shape), `analysis.pronouns-duplicate`, `analysis.pronouns-person-only`, `parse.pronouns-word`. IR: additive optional `IREntity.pronouns` (present only when declared — zero golden churn beyond the empty-key baseline). | ADR-242 (the pronoun fold-in ruled by David: ADR-197's phrase-side seam was never fed from entities — every singular NPC rendered "it"). | `create Mrs Kettle` ⏎ `  a person, proper, guard` ⏎ `  pronouns she` | Approved (ADR-242 ACCEPTED 2026-07-19, session b381db; row lands with the Phase 1 implementation). |
| 2026-07-19 | **H3 — `define pronouns <name> … end pronouns` (ADR-242 D7, Q-1 ruling)**: a named pronoun set for non-standard gender identity — a define-family block with five NAMED rows (`subject`, `object`, `possessive`, `possessive-pronoun`, `reflexive`), order free, all required (named rows over an ordered list: a swapped pair is a compile error, not silently wrong text). Forms are DATA in the IR (`ir.pronounSets`, new manifest list — every IR golden gains `pronounSets: []`); the loader registers each set into the language provider through the `extendLanguage` seam, and the lang-en-us assembler consults the registry before its standard rows (locale text stays in the language layer — ADR-197's boundary). Gates: `analysis.pronoun-set-rows` (missing row, named), `analysis.pronoun-set-duplicate-row`, `analysis.pronoun-set-shadows` (standard four protected), `analysis.duplicate-pronoun-set`, `parse.pronouns-name`/`-row`/`-body`/`-end`. | ADR-242 D7 (Q-1 interview ruling: block with named rows — the `define channel` indented-body precedent). | `define pronouns ze` ⏎ `  subject ze` ⏎ `  object zir` ⏎ `  possessive zir` ⏎ `  possessive-pronoun zirs` ⏎ `  reflexive zirself` ⏎ `end pronouns` | Approved (ADR-242 ACCEPTED 2026-07-19, session b381db; row lands with the Phase 1 implementation). |
| 2026-07-20 | **`##` comment lines, between top-level constructs only, blank-delimited (ADR-249)**: a maximal run of consecutive indent-0 `##` lines, preceded AND followed by blank lines (file start/end count as blank), sitting between top-level constructs (or before the story header / after the last construct), is a comment — skipped at compile, contributing nothing. "Outside blocks" is STRUCTURAL: a construct spans its header through its last body line or `end` terminator, so a `##` line inside that span — even at indent 0 between a `create` header and its indented body — is an error (`parse.comment-inside-block`), never a comment ("create is a block — no comments in blocks"). A `##` run missing a blank delimiter is `lex.comment-blank-lines`. Multi-line comments are stacked `##` lines; there is NO block construct and NO end-of-line comment form on any line (David's simplification ruling, superseding the in-session `#` EOL + `##` whole-line drafts). Indented `##`/`#` have no comment meaning: prose renders them verbatim (opacity §5.2 untouched); code positions stay parse errors with the comment-specific fix-it. One concept, one form (Given 7). | David, 2026-07-20 (session 99aee6), raised as a prerequisite to the ADR-245 phrasebook companion: authors need file-level annotation; prose opacity rules out safe EOL/inside-block stripping, so the strict top-level blank-delimited form wins — mechanically checkable at lex time, self-evident in source. | `## The ghost's locket drives chapter 3.` ⏎ `## Everything below is the winter voice.` ⏎ (blank line) ⏎ `define phrasebook winter …` | Approved (David, 2026-07-20, session 99aee6 — "no EOL comments - only ## comments outside of all of our syntax blocks"; "create is a block - no comments in blocks"; "## blocks must be preceded and followed by blank lines"; "the exception would be top of the file can have ## as the first line"). Implemented 2026-07-20 same session (lexer flag + `lex.comment-blank-lines`; parser top-level skip + `parse.comment-inside-block`; chord 416 green; cloak golden twin modulo spans; fernhill annotated live, 483/483 transcripts). |
| 2026-07-21 | **Phrasebooks (ADR-245/ADR-250)** — three forms, one feature: (1) `define phrasebook <name> [while <condition>] … end phrasebook`, a named predicated phrase collection whose entries are the phrase-override grammar verbatim (`<key>[, strategy]:` + `or` variants, all five strategies, `verbatim`); entry-level `while` is `analysis.phrasebook-entry-gate` (the book's header predicate is the ONLY gate); entry keys are STORY keys only — dotted platform IDs are `analysis.phrasebook-dotted-key` (Q-2 re-confirmation; ADR-230 D5 story-level overrides stay the platform path); a predicate-less book is the DEFAULT (always) book. (2) `use phrasebook <name> [while <condition>]` in the story header — the `phrasebook` sub-word selects the two-word form (plain one-word `use <extension>` untouched); stackable; predicate binds at the use site; `analysis.unknown-phrasebook` against the compile-time manifest registry. (3) `import phrasebook "<file>"` at top level — splices a fragment of the author's own `define phrasebook` blocks at the import position (= arbitration position); `.story` extension required; fragments hold only phrasebook blocks + `##` comments; bare `import "<file>"` stays RESERVED for the parked generalized-import ADR; `TOP_KEYWORDS` gains `import`. Arbitration: first predicate-match in file-appearance order, per key; fallback chain per-entity → story `define phrase` → active book → platform default; variant counters per (book, key). | ADR-245 (ruled intent, David 2026-07-19/20) + ADR-250 design companion (ACCEPTED, David "proceed" 2026-07-21; Q-2 dotted keys ruled NOT legal — "we're not surfacing Sharpee constructs in Chord"; intent: easier phrase blocks + predicated authorial voice; `use phrasebook` retained; default-phrasebook naming). | `define phrasebook winter while midnight` ⏎ `  cold-returns, first-time:` ⏎ `    The cold finds you.` ⏎ `  or` ⏎ `    The cold again.` ⏎ `end phrasebook` | Approved (ADR-250 ACCEPTED 2026-07-21, session 99aee6). Implemented same day: chord 444 green (28 phrasebook tests), loader seam 9, engine read point 6, fernhill phrasebooks.transcript 18/18. |
| 2026-07-21 | **Generalized `import "<file>"` (ADR-251)** — folds ADR-250's typed `import phrasebook "<file>"` into a single bare `import "<file>"` at top level (`import` + a bare word / a missing string is `parse.import-form`; the `phrasebook` sub-word is removed). Fragment files use the `.chord` extension, ASSUMED and never spelled — the compiler appends `.chord` before calling the host `importResolver`, which stays a dumb name→text map (`import "voices/winter"` resolves `voices/winter.chord`). Splice-in-place before analysis IS the whole model (import site = arbitration position, D4): a fragment may hold ANY complete declaration — not just `define phrasebook` — except a `story` header (`analysis.import-fragment-story`) or a nested `import` (`analysis.import-fragment-nested`; imports are FLAT — no cycles by construction). A fragment that fails to parse raises `analysis.import-fragment-content`; a missing file is `analysis.import-unresolved` (main-file import-line span, while the `-fragment-*` family carry the fragment's OWN span, `[<name>.chord]`-prefixed). Ordering, collisions, and cross-file references all fall out of the paste model — no new rules. Hosts wired: devkit (`compose`/`build`/`build-browser`/`author-game`) fs resolver; browser compile-at-boot ships resolved fragments as one inline `dist/web/imports.json` bundle (David's shape ruling), omitted when a story has no imports. Supersedes ADR-250 D2's typed grammar + `.story` fragment extension. | ADR-251 (ACCEPTED, David — five decisions ruled directly 2026-07-21, session eb743f: story block main-file-only; complete declarations, no partials; `.chord` assumed; flat/no-nesting; single bare `import` folding phrasebook; compiler appends `.chord`, resolver dumb). Parked by ADR-245; shaped-to-generalize by ADR-250 D2. | `import "voices/winter"` | Approved (ADR-251 ACCEPTED 2026-07-21, session eb743f). Implemented same session: chord suite green (import.test.ts acceptance — worked example + five span-asserted rejection cases — plus the phrasebooks import block); devkit suite green (fs REAL-PATH `import-fs-resolver` + browser inline-bundle REAL-PATH `chord-import-browser`). |
