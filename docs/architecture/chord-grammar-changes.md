# Chord Grammar Changes — Owner-Approved Log

Governance log for the Chord story language grammar (ADR-210, Open Question 4).
The grammar is a closed set with one canonical form per concept (Given 7).
Any addition or change to the grammar beyond what `docs/work/story-language/design.md`
records as normative requires David's explicit approval, logged here as a dated
entry — a one-way ratchet: entries are never removed, only superseded by later
entries.

**What counts as a grammar change:** a new keyword, statement, selector form,
strategy adverb, kind noun, trait adjective, or any second way to express an
existing concept. Renames and removals count too.

**What does not:** new phrase keys, story content, loader internals, IR schema
changes that don't alter surface syntax, and diagnostics wording.

## Entries

| Date | Form (syntax) | Rationale | Example | Decision |
|------|---------------|-----------|---------|----------|
| 2026-07-10 | **Prose block is the only phrase-text form** — the same-line value in `define phrase` / `phrases <locale>` entries (both the quoted and the bare-text variants) is removed (parse error `parse.phrase-text-form` with a fix-it pointing at the block form). Quoted strings remain for non-prose atoms only: story header strings, hatch module paths. | Given 7 (one canonical form per concept): the quoted one-liner duplicated the prose block and the choice between them tracked line length, not meaning. Raised by David against cloak.story §3.1. | `stumble:` ⏎ `  Blundering around in the dark isn't a good idea!` | Approved (David, 2026-07-10). Supersedes design.md §3.1's quoted examples; §3.1 amended. |
| 2026-07-10 | **Blank line inside a prose block = paragraph break.** A blank line no longer terminates the block while the following line is still indented to the block's level; each paragraph maps to its own text block downstream. In `create` blocks, all consecutive bare paragraphs form the description. | Prose-first: authors type paragraphs the way prose works; the platform's text-block unit *is* the paragraph. | Two indented paragraphs under a room → two-paragraph description | Approved (David, 2026-07-10). |
| 2026-07-10 | **`{br}` built-in marker = hard line break** (flush stacking, no paragraph gap) inside phrase/description prose. `br` is reserved: the unbound-marker gate whitelists it; a story cannot declare a phrase or hatch named `br`. | Verse/signs/addresses need tight line stacking; markers are Chord's one inline-escape form, so this adds a concept, not a second form. | `Here lies the grue.{br}It could not see in daylight.` | Approved (David, 2026-07-10). |
| 2026-07-11 | **Open conditions as selections** — `define condition` (existing form, unchanged) now has two classified uses: a condition whose body never references `it` is CLOSED (a truth test, as today); one that references `it`/`its` is OPEN — a selection criterion over entities. New reference form: `any <condition-name>` in `when`-rule target position and in thing-positions (`has`/`holds`/`wears` objects); `each <condition-name>` reserved for iterate/cardinality positions when those constructs land. Never-guess gates: an open condition as a bare `while` test with no `it` in scope is a load error; `any <name>` naming a closed condition is a load error. | David proposed named selection criteria for `when` clauses (2026-07-11); extending `define condition` instead of adding a `define selection` declaration keeps one form per concept (Given 7) and reuses the closed condition kit as the criteria language — no new expression grammar. | `define condition small-animal: it is pettable and it is in the Petting Zoo` ⏎ `when the player pets any small-animal` | Approved (David, 2026-07-11 — "I'd extend define condition"). Condition names stay single hyphenated tokens (key namespace: phrases/flags/scores/conditions; prose multi-word names remain entity-only) — David, 2026-07-11. Implementation: reference-form analysis in Phase B phase 3, evaluation with the loader phases; `each` form deferred to the iterate/cardinality constructs. |
| 2026-07-10 | **Conditional blocked exits** — `<direction> is blocked while <condition>: <phrase-key>` in `create` room blocks; the refusal applies only while the condition holds (evaluated per movement attempt / recompute, like `dark while`). The unconditional form is unchanged. | Dungeo conversion sweep (docs/work/chord/dungeo-conversion.md §5.2): ~10 transformer-based movement gates and 8 refusal pseudo-actions collapse into this one declarative, load-checkable form. Approved for Phase B scope even though Zoo doesn't need it. | `east is blocked while the player has the basket: too-wide` | Approved (David, 2026-07-10). |
| 2026-07-11 | **Ownership-package removals (Phase C; givens 8+9).** Removed, each with a parse-error fix-it naming its replacement: top-level `when` rules (→ `on`/`after` clauses on the owner), top-level `once <cond>` rules (→ `, once` clause modifier), top-level `every N turns` rules (→ story-owned schedule forms; `define sequence` stays), `define flag` (→ states or derived conditions), the `flag` trait-field type (→ trait-declared states), `if`/`else`/`end if` (→ `must` guards, statement `when` suffix, `select`), `refuse when` with top-level `not` (→ `must`, gate `analysis.negated-requirement`), and top-level `define score X worth N` (→ owner-attached `score … worth N`; CP5). Unchanged-only-extended list in ratchet-drafts.md §Unchanged. Removals land only together with the replacement entries below (one package; both stories migrate inside Phase C). | Givens 8+9; DDD review; decisions 3/6/7/8. | `define flag after-hours starts false` → parse error with story-states fix-it | Approved (David, 2026-07-11 — Phase C P1 checkpoint, "Approve all as drafted"; CP5 = remove `define score`). |
| 2026-07-11 | **D1 — State adjectives**: `open`, `closed`, `locked`, `unlocked`, `on`, `off`, `worn`, `lit` join the condition kit as `is [not] <adj>` predicates, read live from world trait state (OpenableTrait.isOpen, LockableTrait.isLocked, SwitchableTrait.isOn, the worn relation, computed light). Same closed-catalog governance as capability adjectives. | Leg 1: derivable facts must be derivable or authors shadow them into flags (`gate-closed`). | `south is blocked while the staff gate is closed: staff-gate-blocked` | Approved (David, 2026-07-11). |
| 2026-07-11 | **D2 — Story states**: `states:` line in the story header; the story starts in the first declared state; `change the story to <state>`; bare state names remain valid condition refs. | Leg 2: story phases are a state machine, not flags. | `states: open, after-hours` | Approved (David, 2026-07-11). |
| 2026-07-11 | **D3 — `on`/`after` clause split**: `on <verb> it` intercepts (may `refuse`; its `phrase` is the primary message); `after <verb> it` reacts (`refuse` is a parse error inside `after`; its `phrase` appends). Terminators `end on`/`end after`. Mechanism (CP2, spike-confirmed 2026-07-11): standard verbs → interceptor postExecute/postReport append; dispatch verbs → after-clauses compile into the dispatch action's report phase (loader-internal; interceptor hooks empirically do NOT fire on the dispatch path; zero platform changes). | Decision 1: the §5.4 routing boundary drawn on the page. | `after entering it` ⏎ `  award visit` ⏎ `end after` | Approved (David, 2026-07-11 — CP2 loader-internal confirmed). |
| 2026-07-11 | **D4 — `states, reversible:`**: state sets are forward-only by default (backward `change` = load error `analysis.irreversible-state`); the single-word modifier `reversible` permits any declared-to-declared transition. | Decision 2: back-transitions are a defined capability. | `states, reversible: hungry, content` | Approved (David, 2026-07-11 — word `reversible` confirmed). |
| 2026-07-11 | **D5 — `, once` clause-header modifier**: gates a whole `on`/`after`/every-turn clause to one lifetime firing; `first time` body ordinals stay (different scopes, both canonical at their scope). | Decision 4. | `on every turn while after-hours, once` | Approved (David, 2026-07-11). |
| 2026-07-11 | **D6 — `must` requirements**: `<subject> must <predicate>: <phrase-key>` as a `define action` line and a body statement; failure ⇒ refusal with the key. Companion gate: `refuse when` with top-level `not` = load error `analysis.negated-requirement`, must-form fix-it. `refuse without`/`otherwise refuse` unchanged. | Decision 6 ("single closed"): one form per polarity. | `the actor must have its food: no-feed` | Approved (David, 2026-07-11). |
| 2026-07-11 | **D7 — statement `when` suffix**: `<statement> when <condition>` (legal on `award`/`phrase`/`emit`/`move`/`change`/`win`/`lose`) executes only if the condition holds; not a block form. Positionally distinct from select-arm `when <value>` (statement-final vs arm-header) — homonym noted. | Decision 7: the moment-conditional replacement for removed `if`; keyword freed by the when-rule removal. | `award farewell when the player can see it` | Approved (David, 2026-07-11). |
| 2026-07-11 | **D8 — trait-declared states**: `states[, reversible]: …` as a `define trait` line; every composer gets the set; resolution across the composer's full trait set (another composed trait may reference them — `restless` reads feedable's `hungry`); same state name from two composed traits = load error `analysis.state-collision` with rename fix-it. The `flag` field type is removed from trait data. | Decision 5: booleans gone at every scope. | `define trait feedable` ⏎ `  states, reversible: hungry, content` | Approved (David, 2026-07-11). |
| 2026-07-11 | **D9 — three-ring boolean-state gate** (pattern detection over every declared state set): ring 1 `analysis.boolean-state` — literal `true`/`false`/`yes`/`no`; ring 2 `analysis.shadow-state` — platform pairs (`open`/`closed`, `locked`/`unlocked`, `on`/`off`, `lit`/`unlit`, `worn`/`unworn`), fix-it: compose the owning trait; ring 3 `analysis.negated-state` — negation-shaped pairs (`not-`/`un-`/`non-`/`no-` prefixes, `-less`, shared-stem pairs), encouraging fix-it: "a state names what the thing IS, never the absence of another state" with the `fed`→`hungry`/`content` worked example. All errors. | Decision 8 + clarification: catch pos/neg states, encourage real states. | `states: fed, unfed` → ring-3 error | Approved (David, 2026-07-11). |
| 2026-07-11 | **D10 — sequence step anchors**: `when <owner> becomes <state>` joins `at turn N`/`N turns later` as a step header; the step arms on the transition; later relative steps chain from its firing; `at turn N` stays absolute from story start. | Decision 9: timelines chain off phases, not just the wall clock. | `when the story becomes after-hours` | Approved (David, 2026-07-11). |
| 2026-07-11 | **D11 — owner-scoped narration** (semantics entry, no new syntax): story-owned schedules broadcast; entity-owned every-turn clauses fire only when the player is in the owner's location (presence, not sight); `, once` cannot be consumed unwitnessed; RNG conditions in entity clauses roll only on stage. | Decision 10: a phrase's reach follows its owner. | goats' confession fires when the player arrives | Approved (David, 2026-07-11). |
| 2026-07-11 | **D12 — owner-attached scores**: `score <name> worth N` as a line in `create` blocks (any entity), `define trait` blocks, `define action` blocks, and the story header; identities are owner-scoped (goats' `fed` ≠ rabbits' `fed`); max score = load-time sum; `award <name>` in an owner's clauses resolves to the owner's score. Top-level `define score` is removed (CP5). | Decision 3: the owner provides the namespace. | `create the zoo map` ⏎ `  score collected worth 5` | Approved (David, 2026-07-11 — CP5 remove). |
| 2026-07-11 | **D13 — sequence-scope mutation rule**: sequences (story-owned) may mutate world state — `change` on the story or any named entity, and `move` — plus `phrase`/`award`/`emit`/`win`/`lose`; they bind no `it`. Supersedes the proposal's narrower leg-2 recommendation. | CP6: the approved sketch is the spec where the two disagreed. | `change the pygmy goats to hungry` from `define sequence feeding time` | Approved (David, 2026-07-11 — CP6 confirmed). |
| 2026-07-11 | **Given 9 adopted — stickiness: all behaviors belong to "something."** Floating rule forms (top-level `when`, `once`, `every`) are slated for replacement by owner-attached clauses (on-clauses on entities; schedules/states on the story object); orphan global phrase keys migrate to per-entity phrases. Like the Given 8 entry below, this records the decision — the replacement surfaces (story object, clause modifiers, reaction-vs-interception marking) are the Phase C design package (docs/work/chord/phase-c-ownership-proposal.md); removals land together with replacements. Type definitions (`define trait`/`define action`) stay top-level: they declare kinds, not instances of behavior. | Phase B DDD review + David's principle statement (2026-07-11): "all behaviors belong to 'something'. Stickiness is a foundational aspect of DDD." Chord shipped a sticky system and an unsticky system in parallel; every review smell lives in the unsticky half. | `when the player enters the Aviary … end when` → on the Aviary: `on entering it …` | Approved as Given 9 (David, 2026-07-11 — "Promote it"). Implementation deferred to Phase C design. |
| 2026-07-11 | **`define flag` removed — global true/false flags forbidden (Given 8).** The declaration, `set <flag> to true/false`, and bare flag refs in conditions leave the grammar. Facts are either derived (conditions over world state) or owned (data/`states:` on an object). Replacement surfaces are design work before removal lands: state adjectives in the condition kit (`is open/closed/locked/on` — kills the `gate-closed` shadow-flag class) and story-scoped state (the "story object" direction, under exploration) for phases like `after-hours`. Zoo migrates when those exist; removal + replacements ship together so no story is stranded. | Phase B DDD review (docs/work/chord/ddd-review.md §3.1a): every global boolean shipped in Zoo was a disguised state machine — a shadow copy of entity state, a flattened story phase, or a lifecycle-less period. | `define flag after-hours starts false` → (removed; future: `states:` on the story) | Approved as Given 8 (David, 2026-07-11 — "global true/false flags are forbidden"). Implementation deferred to the replacement-design phase; this entry records the decision, not a landed grammar. |
| 2026-07-10 | **`verbatim` phrase modifier** — rides the `define phrase <name>, <modifier>` comma slot beside the strategy adverbs (mutually exclusive with them); the phrase's text is exempt from whitespace collapse (maps to phrase-algebra `whitespace: 'verbatim'`). | Preformatted text (ASCII maps, letters) cannot survive prose collapse; the modifier slot already exists. | `define phrase treasure-map, verbatim` | Approved (David, 2026-07-10). |
| 2026-07-12 | **D6 extension — `refuse when <condition>: <phrase-key>` as a body statement** (clause/trait/action bodies), same form as the define-action line in a new position; parse error inside `after` clauses (reactions cannot refuse, per D3); the D6 negated-requirement gate applies in body position too. | Removing `if` (ownership-package removals, 2026-07-11) left body-level prohibitions inexpressible — the carrying-limit case. Shipped in Phase C P2 as an implementation finding; ratified here. | `refuse when the taker holds the flimsy basket: hands-full` | Approved (David, 2026-07-12 — ratified as part of D6's as-shipped state). |
| 2026-07-12 | **Phase C ownership package — as-shipped reconciliation** (no new syntax; final state of the 2026-07-11 entries). All removals + D1–D13 landed as approved across Phases 2–5; both shipping stories (cloak, zoo) migrated; Cloak gate 81/81, Zoo gate 79/79. As-shipped notes: (1) event-verb register is the gerund — `after entering it` (D3's `<verb> it` clause form); the catalog still holds exactly one event verb, `entering`. (2) The `any <condition>` reference form (open-conditions entry, 2026-07-11) is DORMANT — its only host position (when-rule targets) was removed by this package; it revives with the `each`/iterate backlog. (3) Duplicate same-trigger clauses on one owner: load error keyed strictly for interceptor/dispatch-routed verbs (keyed registration = real masking), and by span-free condition fingerprint for event-verb clauses (condition-differentiated clauses are legal; identical conditions error) — approved by David 2026-07-11 during P3. (4) `select on` arm checking stays arm-validity-only, extended over trait scopes; a missing-arm completeness gate was not requested and would need its own entry. | Phase C P6 close-out: the log's Phase 1 drafts flipped to their final as-shipped state, deviations dated per the append-only discipline. | — | Recorded (Phase C P6, 2026-07-12); underlying approvals dated 2026-07-11/12 above. |
