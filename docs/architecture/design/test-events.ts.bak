/// <reference types="node" />

import { EventEmitter } from 'events';

// Test if EventEmitter is available
const emitter = new EventEmitter();
console.log('EventEmitter test:', typeof emitter.emit);
