# Clarity review — Chapter 29: Transcript Testing & Walkthroughs

Flags: 1

### 1. "Asserting more than text" — closing two sentences — restates-next
WHY: "State assertions are worth reaching for" is an empty lead that the next sentence then justifies; promote the concrete reason and drop the throat-clearing. The substance ("a message can read correctly while the world is wrong") is the real point.
OLD: State assertions are worth reaching for. A message can read correctly while the world
behind it is wrong; asserting on state catches that.
NEW: A message can read correctly while the world behind it is wrong (a score that never incremented, an item that was never consumed), and only a state assertion catches that.
