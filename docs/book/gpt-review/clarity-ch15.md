# Clarity review — Chapter 15: Capability Dispatch

No clarity issues found.

The chapter earns its qualifiers. "One verb, many behaviors, and the entity decides which one runs" is the actual definition, not hand-waving. The "mistake everyone makes once" and "Worth knowing" callouts both state a concrete fact (one behavior per trait+capability; the `createCapabilityDispatchAction()` factory exists). The trace diagram ("How it fits together") shows the real call path rather than asserting it works. "One verb, the entity decides" is a deliberate refrain of the thesis, not an empty restatement of an adjacent sentence. Left intact.
