# Clarity review — Chapter 18: The Language Layer

Flag count: 0

No clarity issues found. Each section says what and why: the message-ID
indirection is explained with a concrete event payload, `addMessage` is shown,
parameters are tied to a worked `{target}` substitution, and the "Why route
everything through IDs" list gives four substantive payoffs (translation,
restyling, consistency, overrides) rather than hand-waving. The lead sentence in
that section ("The indirection buys real things...") promises the list that
follows rather than restating it.
