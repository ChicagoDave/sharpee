# Rendered book outputs — regenerated by scripts/build-book.sh
build/
web/

# Full-resolution art masters (local source; public-domain, re-downloadable from
# metmuseum.org). The build downscales these into the committed art/*.jpg that
# the book references — see optimize_art() in scripts/build-book.sh.
art/_masters/
