# Work Summary — Sharpee Book Execution (Continuation)

**Date:** 2026-06-22 — second run of the day (approx. 22:18 local)
**Repo:** `test-book` (not a standard DevArch repo — summary placed at repo root)
**Prior summary:** `WORK-SUMMARY-2026-06-22.md` — covers session 1 through Ch 20/21 stop-points
**Author run by:** david.cornelson@gmail.com

---

## Objective

Pick up where the first session stopped: both Ch 20 (NPC `onPlayerEnters`/`emote` greeting) and
Ch 21 (scene transition events never firing) were confirmed platform defects. This session's job
was to pull the upstream platform fixes, upgrade the installed packages, re-verify Ch 20 and Ch 21
against 1.1.1, and establish a local git repo for rollback safety before advancing to Ch 22+.

---

## What Was Accomplished

### 1. `cleanup.sh` — uninstall guard for `@sharpee/devkit`

Added `npm uninstall -g @sharpee/devkit` to `cleanup.sh`, guarded by an install-check so it only
runs when the package is actually present. Previously `cleanup.sh` could leave the global CLI
installed between runs.

### 2. Pulled platform fixes from `../sharpee` monorepo

Ran `git pull` in `../sharpee` (commit range 293c1a9e..f5b26ea8). The two stop-point fixes landed:

- **Ch 20 (NPC):** `plugin-npc/src/npc-plugin.ts` — fixed the NPC service so `onPlayerEnters`
  invocations correctly emit and render `emote`-type `NpcAction`s.
- **Ch 21 (Scenes):** `scene-evaluation-plugin.ts` rewritten; Chapter 21 of the book rewritten;
  **ADR-186** ("lifecycle transitions: first-class callbacks") adopted — the new API passes
  `onBegin`/`onEnd` callbacks directly to `createScene` rather than wiring them through
  `world.registerEventHandler`.

Built the updated book PDF via `scripts/build-book.sh pdf` inside the monorepo and copied it
into `test-book/`. The `../sharpee` monorepo is used **only** as the book-PDF source; it is
never a package dependency of `my-zoo`.

### 3. Platform upgrade 1.1.0 → 1.1.1

- Reinstalled `@sharpee/devkit` globally: 1.1.0 → 1.1.1.
- In `my-zoo`: deleted `node_modules/` and `package-lock.json` so `npm install` re-resolved
  `^1.0.0` cleanly. All `@sharpee/*` packages now at **1.1.1**.

### 4. Re-extracted book text

`pdftotext` unavailable in this sandbox. Used PyMuPDF to re-extract the rebuilt PDF to
`book-text.txt`. This is a working file only; it replaces the previous extraction.

### 5. Re-transcribed Ch 20 and Ch 21 from the current book

**Ch 20** — book text unchanged between editions. The existing `my-zoo/src/index.ts` code is
byte-identical to the book; kept as-is.

**Ch 21** — chapter was fully rewritten in the new edition. Applied the following changes to
`my-zoo/src/index.ts`:

- Replaced the old `createScene(…)` call signature with the new first-class-callback form.
- **Deleted** both `world.registerEventHandler('if.event.scene_began', …)` and
  `world.registerEventHandler('if.event.scene_ended', …)` blocks — those APIs are gone from
  the new chapter.
- Removed a stale `// NOTE:` editorial comment that no longer applied.
- The replacement code uses `onBegin` and `onEnd` options passed to `createScene`.
- Compiles clean against 1.1.1.

### 6. Built and verified both chapters via the Playwright harness

Ran `sharpee build` then `run.sh`. Both prior defects are **resolved on 1.1.1**:

**Ch 20 — RESOLVED:**
- The parrot's `onPlayerEnters` `emote` greeting ("The parrot ruffles its feathers and eyes you
  with interest.") now renders on every Aviary entry. On 1.1.0 it never appeared.

**Ch 21 — RESOLVED:**
- Scene `onBegin` fires on entering the Petting Zoo: "A waft of hay and warm fur greets you."
- Scene `onEnd` fires on exiting: "The animal sounds fade behind you."
- Both confirmed `recurring: true` across two round-trips through the Petting Zoo entrance.

**Minor observation (not a defect):** §21.2 imports `SceneTrait`, but the zoo's only scene
never uses it directly — `SceneTrait` is needed solely for the illustrative §21.5 "storm" example.
Per the Ch 16 pattern-only precedent (custom behaviors shown but not added to the playable zoo),
that example was not added. The import was left out of `my-zoo/src/index.ts`.

### 7. Updated `EXECUTION-LOG.md`

Added a re-verification header (2026-06-22 second run, 1.1.1) and ✅ RESOLVED notes to the Ch 20
and Ch 21 entries.

### 8. Initialized a local git repo

```
git init -b main          # inside /home/node/repos/test-book
git config user.name "David Cornelson"
git config user.email "david.cornelson@gmail.com"
```

Created `.gitignore` excluding: `node_modules/`, `dist/`, `*.sharpee`, `*.tsbuildinfo`, `.claude/`.

Initial commit: **`8125b3b`** — "Ch 21 verified on 1.1.1 — both NPC and scene stop-points resolved"

No remote. Purpose: local rollback points as Ch 22–32 are worked through.

**Known gotcha:** `cleanup.sh` currently keeps only the PDF, itself, and `.claude/` — it would
delete `.git` if run. If cleanup is ever needed between chapters, either guard `.git` in the
script or run `git stash` / branch first.

---

## Status

**COMPLETE** — Ch 20 and Ch 21 both verified RESOLVED on platform 1.1.1. State is committed
locally at `8125b3b`. Ready to advance to Ch 22.

---

## Files Modified This Session

- `cleanup.sh` — added guarded `npm uninstall -g @sharpee/devkit`
- `my-zoo/src/index.ts` — Ch 21 scene code replaced (old `registerEventHandler` blocks removed;
  new `onBegin`/`onEnd` callbacks added to `createScene`)
- `book-text.txt` — re-extracted from rebuilt PDF (working file, not committed)
- `EXECUTION-LOG.md` — re-verification header + RESOLVED notes for Ch 20 and Ch 21
- `.gitignore` — new file (created this session)

---

## Next Step

Resume forward from **Chapter 22 — Turns, Timed Events & Daemons** and continue through Ch 22–32,
committing locally after each chapter passes its "Try it" verification.

Chapters remaining: 22 (Timed Events/Daemons), 23 (Scoring), 24 (Channels), 25 (Web Client),
26 (Theming), 27 (Media), 28 (Multi-file story), 29 (Transcript testing), 30 (Saving), 31
(Publishing), 32 (Zifmia).

---

## Notes

- The `../sharpee` monorepo is a book-PDF source only; `my-zoo` always uses published npm packages.
- PyMuPDF (`fitz`) was the only available PDF extractor in this sandbox; `pdftotext` is absent.
- ADR-186 in the platform repo is the authoritative record of the `onBegin`/`onEnd` API change
  that resolved Ch 21.
- No DevArch plan, no `docs/context/` directory. This project uses root-level `WORK-SUMMARY-*.md`
  files and `EXECUTION-LOG.md` as its session record.
