# The Language Layer — Messages & Message IDs

> **Status:** to be written (new chapter). Adapts `lang-en-us` + `event-handlers`
> guides.

This chapter will cover where all user-facing text lives: message IDs, the
language layer, and how stories override or add messages.
