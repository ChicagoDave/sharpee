# Capability Dispatch

> **Status:** pending Phase 2 migration from `v14`. The migration pattern is
> established in *Rooms & Navigation* (Part II).

Same verb, different behavior per entity — traits declaring capabilities and the
behaviors that handle them (ADR-090).
