# Light & Dark

> **Status:** pending Phase 2 migration from `v08`. The migration pattern is
> established in *Rooms & Navigation* (this part).

Dark rooms (`isDark: true`) and the light sources that let the player see
(`SwitchableTrait` + `LightSourceTrait`).
