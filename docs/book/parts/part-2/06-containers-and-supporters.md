# Containers & Supporters

> **Status:** pending Phase 2 migration from `v05`. The migration pattern is
> established in *Rooms & Navigation* (this part).

Holding things inside (`ContainerTrait`) and on top (`SupporterTrait`), with
capacity limits.
