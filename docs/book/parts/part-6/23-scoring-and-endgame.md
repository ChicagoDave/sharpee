# Scoring & Endgame

> **Status:** pending Phase 2 migration from `v16`. The migration pattern is
> established in *Rooms & Navigation* (Part II).

The score ledger with idempotent awards (`world.awardScore()`,
`setMaxScore()`) and a victory condition.
